# `DRAFT-D: none` · V0 · 那张「已清空」的表是假的 · 逐份登记(第二轮)

> **DRAFT-D: none** —— 清账记录,不是裁定,**有意不取 D 号**。
> 第 0 条车道 · 2026-08-11 · 提交 `1186f7e` + `a7fb17f`

---

## 1. ★★★ 本轮最贵的一条:一个刚兑现过的承诺,是最不容易被复查的东西

前一轮把「该取号而没取」从 **17 清到 0**,并写进注释:
「『只许变短』这条纪律**真的被兑现过一次**」。

**那个 0 是假的。真实数是 8。**

反向扫(不问「有没有 `D?`」,改问「抬头里有没有**任何**自陈未定的形状」)捞出
**8 份**自 2026-08-03/04 起就躺在 main 里、抬头逐字写着「**(决议草案 · 取号待定)**」的包。
**此前任何一版判据都没看见过它们。** 表没有变短 —— 是**取样口径把它们排除在外**。

> ★ **一张假的欠债表比没有更坏**:没有表的人知道自己不知道;
> 而看着一张写着 0 的表的人,**以为自己知道**。
> ★★ 它比零命中伪装得更好:**不是空表,是一个刚刚兑现过承诺的数字**。

---

## 2. 逐份 · 本轮改了什么

| # | 对象 | 处置 |
|---|---|---|
| 1 | `admin-app-split-2026-08-07.md` | 补 `DRAFT-D: none` 机器锚点(理由本来就写清了,**只是机器读不懂**) |
| 2 | `p3c-signoff-2026-08-04.md` §5.3 | 划掉 + 时态更正:它说的「worktree-split 仍待取号」**在写下的当天就了结了**(D82,08-04),多挂七天 |
| 3 | `HERMES_INTEGRATION_DESIGN.md:602` | 划掉 + 更正。★ 前一轮称"三处全改完",实际改的是 `:622`;**真正读起来像结论的那句在 `:602`** |
| 4 | `STATE.md` P6 那格 | 补全路径 `gpu_broker.py` → `10-core/gateway/gpu_broker.py`;钉住「11 是对的,别去改准它」 |
| 5 | `check_decision_numbers.py` | **两次扩宽 + 冻表**,四个方向红测(见 §4) |

---

## 3. ★ 抽查上一轮的结果 —— 它大体是真的,而**我两次差点给它定错罪**

- **A 类抽 4 份**(D128 · D116 · D122 · D112):号在 DECISIONS 里**都真有抬头** ✓
- **C 类 6 份**豁免行**都写清了谁哪天裁的** ✓

★★ 我自己的两次错,**都是取样不当**:
1. 用 `head -1` 只抓到标题行,没看到**第 3 行**的豁免说明 ⇒ 差点判「没写谁裁的」;
2. 数 `agent_task` 得 12,判协调层写的 11 是错的 ⇒ 多出的那处在 **`.pyc`**,**11 才是对的**。

⇒ 与本轮 §1 那条是同一个病:**结论对不对,取决于取样口径对不对** ——
而两次错误取样给出的都是**看起来完全正常的数字**。

---

## 4. 闸的四个方向红测(缺任何一个都区分不开)

| 方向 | 做法 | 结果 |
|---|---|---|
| A | 拿掉 `admin-app-split` 的锚点 | 红 · 点名它 ⇒ **扩宽真的抓得到中文形状** |
| B | 去掉 `p3c` 那行的划掉记号 | 红 · 点名 p3c ⇒ **`_STRIKE` 确实是挡住它的那个东西** |
| C | 把「取号待定\|决议草案」从判据拿掉 | 数回落 **0** ⇒ 那 8 份确实是这两个词捞出的 |
| D | 还原 | 复绿 / 复现 **8 / 25** |

★ 少了 A/B 其一,就区分不开「挡住了」与「整条扩宽根本没跑」—— **而两者都是绿的**。

---

## 5. ★ 未做的 —— 逐条,别当成已处理

1. **那 8 份一份都没处置**,只是**第一次被看见并冻进表**。
   它们**与前一轮那 17 份性质不同**:17 份是「已清掉的记录」,
   这 8 份是**真·还欠着** —— 合表只为让闸不恒红(D117),**不表示被处理过**。
   逐份待办:`client-version-visibility` · `identity-elevation-guard` ·
   `integrity-guard-asks-wrong-question` · `one-key-pairing` · `pairing-audit-core-items` ·
   `pairing-ux-final-spec` · `selfpair-review` · `zero-terminal-onboarding`
   (三种处置照旧:指向已有 D 号 / 取号 / 写 `DRAFT-D:` 并写清谁哪天裁的)。

2. **`admin-stack-lifecycle-2026-08-09.md` 抬头写「(D115 草案)」而 D115 今天已落号**
   (实测 DECISIONS 里有它的编号抬头)⇒ **抬头过期,该改成指向 D115**。
   本轮**没改** —— 上下文用尽。★ 它**没被闸捞出**:判据认「决议草案」,而它写的是「D115 草案」,
   **又是一个取样口径漏掉的形状**。

3. **存量欠债那 14 个号仍是 14**,一个没动。理由见
   [`d-number-table-cleanup-2026-08-11.md`](d-number-table-cleanup-2026-08-11.md) §3.1
   (那里已带时态更正过一次「解法写错了」)。

4. **反向扫还剩几种形状没逐个核**:「建议」×2 · 「待用户重」×1 · 「未被续过」×1 ·
   「未决项」×1 · 「待决」×1 · 「待裁」×1。★ 后两种已知是 C 类已豁免的包;
   **前四种没核过**,如实留着。

5. **本轮没跑 `-Full`、没出包、一行产品代码没碰**(V36/V37 在动它们)。
