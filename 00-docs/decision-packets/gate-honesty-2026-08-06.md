# 门禁与产物的诚实性 —— V3 车道交回(2026-08-06)

> 车道:**V3 · 门禁与产物的诚实性**(D92 垂直切片)
> 拥有:`90-ops/**` · `config/**` · `.githooks/**` · 本文件
> 禁区:`10-core/**` 与 `20-client-win/**` **一律只读** —— 下面第 4 节全部是**只报告不修**
> D 号:草案写 `D?`,并入那刻取号(D82 五条里唯一被完整遵守的一条)

★ **这条车道守的是门禁自己的诚实性。** 08-06 审计里有 4 处坏在门禁与断言自己身上。
**守卫必须待在它所检查的范围之外** —— 所以本车道新写的东西全部落在 `90-ops/`,
它检查的 `10-core` 与 `20-client-win` 一个字节都没动。

---

## 0. 一句话账

| | 之前 | 之后 |
|---|---|---|
| 反向全表盖住几处 | **1** 处(Python 测试文件 → `$RULES`) | **4** 处(+ dotnet 入口 · verify 脚本 · 跨进程契约) |
| 跨进程响应契约 | 无人枚举 | **27 条**,已成对 **4**,欠 **23**(逐条登记,只许变短) |
| lan-edge 断言 | 门禁跑 20 条 | 跑 **44** 条(补上 client-e2e 9 + admin-e2e 15) |
| 795 行 `verify-*.ps1` | 没人跑,**也没被记成没跑** | 反向全表登记 + `-VerifyEnv` 一键跑 + 跑没跑都进覆盖账 |
| 扫描器看得见的格式 | `.py` `.cs` | + `.ps1` `.json` `.toml`,且**能扫自己** |
| 产物落后于源码 | 靠人记得出包 | `doctor.py` ⑨ **比提交号**判红 + 覆盖账每次都印 |
| fast 层断言 | 1203 | **1470**(含并入 GPU 车道之后) |
| worktree 里的 `-Full` | **结构上恒红** | 通过,且如实记「没跑客户端自检」 |

**每一条护栏都有一次「看见它红」的记录**(第 5 节 20 条红测)。
没红过的护栏和没有护栏是一回事。

---

## 1. ★ 请第 0 条车道并入的三件事

### 1.1 STATE.md 那条「待复核」可以结掉 —— **它是一次错判**

`STATE.md:91-96` 现在写着:

> 旧口径行写的是 … `lan-edge` 8+6+15 · transport 5,与今天实跑对不上。
> 不是把旧数改成新数就完事:`lan-edge` 旧行三段相加 = 29,而门禁只打印一个 20 ——
> **两者不可能同时对**。已按"门禁实际打印的"记账,**旧行的来历待复核**;
> 在复核之前谁都不许拿这两组数去推"新增了多少条"。

**复核结论:两个数都对,量的不是同一个东西。「不可能同时对」是错判。**

`10-core\lan-edge` 有**三个**测试入口。本车道逐个实跑(2026-08-06):

| 入口 | 实测 | 门禁此前 |
|---|---|---|
| `selftest` | 20 PASS · 0 FAIL | ✅ 跑 |
| `client-e2e` | 9 PASS · 0 FAIL | ❌ **从没跑过** |
| `admin-e2e` | 15 PASS · 0 FAIL | ❌ **从没跑过** |
| 合计 | **44** | 只有 20 |

★ 旧行 `8+6+15` 是**按入口分段**记的(三段),新行 `20` 是**门禁实际打印的那一段**。
两者是"整个工程有多少条"与"门禁跑了多少条"的差别 —— **各自都对**。
旁证很硬:旧行第三段 `15` 与今天的 `admin-e2e` **一模一样**;
另外两段 `8`/`6` 是 `selftest`/`client-e2e` 当时的值,后来长到了 `20`/`9`。

⇒ **真正的病不在记账,在门禁只跑三分之一而覆盖账不说。** 那半已经修掉(第 2.1 节)。

**建议改成(草稿,交第 0 条车道逐字并入):**

```markdown
> 回归口径(2026-08-06 · 门禁实际打印):identity 11+15+42+14+57 · lan-edge **44** · transport 58
> ★ lan-edge 是**三个入口**之和:selftest 20 + client-e2e 9 + admin-e2e 15。
>   旧行 `8+6+15` 与新行 `20` **都对** —— 旧行按入口分段,新行是当时门禁唯一跑的那一段。
>   此前那句「两者不可能同时对」是**错判**,已复核结清(见 decision-packets/gate-honesty-2026-08-06.md)。
>   ★ 真正的问题不是记账:门禁此前只跑第一个入口,另外 24 条**从没跑过、也没被记成没跑**。
>     已于 D? 修掉,现在三个入口都跑。
```

★ 顺带:`STATE.md:97` 的客户端 `1901` 那一行**不用动** —— 那是主树的数,本车道没碰客户端。

### 1.2 `ai-op` 的第二层防护**不存在**,文档却按双保险写

**实测**(`Get-LocalUser`):

```
MISSING ai-op        MISSING ai-ctl        MISSING ai-vigil        EXISTS ai-mem
```

形态 A 把 R1/R2/R3 的遏制性质建立在「`ai-op` 对 `{state}` 有 Deny ACE」之上。
**账户不存在 ⇒ 那条 ACE 不可能存在 ⇒ 被当作双保险的防护实际只剩一层。**

★ **把两层分清楚,别把这条读成比它更严重**:

| 层 | 状态 | 依据 |
|---|---|---|
| ① 网关按**名字**拒:`LOCAL_DENY_ACCOUNTS` 含 `ai-op`/`ai-ctl`/`ai-vigil` | ✅ **真的在,且被断言钉着** | `gateway.py:58`;`test_local_only_registry.py:205-208` |
| ② 文件系统 **Deny ACE** on `{state}` | ❌ **不存在** | 账户不存在 |

⇒ 今天**不可利用**:没有账户就没有以该身份运行的进程。
但**文档承诺了一层并不存在的纵深**,而这正是本项目的签名失败模式。

★ 另外两个账户**不需要**建:`ai-ctl` / `ai-vigil` 全仓只作为 `LOCAL_DENY_ACCOUNTS` 里的
**名字**使用,那条判据不要求账户存在。**只有 `ai-op` 带文件系统层。**
文档里若给它们也写了 ACL 纵深,那句话同样该撤。

**两条出路,请第 0 条车道选一条**(本车道**不擅自动文档**):

- **(a) 建账户** —— 已备好交用户双击的
  [`90-ops/ai-op-account/create-ai-op-ADMIN.cmd`](../../90-ops/ai-op-account/create-ai-op-ADMIN.cmd)。
  ★ 按 D46 交给用户在**自己的**会话里跑,**本车道没有运行它**:
  建账户 + 改 ACL 是系统/安全设置变更且需提权,而 agent 侧本来就跑在提权上下文里。
  .cmd 已按仓库既有规矩写:**纯 ASCII + CRLF + 无 BOM**(实测 0 个非 ASCII 字节)——
  `run-memory-suite.cmd` 的注释记着理由:cp936 下带中文的 .cmd 会解析错并**吞掉结尾的 pause**,
  窗口在你读到之前就关了。中文输出一律交给 `.ps1`。
  它先**打印当前状态**(哪几个账户在/不在),再让人在 `-WhatIf` 预演 / `drive-wide` / `enumerated`
  之间选 —— `create-ai-op.ps1` 的两个参数**故意没有默认值**,给默认值等于替人做一次安全裁决。
- **(b) 把文档里那层撤掉** —— 明写「② 只有在 `ai-op` 账户存在时才成立;本机不存在,
  当前遏制**只有网关按名字拒**这一层」。

★ 无论选哪条,`verify-ai-op.ps1` 现在**已经在覆盖账里**了(第 2.3 节):
它在本机注定在第 ① 节 `exit 1`,而那是**它在说实话**,不是脚本坏了。

### 1.3 `doctor.py` ④ 环的那句背书已经改掉,但**根因在 P4 清单上**

改的是工具(见 2.5)。**没改的是事实**:`/v1/chat/completions` 全路径
(实测 **192 行代码,268 行含注释**)对
`BROKER` / `gpu_broker` / `ensure_loaded` / `ModelLoader` / `_loader` / `acquire_lease`
**零命中**。⇒ 【按需装载】仍是 P4 的未完项,请在 STATE / PROJECT_PLAN 里保持这句话不被冲淡。

---

## 2. 落地了什么(全部在 `90-ops/`)

### 2.1 ★ 跨进程响应契约的成对断言元规则(D92 硬前置 · V1/V2 在等)

新文件 [`90-ops/gate/check_contract_pairs.py`](../../90-ops/gate/check_contract_pairs.py)。

**它照出来的第一件事:全项目 26 条跨进程响应契约,只有 1 条是成对的。**

| 服务 | 契约数 | 已成对 |
|---|---|---|
| 网关(Python/FastAPI)⇒ 客户端(C#) | 13 | 1(`POST /v1/gpu/lease`,即 A1 的病灶本身) |
| lan-edge(C#/Kestrel)⇒ 客户端(C#) | 13 | 0 |

★ **同语言不等于同进程。** D92 的措辞是「跨**进程**响应契约」,而 A1 那条缝的成因是
「两边各自都绿」,与语言无关 —— 所以 lan-edge 那 13 条一条都不能少算。

**设计(照 `ROUTE_TIERS` 反向全表那个已被验证的形状):**

- 契约清单**靠枚举得出**:网关 `import gateway` 直接读 `ROUTE_TIERS`,并**复核一次**
  `unclassified_routes() == []` —— 它一旦破,本工具的遍历源本身就是漏的,而它自己不会说;
  lan-edge 扫 `app.Map*`(**只扫 `10-core/lan-edge/Program.cs` 一个文件** ——
  `20-client-win/transport/Program.cs:442` 也有 `app.MapPost("/pair/enroll"…)`,那是自检里的
  **测试替身**,算进来会凭空多出一批假契约);
- 登记表只当**期望值**做反向全表(`set(实测) == set(登记)`),**绝不当遍历源**;
- ⇒ **反问过一遍:新增一个契约而没登记,默认落哪边?落【判红】侧。** 这是唯一的承重设计。

**明写不覆盖的三处**(不许静默少盖):① 客户端半边没有通用形状可数,棘轮只覆盖服务端;
② 锚点只认在不在、唯不唯一,不判断那条断言判得对不对;③ 请求体方向不在范围内。

**★ 欠债表只许变短**:还了债不改表会红(服务端顶层键集合断言的分布逐文件对拍);
又欠一条必须改 `_EXPECTED_DEBT` 那个数字 ⇒ **"又欠了一条"变成 diff 里的一行**。

**★★★ 合并当天它就抓到了第一条 —— 这不是演练**

并入 GPU 车道的 `dd06af3` 那一刻,元规则**当场判红**:

```
X ★★★ 没有**未登记**的跨进程响应契约   未登记:[('gateway', 'POST', '/v1/gpu/intent')]
----- EXITCODE = 1 -----
```

`POST /v1/gpu/intent` 是那次提交新加的路由。登记它的时候才发现:
**GPU 车道已经把两半都写好了**(`CONTRACT:gpu.intent`,服务端钉键集合、客户端 `ParseIntent`)——
所以它不是一条真欠债,而是一条**没人知道该被数进来**的契约。
⇒ 这正是广度表的用处:**深度那张表说不出"还有几条我没盖到"。**

**★★ 于是两张表被咬在了一起(第 ④ 组,双向)**

GPU 车道在 `test_gpu_broker.py` 里也建了一张 `CROSS_PROCESS_CONTRACTS`,
逐条钉**顶层键集合的具体内容**并去 `Selftest.cs` 找同名 `CONTRACT:` 标记 —— 那是**深度**。
两者不重复,但必须互相咬住:

- **他们那张表是手写的遍历源** —— 27 条契约里它覆盖 5 条,而**剩下 22 条的存在它自己说不出来**
  (ASSERTION-PITFALLS **3b**)。补上这一半正是本文件的职责;
- 反过来,本文件只认"两半在不在",**不看键集合对不对** —— 那是他们的职责。
- ⇒ 双向对拍:他们新登记一条而广度表没跟上 ⇒ 红;广度表标 `paired` 而他们那儿没有 ⇒ 红。

★ 一处**必须分清楚、不能四舍五入**的:`CONTRACT:gpu.intended.blocking` 钉的是
`POST /v1/gpu/intended` **409** 响应里 `result.blocking[i]` 的**子对象**形状,
**不是**该路由 200 的顶层键集合 ⇒ 它**不抵消**那条路由的欠债。
已在 `_SUBSHAPE_CIDS` 里登记并写明理由 —— 把"钉了一部分"读成"钉完了",
正是本项目反复吃亏的那种四舍五入。

**★ 给 V1/V2 的清单**(每条都带消费者文件与后果,跑 `python 90-ops\gate\check_contract_pairs.py` 看全文):

- **GPU/租约切片 欠 4**:`/v1/gpu/snapshot` `/v1/gpu/events` `/v1/gpu/components` `/v1/gpu/intended`
  (已成对 4:`/v1/gpu/lease` · `/v1/gpu/intent` · `/v1/gpu/lease/renew` · `/v1/session/end`)
- **证书/配对切片 欠 13**:`/pair/*` 4 条 · `/identity/renew/*` 2 条 · `/admin/*` 7 条
  ★ 其中 `POST /identity/renew/enroll` 正是 D92 点名的 **A5 那一族**。
- **未分配 欠 6**(请第 0 条车道指派):`/v1/models` `/v1/chat/completions`
  `/v1/sync/push` `/v1/sync/events` `/v1/sync/snapshot` `/health`

**★★ 顺带量出来两条只报告不修的发现**(工具自己算的"有没有消费者"):

- **`GET /v1/sync/snapshot` 客户端一个字都没读它**(全仓 grep 零命中)。
  推(`/v1/sync/push`)与订阅(`/v1/sync/events`)都在,**唯独没有拉全量** ——
  那么**重连之后拿什么对齐?** 这条要么接上,要么撤掉;它现在是一条没人用的路由。
- **`GET /health` 的响应体没有任何消费者**:客户端不读,`start-stack.ps1:144`
  只看 `curl` 退出码、**不解析响应体** ⇒ 它的顶层键集合改成什么都不会有人红。
- `GET /admin/devices` 与 `POST /admin/devices/revoke` **各有两个消费者**
  (`HubAdmin.cs` 与 `HubClient.cs`)—— 两处各自解析同一个形状,漂移时只会有一处被发现。

### 2.2 dotnet 半边:手写清单 → 反向全表(**在 fast 层**)

`run-tests.ps1` 开篇声明的第一性质(「清单靠扫描得出 + 反向全表,落不上就判红」)
**在 dotnet 这一半上此前不成立** —— 那里是一份纯手写的 `Args` 清单,背后没有任何东西核对它盖没盖全。
lan-edge 那 24 条就是这么漏的(ASSERTION-PITFALLS **3b**:判词说"每一个",判据是一份手写名单)。

⇒ 入口清单改从每个工程的 `args[0]` 分派表里**解析**出来(遍历源),`$DOTNET_SUITES` 只当期望值。
`Tests` / `NotTests` 两栏,**`NotTests` 逐条写明理由** ——「这不是测试」是一句需要负责的话,
它是把一个入口挡在门禁外的唯一借口。

★ **跑的和被核对的是同一张表**,不会各自漂移。
★ 放在 **fast 层**(纯静态、毫秒级):新增 `selftest6` 不登记,**提交那一刻**就该红;
等有人想起来跑 `-Full` 才红的话,中间那段时间和没有护栏没区别。

### 2.3 795 行 `verify-*.ps1`:没人跑,**也没被记成没跑**

全仓引用只出现在 README 与彼此的注释里。⇒ `$ENV_VERIFIERS` 反向全表(扫盘得出)
+ `-VerifyEnv` 开关一次跑全部 + **跑没跑都进覆盖账**。

★ **不进自动门禁是有意的**:它们验的是**本机环境**(真实 ACL / `pg_hba` / `pg_ident` / 本地账户),
不是源码。进提交门禁会因"换台机器"而红,而 ASSERTION-PITFALLS 第 5 条已经量过这个代价:
**它训练人去用 `--no-verify`**(D24 里 `-Force` 一路跳过三道闸、显存闸整整一天没生效过)。
⇒ 它们的红**不并进** `$totalPass`/`$totalFail`,也不影响退出码,**单列一段账**。
这不是给它们开后门:反向全表保证它们跑不掉、也躲不进沉默里。

实测(本车道跑过,只读):`verify-isolation` 17 PASS · 3 SKIP;`verify-state-acl` 35 PASS;
`verify-ai-op` **exit 1 于第 ① 节**(见 1.2)。

### 2.4 扫描器盲区(上一轮 B4「一行没动」)

- `scan_stale_claims` 扩到 `.ps1` / `.json` / `.toml`。**第二个实例当场落网**:
  `build-client.ps1:304` 的「AI 模型尚未接入(P4):聊天会记录但不会有回答」在一段
  **here-string** 里,**进了每一份出厂包的安装说明** —— 用户唯一会读的那份文本。
  同一句话在客户端 `.cs` 里被断言钉着、被抓住了;**在没人看的这一侧发了出去。**
- ★ 加 `.toml` 的同时必须把 `config` 加进 `CODE_DIRS`:全仓 6 个 `.toml` 有 5 个在那儿,
  只加后缀是一次**看起来修好了的假修复** —— 而假修复正是这个工具存在的理由。
- ★ here-string 正文**整行没有引号**(`@"` 与 `"@` 在别的行上),沿用"行上要有引号"
  会让这一整类**静默漏掉**,而它恰恰是最要紧的一类。命中标成**【出厂文本】**。
- ★★★ 拿掉 `SKIP` 里的 `debug` —— 那是个 **fail-open 后门**:字面像是跳过构建产物
  (.NET 的是 `bin\Debug`,已被 `bin` 挡住),实际只挡住了 `90-ops\debug` **整个调试工具箱,
  包括扫描器自己**。而 D88 明写「工具自己也要被测」。
  配套把 `_STALE_WORDS` 的针**拼出来**,否则它会报自己 8 次(实测)——
  这正是 ASSERTION-PITFALLS 第 1 条第 8 次带出的写法。

### 2.5 `doctor.py` ④ 环:不再替不存在的机制背书

此前:「后端没在跑**不一定是错的** —— D87 之后是【按需装载】,没人用就该是这样」。
**那是替一个不存在的机制背书**(见 1.3):后端没起的真实后果是**聊天直接失败**。
**一个在系统坏了的时候报"这可能很正常"的体检工具**,正是这套工具存在的理由所指的东西。

★★ 而**直接把那句话改成相反的**会在另一个方向上重犯同样的错:按需装载哪天真接上了,
这句话又变成新的假话,**而且没有任何东西会提醒**。
⇒ 判据改成**当场量出来的**(`autoload_in()` 读 `gateway.py` 文本、去注释、找 Broker/装载器引用),
让它自己跟着源码翻面。三态:接了 / 没接 / **读不到就返回 None 不猜**。

### 2.6 产物身份:让「产物落后于源码」变成会红的东西

`grep dist` 此前在 `run-tests.ps1` 与 `selfcheck.py` 里**零命中**;
`doctor.py` ⑨ 环**只查 `.dirty`、从不比提交号**,判 ✔。
`.dirty` 只回答「出包那一刻工作区干不干净」,**它不问这份产物是哪个提交出的** ——
⇒ 最常见的那种不一致完全不被发现:**在一个干净的旧提交上出的包**。
版本戳里明明写着提交号(`20260806-1655+4e5da1f`),而没有任何东西读它。

⇒ ⑨ 环改成解析版本戳 + 比 `HEAD`,五种结局各说各的:
同一提交 ✔ / 落后 N 个提交 ✘ / 提交号不在当前历史里 ✘ / `.dirty` ✘ / 没有 VERSION.txt ✘。
★ **「说不清这是什么」不许当成「没问题」** —— 身份不明的产物和过期的产物一样不能装出去。

`run-tests.ps1` 的覆盖账每次都印出厂产物的身份,但**只报不判红**:
「有没有重新出包」不是一次提交的性质,判红会造出又一台永久红灯机器。
它和「契约欠配对」同一档:**必须每次都看得见、但不拦提交的事实**。

### 2.7 顺带修掉的两处(都在覆盖账的诚实性上)

- **worktree 里 `-Full` 结构上恒红**:代码和它自己的注释是矛盾的 ——
  注释写「本来就不会有…别为消这条红去出包,如实记成『没跑客户端自检』即可」(意图),
  代码却塞进 `$broken` ⇒ `exit 1`。D92 之后并行车道全在 worktree 里,这条恒红打到每一条车道身上,
  而**一个结构上永远红的门禁携带零信息**。⇒ 让代码服从注释:worktree 记进覆盖账的「没跑」栏,
  主树仍判红(两种情形的下一步完全相反:主树=去出包,worktree=什么都不用做)。
- **覆盖账把 795 行报成 714**(`Measure-Object -Line` 不数空行)。
  ★ 错在**往少的方向**,而这一行的用途正是说清有多少断言躺着没人跑。

---

## 3. ★ 改了 `run-tests.ps1` —— V1/V2 请看这一节

本轮对 `run-tests.ps1` 的改动会同时打到你们身上,**判红时不要以为是自己弄的**:

1. **fast 层多了两张反向全表**(dotnet 入口 · `verify-*.ps1`),在 `-ListOnly` 之前就跑。
   新增一个 dotnet 子命令、或新写一个 `verify-*.ps1` 而不登记 ⇒ **当场 exit 1**,
   失败信息里写着改哪张表。
2. **fast 层多跑一个套件**:`90-ops\gate\check_contract_pairs.py`(126 条)。
   新增一条网关路由而不在 `CONTRACTS` 里登记 ⇒ **当场判红**。这是 D92 的硬前置,**不是可选件**:
   脚本文件不在也判红,不是跳过。
3. **`-Full` 多跑 24 条**(lan-edge `client-e2e` + `admin-e2e`)。
4. **worktree 里 `-Full` 不再恒红**(见 2.7)。
5. 覆盖账多了三段:契约欠配对 · 环境验证脚本 · 出厂产物身份。末行追加欠债提示
   (`PASS=… FAIL=0` 的形状**原样保留** —— `.githooks/pre-commit` 用 `grep -oE` 抓它)。

**基线**:fast `PASS=1355 FAIL=0`(此前 1203) · `-Full` `PASS=1586 FAIL=0`(worktree 内,无客户端产物)。
★ 断言总数变了而你没增删断言 ⇒ 先查是不是跑错了产物(ASSERTION-PITFALLS 第 3 条);
这次是**我加的**,数在这里对得上。

---

## 4. 只报告不修(禁区:`10-core/**` · `20-client-win/**`)

| # | 事实 | 归谁 | 要改哪一行 |
|---|---|---|---|
| R1 | 25 条跨进程契约没有成对断言 | V1/V2 按 2.1 的分组 | 服务端钉顶层键集合、客户端钉「拿这形状能解析出目标字段」,照 `test_gpu_broker.py:1223` 与 `Selftest.cs:6964` 那一对 |
| R2 | `GET /v1/sync/snapshot` **零消费者** | 未分配 | 要么客户端接上(重连对齐没有别的来源),要么从 `ROUTE_TIERS` 撤掉 |
| R3 | `GET /health` 响应体零消费者 | 未分配 | 至少明写"它的响应体不构成契约",否则改坏了不会有人红 |
| R4 | `/admin/devices`(+`/revoke`)**两处各自解析** | 证书/配对切片 | `HubAdmin.cs:238` 与 `HubClient.cs:310` 合成一处,否则漂移时只有一处被发现 |
| R5 | `/v1/chat/completions` 全路径不碰 Broker/装载器/租约 | P4 清单 | 【按需装载】仍是未完项,别让文档把它说成已有 |
| R6 | `build-client.ps1:304` 的过期文案**已出厂** | 本车道**可以**改(90-ops),**本轮未改** | 见下方注 |
| R7 | `strings.json` 3 处占位文案 | 客户端车道 | `usage.pending` / `status.rate_pending` / `common.coming_soon` —— 扫描器报的是**嫌疑**,逐条对照现状看一眼 |

> **R6 为什么本轮没改**:那句话是不是过期的,取决于「聊天现在到底有没有回答」——
> 而本车道刚刚查明 `/v1/chat/completions` **不碰 Broker、后端也没在跑**(1.3)。
> 也就是说:**在当前这台机器的状态下,那句话可能仍然是真的。**
> 一个扫描器报出来的是**嫌疑不是判决**,而这一条恰恰需要产品侧裁定"接入"算到哪一步。
> ⇒ 本车道把它**报出来并让它从此会被扫到**,把措辞留给拿得准的人。
> ★ 这不是推卸:改错方向的代价是**在出厂说明里写一句更假的话**。

---

## 5. 红测清单 —— **每条护栏都亲眼见过红**

> 本仓纪律:**没红过的护栏和没有护栏是一回事**(ASSERTION-PITFALLS 第 10 条结尾)。

### 5.1 契约配对元规则(9 条,全部 `exit 1`)

| # | 改坏什么 | 红的那条 |
|---|---|---|
| R1 | 新增契约未登记 | 反向全表:未登记 |
| R2 | 登记了已消失的契约 | 反向全表:过期登记 |
| R3 | paired 的那半被删(契约号找不到) | 契约号在不在 |
| R6 | 欠债总数对不上 | 欠债数 |
| R7 | lan-edge 源码根不存在 | 零命中判红 |
| R8 | 提取器被改宽(`\b` 词边界丢了) | 提取器反向用例 |
| R9 | 上游反向全表破了(`ROUTE_TIERS` ≠ `app.routes`) | 枚举源核对 |
| X1 | 对方新登记契约号,本表没跟上 | 双向对拍(账虚高那侧) |
| X2 | 本表标 paired 而对方没有 | 双向对拍(钉子被拔那侧) |
| X3 | 对方契约号零命中 | 零命中判红 |
| X4 | 对方那份文件读不到 | 读不到 ≠ 没问题 |
| X5 | 子形状条目从对方表里消失 | 过期登记 |
| X6 | `paired` 却没给契约号 | state 与契约号一致性 |

★ 红测只改**登记表**这一侧,被检查的 `10-core`/`20-client-win` **一个字节没动**(禁区)。
两者走的是**同一条集合差表达式**,所以红的路径是同一条。

★ **R4/R5 已作废,连同它们守的那两条判据一起换掉** —— 记在这里因为换掉的理由本身有价值:

- **R4「锚点恰好出现一次」**:改用契约号定位之后,契约号会**正常地**同时出现在分节注释与
  断言消息里(实测 `gpu.lease.grant` 在 `Selftest.cs` 里 2 次)。继续要求"恰好一次"
  会造出一条**必然误红**的判据 —— 而误红的护栏很快就没人看。
- **R5「按语法数 `set(...keys()) ==` 的棘轮」**:**当天就被证伪** ——
  GPU 车道的新断言写成 `set(_rl.json())`(没有 `.keys()`),正则**一条都没数到**。
  ★ 一个按**语法形状**计数的护栏,换个等价写法就瞎了,而它瞎掉的方向是"少数了" ⇒ **静默放过**。
  改成对拍**声明出来的契约号**,形状怎么写都不影响。
  > 这条值得单独记:**判据不该长在语法上,该长在声明上。**

### 5.2 `run-tests.ps1` 两张新反向全表(6 条)

RD1 dotnet 入口未登记 · RD2 登记了不存在的入口 · RD3 分派块解析不出(零命中)·
RD4 `Program.cs` 找不到 · RD5 环境脚本登记了不存在的 ·
**RD6 做了一次真的**:新建一个未登记的 `90-ops/verify-*.ps1`,门禁当场判红,删掉即恢复。

### 5.3 扫描器 + 产物身份(6 + 5 条)

S1 针写回字面量(扫描器开始报自己)· S2 `debug` 后门加回 · S3 `.ps1` 被从后缀表删掉 ·
S4 `config` 被从 `CODE_DIRS` 删掉 · S5 here-string 识别改坏(**少盖方向**)·
S6 块注释识别改坏(**误报方向**)。
产物身份五种结局逐个实测:同一提交 ✔ / 落后 2 个提交 ✘ / 提交号不在历史里 ✘ /
`.dirty` ✘ / 没有 VERSION.txt ✘。

★ `scan_fake.py` 的红测用**字节备份还原**(不用 git —— git 的还原目标是已提交的版本,
不是"我刚才那一下之前的样子",ASSERTION-PITFALLS 第 2 条踩过两次),还原后 **SHA256 逐字节一致**。

### 5.4 ★★ 红测自己揪出了**四条假绿**,已修

这一节比上面三节都重要 —— 它说明红测不是仪式。

1. **接线时的 fail-closed 分支抓住了我自己**:`$cpOut -match '^\s*==='` 恒不成立
   (PowerShell 的 `-match` 没有 Multiline,`^` 只锚整串开头)。
   而「读不到欠债行 ⇒ 判红,**不当作没有债**」那个分支**当场让门禁未过** ——
   **护栏抓到的第一个东西是它自己的接线。**
2. **S3 第一版不红**:我拿**被检查的那张后缀表**当遍历源(`for _ext in STALE_EXTS`),
   删掉 `.ps1` 时循环少转一圈,一条都不红。**正是 3b 那个坑,我当场又踩了一次。**
   改成独立字面量 `_EXPECT_EXTS` 做集合相等。
3. **S6 第一版不红**:两条反向用例里**没有引号**,于是它们绿的原因是
   「这一行不像字符串字面量」,**跟块注释识别一点关系都没有** ——
   一条**为了别的理由而绿**的假断言,出现在一条专门用来防假断言的自检里。
4. **一个只在出问题时才出错的计数器**:并入 GPU 车道那一刻,工具印出 `PAIRED=2`,
   而登记表里只有 1 条 `paired`。根因是它拿 `总数 − 欠债` **减**出来 ——
   `总数` 来自实测枚举、`欠债` 来自登记表,两者一分家(**也就是恰好在有未登记契约、
   本工具正在报问题的那一刻**),减出来的数就是错的。
   ⇒ 改成从登记表直接数。**偏偏在最需要它准的那一刻说谎**,那正是这个工具存在的理由。

---

## 5.5 ★ 请第 0 条车道并进 ASSERTION-PITFALLS 的三条(用户 2026-08-04 的常设要求)

> 「**同一个陷阱踩了三次以上就要记下,防止以后再踩**」——
> 本车道**不动那份文档**(不在归属内),但今天给它添了新实例,不记就是又绕过一次。

1. **第 7 条(用脚本改文件,改到没打算改的部分)再添一次 —— 又是行尾,不是转义。**
   我用 Python `write_text` 只改了两处字符串,`git diff --numstat` 报 **496 行删除**:
   整份文件被 LF 静默改成 CRLF(Python 写文件默认 `newline=None`,Windows 上翻译成 `\r\n`)。
   ★ 抓到它的仍然是那条判据本身 —— **删除行数必须对得上,删除不会凭空多出来**。
   已按仓库惯例(全部 `.py` 都是 LF)还原,改后 140/76。
   ⇒ 建议补一句:**`pathlib.write_text` 在 Windows 上等同一次全文行尾规范化**;
   要保行尾就 `write_bytes`,或 `open(..., newline="")`。

2. **第 3b 条(守卫只盖住它自称范围的一部分)再添一次 —— 而且是我自己踩的。**
   我给"后缀表少一个就该红"写护栏时,**拿被检查的那张表当遍历源**
   (`for _ext in scan_fake.STALE_EXTS`)⇒ 删掉 `.ps1` 时循环少转一圈,**一条都不红**。
   ⇒ 值得加一句更狠的判词:**凡是"这张表要盖全"的护栏,遍历源绝不能是那张表自己。**
   写的时候它看起来完全合理,而它恰恰把"少一项"变成了"少查一项"。

3. **一条新的,建议单列**:**判据不该长在语法上,该长在声明上。**
   我按语法数 `set(...keys()) ==` 来当"还了债"的棘轮,GPU 车道把等价断言写成
   `set(_rl.json())` —— 正则**一条都没数到**,而瞎掉的方向是"少数了" ⇒ **静默放过**。
   同族的老实例其实已经有两个:`scan_fake` 的盘符正则写死 `D:`(零命中报"未发现问题")、
   `run-tests` 非锚定的 `(\d+)\s*FAIL` 匹配到 HTTP 状态码。
   ⇒ 判据要么落在**声明出来的东西**(契约号、登记表、导出名单)上,
     要么就得配一条"它今天到底命中了几条"的元断言。**语法会换写法,声明不会。**

---

## 6. 推翻条件

1. 若**客户端半边**出现可枚举的通用形状(比如统一的 `TryParse*` 命名约定)
   ⇒ 2.1 的棘轮应扩到客户端,把「只覆盖服务端半边」那句删掉;
2. 若 lan-edge 的 `client-e2e` / `admin-e2e` 开始**随环境漂移**(端口被占、需要真中枢)
   ⇒ 按 D91 的判据重估:进自动门禁的门槛是「结构上不可能」,不是「今天恰好能跑」;
3. 若 `-VerifyEnv` 长期没人跑 ⇒ 说明"给个开关"不够,该考虑挂进收工流程,
   **而不是把它们塞进提交门禁**(那条路已经论证过代价);
4. 若 `ai-op` 账户被建起来 ⇒ 1.2 的 (b) 作废,改跑 `verify-ai-op.ps1` 逐条核对,
   并把它从"注定 exit 1"的登记里改掉;
5. 若「按需装载」接上 ⇒ `doctor.py` ④ 环**会自己翻面**(判据是当场量的),
   但 STATE / PROJECT_PLAN 里那句"未完项"要人工撤。

> 出处:2026-08-06 · V3 门禁与产物的诚实性车道 · 全部数字为本车道实测。
> ★ D 号写 `D?`,并入那刻取号(当前最大 D92)。
