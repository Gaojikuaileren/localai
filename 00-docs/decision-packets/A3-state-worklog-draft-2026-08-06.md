# A3 · STATE / worklog 并入草稿(P4 车道交回)

> 提交人:P4 车道(主树 main)· 2026-08-06
> 起因:`00-docs/audit/AUDIT-2026-08-05-full.md` §5 A3「STATE.md 的 P4 段落已被自己的代码推翻,
> 且最后 5 个提交没有记录」。
>
> ★ **本文件不修改任何中央文档。** 按本轮协调纪律,三条线并行期间
> `00-docs/{DECISIONS,PROJECT_PLAN_v3.0,STATE}.md` 是唯一的串行点,由协调层在并入那一刻统一处理。
> 这里只交回**要并入的内容**与**并入时的注意事项**。
>
> ★ D 号一律写 `D?`,本车道不自取号。

---

## 0. 一句话

STATE 的 P4 段落**在写下的那一刻就已经过期**(S14 是它的祖先却没被记进去),
此后又落了 10 个提交。本文件给出逐条替换文本 + 三段缺失的 worklog。

**★ 最需要协调层注意的是第 4 节「下一步」** —— STATE 自己记着这一节"到今天为止已经过期/写错五次",
而这一轮三条线同时动,是第六次的高危窗口。本车道**只报告自己那部分**,不代其它车道排序。

---

## 1. 逐条核实(我自己跑的,不是照抄审计)

| STATE 原话 | 实测 | 证据 |
|---|---|---|
| 「P4 落地 **S0–S13**」 | 已到 **S16**,其后又有 6 个审计修复提交 | `git log 6a99136..HEAD` = 10 个提交 |
| 「后端仍由 `start-stack.ps1` **静态启动**,所有事务的终点都是 `loader_absent`」 | **假**。装载器 S14 就落地了 | 活体快照 `loader_present=true`;`doctor.py` ⑥ 环实测 `I2/I3 = observed` |
| 「`actual_resident`…**它们不再恒真的那天断言会红**」 | 那天**已经到了**(S14) | 见第 3 节「交回给协调层的第 3 问」 |
| 「客户端 selftest **1854**」 | **1890** | 本机实跑 `PASS=1890 FAIL=0` |
| 「全仓基线 2913」 | **2985** | `run-tests.ps1 -Full` 实跑 |
| 「两台机器的实机同步验证**还没做**」 | 已做过一次**双向**实测 | `f7813d7`:主机本地待办 2→11(含副机 3 条)· 共享会话 4→9 |

★ 审计那张表里写的是 2962/1890;此后又落了 3 个提交(`d0b0a2d` / `10aa8c0` / `447beb9`),
现为 **2985**。**并入时请用 2985,不要用审计里的 2962** —— 那个数在审计写完的那一刻就开始过期了。

---

## 2. STATE.md · 建议替换文本

### 2.1 顶部基线那一段(整段替换)

> **★ 全仓基线(2026-08-06 · P4 落地 S0–S16 + 审计修复后):
> `powershell -ExecutionPolicy Bypass -File 90-ops\run-tests.ps1 -Full` → PASS=2985 FAIL=0。**
> 构成 = Python **1001**(gateway 面 827 + gpu-broker 134 + 调试工具自检 40)+
> dotnet 94(identity 81 · lan-edge 8 · transport 5)+ 客户端 **1890**。
> ★ 逐项实测(2026-08-06):`test_gpu_broker` 344 · `test_vram_gate` 134 · `test_sync` 81 ·
> `test_gpu_policy` 79 · `test_local_only_registry` 69 · `test_component_bridge` 56 ·
> `test_e1` 50 · `test_gateway_e1` 36 · `selfcheck` 40 · 其余 8 个套件合计 112。
> (逐项相加 = 827 + 134 + 40 = **1001**,与门禁报的数一致 —— 已对过账。)
> ★★ 调试工具自检(`90-ops\debug\selfcheck.py`)是**第 18 个套件**,S15 起进门禁,
> **条件执行**:`git rm -r 90-ops/debug` 之后自动跳过,门禁照常绿。
> (08-04 首跑 2111 → 08-05 凌晨 2833 → 08-05 深夜 2962 → 08-06 **2985**。)
>
> ★★ **08-05 起,客户端那个数才真正代表"当前源码"**:此前门禁的客户端半边
> 直接跑已有 exe、从不重编,改了源码它纹丝不动。现已加时间戳判据 ——
> 产物比源码旧就**拒绝执行**并判门禁未过。
> ★ git worktree 里没有 `bin/`(不进 git),那一条会判「没跑起来」并说明**不要为消红去出包**。
>
> ★★ **本轮基线是在【栈起着】的情况下跑的**,而这暴露了两条随环境漂移的断言(见 §3 记账):
> 端点测试用全局 BROKER 单例会认领真实后端 · `vram_gate.main()` 拿实机显存当判据。
> 两条都已改成注入。**建议此后一律在栈起着的状态下跑一次** —— 那是唯一能让这类断言现形的时刻。

### 2.2 P4 阶段行(整段替换)

> ## 当前阶段:**P4 GPU Broker + 仲裁 · 🔵 进行中(S0–S16 + 审计修复)· 只差【按需装载的触发策略】**
>
> **已落地**:S0 闸复活 · S1 gpu.* 架构断言 · S2 词表桥 · S3 Broker 骨架 · S4a 三集合闸 ·
> S4b 租约账本 · S5 推送 + 世代号 409 · S6 硬件指纹 · S7 四集合不变式 · S8 事务语义 ·
> S9 组件目录 + 挑选面板 · S10 权限档位六元组 · S11 接入模型 · S12 行为底线提示词 ·
> S13 内网同步 · **S14 装载器本体** · **S15 调试工具箱 + 全项目审计(D88)** ·
> **S16 实机双边同步 + 显存条改主机唯一源**。
>
> ★★ **`actual_resident` 已经是独立观测**(S14 起)。事实源 = 后端进程活着且 `/health` 回 2xx,
> 与账本无关;不变式 I2/I3 的置信度随之从 `self_reported` 升为 **`observed`**(活体实测)。
> 「点确定」这条路径**已经能真的装模型**,不再恒返 `loader_absent`。
> ⇒ STATE 上一版那句「后端仍由 start-stack.ps1 静态启动、所有事务的终点都是 loader_absent」
> **在写下的那一刻就已经不成立**(S14 是它的祖先)。
>
> ★ 仍然为真的:**后端不会自己起停**。`start-stack.ps1` 静态启动这件事没变 ——
> 变的是"点确定"能装,而不是"没人用的时候会自动卸"。
>
> **只差一条**:**S16-b 按需装载的触发策略**(D87:意图即起 · 空闲即卸 · 显存压力即让)。
> ★ 编号建议由协调层定 —— 本车道此前把「实机两报」也叫了 S16,与 D87 的实现顺序表撞号。

### 2.3 客户端自检那一行

> **客户端 selftest 1890**(2026-08-06 审计修复后实跑,FAIL=0)
> (… → S13 内网同步 1844 → S15 审计 1854 → **S16 + 审计修复 1890**)
> ★ **纪律:这个数只在这一行出现**,别处一律引用而不复写。

---

## 3. worklog · 三段缺失的条目(建议追加到 `2026-08.md`)

★ 顺序注意:**S14 在时间上早于 S15**,而 worklog 现在最后一条是 S15。
建议把 S14 那段**插在 S15 之前**,否则读起来像是装载器在调试工具箱之后才做的。

### 3.1 `## 2026-08-05 · P4-S14 装载器本体(`9762e85`)`

- `model_loader.py` 落地:真的起停后端进程。`SUPPORTED_KINDS = {"llm"}` —— 反向全表,
  未验证过启动方式的 kind **fail-closed**(猜一套参数的后果不是"可能起不来",
  是"看起来支持而第一次真用时才炸")。
- `_health_ok` 要求 **2xx** 而不只是"连得上";`adopt()` 认领中枢重启后的孤儿后端,
  而 `unload()` **永不杀认领来的进程**。
- ★★ 由此 `actual_resident` 从"Broker 自己的账本"变成**独立观测**,
  I2/I3 的 confidence 升为 `observed`。
- ★★★ 而 S7 那条"钉住恒真性"的绊线**没有响** —— 它测的是一个**没接装载器**的 Broker,
  那条路径上恒真性仍然成立。绊线守的是"某个实例",该守的是**两条路径各自的性质**。
  改写成两条分支都显式钉住之后,**立刻**抓到 I3 的 confidence 还是硬编码的(I2 已改成动态)。
  ⇒ 「断言也会说假话」的又一种形态:不是说了假话,是**根本没在看那件事**。

### 3.2 `## 2026-08-05 深夜 · P4-S16 实机两报(`00309cb` … `3b34053`)`

用户实测报了两件事,查下来都不是"哪里写错了",而是**一整个动作从来不存在**。

- **同步只有「追增量」,没有「对齐」**。中枢存档里两台真机**一条记录都没有**(只有测试夹具),
  而本机存档里确实躺着合格的家庭待办与共享会话。根因:推送只在变更那一刻触发,
  而待推队列是**纯内存**的 —— 关一次 App 就没了。⇒ `ReconcileAsync`:连上就对齐。
- **两个真根因都是「静默」造成的**:
  ① 订阅循环被一个订阅者的异常**永久掀翻**(主页那行状态在后台线程写 WPF 的 TextBlock,
  而 `RunAsync` 里那句 `Changed?.Invoke()` 在 try/catch **之外**)。
  ★ 对照 `HubGpu` 一直有 `try{}catch{}` 护栏 —— **GPU 流活着、同步流死了**的全部原因。
  ② 远端记录在被用之前,它所属的 `JsonDocument` 已经释放(`using` + `BeginInvoke` 异步),
  每条都抛 `ObjectDisposedException` 而被逐条 `catch {}` 吞光。
  表现:帧收到了、generation 一路涨、本地一条都不多。
- **删除 = 墓碑 + 先拉后推**(它们是同一个洞的两面:没有删除语义时,"连上就对齐"会把
  对方删掉的东西推回去)。★ 推翻了「删共享会话只影响这台」那条既有裁定,**已当面向用户说明后果**。
- **在线状态**:判据是"此刻有没有活着的订阅",不是"最近推过东西"(后者是过去式,
  关了机的机器还会"在线"好几分钟,而**在线状态错了比没有更坏**)。
- **显存条改成只显示主机的**,本机 NVML/nvidia-smi 读取路径整条删除。
  ★★ 实测发现 P4-S9/D84 那个"拿不到就改标题"的修复**从来没接到界面上** ——
  `VramBar` 的标题是构造函数里写死的「显存」,`Update()` 一次都没读过 `s.Title`。
  改在模型里、断言钉住了、artifact 写着已修,**就是没接到视图上**。
- **实机双向验过**:主机本地待办 2→11(含副机 3 条)· 共享会话 4→9 · 删除同步 13→9(3 秒)。

### 3.3 `## 2026-08-06 · 审计修复(`803464c` … `447beb9`)`

依 `00-docs/audit/AUDIT-2026-08-05-full.md` §7「当晚可做 / 收口前必须做」逐条办:

- **C2** 在线注册写在生成器**外面** ⇒ 客户端建连即断时 `finally` 永不执行,
  那条记录**永远留在名单里**(key 是 `object()`,再也够不着)。★ 这是我一小时前刚写进去的。
- **C1** `device` 完全由客户端自报 ⇒ 在线名单可任意命名 · 冲突归因指错人 ·
  **额度维可绕过**(令牌桶 key 就是这个自报值,每换一个名字就是一个新桶)。
  改为从证书指纹反查成员表;成员表给不出就退到**指纹**,**绝不**退回自报值。
- **A1** 7 处 `_generation += 1` **没有一处在采样路径上** ⇒ 别的程序吃掉 4 GiB 时一帧不发,
  而心跳把客户端的"数据新鲜"喂成真。新增 `stream_rev`(与世代号**分开** ——
  世代号是事务的乐观锁,被显存波动带着涨会让"点确定"一直撞 409);
  心跳改成**自带数据**;客户端判据换成 `LastDataAt`。
  ★ **阈值 0.25 GiB 是实测的**:本机桌面空闲 30 秒,相邻差最大 **0.130**、全程漂移 0.154 ——
  审计建议的 0.1 会被噪声直接打穿。比的是**累计差**,不是相邻差。
- **C4** 拒绝信息只给 `lease_id` ⇒ 用户看不出**是谁在占**。改成结构体数组;
  "已持有多久"由**中枢算好再给**(`granted_at` 是中枢的单调时钟,客户端拿自己的表减是随机数)。
- **doctor** 两处误报(只读 400 字节导致 JSON 截断 · 只探回环导致永远报 lan-edge 没在听),
  以及模型后端那一环顺带查**监听者是谁**(实测撞见另一条车道的 spike 临时占住 18081)。

---

## 4. 「下一步」那一节 —— 本车道只报告自己那部分

> ★ STATE 自己记着这一节**已经过期/写错五次**。三条线并行,这是第六次的高危窗口。
> 以下**只是本车道的状态**,排序与其它车道的合并请协调层定。

**P4 还差的(本车道)**

1. **按需装载的触发策略**(D87)—— P4 清单最后一条。
   ★ 与审计 **B1** 是同一件事的两面:`AutoUnloadIdle` / `AutoStartPreset` 两个开关
   能拨、会落盘、**没有任何读取方**,而它们就长在那段解释"假开关是骗人"的注释正上方。
   S14 之后"接入后由 Broker 执行"读起来变成"已经生效",**比 S14 之前更容易骗人**。
   ⇒ 建议一起做;分开修等于修两次。
2. **B2**(客户端 `loader_absent` 文案仍写着「那是 P5」,服务端已修)—— 当晚可做,尚未做。
3. **C6**(sysmem fallback 真实状态)—— 尚未查。

**P3d(本车道)· 一行未动,理由见第 5 节**

**要用户裁定、本车道不动的**:A2 排期 · C5(两道墙 vs 三段预览,且这次偏离没有 D 号)·
D-5(家庭→个人降级时中枢那份删不删,与「删除同进同退」是一组,建议一并裁)·
S16/D87 留不留在 P4。

### 4.1 ★ 两条**新增欠账**,建议记进 STATE(本车道查实,但不该由我自己并)

**C6 · sysmem fallback 从来没被关过,而三道闸的注释把"已关"当前提在用**

实测(2026-08-06,RTX 5080 / 驱动 610.62):
`HKLM:\SYSTEM\CurrentControlSet\Services\nvlddmkm\Global\NVTweak` 与
`HKLM:\SOFTWARE\NVIDIA Corporation\Global\NVTweak` 下**没有任何** Sysmem/Fallback 项
⇒ 用的是**驱动默认值**,而不是"已关"。全仓也没有任何代码去关它、没有任何断言验过它。

⇒ 超配的真实表现更可能是**静默溢出到内存、慢到不可用**,而不是 `start-stack.ps1`
注释假定的硬失败。★ 两者都违 §12.3,但**长得完全不一样**,排查方向相反
(一个"报错了"、一个"怎么这么卡")。

★ **诚实边界**:我确认的是"**没有覆盖项**"这个事实。"默认值 = fallback 开着"
是按 NVIDIA 的文档语义推的,**本车道没有实测过超配行为**(要实测得真的把显存装爆)。
⇒ 记账时请写成"未关且未验",不要写成"已确认是开的"。

★ **要不要真的去关是用户的决定** —— 那是系统级驱动设置,会影响游戏等所有程序。
本车道只改了那句假前提的注释,不代做。

**C7 · 同步的范围闸把中文字面量当协议值**

`sync_store.in_scope` 判 `sc == "家庭"`。客户端靠 `TodoCenter.WireFamily` 归一化后再上线,
所以**今天是通的** —— 但协议本身不再语言无关:任何不做归一化的调用方
(将来的第二个客户端、调试脚本)推 `Family` 会被**静默判出范围**。
`00309cb` 的提交信息如实记了「彻底的做法是 Scope 存语言无关的键…没做,也没假装做了」,
但这条**只活在一条提交消息里**,没进 STATE 的欠账。
⇒ 建议:服务端接受规范键 `family` 并把三种语言写法当**兼容输入**;欠账进 STATE。
★ 本车道未改 —— 它改的是**协议**,而两台机器上跑着的客户端版本可能不一致,
  属于要先说清升级顺序的那类改动。

---

## 5. P3d:两道闸,建议先裁再写(转交事项 1 的答复)

协调层的判断本车道核过,同意,而且**闸二比闸一更要紧**:

- **闸一**(`tainted.py:240` 登记 `CHANNEL_RELAY`)只要改一行。**正因为只要 5 秒,不动它** ——
  那一行的含义是把全系统最低信任档放进 S0 记忆正文的读取集,然后这些正文经 Signal 出去。
  这是一次**安全裁定**,不是一次实现。
- **闸二**(`tainted.py:252`)`unseal_for_prompt` 判的是**后端是否出境,与 sensitivity 无关**。
  ⇒ 即使闸一放行,只要 Signal 桥被建模成 `egress=true`,S0 正文照样出不去。
  真正要裁的是:**把一段记忆正文渲染成一条 Signal 消息,算不算出境?**
  ★ 与 D81 挂着的 `unregistered-local` 是同一族,**建议一并裁,别分两次**。

⇒ 本车道**不会先写方向 B 的扫描点**。请协调层安排一份 `D?` 决议包把两道闸一起裁完。
否则会把回程闸做完、然后发现验收句在更上游就被拦住了。

---

## 6. 本草稿**不**声称

- **不声称**两台真机现在是好的:`f7813d7` 的双向实测属实,但 `f1f66fe`(墓碑 + 先拉后推)、
  `3b34053`(在线状态)、`447beb9`(C4)**都还没有在两台机器上跑过**。
  ★ 副机目前仍挂着旧包 —— 中枢在线名单里那个 `unknown` 就是它。
- **不声称**记忆库那 459 条断言今天仍然成立:它们**从 2026-07-28 起没有再跑过**。
  本车道 08-05 改过其中两条恒真断言,**只过了 `py_compile`,没有实跑**
  (`psycopg` 未装 + `pg_ident` 只映射 `ai-mem`,结构上跑不了)。
- **不声称** A1 的 0.25 GiB 阈值在**别的机器**上也对 —— 它是本机实测的产物。
  换硬件要重测。
- **不声称**审计报的 111 条嫌疑已经穷尽:静默吞异常那 69 条本车道**没有逐条复核**。
