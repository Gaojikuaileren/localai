# V42 · P5 语音第二批 —— 把 V38 留下的那一格补上(**D?**)

> ★ **本包不取号**(写 `D?`)。★ 号写在**抬头这一行**:D 号闸看不见第 6 行的元信息
> (V37/V38 都在这上面栽过一次)。
>
> 车道:V42(worktree `.claude/worktrees/gate-audit-v36-f9c4f5`,分支 `claude/v42-p5-second-batch-b7ef95`)
> 依据:`decision-packets/p5-voice-survey-2026-08-11.md` §3 那张 19 行派工表
> + `decision-packets/v38-p5-first-batch-2026-08-15.md` 的移交。
> 边界:拥有 `10-core/speech/**` · `InterpretPanel.cs` · `InterpretState.cs` 的语音部分。
> **未动**:`10-core/gateway/**` · `lan-edge/**` · 任何 `Selftest*.cs` · 中央三文档 ·
> `90-ops/**` · `config/**`(★ 红测期间临时改过 `vram-budget.toml`,**已按字节还原**,见 §6)。
> **不出包。** 契约欠债 **DEBT = 1/31 未变**。

---

## §0 一句话

**派工任务给我的三条,V38 已经做掉了两条半;我核实之后没有重做,而是去补 V38 自己点名留下的那一格
——`speech.full` 从来没有人量过。量完发现一件不推不知道的事:**
**两个语音档位的收费与实测**朝相反方向偏**(lite 收少了 0.23、full 收多了 0.23),**
**所以「照 lite 的偏差去修 full」会把一个本来保守的数推向 fail-open。**

★ 另外要更正派工单里的三处事实(下面逐条自证):**「两条留痕仍写着尚未接入」是过期的**
—— V38 在 `a2ab130` 就改掉了,全仓今天 `尚未接入` 在这两个文件里**零命中**。

---

## §1 ★ 我先去量了派工单说的话对不对(结论:三处过期)

任务里点名要我「别读转述,自己量」。量的结果如下 —— **三条里有三条需要更正**:

| 派工单说 | 实况 | 自证 |
|---|---|---|
| ③ CPU ASR 稳态延迟**没量过**,仓库里两个数差 15 倍 | **V38 已经量过**(派工表 #16 做完),并已推翻那两个数 | `a2ab130` 新增 `measure_asr_latency.py`;`launch.toml` `[measured]` 段在册 |
| ② CPU/GPU 那条裁定的**理由站不住** | **成立,而 V38 已更正掉**,并且比调查报的更严重(GPU 快一个数量级,且默认是坏的 —— cuBLAS DLL 雷已拆) | `launch.toml:44-62` 的更正段;`server.py` 的 `add_cuda_dll_dirs()` |
| ★ 两条留痕 `InterpretPanel.cs:17` / `InterpretState.cs:17` **仍写「采集/识别…尚未接入」** | ★ **过期** —— V38 已改,两个文件头现在都带时态、逐项标 ✅/🟡/❌ | `grep -rn 尚未接入 20-client-win/` 在这两个文件里**零命中**;`git log` 显示 `a2ab130` 改的 |
| ★ 安全形状「只登记别修,核一眼 V37 写全了没有」 | ★ **写全了,没重复写**(见 §4) | `v37-...md` §4,四小节俱全 |

⇒ 所以本轮**没有重做**任何一条,而是接着往下做。**这一节本身就是产出**:
派工单是照 V34/调查那一版写的,而 V38 已经把它推进了一大截。

---

## §2 ★★★ 本轮的实测数(派工要求「延迟实测的数」)

工具:`10-core/speech/measure_asr_latency.py`(V38 建,**本轮扩了三个开关**:
`--tier lite|full|both` · `--no-cpu` · `--repeats N`;**不带参数跑出来的东西与 V38 那次同义**)。

### 2.1 CPU(今天真在用的那一档,`int8`)

| 档位 | 2.1s 的话 | 4.4s 的话 | 8.2s 的话 | 加载 |
|---|---|---|---|---|
| **lite**(turbo)复现 A(r=5) | 6.92 | 6.90 | 5.80 | 5.10s |
| **lite**(turbo)复现 B(r=3) | 8.77 | 7.53 | 8.32 | 3.37s |
| ★ **full**(large-v3)**首次被量**(r=3) | **8.56** | **8.50** | **9.30** | 5.81s |

**① V38 的核心结论复现了:耗时与音频长短脱钩。** 8.2 秒的话有时比 2.1 秒的话还快。
**② 而区间上沿要往外推**:V38 记 5.4–7.8,本轮 lite 量到 **8.77**、full 量到 **9.30**。
  ⇒ 结论不变(每次都是这个量级),变的是上沿;差别来自机器当时负载,不来自被测对象。
**③ full 比 lite 更慢(8.5–9.3s),且同样与时长脱钩** —— 这一格此前**空白**。

### 2.2 GPU(★ 只是量,**没有启用** —— 改档是裁定)

**静卡**读数(nvidia-smi 4.9 GiB 已用 / 1–2% 占用):

| 档位 | 2.1s | 4.5s | 8.3s | 加载 | **显存足迹** |
|---|---|---|---|---|---|
| lite(turbo) | 0.21 | 0.23 | 0.27 | 3.38s | **2.26–2.30 GiB** |
| ★ **full**(large-v3) | 0.32 | 0.32 | 0.47 | 4.10s | ★ **3.82 GiB** |

### 2.3 ★★★ 于是那件不推不知道的事

| 组件 | 在册收费(`vram-budget.toml`) | 实测 GPU 足迹 | 方向 |
|---|---|---|---|
| `speech.lite` | 2.07 | 2.26–2.30 | **收少了** ~0.23 —— **fail-open** |
| `speech.full` | 4.05 | **3.82** | **收多了** ~0.23 —— 保守 |

> **⇒ 两档朝相反方向偏。** V38 移交里那句「`speech.full` 的足迹没人量过,**别照 lite 推**」
> 今天有了证据:谁若照 lite 的偏差(收少了)去调 full,**会把一个本来保守的数推向 fail-open**。

★ 本车道**没有**改 `config/vram-budget.toml` —— 理由与 V38 同(取决于 CPU/GPU 那条待裁,
且会改变准入闸放行的集合,该有 D 号)。**做的是让它「动一下就会红」**,见 §3。

---

## §3 ★ 两条断言(都在本车道的 `10-core/speech/selftest.py`,**没碰任何 `Selftest*.cs`**)

**① 钉住 `speech.full` 的收费 4.05** —— 此前**没有任何东西钉着它**(V38 只钉了 lite)。
   失败消息里直接写上「实测 3.82、与 lite **偏差方向相反**、别照 lite 推」。

**② ★★ 修一个我自己刚开的盲区。** 本轮往 `launch.toml` 加了嵌套表 `[measured.full]`,
   而那条「`[measured]` 里的显存读数不许叫 peak」的断言**只看顶层键** ——
   也就是说我一边加读数,一边让守它的闸看不见新地方。⇒ 改成**递归**收集所有层的键名。

> ★ 这与 V36 那批「静默失效」同一形状:**闸还绿着,只是它已经不看那块地方了。**

### 红测(★ 判据是**读**输出,不是数命中 —— V38 在这上面栽过)

| 红测 | 做法 | 结果 |
|---|---|---|
| ① `speech.full` 收费 | 把 `vram-budget.toml` 的 `peak = 4.05` 改成 `3.82` | **FAIL=1 · exit 1**,消息**完整打印**、套件活到汇总行;改回即绿 |
| ② 嵌套盲区 | 往 `[measured.full]` 注入 `gpu_vram_peak_gib` | **旧判据(只看顶层)`[]` → 恒绿**;**新判据 `['gpu_vram_peak_gib']` → FAIL=1 exit 1**。★ 两个都印出来了,盲区是**证出来的**,不是断言出来的 |

`speech/selftest.py`:**PASS=29 FAIL=0**(V38 是 28,本轮 +1)。

---

## §4 ★ 安全形状:V37 那道裁定题**写全了,本轮一个字没加**

任务要求「核一眼它写全了没有,别重复写」。核了 `v37-phantom-and-dead-code-2026-08-15.md` §4:

- §4.1 三处出处逐条列全(`server.py:342` 只绑回环 · `server.py:133` 回环即可信 ·
  `check_backend_auth.py:100-105` 鉴权豁免),并写明「合起来那句话仓库里没写过」;
- §4.2 用大白话讲清这道题问的是什么,并点名 **P6 的 `ai-exec` 就是「同机另一个进程」**;
- §4.3 三个选项 A/B/C,每个都写「**你会经历到什么**」;
- §4.4 顺带登记了 `check_backend_auth.py:102` 那句「FastAPI」说错框架。

⇒ **齐全,且措辞是对用户说的**。本轮**只登记不修**,与任务一致。

---

## §5 ★ 判定「依赖用户裁定 / 不属本车道 ⇒ 本轮不做」的

两问仍在等用户(`p4-realmachine-findings-2026-08-09.md` ⑫):**交互形态**(按住 vs 点击)·
**入口位置**(只在同传页够不够)。凡答案取决于它们的一律没做:

| 派工表 # | 做什么 | 为什么本轮不做 |
|---|---|---|
| 6 | 按住 vs 点击 | **裁定**。★ 本轮的数直接压在这条上:CPU 档松手后要等 **6–9 秒**,GPU 档 **0.2–0.5 秒**。「按住」在 6–9 秒下是一种体验,在 0.3 秒下是另一种 —— **这两条裁定其实是同一道题,建议一起拍** |
| 7 | 语音入口位置 | **裁定** |
| 4 | TTS 客户端半边 + 播放 | 半依赖入口裁定;且 `SpeechClient.cs` **不在**本车道边界内 |
| 8 | CPU/GPU 改档 | **裁定**。理由已由 V38 更正、雷已拆、代价本轮量全(含 full)⇒ **它现在是一道数据齐备、可以直接拍的题** |
| 9 | 收费改成多少 | **裁定**(见 §2.3)。本轮只钉住、不拍数 |
| 1 / 2 / 3 / 5 | 网关代理 · provenance 判序 · 别名 · ASR→记忆 | 网关/记忆地盘,**禁区** |
| 11 | `speech/selftest.py` 进门禁 | `90-ops/**` **禁区** ⇒ 登记(§6) |
| 12 | speech 后端鉴权 | **裁定**(V37 §4 那道题) |
| 17 / 19 | 任务抽屉 · `RequiredModels` | `ModelReadiness`/`HubGpu`/`Selftest*.cs`,**禁区** |
| 10 / 13 / 14 | 权重搬家 · 中文语音 · 许可台账 | 裁定 / 合规线,**至今无人认领** |
| 15 | 真麦克风实机按一次 | 要**实机 + 人手**,D104 自己写着这一格要实机划 |
| 18 | 两个文件头留痕 | ★ **V38 已做完**(§1);`registry.toml` 那半仍等 #3(别名),提前改就是写好的谎 |

---

## §6 ★★ 交给 V36 的断言 / 门禁登记清单(**本车道一条都没动**)

1. **`10-core/speech/selftest.py` 进门禁**(派工表 #11)—— 今天 **29 条断言零覆盖**。
   `run-tests.ps1` 全文 `speech` **只出现一次**,在 `:680` 一句关于后端鉴权的注释里(我逐行看过)。
   先例是 `90-ops\debug\selfcheck.py` 被**显式**接进去。★ D103 §5.2 交给 V3,**今天是第 16 天**。
   ★ 本轮新增的 `measure_asr_latency.py` 同样不被 `test_*.py` 的扫描规则收到 —— 但它是**量具不是断言**,不该进门禁。
   ★★ **本轮替这条清掉一个障碍**:`speech/selftest.py` 在 **PATH 上的 system python**(也就是 `run-tests.ps1`
   跑门禁用的那个 `$SysPy`)下**一样是 PASS=29 FAIL=0 · exit 0** —— 它用的是桩引擎,
   **不需要 speech venv**。⇒ 接它进门禁**不必**额外铺 venv 那条路,照 `selfcheck.py` 的先例显式加一条即可。
2. **`test_vram_gate.py:28-35`** 读真 config 断言 `speech.lite == 2.07`,`:46/:57/:62/:68` 整段算术钉在这个数上。
   ⇒ 收费一旦按裁定改,**必须同轮改它**。★ 本轮新增的 `speech.full` 钉子(§3①)也要一起改。
3. **`check_backend_auth.py:102`** 那句「speech 是自写的 **FastAPI** 服务」—— **不是 FastAPI**
   (`ThreadingHTTPServer`,`server.py:342`)。豁免判定本身不受影响,错的只是理由里的技术栈名字。
   ★ V38 §8 与 V37 §4.4 都登记过,**至今没改**;`90-ops/**` 是禁区,本轮仍只登记(第 3 次)。
4. **`config/vram-budget.toml` 的 `[presets.*]` note**(「余 0.53」「加 speech.lite 就超(9.26)」)——
   收费一改就成假话,要同轮改。

### ★ 红测期间碰过 config 的如实交代

红测 ① 需要真改 `config/vram-budget.toml` 才能证明那条钉子会红。做法:**按字节改、按字节还原**,
还原后 `git status config/` **为空**、`git diff config/` **无输出**(两条都在会话里跑过)。
⇒ **本包提交里不含任何 `config/` 改动**。

---

## §7 ★ 本车道的取证边界(别把没量过的读成量过了)

**我实测的**:CPU lite 两轮复现 · CPU full 首次 · GPU lite/full 静卡与争用卡各一轮 ·
GPU 显存足迹(装载后与跑完全部样本后各读一次)· `speech/selftest.py` 全绿 + **两次红测**(都读了输出)·
两个客户端文件头的现状(grep + `git log`)· `run-tests.ps1` 全文 `speech` 命中 · V37 §4 全文。

**我没做的 / 没量到的**:
① **没有按真麦克风**(派工表 #15 仍要实机划);
② **没有量中文、没有量真人声** —— 音频仍是 Piper 合成的**英文**(本机只有一个英文语音)⇒ 上面的数是**乐观下界**;
③ **没有量 >30 秒的音频**(多窗那一段)—— 「与时长脱钩」这条**不许外推**到 30 秒以外;
④ **没有起过 `server.py` 这个服务** —— 量的是引擎层,**不含 HTTP 那一跳**(与 V38 同,如实留白,不写成量过了);
⑤ **没有改** `config/**` · `DECISIONS.md`/`STATE`/`PROJECT_PLAN` · `Selftest*.cs` · `90-ops/**` · `registry.toml` · 网关;
⑥ ★ **GPU 秒数对同卡上别的程序极敏感**:本轮撞上用户的 `UnrealEditor`(61% 占用),
   同一档 lite 从 0.21–0.27s 变成 0.61–0.73s,一度量出「lite 比 full 还慢」这种**不可能的排序**。
   ⇒ 上表的 GPU 秒数**取自静卡**;★ **显存足迹**则不敏感(装载后与跑完一致)。
   **教训**:GPU 秒数不注明当时卡上有什么,就是一个不能复现的数。
⑦ ★ 一次**我自己猜错、被自己的读数否掉**的推断:我以为同一档三次量出 3.33/3.81/4.05 是
   「更长的音频在后面才申请显存」,加了高水位读数去证实 —— **结果否掉了那个解释**
   (装载完就到位:lite 2.296→2.295,full 3.819→3.823)。真因是**别的程序污染差值法**。
   读数留着(它便宜,且是唯一能把「真长了」与「被污染了」分开的东西),**解释改了**。

---

## §8 门禁与账

- `10-core/speech/selftest.py`:**PASS=29 FAIL=0**;两次红测各 **FAIL=1 · exit 1**,改回即绿;
- 契约欠债:**DEBT = 1/31 未变**;
- **不出包**(任务明令);
- 中央三文档:**未动**。V38 §7 那三条更正请求(RTF 0.385 · D103 裁定⑤ · 「常驻」)**仍然挂着**,
  本轮**再登记一次**,并补一条:★ 更正 RTF 那句话时,区间请用 **6–9 秒**(V42 复现值),不要用 5.4–7.8。
