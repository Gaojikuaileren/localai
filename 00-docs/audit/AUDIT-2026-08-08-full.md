# 全项目审计 · 第三轮 · 2026-08-08

> 审计人:项目审计总监(前两轮同一人;★ 本轮是**全新会话、无上轮记忆**,一切从文档重建)
> **审计跨度**:`271e34c` → `207b7d3` —— **审计期间 main 前进 8 个提交**(V14 管理端 · V16 Broker 卸载重整全部并入)
> 基线工作树:`../.localAI-v11b`(分支 `v11b/audit-round3`),钉在 `271e34c`
> **零生产代码改动**:`10-core/**` 与 `20-client-win/**` 一个字节未动。中央四文档只读。
>
> 方法:**中心问题三块 + 八维度并行 → 每块一名独立反驳者(默认立场「它错了」)→ 完整性批评 → 差量复核**。
> 共 **24 个 agent · 1515 次工具调用 · 2 小时 09 分**。**每条发现都被至少一个独立的人试着反驳过。**

> ### ★ 本次运行:**已完成**
> 报告写完 · 已合进 `main` · `git status` 干净 · 提交号见文末与 `_LAST-RUN.txt`。
> 没有卡住的步骤。未做完的部分逐条列在 §5 覆盖账,**不是"卡住"而是"本轮有意没做/做不了"**。

---

## 0. 任务书里两个数字的更正(先说,免得后面对不上)

| 任务书写的 | 实际 | 依据 |
|---|---|---|
| 基线 `fcb92bd` | **`271e34c`**(下达任务时 main 已前进 1 条) | `git rev-parse HEAD` |
| 「S6 那 **25** 项仍 0 打勾」 | **21 项**,打勾 **0** | `grep -cE "^\s*- \[ \]" 90-ops/lan/S6-acceptance-checklist.md` = 21 |

---

## 1. ★★★ 中心问题的答案

> **4197 条断言全绿,而三格核心功能在真机上开不起来。为什么没有一条断言拦住?**

### 1.1 三个分问题,三个实测出来的数

**① 「构造出来的输入」这一族有多大 —— 比想象的小,而且已经被数清了。**

分母 = `10-core/**/test_*.py` 全部 **32 个文件**(`os.walk` 枚举,不手写名单),用 `ast.parse` 提取替身
(★ AST 结构上不含注释,字符串字面量不可能成为赋值目标 ⇒ 陷阱 5/6 被结构性绕开,不是靠"我记得排除了")。
提取器元断言:原始命中 **127 ≠ 0**,没有静默失灵。

```
替身总数 N = 59 处 / 15 个符号
  合法(结构上够不着:硬件 / OS socket / 上游进程 / 文件系统)  M = 32 处 / 11 个符号
  ★ 打在判据上(答案是「我们自己的代码」)                     K = 27 处 /  4 个符号
  其中已一路钉到生产入口                                       J = 15 处 /  2 个符号
  ⇒ K − J = 12 处 / 2 个符号  ← 今天仍然断着
```

那 2 个符号是 `gateway.membership.active_device`(11 处)与客户端 `LoopbackDemotedWhy` 的喂食口。
**两条各做了一次变异红测,三个方向变异全绿**;同一套变异台的阳性对照当场红 5 条 / 9 条 ⇒ 台子能红。

C# 侧:**monkeypatch 类替身 0 处**,替身只有真 `TcpListener` 与环境变量重定向,**全部合法**。
它的「构造输入」是另一种形状(约 48%–56% 判据的输入是 .cs 源码文本),而这一条项目自己已用
`build-client.ps1:184` 的 SRCHIT/SRCMISS 记着账(开发树 1901 / 发布产物 852,**1049 条静默消失**)⇒ **不算发现**。

**② 「源码里有没有那个词」这一族 —— 大,但绝大多数是有意的。**

```
全仓静态断言 3332 条 · 判据是源码文本的 1254 条(37.6%)
  ├ 合法且必要(反向断言/顺序判据/禁用 API)        绝大多数
  ├ ★ 判词说行为、判据是「某句注释在不在」          17 条
  └ ★★ 实测证明恒真(注入它自称禁掉的东西,一条不红)  6 条
```

★ 那 6 条恒真的里有 4 条是本仓**已经记了三次**的 §3c 形状(用剥字符串的 `CodeOnly` 去查文案),
以及一个**新形状**:`or` 的第二个析取项被一句**永久为真的注释**满足 ⇒ 整条断言恒真。
**ASSERTION-PITFALLS 只钉了「注释把断言弄红」的方向,没钉「注释把断言弄绿」的方向。**

**③ ★★ 缝的普查表 —— 那个能变短的数**

> ## **17 类缝 / 完全覆盖 1 · 结构性豁免 1 · 半覆盖 2 · 欠 13**

**N 怎么枚举出来的**(两根轴相乘,两根都靠机械扫描,不是手边那几个):

- **轴 A(谁和谁)**= 独立构建/部署的产物,枚举源 `find -name "*.csproj"` + Python 入口 + `90-ops/*.ps1` ⇒ 今天 7 个。
  ★ 复核过空目录(`10-core/{job-service,session-service,orchestrator,mcp-tools,trade-stub}` 与 `40-integrations` 各 0 个代码文件),不是漏扫。
- **轴 B(通过什么)**= 生产源码里**实际用到**的跨边界原语,对固定 API 家族表逐条打印命中数,
  **零命中的照样列出来**(陷阱 7)⇒ 15 个家族(14 有命中 + 1 零命中:DB schema 今天不存在,登记为零)。
- 凡一个原语承载**多个能各自独立漂移的契约面**就拆开 ⇒ **17 类**。

**「被覆盖」的判据(严格,两条都要)**:①从结构源枚举该类**所有**实例(不是手写名单);②未登记/未成对**即判红**。
> 「有一条针对某个实例的成对断言」**不算覆盖** —— 那是深度,不是广度。

| # | 缝的类别 | 元规则 | 谁来让它变短 |
|---|---|---|---|
| ① | 跨进程 HTTP **路由型**响应契约 | ✅ D95(实跑 TOTAL=30 PAIRED=29 DEBT=1) | 第 0 条车道裁定 `GET /health` |
| ② | HTTP 响应契约 · **非路由型**(MapFallback) | ❌ `_MAP_RE` 结构上抓不到 | 门禁车道 |
| ③ | HTTP **请求体**契约 | ❌ D95 明写不在范围内 | 门禁车道 |
| ④ | SSE **每一帧**的契约 | 🟡 半(路由枚举了,帧形状不定义) | GPU/同步车道 |
| ⑤ | `error.type` **值域**词表 | ❌ 只钉顶层键集合 | 门禁车道 |
| ⑥⑦⑧ | 命令行动词+参数 / 退出码 / stdout 文本 | ❌ 三类全欠 | V14 车道 |
| ⑨ | 跨语言 **JSON 文件**格式契约(store.json) | ❌ 且唯一相关测试把读取器整个替身掉 | 证书/配对切片 |
| ⑩ | 跨语言 **TOML** 格式契约 | ❌ 四个各自手写的解析器 | 第 0 条车道 |
| ⑪ | 端口/上游地址常量 | ❌ | 门禁车道 |
| ⑫ | 跨语言档位/动作词表 | 🟡 半(V13 为**这一个**装了端到端,没有元规则数「还有几个」) | GPU 车道 |
| ⑬⑭⑮ | 启动时序 / 注册表 / ACL 账户 | ❌ | V14 车道(⑮) |
| ⑯ | 出厂产物目录布局 | ❌ | V14 车道 |
| ⑰ | 跨进程无负载信号(锁文件+命名事件) | ✅ **结构性豁免**:两个 csproj link 同一份源码 | 不需要 |
| (0) | 数据库 schema | — 零命中,登记为「今天不存在」 | — |

**★ 这个数怎么变短(最短的三刀)**
1. **门禁车道一刀减 2** —— `_MAP_RE` 加 `Fallback`(②)+ error.type 值域纳入登记(⑤)。**都在 `check_contract_pairs.py` 里,不动生产代码。**
2. **证书/配对切片减 1** —— store.json 成对断言(⑨),后果最重的一条。
3. **V14 车道减 3** —— 它正在新造 ⑥⑦⑧⑯ 的实例,**建表的最好时机是它落地那一刻**(D95 自己的先例:元规则合并当天就抓到 `POST /v1/gpu/intent`)。

**★ 这个数怎么会自己变大(可证伪性 —— 一个不能变大的欠债表是装饰)**:
今天它**不会**自动变大,这是它与 D95 那张表的**本质差距**——D95 新增契约不登记会判红,而这张表是人手维护的。
⇒ **本表的第一个用途,是说明它自己也应当成为一条元规则。**

### 1.2 一句话答案

> **不是「断言写错了」,是「断言长在缝的两侧,而没有一条元规则去数缝」。**
>
> D95 证明了一件事:**当一类缝有元规则时,欠债会被逼着变短**(27→30 条契约,欠债 23→1)。
> 今天 17 类缝里只有 1 类有元规则。V13 为「档位怎么解出来」补的那条端到端断言是**深度**的胜利,
> 而这一轮的答案是:**深度补一条,广度还欠 13 类。**

---

## 2. 各维度发现(经反驳后的最终定级)

> 计:**A 2 · B 22 · C 20 · D 13**。每条后面标**它被谁反驳过**。

### 2.1 A 级(2 条)

**A-1 · 「关栈入口」不关栈,而两份已并 main 的决议包据此把实机那条 ⚠ 记成解掉了** 【维度③ · 反驳者:维度③复核员(stands)· 差量复核(stands)】
V14 记进 main 的「关栈入口」那条路径上**一个停栈动作都没有**。实机场次 ⚠ 四条里的「无法测:压力即让 / 任务暂停恢复 / 证书归因 —— **缺停栈入口**」被两份决议包引为已解决,**并在它上面算了拆分权衡的净额**。
★ 这是本轮最典型的一条:**不是代码坏了,是账记错了,而后续推理压在那笔错账上。**

**A-2 · 十一个维度共用了一个当场就能证伪的前提:「本机没起栈,所以只能读码」** 【完整性批评 · 反驳者:差量复核(stands)】
实测:网关 **PID 23904** `python -m uvicorn gateway:app --host 127.0.0.1 --port 8080` 自 **2026-08-08 03:36:57** 起一直在听,
**实打 `GET /health` → HTTP 200 `{"status":"ok"}`**;lan-edge **PID 15664** 已跑 13 小时;embedding **PID 8928** 在 18084。
审计工作树 `.git` 创建于 05:16:52 ⇒ **每一个维度都跑在「网关已经起了 1 小时 40 分」之后**,而十一块里没有一块打过一个真请求。
★★ 更刺眼的:**同一轮的维度⑧自己把 lan-edge 记下来了** —— 事实在报告里,前提在另一块里,**没有人把它们放在一起**。
⇒ 这正是 STATE 第 5 条陷阱那句「把栈起起来再跑一次全套,凡是『只在系统没跑起来时才绿』的断言那一刻会全部现形」——**而今天栈是起着的,没人利用**。

### 2.2 B 级(22 条 · 摘要)

**中心问题带出的**
| # | 发现 | 反驳者 |
|---|---|---|
| C1-F1 | `membership.active_device` 是副机身份唯一来源,整条判定链**零断言**;全仓测试里它只以一个 lambda 替身出现过。**三个方向变异全绿**(恒放行 / 恒拒 / store.json 键名改大小写) | 维度复核(stands)+ 差量(stands) |
| C1-F2 | 客户端「回环档位不够退回 Edge」降级路:20 多条断言围着它,而**喂给它的输入在生产里由谁写入,零断言**。把整个喂食口掐死(`NoteLoopbackDenial` 首行 `return false`),自检 **PASS=2140 逐位不变**;阳性对照同文件改一行 ⇒ PASS=2135 FAIL=5 | 同上 |
| C2-1..4,C2-6 | **6 条实测恒真断言** + 17 条「判词说行为、判据是某句注释在不在」。含**新形状**:`or` 第二个析取项被一句永久为真的注释满足 | 维度复核(5/5 stands) |
| S1 | store.json 的 C#→Python 文件格式契约零跨语言断言。★ **复核员推翻了作者的取证路径**(改 `Opt` 命名策略当场红,因为 Store.cs 读写不对称),**并用一个更硬的变异证明了缝本身**:给 `Devices` 加 `[JsonPropertyName("devices")]` ⇒ identity 三个自检**全绿**(15/0 · 42/0 · 14/0)而 Python 侧 fail-closed | 维度复核(REFUTED→B,机制被加强) |
| S2 | lan-edge `MapFallback` 吐着有真实消费者的响应契约,D95 提取器结构上抓不到(V11 的 C-A1 在 main 上仍成立,且现在可量化) | 维度复核 + 差量 |
| S5 | `error.type` 是**值域**契约而 D95 只钉顶层键集合:服务端 21 个词 / 客户端读 6 个 / 其中 2 个断言侧零命中 / 另 15 个一个字没读。改一个词,顶层键集合一个字不变 | 维度复核 |

**其余维度**
| # | 发现 | 反驳者 |
|---|---|---|
| D1-F1 | **A3 残留**:STATE 的「下一步」整段过期,与同一份 STATE 顶部的 08-07 实机场次**直接打架** —— 上一轮点名三处里唯一一字未动的那处 | 维度复核 + 差量 |
| D3-B1 | 开机那次连接探测**只跑一次、不重试、无定时重探**,唯二其它入口都在设置页按钮里 ⇒ 实机那条「副机要手动进设置滑到底才连」的机制找到了 | 维度复核 + 差量 |
| D4-4 | V13 那句「不是 V13 引入、此前一次都不可能被触发」**与本仓 08-07 自己的记录矛盾** | 维度复核 + 差量 |
| D6-F1 | 欠债表唯一那条的 `state` 与注记**与代码不符**:`GET /health` 服务端半边 2026-07-29 就存在且**今天真的会红**(实测:响应体加一个键 ⇒ `test_lan_edge_policy.py` 9 PASS·1 FAIL·exit 1) | 维度复核 + 差量 |
| D6-F2 | 非 paired 条目的 `state` 是**无人校验的自由文本**(none/server-only/client-only 三种写法门禁输出**逐字节相同**)⇒ **任何以 state 值实现的豁免天生是后门** | 维度复核 |
| D6-F3 | 「遍历源写死三个服务」**已不是将来时**:embedding_service(:18084,**此刻在跑**)三条路由 + 另一进程里的响应体解析器,今天一条都不在那 30 条账里 | 维度复核 |
| V5-1 | 本仓 **14 条机械化护栏全部装在「已经决定提交」之后**;「根本没到提交那一刻」这一族护栏数 = **0**,而它两日内发生 4 次 | 维度复核 |
| V5-3 | 根因**不只在措辞**:带了「收工判据」三条的 V16 与没带的 V15,零提交暴露窗口**一样长(77 分 vs 77 分)** | 维度复核 |
| I8-1 | store.json 的「原子替换」在**任何读者持有句柄**时整条失败(实测饱和读者下 **89.6% 的写被拒**);而 Store.cs 唯一那段并发防护装在**实测根本不出事的读者一侧**(0/8086 命中)—— **风险方向与防护方向相反** | 维度复核 |
| C-2 | 项目自己的假防护扫描器:「恒真断言」这一类今天命中 **0**,而 **0 在它的输出里根本不出现**;对本轮实测确认的 4 条恒真断言 **0/4 命中** | 差量 |
| C-3 | **`dist\host`(此刻正在跑的中枢那一半)全仓没有任何脚本产它**、没有 VERSION.txt / SHA256.txt,两个「产物身份」判据**都只看 `dist\client`** | 差量 |
| N1 | (差量新发现)`InstanceLock.cs:63` 把一条**不存在的护栏**当既成事实引用,并作为设计依据:「D106 裁定②已经把那八步逐字钉住(SequenceEqual)」—— 全仓无任何 `SequenceEqual`,只有一句 `Contains` | 差量自验 |
| N2 | (差量新发现)`e69cfc9` 把角色判据指向物换成 `..\admin\localai-admin.exe`,而**全仓没有任何脚本产出它**(`dist/admin` 不存在,`build-client.ps1` 零命中 admin)⇒ `AdminAppPresent` 今天**结构上恒为 false**,`DecideRole` 第②条肯定证据恒不成立 | 差量自验(三个方向自我反驳未推翻) |
| ★ Z1 | **(审计总监自查,非 agent)`run-tests.ps1:845` 硬编码「主树里是 1901 条」** —— 见 §2.4 | 自验 |

### 2.3 C 级(20 条)/ D 级(13 条)

C 级要点:契约表第⑤组「有没有消费者」按路径匹配不认服务(**今天就误判了**:`GET /health` 被算成有消费者,唯一命中的是 speech 客户端)· 同一服务里把路由挪进第二个 .cs 文件就能从账上消失 · D99 那条唯一生产触发点 `ChatView.cs:816` 零断言而守它的 4 条全是「源码里有没有那个词」· `identity-locators.json` 非原子整覆盖(唯一一个丢了中枢就起不来、又被明令排除出备份的文件)· 两个 detached worktree 是「车道收工释放分支名」的空壳但 `git worktree prune` **永远清不掉**(451 MB)· worktree 目录名**已不再标识车道**(v15 目录今天装的是 V17)⇒ 按目录名清理必然打错目标 · i18n 只有正向护栏(缺键后果是界面直接显示 `⟦key⟧`)。

D 级 13 条含 D4-1/2/3(**已随 V16 代码撤销**,见 §3.3)。

### 2.4 ★ 审计总监自查的一条:**门禁自己的覆盖账在说一个过期的数**

`90-ops/run-tests.ps1:845` 硬编码一句给人读的话:

```
'本次运行【没有跑过任何一条客户端断言】(主树里是 1901 条)。'
```

- 它写于 `21baf38`(**2026-08-06 17:55**),自那以后 `Selftest.cs` 改过 **14 次**,一字未动。
- 真实沿革(STATE 自己**每一次都逐套件对上一分不差**):`1893 → 1901 → 1927 → 1961 → 2032 → 2051 → 2074 → 2116`;
  **我本轮实测(worktree 现编 Release/win-x64)= 2140。**
- ★ 它落在 `if ($inWorktree)` 那一支 —— **D92 之后并行车道全在 worktree 里** ⇒ 这句话是**每一条车道都会读到的那一句**,而它把「你没跑的量」少报约 **239 条**。
- 08-06 审计的建议 ⑬ 正是针对这个形状(「把可计算的字段挪成 `90-ops/baseline.json`」)—— **该文件今天不存在**。

> ★ 形状:**它写下的那天是对的,而没有任何东西会在它变错的那天变红。**
> 这是 ASSERTION-PITFALLS §3b 的机制,落在了**项目自己的诚实性仪表**上。
> ★ 严守分寸:`baseline.json` 没做**不是缺陷**(那是一条建议);**硬编码的那句话是缺陷**,因为它做了一个**事实陈述**而该陈述今天为假。

---

## 3. ★ 如实收回的那一节

> 一份只会加码的审计不值得信。上上轮收回 11 条、上一轮 19 条 —— **本轮收回/降级 14 条**,其中 **3 条是被差量复核撤销的**。

### 3.1 被独立反驳者推翻或改判的(11 条)

| 原判 | 结论 |
|---|---|
| **S1** store.json:「C# 改一行命名策略 ⇒ 两侧全绿」,标 `实测` | **取证路径被推翻**。复核员真把 identity 复制出去改 `Opt` 并 `dotnet build` 跑自检 ⇒ **当场红**(`PASS=13 FAIL=1`),因为 `Store.cs` **写用 Opt、读不用**。作者漏读了这条不对称,且自认那半是读码却标了实测。★ **缝本身被复核员用更硬的变异证明并保留(降 B)** |
| **S4** TOML 四解析器 | **两条评级要件塌了**:①「这条约定只存在于实现里、文件一个字没写」是假的 —— `config/paths.toml:70` 白纸黑字写着;②「四个解析器」这个数不实。★ 复核员**把分家机制升格成了实测**(真编译 Paths.cs 跑四种输入,双引号/带空格节名两例 KeyNotFoundException)⇒ 降 C |
| **D2-01** D102/D106「核心不变式零守护」判 A | **降 C**。证据全对,但 **A 级判词塌在 D106 那一半**:D106 的标题句本身就是「它今天成立,而没有任何东西在守它」,其出处包第 3 行明写「本包不改任何生产代码」、§1 每条都带**红测步骤** ⇒ 那是**规格**,不是记账。**把要做的工作写成缺陷**,正是本轮明令禁止的第 3 条坑。★ 存活:`DECISIONS.md:4989`「已钉两个方向」是一句**已落地裁定里的假完成陈述**,与其出处提交 `b4fd524` 的交回直接打架且至今无留痕划掉 |
| **D2-02** `safe_to_stop_stack` 零调用点判 B | **降 D**。①证据句是假的:作者把一段**源码注释**(`gateway.py:839`)当成决议判词引用,`grep "给人看的" DECISIONS.md` **零行**;②反驳角度框错范围 —— 没跨分支查车道,而 V14 **已经把这条一模一样的实测写进提交了的源码**并记为 DEBT ⇒ 增量信息接近零 |
| **D2-03** 「PAIRED=29 虚高 3」判 B | **降 C**。他量的不是那个数声称的东西 —— `check_contract_pairs.py` 文件头有一整节「本文件**不覆盖**什么」明写了这一点。★ 存活一句没人说过的合成:元规则自己点名的**后备控制**(对方那条真打端点的元断言)对 speech 恰好**坐在一个门禁永不执行的文件里** |
| **D1-F2** 记忆系统离线判 B | **降 C**。「46 条断言钉着一个没人调的函数」**数错了** —— 实跑 `test_caller_policy.py` = **20 PASS**,其中引用 `require_trusted_local` 的只有 **6** 个节点 |
| **D1-F4** B19 判 C | **降 D**。唯一那条新增推断是假的:作者称「①已被留痕更正接住,说明有人来过这一段」——`git show d2ae23c` 证明 ①② 是**同一次提交一起写下的,从来没人回头改过**;作者自己那条 `git log -S` 只有一条命中,恰恰就是这个意思,**他读反了**。另有两个行号引错 |
| **D4-6** c83bf73「有意留着」判 B | **降 C**。①数错了(grep 命令没加 `-E`,`\|` 是字面量 ⇒ 0 行,不是 25 行);②引错小节(§11.5 在他自己的基线上是别的内容);③**定级理由是假的** —— 他把 B 压在「符合 D95 入表条件却没入表」上,而那张表数的是**跨进程线契约**,这条不属于 |
| **W4** `.claude/worktrees/` 不合 D82 | **整条收回**。①「布局不符」的前提立不住(D82 正文不含路径);②**他自称「实测排除」的危害,实测结论正好相反** —— 复核员发现门禁里还有第三个扫描器 `test_gpu_tool_isolation.py:73` 的 `rglob("*")`,`_SKIP_DIRS` 里**没有 `.claude`**,实跑遍历 2946 个文件、其中 **2448 个(83%)在 `.claude/worktrees/**` 下**。★ 复核员按自己的证据另立为新发现 |
| **C1/S 系列的 `verified_how`** | 多处「实测」实为读码,已逐条改标(CQ3 作者自己在 not_measured 里主动交代了两处) |
| **S4 取证句** | 差量复核发现 `git grep 'Paths\.(State\|RepoRoot)'` 的原始取证**在基线上也复现不出来**(子串把 `AppPaths.` 吃进去了),实质结论不受影响但取证句须替换 |

### 3.2 ★ 被差量复核撤销的(3 条)—— 基线在脚下移动

**D4-1 / D4-2 / D4-3(V13 撞出的三条,维度④原判全部 A)⇒ 全部降 D「已随代码撤销」。**

V16 的 `13878de` 重写了这三条:期望值改成按实测 `_footprint` 算并落 `STATE_READY`(①)· 给 `RECONCILING→READY` 补了三个实际驱动者并配了「白名单每条边要么有驱动者、要么进欠债表」的映射表(②)· 新增 `residency_truth()` 让候选池 = 全部登记端口(③)。

> ★★ **严格写成时间上的先后,不写成因果。**
> V16 首提交时间戳 **05:16:37**,本审计工作树 `.git` 创建于 **05:16:52** —— **相隔 15 秒,事实上并行**。
> **我没有任何证据说 V16 的改动与这次审计有因果关系。**
> 而且 V16 决议包对 ①③ 的更正(「根因不是语音」「`running()` 的候选池就是账本」)与审计员**独立**得出的结论逐句吻合
> —— 更像是两条独立路径撞到同一个形状。

**★ 撤销之后原样成立的部分(不删掉充数)**:
(a) 在 `271e34c` 上三条确实可达;
(b) ★ 最值钱的那句方法论存活并**已被 V16 机械化**:「`ALLOWED_TRANSITIONS` 的反向全表断言只保证**每个状态都登记了**,不保证**每条边有人走**」;
(c) 「①不是语音专有,默认入口是每一次普通开机之后的第一次取消勾选」这条**更正**(V13 说窄了)独立成立。

★ **同一天、同一个仓库,一类补了、另一类没补**:V16 为「状态机的边」立了那张映射表,而 D2-02 那条(`safe_to_stop_stack` 是「写好了却零调用点」的收尾函数)**没有进任何这样的表** —— 而 D102 裁定②亲手立的通则正是「凡写好了的收尾/清理函数,都必须有一条断言钉住它有调用点」。

---

## 4. 上一轮 A6 / B16 的闭环总账

> 上一轮实为 **24 条**(A1–A6 + B1–B12/B14–B19,B 有跳号)。逐条核到 `271e34c`。

**真闭环 16 · 半闭环 5 · 一行没动 3。**

**闭环质量不低**(不是打勾了事):A1 换了判据 + 补了跨语言成对断言 + 逐 kind 反向全表;A2 真 `dotnet build` 并解析 csproj 反向核对扫描根(零命中判红);A5 三样死代码全部接上调用点,且那句「不要点重新配对」的**劝阻文案已按实测改成「【只能重新配对】」**;B4 换成行为判据。

**今天真正还开着的三件**:
1. **A3 残留**:STATE 的「下一步」整段过期,与同一文件顶部的 08-07 实机场次打架(B 级,见 D1-F1);
2. **A4 的离线半边**:记忆系统在生产路径上零接线,`require_trusted_local` 至今零引用(C 级);
3. **B18/B19**:两条中央文档里过宽的断言句一字未改(D 级)。

**一条新形状**:**A6 的判据闭环了,但它「只报不判红」** —— 而今天那行黄字底下正压着修好 P4 三格 ❌ 的那些提交:`dist/client` 版本戳 `20260807-2340+6c3cbf0`,**落后 HEAD 9 个提交**(上一轮是 31)。装在机器上的客户端仍是 **08-07 暂停签字的那一版**。

★ 完整性批评顺手把 D1 明说「不声称」的一格结掉了:对 `dist\host` 的 dll 做二进制字符串计数(UTF-8 + UTF-16 各扫一遍)⇒ `ServerCertRotator` 1 · `RotationLoop` 3 · `CertLifecycle` 1 · `pair/enroll` 4 ⇒ **08-06 那批 dll 里确实含证书生命周期代码**;且 `10-core/{lan-edge,identity}` 自 08-06 23:19 起零提交 ⇒ **`dist\host` 今天恰好不旧**。

---

## 5. 其余维度的答案(逐条回题)

**② D99–D107**:九条里**七条经得起核**(D99/D100/D101/D103/D104/D105/D107)。任务书给的线索「TaskCenter 生产写入点 = 0」**今天已被代码推翻** —— 那是 D99 的**改动前**记账,D99 本身就是修它的那条(如实标 D)。D102/D106 那一对的问题**经复核降为 C**(见 §3.1),存活的是 `DECISIONS.md:4989` 那句**假完成陈述**至今无留痕划掉。

**③ 实机四件**:两件已被车道认领(任务抽屉 = V14 真做了 · 「不是启动即连」= V15 查过并交接了一半),一件(S6 过度声称)**早在实机场次之前就主动撤回**、今天实数 **0/21**。真正的缺陷不在「没人认领」,而在 **A-1 那条认领本身是假的**。★ 残留:方案书第 3 行至今仍把 P3b 列进「已验收」,而它自己 §14 里 P3b 有 3 格不是 `[x]`、DECISIONS 里根本没有 P3b 的验收签字(C)。

**⑤ 活干完没提交**:**四次属实**,但仓库自己那份清单**把同一次事故数了两遍**。根因**不只在措辞**(V16 带了收工判据、V15 没带,暴露窗口都是 77 分)。真正的结构性事实是 **V5-1**:14 条护栏全在「已决定提交」之后。
★ 另两条结构性的:收工判据第②条「已合进 main」在车道工作树里**执行不了**(必须写进另一个会话正占着的工作树,而那是全项目唯一没有锁的串行点);worktree 目录被**跨车道回收**,而 `git checkout` 会把未提交的活**静默带到新车道的分支上**(实测 rc=0,已改文件与未跟踪文件全部跟过去)。
★★ 承载 V11 上一轮那 744 行的工作树目录**今天已经不在盘上了**。
**可装的护栏(带红测方案)**:见 §7 建议 ③。

**⑥ `GET /health`**:那条注记**今天不准**(见 D6-F1)。**推荐**:不走豁免路线 —— 因为 D6-F2 证明了 `state` 是无人校验的自由文本 ⇒ **任何以 state 实现的豁免天生是后门**。推荐让 `start-stack.ps1:144` **解析响应体**(顶层键集合),DEBT 归零且判据可为假。

**⑦ worktree / 分支残留**:**今天一个字节的活都没丢**。两个 detached worktree 都干净且都指在 main 的祖先上;6 个"孤儿"目录全是空的;5 个被 drop 的 stash 逐条比过、内容全部已被 main 或 `parked/selfpair` 覆盖。真正的残留是**磁盘 + 命名**(451 MB 死壳 + 目录名已不标识车道)。★ `parked/selfpair` 是**有意封存**,不是残留;但今天没有任何**机器可读**的东西知道它存在。**本轮未执行任何清理。**

**⑧ 自选面 —— 状态文件的写入耐久面**(崩溃中途 / 并发读者 / 盘满)。选它的理由:前三轮量的全是「接线通不通、判据真不真、权限拦不拦」,**没有一条问过「这一行写盘失败会怎样」**。结论:本仓「原子写」纪律执行得很整齐,**但两处例外恰好都落在最承重的身份文件上**(见 I8-1 / I8-2)。

---

## 6. 覆盖账 —— 没测到的、测不了的、我造成的机器状态改动

### 6.1 我造成的改动(全部)

- **新建 worktree `../.localAI-v11b`(分支 `v11b/audit-round3`)** —— 任务书要求。
- 该工作树内新建:本报告 · `_gate-full.txt` · `_client-selftest.txt`(后两者为门禁输出,**已从提交中排除**)。
- **在该工作树里 `dotnet build` 了客户端工程**(产出 `20-client-win/app/bin|obj`,`.gitignore` 覆盖,不进 git)。
- 跑了:`run-tests.ps1 -Full`(工作树)· `client --selftest` · `check_contract_pairs.py` · `scan_fake.py` · 一次只读 `GET /health`。
- 子 agent 在 **scratchpad 的整树副本**上做过变异红测(改代码 → 跑 → 比对还原),**仓库内零字节改动**。
- **`10-core/**` 与 `20-client-win/**` 一个字节未改**;中央四文档未改。
- ★ **本轮未执行任何 worktree/分支清理**,未动 `parked/selfpair`。

### 6.2 门禁实跑口径(★ 三种环境的数**不可互比**)

| 口径 | 结果 |
|---|---|
| **工作树 `-Full`** | **PASS=2136 FAIL=0 SKIP=1** · 客户端自检如实记进「没跑」栏(worktree 无产物) |
| **客户端自检**(工作树现编 Release/win-x64) | **PASS=2140 FAIL=0** |
| 契约成对元规则 | **252 PASS · 0 FAIL** · `TOTAL=30 PAIRED=29 DEBT=1` |
| `scan_fake.py` | 141 条嫌疑 / 5 类(★ 「恒真断言」这一类命中 0,而 0 **在输出里根本不出现**) |
| `dist/client` | 版本戳 `20260807-2340+6c3cbf0`,**落后 HEAD 9 个提交** |

★ **没有把输出接 `tail`** —— 全量落盘再筛(上一轮就是这么把失败那一段截掉的)。

### 6.3 没测到 / 测不了

- ★★ **最要紧的一条(A-2)**:栈整个审计窗口都在跑,而**十一块没有一块打过一个真请求**。所有 HTTP 行为结论都是读码或 TestClient,**这一整类模态本轮实际上仍然没被利用**。
- **全程零实机、零第二台机器** —— 所有「副机」都是构造出来的。
- CQ3 的**原语清单是手写的** ⇒ 轴 B 的完整性只**相对于那张清单**成立。没查过的传输面:WM_COPYDATA · 共享内存/MMF · COM · WMI · 剪贴板 · 计划任务 XML;`EnvVar` 96 处命中**没有逐条判断哪些是真跨进程的**(一个知道自己没做完的口子)。
- CQ3 的扫描器只吃 `.py`/`.cs` ⇒ **若还有第 18 类缝只出现在 `.ps1` 里,这张表看不见它**。
- 半覆盖那两类(SSE 帧 / 档位词表)**只读了 docstring 自述,没有逐条红测**。
- V11 §3.3 的 C-B1(自配对进程内委托)**没复核完,因此没进表** —— 今天看到的是两件不同的东西,关系没查清,**不敢照 V11 原话复述**。
- 「谁来让它变短」那一栏是**建议不是裁定**,没有验算过任何一条改法真的能落地。
- `scan_fake.py` 报的 87 条静默吞异常**仍未逐条复核**(上一轮 69 条,同样未做)。
- 记忆库 14 个套件门禁只跑 3 个,**11 个未跑**(含 `test_gate.py` 46 条)。

---

## 7. 建议的下一步

**当晚可做(全是把已经做好的东西接上或说实话)**
1. **`run-tests.ps1:845`** —— 那句「1901」改成**算出来的**(或直接删掉那个数,只留「本次没跑客户端断言」)。★ 这条最便宜。
2. **`DECISIONS.md:4989`「已钉两个方向」** —— 原地留痕划掉(它与自己的出处提交打架)。
3. **`InstanceLock.cs:63`** —— 那条引用了不存在护栏的设计理由(N1),要么补上 `SequenceEqual`,要么改掉理由。
4. **6 条恒真断言 + 17 条「判据是某句注释在不在」** —— 逐条修;并把**「注释把断言弄绿」这个方向**补进 ASSERTION-PITFALLS 第 1 条。
5. **`GET /health`** —— `start-stack.ps1:144` 解析响应体,DEBT 归零(**不要走 state 豁免**)。

**收口前必须做**
6. **A-1** —— 「关栈入口」到底关不关栈,以及两份决议包里据此算出的净额要重算。
7. **N2** —— `AdminAppPresent` 今天结构上恒为 false,`DecideRole` 第②条肯定证据恒不成立。
8. **⑨ store.json 成对断言** —— 缝的普查表里后果最重的一条,复核员已给出可为假的变异形状。
9. **门禁车道一刀减 2** —— `_MAP_RE` 加 `Fallback` + error.type 值域纳入登记。
10. **护栏(维度⑤)** —— 装在「会话要结束」而不是「已决定提交」那一刻。判据:工作树 `git status --porcelain` 非空 **或** `git rev-list --count main..HEAD > 0`。★ 红测方案:造一个有未提交改动的工作树 ⇒ 必须响;干净且已合 ⇒ **必须不响**(误报会训练人绕过它)。

**要你裁定的(待裁,本轮无人应答)**
11. **A-2 的处置** —— 栈一直在跑而没人用。要不要把「跑一次真请求」写进审计口径?
12. **`dist\host` 无人产出**(C-3)—— 它此刻正在服务,却没有任何脚本能重建它。
13. **17 类缝那张表要不要变成元规则**(§1.1 末尾)—— 今天它不会自动变大,这是它与 D95 的本质差距。
14. **worktree 残留清理**(451 MB + 目录名不标识车道)—— 本轮只出建议未执行。
15. 上几轮仍无裁定的:记忆系统接后端 · `EnableLUA=0` 与三个不存在的账户 · 三段预览 C5。

---

## 8. 本次审计不声称

- **不声称**报告写完时仍然准确:审计跨度内 main 前进 8 个提交,**写这句话的时候它可能又动了**。
- **不声称** V16 的三条修复与本次审计有任何因果关系 —— 时间戳相隔 15 秒,事实上并行(§3.2)。
- **不声称**跑过两台真机、跑过任何真实业务请求(只打过一次 `GET /health`)。
- **不声称** 17 类这个枚举是穷尽的 —— 它只相对于一张**手写的原语清单**完整(§6.3)。
- **不声称**那 6 条恒真断言是全部 —— 它们是**实测确认**的下界,而项目自己的扫描器对它们 **0/4 命中**。
- **不声称**上一轮 24 条的闭环判定全部独立复验 —— 被反驳者点名的两条(D1-F2 数错、D1-F4 推断为假)已更正,其余未逐条重验。

---

> 审计完成 2026-08-08 · 跨度 `271e34c` → `207b7d3`(审计期间 main 前进 8 条)
> 24 agent · 1515 次工具调用 · 2 小时 09 分
> 门禁实跑(工作树)**PASS=2136 FAIL=0 SKIP=1** · 客户端自检 **PASS=2140 FAIL=0** · 契约 **30/29/DEBT=1**
> 前三轮:[08-05](AUDIT-2026-08-05-full.md) · [08-06](AUDIT-2026-08-06-full.md) · [08-07 V11](AUDIT-2026-08-07-v11-toward-split.md)
