════════════════════════════════════════════════════════════════════
  收工便条 · 通宵收工单(第 0 条车道)
════════════════════════════════════════════════════════════════════

完成时间 : 2026-08-09 约 02:20(本机时间)
状态     : ✅ 五个阶段全部做完。**没有卡住的步骤。**
           ★ 有两处「做到了但有边界」,逐条写在下面「如实记的限制」里 —— 那不是卡住,是范围。

────────────────────────────────────────────────────────────────────
五个阶段 · 逐个交代
────────────────────────────────────────────────────────────────────

  阶段 1  从 V18 单提两点            ✅ 完成 —— ★ 但**不是今晚做的**
          213cbe9  ClientSourceRoot() 抽成一个函数(+ 反向钉:锚点只许一处)
          a5ba96a  元断言 _srcHit > 0(消灭「根在而一次都没命中」)
          bacb91d  PITFALLS 第 14 条 + V18 决议包照收(不取号)
          ★ 今晚**核实后未重做**:三条都在 main 祖先里;
            ClientSourceRoot 定义 1 处 / 调用 12 处;锚点字面量去注释后 1 处。

  阶段 2  管理端自检接进门禁          ✅ 完成    c17f164
          实跑:admin --selftest PASS=153 FAIL=0 SKIP=1,**单列不并进合计**(合计仍 4404)
          ★ 已红测:注入一条必失败断言 ⇒ FAIL=1 · 门禁未过 · **退出码 1**;还原后复绿

  阶段 3  文档收口                    ✅ 完成    5f4629f
          COORDINATOR-HANDOFF 第 4 节 + worklog 补 08-08 一整天

  阶段 4  未了项总表                  ✅ 完成    c69982b
          00-docs/OPEN-ITEMS-2026-08-09.md —— 扫 212 条,查实 41 条已解,登记 171 条

  阶段 5  重写实机验收清单            ✅ 完成    11d2e2f
          90-ops/lan/P4-acceptance-checklist.md

────────────────────────────────────────────────────────────────────
本次收工单产生的提交(按时间先后)
────────────────────────────────────────────────────────────────────

  5f4629f   阶段 3 · 文档收口
  11d2e2f   阶段 5 · 实机验收清单
  c17f164   阶段 2 · 管理端自检接进门禁
  c69982b   阶段 4 · 未了项总表

  ★ 本便条本身也会产生一条提交 —— 提交号见 `git log`。
    (不写"HEAD 会比 X 新几条":那种句子第二天就是假的。)

  远端:每个阶段结束都推过一次,四次全部成功,没有失败要记。

────────────────────────────────────────────────────────────────────
★ 如实记的限制(不是卡住,是范围 —— 但别读成"全做完了")
────────────────────────────────────────────────────────────────────

  ① 管理端自检在门禁里带 **1 条 SKIP**:「八步优雅退出的实跑验证」。
     它要 client 与 admin **并排**才跑得了(自检去找 ..\client\),
     而门禁跑的是 bin 里的构建产物,没有那个形状。
     ⇒ **只有 build-client.ps1 那一趟会真跑它。**
     ★ 这**不是**技术上做不到(把两个 bin 目录拷成并排即可),是本次**有意没做**:
       代价是每次门禁多拷数百 MB。**如实登记,留给用户裁要不要付这个代价。**
     ★★ 覆盖账里已经把这条 SKIP 单独说了一遍:**跳过 = 那条没被验过**,不是通过。

  ② 阶段 4 的扫描范围是**按日期**取的(`*2026-08-0[789]*`),
     而**欠债不按日期排队** —— 用户点名的三条(待决 6/7 · D81 待裁五条 · 记忆库 B 档 269 条)
     都落在窗口之外,是**手工补进去的**,不是扫出来的。
     ⇒ 下次要按「**所有还没被裁定关闭的包**」取范围。这条已写进那份表的 §1 末尾。

────────────────────────────────────────────────────────────────────
★★★ 早上第一件该做的
────────────────────────────────────────────────────────────────────

  **打开 `90-ops/lan/P4-acceptance-checklist.md`,先做 §0.1。**

  §0.1 是:把 `dist\admin-pack\localai-admin.exe` 放到客户端 exe 旁边的 `..\admin\` 里。
  ★ 为什么它必须是第一件:客户端判「这台是不是主机」的指向物,V14 裁定②起是
    **管理端装没装**,而 `dist\admin-pack\` **不是**客户端会去找的那个位置。
  ⇒ 这一步不做,清单 §2「客户端里该看不到什么」**整节会看起来像没搬成功** ——
    而真相只是客户端没找到管理端。**先做这一步,再往下走。**

  做完之后按清单往下:§1 管理端三页真的点开 → §2 客户端里该看不到什么 →
  §3 副机拉得到主机已有共享会话 → §4 五格 S6(至今 0 项打勾)。

────────────────────────────────────────────────────────────────────
今天的两个数(★ 三种口径不可互比,别加成一个总数)
────────────────────────────────────────────────────────────────────

  run-tests.ps1 -Full   PASS=4404  FAIL=0  SKIP=1   · 契约欠债 1/30
  admin --selftest      PASS=153   FAIL=0  SKIP=1   · **单列,不并进上面那个**
  出包(20260809-0113)  客户端两种形态各 1009/0 · 管理端出包形态 111/0

  ★ 客户端自检 2189 → 2092 的**下降是正常的**:D114 把 3100 行搬进管理端,断言跟着搬。
    详见 STATE.md 基线那一段(它把三个数分开写并注明了不可互比)。
