# V40a · 审计上半 · 2026-08-15 之后的改动面 · 2026-08-22

> 车道:`claude/v40-full-audit-86ae10`(**只读**,零代码改动)。
> **只审两族**:① 函数写好、零生产调用点 · ② 判据在替不存在的东西作证。
> 每条**独立反驳一次,默认反驳成立**;推不倒的才留下。
> ★ 交回时限内完成,**没有交半份** —— 覆盖账在 §6,写清了哪些是我**有意没做**的。

---

## 0. 窗口是怎么圈的(git 自己圈,不手写名单)

```
BASE = 9b72385  (2026-08-11,窗口外最后一个提交)
HEAD = 4b7fa28  (2026-08-20)
git rev-list --count 9b72385..HEAD           ->  25 个提交
git diff --stat 9b72385 HEAD                 ->  52 个文件 · +4556 / −338
git diff --stat 9b72385 HEAD -- *.cs *.py *.ps1 -> 14 个文件 · +2092 / −220
```

**窗口里的 14 个代码文件**(这就是本报告的全部靶面):

```
10-core/speech/measure_asr_latency.py  +240   20-client-win/app/Selftest.cs             +621
10-core/speech/selftest.py              +88   20-client-win/app/SelftestModelGate.cs     +77
10-core/speech/server.py                +37   20-client-win/app/SelftestUiPromises.cs    +40
20-client-win/admin/Selftest.cs         +66   20-client-win/app/Services/InterpretState.cs +88
20-client-win/admin/SelftestMoved.cs   +342   20-client-win/app/Views/InterpretPanel.cs  +30
20-client-win/admin/Services/HubAdmin.cs +133 90-ops/build-client.ps1                  +233
90-ops/gate/check_decision_numbers.py  +157   90-ops/run-tests.ps1                     +160
```

★ 任务书说「今天新增的那几千行」—— 实测**代码 +2092 行**,连文档 **+4556 行**。窗口比"几千行"小一点,先说清楚。

---

## 1. 结论,两句话

> **① 第七例:窗口里有,只有一个 —— `_heading_marks_draft`(D 号闸内部的死函数)。**
> **② 判据替不存在的东西作证:窗口里零命中。**
> 而**零命中不是不存在** —— §3 把分母、量法、以及"提取器坏了会怎样"全写出来了,请照着复核。
>
> ★ 另外:在**窗口外、但在上一次全项目审计(2026-08-08)之后**的那段里,
> 两族**各有真货**,而且其中一条是本轮形状最值得记住的一条。它们在 §4,标着"不在你圈的窗口里"。

---

## 2. 族 ①(窗口内)· 第七例:`_heading_marks_draft`

**在哪**:`90-ops/gate/check_decision_numbers.py:226` · 引入 `1186f7e`(2026-08-15)· **在窗口内**

**实测**(全仓,两处而已):
```
90-ops/gate/check_decision_numbers.py:192:  下面 `_heading_marks_draft` 的 docstring 逐字写着「只看抬头、不 grep 全文」,
90-ops/gate/check_decision_numbers.py:226:  def _heading_marks_draft(txt: str) -> bool:
```
—— **它自己的定义,加一句引用它的注释。零调用点。**

**它是怎么变成死的**:同一轮(第三次扩宽)写了 `_self_declares_draft`(`:188`)来取代它。
取代者**真的被调**(`:385` `if not _self_declares_draft(txt):`),而旧的那个没有被删。

**★ 这一条的形状比"多了个死函数"更值得记**:
**取代者的 docstring 反过来把被取代者当权威引用** ——
`:192` 逐字写着「下面 `_heading_marks_draft` 的 docstring 逐字写着『只看抬头、不 grep 全文』」。
于是这道闸的**理由链**挂在一个已经没有人执行的函数身上。
哪天有人按"死代码"把 `:226` 删掉,`:192` 那句解释就指向虚空 —— 而闸照样绿。

**独立反驳(默认成立)**:
1. 「它是不是通过别的名字被调?」——`getattr` / `globals()[...]` / 装饰器注册,全仓在这个文件里**零命中**;这个文件没有任何反射式派发。**反驳失败。**
2. 「它是不是被测试调?」——`90-ops/gate/` 下没有对应的 `test_*`;`run-tests.ps1` 是把这个闸当**脚本**跑(比退出码),不 import 它的私有函数。**反驳失败。**
3. 「留着是不是有意的(照 `DiscoverEdgeDialsAsync` 那样写了保留理由)?」——`DiscoverEdgeDialsAsync` 那处有整段「保留理由 + 删除条件」;**这一处一个字都没有**。**反驳失败。**

**判级:C**。它是闸**内部**的死函数,不影响任何判定,也不进任何用户可见的路径。
**登记,不要顺手修** —— 删它要连 `:192` 那句引用一起改,而那属于 D 号闸的车道。

**看得见的判据(今天没有)**:没有任何东西会因为这个函数死掉而红。
要让它可被发现,只能靠一条全仓的"零调用点"扫描 —— 而本仓今天那条元断言
(`SelftestMoved` 的「唯一入口必须有生产调用点」)**只认自称「唯一入口」的函数**,
且只扫 `20-client-win/**`,够不到 `90-ops/gate/**` 的 Python。

---

## 3. 族 ②(窗口内)· 零命中 —— 以及为什么这句话可以被复核

> ★ 本仓纪律:**零命中不是不存在。** 所以这一节写的是**分母**和**量法**,不是结论。

**量法**:从窗口的**新增行**里抽出所有 `Contains("<标识符>")` 的字面量
(即"某条判据声称某个符号存在于某份源码里"),再拿去**剥掉注释与字符串**之后的**生产**代码里搜。

**分母 = 9 个**(窗口新增行里全部这类字面量):

| 字面量 | 在生产代码里 | 判 |
|---|---|---|
| `DiscoverEdgeDialsAsync` | **在** | ★ 见 §4-②-1:在,但**只在它自己的声明行上**(那个函数零调用点) |
| `PairingChannelError` | 在 | 真符号,有调用 |
| `SecondsLeft` | 在 | 真符号,有调用 |
| `chat` | 在 | 工作空间键 |
| `MarkerSameLine` / `MarkerAfterUrl` / `MarkerAfterCharQuote` / `MarkerInComment` / `MarkerInString` | **不在** | ✅ **本来就该不在** —— 它们是 `SelfCheckCommentStrippers()` 自己造的夹具字符串(去注释器的自检台),不是产品符号 |

**⇒ 窗口内新增的判据里,没有一条在替一个不存在的符号作证。**

**元断言(判据自己坏了会红)**:
- 剥离器剥完之后,全仓 `Assert(` 计数 **2697 → 2687**:必须**变小**(证明它真的剥掉了东西)、且**不许归零**(证明它没把代码一起吃掉);
- 已知活符号 `AbortAll` 剥离后仍在;逐文件行数剥离前后**完全相等**;
- 抽取器的分母 **9 ≠ 0**(为 0 就说明是抽取器坏了,不是全清白)。

★ **这一节没有覆盖到的形状**(如实写,别把"没查"读成"查过没有"):
① 窗口里新增的 Python 判据用的是 `inspect.getsource` + 子串的形态(`assert_helpers.py` 那一族),**我没查**;
② 判据"在替不存在的东西作证"还有一种形状是**行为层**的(断言的对象存在,但那条路生产里走不到)——
族 ① 与族 ② 的交集就长这样,而本节只做了**符号层**。交集的那两条在 §4。

---

## 4. ★ 不在你圈的窗口里,但在**上一次全项目审计(2026-08-08)之后** —— 两族各有真货

> 上一次完整审计的终点是 `207b7d3`(2026-08-08)。从那里到今天是 **171 个提交 · 代码 +21809 行**。
> 下面四条落在 `207b7d3..9b72385` 那一段里 —— **窗口外,但没有人审过它们**。
> ★ 我把它们分开列,是因为"你圈的那个窗口里有没有"和"上次审计之后有没有"是两个问题,答案也不一样。

### ④-①-1 · **第七例(真正的那一个)**:`OpenProjectInChat` 零调用点,而一条断言宣布「入口就位」

`20-client-win/app/MainWindow.xaml.cs:828`(声明)· `20-client-win/app/Selftest.cs:480`(断言)

`git grep -n "OpenProjectInChat"` **全仓只有两行**:它自己的声明,与那条断言里的**字符串字面量**。
两个兄弟方法都有真调用方(`OpenProjectEditor` ← `ProjectLibraryView.cs:55`;`OpenProjectLibrary` ← `HomeView.cs`),它一个都没有。

断言原文:`Assert(mwStatus.Contains("OpenProjectEditor") && … && mwStatus.Contains("OpenProjectInChat"), "项目编辑/项目库/进项目聊天入口就位")`
—— **它读的是源码文本,而被它判活的那个方法的声明行本身就把它喂绿了。**
这与 `HubAdmin.cs:618` 已登记的那条是**同一个形状**,只是这一处**没有登记在任何地方**。

**独立反驳**:① XAML 里有没有绑?——`git grep OpenProjectInChat -- '*.xaml'` 零命中;② 是不是反射/命令绑定?——全仓只有那两行,不可能;③ 是不是"故意留着的将来入口"?——没有任何保留理由或删除条件的留痕。**三条全部反驳失败。**

**今天咬不咬人:不咬。** 活着的那条路是 `NavigateToProject(workspaceKey, projectId)`(`:772` ← `HomeView.cs:1768`),而且**更对** —— 它跳到项目**自己所属**的工作空间;`OpenProjectInChat` 写死 `Navigate("chat")`。
**将来会怎么咬**:谁把项目抽屉接上"进项目聊天"时顺手用了这个名字对得上的方法,
一个属于「翻译」空间的项目就会被送进「聊天」空间 —— 正是隔壁 `NavigateToSession`
注释白纸黑字警告过的那件事(「点 A 打开 B,静默错开,比不跳更坏」)。**判级 B。**

### ④-①-2 · **第八例**:`PttHealthAsync` 零调用点 —— 而**留痕逐字写着「此前零调用点,已修」**

声明 `20-client-win/app/Services/InterpretState.cs:362`;两句留痕 `20-client-win/app/Views/InterpretPanel.cs:105` 与 `:189`,
均由 `42527e5`(**2026-08-10** · V30)写入。

- `:105` —— 「★ 进页面探一次本机语音服务。★★ 这是 `PttHealthAsync` 这条路**第一个真实调用点** —— 在它之前那个方法零调用……」
- `:189` —— 「★★ 顺带修掉一处旧的静默:`PttHealthAsync` 此前**一个调用点都没有**……现在由闸来说,而闸是探出来的。」

**实测**:`git grep -n "PttHealthAsync"` 全仓 **4 处** —— 声明 1、上面两句注释 2、
`Selftest.cs:6951` 拿它的**声明字符串**当 `Slice` 下界 1。**零调用点。**
`InterpretPanel.cs:106` 真正调的是 `TheApp.Interpret.ProbeSpeechAsync()`(`InterpretState.cs:382`)。

**★ 形状**:**一句「我已经把零调用点修好了」的留痕,写在了一个并没有调用它的调用点旁边。**
`InterpretState.cs:379` 甚至解释了为什么两个方法要**分开留着** —— 设计意图是两个都活着,实际只有一个有消费者。

**独立反驳**:「是不是 `ProbeSpeechAsync` 内部调了它?」——**不是**,它直接 `await _speech.HealthAsync(ct)`;
两者是 `SpeechClient.HealthAsync` 的两个平行门面。**反驳失败。**

**今天咬不咬人:不咬。** 这一页**真的**探了,按钮的灰/亮与旁边那句说明都是探出来的。
**行为是对的,留痕是假的。判级 B。**
★ 处置留神:若选"删掉 `PttHealthAsync`",`Selftest.cs:6951` 的 `Slice` 下界会没,
`Assert(rel is not null, "切片得真的取到 PttReleaseAsync")` 会红 —— **而它给的理由是错的**。

### ④-①-3 · **第九例**:`CountFor` 零调用点(`10-core/lan-edge/Program.cs:1573` · `8617f17` · 2026-08-10)

它的文档逐字写着「自检用:断言登记表真的有东西,否则 `AbortAll` 恒返回 0 也叫通过」——
**一处为了防恒真而写的保险,而它从来没被装上。**

**独立反驳(部分成立 ⇒ 降级)**:它要防的那个恒真,已被同一段里
`Assert(killedInMs >= 0 && killedInMs < 200)` 实质挡住 —— 那条把「登记表 0 延迟(实测 ~20ms)」
与「流内周期复查(最快几百 ms)」分开,**把登记表整个摘掉它就红**。
⇒ 是一处**没装上的保险**,不是一个洞。**判级 C。**

### ④-②-1 · 已知第五例 `DiscoverEdgeDialsAsync` **至今仍是零调用点**,而守它的断言被它自己的声明喂绿

`20-client-win/admin/Services/HubAdmin.cs:622`(声明,零调用点)· `SelftestMoved.cs:1161`
`Assert(haSrc2.Contains("DiscoverEdgeDialsAsync"), "★ 换成返回【全部】通过校验的地址,由界面让人自己挑")`。

★ **仓库自己已经完整登记了这一条**(`HubAdmin.cs:617-620` 逐字:「那条断言**证明不了接线** ——
它读的是本文件的源码文本,而**下面这一行声明本身就把它喂绿了**」),并写了保留理由与删除条件。
⇒ **不是新发现。** 新的只是一个事实:**23 条车道之后它还在**,而那条断言仍在替它作证。**判级 C。**

★ 注:`SelftestMoved.cs:1161` 这一行在窗口的新增行里**出现过**(该文件本窗口 +342 行),
但 `git log -L 1161,1161` 显示它的内容来自 `54630d6`(2026-08-08)—— 是被搬动,不是被新写。
**这就是为什么 §3 说"窗口内零命中"而这里说"有"** —— 两者用的不是同一个"在窗口里"的定义,我把两个都写出来。

---

## 5. 被反驳掉的(默认立场「它错了」)

| # | 我一度认为的发现 | 反驳 | 判 |
|---|---|---|---|
| R1 | 窗口新增判据里 5 个 `Marker*` 符号全仓不存在 ⇒ 判据在替不存在的东西作证 | **反驳成立**:它们是 `SelfCheckCommentStrippers()` 的**自检夹具**,存在于测试自身的字符串里;"不在生产代码里"正是它们的设计 | **不报** |
| R2 | `SelftestOnDemand.cs:56` 断言里的 `vision.vlm` 全仓不存在 | **反驳成立**:纯函数测试自造的夹具 id;被测的 `ComponentPicker.InitialPermitted` 就是 `LoadAsync` 生产走的那一个 | **不报** |
| R3 | 窗口新增的 11 个 Python 函数 + 7 个 C# 成员里有更多零调用点 | **逐个查过,反驳成立**:`_resample_to_16k` `_sine_wav` `_synth` `_time_transcribe` `_wrap_wav` `_run_tier` `_emit` `add_cuda_dll_dirs` `_self_declares_draft` `_h1_names_real_number` · `ClientSourceRoot` `Skip` `Owed` `ReadNoTally` `IsStillCommentFed` `RunCommentFedGuard` `SelfCheckCommentStrippers` `CommentPinned` —— **全部有真调用点**。只有 `_heading_marks_draft` 一个是死的 | **只留 1 条** |
| R4 | 5 处"断言只被声明喂绿"的重言式 | 逐条核对后 **4 处假阳性**(`ChatOpener` / `DoubleAnimation` 命中的是**调用**而非声明;`SwallowClick` / `OpenProjectEditor` 别处有真调用方)。只有 1 处成立 ⇒ 已升为 ④-①-1 | **4 条不报** |
| R5 | 21 处路径字面量指向不存在的文件 | **反驳成立**:全是 `..\` 相对路径,逐个核对 **21/21 存在**(我的检查器不解析 `..`) | **不报** |

---

## 6. 覆盖账(不是"卡住",是有意没做 / 做不了)

1. **一次构建、一次红测都没跑** —— 车道边界是全仓只读。上面每条"零调用点"是用
   **剥离注释与字符串后**的引用计数 + `git grep` **双路**核的,**不是**注入验的。
2. **族 ② 只做了符号层,没做 Python 的 `inspect.getsource` 形态**(`10-core/gateway/assert_helpers.py` 那一族)。
3. **`10-core/memory/**` 与 `40-integrations/**` 不在窗口里,一个字没看。**
4. **窗口外那四条(§4)是顺带捞到的**,不是对 `207b7d3..9b72385` 那 146 个提交的系统性普查 ——
   那一段的完整结果在 `AUDIT-2026-08-22-v40-after-nineteen-lanes.md`。
5. **两条已跑的闸**(`check_decision_numbers.py` 7 PASS · `check_contract_pairs.py` 331 PASS / DEBT=1)
   是**在本工作树里**跑的;按本仓惯例全仓扫描类的闸要到主工作树再跑一次才算数,**我没跑**。

---

## 7. 交回

- **族 ①(窗口内)**:**1 条** —— `_heading_marks_draft`(C 级,闸内部死函数,取代者的注释反过来引用它)。
- **族 ②(窗口内)**:**0 条**。分母 9、量法与三条元断言在 §3,可复核。
- **窗口外、上次审计之后**:族 ① 又 **3 例**(第七 / 八 / 九例:`OpenProjectInChat` · `PttHealthAsync` · `CountFor`),
  族 ② **1 例**(已知第五例 `DiscoverEdgeDialsAsync` 仍死,断言仍在替它作证)。
  其中 `OpenProjectInChat` 与 `PttHealthAsync` **在任何地方都没有登记过**。
- **零代码改动**:本轮只写报告。**发现登记,不顺手修。**
