# 全项目审计 · 第四轮 · 2026-08-22 · V40

> 审计人:V40 只读车道(`claude/v40-full-audit-86ae10`)。**零生产代码改动** —— 本轮只产出本文件与用户版两份报告。
> **审计跨度**:`207b7d3`(上一轮全项目审计的终点)→ `4b7fa28`(main,2026-08-20)。
> 方法:六族并行普查 → **每条发现独立反驳一次(默认立场「它错了」)** → 推不倒的才留下 → 分 A/B/C。
> ★ 本文件不写会随时间变假的句子;每一条数都写清**是怎么量出来的**。

---

## 0. 任务书里三个数的更正(先说,免得后面对不上)

| 任务书写的 | 实测 | 依据 |
|---|---|---|
| 「上一次完整审计是 **2026-08-06**」 | **2026-08-08**(第三轮,`AUDIT-2026-08-08-full.md`) | `00-docs/audit/` 里有 08-05 / 08-06 / 08-07(v11 专题)/ **08-08(full)** 四份;08-08 那份自称"第三轮 · 全项目" |
| 「今天并入**十九**条车道」 | **23** 个 V 号(含子号 29 个) | `git log --format=%s 207b7d3..HEAD` 里出现的 `V<数字>` 去重计数 = 23:V0 V11b V13 V14b V15 V18 V20 V21 V22 V23 V24 V25 V26 V27 V28 V29(b) V30(b) V31 V32(b,c) V33 V35 V36 V37 V38 V39 |
| 「**87+** 提交,+数千行」 | **171** 提交(其中 42 个 merge);`.cs/.py/.ps1` **+21809 / −3930**(93 个文件);连文档 **+34916 / −3989**(184 个文件) | `git rev-list --count 207b7d3..HEAD` · `git diff --stat 207b7d3 HEAD -- '*.cs' '*.py' '*.ps1'` |

★ 三处都往**大**的方向差。这不改变结论,但它改变"这一轮该看多宽" —— 按 87 提交去抽样会漏掉一半。

---

## 1. 结论一句话

> **23 条车道各自证明的那个绿,大体上是真的。**
> 本轮**没有**找到"缝的两侧各自都绿而中间断了"的**功能性**断线
> (2026-08-08 那轮 A 级 6 条里占 4 条的那个形状)。
>
> 真正还在咬人的是**另一族**:**界面上的字指着一个不存在的东西** —— 三条,
> 全部**已登记、无人认领**,其中两条自 2026-08-11 起原地未动地穿过了这 23 条车道。
>
> 而「函数写好、零生产调用点」**确实有第七例**,而且**一共四例新的**;其中一例是
> **同一轮提交里,留痕逐字写着「此前零调用点,已修」—— 而它今天仍然零调用点**。

---

## 2. A 级(今天就会咬到用户)

> ★ A 级的写法:**它今天会怎么咬人**,不是"这不符合最佳实践"。
> ★ 每条都给出**一个具体改动能让它红** —— 凡我说"有断言守着",那句话必须是可执行的。

### A1 · 管理端把人指向客户端里**不存在**的「设备」页

**在哪**:`20-client-win/admin/Views/HostHubView.cs:660`
逐字:`"  ① 打开这台的客户端 →「设备」→ 点「开始寻找主机」;\n"`

**今天会怎么咬人**:主机那台还没配对时,管理端「主机中枢」页给的就是这条三步指引。
用户照做,打开客户端 —— 左栏只有 **首页 / 聊天(各工作空间)/ 扩展 / 设置**
(`MainWindow.xaml.cs:532`–`557` 的 `AddItem` 全表就这几行)。**没有「设备」。**
他会把设置从头翻到底去找 —— 这**逐字就是 2026-08-07 实机反馈那句话**
(「副机需要打开设置下滑到底才可以开始连接」),只是这次说错话的是管理端。

**为什么没有任何东西红**:`SelftestUiPromises` 这道护栏**看得见它** ——
它被写在 `KnownBroken` 登记表里(`SelftestUiPromises.cs:100`),理由是
「V24 车道禁区:`admin/**`(协调层另派)」。**登记不是修复。** 那条登记从 2026-08-11 起原地未动。

**看得见的判据(怎么让它红)**:
- 把 `SelftestUiPromises.cs:100-102` 那三行 `KnownBroken` 条目删掉、**文案不动** ⇒
  这句指路变成未登记违规,客户端自检当场 FAIL。
- 反向也钉着:把文案**改对**而**不**删登记 ⇒ `SelftestUiPromises.cs:254` 那条
  `staleKnown.Count == 0`(「已知坏登记表**只许变短**」)当场 FAIL。
- **两个方向都可以为假** —— 这一条我核过代码本身(`:252`–`:259`),不是照抄它的自述。

**代价**:改一句话。★ **谁来改是个裁定** —— `admin/**` 当时被划在 V24 车道之外,而此后没有任何车道认领它。

---

### A2 · 同一页两处让人点「开始配对」,而那颗按钮叫「开始寻找主机」

**在哪**:`HostHubView.cs:843`(没有待批请求时的说明)与 `HostHubView.cs:925`(批准返回 409 时的弹窗)

**今天会怎么咬人**:
- `:843` —— 「到那台新电脑上打开客户端,在同一页里选中这台主机、点**「开始配对」**。」
- `:925` —— 「这条请求已经过期或已被处理了。请让对方重新点一次**「开始配对」**。」

客户端上唯一与配对有关的主按钮是 **「开始寻找主机」**(`DevicesView.cs:352`);
危险键叫 **「解除本机配对」**(`DevicesView.cs:186`)。**没有「开始配对」。**

`:925` 那一处最坏:**配对刚刚失败**,人正在最需要一句准话的时刻,而给他的下一步指向一个不存在的键。
按钮改名发生在 `fb59d4e`,那一提交改了一句配套文案、**漏掉另外三句**。

**为什么没有任何东西红**:同 A1 —— `KnownBroken` 第 2 条。
★ 顺带更正:**那条登记里写的两个行号今天是错的**(写着 `:829` 与 `:912`,真实位置 `:843` 与 `:925`)—— 见 B3。

**看得见的判据**:同 A1(删登记 ⇒ 红;改文案不删登记 ⇒ 红)。

**代价**:改两句话。

---

### A3 · 证书快过期时告诉用户「不要点『重新配对』」—— 客户端上没有这颗键

**在哪**:`20-client-win/transport/TlsFailure.cs:247`
逐字:`"★ 不要点「重新配对」:私钥还在,续签就够了,重配会把它删掉。"`

**今天会怎么咬人**:这句话出现在**本机设备证书临近过期**那一格 —— 也就是这条建议最要紧的那一刻。
它让人去**避开**一颗不存在的按钮。客户端上真正对应的危险动作叫 **「解除本机配对」**(重配 = 先解除再配一次)。

于是这句提醒**保护不到它想保护的那个动作**:一个决心"重新配对"的用户会去点「解除本机配对」,
而这句话没有拦住他 —— 它拦的是另一个名字。后果正是这段代码自己写着的那个:
**亲手删掉一把只需要续签的私钥**。

**为什么没有任何东西红**:`KnownBroken` 第 3 条,归属 `20-client-win/transport/**`
(V25 当时在动 `HubClient` 一线)。同样是**已登记、无人认领**。

**看得见的判据**:同 A1。

**代价**:改一句话(把「重新配对」换成「解除本机配对」,并说清"那等于重配")。

---

> ### ★ A 级三条的共同形状,值得单独说一句
> 三条都**不是**没有护栏,而是护栏**看见了、记下了、然后车道边界把它们隔在外面**。
> `SelftestUiPromises` 的设计是对的(登记表只许变短、两个方向都能红),
> 而它没有回答的问题是:**「不在任何车道里的那几条,谁来认领?」**
> 这不是一个技术缺陷,是一个**派工缺口** —— 而它已经存续了 11 天、23 条车道。

---

## 3. B 级

### B1 · `OpenProjectInChat` 零生产调用点,而一条断言逐字宣布「进项目聊天**入口就位**」

**在哪**:`20-client-win/app/MainWindow.xaml.cs:828`(声明)· `20-client-win/app/Selftest.cs:480`(断言)

`git grep -n "OpenProjectInChat"` 全仓**只有两行**:

```
20-client-win/app/MainWindow.xaml.cs:828:    public void OpenProjectInChat(string projectId)
20-client-win/app/Selftest.cs:480: Assert(mwStatus.Contains("OpenProjectEditor") && mwStatus.Contains("OpenProjectLibrary")
                                          && mwStatus.Contains("OpenProjectInChat"), "项目编辑/项目库/进项目聊天入口就位");
```

—— 它自己的声明,与那条断言里的**字符串字面量**。
两个兄弟方法都有真调用方(`OpenProjectEditor` ← `ProjectLibraryView.cs:55`;`OpenProjectLibrary` ← `HomeView.cs`);
它自己**一个都没有**。

**这是第七例**(已知六例:`TlsFailure` · doctor ⑫环 · `loader.shutdown()` · `EnsureStackAsync` ·
`DiscoverEdgeDialsAsync` · `StartEdgeCmd`),而且**没有登记在任何地方**。

**判据为什么挡不住**:那条断言读的是 `MainWindow.xaml.cs` 的**源码文本**,
而**被它判活的那个方法的声明行本身就把它喂绿了** —— 与 `HubAdmin.cs:618` 记着的
`Assert(haSrc2.Contains("DiscoverEdgeDialsAsync"))` 是**同一个形状**,只是那一处已登记、这一处没有。

**今天咬不咬人**:**不咬**。活着的那条路是 `NavigateToProject(workspaceKey, projectId)`
(`MainWindow.xaml.cs:772` ← `HomeView.cs:1768`),而且它**更对** —— 它跳到项目**自己所属的**工作空间;
`OpenProjectInChat` 写死 `Navigate("chat")`。

**将来会怎么咬**:哪天有人给项目抽屉接上"进项目聊天",顺手用了这个名字对得上的方法,
一个属于「翻译」空间的项目就会被送进「聊天」空间 —— **而这正是隔壁 `NavigateToSession`
的注释白纸黑字警告过的那件事**(「点 A 打开 B,静默错开,比不跳更坏」)。

**处置建议(登记,别顺手改)**:删掉 `OpenProjectInChat`,并把 `Selftest.cs:480` 那条断言的第三项
换成 `NavigateToProject` —— 那是今天真的在守的那条路。

---

### B2 · `PttHealthAsync` 仍是零调用点,而**同一轮的留痕逐字写着「此前零调用点,已修」**

**在哪**:声明 `20-client-win/app/Services/InterpretState.cs:362`;
两句留痕 `20-client-win/app/Views/InterpretPanel.cs:105` 与 `:189`。

留痕原话:
- `:105` —— 「★ 进页面探一次本机语音服务。★★ 这是 `PttHealthAsync` 这条路**第一个真实调用点** ——
  在它之前那个方法零调用,于是这一页从来没真的知道过语音服务起没起。」
- `:189` —— 「★★ 顺带修掉一处旧的静默:`PttHealthAsync` 此前**一个调用点都没有** …… 现在由闸来说,而闸是探出来的。」

**实测**:`git grep -n "PttHealthAsync"` 全仓 **4 处** —— 声明 1 处、上面这两句注释 2 处、
以及 `Selftest.cs:6951` 拿它的**声明字符串**当切片下界 1 处。**零调用点。**
`InterpretPanel.cs:106` 那一行真正调的是 `TheApp.Interpret.ProbeSpeechAsync()`(`InterpretState.cs:382`)。

**这是第八例**,而且是本轮**形状最值得记住**的一条:
**一句「我已经把零调用点修好了」的留痕,写在了一个并没有调用它的调用点旁边。**
`InterpretState.cs:379` 甚至解释了为什么两个方法要**分开留着**
(「那个是"我要这个值",这个是"顺便让闸也知道";合成一个会让任何一次取值都产生副作用」)——
设计意图是两个都活着,而实际只有一个有消费者。

**今天咬不咬人**:**不咬**。这一页**真的**探了(走 `ProbeSpeechAsync`),
按住说话按钮的灰/亮与旁边那句说明都是探出来的。**行为是对的,留痕是假的。**

★ 反驳记录:我先怀疑「是不是 `ProbeSpeechAsync` 内部调了 `PttHealthAsync`」——
**不是**,它直接 `await _speech.HealthAsync(ct)`;两者是 `SpeechClient.HealthAsync` 的两个平行门面。

**处置建议(登记,别顺手改)**:二选一 ——
① 让 `ProbeSpeechAsync` 走 `PttHealthAsync()` 取值,两句留痕即刻成真;
② 删掉 `PttHealthAsync` 并改掉那两句留痕。
★ 选 ② 要留神:`Selftest.cs:6951` 拿它的声明当 `Slice` 的**下界**,删了之后
`Assert(rel is not null, "切片得真的取到 PttReleaseAsync")` 会红 —— **而它给的理由是错的**(真因是下界没了)。

---

### B3 · 跨文件 `file:line` 引用漂移:能自证的 36 处里 **15 处指错**,4 处偏 ≥400 行

**怎么量的**:扫全部 `.cs/.py/.ps1` 里形如 `<文件名>.<扩展>:<行号>` 的**跨文件**引用,
只取能唯一定位到一个受管文件的那些 ⇒ **115 处**。其中引用行里带着可核对证据
(反引号里的标识符,或「」里的原话)的 **36 处**;把那段证据拿到目标文件**全文搜**:
落在 `±3` 行内算对,落在别处算漂,搜不到算"原话没了"。逐条人工复核过 ——
自动判定的假阳性(全角/半角标点、`【】` 与 `「」` 互换、`:263-278` 这种范围引用)已剔除。

**结果(逐条,已复核)**:

| 引用写在哪 | 它说的是 | 指到 | 实际在 | 偏 |
|---|---|---|---|---|
| `10-core/gateway/test_e4_egress.py:27` | e4 的挂点 | `gateway.py:439` | `gateway.py:2243` | **+1804** |
| `10-core/gateway/gateway.py:898` | 客户端每次退出都调它、失败还被吞掉 | `HubClient.cs:230` | `HubClient.cs:703`/`:706` | **+476** |
| `10-core/gateway/gateway.py:1568` | 同上 | `HubClient.cs:230` | `HubClient.cs:706` | **+476** |
| `10-core/gateway/test_gpu_broker.py:435` | 同上 | `HubClient.cs:230` | `HubClient.cs:703` | **+473** |
| `20-client-win/app/Services/AppSettings.cs:60` | 「主体只来自成员表」那条铁律 | `gateway.py:227` | `gateway.py:683` | **+456** |
| `10-core/lan-edge/Program.cs:1500` | 每次请求进来时复查 | `gateway.py:1965` | `gateway.py:2363` | **+398** |
| `20-client-win/admin/Services/StackStop.cs:7` | `safe_to_stop_stack()` | `gateway.py:849` | `gateway.py:1095`(对外入口 `:914`) | **+246** |
| `20-client-win/admin/Views/AdminChrome.cs:144` | 拖动 + 双击最大化那套手感 | `MainWindow.xaml.cs:458` | `MainWindow.xaml.cs:477` | **+19** |
| `20-client-win/app/SelftestUiPromises.cs:9` | 「设备/配对已并入设置」 | `MainWindow.xaml.cs:528` | `MainWindow.xaml.cs:547` | **+19** |
| `20-client-win/app/Views/ChatView.cs:1093` | 同上 | `MainWindow.xaml.cs:528` | `MainWindow.xaml.cs:547` | **+19** |
| `20-client-win/app/SelftestUiPromises.cs:104` | 「开始配对」那两处 | `HostHubView.cs:829` / `:912` | `HostHubView.cs:843` / `:925` | **+14 / +13** |
| `10-core/lan-edge/Program.cs:2078` | `SweepStaleProvisioning` | `Store.cs:203` | `Store.cs:194` | **−9** |
| `20-client-win/app/SelftestUiPromises.cs:102` | 「打开这台的客户端 →『设备』」 | `HostHubView.cs:654` | `HostHubView.cs:660` | **+6** |
| `20-client-win/admin/SelftestMoved.cs:514` | 「拿不到再看【线索】」 | `HostHubView.cs:299-301` | `HostHubView.cs:305` | **+6** |
| `10-core/gateway/e4_egress.py:4` | `import e4_egress as e4` 那一行 | `gateway.py:28` | `gateway.py:33` | **+5** |

**为什么这算 B 而不是 C**:前 7 条**已经不指向任何相关的东西**了 ——
`gateway.py:439` 落在一条报错文案的字符串中间,`HubClient.cs:230` 落在一段讲 allowlist 的文档注释里。
这个仓库的注释是**承重的**(很多判据的**理由**只写在注释里),而"照着行号去读"是它默认的读法。
**一个指到别处去的行号,比不给行号更坏** —— 它让人读了一段无关的代码,然后以为自己读懂了。

**为什么没有任何东西红**:全仓**没有任何一条判据检查行号引用** —— 既没有提交时的,也没有全仓扫描的。
(这是我挑的那一族的第三个实例,见 §6。)

★ 反驳记录(被我自己推翻的一条):初版我把 `00-docs/decision-packets/` 里 **7 处**指到文件尾之外的
`DevicesView.cs:NNN`(V21 把该文件从 ~1500 行砍到 506 行)也算了进来。**反驳成立** ——
决议包是**冻结的历史记录**,本仓纪律是「原文划掉不删」,它们描述的是**当时**为真的位置。⇒ **不计入**。

---

### B4 · speech 三条契约在闸里记为 `paired`,而登记的"消费者半边"在生产里**零导入**

**在哪**:`90-ops/gate/check_contract_pairs.py:245 / 253 / 262` ——
`("speech","GET","/health")` · `("speech","POST","/v1/speech/asr")` · `("speech","POST","/v1/speech/tts")`,
三条都是 `"state": "paired"`,`"client_file": "10-core/gateway/test_speech_contract.py"`。

**实测**:
- `10-core/gateway/gateway.py` 的 import 段(`:17`–`:41`)里**没有 `speech_proxy`**;
- 全仓引用 `speech_proxy` 的只有 `test_speech_contract.py:28`(测试)加两句注释;
- ⇒ **`speech_proxy.py` 在生产里零导入。** 而它的文件头自称是
  「网关侧的 speech 后端消费者」「那三条契约的**客户端半边**」;
- **真正的生产消费者是 C# 的 `20-client-win/app/Services/SpeechClient.cs`**(客户端直连 `127.0.0.1:18085`),
  **它不在这张表里**。

**为什么重要**:D92 那条元规则要防的是 A1 的形状 —— "服务端与消费者各测各的,中间那条缝谁也没看"。
这里的情况更靠前一步:**登记在册的那个消费者,生产里根本不存在**。
今天不咬人(网关与 speech 之间还没有任何流量);
**而当有人去接那条线时,这张表会告诉他"这条缝已经被成对断言守住了"。**

**并且有一句留痕在替它作证**:`20-client-win/app/Views/InterpretPanel.cs:36` 逐字 ——
「★ 而**仓库另一侧真的有一个消费者**:网关 `speech_proxy.py:88` 逐字 `obj["audio_b64"]`
**把它转发出去** ⇒ 将来改这个键名,**那边要一起改**。」
**"转发出去"今天不成立** —— 网关不转发任何 speech 应答。

★ 反驳记录(部分成立):这句话的**结论**(「改键名那边要一起改」)**是对的** ——
因为 `test_speech_contract.py` 确实会红。错的是它给的**理由**(说得像一条活着的生产转发路)。
⇒ 判为"判词对、出处假",仍然计入,但**不升 A**。

★ 另一条反驳(成立,已据此收窄):闸自己在文件头「本文件不覆盖」里明写它
「只认两半**在不在**,不看键集合对不对」,所以**它没有超出自己的承诺**。
缺陷不在闸的实现,在**这三行登记填的对象**。

---

### B5 · 控制字符护栏只看**流量**,看不见**存量**;而存量里今天正好有一个

**实测**:全仓扫描 `.cs/.py/.ps1/.md/.json/.toml/.xaml/.cmd`,
C0 控制字符(`\t \n \r` 之外)**命中 1 个文件、1 个字节**:

```
20-client-win/admin/Views/ModelsView.cs  第 22 行 · 字节偏移 1013 · 值 0x07(BEL)
  //   从此落在 `%LOCALAPPDATA%\LocalAI<BEL>dmin\settings.json`,**不碰客户端那份**。
                                        ^^^^^ 本该是 反斜杠 + admin
```

**来历**:`git log --follow` 逐版数控制字符 ——
`dd06af3`(2026-08-06)= 0 · `6af0bce`(**2026-08-08**,V21 搬 3100 行进管理端)= 1。
**而 `.githooks/pre-commit` 第 ⑤ 段的控制字符护栏是 2026-08-09 加的 —— 晚了一天。**
`6af0bce` 在本轮跨度**之内**(它不是基线 `207b7d3` 的祖先)。

**为什么它今天仍在**:那道护栏遍历的是 `git diff --cached --name-only --diff-filter=ACM`
—— **只看这一次提交改了什么**。它是一道**流量**闸,没有**存量**版本;
`run-tests.ps1` 里也没有任何全仓控制字符扫描(全文搜 `控制字符` / `tr -dc` 零命中)。

**今天咬不咬人**:**不咬** —— 它在一句注释里,而那条路径的真身写在
`AdminSettings.cs:16`(正确)与 `AdminPaths.StateDir`(代码)。
★ **是这一个实例无害,不是这一族无害**:同一个字节落在一个**路径字符串**里就是一条静默的生产缺陷,
而今天**没有任何东西会发现它已经在树里了**。

**处置建议**:① 按字节把那 1 个 0x07 换回 反斜杠 + `admin`;
② 给 `run-tests.ps1` 加一条全仓扫描,照 `_EXPECTED_UNNUMBERED` 那个"上界只许变短"的形状(今天上界 = **0**)。

---

### B6 · speech 端口 18085 有四份拷贝,没有任何判据把它们绑在一起

| 在哪 | 谁读它 |
|---|---|
| `10-core/speech/launch.toml` 的 `[service].port` | **真读** —— `server.py:373` `int(spec["service"]["port"])` |
| `config/vram-budget.toml:132` 与 `:139` | **没人读**(`launch.toml:29` 自己写着「那边的 port 今天仅登记用、没有代码读它」) |
| `20-client-win/app/Services/SpeechClient.cs:45` `public const int Port = 18085;` | **真用**(客户端拨号)。注释自称「★ 与 `launch.toml` 的 `[service].port` 一致」 |

**实测**:在 `20-client-win/**/*.cs` 里搜 `launch` **零命中**
⇒ **没有任何 C# 断言读过 `launch.toml`**。那句"一致"是一句人肉承诺。

**今天咬不咬人**:不咬(两边今天都是 18085)。
**改了会怎样**:把 `launch.toml` 的 port 改掉 ⇒ 服务端绑新口,客户端仍拨 18085 ⇒
按住说话按钮灰着、旁边写「连不上本机语音服务(127.0.0.1:18085)」。

★ 反驳记录(部分成立,已据此降级):**失败是可见且说得清的**,不是静默 ⇒ **不升 A**。
但 `check_contract_pairs.py` 明写「请求方向不在本文件范围内」,
所以**这条缝今天在任何一张表上都没有名字**。

---

## 4. C 级

| # | 发现 | 在哪 | 为什么只是 C |
|---|---|---|---|
| C1 | `CountFor` **零调用点**(全仓仅其声明行),而它的文档逐字写着「自检用:断言登记表真的有东西,否则 `AbortAll` 恒返回 0 也叫通过」 | `10-core/lan-edge/Program.cs:1573`(`8617f17` · 2026-08-10 · **在本轮跨度内**) | ★ **反驳部分成立**:它要防的那个恒真,已被同一段里 `Assert(killedInMs < 200)` 实质挡住 —— 那条把「登记表 0 延迟」与「周期复查 ~600ms」分开,摘掉登记表它就红。⇒ 是一处**没装上的保险**,不是一个洞。**第九例** |
| C2 | `_heading_marks_draft` 零调用点,已被 `_self_declares_draft` 取代;而**取代者的 docstring 反过来把它当权威引用**(`:192`「下面 `_heading_marks_draft` 的 docstring 逐字写着……」) | `90-ops/gate/check_decision_numbers.py:226`(`1186f7e` · 2026-08-15 · **在跨度内**) | 闸内部的死函数,不影响判定。**第十例** |
| C3 | `DiscoverEdgeDialsAsync` **仍然零调用点**(已知第五例);守它的 `Assert(haSrc2.Contains("DiscoverEdgeDialsAsync"))`(`SelftestMoved.cs:1161`)被它自己的声明喂绿 | `20-client-win/admin/Services/HubAdmin.cs:622` | **仓库已完整登记**(`HubAdmin.cs:617-620` 逐字写明「那条断言证明不了接线」),并写了保留理由与删除条件。⇒ 不是新发现;新的是**23 条车道之后它还在**这个事实 |
| C4 | `SingleInstance.RequestClientQuit()` 零调用点 —— 管理端走的是 `ClientLink.RequestClientQuitAsync` → `InstanceLock.SignalQuit`,绕过这个门面 | `20-client-win/app/Services/SingleInstance.cs:45` | 门面重复,行为无碍;`ClientLink.ClientAppKey => SingleInstance.AppKey` 是单一来源,**没有常量分家** |
| C5 | **7 个 `.ps1` 没有 BOM**,其中 `90-ops/lan/lan-firewall.ps1` 含 3 个非 ASCII 字节(一个 `★`)⇒ Windows PowerShell 5.1 会按 cp936 读它。全仓**没有任何 BOM 护栏** | `90-ops/lan/lan-firewall.ps1` 等 7 个 | 那 3 个字节在第 3 行的 `#` 注释里 ⇒ 只乱码,不炸。另外 6 个是纯 ASCII、今天无害 —— 但它们是"下一个往里写一句中文的人"的陷阱 |
| C6 | **26 处** `Assert(<src> is null 或 …)` 的 fail-open 形态(`app/Selftest.cs` 20 处 · `admin/SelftestMoved.cs` 6 处):出包形态下**全部无声通过** | 见 §8 量法 | 缺口**记了账**(`TryReadSource` 落空计入 `SRCMISS`,出包门禁把它印出来)。★ 但 `SRCMISS` 只说"读了多少次没读到",**说不出是哪几条判据因此没说话** |

---

## 5. 被反驳掉的(默认立场「它错了」,推不倒的才留在上面)

> ★ 这一节和上面同样重要。**一个假的缺陷报告比不报告更坏。**

| # | 我一度认为的发现 | 反驳 | 判 |
|---|---|---|---|
| R1 | speech 后端**没有任何鉴权**(`check_backend_auth.py` 明文豁免 `kind=speech`)⇒ 本机任意进程都能连 `127.0.0.1:18085`,拿到 `provenance = user_voice_asr` —— 那是唯一允许直通记忆写入的档位 ⇒ 本机程序可以"替用户说话" | **反驳成立(两层)**:① `server.py:158` 逐字写明这就是它的威胁模型 ——「回环之所以算可信:**能连上 127.0.0.1 就已经在这台机器上了**」;对单用户桌面产品这是**站得住的裁定**,不是疏忽。② 那条"直通记忆写入"的路**今天不存在** —— `may_write_memory` 在生产里零调用点,网关也不导入 `speech_proxy`。⇒ 没有可被利用的通路 | **不报** |
| R2 | `CountFor` 是"承重判据上的一个洞" | 它防的恒真已被 `killedInMs < 200ms` 那条实质覆盖 | **降到 C1** |
| R3 | `SelftestOnDemand.cs:56` 断言里的 `vision.vlm` 全仓不存在 ⇒ 判据在替不存在的别名作证 | **反驳成立**:那是**纯函数测试自造的夹具 id**;被测的 `ComponentPicker.InitialPermitted` 就是 `LoadAsync` 生产走的那一个。构造输入在这里**合法** | **不报** |
| R4 | 21 处路径字面量指向不存在的文件 | **反驳成立**:全部是 `..\` 相对路径,逐个核对 **21/21 都存在**(是我的检查器不解析 `..`) | **不报** |
| R5 | 7 处 `DevicesView.cs:NNN` 指到文件尾之外 | **反驳成立**:7 处全在 `00-docs/decision-packets/` 的**冻结历史包**里 | **不计入 B3** |
| R6 | 判据在 Debug / Release-win-x64 两种形态下说的话仍然不一样(V36 量到过 2190 / 2186) | **反驳成立**:V36 已修 —— 两处源码根锚点收拢成 `ClientSourceRoot()` / `AdminSourceRoot()`。我复核了全部 22 处 `".."` 路径字面量,**没有一处再写死层数**;两个 `csproj` 里也没有任何 Debug/Release 条件,全仓 `#if DEBUG` / `Conditional("DEBUG")` **零命中** | **不报**(但见 §7:我**没有真的跑**这两个形态) |
| R7 | 界面词表里有指不到的键(`Strings.Get` 缺键会把键名本身显示到屏幕上) | **零命中,且元断言复核过**:66 个字面量键 vs 108 个登记键;3 个"缺失"分别是负测夹具 `__no_such_key__`、变量名 `k`、前缀拼接 `"settings.skin."` | **不报** |
| R8 | 跨进程 HTTP 路由有对不上的(客户端打一条服务端没有的路径) | **零命中**:39 条已登记服务端路由 vs 全仓 URL 字面量;唯一 4 个"未匹配"都是 speech 那个 `BaseHTTPRequestHandler`(在 `do_POST` 里比路径,不用装饰器)与前缀片段 | **不报** |
| R9 | 5 处"断言只被声明喂绿"的重言式 | 逐条核对后 **4 处是假阳性**(`ChatOpener` / `DoubleAnimation` 命中的是**调用**不是声明;`SwallowClick` / `OpenProjectEditor` 在别处有真调用方)。只有 1 处成立 ⇒ 已升为 B1 | **4 条不报** |

---

## 6. ★ 我自己挑的那一族(任务书 ⑥):**字节层面的护栏只有流量版本,没有存量版本**

这一族是本轮唯一一个**不是从既有清单推出来的**发现。三个独立实例:

| 实例 | 流量闸(提交时) | 存量闸(全仓) | 存量里今天有几个 |
|---|---|---|---|
| **控制字符** | ✅ `.githooks/pre-commit` 第 ⑤ 段(2026-08-09) | ❌ **没有** | **1**(`ModelsView.cs:22` 的 0x07,比闸早一天进来) |
| **`.ps1` 的 BOM** | ❌ 没有 | ❌ **没有** | **7** 个无 BOM,其中 **1** 个已经含非 ASCII |
| **`file:line` 交叉引用** | ❌ 没有 | ❌ **没有** | **15** 处指错(能自证的 36 处里) |

**共同形状**:本仓的元规则做得极好的那一半是**「这次提交改了什么」**;
而**「仓库现在是什么样」**这一半,在**字节层与引用层**上是空的。

★ 措辞要准(我第一版写成「全仓没有任何存量判据」,**那是假的**,自己推翻了):
`check_decision_numbers.py` 扫全部 71 份决议包、`check_contract_pairs.py` 反向全表所有路由、
`SelftestMoved` 那条「唯一入口必须有生产调用点」扫整个生产编译集 —— **这些都是存量闸**。
缺的只是**字节层与引用层**这三样。

**为什么它值得单独成族**:这三样有一个共同的、本仓已经付过学费的性质 ——
**零命中与全清白长得一模一样**。控制字符看不见;缺 BOM 在开发机上永远试不出来;
错行号读起来完全像对的。⇒ 它们恰好是**最需要机器、而最不可能被人看出来**的那一类。

**处置建议(一条闸盖三样)**:在 `run-tests.ps1` 加一段全仓扫描,照 `_EXPECTED_UNNUMBERED` 的形状,
三个上界**今天分别是 0 / 7 / 15,只许变短**:
① C0 控制字符计数;② `.ps1`/`.psm1` 无 BOM 计数;③ `file:line` 引用中"证据不在目标 ±3 行内"的计数。
★ 第 ③ 条必须带**元断言**:扫出来的引用总数(今天 115、其中可自证 36)不许为 0 ——
否则提取器坏掉的样子会和全清白一模一样。

---

## 7. ★ 覆盖账:本轮**没有**做到的(逐条 —— 不是"卡住",是"有意没做 / 做不了")

1. **一次构建、一次自检都没跑。** 本车道边界是"全仓只读",而 `dotnet build` 会落 `bin/obj`;
   且本机检出到一个常驻的 `VBCSCompiler.dll` 进程(另一轮出包留下的锁)。
   ⇒ **§5 的 R6(形态一致性)全部是静态推断 + 复核 V36 的记录,不是我量的。**
   要真判它,得跑 `bin\Debug\<tfm>\` 与 `bin\Release\<tfm>\win-x64\` 两趟并比 PASS 数。
2. **一条红测都没做。** 边界是只读,所以"改一个字让它红"这件事我只**写出了配方**(A1/A2/A3 各一条),没有执行。
   ★ 请把这一点当成**本报告的已知弱点**:A 级三条"有断言守着"是我**读代码**核的
   (`SelftestUiPromises.cs:252-259` 的 `staleKnown` 确实存在且双向),**不是注入验的**。
3. **`10-core/memory/**`(约 30 个 `.py`)只过了零调用点扫描**,没有逐条读。
   那个子系统本轮几乎没被改动(不在 93 个变更文件里),但"没被改"不等于"没问题"。
4. **`40-integrations/**` 一个字没看。** 它不在本轮变更里。
5. **Python 侧的「判据替不存在的东西作证」没查全。** 我只做了 C# 的 `Contains("<标识符>")` 形态;
   Python 那边 `inspect.getsource` + 子串的形态(`assert_helpers.py` 那一族)**没查**。
6. **中央文档的内部一致性没审。** 只查了它们里面的 `file:line`(见 R5)。
   `STATE.md` 的四个形态数我**没有复算**。★ 而它们在我写这份报告**期间就变了一次** —— 见 §10。
7. **`00-docs/OPEN-ITEMS-2026-08-09.md` 那 171 条未了项没有逐条对账。**
8. **没有查 SSE 那三条契约的帧级键集合**(`check_contract_pairs.py` 自己也明写「本文件只登记它们欠着」)。
9. **两条已跑的闸**(`check_contract_pairs.py` 31/30/DEBT=1 · `check_decision_numbers.py` 7 PASS)
   是**在本工作树里**跑的。按本仓惯例,全仓扫描类的闸**要到主工作树再跑一次才算数** —— 我没跑。

---

## 8. 本轮用到的量法(可复现,含元断言)

| 量什么 | 怎么量 | 元断言(判据自己坏了会红) |
|---|---|---|
| 零生产调用点 | 剥掉注释与字符串字面量后,统计每个方法名在**非 `Selftest*`** 文件里的引用数 | 剥离前后 `Assert(` 计数 **2697 → 2687**(必须变小、不许归零);已知活符号 `AbortAll` 剥后仍在;逐文件行数不变 |
| 判据替不存在的东西作证 | 测试里 `X.Contains("<标识符>")` 的字面量,拿去**剥离后**的生产代码里搜 | 同上;并对每个命中人工判正向/反向断言(反向断言本来就该搜不到) |
| `file:line` 漂移 | 引用行里的反引号标识符 / 「」原话,到目标文件全文搜,比对行距 | 扫到 115 处(不许为 0);标点归一化后重跑,假阳性从 38 降到 15 |
| 控制字符 | 全仓字节扫 `0x00-08 · 0x0B-0C · 0x0E-1F` | 与 `.githooks/pre-commit` 第 ⑤ 段**同一口径**(含 11/12) |
| 路由 / 端口 / 词表 | 服务端路由枚举 vs 全仓 URL 字面量;`Strings.Get` 字面量 vs `strings.json` | 每一项都先打印**分母**(39 条路由 / 66 个键 / 26 处路径字面量)—— 零命中必须能与"提取器坏了"分开 |

---

## 9. 交回

- **A 级 3 条**:A1 指向不存在的「设备」页 · A2 指向不存在的「开始配对」键(两处)· A3 让人避开一颗不存在的「重新配对」键。
  三条**全部已登记在 `SelftestUiPromises.KnownBroken`、无人认领**,最早一条自 2026-08-11 起未动。
- **B 级 6 条** · **C 级 6 条** · **被反驳掉 9 条**(其中 4 条是我自己的假阳性)。
- **零调用点家族**:找到**第七 / 八 / 九 / 十例**
  (`OpenProjectInChat` · `PttHealthAsync` · `CountFor` · `_heading_marks_draft`);
  已知第五例(`DiscoverEdgeDialsAsync`)**至今仍是零调用点**。
- **我挑的那一族**:**字节层面的护栏只有流量版本,没有存量版本**
  (控制字符 / BOM / 行号引用,今天分别是 **1 / 7 / 15**)。
- **零生产代码改动**:`10-core/**` · `20-client-win/**` · `90-ops/**` · `config/**` 与中央四文档**一个字节未动**;
  本轮只新增本文件与 `AUDIT-2026-08-22-v40-用户版.md`。
- **不取号**(审计不是裁定)。本轮发现**只登记、不顺手修** —— 修了会打乱另外三条车道的归因。

---

## 10. ★ 写作期间 main 前进了 4 个提交 —— 逐条复核过,结论没变

审计开始时 main = `4b7fa28`;交回时 main = `06f4b37`。其间并入:

| 提交 | 内容 | 对本报告的影响 |
|---|---|---|
| `a795ad5` | 修出包锁泄漏(作者自陈第一版修错且一声不吭,已双向红测) | 无 |
| `f96c633` | 四个形态重跑:全量门禁 **PASS=4759 · 退出码 0**;客户端仓库形态 **2215 · SKIP=0**(上一趟 `2214 · SKIP=1`,**合计同为 2215**)。★ **本轮没有重出包**,⑤⑥ 两格仍是 08-20 的数 | **§7-6 与 v40b ④-2 的数已按此更正** |
| `234d7de` / `06f4b37` | V42 · P5 语音第二批:两个语音档位的收费朝相反方向偏(`speech.full` 第一次被量);改了 `launch.toml` · `measure_asr_latency.py` · `speech/selftest.py` · `build-client.ps1` | **逐条复核,三条结论都没变**,见下 |

**逐条复核(不是推断,是重新量的)**:

1. **B4 / v40b ③-1(speech 契约的消费者半边生产零导入)** ——
   `grep -c speech_proxy 10-core/gateway/gateway.py` 在 `06f4b37` 上仍是 **0**。**结论不变。**
2. **B6 / v40b ③-2(18085 端口四份拷贝无绑定判据)** ——
   V42 对 `10-core/speech/selftest.py` 的新增行里,`port` / `18085` / `SpeechClient` / `.cs` **一处都没有**;
   `launch.toml` 的新增行里也没有碰 `[service].port`。**没有新增任何绑定判据,结论不变。**
3. **v40b ④-2 / ④-4(形态账对不上)** —— 新版 STATE 反而**把这件事说得更清楚了**:
   它在 ⑤⑥ 两格逐字标了「**2026-08-20 出包那一趟的数,本轮没重出包**」。
   ⇒ 那两格与 ③ 那格**不是同一趟** —— 差额的**量级**可靠、**个位数不可靠**,本报告已按此改写。
   ★ 而 ⑤ 那一格**仍然只有 `PASS` 与 `FAIL`**,没有 `SKIP` / `OWED`(⑥ 有)⇒ **④-4 不变。**

★ 按本仓惯例,「审计期间 main 前进了几个提交」必须写出来,并逐条说清有没有推翻结论 ——
2026-08-08 那轮也是这么做的(它那次前进了 8 个)。
