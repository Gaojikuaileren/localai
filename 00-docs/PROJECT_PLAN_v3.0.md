# 本地 AI 中枢 — 项目方案书与推进纲领 v3.0

> 状态:**执行中**。P0 / P1 / P2 / P3a / P3b / **P3c 已验收**(P3c 于 **2026-08-04** 两台真 PC 签字,见 `decision-packets/p3c-signoff-2026-08-04.md`);下一站 **P4**。
> **阶段实况以 `STATE.md` 为准**,本文件给的是判据与范围。
> **v2.1 已被本文档取代**;v0.4 / v1.0 / v2.0 / v2.2 均已归档
> 编制日期:2026-07-26 · **最后校准:2026-08-03**
> 修订依据:两轮多智能体设计与对抗性审查(共 22 个 agent · 84 条发现 · 36 条严重)

---

## 0. 本版做了什么

> ### v3.0(2026-07-28)· 阶段重排,设计章节不动
>
> **触发**:传输层与客户端形态整体改向。见 **D34–D41**(00-docs/DECISIONS.md)。
>
> **改了什么**
> · **§14 推进纲领整节重写** —— 原 P3b「多设备(Tailscale + 手机 PWA)」作废,
>   拆为 P3b(局域网身份与准入)/ P3c(统一客户端)/ P3d(外联通道 Signal);
>   原 P7「Windows App」前移为 P3c 并升级为**唯一入口**;新增 P11 产品化(条件触发)。
> · 版本号 v2.2 → v3.0。
>
> **没改什么(重要)**
> · §4 记忆系统 · §6 权限区域 · §8 显存模型 · §17 Vigil —— **设计不因传输层改向而作废**。
> · P1 的 17 项实测结论全部保留。
>
> **哪些段落已被 D34–D41 局部推翻**(正文暂留原文,以决议为准;将随各阶段落地时逐段改写):
> · §3 架构图的网络层「Tailscale / MagicDNS / Tailnet Lock」→ 待 D34 待决项 1 裁定
> · §4.11「多设备记忆」→ 手机不再是记忆设备;S2 隔离机制**不作废,改服务于外联通道**
> · §4.11.5 出境判据 → 由 **D39** 精确化(端到端加密的中继不构成出境)
> · §5.6.2 L5 → **不推翻**,由 D39 精确化并首次给出代码对应物(响应侧出境闸)
> · §6.3.2 设备身份的 `tailscale_node_id` 交叉绑定 → 改为客户端证书指纹(D35)
> · §8.1 显存模型 → `desktop_floor` 语义扩为「桌面 **+ 常驻实体**」(D40)
> · §14 P2 的「WebAuthn 设备身份」→ 作废(非本机设备上不再有浏览器)
>
> ★ **本文件不是法律意见。** 产品化(P11)相关的许可与法规条目须由执业律师审阅。


v2.1 的方向不变。本版落地三件用户在本轮提出的新东西,并修掉审查发现的问题。

| # | 新增 | 起因 |
|---|---|---|
| **1** | **组件选择器**(§8.1 · **D25 已取代原五档开关**) | 「这台机器平常也要用、要打游戏」——把资源分配从「AI 为主、游戏是例外」翻转为「人显式决定给多少」。**你直接勾组件,不选档位** |
| **2** | **常驻清醒助手 Vigil**(§17)+ **权限区域**(§6.7) | 「无时无刻清醒、只读授权区域的文件」 |
| **3** | **打扰评分**(§4.7,整节替换「克制的主动性」)| 用户明确决定**推翻 §4.7 放权**,改为评分超阈值才可主动打扰 |
| **4** | **多设备记忆**(§4.11) | 「局域网共享记忆库,统一所有 PC,手机远程可调用」 |

### 0.1 相对 v2.1 的实质变更

| 变更 | 位置 | 起因 |
|---|---|---|
| 三模式(daily/deep/asset)→ 五档 → **★ 组件选择器**(D25) | §8.1 | 新需求 |
| **修正 v2.1 的显存算术错误**:daily 漏算 3 个 CUDA context,实为 14.6 非 13.0 | §8.1.2 | 审查 R-1 |
| **修正 KV 缩放错误**:KV 与参数量无关,4B 与 8B 的 KV **完全相同** | §8.1.2 | 审查 R-2 |
| §4.7「克制的主动性」**整节替换**为打扰评分 | §4.7 | 用户决定 |
| §6.5 隐藏清单 → **§6.7 权限区域**(默认拒绝的白名单) | §6.7 | 新需求 |
| 记忆不出境:配置开关 → **三层强制(类型/渲染/存储)** | §4.6 | 审查 P-1 / S5 |
| §5.6 的 L3 → **L3a / L3b**,后者并入 L5 永久禁止 | §5.6 | 审查 P-1 |
| 设备身份 → **统一 WebAuthn**,不存长期 bearer 秘密 | §6.3.1 | 审查 S12 |
| **B1 / B2 从 Backlog 提到 P3b** | §13 / §14 | 新需求 |
| P3 **拆为 P3a / P3b** | §14 | 审查 S15 |
| 备份**保持手动**,新增「送达度提醒」 | §8.5 | 用户决定(D17) |
| `backup.ps1` 未加密目标 警告 → **拒绝执行** | §8.5 | 审查 P-4(已实施) |
| §9.2 工具结果 **默认只记摘要不记正文** | §9.2 | 审查 P-2 |
| 任务数 55 → **约 75**(P3 拆分后) | §14 | — |

### 0.2 三条贯穿全文的机械规则

> **NUM-1**:显存数字只允许出现在 **§8.1.2 一张组件表**里。其余章节一律引用 `component id`,不得复述数字;任何预算表的行必须由 component id 求和生成,**禁止手写小计**。
>
> **CFG-1**:凡文档里写着「硬断言 / 铁律 / 配置层不接受其他取值」的项,**不得存在于任何 AI 可写路径**。
>
> **DOC-1**:现行文档不引用已作废文档。仍然有效的内容必须**内联**进现行版本。

---

## 1. 定位与范围

> **一个真正的私人助理。** 记忆是它的核心,不是附加功能。
> 资产生成与投资研究是围绕这个核心的能力扩展,不是并列的三条腿。

| 领域 | v1 | **v2.x** | 说明 |
|---|---|---|---|
| **日常助理 + 记忆** | 场景之一 | ★**核心** | §4,持续打磨 |
| **常驻清醒助手** | 无 | ★**新增** | §17 · CPU · 0 显存 · 只读授权区域 |
| 电脑操控 | 场景之一 | 保留,服务于核心 | 优先 UIA 树(理由见 §8.2) |
| 语音 | P4 / P7 | 提前,常驻 | 记忆的主要写入通道 |
| 2D 资产 | 本地重型 Flux FP8 | 本地 ComfyUI + 云端 nano-banana 双轨 | §5 |
| 3D 资产 | 本地生成式 | 推迟 | Backlog B3 |
| 模型管家 + 提示词转译 | 无 | ★资产侧的真正重点 | §5.3 / §5.4 |
| 投资自动化 | P8 / P9 完整实现 | 只预留接口 | §7 · Backlog B4 |
| **多设备记忆** | 无 | ★**新增,进 P3b** | §4.11 |
| iOS 访问 / 外网访问 | Backlog B1 / B2 | **提到 P3b** | 因「手机远程可调用」 |

---

## 2. 目标机器

| 部件 | 实际 | 核实 |
|---|---|---|
| GPU | RTX 5080 16GB · sm_120 · **驱动 610.62** · 84 SM | 已实测 |
| 总显存 | 16303 MiB = **15.92 GiB** | 已实测 |
| CPU / 内存 | Ryzen 9 9950X 16C/32T · 64 GB(可用约 49 GB) | 已实测 |
| 存储 | C: 392GB空 · **D: 578GB空 + E: 467GB空(同一块物理盘 Disk 1)** · G: 移动固态 461GB空(Disk 2) | 已实测 |
| 主板 | ASUS PRIME X870-P WIFI · 尚有 2 个空 M.2 插槽 | 已确认 |
| 网络 | **有线 1 Gbps** → FRITZ!Box LAN1 · 延迟 <1ms · 抖动 0 | 已实测 |
| 网络备注 | ★ **下行实测 ≥ 232 Mbps**(HF 多连接 29 MiB/s,两次一致)。~~之前记的 ~11 MB/s~~ 是 **NVIDIA CDN 单连接限速**,不是接入侧瓶颈。**上行带宽仍未知** | 已实测 |
| CUDA | Toolkit **13.2**(nvcc V13.2.51)· sm_120 编译+运行已验证 | 已实测 |
| 编译链 | cl 19.38.33145 · cmake 3.31.6 · ninja 1.12.1 · 经 `devshell.ps1` 校验 | 已实测 |
| ComfyUI | `D:\ComfyUI` · torch 2.11.0+cu128 · arch list 含 sm_120 · models 16.92GB | 已实测 |
| **桌面显存占用** | 轻负载 **691–1085 MiB**;重负载估 2.5–3.5 GB,**上界待 P1-A7** | 部分实测 |
| **BitLocker** | **C/D/E/G 四卷全部 Off** ⚠ | 已实测 |
| 显示输出 | **永久接独显**(D8 + **D24 冻结**,见 §16)。核显不再是任何情况下的备选项 | 已决 |
| **UE5** | `C:\Program Files\Epic Games\` 下装有 **UE_5.5 / 5.6 / 5.7**;工程 `E:\.UE5.5\FloatingRoom2\` | 已实测 |
| **桌面显存采集** | 逐进程 NVML 返回 `[N/A]`(WDDM)。总量用 `nvidia-smi memory.used`;逐进程用 `\GPU Process Memory\Dedicated Usage` **仅排序不求和**(高估 27%) | 已实测 |

---

## 3. 系统架构

> ⚠️ **架构已更正(2026-07-29,D43;此图未重绘)**:下图仍画着 **Tailscale + WebAuthn** ——
> 这两者都已废止。局域网互联改走 **mTLS**(应用自有 CA,私钥落 TPM 不可导出),
> 命名走 `localai-<hub-id-short>.local`,不再用 Tailscale/MagicDNS;WebAuthn 早于 D34 作废
> (非本机设备上不再有浏览器,RP ID 不可变陷阱整类消失)。统一入口的"认证"一行现在是 mTLS 客户端证书,不是 WebAuthn。
> 保留原图仅作历史参照;以 D34 / D43 与 P3b 实机记录为准。

```
     本机 App        局域网第二台 PC(尚不存在)        iPhone(PWA)
        │                    │                          │
        └────────┬───────────┴──────────────────────────┘
                 │  Tailscale (WireGuard) · MagicDNS · Tailnet Lock
        ┌────────▼──────────────────────────────┐
        │  统一入口:WebAuthn 认证 · 六元组 · 审计 │
        └──┬────────┬────────┬────────┬─────────┘
     ┌─────▼──┐ ┌───▼───┐ ┌──▼────┐ ┌─▼────────────┐
     │ MODEL  │ │  JOB  │ │SESSION│ │ ★ MEMORY     │
     │/v1/... │ │/jobs/ │ │ 语音   │ │ /memory/...  │
     └─────┬──┘ └───┬───┘ └──┬────┘ └─┬────────────┘
           └────────┴────────┴────────┘
        ┌─────────────▼──────────────────┐
        │  编排 + 服务端权限策略(六元组)   │
        └──┬────────┬────────┬───────┬───┘
   ┌───────▼──┐ ┌───▼────┐ ┌─▼─────┐ │
   │★记忆系统 │ │低风险  │ │高权限 │ │
   │ 五层+双轨│ │HTTP MCP│ │stdio  │ │
   └──────────┘ └────────┘ └───────┘ │
        ┌───────────────────────────▼──┐
        │  GPU 资源仲裁器(NVML 实测)    │
        └──┬───────────────────────────┘
   ┌───────▼─────────────────────────────┐
   │ llama.cpp │ ASR+TTS │ 小VLM │ComfyUI │
   │            RTX 5080 15.92 GiB        │
   └──────────────────────────────────────┘
   ┌──────────────────────────────────────┐
   │ embedding / rerank (CPU,不占显存)    │
   │ ★ Vigil 常驻助手 (CPU,0 显存)        │
   └──────────────────────────────────────┘

   云端(过出境闸门):escalate.cloud · nano-banana
   交易链:本期仅预留接口与表结构
```

### 3.1 四个协议平面

| 平面 | 契约 | 用途 |
|---|---|---|
| **MODEL** | `/v1/...` OpenAI 兼容 | 对话 · 视觉 · 嵌入 |
| **JOB** | `/jobs/...` | 出图 · 长任务 |
| **SESSION** | 语音会话 | 半双工(本期)/ 全双工(B5) |
| **★ MEMORY** | `/memory/...` | **新增**。检索 · 候选提交 · 面板 CRUD。见 §4.11 |

> **MEMORY 单列一个平面,不是挂在 MODEL 下面 —— 这是 §4.6 出境阻断的结构性前提**(见 §4.6.3)。

### 3.2 能力别名

| 别名 | 上下文 | 备注 |
|---|---|---|
| `assistant.voice` | 8K | 延迟优先 |
| `assistant.fast` | **由当前装着哪个 LLM 组件定义,最低 8K**(D25;原文写「由当前档位 manifest 决定」,档位已取消) | 不再写死 16K(审查 C-4) |
| `assistant.deep` | 32K | 上限 |
| `assistant.vision` | 16K | 截图降采样 1280×720 |
| `code.default` | 32K | |
| `embedding.default` / `rerank.default` | — | **跑 CPU** |
| `image.concept` | — | JOB · ☁ nano-banana |
| `image.texture` | — | JOB · ⌂ 本地 ComfyUI |
| `speech.stt` / `speech.tts` | — | 常驻 |
| **`assistant.resident`** | **2048 硬上限** | ★新增 · Vigil 专用 · CPU |
| `trade.research` | 32K | `not_provisioned` |
| `escalate.cloud` | 200K+ | 仅升级推理能力 |

**★ 刻意不存在(工具注册表级,不是散文约定)**:

| 工具 | 理由 |
|---|---|
| `trade.execute` | §7:本期只预留接口,不实装交易 |
| **凡改变 GPU 驻留集合或供电状态的调用** | **D25**。涵盖 `set_components` / `apply_preset` / `unload` / `power` —— **不论叫什么名字**。它是「从提示词注入到卸载安全相关组件」的一条直链 |
| `secret.request`(P1–P5) | §6.9.0:带外控件的接收方要到 P6 才存在 |

**★ `gpu.apply@workstation` 的宿主后缀不是参数,是工具身份的一部分** ——
照 §6.3.1 既有句式:「`execute@workstation` 与 `execute@lan-pc-2` 是**两个不同的工具**」。
远程会话的工具池里**根本不存在** `gpu.apply@workstation`。**做成参数它就能被不可信计划者操纵。**

**★ GPU 变更面不构成第五个协议平面。** §3.1 只有 MODEL / JOB / SESSION / MEMORY 四个。
变更面走 **Tauri IPC / 命名管道**(§6.4 的「高权限本机执行器」那一行:无网络面、进程级父子信任)。
**网关上因此不存在该路由 —— 是「没有端点」,不是「有端点但拒绝」。**
「只有主机可操作」由此成为一个**不存在的能力**,而不是一条被检查的规则。

> **★ 删掉 `client_error_prompt`(D25)。** 原 §8.1.4 写
> `allowed_surfaces = ["tray", "local_app_settings", "client_error_prompt"]`,
> 第三项是客户端错误弹窗上的「升档并重试」按钮,理由是「仍是用户发起的 UI 动作」。
> **它是全库唯一一条已写下的远程写授权** —— 远程客户端的错误弹窗会提供改变主机显存分配的入口。
> 新的 `allowed_surfaces = ["tray", "local_app_settings"]`。
> 远程遇到 ❌ 时只显示缺口与「需在主机上调整」,**不提供动作**。

档位可用性矩阵已由 **§8.1.4 能力 → 组件依赖表**取代。

---

## 4. ★ 记忆系统

### 4.1 五层记忆

| 层 | 内容 | 存续 | 存储 |
|---|---|---|---|
| **L0 工作记忆** | 当前会话上下文窗口 | 易失 | LLM context |
| **L1 会话摘要** | 滚动压缩 | 数天 | PostgreSQL |
| **L2 情节记忆** | 带时间戳的事件 | 长期 | PostgreSQL + Qdrant |
| **L3 语义记忆** | 关于你的结构化事实 | 长期 | PostgreSQL |
| **L4 程序记忆** | 学会的工作流 | 长期 | **版本化 + 签名,见 §4.4.2** |

### 4.2 ★ 双轨检索

> **只做向量 RAG 的记忆系统,会在最需要精确的地方失败。**

| 轨道 | 回答 | 技术 |
|---|---|---|
| 向量检索 | 「**相关的**是什么」 | Qdrant · bge-m3 + rerank |
| 结构化查询 | 「**确切**是什么」 | PostgreSQL · SQL over 实体表 |

「我妹妹叫什么名字」——向量检索返回一堆相关片段但答不出名字,必须走结构化查询。反过来「上次聊的那个灯光问题」必须走向量。**两轨都要,由编排器按问题类型选路。**

### 4.3 实体图谱

`Person`(关系边:称谓 · 亲疏 · 重要日期)· `Project` · `Device` · `Place` · `Thing` · `Event`(带时间戳,可关联任意实体)· `Preference`(带置信度与时效性)

### 4.4 写入把关(Memory Gate)

#### 4.4.1 三问

- 是否是关于用户的**持久**事实?
- 是否会影响未来的行为?
- 是否用户明确要求记住?

写入后**必须**:用户可见 · 可编辑可删除 · **记录来源**。

#### 4.4.2 ★ 置信度只能由服务端可验证的信号产生(v2.2 新增)

> **审查 S1**:客户端自报 `user_explicit` 可铸造 confidence=1.0 的「用户事实」并自动 supersede 真实事实。而 L4 是可执行工作流 —— **这条链直通代码执行**。

Gate 输入必须携带 `provenance`,且 `body` 里的 `user_explicit` 字段**一律忽略**(与 `device_id` 同等对待):

| 来源 | `source_confidence` | 能否自动 supersede |
|---|---|---|
| 记忆面板上的显式点击(带票据) | 1.0 | ✅ |
| 客户端 UI 标记,且该输入经设备密钥签名 | 1.0 | ✅ |
| 助理从对话流推断 | ≤ 0.6 | ❌ |
| `tool_result` / `web_content` / `rag_chunk` 派生 | ≤ 0.4 | ❌ 且强制 `pending` |

`provenance ∈ {user_typed, user_voice_asr, tool_result, rag_chunk, web_content}`,**只有前两类可产生 explicit 候选**。

**`pending_review` 禁止批量确认。** 逐条确认,且「这条将取代哪条现有事实」是主视觉。积压上限 50 条,超限告警并**暂停接受远程候选** —— 积压本身是攻击信号。

> 这是 §6.6「计划本身要被审查」在**写侧**的对应物。

#### 4.4.3 ★ L4 是代码资产,不是记忆的一层(v2.2 新增)

> **审查 S2**:L4 程序记忆是可执行工作流,却与 L2/L3 共用写权限。

1. L4 写入**仅 `trusted-local`**,且**逐次哈希绑定批准**(与 §12.4 对批量文件操作同级)。远程档位的路由表里**不存在 L4 写端点**。
2. L4 条目**版本化并签名**。策略层在消费任何 L4 派生的提案前,校验源条目当前哈希与**你最近批准过的**哈希一致,不一致则拒绝执行。这把 §12.4 的哈希绑定从「提出到执行之间」延长到「**教会到执行之间**」。
3. L4 的 supersede = **需批准的版本升级**。§4.5 的自动裁决对 L4 一律不适用。
4. **远程档位检索默认排除 L4** —— 读工作流步骤对远程问答无价值,却给了攻击者一张可执行面的地图。

### 4.5 时效、冲突与可解释性

每条事实携带 `asserted_at` · `confidence` · `source_ref` · `superseded_by` · **`origin_device_id`** · **`write_seq`**。

**冲突**:新旧矛盾时**不覆盖**,新增一条并将旧条标记 superseded,保留完整历史。
**铁律**:**自动事实不得 supersede 用户事实**。否则任何能把文件写进授权区域的东西都能改写你的记忆。

**时效**:部分事实天然过期 → 定期 review 提示。

**★ 可解释性是信任的前提**:助理说「我记得你提过 X」时,必须能一键展开看到 **来源 · 时间 · 原文片段 · 记录设备**。没有可解释性的记忆既不敢信任,也无法纠错。

### 4.6 ★ 隐私红线(2026-07-26 按 D22 重写)

> **★ 本节最重要的一句:记忆库在磁盘上是明文。**
> 决议 D22 推翻了 D2(BitLocker VHDX),记忆库改为普通文件夹 `D:\AI\state\memory\`。
> 文档不得再声称任何形式的静态加密 —— **声称高于实际的保护,比防护弱本身更危险。**

| 规则 | 状态 |
|---|---|
| ~~静态加密~~ | **❌ 已取消(D22)。磁盘上是明文,备份盘上也是明文** |
| **运行态保护** | ✅ **OS 账户隔离**(§6.8)+ **网关认证** |
| 同机其他账户 | ✅ NTFS ACL |
| **永不出境** | ✅ 见 §4.6.1–4.6.3 的三层强制。**这一条不受 D22 影响** |
| 不进日志 | ✅ 见 §9.2 |
| 物理访问 | ❌ **无保护**。拿到硬盘或备份盘的人可直接读全部记忆 |

#### 4.6.0 D22 之后,保护面到底剩什么

**仍然有效的(这些恰恰是日常最可能发生的威胁):**

| 威胁 | 防住了吗 | 靠什么 |
|---|---|---|
| ComfyUI 的第三方节点读记忆库 | ✅ | §6.8 账户隔离 + NTFS ACL |
| 网络上的其他设备越权读 | ✅ | 网关认证 + §4.11.4 S2 结构性隔离 |
| 记忆内容被写进明文日志 | ✅ | §9.2 |
| 记忆内容被送进云端 prompt | ✅ | §4.6.1 三层强制 |
| 提示词注入改写记忆 | ✅ | §4.4.2 置信度只由服务端信号产生 |

**不再防的:**

| 威胁 | 后果 |
|---|---|
| 工作站硬盘被物理取走 | 全部记忆可读 |
| 移动固态丢失 | 备份里的全部记忆可读 |
| 拿到机器且能开机 | 同上 |

> **一个诚实的补充:这个取舍不是单向的。**
> 加密把「被窃取」的风险换成了「密钥丢失」的风险,而后者在 §15 风险清册里原本是**高**级
> —— BitLocker 恢复密钥丢失 = 记忆库永久不可读,而这对一台家用单人机器来说,
> **发生概率高于定向窃取**。D22 消除的正是这个风险。
> 代价写在上表,不粉饰;收益也不抹掉。

#### 4.6.1 三层强制(缺一不可)

> **审查 S5**:`no_export` 属性在 `r.text` 这一步就失效了。`r.text` 是 `str`,不是 `MemoryResult` —— **引用链在属性读取那一刻就断了**。而 `ctx = "\n".join(r.text for r in results)` 是 RAG 注入的标准路径,**每次都走这一步**。

| 层 | 做法 |
|---|---|
| **类型层** | `MemoryResult.text` 返回 `TaintedText`(密封,**不实现 `__str__`**,不实现 `Exportable`),只能经 `render_for_prompt(backend)` 取值;`backend.egress == true` 时抛 `MemoryExportViolation` |
| **渲染层** | 出境闸门的**最终**强制点放在**已组装完成的 prompt 字符串**上,不是组装前的类型检查 |
| **存储层** | `CHECK (no_export = true)`;`UPDATE ... SET no_export = false` 在数据库层被拒 |

#### 4.6.2 ★ 必须明文写下的一句话

> **不得声称「记忆零外发」是被证明的。**

`no_export` 作用于对象与已组装文本,**无法阻止你手动复制粘贴**。§10 的断言相应改为两条可验证的:

1. 「不存在任何程序路径使记忆文本进入出境载荷构造器」—— 否定用例须覆盖 **str 拼接 · f-string · 模板渲染 · 日志格式化** 四种取值方式
2. 「重叠检测在记忆 eval 集上的召回率 ≥ X%」—— X 由实测填,不预设

#### 4.6.3 Off 档的结构性保证

> **审查 S6**:Off 档「记忆可查 + 云端推理」若靠 UI 文案与运行时判断,用户看到的提示会与实际发生的事**恰好相反**。

按 §6.2「按会话挂载工具池而非运行时判断」落实:

- 模型别名注册表给每个后端打 `egress: true|false`
- 会话建立时若解析出的生成后端 `egress=true`,**该会话的工具池里根本不挂载 `memory.search`**
- 记忆检索由客户端走**独立的 MEMORY 平面**单独发起,结果只渲染成卡片,**永不回流进 MODEL 平面的请求体**

**「云端基于记忆推理」在会话结构上不可表达**,而不是靠运行时判断或文案。

> Off 档的正确表述是「**记忆可查 + 云端推理**」,**不是**「云端基于记忆推理」。UI 文案必须写清。

### 4.7 ★ 打扰评分(整节替换 v2.1 的「克制的主动性」)

> **本节起因**:用户于 2026-07-26 明确决定**推翻 v2.1 §4.7 放权**。原文「只在你曾明确请求过的事情上主动」与「无时无刻清醒、持续分析」的助手直接冲突。

#### 4.7.1 放权的实际含义

放权不是「随便打扰」,而是**把「能不能主动」从一条禁令,改成一个有阈值、有配额、有反馈的机制**。

原则从「只在你请求过的事上主动」变为:

> **主动性是挣来的。** 新的打扰类型从最低级别起步,靠你的反馈逐步解锁更高级别。

#### 4.7.2 四段流水线与信任边界

```
① 提名  ← Vigil(1.7B CPU)。只填受约束的槽,不给分数
② 校验  ← 确定性代码。claim_class 白名单、证据行号存在性、来源类型
③ 评分  ← 纯确定性公式。Vigil 读不到公式,也读不到自己的得分
④ 投递  ← 配额闸 + 分级路由
```

> **★ 核心难题与解法**:§6.6 说模型是不可信计划者。**如果评分由模型自己给,它就能给自己打高分来打扰你。**
> 解法不是「让模型诚实」,而是**改变模型的任务** —— 它只做分类(属哪类 / 命中哪个既有主题 / 是否新颖 / 是否命中承诺 id),判断留给它读不到的确定性公式。

#### 4.7.3 提名契约(模型只能填槽)

`title` **不是模型自由文本**,由 `claim_class` 固定模板 + `{root,rel}` + 证据行号拼装。
`claim.summary` 上限 **80 字**,必须以模板起头 ——「`{rel}` 在 `{行号}` 附近出现与 `{topic}` 相关的变化」。

> 自由文本越长,注入载荷的携带能力越强。

#### 4.7.4 五维评分与来源封顶

| 维 | 含义 | 权重 |
|---|---|---|
| U 时效性 | 错过就没用了吗 | 0.25 |
| S 后果严重性 | 由 `claim_class` 决定 | 0.30 |
| R 关联度 | 是否命中你曾表达关心的主题 | 0.20 |
| N 新颖性 | 是否重复既有信息 | 0.15 |
| C 置信度 | 证据强度 | 0.10 |

**★ 来源封顶(防操纵的核心):**

| `claim_class` 档 | 开放给 |
|---|---|
| `irreversible_loss` / `financial` / `system_degradation` | **仅 `source=deterministic`**(§4.7.6 白名单与 §9.3 告警器) |
| `self_commitment` 及以下 | `source=model_nominated` **封顶 S=0.40** |

`deadline_src=file` 的 U **上限 0.35**(与 `inferred` 同档)—— 文件里的时间戳正是攻击者最容易控制的东西,「字面出现在 quote 里」只证明它来自那个文件,不证明它是真的。

> **修复后重算注入攻击链**:Base = 100×(0.25×0.35 + 0.30×0.40 + 0 + 0.15×0.30 + 0.10×1.0) = **34.8** → ×0.925 = **32** → **L1 日报**。
> 攻击者能拿到的最好结果是「进日报」,不是「弹通知」。

#### 4.7.5 分级与配额(v1 只开两档)

| 级 | 分数 | 通道 | v1 |
|---|---|---|---|
| L0 | < 20 | 静默记录 | ✅ |
| **L1** | 20–59 | **每日 22:00 日报,每天 3 条** | ✅ |
| L2 | 60–79 | 托盘角标 | ❌ P9 之后 |
| L3 | 80–94 | 系统通知 | ❌ 需解锁 |
| **L4** | ≥ 95 | 强提醒 | ✅ **仅确定性白名单** |

**★ 硬性配额优先于一切分数**:每日主动打扰(L2 及以上)**上限 3 次**。模型再怎么给自己打高分也突破不了。**配额不依赖模型诚实,这是它相对评分公式的根本优势。**

**唯一自适应通道**:同一 `claim_class` 累计 **3 次**你标记「该早点说」→ 解锁 L3。

> **为什么 v1 砍到两档**:完整设计的自适应能力依赖一次 shadow 期末的 2–4 小时人工标注。那次标注不会被完成(单人 · 业余 · 6–12 个月跨度)。不完成则整套退化成一个拍脑袋的固定公式,而 L3 阈值在真实分布下几乎无人能过 —— 你一个月收到 2 条通知,结论是「这功能没上线」,然后关掉常驻助手。

#### 4.7.6 确定性 L4 白名单(不经模型)

磁盘水位越线 · 显存租约被拒 · 出境费用达月度上限 80%/100% · **备份任务失败** · 记忆写入失败 · 隔离区即将到期 · 你曾明确设定的时间提醒。

#### 4.7.7 反馈与可解释性

日报每条两个按钮:**「该早点说」** / **「不用告诉我」**。

每次打扰可展开看到**各维度得分与判定依据** —— 承 §4.5「没有可解释性的东西既不敢信任也无法纠错」。

#### 4.7.8 手动可调

全部参数(阈值 · 配额 · 各维权重 · 日报时间 · 各 `claim_class` 的默认级别)在设置里可手动调整。
**但配置文件位置受 CFG-1 约束** —— 见 §6.7.4。

### 4.8 多模态写入通道

随口一句「记一下,下周要交房租」应直接进入 L2/L3。这要求语音层与记忆层打通,也是本版把语音提前并设为常驻的主要理由。

**选 `speech.cpu` 时语音走 CPU**(§8.1.3)。CPU 语音的定位是**异步的记忆随手写入**,不是对话 ——「记一下,下周三交房租」是 3 秒音频,CPU ASR 实时率 0.3–0.6 意味着 1–2 秒转写,对写记忆完全够用。**文档必须写清这个定位,不要让人以为低档能用语音对话。**

### 4.9 冷启动、评测与兜底

#### 4.9.1 冷启动

P3a 提供一次**结构化初始化会话**:助理按实体类型逐项发问(重要的人及称谓 · 常用设备 · 进行中的项目 · 明确的偏好与禁忌),你口述,它写入 L3 并当场展示、允许你改。一次约 20–30 分钟。

**★ 顺带完成敏感度定级**(§4.11.4 的 S2 标记)。§4.9.1 的初始化会话只跑一次,**错过就没有第二次**。

#### 4.9.2 评测集与四项指标

| 指标 | 含义 |
|---|---|
| 召回率 | 该记得的记住了 |
| 精确率 | 检索出来的确实相关 |
| **误记率** | 记住了错的事实 —— **最危险** |
| **溯源完整率** | 每条记忆都能展开到原始来源 |

**自动记忆(Vigil 派生)单列一组指标**,其误记率风险显著更高。

#### 4.9.3 漏记与误记的兜底

- **漏记**:你说「你怎么不记得 X」时,助理须能**即时补录**并标注「事后补录 + 补录时间」
- **误记**:每周推送一条「本周新记住的事」摘要(仅标题级),扫一眼即可发现离谱条目

### 4.10 规模增长

L2 只增不删(进 GC 白名单)。数据模型带 `last_accessed` 与 `access_count`;检索时对久未命中且低置信度的旧条目**降权**(不删除);超过 N 年的 L2 事件**归档为 L1 级摘要**,原文保留但不进主索引。

### 4.11 ★ 多设备记忆(v2.2 新增)

#### 4.11.1 拓扑:单一权威 + API 访问

记忆库**只有一份**,在工作站的 `D:\AI\state\memory\`(普通文件夹,**不加密** —— D22)。
其他 PC 与手机通过 MEMORY 平面读写。

**不做分布式副本。** 理由:单人使用真并发极少;§4.5 的「不覆盖,新增并标 superseded」在单权威下是几行代码,分布式下是研究课题;多份加密副本 = 密钥管理复杂度爆炸(而「避免密钥管理复杂度爆炸」正是选这个拓扑的理由之一)。

**已接受的后果**(用户 2026-07-26 明确接受):

> **工作站关机 = 所有设备都没有记忆 + 所有机器 AI 失效无法调用。**

本文档**不偷偷改写为「工作站必须常开」**。但失效必须是**可诊断、可预期**的 —— 见 §4.11.6。

#### 4.11.2 写入路径

所有写入都经工作站的 Memory Gate。**客户端无本地队列** —— 无网即明确失败。

> **为什么不做 Outbox**:iOS 的 WebCrypto 不用 Secure Enclave,`extractable:false` 只挡 JS,**密钥材料与密文同在一个 IndexedDB 里**;而 IndexedDB 在 iOS 备份范围内,默认非端到端加密的 iCloud 备份是「你无法单方面终止访问的系统」。按 §4.11.5 的判据,**加密后的 Outbox 仍构成出境**。

**推翻条件**:`mobile-remote` 写入送达率连续 4 周 < 99% → 进 P3.5 按受控规格实现(纯素材 · 非记忆 · 72 小时 · 恢复后逐条确认)。

#### 4.11.3 `write_seq` 与冲突

- `write_seq` **全局单调**,由工作站 PostgreSQL 的 advisory lock 唯一分配。**客户端不参与分配。**
- **时间戳以服务端为准**(客户端时钟不可信)
- 冲突沿用 §4.5:不覆盖,新增并标 superseded
- **自动事实不得 supersede 用户事实**(§4.5 铁律),多设备下「用户事实」的认定见 §4.4.2

`source_ref` 扩展为携带 `origin_device_id` 与设备显示名。**记忆面板必须显示「这条是在哪台设备上记的」** —— 否则 §4.5 的可解释性在多设备下就残缺了。

#### 4.11.4 敏感度分域(两级,不是三级)

| | P3b 实装 | P3.5 启用 |
|---|---|---|
| **S2 机密** | **结构性不可见**,任何远程档位永久不可读,**无提级路径** | 不变 |
| **S0 / S1** | **不区分**。远程档位可读全部非 S2 内容 | 用一个月真实读取日志定边界后启用三级分域 |
| 约束手段 | 读取预算(首月只观察不硬停)+ 逐条全量审计 + **溯源展开需生物识别** | 增加域过滤 |

**为什么 S2 可以第一天就强制而 S0/S1 不行**:S2 的定级是**确定性的**(IBAN / 税号 / 证件号 / 地址模式 / 「密码是」/「恢复密钥」的正则族 + 用户手动标记),不需要 LLM 判断也不需要真实数据校准;量很小,不会造成「手机很傻」;而它恰恰是丢手机场景下伤害最大的部分。S0/S1 的边界**必须靠真实数据**,在没有它之前 fail-closed 会把手机压到不可用。

**结构性隔离,不是运行时过滤:**

```
PostgreSQL:  role ai_mem_local  → 基表全表
             role ai_mem_remote → 只 GRANT SELECT ON v_memory_nons2
Qdrant:      collection mem_main  ← 远程会话唯一持有的句柄
             collection mem_s2    ← local 独有,独立 loopback 端口
```

> 漏了 payload filter 是**沉默的**(返回多余结果,没人报错);漏了 collection 句柄是**响亮的**(直接异常)。§12.3 门禁第三条要的正是后者。

**「我妹妹叫什么」这条 P3 验收例句,必须在手机上也通过。** 否则这条验收是自欺。

#### 4.11.5 ★ 「出境」的精确定义

> **审查 S11**:五份设计给了「境」五个定义。

| # | 判据 | 结论 |
|---|---|---|
| **D1** | 内容离开**你控制的设备集合** | **出境**。禁止 |
| **D2** | 内容进入**你无法单方面终止访问的系统** | **出境**。禁止(涵盖 iCloud 备份、第三方云) |
| **D3** | 内容投递到**你已注册、可吊销、可审计的设备**,且**不落盘** | **不是出境** |

**D3 的三个条件必须同时满足。** 原设计的「已下发内容随之失效」是一个**已知不成立的前提**(下发出去的东西收不回来),用假前提证明「投递不是出境」是边界论证的典型失效形态。改为「**不落盘**」—— 这是可兑现的。

**明文例外条款**:

1. **iOS 应用切换器快照**属于系统行为,PWA 无法阻止。**明文接受为已知风险**,不在「禁止」与「接受」之间摇摆
2. **手动复制粘贴**无法阻止(§4.6.2)
3. **Tailscale 控制平面可见连接元数据**(不含内容),记入 §5.6 的第三方清单

#### 4.11.6 故障分类(§12.3 失败可见)

五种在客户端都表现为「用不了」,但原因与应对完全不同,**必须区分**:

| 故障 | 客户端表现 |
|---|---|
| 工作站关机 / 休眠 | 「工作站未响应 · 上次成功连接 3 小时前」 |
| 服务未起 | 「工作站在线,但记忆服务未启动」 |
| **记忆卷未挂载** | 「记忆库已上锁」+ 解锁指引 |
| 网络断 | 「网络不可达」+ 区分局域网/tailnet/互联网 |
| 工作站在完全 Off 档 | 「AI 已完全关闭」 |

> **诚实限制**:iOS 的 Tailscale 走 NetworkExtension,PWA 的 JS 拿不到 peer 状态。
> **明确合并为一态**:「无法连接 · 上次成功连接 {时间}」。§12.3 要求的是「不要伪装没事」,不是「必须诊断出根因」。

#### 4.11.7 Wake-on-LAN

**不做。** 理由:它把「工作站关机 = 全失效」这条已被接受的约束偷偷改写成「工作站必须能被远程唤醒」,而唤醒路径本身是一个新的、无认证的攻击面。若实际使用中确实频繁受阻,进 P3.5 单独评估。

---

## 5. 资产工作室

### 5.1 事实澄清:nano-banana 不做 3D

> **nano-banana(Google Gemini 2.5 Flash Image)是 2D 图像生成 / 编辑模型,不生成 3D 网格。**

它原生接受参考图 + 指令式编辑,角色一致性好,适合概念阶段。**3D 本期不做**(Backlog B3)。

### 5.2 双轨分工

| 阶段 | 走哪条 | 理由 |
|---|---|---|
| 概念图 / 风格探索 / 迭代修改 | ☁ nano-banana | 交互自然,不占显存不占磁盘 |
| 批量生产 / 风格锁定 / 精确控制 | ⌂ 本地 ComfyUI | 可复现 · 不花钱 · 不出境 |
| 贴图 / PBR / de-light | ⌂ 本地 ComfyUI | 需要确定性管线 |
| 3D | ⏸ 本期不做 | §5.1 |

### 5.3 ★ 模型管家(Model Curator)

建在已有的 **ComfyUI-Manager** 之上,不重造下载与安装。缺的是**语义层与治理层**:

```
自然语言需求 → 意图理解 → 结构化检索条件(底模 · 风格 · 用途 · 许可证)
→ 查询 Civitai / HuggingFace → 候选 + AI 解读 → 你选择
→ 调 ComfyUI-Manager 安装 → 校验哈希 → 写入模型注册表
```

**注册表额外记录**:许可证 / 商用条款 · 底模兼容性 · 最后使用时间与次数 · **重获成本** · 你的评价笔记。

**清理助手**:引用计数保护 + **只提示不自动删**。

### 5.4 ★ 提示词转译器

**你不写硬核 prompt。** Asset Spec 是唯一人机接触面:

```
中文自然语言 + 参考图 → Asset Spec(与目标模型无关)
→ 多目标编译器 → SDXL prompt / Flux prompt / nano-banana 指令 / ComfyUI workflow JSON
```

**反向能力**:贴进别人的 prompt → 解析成 Asset Spec → 逐段解释 → 存为可复用模板。

### 5.5 ComfyUI 集成

不重装不迁移存量;配置 `extra_model_paths.yaml` **额外**读取 `D:\AI\models\`;定位为 JOB 平面的媒体 Worker;显存受 GPU Broker 管辖。

**★ 并且跑在与记忆栈隔离的 Windows 账户下**(§6.8)。

### 5.6 ★ 数据出境闸门

#### 5.6.1 两级授权

| 级别 | 适用 | 机制 |
|---|---|---|
| **会话级授权** | 概念阶段(公开素材 / 自己的草图) | 一次授权覆盖本次迭代会话;**会话结束自动失效**;实时显示已发送条目与累计费用 |
| **逐次确认** | 项目专有素材 · 生产阶段 · 首次使用某条新路径 | 执行前逐条展示 |

#### 5.6.2 分级粒度(L3 已拆)

| 级 | 允许发送 | 状态 |
|---|---|---|
| L0 | 不启用任何云端路径 | Vigil 永久锁定在此级 |
| L1 | 仅你手打的问题文本 | |
| **L2** | + 你**显式选中**的内容 | **`escalate.cloud` 锁定在此级**(D-Q2) |
| **L3a** | + 文档 / 资产 / 代码的 RAG 检索片段 | 可临时升级 |
| **L3b** | + **记忆库来源的 RAG 片段** | **并入 L5,永久禁止,不可临时升级** |
| L4 | + 截图与文件原文 | 需逐次确认 |
| **L5** | + 记忆库内容 | **永久禁止** |
| **L5-cred** | + **经 secret provider 取出的值及其一切派生物**(表单截图 · 动作预览 · 执行回执 · OCR 文本) | **永久禁止,不可临时升级,不可逐次确认** |

> **临时升级不适用于记忆库来源的片段。**

#### 5.6.3 预算

月度上限是**设置项,随时可手动调整**,不写死。达 **80%** 告警,达 **100% 硬停**并提示可手动提额或改走本地 ComfyUI。**不静默继续计费。**

#### 5.6.4 第三方条款快照

启用 nano-banana 时,把当时的 Gemini API 数据使用条款存快照至 `00-docs/snapshots/`。同样适用于 Tailscale 的隐私条款。列入 §13 的定期复核。

---

## 6. ★ 安全与权限

### 6.1 核心原则

> **能力升级可以上云,权限升级永远不行。**

```
计划层  ← 可升级到云端,输出【结构化提案】
   ↓
策略层  ← 永远本地:权限校验 · 参数校验 · 额度 · 动作预览
   ↓
执行层  ← 永远本地:stdio 执行器
```

**★ 「本地」的精确定义**(v2.2 澄清):

> **策略层与执行层必须与被操控的资源同机。**

「永远本地」指的是**相对被操控资源**,不是「每台机器都有一份完整权威」。这与 §4.11 的「记忆单一权威」不矛盾:第二台 PC 对自身的操控需要本机策略层,但它的**记忆**仍走工作站。

### 6.2 权限强制:六元组

服务端按 **(用户 / 设备 / Agent / 工具 / 参数 / 额度)** 强制。

**参数级尤其关键**:同一个「写文件」工具,**路径参数决定它是安全还是灾难**。六元组里如果只实现了前四维,等于没有实现。

权限在**服务端按档位挂载工具池**,而非运行时判断。低权限档位的会话在建立时其工具池里就**不存在**高风险工具。

### 6.3 权限档位

| 档位 | 设备 | 问答/语音 | 资产 | **记忆读** | **记忆写** | 文件操作 | 电脑操控 |
|---|---|---|---|---|---|---|---|
| `trusted-local` | 本机 App | ✅ | ✅ | **全部** | ✅ 含 L4 | 逐次确认 | 逐次确认(**本机**) |
| `trusted-lan` | 局域网 PC | ✅ | ✅ | **非 S2** | ✅ 不含 L4 | 逐次确认 | 逐次确认(**仅自身**) |
| `mobile-remote` | 手机 | ✅ | ✅ | **非 S2** | ✅ 不含 L4 · 无队列 | ❌ | ❌ |
| **`resident-observer`** | Vigil | — | ❌ | **仅 `mem.exists` + 投影** | **仅 `vigil_observations`** | **只读白名单** | ❌ |

#### 6.3.1 ★ 电脑操控是二维的(v2.2 修正)

v2.1 的「电脑操控」一列把两个维度压成了一维。实际是:

| 发起 ＼ 操控 | 工作站 | 局域网 PC | 手机 |
|---|---|---|---|
| **本机 App** | 逐次确认 | ❌ | — |
| **局域网 PC** | ❌ | **逐次确认** | — |
| **手机** | ❌ | ❌ | ❌ |

**对角线 = 本机操控本机;非对角线 = 跨机操控 = 默认禁止。**

跨机必须禁的理由:§6.5 的安全边界(浏览器 profile 禁止清单、文件隐藏清单)都是**按机器**定的;远程发起时你看不到那台机器的屏幕,无法核对动作预览;允许跨机会让「丢一台设备」升级成「丢所有设备」。

**★ 操控目标必须是工具身份的一部分,不能是参数。** `execute@workstation` 与 `execute@lan-pc-2` 是**两个不同的工具**。局域网 PC 的会话工具池里只挂 `execute@self`,**根本不存在** `execute@workstation`。若做成参数,它就能被不可信的计划者操纵。

#### 6.3.2 ★ 设备身份:统一 WebAuthn(v2.2 新增)

> **审查 S12**:原设计写「手机设备密钥存 iOS Keychain(`kSecAccessControlBiometryCurrentSet`)」—— **这是原生 Security.framework 常量,PWA 调不了**。而 B1 已定不做原生 App,所以这条约束永远不会被满足;实际得到的是一个 32 字节长期 bearer 秘密躺在 IndexedDB 里,而文档上写着「生物识别保护」。

- **设备身份 = WebAuthn 平台认证器**,PC 与手机共用同一套实现(Windows 侧走 Windows Hello,私钥由 TPM 保护)
- 服务端**只存公钥与 `credentialId`**,不存任何秘密的哈希
- **不使用长期 bearer 令牌**;短寿命令牌 + `tailscale_node_id` 交叉绑定 + 双层吊销
- **配对码**:8 位 Crockford base32 · **工作站显示 / 新设备输入** · 5 分钟一次性 · 同会话失败 5 次作废 · 全局每分钟 10 次

> **方向比强度更重要**:反方向(新设备显示、工作站输入)下工作站屏幕上只有几个数字;正方向下 admin 面板可以先让你选定设备名与档位,**再完整显示你正在授予什么**。

> **`tailscale_node_id` 绑定的强度必须如实描述**:它防「密钥离机」,**不防「整机沦陷」**。文档不得用它来抵消凭据保护的弱点。

**★ RP ID 必须在 P2 之前定死。** WebAuthn 的 RP ID 一经确定不可更改。⚠ **D28 修正:工作站本机不注册 WebAuthn credential**(本机走 OS 信任)—— 所以「工作站本机 credential 作废」的风险已消除;RP ID `<tailnet>.ts.net` 只有远程设备用。因此把「定死 tailnet 名与机器名」这个 **0.2 天的命名决定**提到 P2 之前。

### 6.4 工具传输分层

| 类别 | 传输 | 理由 |
|---|---|---|
| 高权限本机执行器 | **stdio / Named Pipe** | 无网络面;进程级父子信任 |
| 低风险只读 | 认证后的 MCP over HTTP | 需跨设备复用 |
| **记忆读写** | **MEMORY 平面(独立)** | §4.6.3 的结构性前提 |

### 6.5 ★ 四项操控需求的具体设计

> 这张表是 P6 的**执行依据**。约束才是实质。

| 需求 | 风险 | 技术路线 | **关键约束** |
|---|---|---|---|
| 写文档 | 低 | 只读输入 + 写指定输出目录 | 可较宽松 |
| **操作浏览器** | **高** | Playwright / CDP,**独立 profile** | **邮箱 / 银行 / IBKR / 密码管理器 / 个人浏览器会话默认不可见**;点击提交类控件逐次确认;网页内容标记为不可信 |
| **整理文件夹** | **高** | 事务化文件操作 | 精确清单 → **哈希绑定批准** → 执行前复核哈希 → **隔离区(永不 delete)** → 尽力回滚;区域约束见 §6.7 |
| 游戏日常 | 中 | **模板匹配 + OCR + 确定性状态机** | 游戏无 UIA 树;固定 UI 用模板匹配更快更准更省(~0.2GB vs 6GB);**VLM 仅负责未知状态诊断,不得自行猜坐标继续操作** |

### 6.6 威胁模型

- **模型是不可信计划者**:网页、邮件、文档、参考图元数据、**授权区域里的文件**均可能携带提示词注入 → **计划本身要被审查**
- **资产导演 Agent 独立身份**:与日常助手共用模型别名但不共用工具池与记忆域
- **Vigil 是持续摄入不可信内容的主体**:它读的文件可能被投毒 → 见 §17.7 的隔离设计
- **记忆库是最高价值目标**:任何能读记忆的路径都是攻击面

### 6.7 ★ 权限区域(v2.2 新增,取代 §6.5 的隐藏清单)

> **本节起因**:用户明确要求「设立一个板块专门告诉 AI 哪些地方是有权限的」。

#### 6.7.1 三条基本裁决

1. **默认拒绝。** 未在白名单内的路径一律不可读。与 §6.2「按会话挂载工具池」同构。
2. **区域是分主体的。** `vigil` / `assistant.main` / `asset_director` 各有各的区域集合,不共享。
3. **黑名单优先于白名单。** 命中硬禁止清单即拒绝,不论白名单怎么写。

#### 6.7.2 数据模型

```toml
[[zone]]
id       = "proj_localai_docs"
subject  = "assistant.main"        # vigil | assistant.main | asset_director
path     = "{code}/00-docs"        # 用 paths.toml 的根变量,不写绝对路径
mode     = "ro"                    # list | ro | rw
recursive = true
purpose  = "项目文档与决策日志"
exclude  = ["**/snapshots/**", "**/worklog/**"]
```

`mode` 三级:`list`(只能看到文件名/大小/时间)· `ro`(可读内容)· `rw`(可写)。

**隔离区继承来源区域的 mode。** 从 `list` 区域移入隔离区的文件,`fs.read_head` 必须拒绝并给出 `origin_mode=list` 的理由 —— 否则隔离区会变成把 list-only 目录的正文搬进可读位置的通道。

#### 6.7.3 Vigil 的区域(最严)

| 路径 | mode | 说明 |
|---|---|---|
| `{code}/00-docs` | **元数据级** | **不给内容**。项目状态经编排器生成的结构化只读投影获取(见 §17.6) |
| `{assets}/draft` | `ro` | 自己产出的草稿 |
| 用户显式添加的工作目录 | `ro` | 默认带编译产物排除模式 |

**Vigil 永久黑名单**:`{code}/**` 的全部内容级读取 · `00-docs/snapshots/**` · `00-docs/worklog/**` · `.git/` · `.githooks/` · `config/` · `{state}/**`

> **为什么 `worklog` 与 `snapshots` 必须黑名单**:前者按定义装工具输出(第三方报错原文),后者按定义装第三方条款原文。**它们是注入载荷最自然的落点**,而 Vigil 会把「项目自身文档」当作高可信来源。

#### 6.7.4 硬禁止清单(`zones.local.toml`,不进 git)

```
%LOCALAPPDATA%\Google\Chrome\User Data\**
%LOCALAPPDATA%\Microsoft\Edge\User Data\**
%APPDATA%\Mozilla\Firefox\**
%USERPROFILE%\.ssh\**
%APPDATA%\Bitwarden\**   %LOCALAPPDATA%\1Password\**   %APPDATA%\KeePass\**
%APPDATA%\Thunderbird\**   %LOCALAPPDATA%\Microsoft\Outlook\**
%USERPROFILE%\.aws\**   %USERPROFILE%\.config\**
**/.env   **/*.key   **/*.pem   **/*.pfx   **/credentials*   **/secrets*
{state}\**                      ← 记忆库、票据、日志
<备份目标盘>\**                  ← §8.5 铁律 4
任何恢复密钥 / 主密码 / 凭证的存放位置
```

> **本清单只写路径模式,不探测本机是否安装了这些软件。** 写一条 `%APPDATA%\Bitwarden\**` 的禁止规则不需要知道 Bitwarden 装没装,而**探测本身就是凭证侦察的行为模式**。

#### 6.7.5 强制点与诚实说明

**强制在工具层**:`fs.read_head` / `fs.stat` 等工具自身校验区域,不信任调用方传入的路径。

> **诚实限制**:在 P6 建立专用 Windows 账户之前,**OS ACL 层是零防护**。工具层就是全部的边界。因此它必须按安全关键代码对待,**否定用例先于功能写**(§10)。

#### 6.7.6 配置文件的保护(CFG-1)

`zones.toml` / `zones.local.toml` / `vram-tiers.toml` / `interrupt-policy.toml` 全部:

- **不在任何 Agent 的可写区域内**(`self_protect` 通配 `config/*.toml`)
- 挂载时校验 `sha256`,不匹配则 `refuse_mount`
- 安全不变式(如出境级别、`counts_as_active_task`)移入 `{state}/policy/`,config 里只留只读镜像用于展示

**否定用例**:以 `assistant.main` 身份写 `config/*.toml` 必须被拒。

#### 6.7.7 变更流程

| 动作 | 审批级(§12.4) |
|---|---|
| 新增 `list` 区域 | 逐次确认 |
| 新增 `ro` 区域 | 逐次确认 + 展示该目录的文件计数与总大小 |
| 新增 `rw` 区域 | **哈希绑定批准** |
| 修改硬禁止清单 | **哈希绑定批准** |

**一键临时区域**:被拒绝时可一键授予「本次会话内只读」,会话结束自动失效。拒绝日志驱动扩权 —— 每周日报汇总「本周被拒最多的 3 个路径」。

> **诚实**:默认拒绝的头两周确实难用。这是白名单模型的固有代价。

### 6.8 ★ 账户隔离(v2.2 新增,从 P3 提前到 P2)

> **审查 S3**:ComfyUI 的第三方节点与记忆服务同一个 Windows 用户,是一条**绕过网关、六元组、预算、审计**的读全库路径。

| 账户 | 跑什么 |
|---|---|
| `ai-mem`(专用低权限服务账户) | PostgreSQL · Qdrant · embedding/rerank · memory-service |
| `ai-asset` | ComfyUI · 浏览器执行器 · 游戏自动化 |

- `D:\AI\state\memory\` 的 **NTFS ACL 拒绝资产侧账户**(继承关闭,显式 ACE)
- PostgreSQL 按 SSPI/SID 限制连接源;Qdrant 前置只认本地 token 的代理
- **任何其他本机连接尝试写审计并触发 §9.3 告警**(目前这类尝试完全静默)

> **★ D22 之后,本节从「第二层」升为「主要保护层」。**
> 运行态与静态的保护**全部**来自 OS 账户隔离 + NTFS ACL + 网关认证 —— 没有加密可以兜底。
> 因此本节的**否定用例必须先于功能写**(§10),与权限区域同级。

### 6.9 ★ 凭证与敏感信息(D23 · 经对抗性审查修订)

#### 6.9.0 ★★ 门禁:本节在 P6 之前整体 `not_provisioned`

> **规范说「手动输入现在唯一可用」,实现说「现在什么都不存在」——
> 这个缝隙里,你面对的唯一入口是对话框,而那恰恰是本节要禁止的动作。**

带外输入控件需要一个**接收方**(执行器),而执行器要到 P6 才存在。因此:

| 阶段 | `secret.request` | 助手的正确行为 |
|---|---|---|
| **P1 – P5** | **工具池里刻意不存在**(照 §3.2 `trade.execute` 的先例) | 遇到需要凭证的步骤:把任务拆到那一步为止,给出**你自己操作的指引**,然后停下。**不接收、不转述、不代填、不建议你发给它** |
| **P6 起** | 上线,`provider=manual` | 见 6.9.5–6.9.7 |

**这不是取消手动输入,这是手动输入在没有接收方时的唯一可能形态。**
§12.3「失败可见,不静默降级」在此的字面要求就是:**能力不存在时明确不提供,而不是降级到对话框。**

**P2 必须先行的两项**(它们保护的正是这个空窗期):**入口凭证检测器**(E1)· **`secret_ref` 登记表建表**。

#### 6.9.1 核心决定

> **凭证类敏感信息不进记忆库。** 记忆库只存引用,不存值。

IBAN · 税号 · 证件号 · 密码 · API key · 恢复密钥 —— 存放在独立加密应用中,每次使用经授权取出。

> **★ 泄露面的准确表述(原文低估了):**
> 拿到硬盘的人读不到凭证**值**,但读得到 **存在性 · 类别 · 用途 · 使用时间线**。
> 一份「你在哪家银行 · 报不报税 · 有没有加密货币恢复密钥 · 每个多久用一次」的清单,
> 对钓鱼与胁迫的价值高于「一个账户」。
> **准确说法:凭证值读不到;凭证的存在性、类别与使用时间线仍以明文可读。**

#### 6.9.2 三种取值模式

| 模式 | 状态 | 可用阶段 |
|---|---|---|
| **你自己填,不经过助手** | ✅ **P1–P5 唯一形态** | 现在 |
| **手动输入(带外控件)** | ⏸ P6 上线 · **永久保留** | P6 起 |
| 外部加密应用接口 | ⏸ 待应用建成,**前置为 P7** | 见 6.9.7 |

> **手动输入必须永久保留**(用户明确要求):外部应用可能未装、不可用、或你在另一台设备上。
> **它是唯一不依赖任何外部系统的路径。**

**★ 把这条从注释变成断言**:`provider` **不是** `secret.request` 的参数
(理由同 §6.3.1:参数可被不可信计划者操纵),而是**你在授权控件里当场选的**。
授权控件**永远**渲染 `manual` 按钮,其存在性是**编译期常量,不是配置项**。

**§10 否定用例**:`external` 已配置且健康时,授权控件仍必须渲染 `manual` 入口。

#### 6.9.3 记忆库里存什么

```
Thing "德国工资账户"
  secret_ref         : "bank.de.sparkasse-payroll.iban"   ← 句柄
  issuer             : "Sparkasse"          ← 结构化辨识字段
  last4              : "0000"               ← 让你认出是哪个账户的最小充分信息
  purpose_tag        : "salary"             ← 受控枚举,非自由文本
  sensitivity_domain : "S2"                 ← 强制
  (值不在记忆库中,任何层都没有)
```

**三条裁决:**

1. **`note` 自由文本字段取消**,改为上述结构化字段。理由:句柄不可辨识 → 你三个月后分不清
   `bank.de.a` 和 `bank.de.b` → **必然把值抄进 `note`**,而记忆面板的直接编辑是 §4.4.2 里
   `source_confidence=1.0` 的最高可信路径。**消除动机比堵路径有效。**
   若仍保留任何自由文本字段,**服务端强制过正则族并拒绝保存**。
2. **凭证元数据一律 `S2`**:登记表 · 携带 `secret_ref` 的实体 · 取用审计。
   排除出 `v_memory_nons2`,Qdrant 侧只入 `mem_s2`。
   理由:值被移走后,这些元数据**反而失去了原本跟着值一起获得的 S2 保护** ——
   **这是 D23 自己引入的新泄露面,不是旧的。**
3. 记忆面板上 `secret_ref` 条目渲染成显式徽章:「**值不在这里** · 取值方式:你自己填(当前)」。

#### 6.9.4 ★★ 凭证正则族是四点入口过滤器,不是 Gate 的一条规则

> **原文的失效点**:把正则族挂在 Memory Gate 上,而 Gate 是**写入时**才生效的 ——
> 那时 prompt 早就形成了,L1 摘要也照常生成。

**正则族**(`config/cred-patterns.toml`,受 §6.7.6 CFG-1 保护):

| 类别 | 模式 |
|---|---|
| `iban` | `[A-Z]{2}\d{2}[A-Z0-9]{11,30}` **+ mod-97 校验**(校验位滤掉绝大多数误报) |
| `tax_id_de` | Steuer-ID(11 位 + 校验)· Steuernummer(各州格式)—— **两个都叫「税号」,都要** |
| `card_pan` | 13–19 位 **+ Luhn** |
| `id_doc` | 德国身份证 / 护照号格式 |
| `secret_phrase` | 「密码是」`password is`「recovery key」「seed phrase」「助记词」+ 其后 N 字符 |
| `high_entropy` | 长度 ≥32 且 Shannon 熵 ≥3.5 的连续 token。**误报率高 → 只用于 E1/E4,不用于 E3 拒绝** |

**归一化前置(必须,否则语音通道命中率接近零)**:去空格 · 去 `-` `.` `/` · 中文数字→阿拉伯 · 全角→半角。**ASR 输出必过此步。**

**★ 五个强制点(缺一不可,与 §4.6.1 三层强制同构):**

| # | 位置 | 命中行为 |
|---|---|---|
| **E1** | **入口**:用户输入 → 组装 prompt **之前**。**在网关侧,不信任前端**(Open WebUI 是第三方前端) | **拦下本轮**:不发送、不落 L0、不记正文。提示「这一轮没有发送,也没有记录」+ 两个按钮:「删掉这段」/「这不是凭证,继续」。**「继续」记审计(只记类别,不记值)** |
| **E2** | **L1 压缩器**:输入侧 + 摘要落库前(两侧都挂) | 该轮**整体不进摘要**(不是脱敏后进)。理由:摘要器倾向保留「具体数字」这类它认为关键的信息 |
| **E3** | **Memory Gate**:候选 `body` **+ `provenance` 快照** | 拒绝写入,**不落盘**(见 6.9.8) |
| **E4** | **出境载荷**:已组装的 prompt 字符串,与 §4.6.1 渲染层**同一个点** | **fail-closed**。命中片段替换为 `<secret?>`,逐处裁决才能发送。**与来源无关** —— L1「你手打的问题文本」不构成豁免 |
| **E5** | `worklog/` 写入前 + 运行日志 filter + **pre-commit 钩子** | 拒绝并告警。理由:`00-docs/worklog/` **是 git 跟踪文件**,`.gitignore` 无排除项 —— 一次填表失败回显 + 一次 commit = 凭证进版本历史,**删不干净** |

> **★ E1 与 §4.6.2 的关系**:§4.6.2 说「不得声称记忆零外发是被证明的,无法阻止手动复制粘贴」。
> E1 不推翻这句 —— 它把「意外」变成「必须显式点确认的行为」。
> **能拦住意外,拦不住坚持。** 这个区别必须写清,不要让 E1 被理解成保证。

**★ S2 定级与拒绝是一族两用,不是二选一**(推翻原文「正则族**改作**此用」):

- 命中 → (a) E3 拒绝并提示登记;**且** (b) 若经任何路径已写入,强制 `sensitivity_domain='S2'`
- `sensitivity_domain` 列 **`NOT NULL` 且不设 DEFAULT** —— 忘记定级 = INSERT 失败 = **响亮**
  (§4.11.4「漏了 collection 句柄是响亮的」的应有延伸),而不是静默落进 `v_memory_nons2` 被手机读到
- 调用方声明的 `sensitivity_domain` 在正则命中时**一律被服务端覆写**(同 §4.4.2 对 `user_explicit` 的处理)

#### 6.9.5 带外通道:七条硬约束

| # | 约束 |
|---|---|
| 1 | **不在对话框里输入**,用专用凭证输入控件(P6 起) |
| 2 | **不经过 LLM 上下文** —— 值从 UI 直送执行器。**Schema 层强制,见 6.9.7** |
| 3 | 助手只知道「你提供了 `<ref>`」,不知道值 |
| 4 | **不进任何日志**:`log_result: none` **且** `log_args: schema_only`(§9.2 新增维度) |
| 5 | **不进记忆**,用完即弃,不留 L0–L4 任何痕迹 |
| 6 | **不进出境载荷**:§5.6.2 的 **L5-cred**,含**一切派生物** |
| 7 | **★ 禁止经由剪贴板 · SendInput 键盘模拟 · 环境变量 · 命令行参数传递。** 只允许 UI 控件 → 执行器的**命名管道直传**(与 §6.4 一致) |

**★ 剪贴板的具体处置:**

- 控件**禁用粘贴**;若必须支持,粘贴后立即 `EmptyClipboard`,并对自身写入的内容附加
  `ExcludeClipboardContentFromMonitorProcessing` / `CanIncludeInClipboardHistory=0`
- **P2 验收项**:检查 Windows 剪贴板历史与跨设备同步状态。**不合规时凭证控件 fail-closed**
  > **2026-07-26 实测已关闭**(`IsHistoryEnabled()=False` / `IsRoamingEnabled()=False`)。
  > ⚠ 但策略层未配置 —— 这是用户设置,可被改回。**P2 时必须复检,不能沿用本次结论。**
- **§4.11.5 明文例外条款新增「剪贴板历史及其云同步」** —— 它按 D2 判据构成出境,
  而现在处于「未提及」状态。**未提及比明文接受更危险。**

**★ 运行态归属(修补 §6.8):新增第三个账户**

| 账户 | 跑什么 |
|---|---|
| `ai-mem` | PostgreSQL · Qdrant · embedding/rerank · memory-service |
| `ai-asset` | ComfyUI · 游戏自动化 |
| **`ai-exec`** ★新增 | **浏览器执行器 · 文件执行器 · 凭证填充** |

> **理由**:D23 之后凭证值「从 UI 直送执行器」——也就是直送浏览器执行器。
> 把它和 ComfyUI 第三方自定义节点放在同一个账户下,等于把最高价值的明文数据
> 交给一个**文档已明确认定为不可信、且正在跑第三方代码**的账户。
> 同账户进程可连 CDP 端口 `Runtime.evaluate` 读出已填入的 `input.value`。
> **§10 否定用例**:以 `ai-asset` 身份连接凭证执行器的管道/CDP 必须被拒并告警。

#### 6.9.6 授权粒度与预览

**每次使用都要授权,不做会话级。** 理由同 §12.4 铁律 3。

**★ 裁决「精确动作预览」与「模型看不到值」的冲突**(§12.4 vs 6.9.5 约束 2):

> **预览 = 掩码值 + 完整目标。**
> `DE89 •••• •••• •••• 0130 00` —— 掩码由 UI 层就地生成,**不进任何可序列化载荷**;
> **完整显示** `sink`(工具身份 · origin · 字段选择器),且 origin 由**执行器上报,模型不可写**。

**★ 授权控件版面强制**:系统生成、模型不可写的区域固定显示
`provider` · `ref` · `sink.origin` · 发起会话 · 发起时刻;
模型的 `intent` 只能出现在一个受限长度、标注「**以下文本来自助手,不可信**」的框里。

> 对照 §4.7.3:打扰提名这种低危通道已经上了模板锁,索要 IBAN 这种最高危通道反而留自由文本,是刺眼的不一致。

**★ 审批级别提升**:凭证类动作从「逐次确认」提升到 **§12.4 哈希绑定批准** ——
执行前复核 `(ref, sink)` 哈希。**哈希对 `secret_ref` 与 `sink` 取,不对值取;值不进计划文本。**

#### 6.9.7 ★★ 接口预留:完整契约(原文最大的洞在这里)

> **原文只定义了入参,没有定义返回值。** 按工具调用的默认语义,工具结果**无条件回到模型上下文** ——
> 也就是说,一个完全按 §6.9 实现、你完全按规矩操作(走控件、不碰对话框)的系统,
> **会在第一次真实使用时就把 IBAN 送进 prompt,而没有违反任何一条写下来的规则。**

```jsonc
// 请求 —— 模型可填的字段全部受约束
{
  "refs":        ["bank.de.sparkasse-payroll.iban"],  // ★ 数组:原子多值,同一次授权同一次兑换
                                                       //    (API key + secret 必须同时取、版本一致)
  "value_kind":  "string",                             // string | file | certificate | otp
                                                       // 后三者现为 not_provisioned,但槽位必须留
                                                       // (ELSTER 登录需 .pfx + 密码)
  "intent":      "fill_form",                          // ★ 受控枚举,取代自由文本 purpose
  "plan_hash":   "<§12.4 已批准计划的哈希>",
  "sink": {                                            // ★ 必填。取值与用值合成一次不可分割的批准
    "executor_id":    "browser@workstation",
    "target_kind":    "form_field",
    "target_locator": "<selector>",
    "origin":         "<执行器上报的实际已加载 origin,模型不可写>"
  },
  "request_id":  "<客户端生成,服务端去重>",            // ★ 幂等
  "timeout_ms":  30000,                                // ★ 必填
  "on_provider_unavailable": "fail"                    // ★ 唯一合法取值
}

// 返回 —— ★★ Schema 层禁止出现值
{
  "status":          "granted | denied | timeout | provider_error | cancelled",
  "fill_token":      "<一次性句柄,执行器侧兑换>",       // 兑换只发生在策略层/执行层进程内
  "max_redemptions": 1,                                 // ★ 重试走 TTL 内的第二次兑换,
  "ttl_ms":          15000,                             //    而不是缓存值(那是会话级授权改名)
  "sink_ack":        "<执行器回执的 origin 与字段>",
  "expires_at":      "<ISO8601>"
}
```

**★ 五条不可协商的 Schema 约束:**

1. **返回值不含值。** §10 否定用例:断言 `secret.request` 的任何返回路径都不含高熵字符串
2. **`provider` 不是请求字段**(6.9.2)
3. **`purpose` 自由文本取消**,改 `intent` 枚举 + `plan_hash`。理由:模型上下文里一旦有过凭证片段,
   它会自然写出 `purpose: "为 DE89... 的转账填表"`,而**审计表的保留期是不删**
4. **`fill_token` 在兑换点给目标参数打 `secret_bearing: true`**,任何可能接收它的参数强制
   `log_args: redact_marked`(记 `ref` + 位置,不记值)
5. **`on_provider_unavailable` 只允许 `fail`。** 回落手动只能由**你在失败提示上重新发起** ——
   否则这就是一个**可被注入按需召唤、文案由模型控制的索要凭证窗口**

**其余预留项:**

| 项 | 本期要做 |
|---|---|
| **命名规范** | 固定四段 `<domain>.<region>.<entity>.<field>`,`entity` 只允许 `[a-z0-9_-]`(**禁点号**),全小写。`entity` 用稳定标识,**不用 `main` 这类相对别名**(开第二个账户即歧义)。**★ ref 本身不得含机构名与账号尾号** —— 它们进登记表独立字段,标 S2 |
| **引用登记表** | `ref` · `value_kind` · `issuer` · `last4` · `purpose_tag` · `version` · **`provider_kind`** · **`provider_locator`**(不透明 JSON,预留给 1Password 的 `vault_id+item_uuid+field_label` / Bitwarden 的 `cipher_id`)· 创建时间 · **绝不含值**。登记表随 PostgreSQL 进 STATE 备份;`provider_locator` 属 S2 |
| **审计** | 记 `(ts, refs, intent, plan_hash, sink.origin, outcome)`。**「不记是否成功取到」删除** —— 它与 §12.3「失败可见」直接冲突,而其理由(怕反推值)不成立:成败只泄露「该 ref 是否存在」,而存在性本来就明文写在登记表里 |
| **告警**(§9.3 新增) | 单位时间内 `denied` 越线 · 同一 ref 短窗口重复请求越线。**熔断**:`denied` 连续越线则该会话工具池移除 `secret.request`,需显式恢复(同 §4.4.2 `pending_review` 积压熔断) |
| **权限档位** | 仅 `trusted-local`。远程档位工具池里**不存在**该工具 |

**不做**:任何具体外部应用的适配器。

> **★ 待决(进 §16)**:`external` provider 自己的凭据放哪?不能进记忆库、不能进明文 PG、
> Credential Manager 要到 P7。**因此 `external` 接入的前置是 P7。**
> 不写死这条的话,P3 期间某个「先跑通」的实现会把它写进一个 `.env` ——
> 而 `**/.env` 恰在 §6.7.4 硬禁止清单里,意味着**连事后审计都看不见它**。

#### 6.9.8 ★ 残留与擦除

| 位置 | 裁决 |
|---|---|
| **Gate 拒绝的候选** | **拒绝 = 不落盘。** 只持久化 `(类别, 时间, 会话id)`。**不记 body、不记片段、不记哈希**(定长 IBAN 的哈希可爆破:德国 IBAN 实际搜索空间约 2×10¹³,BLZ 是公开表)。**不进 `pending_review`**(否则拒绝越多滞留越多,且攻击者查拒绝标记就能定位所有凭证)。提示语不回显原文 |
| **溯源快照** | ★ **§4.5 可解释性的唯一例外**:凭证登记流程产生的记忆,`source_ref` **不引用原始发言快照**,只记「由凭证登记流程创建 + 时间 + 会话 id」 |
| **异常文本** | 凭证值在执行器内用**不实现 `__str__`/`__repr__`** 的密封类型承载(复用 §4.6.1 `TaintedText` 模式)。校验失败只抛不含值的错误码。凭证子进程 stderr 不入盘。**这是最可能发生的一次异常**:22 位 IBAN 手输错一位,而多数校验库的异常消息把输入原样回显 |
| **执行回执** | 封闭枚举 + 错误码。**站点回显的错误文本经执行器剔除凭证串后才交给上下文** |
| **下游产物** | 凭证类动作输出目录**固定为 `{state}/cred-out/`** —— `{state}/**` 已在 §6.7.3 Vigil 永久黑名单里,**零新增机制**。该目录加入 §8.5.3 备份排除清单(与 `quarantine` 同级)+ 短 TTL |
| **备份代** | ★ **无擦除路径,必须明写。** D21+D22 全链路不加密 → **没有 crypto-shredding**。一旦某代备份含凭证,只能物理覆写或等它轮出(12 代 × 手动间隔 ≈ **半年**)。**新增「凭证误入清除」流程**:把 `(备份代号, 表, 行)` 写进必须手动执行的 purge 清单,§9.3 告警「存在未完成的凭证 purge」,托盘保持不可忽略提醒直到受影响代全部轮出 |

#### 6.9.9 对其他章节的影响

| 章节 | 改动 |
|---|---|
| §4.5 | 新增可解释性的唯一例外(6.9.8) |
| §4.11.4 | S2 正则族**一族两用**;`sensitivity_domain NOT NULL` 不设 DEFAULT |
| §4.11.5 | 明文例外新增「**剪贴板历史及其云同步**」 |
| §5.6.2 | ★ 新增 **L5-cred**:「经 secret provider 取出的值**及其一切派生物**(表单截图 · 动作预览 · 执行回执 · OCR 文本)**永久禁止出境,不可临时升级,不可逐次确认**」,与 L3b 同级 |
| §6.7 | 外部加密应用数据目录进硬禁止清单;**补充**:该清单是**路径**边界,`external` 走 HTTP/IPC 完全不经 `fs.*` —— 它只覆盖 provider **不被使用**时的路径 |
| §6.8 | 新增 `ai-exec` 账户 |
| §9.2 | ★ 工具注册表新增 **`log_args: none \| schema_only \| redact_marked \| full`**。凭证工具强制 `schema_only` |
| §10 | 新增 8 条否定用例(见 6.9.10) |
| §14 | ★ **P2** 补:E1 入口检测器 · `secret_ref` 登记表 · 剪贴板状态检查。**P6** 补:带外控件 · `secret.request` provider=manual · `ai-exec` 隔离。**P7** 补:`external` 凭据存 Credential Manager |
| §15 | 「凭证在对话框里被打出」的缓解改为「E1 入口检测器(P2)」;「记忆经物理途径泄露」的等级下调**限定为凭证值** —— 索引与时间线不算无害 |

#### 6.9.10 §10 新增否定用例

① 含 IBAN 的候选 → PG 全库(含 audit / pending_review / logs / worklog)grep 不到该串
② 长会话触发 L1 压缩 → L1 表与 `pg_dump` 产物均不含该串
③ 出境哨兵串增加「手打凭证」组,覆盖 L1/L2 两级,**在所有开关翻转的对抗配置下重跑**
④ 溯源展开结果不含原值
⑤ 非法 IBAN → logs 与 PG 日志不含输入串
⑥ `vigil_observations` 无命中
⑦ 远程角色查询 `secret_ref` 必须异常
⑧ `external` 健康时授权控件仍渲染 `manual` 入口

---

## 7. 投资:接口预留(本期不实装)

**触发条件:新硬盘到位后单独启动**(Backlog B4)。

| 预留项 | 本期要做 |
|---|---|
| 能力别名 | 登记 `trade.research`,标记 `not_provisioned` |
| 工具契约 | 定义 `submit_trade_proposal` 的完整 JSON Schema |
| 数据库表结构 | 建好空表:提案 · 批准票据 · **幂等账本** · 对账 · 法规快照 |
| **账本表须定到字段级** | 表名不构成 Schema。账本是唯一有法定保存义务、最难回填的表 |
| 分池字段 | 德国亏损抵扣按类别隔离 |
| 架构位置 | 保留「纯 CPU 独立进程」这个位置 |
| GC 白名单 | 交易账本表现在就加入 |

**不做**:行情管道 · 回测框架 · 风控引擎 · IBKR 适配器 · 任何下单。

---

## 8. 显存、上下文与存储

### 8.1 ★ 组件选择器(D25 · 整节替换五档开关;五档曾整节替换 v2.1 的三模式)

#### 8.1.1 口径(先定死,否则每个数字都会被误读)

| 术语 | 定义 |
|---|---|
| **`desktop_floor`** | ★**你设的「至少给桌面留多少」**。服务端设置项,跨会话持久,**不是逐请求参数** |
| **`vram_budget`** | **导出值**,不单独设置:`15.92 − desktop_floor − SAFETY_MARGIN(0.8)` |
| **准入白名单** | `component_id ∈ committed_resident_set ∪ permitted_on_demand_set` |
| **静态闸** | `Σpeak(申请后的 AI 驻留集合) ≤ vram_budget` |
| **动态闸** | `NVML 实测 free − 本次申请(peak) ≥ SAFETY_MARGIN(0.8 GiB)` |
| **余量核算** | ★**必须用组件的实际 peak 之和,不能用 `vram_budget`**。预算是上限不是占用 |

> **★ 为什么设「桌面预留」而不是「AI 上限」——两者数值等价,心智模型不等价。**
> D24 花了大篇幅纠正一个误解:**降 AI cap 腾不出一个字节**(cap 本来就不含桌面)。
> 这个错误在「桌面预留」这个单位下**不可能犯** —— 「提高桌面预留能给桌面留出显存」是对的。
> 且它与 A7 梯级表共用同一条式子(`上限 = 15.92 − P99 − 0.8`),推荐组合不必另发明。
>
> **UI 必须同时显示两个数**(它们是同一个滑块的两端):
> 「给桌面至少留 **3.5 GiB** ／ AI 最多可用 **11.62 GiB**」。

**`desktop_floor` 的三条规定(用户 2026-07-26 拍板)**

| | |
|---|---|
| **默认值** | ★ **6.6 GiB**(P1-A7 实测,2026-07-26)· 标记 **`calibrated: partial`** |
| ~~原默认 1.06~~ | 已作废 —— 那是**轻载**实测值,而 A7 证明日常工作负载是 **6.53 GiB**,差 6 倍 |
| **改它的审批级别** | **§12.4 哈希绑定批准** —— 与 §6.7.7「新增 rw 权限区域」同级 |
| **理由** | 它是**唯一能突破 Σpeak 上限的旋钮**,整个「防止上次勾多了」的保护强度由它决定 |

> **★ 必须写明的张力(不掩饰)**:默认 1.06 意味着第一次打开界面能勾到 **14.06** ——
> 包含一批桌面一忙就装不上的组合。**这道闸在默认状态下几乎不设防,
> 而那恰恰是它最该设防的时候**;同时哈希绑定批准又让调整它变贵。
>
> **处置**:默认值**不是**一个标定过的数,它是占位。
> `desktop_floor` 带一个 `calibrated: false` 标志,**预览第三段固定显示「未标定(A7 未测)」**;
> **A7 出结果后提示设一次真实值**,那一次改动同样走哈希绑定批准。
> 不加摩擦,但不假装它经过标定。
>
> **这条本身就是 D25 推翻条件 ① 的观察对象** —— 「勾完才发现桌面被挤」月度 > 2 次
> 就说明这个默认值选错了。
>
> **⚠ `cap` 这个词全文废止。** 不是美学问题:D24 已经把「cap」与「降它能腾显存」的误解
> 永久绑定在文档里,留着这个词等于给下一个读者留一个已知陷阱。

**★ 三层检查防的不是同一件事,不能合并**(这是 D25 从原「一个 cap 管一切」拆出来的):

| 检查 | 防什么 | 何时求值 |
|---|---|---|
| **准入白名单**(集合成员) | bug · 重复 spawn · 泄漏租约 · 未授权组件 | 每次租约申请 |
| **静态闸**(算术) | 「上次不小心勾多了」· 跨会话持久 · 给桌面留地方 | **提交时** 且 每次租约申请 |
| **动态闸**(实时) | 此刻装不下 · 桌面临时涨了 | 每次租约申请 |

> **为什么 bug 那一类必须靠白名单而不是靠闸**:闸是**算术**,可以被凑数凑过去;
> 白名单是**集合**,漏了会响亮报错。这是 §4.11.4「漏了 payload filter 是沉默的,
> 漏了 collection 句柄是响亮的」在显存侧的同一条道理。

**所有预算表一律用 `peak`。Broker 按 peak 发租约。**

#### 8.1.2 组件显存清单(v2.2 唯一权威表 · NUM-1)

KV 按 `2 × 36层 × 8 KV头 × 128 dim × bytes × ctx` 计(F16=2B / q8_0=1B)。
**★ 2026-07-27:LLM peak 列已换 P1 实测值(默认 q8_0 KV,B3b 确认零质量损失)。comfyui.sdxl 实测 8.14。**
**★ Qwen3-8B 与 Qwen3-4B 的层数/KV头数/head_dim 完全相同 → KV 完全相同。KV 与参数量无关。**

| component id | 权重 | KV | CUDA ctx | **peak** |
|---|---|---|---|---|
| `llm.assistant.8b@8k` | 5.0 | q8:0.61 | 0.4 | **5.31** ★实测(F16 5.83)|
| `llm.assistant.8b@16k` | 5.0 | q8:1.21 | 0.4 | **5.92** ★实测(F16 6.97)|
| `llm.assistant.8b@32k` | 5.0 | q8:2.48 | 0.4 | **7.19** ★实测(F16 9.23)|
| `llm.assistant.4b@8k` | 2.5 | q8:0.61 | 0.4 | **3.27** ★实测(F16 3.78)|
| `llm.assistant.4b@16k` | 2.5 | q8:1.21 | 0.4 | **3.86** ★实测(F16 4.92)|
| `speech.full`(large-v3 ASR + Piper CPU TTS) | 实测 | — | — | **4.05** ★实测(large-v3 GPU 4.05 + Piper CPU 0;TTS 首字节 83ms)|
| `speech.lite`(GPU ASR + CPU TTS) | 实测 | — | — | **2.07** ★实测(turbo ASR 2.07 GPU + Piper CPU 0;原估 1.4 低了)|
| `speech.cpu`(CPU ASR + Piper CPU TTS) | 0 | — | 0 | **0.0** ★实测:ASR RTF 0.385 · TTS 83ms |
| `vlm.small` | 2.5 | — | 0.4 | ~~**3.2**~~ → **实测 4.35**(候选 Qwen2.5-VL-3B,含 mmproj f16 1.25;原估漏算 mmproj)·P1-A2 |
| **`llm.assistant.30b-a3b@32k`** ★ | **实测** | q8_0 | 含 | **11.9**(ngl 30 · 38% offload · **57.5 tok/s**)|
| `comfyui.sdxl` | 含 | — | 含 | ~~11.0~~ → **8.14** ★实测(1024²·P1-D1)|
| `comfyui.sdxl.lowvram` | 含 | — | 含 | **7.8** |
| `embedding.default` / `rerank.default` | CPU | — | — | **0.0** |
| **`resident.assistant`(Vigil)** | **CPU** | — | — | **0.0** |

> **⚠ 本表的 `peak` 列无法由本行三分项相加得出** —— 每行都多 **+0.10 ~ +0.30**,
> 大部分约合 4%(像是碎片余量),但 `vlm.small`(+10.3%)与 `8b@32k` 连这个都不符。
> **这是 NUM-1 的一处内部不自洽**,必须知道:
>
> | | |
> |---|---|
> | 影响 A2 | A2 的判据是「实测 vs 估算偏差 ≤10%」,而 `speech.lite` 的 ±10% 只有 **±0.14**,**未说明的余量就有 +0.10** |
> | 影响预览 | 勾选界面把这一列加起来 —— 加的是一列**无法由分项推导**的数 |
> | **处置** | **不现在改数**(那是在一个我推断出来的公式上瞎改)。**A2 用实测值整列替换**,届时同批补上「碎片余量」这一列或写死推导公式 |
> | **在此之前** | 界面诚实规则 1 已规定未测行显示「**估算(P1-A2 未测)**」—— 这条覆盖了本问题 |

**★★ 硬规范(P1-A2 实测后新增):KV 类型必须显式指定**

> 本表的 KV 列按公式 `2 × 36层 × 8 KV头 × 128 dim × **1 B** × ctx` 计 —— 那个 `1 B` 是 **FP8 假设**。
> **llama.cpp 默认跑 F16,即 2 B,每个上下文吃 2 倍。**
>
> **实测(2026-07-26)**:`8b@16k` 6.97 GiB(F16)→ **5.92**(`q8_0`),省 **1.05**;
> `8b@32k` 9.23 → **7.19**,省 **2.04**。
>
> ⚠ **KV 量化不是免费的** —— 会损失精度,长上下文尤其。
> **B3 必须加测 q8_0 vs F16 的质量差;在那之前不得写成默认配置。**
> 本表的数字**按 FP8 口径**,使用时必须确认 worker 真的开了量化,否则实际占用是表里的 KV 列 × 2。

**硬规范**:所有 LLM Worker 启动时**预分配全量 KV**。否则「加载成功」不等价于「运行期不会 OOM」,长对话会在第 30 轮才炸。

> **★ 这张表修正了 v2.1 §8.1 的两处算术错误**:
> ① daily 漏算三个进程各自的 CUDA context —— 实际是 `6.8 + 4.6 + 3.2 = 14.6`,不是 13.0
> ② 4B 的 KV 被按参数量线性缩放 —— 实际与 8B 相同

#### 8.1.3 ★ 组件选择器(取代五档定义表)

**你直接勾选要装哪些组件,不选档位。** 勾选之和就是分配量 —— 中间没有一层需要你换算的抽象。

**为什么取消档位**(完整理由见 **D25**,这里只留一条最重要的):

> **档位表是 O(n) 维护,勾选是 O(1)。** 每加一个组件,五个档位的组成都要重新推导一遍,
> 而且每次都可能推错 —— v2.1 → v2.2 那两处算术错误(daily 漏算三个 CUDA context、
> 4B 的 KV 按参数量缩放)正是这么来的。加一个模型只该是加一行。

**第二条理由是穷举得出的**:模型 × 上下文 × 语音 × 视觉共 30 个组合,
**帕累托前沿 = 全部 30 个,零个被支配**。同价位存在能力剖面完全不同的多个非支配选择 ——
例如 peak **11.4** 有三个:`8b@16k`+`full` ／ `8b@16k`+`lite`+`vlm` ／ `4b@8k`+`full`+`vlm`。
**在一个零支配的前沿上钉三个点是武断的**,而且档位模型表达不了这种差别。

##### 互斥与依赖(UI 是单选组 + 复选项的混合,不是纯复选)

| 组 | 语义 | 成员 |
|---|---|---|
| **LLM** | **单选** | `4b@8k` `4b@16k` `8b@8k` `8b@16k` `8b@32k` |
| **语音** | **单选** | `speech.cpu` `speech.lite` `speech.full` |
| 视觉 | 可选 | `vlm.small` |
| **出图** | **按需槽,不是常驻** | `comfyui.sdxl` `comfyui.sdxl.lowvram` |

> `resident.assistant`(Vigil)**不出现在本界面** —— 它跑 CPU、恒为 0.0 显存、不持 GPU 租约。
> 它的开关在 §17,与显存无关。把 0.0 的东西放进显存界面只会让人以为它有代价。

##### 三段预览(缺一段就是在骗人)

```
已选组件            8b@16k 6.8 + speech.full 4.6              = 11.4   ← 估算,见下
你的桌面预留        desktop_floor 3.5  →  vram_budget 11.62
此刻实际可用        NVML free 14.86 − 0.8                     = 14.06
─────────────────────────────────────────────────────────────
可以确定 ✓          还能让桌面再涨 2.66 GiB 才会出问题
```

**★ 最后那一行是本界面存在的理由。** 只显示「装得下」是不够的 ——
桌面占用是**波动**的,用户需要知道自己离墙有多远,而不是知道此刻没撞墙。

##### 「确定」按钮的启用条件

```
Σpeak(已选)  ≤  min( vram_budget , NVML free − 0.8 )
```

**★ 撞到哪一边,提示必须不同 —— 合并成一句「显存不足」是有害的:**

| 撞到 | 提示 | 用户能怎么办 |
|---|---|---|
| **`vram_budget`** | 「桌面预留 3.5 GiB,AI 最多 11.62,当前选择 12.5,超 0.88」 | 取消组件,**或改桌面预留** |
| **实时可用** | 「此刻只有 10.4 可用(桌面正占 4.72)」 | **改预留没用** —— 关掉占显存的程序,或选小的 |

合并显示的后果:用户会去调预留,调完发现没用,因为撞的是物理墙。

##### 确定 = 一次事务

1. **点确定时重新求值,不用预览时的快照。** 「预览过、确定时不过」是必然会发生的 ——
   挑组件要花几十秒,期间桌面会变。
2. 预检不过 → **回到编辑态,不卸载任何东西**。
3. 装载中途失败 → 回滚到上一个成功集合;回滚也失败 → `DEGRADED_SAFE`。
4. **有任务在跑 → 弹提示**(见 §8.1.6,该规则的主语已从「降档」改为「变更驻留集合」)。

##### 侧栏推荐组合

一键套用 = 一次普通的集合变更,**走完全相同的确认与事务路径**,不走捷径。

> **★ 已按 P1-A7 实测重排(2026-07-26)。** 原表把 `speech.full` 放进前两位 ——
> **实测证明在用户的日常工作负载下(UE5 Epic 实时,桌面 P99 **6.53 GiB**)那两个都装不上。**
> `speech.full` 要 4.6 GiB,是买不起的那一项;而 32K 上下文买得起。
> 排序改为**按「什么时候能用」**,不按「谁听起来最好」。

> **★ 2026-07-27 已按 P1 实测重算,并改用 q8_0 KV(B3b 已确认零质量损失→转正)。**
> **LLM/出图/VLM 均为实测,语音仍为估算**(A5/C4 待选型)。实测把很多组合从「装不下」变「装得下」——
> 尤其 **32K 上下文与出图现在都进日常了**(原估 12.5/11.0 过订,实测 q8_0 后是 7.19/8.14)。

| 推荐 | 组成 | peak | 可容桌面 | 在 `desktop_floor`=6.6 下 |
|---|---|---|---|---|
| **★ 日常** | `8b@16k`+`speech.lite` | **7.99** | 7.13 | ✅ **默认** · 余量 0.53(speech.lite 实测 2.07)|
| **★ 长上下文** | `8b@32k`+`speech.cpu` | **7.19** | 7.93 | ✅ 余量 1.33 —— **32K 现在日常可用!** |
| 省显存 | `8b@8k`+`speech.lite` | 7.38 | 7.74 | ✅ |
| 让位游戏 | `4b@16k`+`speech.cpu` | 3.86 | 11.26 | ✅ 任何时候 |
| **出图** | `comfyui.sdxl`+`speech.cpu` | **8.14** | 6.98 | ✅ 余量 0.38 —— **实测,原估 11.0 过订;出图现在也进日常** |
| **★ 深度(30B)** | `30b-a3b@32k`(ngl 30) | **11.88** | 3.24 | 🔷 deep 专用 · 桌面轻时 **57.5 tok/s**(A3/C3 实测) |
| 长上下文+GPU语音 | `8b@32k`+`speech.lite` | 9.26 | 5.86 | ⚠ 需较轻桌面 |
| 加视觉 | `8b@16k`+`speech.cpu`+`vlm.small` | 10.27 | 4.85 | ⚠ 需轻桌面(vlm 实测 **4.35** 不是 2.5) |
| 全语音(高精 ASR) | `8b@16k`+`speech.full` | 9.97 | 5.15 | ⚠ 需轻桌面(large-v3 精度,实测 4.05)|

> **★ 2026-07-27:全部转实测**(语音也定了 —— Piper 全档 TTS,D27 修订)。
> peak 口径:LLM q8_0 KV 实测 · `comfyui.sdxl`=8.14 · `vlm.small`=4.35 ·
> **`speech.lite`=2.07(turbo GPU ASR + Piper CPU)· `speech.full`=4.05(large-v3 + Piper)· `speech.cpu`=0**。
> **推荐组合现在 100% 实测,无估算项。** 仅 C4 的 WER 准确率待你的语料。

> **★ 推荐组合与 A7 梯级表对齐,不是两套。** A7 的「不达标时」已冻结,给出六级
> (11.4 / 10.8 / 9.3 / 8.2 / 6.2 / 3.6),自变量正是桌面占用 —— 与本表同一条式子。
> **不能各写各的**,那会出现两套互相矛盾的推荐来源。
> 拒绝归因单里提示的降级建议,一律从 A7 梯级表取。

> **★ 「一键套用」会自动卸载不需要的组件 —— 因此它必须先显示 diff,再确认。**
> 「点一下就变好了」的观感掩盖了「杀掉了正在跑的东西」的事实。
> 弹窗必须写:**将装载 X · 将卸载 Y · Y 上有 N 个任务在跑**。
>
> **✅ 用户 2026-07-26 决定:保留,但卡片上标死条件。**
> 固定文案:「**轻负载桌面专用 · 开 UE5 或多标签 Chrome 时会被拒绝**」。
>
> **⚠ 「长上下文」这一档的可容桌面是 2.62,低于 A7 的阈值 3.5。**
> 也就是说它在重负载桌面下装不上 —— **这不是缺陷,是必须在 UI 上说清的事实**:
> 推荐列表里它要标「轻负载桌面专用」。
> 这条也正好演示了自由勾选的风险:**它能走到没人审过的角落**,所以 `desktop_floor` 这道闸是必需的。

##### ★ 界面诚实规则(18 条 · 缺一条就有一处在骗人)

> §12.3「失败可见,不静默降级」在界面上的具体兑现。
> **预览若给出虚假的安全感,就是最坏的静默。**

**数据来源**

| # | 规则 |
|---|---|
| 1 | 组件卡片固定四字段,**全部服务端下发,前端不得改写**:`component_id`(用 §8.1.2 原文 id,**不做展示名替换**)· `peak` · `residency_class` · **数据来源标记**(P1-A2 之前一律显示「**估算(P1-A2 未测)**」,之后显示「实测 YYYY-MM-DD」) |
| 2 | **前端不存任何显存数字。** 请求体只含 `component_ids: string[]`(受控枚举)。措辞是「**不接受任何由客户端计算得出的量**」,不是「拒绝数字型字段」—— 后者会误伤 `request_id`/`timeout_ms`。**NUM-1 的适用范围从「文档章节」扩到「代码与前端资源」**,加构建期断言:除 `vram-presets.toml` 外任何源文件出现 peak 形状的浮点常量即失败 |
| 6 | **「桌面 X GiB」不得作为独立权威数字展示** —— WDDM 下逐进程 NVML 常返回 N/A,基线工具尚未确认。它只能在**拒绝归因单**里作为「**推算值**」出现并如此标注 |
| 9 | **不显示「预计耗时」。** 装载时延方案书里从无数字(P1 只测峰值)。**显示一个编出来的秒数本身就是不诚实;宁可不显示** |

**预览**

| # | 规则 |
|---|---|
| 3 | 预览分三段、视觉分层,**判定色(✅/❌)只给实测段**。① 实测段:NVML 总 free(带 `sampled_at`)+ AI 侧已用 + 本次新增 Σpeak;② **潜在段(永不带判定色)**:按需组件逐项列;③ 策略段:可容忍桌面与 `desktop_floor`。**三段不得混排** —— 三套口径并列反而会被判定为「预览在撒谎」 |
| 4 | **按需项的预览不得写成求和。** 按需与常驻是**替换关系而非叠加关系**(`assistant.vision` 要驱逐、`image.texture` 是 `requires_swap`),朴素求和给出的是一个**永远不会发生的峰值**。逐项列出,不合计 |
| 5 | **首要数字是「可容忍桌面」,不是 Σpeak。** 旁边固定对照:「实测轻载 0.69–1.06 / 重载估 3.5(A7 未测)」。理由同 §8.1.1:在「留给桌面多少」这个单位上,用户不会犯 D24 那个错 |
| 7 | 按需分区每项旁注固定文案:「**勾上 = 允许在需要时申请;不等于现在占用,也不保证申请时成功**」。且**按需项默认必须是「允许申请」** —— 默认不勾会静默禁掉 `image.texture`,那正是 §12.3 禁止的静默降级 |
| 18 | **预览与准入必须是同一段代码。** 预览返回服务端结构体(静态闸结论 + 动态闸结论 + `sampled_at` + 该次 free 值),提交时由**同一个 Broker 模块**重跑 |

**提交**

| # | 规则 |
|---|---|
| 8 | **确定前显示 diff,不是显示新状态。** 三行:将卸载(逐个 id)· 将装载 · **受影响的正在跑任务**(沿用 §8.1.6 的 `completed_units`/`next_unit`/`steps_done`) |
| 11 | 侧栏 preset 点击**只写 `staged_set`,绝不直接提交**,并与手动勾选走**完全相同的端点**。这同时消灭了「两条写路径」与「快捷路径绕过事务语义」。**助手不得生成携带勾选状态的深链** —— 预填好的面板会把逐项确认在心理上退化为点「确定」,那正是「**用户替注入点确定键**」 |
| 12 | **拒绝必须归因到具体哪一道闸并带数字。** 三选一,措辞形如:`静态闸`「这组 Σpeak 12.6 > vram_budget 11.62;你设的桌面预留 3.5,这组只够留 2.52」。**全系统只有一条拒绝呈现路径** |
| 13 | **读数漂移本身不得额外加一次确认。** 只有重跑判定为**拒绝**时才弹差异单。桌面空载本就有 0.69–1.06 的抖动,严格比较会让几乎每次「确定」都多一次点击 —— **那会训练用户闭眼点** |
| 17 | **「记住我的选择」明令禁止**(§8.1.6 已有此禁令与替代品 `resident_change.default_action`「冷静时定的,不是着急时点的」),不得因改版丢失 |

**状态与只读**

| # | 规则 |
|---|---|
| 14 | **只读设备必须显示「只读」的原因,不是把按钮灰掉。** 固定文案:「配置只能在工作站上更改(**结构性限制,非权限不足**)」+ 当前 `actual_resident_set` + 每项真实契约。**灰按钮是 UI 层的谎** —— 它暗示「有权限就能点」,而实际是端点不存在 |
| 15 | `contract_changed` 原样存活,触发源从「档位不匹配」扩到「**组件不在 `actual_resident_set` 内**」。这是 `RECONCILING` 态下继续服务的诚实兑现 |
| 16 | `intended` / `committed` / `actual` 三者不等时 **UI 常驻显示差异**,并写明当前状态与该状态下**还能用什么**。`STARTING` **不红托盘** |
| 10 | **preset 卡片与组件卡片内不允许出现模型生成文本。** 组件的「说明与用途」文案是**静态资产,随代码走,模型不可写**。内置组合放 `config/vram-presets.toml`(CFG-1 已覆盖),用户自建放 `{state}/policy/` |

##### ★ P2–P5 空窗期:整体 `not_provisioned`

桌面壳 **P5 才有**,而现行界面是 Open WebUI(第三方前端)。空窗期的处置:

> **整体 `not_provisioned` —— 只有本机脚本 + 托盘,GPU 变更不上网关。**
> **照 §6.9.0 凭证控件的先例**(那里的原话:「能力不存在时明确不提供,而不是降级到对话框」)。
>
> **不开临时 HTTP 端点**,哪怕加 CSRF 三件套 + loopback 限定。理由:
> 「**临时的东西会永远留着**」是本项目已经点名过的失效模式,
> 而这个临时端点恰好是「只有主机可操作」这条结构性保证的唯一缺口。
>
> **代价已接受**:P4 的 Broker 验收要靠脚本操作,不能点界面。

##### ★ 一个尚未验证的前提

预览 = 把 §8.1.2 的 peak **加起来**。而 **P1-A2 要验证的正是可加性** ——
两个 llama.cpp 进程可能共享 CUDA 模块缓存,也可能各持一份。
**若不可加,本界面的实现方式要推翻重来。** 因此本节在 **P1-A2 出结果前不定稿**。

#### 8.1.4 能力 → 组件依赖表(取代「档位 × 能力矩阵」)

原矩阵的**列就是五档**,档位取消后它无锚点。而它真正表达的是另一件事:
**每个能力别名需要哪些组件在位**。这张表不随档位变,只随组件表变 —— 同样是 O(1)。

四态遵守 §12.3:✅`ok` · ⚙`contract_changed`(**默认放行,响应回带真实契约**)· ⚠`requires_swap`(202 + 确认单)· ❌`unavailable`(503,带缺口)。

| 别名 | 需要什么在位 | 不在位时 |
|---|---|---|
| `assistant.voice` / `assistant.fast` | 任一 LLM | ❌ |
| `assistant.deep` | **`30b-a3b@32k`**(P1 实测可行,57.5 tok/s)· 或 `8b@32k` 兜底 | ⚠ 需换装(确认单)· 或 ⚙ 转云 |
| `assistant.vision` | `vlm.small` | ⚠ 需装载(+3.2);装不下则 ❌ 带缺口 |
| `speech.stt` / `speech.tts` | 任一 speech.* | ⚙ 降级契约回带 |
| `image.texture` ⌂ | `comfyui.sdxl`(11.0) | ⚠ 需驱逐 LLM 后装载 |
| `image.concept` ☁ | — | ✅(云端) |
| `embedding` / `rerank` | — | ✅(CPU) |
| **记忆双轨检索** | — | ✅(不占显存) |
| **Vigil** | — | ✅(CPU) |

**`assistant.fast` 不写死 16K** —— 契约由**当前装着哪个 LLM 组件**定义。
`contract_changed` **默认放行**,响应 `model` 字段回写真实契约(如 `assistant.fast@8k(Qwen3-8B-Q4)`),
外加 `X-LocalAI-Contract` 响应头。「不静默降级」由 **UI 常驻显示真实契约**兑现,不用阻塞式 409。

> **★ 原 §8.1.4 末段那条安全规则已搬到 §3.2 的工具注册表,并且加强了。**
> 原文是散文里的一句「档位切换工具不得进入任何 Agent 的工具池」,
> 量级明显低于 `trade.execute` 那种写进注册表的处理。搬迁详情与
> **删掉 `client_error_prompt` 的理由**见 §3.2。

#### 8.1.5 Off 与完全 Off(电源轴 —— 不是档位)

> **★ D25 后的定位(用户 2026-07-26 拍板:二态)**
>
> | | 位置 | 理由 |
> |---|---|---|
> | **ON / OFF** | **主界面二态开关** | 日常动作 |
> | **完全 Off** | **托盘菜单**,不在主界面 | 它更像「**紧急全停**」而不是日常档位 —— 其**唯一真实用途是安全事件的紧急切断**(Off 已经是 0 显存,「省资源」在数字上不成立) |
>
> Off / 完全 Off **都不属于组件选择器**,它们是**电源轴**上的位置。
> 组件选择器管「装什么」(意图轴),总开关管「通不通电」(电源轴)。
> **不变式 I4 保证两轴不互相污染**:`power == off` 时 `actual` 清空,
> 但 `intended_resident_set` **不变** —— 一次 OFF 不会吞掉你的勾选,ON 回来按原样重装,不用重勾。

| | ON(已选组合) | **Off** | **完全 Off** |
|---|---|---|---|
| AI 侧 GPU | Σpeak(已选) | **0** | **0** |
| PostgreSQL / Qdrant | ✅ | ✅ | **❌ 停止** |
| 网关 / MEMORY 平面 | ✅ | ✅ | **❌ 停止** |
| **记忆双轨检索** | ✅ | **✅** | ❌ |
| 记忆写入 | ✅ 即时 | ⚠ **入队延后**(Gate 无本地模型,**绝不送云**) | ❌ |
| `escalate.cloud` | ✅ L2 | ✅ L2 | ❌ |
| **云端基于你的记忆回答** | ❌ | **❌ 永远不做** | ❌ |
| Vigil | ✅ CPU | **✅ CPU 零降级** | ❌ |
| **记忆库文件本身** | 可读 | 可读 | **仍可读** ⚠ |

> **Off 是「AI 不再用显卡,但它还记得你」;完全 Off 是「所有服务停止」。**

##### ⚠ D22 让「完全 Off」的含义变窄了(必须写清)

原表这一行是「`memory.vhdx` 挂载 / 挂载 / **卸载**」,那时完全 Off 意味着**记忆库上锁**。
D22 取消加密卷之后,**没有可以卸载的东西了** —— 记忆库是普通文件夹,始终可读。

因此完全 Off 的实际作用缩小为:

| 还能防的 | 不再能防的 |
|---|---|
| 网络侧访问(网关停了) | **任何能登录这台机器的人直接读文件夹** |
| 服务被利用(进程都停了) | 硬盘被取走 |

**完全 Off 仍有意义**(「我怀疑网络侧出事了,先全停」),
但**它不再是「记忆库上锁」** —— 这个说法在 D22 之后是假的,不得保留。

#### 8.1.6 切换与中断

**硬规则:一律先卸后装。** 卸载后**轮询 NVML 直到 free 回升到预期 ±0.2 GiB**(超时 10 s 报 `vram_not_reclaimed`)。

**`requires_swap` 必须有事务语义**:
1. 进入前做**整组预检**,不过直接拒绝、**不卸任何东西**
2. 卸载后若任一新组件加载失败 → 尝试回滚
3. 回滚失败 → `DEGRADED_SAFE`(等价 Off + 托盘红 + 不可忽略通知)

**★ 变更驻留集合时有任务在跑 → 弹提示让你选**「优雅中断」或「等它跑完」。先给 **5 秒排空窗口**。

> **★ 主语已由「降档」改为「变更驻留集合」(D25)。** 原文的主语是档位,
> 档位取消后这条安全规则会**字面失效** —— 而它保护的是「一键套用推荐组合」这条
> 最容易静默杀掉运行中任务的路径。**先搬走并改主语,再删章节**,顺序不能反。

`blocking_set = {USER_BLOCKING, USER_ASYNC, RESIDENT_TASKED}`。
**Vigil 的空闲观察循环不在集合内** —— 它跑 CPU、不持 GPU 租约,**天然不在,不需要特例代码**。

**ComfyUI 中断语义**:**不保存 latent,重跑不续跑**(ComfyUI 不支持跨进程恢复采样器状态)。
**批量 JOB 按产物单元**:已完成的 N 张正常入 draft 池;正在采样的第 N+1 张标 `partial=true, steps_done=18/30`;记录 `completed_units` / `next_unit`。弹窗给可验证的具体数字:

```
已完成的 12 张会保留;正在出的第 13 张作废(已算 18/30 步,重跑约 9 秒);
剩余 8 张可在改选更大的组合后从第 13 张继续。
```

**MODEL 平面截断的输出不进 Memory Gate**(半句话是误记源)。

**不用「记住我的选择」复选框** —— §12.4 铁律 3:批准的是具体计划,不是一类操作。替代:设置项 `resident_change.default_action`(**冷静时定的,不是着急时点的**)。

#### 8.1.7 「不做自动触发」的精确边界

| 允许 | 禁止 |
|---|---|
| 自动**拒绝租约** | 自动改变**驻留集合** |
| 自动**通知**「疑似过订,建议改选更小的组合」 | 自动卸载/换装 |
| 自动驱逐 `WARM` / `MAINTENANCE` 类租约 | 中断 `blocking_set` 成员 |

> **★ D10 的「不做自动触发」完整存活,只是主语从「档位」扩到「驻留集合」。**
> 这是 D10 里唯一不随档位概念一起作废的部分 —— D25 点名保留它。

##### 四个集合(不变式的锚点;不先定义,不变式就无从求值)

| 集合 | 是什么 | 谁能写 |
|---|---|---|
| `intended_resident_set` | 你在主机面板勾选并点了确定的常驻集合 | **只有主机变更面** |
| `committed_resident_set` | Broker 事务成功后写入的实际目标 | Broker(提交成功时 `committed := intended`) |
| `actual_resident_set` | 租约账本 + NVML 反映的**真实**驻留 | 运行时 |
| `permitted_on_demand_set` | 授权「允许在需要时申请」的按需槽 | 只有主机变更面 |

> `permitted_on_demand` 与 `intended` **是两个字段,不合并** ——
> 合并会让 `intended` 里出现永远不参与 I2 判定的成员,三元组语义就脏了。

##### 不变式

**I1**(一字不改):自动机制只能拒绝新租约,**永不撤销已授租约**;只有用户显式动作才能撤销。

**I2 · 该在的都在**(作用域限 `residency_class = resident`):
```
state == READY   ⟹   actual_resident_set == committed_resident_set
```
不等时 `state` 必须是 `STARTING` / `RECONCILING` / `DEGRADED_SAFE` 之一,且 **UI 常驻显示差异**。

> **保留蕴含形态是关键**:`DEGRADED_SAFE` 不是 `READY`,前件为假,I2 自动成立 ——
> 不存在「永久违反不变式、告警无法消解」的状态,也就不需要让系统自动改写锚点
> (那恰好会违反 D10 存活下来的「不做自动触发」)。写成双条件就会造出那个两难。

**I3 · 在的都该在**(★ 新增。**无状态前件,任何状态下恒成立**):
```
∀ c ∈ actual_resident_set ∪ actual_transient_set :
      c ∈ committed_resident_set ∪ permitted_on_demand_set
```
违反即 §9.3 告警。**这条才是接住「某个 bug 装了不该装的」的那一条。**

> 原 I2 的「严格等于」把 I2 与 I3 压成一句 —— 这正是它一取消档位就**同时**崩掉两件事的原因。

**I4 · 电源轴与意图轴分离**(★ 新增):
```
power == off   ⟹   actual_resident_set == ∅   ∧   intended_resident_set 不变
```
**ON/OFF 总开关不写 `intended`。** 否则一次 OFF 就吞掉你的勾选,等价于系统改写了用户配置。
ON 回来时按 `intended` 重新装载,**不需要重勾**。

##### ★ 检测器(没有检测器的不变式等于没有)

`RECONCILE_WATCH`:周期比对 `actual` vs `committed`,**只报告不修复**(修复即违反「不做自动触发」)。
§9.3 告警最小集新增「**常驻组件掉线**」。

> **这是原 I2 最大的实际缺陷**:全文今天只有 §17.3 的 W0 讲 Vigil 托盘进程存活,
> **没有任何机制能发现「3 个装了 2 个却仍报 READY」** —— 而 I2 本来就是为禁止这件事写的。

##### 状态机(原文只有 READY / DEGRADED_SAFE 两个点)

```
STARTING ──▶ READY ──▶ STAGING ──▶ PRECHECK ──▶ APPLYING ──▶ READY
   │           │          ▲            │
   │           │          └────────────┘  预检不过:回编辑态,不卸任何东西
   │           ▼
   └────▶ RECONCILING ──▶ DEGRADED_SAFE(回滚失败终态,等价 Off)
```

- `STARTING`:开机装载中,`actual ⊊ committed`。**不红托盘**(那是正常过程),有超时 → `RECONCILING`
- `RECONCILING`:worker 掉线 / 装载失败。**必须继续按 `actual_resident_set` 提供服务**,
  复用 §8.1.4 既有的 `contract_changed`(默认放行 + 回带真实契约 + `X-LocalAI-Contract` 头)。
  > 否则一个语音 worker 掉线会连带把仍然可用的 LLM 也判成不可用,**比故障本身更糟**。

##### ★ 失败落点(原文完全没有定义 —— D24 排查带出)

五件事叠加:A7 的 3.5 是 **P99**(1% 采样点更高)· 真实缓冲可能只剩零点几 GiB ·
I1 不撤已授租约 · KV **启动即预分配全量** · sysmem fallback 已关闭。
结果是桌面尖峰时,**先撞上分配失败的很可能是 Chrome / UE5,而不是 AI 侧** —— 与设计意图相反。

**规定**:驱逐 `WARM` / `MAINTENANCE` 后仍不足时,**失败落在 AI 侧**(拒新申请 / `DEGRADED_SAFE`),
**不得由桌面承担分配失败**。

> **诚实边界**:这条是**策略**,不是保证。WDDM **不按优先级驱逐** ——
> 已经发生的驱逐落在谁头上,本项目控制不了。本文档不得声称档位/预算能保证桌面不被挤。

**「检测独占全屏游戏」这条特例删除**,替换为通用规则:`free_vram` 1 Hz 采样,5 s 内降幅 > 1.0 GiB → `admission_guard`;全屏进程名**仅用于生成告知文案,不产生策略动作**。理由:这台机器上比游戏更常见的显存大户是 UE5 与多标签 Chrome。

### 8.2 上下文预算

| 上下文 | KV(FP8,8B/4B 同) | 备注 |
|---|---|---|
| 8K | 0.56 GiB | 语音模式上限 |
| 16K | 1.13 GiB | 高档默认 |
| 32K | 2.25 GiB | 深度模式上限 |
| 128K | ~9 GiB | 本机不可行 |

**三条约束:**
1. **显存** — 见上
2. **Prefill 延迟** — 32K 冷启动首 token 约 4–10 秒,语音模式不可接受。**必须开 prompt cache / prefix 复用**
3. **有效上下文 < 标称上下文** — 长上下文中段检索准确率显著下降

**隐藏杀手:一张 2560×1440 截图在常见 VLM 中是 1500–4000 tokens。** 10 步 GUI 循环仅截图就 20–40K。

> 这是「电脑操控优先 UIA 树」的**第二个理由**(第一个是显存):同一窗口的 UIA 树通常只需 200–800 tokens,且更精确。

### 8.3 存储预算

| 项 | v2.x |
|---|---|
| LLM(8B + 4B + 小 VLM) | ~28 GB |
| Embedding / Rerank(CPU) | 5 GB |
| 语音 | 9 GB |
| 图像 | 复用现有 16.9 GB + 增补 LoRA ~5 GB |
| 3D | 0 |
| 运行时 | ~30 GB |
| 记忆库 + 数据库 | 2–10 GB |
| **Vigil 模型** | **~1.2 GB** |
| 候选区 | +35 GB |
| 缓存 | ~30 GB |
| 备份(本地 + 移动盘) | +40–80 GB |
| **起步** | **~78 GB** |
| **全功能就绪** | **~135 GB** |
| **第一年末** | **250–400 GB** |

### 8.4 存储生命周期与 GC

| 根 | 可自动删 | 策略 |
|---|---|---|
| CODE | ❌ | git 管理 |
| **STATE** | ❌ | **只归档不删。记忆库与交易账本进白名单 + 独立备份链** |
| MODELS | 🟡 | **引用计数**,非 LRU;只提示不自动删 |
| ASSETS | ✅ | 三池分级 |
| CACHE | ✅ | 纯 LRU/TTL — **但 `cache/hf` 例外,见 §8.4.1** |

**产物三池**:`draft`(TTL 后回收,永久保留 Asset Spec + seed)· `adopted`(永久 + 备份)· `exported`(只留元数据引用)

> **可复现性把「存储」变成了「缓存」。** 删掉一张 2MB 草稿只损失几 KB 记录。

**水位线**:75% 提示 · 85% 清 CACHE · 92% 回收超期 draft · 95% 拒发新 JOB 租约。

**回收器要求比人工整理更严**(它无人按确认键):永不 `delete` 只移隔离区 · 每次产出清单与释放报告 · 除 CACHE 外先报告后执行 · **备份目录对回收器不可见**。

**第一个月只观察不清理。**

#### 8.4.1 `cache/hf` 例外

`cache/hf` 语义上属 CACHE 根,但**不参与自动清理**。

| | 一般缓存 | `cache/hf` |
|---|---|---|
| 体积 | 几 GB | 十几到几十 GB |
| 重建耗时 | 秒到分钟 | **实测 ~11 MB/s,17GB 约 27 分钟** |
| 能否重建 | 一定能 | **不一定** —— 模型可能下架或改许可 |

> **分界不该只看「能不能重建」,还要看「重建的代价」。** 一个能重建但要花半小时且可能失败的东西,不该被静默删除。

### 8.5 ★ 备份与灾难恢复(v2.2 改为自动)

#### 8.5.1 为什么只有记忆非备不可

| 数据 | 丢失后果 |
|---|---|
| CODE | git 有 |
| MODELS | 可重新下载 |
| ASSETS · draft/exported | Asset Spec 可重放 |
| CACHE | 可随时清空 |
| **STATE · 记忆库** | **不可重建。没有任何来源可以重放。** |

#### 8.5.2 单轨:手动全量到移动盘

| 目标 | 频率 | 加密 | 保留 |
|---|---|---|---|
| 移动固态 G:(Disk 2,独立物理盘) | **你插盘时手动触发** | **不加密**(D21) | 12 代 |

> **2026-07-26 决议 D17(修订)**:用户明确决定**不做每日自动到 C: 的增量备份**,只保留手动到移动盘。
> 设计阶段曾提出自动增量方案(公钥加密到 C:),**已作废**。

**★ 但「送达度提醒」保留,而且在纯手动模式下更重要。**

每台设备(含手机)每次连上时读一个只读状态端点,比对上次成功备份时间:

```
6 天前  → 托盘角标
14 天前 → 进日报(§4.7.5 L1)
30 天前 → 确定性 L4 白名单,强提醒
```

它不需要自动备份存在,只需要知道「上次备份是什么时候」。**这是纯手动模式下唯一能对抗「取决于你是否记得」的低成本机制。**

##### ⚠ 手动模式必须明确接受的代价

| 事实 | 后果 |
|---|---|
| 记忆库(P3a 起)**每天都在变** | 一周不备就可能丢一周的记忆 |
| **Vigil 上线会把日变更量再提高一个量级** | 间隔越长丢得越多 |
| 移动盘不常插 | 备份间隔取决于你的习惯,不取决于任何机制 |
| 唯一副本在工作站的 Disk 1 上 | 而 code 根(E:)与数据根(D:)**同在 Disk 1** —— 该盘故障时两者一起没 |

**推翻条件**(写进 D17):
- **实际备份间隔中位数连续 4 周 > 14 天**,或
- 发生过一次「想恢复却发现备份太旧」

→ 重新评估自动备份。届时若恢复自动方案,**必须用公钥加密**:一个无人值守的任务必须能自己取到密钥,而任何本地可读的对称密钥都能被同用户的进程取到 ——「工作站被入侵时备份仍然安全」这句话在对称密钥下是假的。

#### 8.5.3 备份矩阵

| 根 | 内容 | 说明 |
|---|---|---|
| CODE | `git bundle --all` | 单文件,含全部历史,自带校验 |
| **STATE** | 全量,**排除 `quarantine`** | 隔离区装的是打算删除的东西,不该在 12 个备份代里继续存活 |
| MODELS | **只备清单**(路径 + 大小 + sha256 + 来源 + 许可证) | 权重可重下 |
| ASSETS · adopted | 全量 | |
| ASSETS · draft/exported · CACHE | 不备份 | |

#### 8.5.4 六条铁律(铁律 1 于 2026-07-26 修订)

1. **~~备份必须加密~~ → 全链路不加密(D21 + D22)。**

   `backup.ps1` 由 `paths.toml` 的 `[backup].require_encryption` 控制,当前为 `false`。

   > **★ 必须写清、不得含糊的一句话:**
   >
   > **记忆库在工作站磁盘上是明文,在备份盘上也是明文。**
   > 拿到任一者的人可以直接读取全部记忆 —— 家人关系、偏好、习惯、所有对话事实。
   >
   > D22 之前曾有一条「备份 `.vhdx` 文件本身则介质上仍是密文」的性质。
   > **该性质随 D22 一并失效** —— 没有加密卷,就没有文件内部的加密。

2. **~~恢复密钥离线保管~~ → 不适用(D22 后无密钥)。**

   > 这条铁律的消失是 D22 的**收益**而非疏漏:
   > §15 原本把「BitLocker 恢复密钥丢失 = 记忆库永久不可读」列为**高**风险,
   > 而对一台家用单人机器,密钥丢失的概率高于定向窃取。**D22 消除了这个风险。**
   > 代价写在铁律 1,收益写在这里,两边都不抹掉。
3. **恢复演练** —— **没演练过的备份不算备份**。P3a 上线前跑通一次完整恢复,此后每季度一次
4. **备份不可由助理自身的文件工具触及**(进 §6.7.4 硬禁止清单)
5. **备份失败必须告警**(进 §4.7.6 确定性 L4 白名单)
6. **校验清单必须可移植**:无 BOM · LF · 正斜杠 · GNU `sha256sum` 兼容格式。灾难恢复的典型场景就是 Windows 起不来 —— 清单只能被 PowerShell 读,等于把恢复路径限死在「Windows 还能启动」这个前提上

#### 8.5.5 ⚠ 仍未解决:活数据库怎么备才能真的恢复出来

> **D22 让这一节简单了一半,但没有让它消失。**

**已因 D22 消失的问题**:VHDX 挂载态被占用、`.vhdx` 复制到 exFAT 时的稀疏文件膨胀、
备份前后要挂载/卸载 —— **这些全都不存在了。** 记忆库是普通文件夹,直接 robocopy 即可。

**仍然存在、且与加密无关的问题:**

> 对一个**正在运行**的 PostgreSQL 做文件级复制,得到的是**不一致快照**。
> 恢复出来的数据库可能无法启动,或丢掉最近的事务。

这是数据库的固有性质,跟加不加密、放不放 VHDX 都没关系。而它属于
「**备份看起来成功了,但需要时恢复不出来**」—— 正是铁律 3 要防的情形。

**标准解法(简单且成熟,P3a 实现时采用):**

| 组件 | 做法 | 说明 |
|---|---|---|
| PostgreSQL | **`pg_dump`** 逻辑备份 | 单文件输出,不用停服,恢复用 `pg_restore`。2–10 GB 的库通常几分钟 |
| Qdrant | **snapshot API** | Qdrant 自带,生成一致的快照文件 |
| 其余 state | 直接 robocopy | 票据 · 日志 · 隔离区(已排除) |

> **不要用「停服 + 文件复制」** —— 它需要备份脚本去停止/启动服务,多两个会失败的步骤,
> 而且用户会因为「备份还要停服」而干脆不备。`pg_dump` 不用停服。

**待 P3a 落实**:`backup.ps1` 增加 PG 与 Qdrant 的导出步骤。
**在此之前,`backup.ps1` 对记忆库的备份能力仍未经验证** —— 但因为记忆库要到 P3a 才存在,
这不构成当前阻塞。

---

## 9. 可观测性与日志

### 9.1 三类记录

| 类 | 记什么 | 存哪 | 保留 |
|---|---|---|---|
| 运行日志 | 元数据:时间 · 别名 · token · 延迟 · 显存 · 成本 · 工具名 | `{state}/logs/` | 90 天 |
| 审计日志 | 谁对什么做了什么 · 批准链 · 哈希 · 回执 | PostgreSQL | **不删** |
| 指标 | 显存/磁盘水位 · 任务时长 · 失败率 · API 累计花费 | 时序表 | 1 年 |
| **区域访问** | Vigil 与主助手读了哪些区域(含 `rel`) | `{state}/logs/zones/` | 30 天 |

### 9.2 ★ 隐私规则(v2.2 收紧)

```
永远记录:元数据
工具调用的**结果**:默认只记类型、字节数与 sha256,**不记正文**
默认不记录:对话正文、语音原始转写
永不记录:记忆库内容
按需开启:单会话显式开关 + 短 TTL(D22 后无加密可用,TTL 是唯一约束)
eval 语料:显式「捐赠本轮」按钮采集,非默认全量抓取
```

> **审查 P-2**:v2.1 的「永远记录工具调用参数与结果」会把文件正文写进明文日志。

工具注册表每个工具加两个字段:`log_result: none | digest | full` 与
**`log_args: none | schema_only | redact_marked | full`**。
**凡具备读取能力的工具默认 `log_result: none`**;设为 `full` 必须在 `DECISIONS.md` 留条目。

> **★ 为什么必须有 `log_args`**:`log_result` 只管**结果**,而带值的从来是**下一个**工具的**参数**。
> 凭证工具强制 `log_args: schema_only`;任何可能接收 `fill_token` 的参数强制 `redact_marked`。

**★ 原有的「区域访问日志进加密卷是 §6.7 上线的前置条件」已随 D22 取消。**

那条前置条件在 D22 之后**永远无法满足**(没有加密卷了),留着就是一条系统不可能执行的规则。
裁决:**正常记录 `rel`,不做削减**。理由:

> **日志不可能比它记录的对象更敏感。**
> 记忆库本身已经是明文躺在同一块盘上,再去保护「谁读了哪个文件」的访问日志,是安全剧场。
> 真正该做的是 §6.8 的账户隔离 —— 它同时保护记忆库和它的日志。

保留 30 天上限与 §6.7.4 硬禁止清单里的 `{state}\**`(Vigil 与主助手都读不到自己的访问日志)。

### 9.3 告警最小集

磁盘水位越线 · 显存租约被拒 · 出境费用达 80%/100% · **备份任务失败** · **备份过期** · 记忆写入失败 · 隔离区即将到期 · **非授权账户尝试连接记忆库** · **`pending_review` 积压超 50 条**

---

## 10. 测试策略

| 组件 | 风险 | 最低要求 |
|---|---|---|
| **文件回收器 / GC** | **最高** — 无人按确认键的自动文件整理器 | 单元测试 + **干跑模式** + 隔离区可恢复性测试 |
| **权限区域** | **最高** | **N1–N14 否定用例先于功能写**;区域外读取必须拒绝 |
| **出境闸门** | 最高 | 断言覆盖 **str 拼接 / f-string / 模板渲染 / 日志格式化** 四种取值方式 |
| **权限策略引擎** | 高 | 以**否定用例**为主;六元组每维都要有拒绝用例 |
| **打扰评分** | 高 | 注入用例:构造带恶意 deadline 的文件,断言最高只能到 L1 |
| **记忆系统** | 高 | eval 集;冲突/superseded 回归;**自动事实不得 supersede 用户事实** |
| **多设备** | 高 | 远程角色读 S2 必须异常;Off 档出境载荷不含 memory_id 对应文本 |
| GPU Broker | 中 | 并发申请/驱逐/组件集合变更的事务性 |

**原则:凡是「自动执行且难以撤销」的组件,先写否定用例再写功能。**

**★ 对抗配置下重测**:出境哨兵串用例必须在**所有能翻转的开关都翻转**的配置下再跑一遍。只测默认配置等于没测。

---

## 11. 目录结构

```
E:\.meine\.Proj_Soft\.Proj\.localAI\        ← AI_ROOT(git)
├── 00-docs\
│   ├── PROJECT_PLAN_v2.2.md    本文档(现行)
│   ├── AUDIT_v2.0.md · P1-thresholds.md
│   ├── STATE.md · DECISIONS.md · worklog\ · snapshots\ · audit\
│   └── archive\                v0.4 · v1.0 · v2.0 · v2.1
├── 10-core\
│   ├── gateway\ · memory\ · orchestrator\ · gpu-broker\
│   ├── job-service\ · session-service\
│   ├── mcp-tools\{stdio,http}\ · trade-stub\
│   └── vigil\                  ★常驻清醒助手(CPU)
├── 20-client-win\ · 30-client-web\(★PWA)
├── 40-integrations\{comfyui,model-curator,prompt-translator,cloud-image}\
├── 90-ops\{backup,storage-audit}\ · devshell.ps1
└── config\
    ├── paths.toml              ★唯一路径配置源
    ├── vram-budget.toml        ★desktop_floor + 推荐组合(D25)
    ├── zones.toml              ★权限区域(进 git)
    ├── zones.local.toml        ★硬禁止清单(不进 git)
    └── interrupt-policy.toml   ★打扰评分参数

D:\AI\  ← 数据区   models\ assets\ cache\
        └── state\  db\ logs\ tickets\ quarantine\ policy\ state\memory\
D:\ComfyUI\  ← 保持原位
G:\localAI-backup\  ★手动全量(不加密 — D21)· 12 代保留
```

### 11.1 路径契约

1. **代码中禁止绝对路径**,pre-commit 拦截
2. **数据库存 `{root, rel}` 二元组**
3. **第三方缓存全部收编**
4. **迁移必须带校验**

---

## 12. 执行协议

### 12.1 记录协议

| 文件 | 回答 | 更新时机 |
|---|---|---|
| `STATE.md` | **现在在哪** | 每个节点结束;中断前 |
| `DECISIONS.md` | **为什么这么定** | 每次不易反悔的选择 |
| `worklog/YYYY-MM.md` | **做了什么、测出什么** | 每次实作或实测 |

`DECISIONS.md` 条目格式:背景 / 选项 / 决定 / 理由 / 代价与已知风险 / **推翻条件**。

> **「推翻条件」是本格式里最重要的字段。** 半年后你需要知道的不是「当时决定了什么」,而是「这个决定现在还成不成立」。
> **★ 不得把新决定伪装成旧决定的延续** —— 那恰好绕过了这个字段。

### 12.2 节点循环

```
1. 读 STATE.md → 2. 做一件事 → 3. 写 worklog → 4. 若有不易反悔的选择则写 DECISIONS
→ 5. 更新 STATE.md → 6. 若跨阶段则对照验收标准逐条确认
```

> **不更新 `STATE.md` 就不算做完。**

### 12.3 首轮门禁

| 门禁 | 说明 |
|---|---|
| **路径合规** | 配置全部来自 `paths.toml` |
| **显存申报** | 声明峰值;P4 后走 Broker 租约 |
| **失败可见** | **不静默降级** |
| **可回退** | 知道怎么关、怎么恢复 |
| **数据边界** | 能读什么、能写什么;出境须过闸门 |

### 12.4 审批规则

| 级别 | 适用 | 机制 |
|---|---|---|
| **自动** | 只读、可重放、无外部后果 | 事后可查 |
| **逐次确认** | 写文件、点击提交类控件、单次出境 | 执行前展示**精确动作预览** |
| **哈希绑定批准** | 批量文件操作 · GC 回收 · **L4 写入** · 交易提案 · 新增 rw 区域 | 计划哈希绑定 → 批准 → **执行前复核哈希** → 执行 → 回执 |

**三条铁律:** 永不 `delete` 只移隔离区 · 除 CACHE 外先报告后执行 · **批准的是具体计划,不是一类操作**。

---

## 13. Backlog

| # | 事项 | 触发 | 备注 |
|---|---|---|---|
| ~~B1~~ | ~~iOS 访问~~ | — | **已提到 P3b** |
| ~~B2~~ | ~~外网远程访问~~ | — | **已提到 P3b** |
| **B3** | 3D 资产生成 | 2D 与交互打磨稳定后 | 本地生成式 / 云端 API / **Blender 脚本化** 三选 |
| **B4** | 投资自动化 | 新硬盘到位后 | 接口与表结构已预留(§7) |
| **B5** | 全双工语音(LiveKit) | 半双工体验验证后 | |
| **B6** | 第二台 PC **完整接入** | **有一个具体的、写得出来的任务** | 见 §14.9 |
| **B7** | UE5 导入管线 / PBR de-light | 3D 启动时 | 与 B3 绑定 |
| **B8** | 打扰评分 L2/L3 档与自适应 | 有真实数据后 | §4.7.5 |
| **B9** | S0/S1 三级分域 | 一个月真实读取日志后 | §4.11.4 |
| **B10** | **★ 硬件变更的自动重标定** | **随 P4 一起做** | 见下方 B10 详述 |
| **B11** | 检索词表补【日语】条目 | 有日语记忆内容时 | i18n(D42.1):**UI 三语骨架已支持,正文铺满随 P7(D77)**,日语【内容】进记忆才需要补词表;UI 语言 ≠ 内容语言 |
| **B12** | 翻译工作室「记住我查过的词」 | 翻译工作室稳定后 | D42.2:翻译内容默认不进记忆;这是后期独立集成 |
| **B13** | **★ 联网搜索(出入网,待定)** | **须先有出入境裁定** | D42.5:更新知识库+搜索,内置非单独界面。出站碰 L5/出境闸,入站是不可信内容须过 E1/走 pending。**主门=每任务手动【放行】按钮**(trusted-local 带外动作、每按一任务、不可被派生内容触发,是 E1 带内解除的正确版)。**预留,不本期做**,方案如 D38/D39 那样先裁定再动手 |
| **B14** | 课件预设文件的内容与格式 | 做 PPT 工作室时 | D42.3:声明式模板(课程名/听众/版式/章节骨架/术语表/常设指令);用户手管、不进记忆;AI 代改则走 P6 文件区+哈希绑定 |
| **B15** | **换本地模型时的替换评测** | 未来换模型/换量化时(约 P4 附近) | Codex 交付评测包 `90-ops/localai-model-replacement-eval/`(55 题 + RUBRIC + scorecard + 6 视觉夹具)。★ **五步纪律(别忘)**:①先用当前模型建基线 ②按别名分测 assistant.fast/deep/vision ③依次 smoke→core→safety→full ④**安全硬失败不能被其他高分抵消** ⑤通过后才改语义别名映射。只比模型、不改架构/registry/中央文档;不因接入它打断 P3b |
| ~~**B16**~~ | ~~curated 六词 SAS 词表~~ | ✅ **2026-08-06 已交付(D89)** | `localai-sas-wordlist-v1-en2048`(2048 词)取代 v0 CVC 占位。判据全部反向钉死:恰好 2048 · 互不相同 · 3–8 位小写 ASCII · **前 4 字母互不相同** · **不含同音词对**(已逐对剔除 `right/write`、`pair/pear`、`peace/piece`、`wear/where` —— 念出口比对是它的**主要**用法,同音词会让一次真正的不一致被听成"对上了")。**版本与内容机械绑定**:`KnownVersions` 是 `(版本, 内容 SHA-256)` 冻结表,改任何一个词就当场判红,想变绿只能新加一行版本 ⇒ **版本字符串无法不跟着变**(已红测:`zebra→zebroo`)。「只换显示词,不改索引/HKDF/安全性质」由一条**冻结索引向量**断言钉死(固定 transcript → `274,9,590,909,1496,1156`,红测期间保持绿色 = 现场证据) |
| ~~**B17** TPM↔TLS~~ | ✅ **2026-07-29 裁定=选项②(D44)** | — | 攻坚选项①(`b17-tpm-tls` spike)全败(CngKey+CopyWithPrivateKey / AllUsages / New-SelfSignedCertificate PCP 均不行)。裁定:**TLS 叶密钥用不可导出软件 CNG,CA 仍 TPM**(修订 D35/D43 S0.7)。S4 生产路径 selftest 8/8 通。未来解决 TPM↔SChannel 可迁回 |

**复核规则**:每次版本更新时逐条复核;同时复核 `snapshots/` 里的第三方条款是否已变。

### B10 · 硬件变更的自动重标定(随 P4 实现)

**问题**:硬件换了但预算没重标定时,闸门会拿着旧机器的数字在新机器上做判断,**而且不报错**。
这与 S2 的编码指纹是**同一个失效模式**——不一致时不会崩,只会悄悄给出错误结论。

#### ① 两组指纹,各自驱动不同的重测项

不是"换了 GPU 就全部重测"。要分开,因为它们的可移植性不同:

| 指纹 | 内容 | 不一致时需重测 |
|---|---|---|
| **GPU 指纹** | 型号 · `total_vram` · 驱动版本 | 组件 `peak` 里**与硬件相关的那部分**(见 ②) |
| **显示指纹** | 分辨率 · 显示器数量 · 缩放 | **`desktop_floor`**(A7) |

★ `desktop_floor` 的决定因素是**显示配置**而非 GPU 型号 ——
桌面的工作集(framebuffer · DWM 合成表面 · 浏览器 GPU 纹理)由分辨率与显示器数量决定。
所以换卡但显示配置不变时,`6.6` **大体可移植**;换显示器/改分辨率则必须重测,哪怕 GPU 没动。

#### ② ★ 已测值的真正价值:把组件 peak 拆成「可移植 + 需重测」

一个组件的 peak 由几部分构成,可移植性完全不同:

| 组成 | 由什么决定 | 换 GPU 后 |
|---|---|---|
| 模型权重 | 量化格式与模型本身 | **不变** |
| KV cache | 上下文长度 · 层数 · 注意力头数 | **不变**(可由公式推) |
| **CUDA context** | **驱动与 GPU** | **需重测**(P1-A1 实测本机 227 MiB/进程,线性可加) |
| 激活与临时缓冲 | 实现与 batch | 小幅变化 |

⇒ 换硬件时**不必全部重跑 P1**:只需重测 A1(CUDA context,很快),其余按已知结构外推,
再用**少量抽检**验证外推是否成立。这比全量重测省得多,又比直接沿用安全。

#### ③ 已测值同时是**回归基线**

重测出的新值若与旧值差异超阈值,**多半是测错了而不是硬件不同** ——
例如 `8b@16k = 5.92` 的主体是固定的权重与 KV,换卡后应当只差几百 MiB 的 CUDA context;
若测出 3.0 或 9.0,那是测量出了问题。
**已知值让新测量能自我校验** —— 这正是 P1 那 17 项实测在硬件变更后的复用方式。

同理适用于性能数字:新卡若比旧卡**更慢**,那是配置问题(offload 设错 / 驱动没装好),应当告警。
`30B-A3B 57.5 tok/s` 因此是一条有用的下界期望 —— 但要注意它是在 **38% offload** 下测的,
显存宽裕后 offload 减少,该数字**只能作下界不能作等值**。

#### ④ 降级检测:把沉默的症状变成一句话的诊断

`vram_budget = total_vram − desktop_floor − safety_margin`,而 **`desktop_floor` 是刚性的**
—— 换小卡时桌面需求不会跟着变小,AI 侧被挤压得不成比例:

```
16 GB → vram_budget = 15.92 − 6.6 − 0.8 = 8.52
 8 GB → vram_budget =  8.00 − 6.6 − 0.8 = 0.60   ← 等于没有
```

⇒ 启动期自动判定:
```
若 vram_budget < min(所有组件 peak)
  → 明确报「这张卡装不下任何 AI 组件,GPU 路线不成立」
  → 而不是让三层闸逐个静默拒绝
```
**降级不是"少装几个模型",可能是整个 GPU 路线不成立。** 这条诊断必须响亮。

#### ⑤ 实现挂钩

`config/vram-budget.toml` 里已有 `calibrated = "partial"` 字段,是现成的挂钩;
`vram_gate.py` 已在读 NVML,拿到 GPU 型号与总显存的代价接近零。
新增 `[calibration]` 段记录指纹与测量日期,启动期比对,不一致则**拒绝启动并说清要重测什么**
(§12.3 失败可见 · 与 S2 编码指纹 fail-closed 同一手法)。

#### ⑥ ★ 重标定要做成**主机管理端里的一键流程**,不是一份人工跑的清单

**归属**:主机后端管理端(D36)。它属于**管理操作**(D37)——只在主机本地、走 IPC、
网关上没有这条路由。因为它会改 `desktop_floor`,而改那个值需要**哈希绑定批准**(§12.4)。

★★ **产品化后这不是可选项**:每个客户的硬件都不同,**他们全都需要重标定**。
所以本条是 **P11 的必要组件**,不只是自用的便利。
换个说法:**重标定 = P1 里可重复的那个子集被产品化**,一次性基准测试变成常备工具。

##### 两类测量,自动化程度完全不同 —— 这条界线必须画清

| 类别 | 项 | 能否一键 | 为什么 |
|---|---|---|---|
| **机器能自己造负载** | **A1 CUDA context**(启 N 个进程读 NVML 取差值)· **各组件 peak**(逐个装载→读峰值→卸载) | ✅ **可全自动** | 负载由程序自己制造,确定性,分钟级 |
| **负载必须由人制造** | **A7 `desktop_floor`** | ❌ **不可能一键** | 它测的是「UE5 + 多标签 Chrome + 视频」这种**真实重负载**下的桌面占用上界。程序造不出有代表性的桌面负载 |

⇒ `desktop_floor` 的正确形态是**引导式测量**而非一键:
「请打开你平常最重的那套东西 → 点开始 → 采样 N 分钟取上界」。
★ 且必须是**持续采样取上界**,不是读一次瞬时值 —— §8.1 自己记着
「开测前的瞬时读数不能用来判断负载特征」。

##### 重标定期间必须独占 GPU

装载组件测 peak 会占满显存。⇒ 重标定流程**先取一个独占租约**
(复用 P4 的同一套租约机制,见 D37「一套机制三处用」),期间拒绝其他客户端的装载请求,
并在各客户端面板上显示「主机正在重标定」——**不是静默阻塞**。

##### 结果不得直接写入

测完**不能直接覆盖** `vram-budget.toml`。必须:

1. **与旧值对拍**(B10③ 的回归基线)—— 差异超阈值时报「多半是测错了」,让人决定,不自动采纳
2. 改 `desktop_floor` 走**哈希绑定批准**(§12.4)
3. 通过后一次性写入:新数值 + 新 `[calibration]` 指纹 + `measured_at`
4. **事务语义**:要么全套写入,要么一个字不写 —— 跑到一半崩掉不得留下半套数字
   (半套数字比没有更危险:闸门会拿着混合了两代硬件的数据做判断)

---

## 14. 推进纲领

> **v3.0 重排(2026-07-28)。** 触发原因:传输层与客户端形态整体改向 —— 见 D34–D41。
> 重排的是**阶段结构**;§4 记忆系统、§8 显存模型、§6 权限区域等设计章节**不受影响**,
> P1 的 17 项实测结论全部保留。
>
> **相对 v2.2 的实质变化**:
> · 原 P3b「多设备(Tailscale + 手机 PWA)」**整条作废**,被 P3b/P3c/P3d 三段取代
> · 原 P7「Windows App」**大幅前移**为 P3c,并从"补齐"升级为**唯一入口**(Open WebUI 退役)
> · 新增 P3d「外联通道」,其**硬前置是响应侧出境闸** —— 审查确认 L5 在代码里从未实现
> · P4 扩容:多客户端仲裁 + **权限档位六元组从 P2 挪来**(D37 让它有了真实客户)
> · P8 Vigil 获得**形体**(桌面宠物),并新增实测项 A7-pet
> · 新增 P11「产品化」(条件触发,非必经)

### 阶段依赖(必须按此顺序)

```
P3a 记忆系统 ──┬── P3c 客户端 v1 ── P4 Broker+仲裁 ── P5 语音 ── P6 操控 ── P7 客户端 v2 ── P8 Vigil+宠物
P3b 身份与准入 ─┘                                                                    ↑
P3d 外联通道(独立,前置=响应侧出境闸) ─────────────────────────────────────────────────┘
```

---

### P0 · 地基 ✅ 已验收(2026-07-26)

核实 C: 内容 · HF 缓存回收 · 接网线 · 目录结构 + paths.toml + pre-commit · CUDA 13.2 · git + 记录协议 · 备份脚本与恢复演练。
**额外交付**:`devshell.ps1` · cmake/ninja 入 PATH · 关闭 NVIDIA App。

### P1 · 基准测试 ✅ 已完成 17/17(2026-07-27)

全部项有实测值,每项都有书面结论。五个改变架构的发现见 §0.1 与 STATE。
**新增待测项 A7-pet**(桌宠渲染的稳态显存占用)—— 归属 P8,列在此处仅为口径一致。

### P2 · 核心链路 ✅ 基本完成(2026-07-28)

- [x] llama.cpp server + **自写网关**别名层(D29)+ 模型注册表
- [x] embedding / rerank 服务(CPU :18084)+ 别名注册
- [x] PostgreSQL + Qdrant×2(含 `sensitivity_domain` / `crypto_tier`)
- [x] 账户隔离 `ai-mem` / `ai-asset` / `ai-exec`(§6.8)
- [x] **E1 入口凭证检测器**(网关侧)—— 2026-07-28 加固:解除权改为**按档位授权**
- [x] `secret_ref` 登记表
- [x] 剪贴板历史与跨设备同步状态检查
- [x] 无 Broker 期显存过渡措施(`vram_gate.py` 三层检查)
- [x] 首版界面 Open WebUI —— ★ **定性改为「过渡界面」**,P3c 就绪后退役
- [x] ~~命名决定:定死 tailnet 名~~ → 产物待定(D34 待决项 1)
- [ ] ~~WebAuthn 设备身份~~ → **作废**:非本机设备上不再有浏览器(D34),改走 mTLS(P3b)
- [ ] ~~权限档位(六元组)~~ → **移至 P4**(D37 让它有了真实客户:谁能改 GPU 状态)

**§14 验收标准**:①本机可用别名对话 ✅ ②换别名映射客户端零改动 ✅
③非授权账户连接记忆库被拒且告警 🟡(被拒 ✅;告警仅落审计文件,通道待 P7)

### P3a · 记忆系统单机版 ★ 核心里程碑 ✅ 已验收(2026-07-28)

> 验收签字见 `STATE.md` 顶部与 `DECISIONS.md`「2026-07-28 · P3a 验收签字」。四门全绿:
> 召回 1.0 · 精确 1.0 · **误记 0**(独立硬上限)· **溯源 1.0**(硬线)· **S2 泄漏 0**(硬线)。
> 逐步断言数见 STATE:S1 22 · S2 43 · S3 50 · S4 35 · S5 27 · S6 15 · S7 15 · S8 19 · S9 演练 12。

| 步 | 内容 | 状态 |
|---|---|---|
| S0 | schema 加固:append-only · supersede 方向 · tombstone/隔离区 · 分级需票据 · 禁批量确认 | ✅ |
| S1 | `tainted.py` 类型层 → `repo.py` 唯一写模块 → `gate.py` 唯一写入口 → `route.py` 纯函数选路 | ✅ 22/22 |
| S2 | 向量轨 + 双 Qdrant + **编码指纹** | ✅ 43/43 |
| S3 | 完整记忆闸(分级 · E3 · 队列 · 熔断 · 冲突) | ⏭ **下一步** |
| S4 | 隐私三层强制 + 账户隔离验证 + 日志纪律 | |
| S5 | 记忆面板的**后端 API**(浏览/编辑/删除/溯源)—— ★ **UI 移至 P3c** | |
| S6 | 冷启动初始化会话 + S2 定级(只跑一次) | |
| S7 | 评测集 + 四项指标 + 阈值标定 | |
| S8 | L4 写路径 + 哈希绑定批准 | |
| S9 | 备份/恢复演练 + P3a 验收签字 | |

**验收**:eval 通过率达标;两条例句都对(均已跑通);每条可溯源;恢复演练成功。

### P3b · 局域网身份与准入(取代原「多设备」)· 精简优先(D43)

> **方向=精简优先(D43 S0.10):** 先交付 load-bearing 的核心六件开 LAN,重度纵深防御推迟 P3b.2。
> **S0–S7 一次一节;S1–S4 回环门禁全过前禁止任何非回环监听或 LAN 开放。** Tailscale 已移除(D43 S0.4)。

- [x] **S0 · 先写决议不改运行态** —— **D43 已落**(废 WebAuthn/RP ID · D32 收窄 · D34 改写 · 移除 Tailscale ·
      新增 LAN_EDGE · 覆盖 D36 防火墙 · D38 CA 落 CNG/TPM · identity 排备份 · 审计硬化三条 · 精简优先)
- [x] **S1 · 回环技术 Spike ✅**(全程回环、未开 LAN;8 spike 全绿,见 worklog 2026-07-29):
      TPM 不可导出密钥 9/9 · Kestrel AllowCertificate+自定义根 mTLS 6/6 · 流式 6/6 ·
      撤销断流(13ms)+generation/freshness fail-closed 6/6 · ServerCertificateSelector 热切 4/4 ·
      匿名资源边界 3/3 · 命名管道 PID/SID 互认+DACL 5/5。**推迟 P3b.2**:DNS-SD 自动发现 · 服务账户三服务分权
- [x] **S2 · PKI 与身份存储 ✅**(48 断言全绿,对 scratch 测;★ production `init` 未运行):
      - [x] **S2.1** CA + 证书签发核心(`10-core/identity/`,.NET):CA 私钥落 TPM 不可导出 · 服务器/客户端叶签发 ·
        **扩展全部服务端生成(CSR 注入被忽略)** · CSR PoP 校验 · **11/11**(D43 S0.7)
      - [x] **S2.2** `init`(铸 hub_id+CA)· JSON 成员表(devices/device_certificates/单调 generation,原子写)·
        host-admin CLI(init/status/list-devices/revoke-device)· `${state}/identity` 根 + 幂等 fail-closed · **15/15**(D43 S0.8)
      - [x] **S2.3** **双向六词 SAS**(自写确定性 CBOR→SHA256→HKDF→66bit→6 词;MITM 换服务器叶即被抓)·
        enroll/status/claim/complete 幂等状态机 · 窗口门 + 队列封顶 8 · **单条批准**(拒 re-approve/批量)· **22/22**
      - [x] ~~**推迟 S4/P3b.2**~~ **✅ 四件已全部交付**(HTTP `/pair/*` 路由 → S4 · 其余三件 → **D89**,2026-08-06):
        `/pair/*` 匿名路由(LAN Edge,见 S4)· **续期状态机**(`enroll`/`complete` **两条**独立路由 + 两张独立状态表;
        ★ `complete` 之前绝不退休旧证书)· **curated SAS 词表 B16 v1**(`localai-sas-wordlist-v1-en2048`,
        2048 词,版本与内容 SHA-256 机械绑定)· client-transport CLI(`20-client-win/transport/`)
        > ★ 2026-08-06 更正勾选:此前这一格记作「推迟」,而四件在 D89 落地时已全部做完 ——
        > 方案书落后于实测。**不是把新决定伪装成旧决定的延续**:D89 是新决议,这里只是把勾选对齐到事实。
- [x] **S3 · 网关先加固 ✅**(LAN 监听前必须完成 · 10/10 + 全回归:caller_policy 15 · gateway_e1 36 · e1 50 · egress 12):
      · ★ 带证书指纹头→查 S2 成员表→**封顶 `LAN_DEVICE`**;即便 classify_caller fail-open 成 trusted-local,
        带指纹请求也拿不到 trusted-local 能力(尤其**解不了 E1**);本机进程伪设此头只会降权;主体只来自成员表,不认自报 device_id
      · 路由归类元测试(`ROUTE_TIERS`+`unclassified_routes`,新路由未归类=失败)· 关掉 /docs·/redoc·/openapi.json
      · `/health` 收窄(不泄别名)· `/v1/models` 认证 · 新增 `LAN_EDGE` 档(纵深防御,永不落 trusted-local)
      · `require_trusted_local` 仍备(记忆端点将用)· **P3a caller-policy + E1 契约回归全过**
      · 剩(不阻塞开 LAN 的门):llama-server `--api-key`(防绕过直连 18081)→ 随 start-stack/secrets
- [x] **S4 · LAN Edge ✅ 逻辑完成**(`10-core/lan-edge/`,.NET Kestrel · selftest 8/8,回环):
      mTLS 终止 + 客户端证书验证(链到 CA + clientAuth + **成员表 active**)· 业务代理回环网关(**注入验证过的指纹、
      剥除客户端自带 X-LocalAI-***)· `/pair/*` 匿名路由接 S2 配对 · Kestrel 资源上限 · 只绑回环。
      ★★ 开真 LAN 前置 = 裁定 B17 —— **已于 2026-07-29 裁定 = 选项②(D44)**:TLS 叶密钥用不可导出软件 CNG、CA 仍 TPM;生产路径 selftest 8/8 通,LAN 已开通并实机验收(SChannel 不能用 TPM 密钥做 TLS 凭据;逻辑已用软件密钥证全对)
- [~] **S5 · 可恢复地开放 LAN**(脚本就绪 · 执行=用户,对外行为):
      **防火墙窄化脚本已写** `90-ops/lan/lan-firewall.ps1`(选定网卡+Private+LocalSubnet+EdgeTraversal=Block+程序限定,
      拒 Public,扫冲突宽规则;`-Remove` 关闭)—— **你 elevated 运行**(网络/防火墙改动=用户执行)。
      activation 精简(启动对账+不满足即回环)+ Edge 绑选定网卡 = 随 S6 真机联调时收尾。7 步持久 saga 推迟 P3b.2
- [x] **S6 · 第二台 Windows PC 实机验收 ✅**(2026-07-29,两台真 PC):
      **验收清单** `90-ops/lan/S6-acceptance-checklist.md`(初次配对/六词SAS · 拔WAN冷启动 · 换DHCP免重配 ·
      单台吊销+流式中吊销 · 暴露面扫描 · 权限回归 · 拒绝且告警)**已按清单走完,全过**。
      实测:配对(mTLS + 六词 SAS,`SENIORBIRDS` → active/`LAN_DEVICE`)· 成员调用 200 且 Edge 注入的是**已验证指纹**
      (客户端伪造的 `X-LocalAI-*` 被剥离)· **吊销后 401** · 重配对新证书 active。真 hub = `f6hsduipeesexb6f`。
      > ★ 2026-08-06 更正勾选:`STATE.md` 自 2026-07-29 起就记着「P3b ✅ 实机上线完成(两台真 PC)· S0–S6 全绿」,
      > 而这一格一直是 `[ ]` —— **两份中央文档对同一件事给出相反的状态**。实测支持 STATE ⇒ 改方案书。
- [ ] **S7 · 串行更新中央文档**(DECISIONS/PLAN/STATE/worklog,唯一由主 Claude 一次性写)

**推迟 P3b.2(不丢核心安全属性)**:三服务分权(edge/registry/signer named-pipe SID 互验)·
~~双证书**自动**轮换状态机~~ **✅ 已于 2026-08-06 交付(D89)** —— `ServerCertRotator`,**没有自己的持久状态**
(每一跳重读 `server.cer` 的 `NotAfter`,证书自己就是进度);Kestrel 改 `ServerCertificateSelector` ⇒ **不中断已有连接** ·
7 步持久 activation saga · DNS-SD 自动发现(先静态配置对端)。

**★★ 铁律**:网关绑 LAN 与 LAN 上有 TLS 是**同一个不可分割交付项** —— 在 `90-ops` 加断言:未带 TLS 则**拒绝绑非回环**(fail-closed)。
全仓库零 TLS 资产、WPA2-PSK 无前向保密;**做不出 TLS 之前继续只绑回环。**

**验收**:未授权 LAN 设备被拒**且告警**;吊销一台后该台立即 401 而其余不受影响;拔 WAN 后局域网路径完全不受影响。

### P3c · 统一客户端 v1(原 P7 前移,升级为唯一入口)

> ★★ **设计已定稿(2026-07-29)**:`00-docs/CLIENT_UX_DESIGN_v1.md` + 决议 **D45**。
> 定位 = **两位成年人的「家庭操作系统」**,主页(今天)为中心;IA = 主页 / 工作空间六个 / 系统三个;
> **三层可见范围**(家庭·个人·仅本人)· 工作空间用 **Chrome 式标签页**(非下拉)· 三皮肤(微风/墨白/暖萌)·
> 天气(三城完整)与时间(四地等位)**分层** · AI 可读上下文规则。
> ★ **硬前置(D45)**:**身份层要从「设备」扩到「设备 × 成员」**——P3b 只认证设备,而「仅本人」与成员二次确认需要「人」。

> **客户端主功能界面(D42):聊天 · 资产生成 · 投资 MCP · 翻译 · PPT/课程生成 · 电脑操控。**
> 记忆面板 · 组件选择器 · 宠物 · 语音是贯穿性的,不单列。引擎归属见 D42
> (翻译/i18n=纯客户端;PPT=P9+**P6 落盘**;资产=P9;投资=§7/B4;电脑操控=P6)。
> **★ 界面切换发"意图信号"给 P4 Broker(D42.6):P3c 先只做"当前组件"显示 + 手动勾选,
> 自动跟随与两机争用仲裁在 P4 实装。**

- [x] ~~Tauri 壳~~ → **WPF 原生壳(D47 改选型)**:独立窗口 · 托盘(关窗不退出)· 开机自启(HKCU,普通用户)·
      单实例 · 连接 Profile(配对一次永久记住)· 优雅退出善后 · 协议版本协商(不匹配明确拒绝)
      —— 协议协商 2026-08-03 补齐:客户端自报 `X-LocalAI-Protocol`,对不上单列 `ProtocolMismatch` 并说清该更新哪端;
      **网关侧回这个头的那半行归网关线**(客户端如实显示「中枢未声明协议版本 · 未协商」,不假装协商过)
- [→P7] ~~**★ i18n 中日英**(D42.1)~~ —— **2026-08-03 用户裁定移到 P7**(见 D77)。
      骨架(运行时切换 · 缺键刺眼 · Audit · 101 键)已在 P3c 完成并保留;
      **铺满正文(2275 条硬编码)整条移走** —— 界面还在长,现在铺等于铺一遍再铺一遍
- [x] 聊天界面(壳与项目体系完成;「取代 Open WebUI 的日常用途」要等 P4 接 AI 才谈得上回答)
- [x] **记忆面板 UI** —— 浏览 · **编辑** · 删除 · **溯源展开**(2026-08-03 补齐后两项;
      编辑只改标题与正文并打人工修改标记,范围/来源不许在编辑框里悄悄改)。
      ★ **接 P3a S5 后端改挂 P4 之后**:客户端侧是本机结构,后端连线需要记忆服务化(见 D77)
- [x] **翻译工作室**(D42.2)—— 形态已按 07-30 用户裁定改写为「语言池 + 四档程度」,
      并在 P3c 期扩成四个场景:文字翻译 · 同声传译(界面骨架,语音链路未接)· 文件翻译(含 PDF 预览)· 多语言表
- [x] 配对流程 UI(六词 SAS 带外比对 · 三类失败分因 · 界面不代人比对、不提供跳过)
- [~] 主机自识别:现为「拨号地址指向本机」的启发式且只做状态显示;D36 原文的「只配主机名 + 解析到回环即主机」未做
- [x] **Open WebUI 退役**并从 start-stack 移除(2026-08-03)
- [x] **可分发产物** `90-ops/build-client.ps1`:exe + SHA256 + 版本戳 + 一页说明,**自检不过不出包**。
      ★ 真正的安装器(卸载项/自动升级/签名)归 **P7/P11** —— 没有升级策略之前先造安装器 = 先造一个升级问题

**验收** —— ✅ **2026-08-04 通过**(两台真 PC;签字 = **D80**,包见 `decision-packets/p3c-signoff-2026-08-04.md`):
- [x] 同一个安装包在主机与副机上都能装 —— **实机**:`HONGKONGPINGPON` 与 `SENIORBIRDS` 双双 active,
      副机装的就是 `client-pack` 的包。★ **注意 08-03 标的 S4「✅」当时并不完整** ——
      图形界面那条配对路径**从没走完过**(P3b 走控制台绕开了 enroll 之后那段),`b136f01` 修掉后才第一次通;
- [x] 副机未获批时**看得到但进不去** —— 用户实机确认;自动化侧另有 401 三处覆盖;
- [~] 换网段后能改连接地址且证书/配对不失效 —— **结构性成立**(`SetDial` 只改 `Dial` 字段,
      证书/CA/私钥一字未动;入口有断言)· **实机换网段由用户裁定免验**,
      ★ 这是 P3c 验收里**唯一一条没有实机背书**的,真换网段出问题先看它;

原文留档:同一个安装包在主机与副机上都能装;副机未获批时**看得到但进不去**;
~~换路由器后自动重新发现且**无需重新配对**~~ → **2026-08-03 修订(D77)**:换网段后**能改连接地址且证书/配对不失效**
(「自动发现」与 D43「DNS-SD 推迟 P3b.2」直接打架,本阶段结构上不可能达成;自动发现补上时填的是同一个字段);
~~界面可切中/日/英三语~~ → **随 i18n 整条移到 P7**。

### P3d · 外联通道(Signal)—— 独立于 P4/P5/P6

- [ ] **★★ 硬前置:响应侧出境闸**(D39)。在它落地前,桥的最高允许形态是"不读 upstream
      的 content 字段、只回固定模板",且由代码保证。
      —— 非流式与流式**两条分支都要过闸**;`unseal_for_prompt` 增加必填 `sink` 参数
- [ ] 第四个隔离 Windows 账户 + `channel-relay` 档位(不在 `E1_OVERRIDE_ALLOWED_TIERS` 内)
- [ ] 桥:**独立进程 + IPC**,映射到 PG 的 `ai_mem_remote`(D38)
- [ ] signal-cli 接入(主机独立号码)+ **发件人白名单(代码,fail-closed)**
- [ ] 出站收件人**写死常量** + 架构测试(grep send 调用点断言 to 只来自常量)
- [ ] 对端自由文本字段一律不进 prompt,只进审计
- [ ] 语音消息:ASR 接入,provenance 由**通道**决定(不得为 `user_voice_asr`)
- [ ] 幂等键 + 接入 `mem.idempotent_ledger`(重投不得重复写入)
- [ ] 通道契约(`ChannelCapabilities`)+ Discord/WhatsApp/Telegram/微信/LINE 的**契约位**
      —— 只定契约,不写空实现

**验收**:在外面用 Signal 问「上次聊的那个灯光问题」能答;
陌生号码的消息**在进 LLM 之前就被丢弃**并计数告警;S1/S2 内容**结构上出不去**。

### P4 · GPU Broker + 组件选择器 + 多客户端仲裁

- [ ] 资源仲裁器(NVML · **租约** · 准入白名单 + 静态闸 + 动态闸 · `desktop_floor`)
- [ ] **★ 多客户端争用四件套**(D37):单一权威+副本 · 推送非轮询 · **世代号** · **预留先于装载**
- [ ] 推送流须包含**非 AI 的显存变化**(游戏/桌面占用)
- [ ] 拒绝信息含**占用者**(谁装的、何时、是否在用)
- [ ] **租约一套三处用**(宠物在场 · 模型引用 · 客户端会话);心跳条件写 + `clock_timestamp()`
      ★ **D65 追加第四处:agent 任务会话** —— 一次任务 = N 次模型调用,与桌宠/同传抢同一块 8.52
      (日常 7.99,只剩 0.53)。规则:**申请失败排队或降级,不抢占**;长任务心跳续租;
      一键中止复用 P6 全局急停。**⇒ P4 因此成为 Agent Worker 的硬前置(没有 Broker 就没有 Agent)**
- [ ] **★ 权限档位六元组**(从 P2 移来)—— 谁能改 GPU 状态是它的第一个真实客户
- [ ] **`gpu.apply@workstation` 永不进入任何 Agent 工具池**(P4 原条款,一字不改)
- [ ] **★ 硬件变更的自动重标定**(B10)—— GPU 指纹 + 显示指纹双组比对;
      不一致则**拒绝启动并说清要重测什么**(与 S2 编码指纹 fail-closed 同一手法);
      **已测的 P1 数值保留为回归基线与外推依据**,使重标定只需重测 A1 + 少量抽检;
      降级时报「GPU 路线不成立」而非静默拒绝一切
- [ ] **★ 重标定的一键流程**(B10⑥,在主机管理端内)——
      A1 与各组件 peak **可全自动**;`desktop_floor` **只能引导式测量**(负载须由人制造,
      且必须持续采样取上界);期间**取独占租约**并在各客户端显示「正在重标定」;
      结果**先与旧值对拍**再走哈希绑定批准,**事务性写入**(不得留下半套数字)
      ★ 产品化前置:每个客户硬件不同,人人都要重标定
- [ ] 组件选择器界面(推荐组合 + 三段预览 + 确定即事务)
- [ ] **★ 界面意图驱动的组件切换**(D42.6)—— 客户端界面切换发"期望组件集"意图给 Broker;
      已加载/能共存→瞬时;能装→自动装+提示;**需卸别人正用的→不静默抢,弹选择**
      (现在切/等它跑完/用当前将就)。两机争用=前台跟随+冲突可见(§12.3)。
      手动勾选仍在,**自动跟随是默认、手动是覆盖**(承 D25)
- [ ] 不变式 I2/I3/I4 + `RECONCILE_WATCH`(只报告不修复)
- [ ] 失败落点规定 · 集合变更事务语义 + DEGRADED_SAFE · 关闭 CUDA sysmem fallback

### P5 · 语音 v1(半双工,常驻)

- [ ] ASR + TTS 部署(依 P1 结论:faster-whisper + Piper)
- [ ] 按住说话 · 语音直通记忆写入(**仅本机/已认证 LAN 设备的麦克风**可用 `user_voice_asr`)
- [ ] 客户端内的语音前端(壳已在 P3c)

### P6 · 电脑操控 + 权限区域

> **★ 自主度两模式(D42.4):引导 / 直接。** 与 §6.3.1 的【发起×目标】二维正交,是第三维。
> 引导模式 AI 只移鼠标到目标 + 告诉用户点哪(用户亲手点)→ **从不提交不可逆动作** →
> 风险面小一个量级,先做。直接模式 AI 亲自点击 → 必须配**全局急停**(§12.3:一键中止、
> 不经确认、AI 不可拦截)+ 权限区域 + 逐次确认,待这些都实装后再开。

> **★ Agent Worker(D65,2026-08-03 采纳)**:P6 的**调度层**= 外部 Agent Worker(当前实现 Hermes,MIT,可替换),
> 只做规划与 loop;治理全留 LocalAI。
> ★ **它不是模型、不产 token、不占 VRAM** —— 拆步骤、记进度、失败重试是它的活;
> 每一步要「想」时它回头问网关,产 token 的**始终**是我方本地模型(别名由 capability alias 定,D29)。
> 别把它读成「本地模型的替代」——**它是本地模型的使唤者**,只会让本地模型被调用得更频繁。接法 = 注册成网关别名 `agent.default` 的后端,
> 其上游模型回指网关(**两次穿闸** ⇒ E1/E4/审计/档位自动覆盖)。
> 规格见 [HERMES_INTEGRATION_DESIGN.md](HERMES_INTEGRATION_DESIGN.md)(DD-1…DD-10 · 分期 H0–H4)。
> **H0 已完成(设计 + D65 + 本挂点),H1 起须先满足 P4 租约 + 隔离路线裁定。**

- [ ] MCP 工具池(stdio / HTTP 分离)
- [ ] **★ MCP 服务端**(D65/DD-2)—— 会话级端点 + 一次性 token,把 §6.2「按会话挂载工具池」
      投喂给 worker;**worker 不得拥有自己的工具注册表**。工具命名照 §6.3.1(目标是身份不是参数)
- [ ] **★ 权限档位 `agent-worker`**(D65/DD-4)—— 记忆读**非 S2 且不含 L4** · 记忆写**一律 pending** ·
      `secret.request` 不在其工具池 · `egress=false`。**新档位在所有 allowlist 里必须显式落拒绝侧**
      (P3a 第 10 条缺陷的第一个真实回归场景)
- [ ] **★ worker 调用方身份走 mTLS**(D65/DD-3)—— D30 的 端口→PID→WMI GetOwner 不跨隔离环境;
      复用 P3b 的 CA 签 `worker` 类证书
- [ ] **★ trace 翻译层**(D65/DD-8,**非可选**)—— loop 内部步骤翻译进我方审计模型。
      验收口径:一次 agent 任务后**能从审计重建完整动作链,无"中间不知道"的空档**
- [ ] **★ provenance `agent_worker` 结构性 forced pending**(D65/DD-5)—— 触发器强制,非应用层判断;
      **必须留经审批转正的出口**(勿复制 S3 `pr_derived_forced_pending` 的死锁形状)
- [ ] **★ 主机管理端 · worker 生命周期板块**(D65/DD-13)—— 形状复用 VB-CABLE 那套(检测/安装/更新/卸载 + 安装前签名校验),
      放管理面(D48 仅回环)。三处差异必须照顾:① 版本检测语义 = **"是否 = 我方钉定 commit"**,
      不匹配则**拒绝启动**(同 S2 编码指纹 / B10 GPU 指纹的 fail-closed);上游有新版**不提示升级**(DD-10);
      ② **提权分两段**(D46):启用 WSL/Hyper-V 要管理员 ⇒ 引导用户**亲手双击签名 `.cmd`**,其后全部用户态;
      ③ **卸载自证卸干净**(容器删 · scratch 清 · **证书吊销** · 端点停 · 别名条目移除 + 卸载后自检),
      **不动**已批准 L4 技能与审计记录,残留物**进隔离区留证不直接删**。
      集成度 = **把 H1 最小环复用成安装后自检**,绿了才允许开关升到档 1
- [ ] **★ worker 是可选依赖**(D65/DD-14)—— 网关**不得**在导入期/启动期依赖它;
      ★ 承 07-30 `e4_egress` 事故形状(import 一个没入库的模块 → 整个网关一天起不来)。
      **加一条专测断言「worker 不存在时全栈自检仍全绿」并进回归口径**
- [ ] **★ 与用户自有 Hermes 共存**(D65/DD-15)—— **不接管、不复用、不改动**;命名与数据目录完全分离
      (我方那个不叫 Hermes);**身份判据是 mTLS 证书,不看端口不看进程名**(用户那份没证书,连不上 Gateway);
      非主机客户端**不显示安装按钮**;卸载**以我方安装台账为准,绝不按名字匹配删**;UI 措辞不出现 "Hermes"
- [ ] **★ Agent 三档开关**(D65/DD-11)—— 0 关闭(默认,进程不启动)/ 1 只读 / 2 含副作用,粒度对齐 H1–H3。
      **★ 开关自身不得进入任何 Agent 工具池**(照 `gpu.apply` 与"档位切换工具"先例,§12 行 1457):
      worker 不能自开、不能自升档。fail-closed:不可达/租约失败**回落普通对话并如实说明,不假装在干活**;
      运行中关闭 = 全局急停语义(§12.3,AI 不可拦截)
- [ ] **★ worker 进程级隔离**(D65/DD-7)—— ★ Windows 防火墙不过滤回环、ACL 管不了 TCP ⇒
      账户隔离挡不住直连 18081。隔离路线 A(容器)/ B(后端口鉴权)**二选一待裁**
- [ ] 权限策略引擎(六元组**含参数级**;基础档位已在 P4)
- [ ] §6.7 权限区域实装 + N1–N14 否定用例
- [ ] **★ 引导模式**(advisory):只移动鼠标到目标 + 文字指示,AI 不点击 —— 先交付这一档
- [ ] **★ 直接模式**(autonomous)+ **全局急停热键**:直接操控,人工随时一键切断(D42.4)
- [ ] **★ 跨机执行器**(D65/DD-12,**原清单遗漏**)—— worker 只在主机一个(调度集中),
      但 ActionManifest 要落到目标机:照 §6.3.1,`lan-pc-2` 的会话只挂 `execute@self`;
      又照 D52 数据默认只在本机 ⇒ **主机看不见 `lan-pc-2` 的文件**。
      每台全功能端需一个 `ai-exec` 对端,**隔离区与回滚也在目标机本地**
- [ ] **★ 多成员会话隔离**(D65/DD-12)—— Hermes 官方定位单租户;若内部状态跨会话共享 =
      **D45 可见范围事故**。默认走「**每会话一个短命 worker 实例**」(结构上不共享),
      不采用"证明它内部隔离"那条(那是 S5 缺陷 11 的形状)
- [ ] 文件整理:清单 → 哈希绑定 → 隔离区 → 回滚
- [ ] 浏览器执行器(独立 profile)· 游戏日常
- [ ] 带外凭证输入控件 + `secret.request`(`provider=manual`)
- [ ] 凭证 E2–E5 强制点

### P7 · 客户端 v2(补齐)

- [ ] 通知与**告警通道**(§9.3 —— P2 验收标准③的缺口在此闭合)
- [ ] **★ i18n 中日英铺满正文**(D42.1;**2026-08-03 由 P3c 移入,见 D77**)——
      骨架(运行时切换 · 缺键刺眼 · Audit · 101 键)已在 P3c 完成;本期铺 **2275 条硬编码正文**,
      并**接回 P3c 让出的那句验收**「界面可切中/日/英三语」。
      日语【内容】进记忆需补检索词表(Backlog B11)随此条一起走
- [ ] 审批界面(L4 哈希绑定 · pending_review 逐条确认)
- [ ] 组件选择器 UI 完整版 · 只读设备的「正在跑什么」视图
- [ ] `external` provider 凭据存 Credential Manager
      (**这是 `external` 接入的前置** —— 不写死这条,某个"先跑通"的实现会把它写进 `.env`,
       而 `**/.env` 恰在 §6.7.4 硬禁止清单里,连事后审计都看不见)

### P8 · 常驻助手 Vigil + 桌面宠物(它的身体)

**前置**:P6 权限区域必须先实装(§17 原有)+ **A7-pet 实测完成**。

> ★ **2026-08-03 · D76:本节的一部分已在 P3c 期间提前做掉。** 阶段归属**不变**(桌宠一直在 P8),
> 提前做的只是**不依赖 P6、不依赖 Vigil 本体、不依赖任何一张帧**的运行时内核。
> 理由:**A7-pet 是本节第一个 checkbox,而它需要一个能跑的渲染实现才测得了** —— 先做内核是在解硬前置。
> 拆段:**v1a 家(零平台风险)→ v1b 桌面(A7-pet 挡着)→ v1c 串门(P4 租约挡着)**。

- [x] **渲染选型** ✅ **纯像素逐帧 6 fps(D76)** —— 作废「骨骼/网格 + 可替换贴图槽」方向。
      素材解析器攻击面随之归零(只剩 PNG + 自家 JSON);Live2D/Expandable 的贩卖阻断线消失
- [x] **三层拆分的前两层** ✅ 表演层 + 反应层已实作(`Services/Pet/`,46 条断言)。
      决策层(复用已加载助手)只留了**意图把手**,本体待 Vigil
- [x] **两层状态机 + 意图通道** ✅ 动画状态机(姿态唯一权威 · BFS 寻路 · must_finish/can_exit ·
      朝向只经 turn_180)+ 助手把手(枚举 allowlist · 三态返回 · 速率限制 · 审计 ·
      **结构上不存在 `Speak`** · **助手不能开门只能让猫去挠门**)
- [ ] **A7-pet**:桌宠渲染的稳态显存与空闲 GPU 占用实测(性质等同 A7)
      → 定 `desktop_pet_reserve`,并按 §12.4 哈希绑定批准修改 `desktop_floor`。
      ★ 实测对象已定为「逐帧位图 + WPF 分层透明窗口 + DWM 合成」——
      **那笔窗口/合成的钱两种渲染方案都要付,真正的变量只是 atlas**
- [ ] **帧资产**(真瓶颈,美术活不是代码活):v1a **84** 身体帧 + 12 加载环;完整集 168 + 12。
      A–F 六批,**A 批 13 张身份锁定帧尚未产出**。规范 §10 的跨帧一致性验收排除扩散模型逐帧生成
- [ ] **v1a 家**:主页横条(空白背景,以下方板块上沿为地板)+ 两扇门
      (右=本机桌面可开关 / 左=其他 PC 常开)+ 行为逻辑树(Place × Mode + arousal + 冷却)
- [ ] **v1b 桌面**:分层透明窗口 · 逐像素命中 · 任务栏当地面 · 多显示器 · DPI
- [ ] 在场租约(复用 P4 的同一套机制)+ 迁移为**显式动作**(不做"跟随前台")
      + 指数退避 + 最小驻留时间。★ 唯一实体的权威落 hub;**hub 离线 → 走廊断,猫只能待在本机**
- [ ] 休眠/唤醒:`PBT_APMSUSPEND` 分支先隐藏并释放租约(DWM 会用挂起前的表面直接合成)。
      ★ **恢复必须先问权威猫在谁手里** —— 无条件走 `wake_from_hidden` 会让本机凭空生出第二只猫
- [ ] **纯输出面**:不采集屏幕 / 窗口标题 / 输入焦点。
      ★ 待裁:系统空闲秒数(`GetLastInputInfo`,不含"你在做什么")能否用于"猫去有人的那台机器"
      —— 它是这道斜坡的第一级台阶,**默认落禁止**
- [ ] 独占全屏时整体挂起(→ 自然产生"它挪去别处"的行为)。
      ★ **门的关闭只约束出门,不约束回家** —— 回家是消防通道,否则挂起纪律与门的语义会打架
- [ ] Vigil 本体:四层漏斗 · 节奏与配额 · 交接单 · 独立开关(§17)
      —— **在场不算打扰,开口才算**,宠物受同一份配额管

**v1 不做手机端宠物。** **v1 也不开口** —— 哑巴猫是结构性的(意图枚举里没有 `Speak`),
因此 D40「屏幕本身是出境面」那条未写完的条款**不在关键路径上**,可与 P3d 方向 B 出境闸一起补。

### P9 · 资产工作室(持续迭代)

Asset Spec · 提示词转译器 · 模型管家 · ComfyUI 挂接 · nano-banana + 出境闸门 · IP-Adapter · 小 VLM 产物 QA。
**v1 冻结点**:端到端产出 10 个可用于 UE5 的资产,全程不写硬核 prompt。

- [ ] **★ PPT / 课程生成工作室**(D42.3,P9 相邻)—— 大纲 → 幻灯/课件结构(Markdown)→
      渲染成**自包含 HTML 幻灯组**(reveal/impress 一类;★ **不用 Office/`.pptx`**,用户没有也不想要)。
      配图复用本工作室(ComfyUI/nano-banana)内联 data: URI,命中 §5.6 出境闸门。不碰记忆核心。
- [ ] **课件预设**(D42.3)—— 界面内加载**预设课程文件**作模板;CRUD 入口(新建/复制/删除/编辑,
      删除可逆)。预设是用户手管的文件、**不进记忆库**;**预设内容与格式待设计(B14)**
- [ ] **落盘依赖 P6**:AI 直接把 `.html` 写进课程文件夹时走 ai-exec + §6.7 文件区 + 哈希绑定;
      过渡期默认"AI 生成 + 用户另存"(不碰 P6)

### P10 · 持续完善

记忆 v2 · 季度评估 · GC 校准 · 对权限引擎与回收器安排外部评审。

### P11 · 产品化(条件触发,非必经)

仅当决定真正贩卖时启动。届时 D41 各条从"记账"变为"逐条裁决"。

- [ ] 安装器:账户隔离 / SSPI / ACL 在别人机器上自动落地
- [ ] 迁移自动化(数据可搬、身份不可搬 —— 后者必须走重新签发与配对)
- [ ] 通道契约的其余实现(按各平台商业条款筛选可交付集合)
- [ ] 许可证合规(signal-cli GPLv3 传染边界 · **模型权重的商用许可**)
- [ ] 法规:GDPR 角色 · 第 25 条 by default(D21/D22 的默认值须重估)· EU AI Act ·
      产品责任指令 2024/2853 · **Cyber Resilience Act**
- [ ] **找执业律师** —— 本项目任何文档都不构成法律意见


---

## 15. 风险清册

| 风险 | 等级 | 缓解 |
|---|---|---|
| **记忆系统做不好则整个定位落空** | **高** | 双轨 · 可解释性 · eval 集使验收可判定 |
| 记忆数据**经软件路径泄露** | **高** | 三层强制 · 账户隔离 · L3b 永久禁止 · 不入日志 |
| **记忆数据经物理途径泄露**(硬盘或移动盘被取走) | **中** ⚠ 仅就**凭证值**而言降级 | ⚠ 无技术缓解,仅靠物理安全。**但 D23 已把凭证移出记忆库** —— 能读到称谓/偏好/习惯/项目状态,**读不到凭证**,故由高降为中 |
| **凭证在对话框里被直接打出** | **高** | **§6.9.4 的 E1 入口检测器(P2 上线)**。在 E1 之前只能靠约定。E1 之后:能拦住意外,**拦不住坚持** |
| **记忆数据丢失** | **高** | §8.5 手动备份 + 送达度提醒 · P3a 前恢复演练 |
| **备份「成功」但恢复不出来** | **高** | §8.5.5:`pg_dump` + Qdrant snapshot,不对活库做文件级复制 |
| ~~BitLocker 恢复密钥丢失~~ | ~~高~~ | **✅ 已因 D22 消除** —— 没有密钥就没有丢密钥的风险 |
| 提示词注入 | **高** | 模型视为不可信计划者;Vigil 隔离设计;打扰评分来源封顶 |
| **写侧投毒**(伪造 user_explicit) | **高** | §4.4.2 服务端可验证信号;pending 禁止批量确认 ← v2.2 新增 |
| **L4 是可执行面** | **高** | §4.4.3 独立写路径 + 签名 + 执行前哈希复核 ← v2.2 新增 |
| **丢手机** | 中 | S2 结构性不可见 · 无本地缓存 · WebAuthn · 双层吊销 ← v2.2 新增 |
| 交互打磨无止境 | 中 | P9 设 v1 冻结点 |
| nano-banana 数据出境 | 中 | 两级授权 · 分轨 · 付费层 |
| 16GB 显存 | 中 | 组件选择器 + 三层闸;P1-A7 定 `desktop_floor` |
| **★ 预览与现实不一致**(桌面波动 / peak 是估算 / 可加性未验证) | **中** | 三段预览显示「还能让桌面再涨多少」· 确定时重新求值 · **P1-A2 验证可加性,不成立则预览实现推翻重来** |
| 单人项目无第二双眼睛 | 中 | P10 外部评审 |
| 长期中断丢上下文 | 中 | §12 执行协议 |
| 第三方 API 条款变动 | 中 | 条款快照 + 定期复核 |
| **工程量超出单人业余投入** | 中 | P3 拆分;打扰评分砍到两档;GPU 短租不做 ← v2.2 新增 |
| 游戏自动化违反 ToS | 中 | 已知悉,风险自担 |
| 云端 API 成本 | 低 | 硬上限 + 本地兜底 |
| 忘记 Backlog | 低 | 每次版本更新复核 |

---

## 16. 决议记录

### 随 v2.1 及之前

| # | 事项 | 决定 |
|---|---|---|
| D1 | 定位 | 私人助理为核心,记忆为第一优先级子系统 |
| ~~D2~~ | ~~记忆库加密卷~~ | **已被 D22 推翻(2026-07-26)**。记忆库改为普通文件夹 `D:\AI\state\memory\` |
| D3 | nano-banana 接入 | Gemini API(非 Vertex),**必须付费层** |
| D4 | 进度主页 | Artifact 控制台。**磁盘 markdown 是执行期事实来源** |
| D5 | 资产减重 | 本地 Flux FP8 与生成式 3D 移出本期 |
| D6 | 投资 | 只预留接口与表结构 |
| D7 | 后端 | llama.cpp 而非 vLLM |
| Q1 | WSL2 + Docker | 暂不装 |
| Q2 | `escalate.cloud` | 启用,**粒度锁 L2** |
| Q3 | nano-banana 预算 | 月度上限可手动调整;80% 告警,100% 硬停 |
| Q4 | 主板 M.2 | 已确认还有 2 个空插槽 |
| Q5 | 新盘采购 | 不由本项目跟踪 |

### v2.2 新增(2026-07-26)

| # | 事项 | 决定 | 推翻条件 |
|---|---|---|---|
| **D8** | **显示输出** | **保持接独显**,不改接核显(**决定不变,已由 D24 强化**) | ~~若 P1-A7 实测桌面重负载 > 3.5 GiB,或 `speech.lite` 的 ASR 准确率不达标,应重新评估接核显~~ → **两个触发器均已由 D24 作废(2026-07-26)**。① A7 超线 → **换常驻组成**(降 cap 是伪动作,见 D24)· ② 准确率不达标 → `speech.cpu` + 异步写入,准确率并入 C4。**新推翻条件见 D24** |
| ~~**D9**~~ | ~~记忆卷解锁方式~~ | **已因 D22 作废**。没有加密卷,就没有解锁问题。C: 是否加密与本项目无关 | — |
| **D21** | 备份介质加密 | **不加密**。`paths.toml` 的 `[backup].require_encryption = false` | 移动盘曾丢失或曾离开你的控制 |
| **D22** | **记忆库静态加密** | **全部取消**。记忆库为普通文件夹 `D:\AI\state\memory\`,磁盘与备份盘上均为**明文**。§4.6「静态加密」整条删除 | 家中出现其他人日常使用该工作站 · 或工作站需要离开家(带去别处) · 或记忆库里开始出现你不希望任何拿到硬盘的人看到的内容 |
| **D23** | **凭证与敏感信息** | **凭证类(IBAN·税号·证件号·密码·密钥)不进记忆库**,存独立加密应用,每次授权取用。记忆库只存 `secret_ref` 句柄。**手动输入永久保留**,且必须走带外通道(不经 LLM 上下文) | 若「每次授权」实际太烦导致你绕过它 → 重评授权粒度,**但不得改为会话级**(那等于取消授权) |
| **D10** | ~~五档显存开关~~ | **已由 D25 取代(2026-07-26)** —— 改为组件选择器。⚠ 但「**不做自动触发**」这一条**完整存活**,主语从「档位」扩到「驻留集合」 | ~~若 P1-A7 显示 12.0 仍过订,降到 11.0 或重启 D8~~ → **两个动作 D24 已分别判为伪动作与已放弃选项**。⚠ 这一格是 D24 列出的四处必改中**唯一漏掉的一处**,由 D25 补上 —— 留痕不静默修 |
| **D24** | ★ **彻底放弃核显** | 显示拓扑**永久接独显**,连 P1 对照测量也取消。D8 的**决定被强化**,作废的是它的**推翻条件** | **不由「显存紧张」触发**。① P1 期间冻结 ② A7 的 P99 > 约 7.5 GiB(收益侧反转)③ 显示拓扑因其他原因本就要重排(成本侧消失) |
| **D26** | 解冻 A5/A8/B1 三格「不达标时」 | 它们各自指向一个已废止的对象(`cap` / `daily` 三模式命名),照字面无从执行 | 若 P1 期间发现还有第四格漏网 → 说明「按清单解冻」这个做法本身不可靠,改为全表逐项过一遍 |
| **D25** | ★ **组件选择器** | 取消档位,主机面板直接勾选组件 + 实时预览 + 确定生效;侧栏推荐组合;**只有主机可配置**;`desktop_floor` 作为跨会话持久上限,超过则无法确定 | ① 「勾完才发现桌面被挤」月度 > 2 次 → 重评上限是否该做成不可随手调的硬值 ② **P1-A2 可加性不成立** → 预览实现方式需重做,整条重议 ③ 换 ≥24 GB 显卡 → 自动失去意义而非被推翻 |
| **D11** | 推翻 §4.7 克制的主动性 | 改为打扰评分 + 硬性配额 + 反馈校准,**v1 只开 L1 日报与 L4 白名单** | 若 6 个月内日报的「该早点说」标记数为 0,说明这套机制没有价值,应整体移除而非调参 |
| **D12** | 常驻助手 Vigil | **CPU 进程,0 显存,只读授权区域,不写 L0–L4** | 若 P1-E5 实测 CPU 占用不可接受,或它持有了任何 GPU 租约 |
| **D13** | 多设备记忆拓扑 | **单一权威 + API**。**工作站关机 = 全失效,已接受** | 若实际使用中因工作站休眠而频繁受阻,进 P3.5 评估 Wake-on-LAN |
| **D14** | 设备身份 | **统一 WebAuthn**,不存长期 bearer 秘密 | — |
| **D14a** | **命名(RP ID 的构成)** | **机器名沿用当前 `HongKongPingPong`**(MagicDNS 小写化为 `hongkongpingpong`)。**RP ID 取 `<tailnet>.ts.net`,不带机器名** —— 将来加第二台 PC 时 credential 可复用。tailnet 名于注册 Tailscale 时确定,**确定后绝不更改** | **不可推翻。** 改 tailnet 名 = 改 RP ID = 所有已注册设备的 credential 全部作废 |
| **D15** | 手机本地队列 | **不做**。无网即明确失败 | **`mobile-remote` 写入送达率连续 4 周 < 99%** |
| **D16** | iOS passkey 走 iCloud Keychain | **接受**。第一道门是 Tailnet Lock,passkey 是第二道 | 若 Apple ID 与 Tailscale 账户的双因素强度不足 |
| **D17** | 备份触发方式 | **只做手动全量到移动固态 G:**(不加密,D21)。**不做每日自动到 C:** —— 用户 2026-07-26 明确决定。保留「送达度提醒」(6/14/30 天三级) | **实际备份间隔中位数连续 4 周 > 14 天**,或发生过一次「想恢复却发现备份太旧」→ 重新评估自动备份(届时必须公钥加密) |
| **D18** | P3 拆分 | **P3a 单机 / P3b 多设备**,P3b 前置为 P3a 验收 | — |
| **D19** | `cache/hf` GC 例外 | 排除出自动清理 | 若增长失控而磁盘吃紧,改为按 `last_accessed` 分级清理 |
| **D20** | 关闭 NVIDIA App / ShadowPlay | 已关闭;驱动核心服务保留 | 若要用 ShadowPlay 录游戏 |

### 待决事项

**（无）**

---

## 17. ★ 常驻清醒助手 Vigil

### 17.1 它是什么

| 维度 | 定义 |
|---|---|
| Agent id | `vigil` |
| 能力别名 | `assistant.resident`(唯一) |
| 权限档位 | `resident-observer`(最低) |
| 运行时 | **CPU 独立进程**,llama.cpp,IDLE 优先级 |
| 模型 | Qwen3-1.7B Q4_K_M(备选 Qwen2.5-1.5B)· **不得选 4B 级** |
| 上下文 | **2048,硬上限**;前缀缓存是硬性要求 |
| **GPU 占用** | **0 MiB。硬断言** |
| 内存 RSS | ≈ 1.87 GB(估算待实测) |
| 工具池 | **只读三件套**:`fs.stat` / `fs.read_head`(≤4 KB,白名单内)/ `embed.query`。**无写、无删、无网、无进程、无剪贴板** |
| 记忆读 | **只有** `mem.exists`(布尔)与 `vigil_context` 投影(用户逐条勾选,≤50 条) |
| 记忆写 | **只能 INSERT 到 `vigil_observations`**。不能写 L0–L4 任何一层 |
| 出境 | **L0,永久。配置层不接受其他取值,代码里不存在通往云的路径** |
| 会话 | 单次脉冲 = 单个会话,结束即销毁 |

### 17.2 它不是什么

- **不是屏幕监控。** 它**读文件,不看屏幕**。任何未来版本引入屏幕采集必须另开一章、另走审批
- **不是记忆写入者。** `vigil_observations` 不在 L0–L4 之内。进入 L2/L3 必经:你点开交接单 → 主助手接手 → **主助手重读证据** → Memory Gate → 写入并记来源
- **不是打分者。** 模型只填受约束的槽,总分由它读不到的确定性公式合成
- **不占显存。** 组件表里它是恒为 **0.0** 的一行,且**不出现在选择器界面上**(把 0.0 的东西放进显存界面只会让人以为它有代价)
- **不持 GPU 租约**,因此天然不在「运行中任务」集合内 —— **不需要任何特例代码**

### 17.3 「清醒」的可验收定义

| 层 | 是什么 | 恒真? |
|---|---|---|
| W0 进程存活 | 托盘进程、事件循环、DB 连接 | ✅(除完全 Off) |
| W1 事件捕获 | 文件变化进持久队列,一条不漏 | ✅(除完全 Off) |
| W2 模型驻留 | 权重已加载 | 🟡 按开关态 |
| W3 持续算力 | 真的在跑推理 | ❌ 只有脉冲 |

> **清醒 = W0 + W1 恒真。清醒的本质是「不漏事」,不是「一直在想事」。**
> **文档必须写「除完全 Off 外,无时无刻清醒」** —— 不留一句字面上不成立的承诺。

| # | 判据 | 阈值 |
|---|---|---|
| A-1 | 原始事件落进持久队列 | **< 1 s** |
| A-1b | change-set 成型(15 s 去抖 + 60 s 合并) | **≤ 90 s** |
| A-2 | 关机/休眠期间的变更,开机后由 T0 补扫产出 | **≤ 120 s** |
| A-3 | 状态可查(今日脉冲/CPU 用时/待处理/上次补扫) | 恒可查 |
| A-4 | watcher 缓冲溢出必须告警 + 立即补扫,**不静默丢事件** | 恒成立 |

### 17.4 四层漏斗

> **功耗低不是因为模型小,是因为绝大多数事件根本不进模型。**

```
文件系统事件
 ↓ 层0 授权闸(0 token):白名单∩¬黑名单 → 扩展名/大小/深度 → 去抖15s → 合并60s   过滤 ~70%
 ↓ 层1 特征提取(0 token):{root,rel} · size · mtime · sha256 · 首4KB · 结构化diff  过滤 ~20%
 ↓ 层2 语义预筛(CPU embedding,0 LLM token):bge-m3 复用,余弦 < 0.35 丢弃         过滤 ~7%
 ↓ 层3 LLM 脉冲(CPU 1.7B):受约束槽,≤160 token,GBNF 强制 JSON                  ≈3%
 ↓ 层4 打扰评分(§4.7,纯代码)+ 交接单
```

**必须自带的编译产物排除模式**(否则任何真实工程目录一加进白名单就打爆配额):

```
**/Intermediate/**  **/Saved/**  **/DerivedDataCache/**  **/Binaries/**
**/node_modules/**  **/.venv/**  **/__pycache__/**  **/target/**  **/dist/**
```

### 17.5 节奏与配额

| 触发 | 周期 | 调 LLM |
|---|---|---|
| T0 补扫 | 启动 / watcher 溢出 / 休眠恢复 | ❌ |
| E 事件脉冲 | 去抖 15 s,合并 60 s | 🟡 仅过层 2 |
| T1 轻扫描 | 每 10 分钟,纯 stat | ❌ |
| T2 小时汇总 | 每 60 分钟,**队列为空直接跳过** | 🟡 |
| T3 日终 | 每日 22:00 | ✅ |
| T4 周终 | 每周日 22:10 | ✅ |

**日预算(估算,待 P1-E5 实测)**:≈29 次脉冲 · ≈45K token · ≈6 分钟 wall clock · ≈**2088 核心秒** · < 8 Wh · **0 MiB 显存**。

**配额**:`cpu_core_seconds_per_day = 5000`(**必须高于日均 2088**,否则正常使用日就会 halt,几天后你会把配额调到无穷大或直接关掉);`pulses_per_hour = 12`;配额告警按同一原因 24h 内只报一次折叠。

### 17.6 项目状态投影(而非读原文)

Vigil 对 `{code}/00-docs` **只有元数据级权限**。项目状态经编排器每日 03:00 生成的结构化只读投影获取:

```
project_state_projection
  phase          : "P1"
  next_steps[]   : ["P1 基准测试", ...]     ← 纯字符串,≤3 条
  blockers[]     : ["活数据库备份方式待定"]
  active_topics[]: [{id, name}]
  generated_at   : ISO8601
```

**原始 markdown 永不进 Vigil 的 prompt。** 这同时消解**自指循环**(它读不到 DECISIONS.md 的「推翻条件」原文,那种句子可能被当作可执行指令)与**注入**(投影是纯字段,无自由文本)。

### 17.7 交接单(唯一输出形式)

Vigil 不直接对你说话,它产出**交接单**:

1. **交接会话不继承你当前的档位权限。** 强制挂载只读工具池(`fs.read` / `mem.search`),无写、无执行、无网。写权限需要你**看过证据之后**发起**第二次、独立的**动作
2. **`evidence` 原文默认只渲染到 UI,不进模型上下文。** 主助手确需读原文时,走一个**禁用全部工具调用**的只读子会话做结构化提取
3. **UI 上证据在上、Vigil 的说法在下**,并标注「不可信来源的转述」

### 17.8 独立开关

| 态 | W0 | W1 | W2 | 说明 |
|---|---|---|---|---|
| `on` | ✅ | ✅ | ✅ | 完整 |
| `observe_only` | ✅ | ✅ | ❌ | 只记事件,不跑 LLM,不产交接单 |
| `paused` | ✅ | ✅ | ❌ | 同上,但事件队列继续积累,恢复后补处理 |
| `off` | ❌ | ❌ | ❌ | 完全停止 |

**Vigil 开关与显存组件选择正交**(它不占显存,不在选择器里),但 **完全 Off** 强制 Vigil 为 `off`。

---

## 18. 术语表

| 术语 | 含义 |
|---|---|
| **五个逻辑根** | CODE / STATE / MODELS / ASSETS / CACHE |
| ~~档位 cap~~ | **已废止(D25)**。原定义「AI 进程组的分配上限,不含桌面」正是 D24 批评的那个陷阱来源 |
| **`desktop_floor`** | 你设的「至少给桌面留多少」。跨会话持久的服务端设置项 |
| **`vram_budget`** | 导出值:`15.92 − desktop_floor − 0.8`。**不单独设置** |
| **驻留集合** | `intended` / `committed` / `actual` / `permitted_on_demand` 四个,见 §8.1.7 |
| **peak / steady** | 峰值 / 稳态显存。**Broker 一律按 peak 发租约** |
| **component id** | §8.1.2 表中的组件标识。NUM-1 要求各处引用它而非复述数字 |
| **能力别名** | 客户端使用的稳定名称,与背后的真实模型解耦 |
| **租约** | Broker 按 NVML 实测发放的显存使用许可,有期限、可被驱逐 |
| **哈希绑定批准** | 批准的是**一个具体计划的哈希**;执行前若哈希已变则拒绝 |
| **三池** | draft(可回收)/ adopted(永久+备份)/ exported(仅元数据) |
| **六元组** | 用户 / 设备 / Agent / 工具 / **参数** / 额度 |
| **L0–L4** | 记忆五层:工作 / 会话摘要 / 情节 / 语义 / 程序 |
| **双轨检索** | 向量(相关的是什么)+ 结构化(确切是什么) |
| **Memory Gate** | 记忆写入前的把关 |
| **superseded** | 新旧矛盾时旧条被标记为已被取代,但**保留不删** |
| **provenance** | 候选记忆的来源类型,决定置信度上限(§4.4.2) |
| **S0/S1/S2** | 记忆敏感度分域。**S2 对远程档位结构性不可见** |
| **L2/L3a/L3b/L5** | 出境粒度。**L3b 与 L5 永久禁止** |
| **TaintedText** | 密封的记忆文本类型,不实现 `__str__`(§4.6.1) |
| **Asset Spec** | 结构化的资产描述,与目标模型无关 |
| **权限区域 / zone** | §6.7 定义的可读写路径白名单单元,分主体 |
| **隔离区** | `delete` 的替代品,继承来源区域的 mode |
| **Vigil** | 常驻清醒助手的代号(§17) |
| **交接单** | Vigil 的唯一输出形式(§17.7) |
| **claim_class** | 打扰提名的固定分类,决定严重性上限(§4.7.4) |
| **首轮门禁** | 新组件首次运行前的五条检查(§12.3) |
| **节点** | 一次坐下来做事的工作单元 |

---

## 附:版本关系

| 版本 | 状态 | 说明 |
|---|---|---|
| v0.4 | 归档 | 四协议因原文不可得,已由 §12 重新撰写取代 |
| v1.0 | 归档 | 安全章 / GC / 上下文预算已内联 |
| v2.0 | 归档 | 定位转向与范围决议保留 |
| v2.1 | **已被取代** | 全部有效内容已内联进本版 |
| **v2.2** | **现行** | 本文档 |

> **DOC-1:现行文档不引用已作废文档。** 归档件在 `00-docs/archive/`,仅作历史参考。
