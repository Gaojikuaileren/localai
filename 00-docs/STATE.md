# STATE — 本地 AI 中枢

> 更新时间: **2026-08-11 凌晨**(★ 十条车道的 D 号补落 **D118–D125** · D 号闸补上「该取号而没取」那一半 · 基线重跑)
> ★ 上一版此处停在「2026-08-06 夜」,而其间已并入 **D93–D125**(三十余条)——
>   **一个停了五天的"更新时间"比没有更坏:它让读的人以为自己看的是当前状态。**
> ★★ 上一版此处写「**P4 主体完成**」—— **当晚查出不成立并已更正**,理由见下方阶段头。
> 方案书版本: **v3.0**(v2.2 已归档 · 阶段重排见 D34-D41)
> 格式定义: v3.0 §12.1 记录协议
>
> ★ 2026-07-28 重写:此前本文件累积了三段互相矛盾的 P1 状态、已解决却仍列着的阻塞项、
>   以及两处断裂表格(P2 收尾审查发现)。本版只写**当前为真**的状态。

---

## 当前阶段:**P4 GPU Broker + 仲裁 · 🔴 暂停签字(2026-08-07 实机场次退回)· 清单:6 ✅ · 3 🟡 · 3 ❌ · 1 移出**

> ### ★★★ 2026-08-07 实机场次(两台真 PC · 包 `20260807-2340+6c3cbf0`)—— **P4 暂停签字**
>
> 上一版这里写着「🟢 收官 · 9 ✅ · 3 🟡 · 1 移出」。**那是按代码路径判出来的,
> 而实机把其中三格证伪了。状态如实退回,不保留比事实好看的写法。**
>
> **✅ 通过**:自动起栈(**没跑 `start-stack`**)· 自配对走完(本机客户端已配对/已连接)·
> 配对窗口关着 · 三段预览三段齐且含余量行「还能让桌面再涨 7,71 GiB」。
>
> **✅★★ 不变式实机成立** —— 关掉主机客户端 → 副机仍可共享 → 主机开回来消息还在。
> **这是 D106 钉的那条核心契约第一次在真机上被验证**(在此之前只有断言)。
>
> **❌ 三格**(详见 `PROJECT_PLAN §14 P4` 逐格):
> 意图即起没生效(敲字显存无变化、模型没开启)· 组件「确定」按不下去(**主机也不行**,
> 面板报「这台设备不能做这个操作」)· 任务抽屉**完全没有**。
>
> **⚠ 四条**:副机要**手动进设置滑到底**才开始连(不是启动即连)·
> 副机开机**拉不到**主机已有的共享会话 · 副机能看到模型选择面板(裁定:**不该看到**)·
> **无法测**:压力即让 / 任务暂停恢复 / 证书归因 ——
> **缺停栈入口**(`dist/host` 只有启动没有停止,而自动起栈是隐藏进程)与模型。
>
> #### ★ 根因:**一个原因,两条支路**(已在代码里逐条核过)
>
> > ### ★★★ 2026-08-08 留痕更正:**下面这段的第一句是错的**(V13 复核推翻,见 D112 裁定①)
> >
> > 下面写着「`run` → `run-lan` 把主机客户端推上了网卡 ⇒ …… ⇒ **从 `trusted-local` 掉了下来**」。
> > **`run` → `run-lan` 不是成因,而且主机客户端从来就不是 `trusted-local`。**
> > lan-edge 对**每一个**转发请求先剥掉客户端自带的 `X-LocalAI-*`、再注入它自己验过的证书指纹,
> > 而网关一见指纹即封顶 `lan-device` —— **与 lan-edge 绑在哪个地址无关**。
> > V12 之前 `run` 把业务口绑在**回环**,客户端拨 `127.0.0.1:8443` ——
> > **那仍然是 lan-edge、仍然带指纹、仍然是 `lan-device`。**
> > ⇒ **单靠 `change_resident` 那一处改动就足以让主机点不动确定。**
> >
> > ★★★ **为什么必须留痕而不是悄悄改掉**:照那句错的根因,下一个人会去
> > 「**把绑定改回回环**」—— 改回去之后主机**依旧**点不动,
> > 而 D107 刚修好的自配对链会**重新断掉**(`DiscoverEdgeDialsAsync` 逐张网卡找 8443,**明确跳过回环**)。
> > **那是一次看起来对症、实则两头落空的修改。**
> > 这正是 ASSERTION-PITFALLS 第 9 条:**给出的理由是假的,而人会照着假理由去改一个没坏的东西。**
> > ★ 这句错是第 0 条车道自己写的(2026-08-07 `991f36e`),由 V13 复核推翻。
> > ★ 下面那张**两条支路的表仍然成立** —— 错的只是"为什么会掉下去",不是"缺了哪两个动作"。
>
> ~~`run` → `run-lan` 把**主机客户端推上了网卡** ⇒ 请求带证书指纹 ⇒ `resolve_lan_principal`
> **封顶 `lan-device`** ⇒ **主机客户端从 `trusted-local` 掉了下来**。~~
> **实为:主机客户端的业务调用必经 lan-edge ⇒ 必带指纹 ⇒ 一直是 `lan-device`。**
> 而 `lan-device` 今天是 `{read, lease}`:
>
> | 表现 | 缺的动作 | 什么时候断的 |
> |---|---|---|
> | 「确定」按不下去 | `change_resident` | **今天**(D107 从该档拿掉) |
> | 意图即起没生效 | `permit_on_demand` | **08-06**(`dd06af3` 引入它时就没给过 `lan-device`) |
>
> ⇒ 面板上那句「只能在主机上改」**变成了一句没有设备能满足的话**。
> ★★★ **修法不是把 `change_resident` 塞回 `lan-device`**:那样意图即起照样不行
> (缺的是另一项),而且会**重新捅开** D107 刚堵上的洞。
> ~~**方向是让主机客户端不要掉到 `lan-device`** —— 具体怎么做**待裁**。~~
> **★ 2026-08-08 已裁并落地(D112)**:已判定为主机的那台,**业务调用直连回环网关**
> (明文 HTTP、不带任何 `X-LocalAI-*` 头,身份按 OS 账户解析)⇒ `trusted-local`。副机不变。
> ★ **不是**"让它不要掉下去" —— 它从来没在上面过;是**给它开一条本来就不存在的路**。
> ~~★ **意图即起那条其实 08-06 就断了**,一直被「主机客户端走回环所以是 `trusted-local`」
> 这个**巧合**遮着 —— 见 ASSERTION-PITFALLS 第 12 条。~~
> **★ 更正:没有那个"巧合"** —— 主机客户端从来就不是 `trusted-local`,
> 所以「意图即起」那条**从 `dd06af3`(08-06)引入的那一刻起就一直是断的**,
> 不是"被巧合遮着"。**遮住它的是没有人在真机上按过那个按钮**,而那才是第 12 条真正的形状。
>
> #### ★ 方法论(已进 ASSERTION-PITFALLS 第 12 条)
>
> > **4197 条断言全绿,而核心功能在真机上开不起来。**
> > 没有一条断言走「主机客户端的真实连接路径」——**测试里 tier 是直接构造的**。
> > 与第 3 条「跑的不是刚改的那个产物」同族:**判据没错,错在它测的不是最终会跑的那个东西。**
>
> #### ★ 用户 2026-08-07 当场的三条新裁定
>
> 1. **直接上管理端** —— 不等 P4 签字(P4 挂着);
> 2. **接上 `/v1/sync/snapshot`** —— 副机开机要能拉到主机已有的共享会话
>    (该端点 V6 已接进 `SyncClient.PullFullAsync`,但**开机那条路上没走它**);
> 3. **任务抽屉没有任务时也常驻** —— 今天是「有任务才出现」,
>    而 D99 的「在任务进度里再开」**全部前提是用户看得到任务进度**。
>
> #### ★ 留痕更正:V10 §13 的前提**已被实机证伪**
>
> V10 拆分包 §13 建议「**P4 签字之后**再单独做一轮」,理由之一是
> 「拆分收益**不在 P4 验收路径上**」。
> **实机把这条理由证伪了** —— 三格 ❌ 里有两格的根因就是
> 「主机客户端与副机客户端**是同一个 exe、走同一条网络路径、因而拿同一个档位**」,
> 而那正是拆分要解决的事。⇒ 用户已裁**直接上管理端**,§13 那条时机建议**作废**。
> ★ 原文不删:它记录的「为什么当时认为可以等」与今天「为什么不能等」合起来才是完整的账。

> ★★★ **2026-08-07 P4 收官逐条核实的账**(方案书 §14 P4 那 13 格,详见 `PROJECT_PLAN_v3.0.md`):
> · **10 格 ✅**:资源仲裁器 · 争用四件套 · 非 AI 显存变化进推送 · 拒绝含占用者 ·
>   权限档位六元组 · `gpu.apply` 不进 Agent 工具池 · 组件选择器(三段预览由 V8 补齐)·
>   意图驱动切换(P4 范围内那半)· 不变式 I2/I3/I4 + `RECONCILE_WATCH` ·
>   **失败落点/事务语义/`DEGRADED_SAFE`(sysmem 那三句已逐句落实,见下 ③)**。
> · **2 格 🟡 部分,如实标着,不勾成完成**(那正是审计 C6 抓过的形状):
>   ① **租约「一套三处用」** —— 五种 kind 都在闭集里,但**只有 `client_session` 与 `model_ref`
>     有生产调用方**;`pet_presence`/`agent_task`/`recalibration` **生产调用方 = 0**
>     (桌宠归 P8 · agent 归 P6/D65 · 重标定随 B10⑥ 移出)。**机制在,而"三处用"里两处没人用。**
>   ② **B10 双组比对** —— GPU 那半已落地并 fail-closed;**显示指纹那半没有基线**
>     (`vram_gate.py:63`:P1-A7 那轮没记录分辨率/显示器数量,**不可回填**)⇒ **"双组"今天只有一组**。
>
> ★★★ **sysmem 那一格已于同日补完并转 ✅**(原本因第三句未做而标部分):
>   ① 我们自己的闸提前介入 —— 已随 **D99** 落地;
>   ② 系统注册表那个开关**不动** —— **裁定不做,不是遗漏**(系统级驱动设置,影响所有程序);
>   ③ **「启动期读一次并如实上报」已落地** —— `doctor.py` 第 **⑫** 环只读扫三个注册表根,
>      **恒返回提示态**:判 ✘ 会有人跑去关系统设置,判 ✔ 等于声称已经安全而我们并没有验过。
>      ★ 它**自己说出够不着的那一半**:控制面板那个设置在 `Drs/nvdrsdb*.bin` 二进制里,本检查**不解析**
>      ⇒ **「注册表没有覆盖项」不等于「fallback 已关」**。
>      护栏 `selfcheck.py` 第 ⑫ 组 **11 条**(识别器正反各钉 + 源码层钉住"不许下判决"),
>      **红测两次各红一条**(放宽识别器 / 改成返回 True)。
>   ⇒ **② 的「没做」是裁定,③ 的「做了」是事实。两者别混。**
> · **1 格移出**:B10⑥ 一键重标定 → P11(**D100**);D42.6 的两机抢显存弹窗同样移出(同 D100)。
>   原位保留删除线与注记,**不静默删**。
>
> ★ 新增决议:**D99**(压力即让 + 任务暂停/恢复 + 三段预览;★ **比 D87③ 多一条恢复路径**)·
> **D100**(两项正式移出 P4 —— 同一件事在本文件里以**建议**出现过两次都没落成决议,这次落死)。
> ★★ **2026-08-05 更正两处过度声称**(同一晚写下、当晚查出):
> ① 上一版这里写「🟢 主体完成」——**当时不成立**(六元组一维都没做)。
>   ★ **六元组已于 2026-08-05 落地(S10)**;剩下的只有【按需装载】。
>   方案书 §6.2 原话:「六元组里如果只实现了前四维,**等于没有实现**」——
>   而缺的恰好是它点名最关键的**参数维**,以及**额度维**。
> ② 上一版写「下一站 P5 装载器」——**P5 是「语音 v1(半双工,常驻)」**(方案书行 2306)。
>   **「装载器」这个词在方案书里一次都没出现**,是当晚临时造的说法;
>   它实际隐含在 P4 的「**预留先于装载**」(D37 四件套之一)里,后端今天由
>   `start-stack.ps1` **静态启动**。⇒ 按需装载**属于 P4**,不是 P5。
> P3c 已于同日 ✅ 验收签字(D80,两台真 PC)。

> ### 2026-08-06 · 三条并行车道并入 main(线性历史,无冲突)
> · **D89** 证书生命周期闭环(core/identity 车道)—— 设备证书续签两条路由 · 服务器证书自动轮换 ·
>   第五种归因 `LocalProfileUnusable` · B16 curated 词表 v1。**方案书 P3b 的三处勾选已同步对齐**
>   (`推迟 S4/P3b.2` 四件 · `S6 实机验收` · `B16`)—— 那三处此前**落后于实测**。
> · **D90** 按需装载:**禁令的主语收窄,不是禁令被推翻**。§8.1.7 三格禁止列原样有效。
>   ★ **两条未决**(见下方待决事项 9/10),**写代码前必须解掉**。
> · **`test-tainted-gate-promotion` 一份【不取号】** —— 它第 3 行自己写着「性质:执行清单,不是决议」,
>   而清单已执行完并进 main。**给一份清单追认 D 号 = 追认**,正是本项目反复拒绝的动作
>   (同 S6 硬件指纹「拒绝回填」· A7「拒绝把今天的配置追认为当时的测量条件」)。
>   它的价值已改落 [ASSERTION-PITFALLS.md](ASSERTION-PITFALLS.md) 第 10 条 —— 那才是它真正值得长期留的东西。
> · 另五份决议包(`appcontainer-isolation` · `egress-b-gates-impact` · `sink-axis-change-list` ·
>   `memory-suite-runnability` · `memory-suite-revival`)**一律不取号,裁定权在用户**。
>
> ### 2026-08-06 夜 · 两条【垂直切片】车道并入(D92 之后的第一批)
> · **V2 · GPU 面身份与租约收口 + S16-b 接动作**(`dd06af3`,包 `gpu-identity-ondemand-2026-08-06.md`,**待取号**)
>   —— 审计 B 级六条一批收口(B1 自报 holder · B2 点名释放别人的租约 · B3 两套身份 ·
>   B4 续租吃变更配额 · B5 `lease_count` 不过滤过期 · B6 按需授权缺主机闸);
>   S16-b **接了动作**:按需装载走一条**结构上够不着 `committed`** 的 transient 平面。
>   ★ **B1 与 B3 在同一提交** —— 不是洁癖:两面身份各自自洽所以谁也不炸,
>   而 `/v1/session/end` 恰好横跨两者,只改一边那条路径当场失配 ⇒ **退出不再释放任何租约**,而它吞异常。
>   ★★ **D90 的两条未决项(待决 9/10)已给出处置与代价,但裁定权在用户** —— 见下方那两行。
> · **V3 · 门禁与产物的诚实性**(`5734fb9`…`f7409be`,包 `gate-honesty-2026-08-06.md`,**待取号**)
>   —— 反向全表从 1 处扩到 4 处 · 产物落后于源码会红 · lan-edge 的 24 条终于有人跑 ·
>   **跨进程契约配对元规则**(`90-ops/gate/check_contract_pairs.py`)。
>   ★ 元规则**合并当天就抓到第一条**:V2 新加的 `POST /v1/gpu/intent` 未登记,两张契约表双向咬住。
>   ⇒ 现状:**27 条契约,已成对 4,欠 23**。那是一张**只许变短**的欠债表,
>   门禁把它印进覆盖账 —— 「欠配对」不等于「已裁定没问题」。
>   ★★ **`23` 才是那条车道最有价值的产出,不是「A1 被修好了」。**
>   **A1 只是这 23 条里被撞出来的那一条** —— 同形状的缺陷**结构上还有 23 个位置可以长出来**,
>   而在这张表出现之前,没有任何东西会告诉你它们存在。取号 **D95**。
> · **V1 · 证书生命周期接到底**(`dcbab13`,包 `cert-lifecycle-wiring-2026-08-06.md`,取号 **D93**)
>   —— **审计 A5 闭环**。此前 `localai-client.csproj:41-42` 把 `CertLifecycle.cs`/`TlsFailure.cs`
>   编进了构建,而 `HubClient.cs` 里**零调用点** ⇒ **随包发布的死代码**。
>   ★ **验收证据(实测,不是"已修"两个字)**:
>   `grep -c "CertLifecycle\|RenewDeviceCertIfDue" 20-client-win/app/Services/HubClient.cs`
>   —— 并入前 **0** → 并入后 **4**;`TlsFailure` 引用 **17** 处。
>   ★★ 接的时候发现**原文案是反的**:设备证书一旦过期,续签路由结构上就够不着了(实测三条),
>   而文案却在许诺自动续签、劝阻唯一能自救的重新配对 ⇒ **推翻 D89 §1.6 那张表的左半**。
>   ★ 跨语言成对断言做成**一份被三个 csproj 编译的常量**(`10-core/identity/WireContracts.cs`),
>   而不是两份手抄的期望值 —— 照 D92 字面做**会再造一个 A1**。
>
> ★★★ **D92 第一次实践的账,如实记一条它没覆盖的**:V2/V3 先并了而 **V1 晚并**,
> 于是**A5 在 `main` 上多活了一段时间** —— 期间任何一次出包都会把那份死代码再发一次。
> 垂直切片解决了「缝在车道边界上」,**但没有解决「并入是串行的」**。
> ⇒ 切片让每条车道内部自洽,可**只要还有一条没并,它修的那个洞就仍然开着**;
>   而这一点在 D92 里一个字都没写。留给下一轮定:是否要求「同一批切片要么全并、要么都不并」。
>
> ### 2026-08-06 收盘 · 契约欠债三条切片并入 + ★ 欠债表自己那个空方向
> · **V5**(GPU/租约切片,**D97**)· **V6**(sync + chat 切片,**取号留空**,见下)· **V4**(证书/配对切片,**D96**)。
>   ⇒ **契约欠债 23 → 1**(`TOTAL=27 PAIRED=26`)。**只剩 `GET /health` 一条。**
>   ★ 那一条**不要为了把数字清零而随手配一条断言** —— 给一条没人走的路配断言,
>   断言是绿的,而它什么都没守。它该被裁定为「接上」或「撤掉」,不是被凑掉。
>   ★★ 还债的收益**不是那个数字变小**:V5 那 4 条债**各自咬出一个真缺陷**
>   (`LeaseKeeper` 的 `generation` 有默认值 0 ⇒ 读失败**稳定伪装成「中枢忙」**·
>   SSE 的 `error` 帧被当成快照解析 ⇒ 中枢说了原因、客户端翻译成一句指向别处的猜测 ·
>   `ParseOutcome` 的注释与代码说反了);V4 还带出两处「双消费者只记了一个」。
>
> · **★★★ D98:那张欠债表【自己有一个方向是空的】。**
>   `check_contract_pairs.py` 顶部白纸黑字写着两个方向,而**第一个方向只对着一个文件** ——
>   于是在 `WireContracts.cs` / `test_sync.py` / `SyncClient.cs` 里声明的契约号它一个都看不见:
>   **代码里躺着 9 个本表不认识的契约号,门禁照报 189 PASS · 0 FAIL。**
>   一张**专门用来抓「看着有防护、实际没有」的表,自己就是那个形状**;
>   而它长在 **D95** 上 —— 当天刚立的那条决议。
>   > **★ 最要紧的不是这个洞,是它怎么被发现的:靠人工核对,不是它自己报的。**
>   已补全仓契约号反向全表(未登记 ⇒ 判红并点名 id 与文件),**红测两次**:
>   护栏的第一跑就抓到那 9 个 · 另做一次人工红测(临时放一个未登记 cid ⇒ 226 PASS · 1 FAIL)。
>
> · **★ V6 今天没有取号,如实空着。** 它只产出了 `sync-snapshot-disposition-2026-08-06.md`,
>   而那一份是**待用户裁定**的处置建议(按 D91 同源理由不取号)。
>   **没有为了凑一个号去编一份包,也没有给待裁的包取号。** 它的工作已并入且可核验。
>
> · **★ 历史不再线性**:本轮出现两个 `Merge branch 'main' into <branch>` 提交
>   (`8d2d325` · `4cbfcff`)—— 那是「把 main 并进分支」而不是 rebase。
>   V4 这次已回到 rebase + `--ff-only`。**那两个 merge 提交要不要留着,待裁**(见下方待决事项)。
>
> · **★ 全仓还剩 22 处 `D?` 占位**散在 10 个文件里(`gpu_broker.py` · `identity/*` · `WireContracts.cs` …)。
>   本轮只回填了 blame 得出归属的 3 处(2→D96 · 1→D89)。
>   **一次改 22 处而只 blame 了 3 处,填错的那几处会比空着更坏。**
>
> ### 2026-08-07 · V12 · P4 签字前两处收口(**D107**)
> · **自配对在开机路上【结构上】不可达** —— 自动起栈用 `run`,而 `run` **不绑回环管理面**
>   (`EdgeConfig.AdminPort` 默认 0)⇒ 主机上的客户端把自己判成「不是主机」;
>   **且业务口只在回环上,而 `DiscoverEdgeDialsAsync` 跳过回环** ⇒ **两道闸,只补管理面是不通的**。
>   改用 `run-lan <ip>`(★ 管理面**仍写死绑回环,D48 一个字没动**;**绑上 ≠ 够得着**,防火墙那步也没碰),
>   绑定地址**问内核路由表**而不是猜。顺带堵上 `run` 会自动敞开 30 分钟准入窗口那个洞。
> · **副机今天真能写 `intended_resident_set`** —— **实测 200**,不是读代码读出来的。
>   四集合表两行都写着「只有主机变更面能写」,而 08-06 审计 B6 **只给 `permitted_on_demand_set` 装了闸**。
>   已收口成 **403 + `dimension=tool`**,判据测**行为**不测源码,并已红测(档位表改回去当场判红)。
>   ★ **规格没错,错的是代码** —— §8.1 四集合表那一行**不用改**。
>
> ★★ **代价如实记,两句都要写全:**
> · 副机的组件勾选面板**变为只读** —— 这是真代价;
>   **但不是「副机什么都动不了」**:`lease` 档**保留** ⇒ D87①「意图即起」在副机**照常工作**,
>   变只读的**只有「替机主改写常驻清单」**这一件。
> · ★★★ **B6 那道闸今天【够不着了】—— 这不是"已加固"。**
>   副机在「解出动作」那一步就被拦住,走不到判 B6 的那一步;而只有 `trusted-local` 有 `change_resident`,
>   它同时也有 `permit_on_demand` ⇒ **今天没有任何档位能走到 B6**。
>   **不删它**:从「唯一那道闸」**降级为纵深防御第二层** ——
>   哪天有人加「能改驻留、但不能签按需授权」的新档位,它**立刻重新承重**。

> P3c 选型改为 **WPF 原生窗口**(D47,用户否决 HTML/Tauri)。新增决议 **D48**(管理面仅回环)、**D49**(证书续签)、
> **D50**(客户端明文落盘 + 日历本地优先与增量合并)、**D51**(项目软删除/共享垃圾篓/只读浏览/分支)、
> **D52**(数据归属:默认只在本机,共享是显式提升;含主机离线与一网多主机)。
> **★★★ 全仓基线(2026-08-11 凌晨 · V29–V32c 并入、D118–D125 落号之后实跑,`main@1b4634c`)**
>
> ★★★ **四个形态,量的不是同一个东西,【不许加成一个总数】。** 每一个都标了形态与量程。
>
> | # | 形态 | 数 | 什么时候拿到的 |
> |---|---|---|---|
> | ① | **`run-tests.ps1 -Full` 全量门禁** | **`PASS=2544 · FAIL=0 · SKIP=1`** | 2026-08-11 实跑 |
> | ② | **跨进程契约欠债** | **`1 / 31`**(已成对 30) | 同上 |
> | ③ | 客户端自检 · **仓库形态** | ✗ **本次没跑起来** | 见下 |
> | ④ | 管理端自检 · **仓库形态** | ✗ **本次没跑起来** | 见下 |
> | ⑤ | 客户端 · **出包形态**(原位 / 换个安装位置) | **`1053` / `1053` · FAIL=0** | 出包那一趟 |
> | ⑥ | 管理端 · **出包形态**(client 与 admin 并排) | **`203` · FAIL=0** | 同上 |
>
> **出包(2026-08-11)**:三件产物**同一轮**、版本戳 **`20260811-2030+99aee46`**、**无 `.dirty`**;
> `[5c]` 逐字报「四个 exe 的 ProductVersion 逐字相同,且 **HEAD 全程没动**」;退出码 0。
> ★ ⑤⑥ 与 ①③④ **不是同一个量程**:发布产物旁边没有源码,一批结构/接线判据整段跳过
> (既不计 PASS 也不计 FAIL,第 11 条)。**不许相加,也不许相减。**
> ★ 出包自检的哨兵**还没带 `OWED`/`LIFE`**(它自己报的),
> 所以 ⑤⑥ 这两个数**分不开「这个形态下测不了」与「本该跑而没跑」**(D117 那条口径尚未覆盖到它们)。
>
> ★★ **③④ 为什么是空的 —— 这是闸在正确工作,不是漏跑**:
> 门禁逐字报「**产物比源码旧,跑它等于测上一版 —— 先出包**」,于是它**拒绝执行**
> (ASSERTION-PITFALLS 第 3 条:**跑出来的绿数字比不跑更有害**)。
> ⇒ 判定是「**FAIL=0,没跑起来 2 个**」,退出码 1。
> ★★★ **`FAIL=0` 与「门禁过了」不是一回事** —— 本行把两者分开写,就是不让它们被读成一句话。
>
> ★ 那两个数要等**出包**之后重跑才拿得到(出包会刷新被测产物)。
> **在拿到之前这两格留空,不填任何"应该是多少"的数** ——
> 车道交回里给过 `client 2190` / `admin 346`,那是**它们在自己环境里**量的,
> **与本行的形态不是同一个量程**(第 11 条:发布产物旁边没有源码,一批结构判据整段跳过)。
>
> ---
>
> **↓ 以下是【已被取代的 2026-08-09 那一版】,留档不删**
>
> ★★★ **下面是三个数,它们量的不是同一个东西,【不许加成一个总数】。**
> 每一个都注明了量程;拿任意两个相加或相减,得到的都是没有任何东西量过的数字。
>
> | # | 数 | 它量的是什么 | 什么时候跑 |
> |---|---|---|---|
> | ① | **`PASS=4404 · FAIL=0` · SKIP=1** | `run-tests.ps1 -Full` 全仓门禁 —— 23 个 Python 套件 + 10 个 dotnet/客户端套件,**含** ② | 每次提交前/收工 |
> | ② | **`client --selftest 2092 · FAIL=0`** | 客户端自检,**Release/win-x64 · 仓库形态**(源码在旁边) | 含在 ① 里 |
> | ③ | **管理端自检 `164 · FAIL=0`**(2026-08-09 · V22/V23 之后) | **出包形态**(client 与 admin 并排,**旁边没有源码**) | 出包那一趟 · ★ **另外它今天也进了 ①**,见下 |
>
> 门禁逐字:「√ 门禁通过:PASS=4404 FAIL=0」· ★ 跨进程契约欠债 **1/30**(只剩 `GET /health`)。
> 出包同趟实跑:客户端**原位 `1009`** / **换个安装位置 `1009`**,`FAIL=0`,`exit 0`。
>
> > ### ★ 2026-08-09 更正:下面那段「`-Full` 对管理端只验编译」**已经不成立了**
> >
> > 它写于 `045b5b8`,而 **`c17f164` 已经把管理端 `--selftest` 接进了 `run-tests.ps1`**
> > (仓库形态 · **单列不并进 ① 的合计** · SKIP 透出 · FAIL>0 判红,并红测过)。
> > ⇒ 「管理端的断言一条都不在提交门禁里」**那句话今天是假的**。
> > ★ 原文留着不删:它记录的是那笔明账**当时**的样子,而下面紧接着那句
> > 「**编得过 ≠ 跑得起来**」仍然是这条账的正确读法 —— 变的是结论,不是道理。
> > ★★ 仍然为真的那一半:**`③` 那个 164 是【出包形态】的数**,
> > 与门禁里那个仓库形态的数**不是同一个量程**,两者都不许并进 ① 的合计。
>
> ~~★★★ **③ 那一格必须单独盯着:`-Full` 对管理端【只验编译】** ——
> 门禁里那一行逐字是「√ 管理端工程编译通过(**只 build,未 publish**)」。
> ⇒ **管理端的断言一条都不在提交门禁里**,只有跑 `build-client.ps1` 那一趟才会真跑。~~
> **改坏管理端而不出包,`-Full` 会一直是绿的** —— 这正是「**编得过 ≠ 跑得起来**」那句话的形状,
> 而管理端现在扛着 D114 搬过去的那 3100 行。**这是一笔明账,不是漏记。**
>
> ★★ **和车道报的数对不上是【量程差】,不是回归 —— 已逐条对过账**:
> · V21 报「全量门禁 **2312**」+ 本次 `client --selftest` **2092** = **4404**,与 ① **一分不差**
>   ⇒ V21 那 2312 是它 worktree 里**跑不了客户端自检**的那一半;
> · 客户端 V21 报 **2095** / 本次 **2092**;管理端 V21 报 **170** / 本次 **111** ——
>   都是**仓库形态 vs 出包形态**的差(发布产物旁边没有源码 ⇒ 一批结构/接线断言整段跳过,
>   既不计 PASS 也不计 FAIL、更不计 SKIP,见 ASSERTION-PITFALLS 第 11 条)。
>   ★ **三种环境(worktree / 仓库 / 出包产物)的数天生不可互比**,对账要拿**同一种形态**的两次比。
>
> ★ **客户端 2189 → 2092 的下降是【正常的】**:D114 把 3100 行搬进管理端,断言跟着搬走。
> **不是覆盖变少了** —— 搬走的那些今天在 ③ 里跑,而 ③ 不在门禁里(见上一段)。
>
> > **↓ 以下是【已被取代的 2026-08-08 那一行】,留档不删**
> >
> > `-Full` → `PASS=4431 · FAIL=0` · SKIP=1 · 契约欠债 1/30 · 含 `client --selftest 2189`。
> > ★★ **4424 → 4431 的 +7 对得上账**:V14b 在 `5ae762e` 报的是 4424(它在自己工作树里量的),
> > 差的 **+7** 正是 D111 那条串行闸的反向断言(`test_gpu_broker.py` 684 → 691)。
> > **两个独立的数相差一个能说得出名字的量 —— 这才叫对上账。**
> > ★ 同日出包:客户端原位 `1045` / 换个位置 `1036` / 管理端 `29`,全 `FAIL=0`。
>
> > #### ★ 留痕:同一天早些时候,这一行曾经【空着】,而那是对的
> >
> > 08-08 上午跑 `-Full` 拿不到完整数:Python + dotnet 侧 `PASS=2235 · FAIL=0`(**没有一条红**),
> > 而 **`client --selftest` 没跑起来** —— 产物比源码旧,门禁按 ASSERTION-PITFALLS 第 3 条
> > 那道闸**拒绝执行**;判定逐字是「**FAIL=0,没跑起来 1 个**」。
> > 当时**没有把 2235 加上一个客户端数凑成基线**,也**没有拿 4197 去减** ——
> > **那是「算出来的基线」,本仓反复点名要防的东西。没实跑就没有数,宁可留空。**
> >
> > 补不上的原因是**出包出不来**:`build-client.ps1` 第二趟「换个安装位置」4 条红。
> > 根因**不是产品缺陷,是判据被环境骗了** —— 那一趟把 exe 拷到 `GetTempPath()` 下,
> > 而本机 Temp 里正住着各会话的 scratchpad(`Temp\claude\…\<会话>\scratchpad\*.cs`),
> > 自检往上翻找「源码根」**翻到了 Temp**,把别的会话遗留的 `.cs` 当成了仓库源码
> > (V14b 量到:递归扫进 **1218 个**不相干的 `.cs`)。
> > 铁证是那条元断言自己喊出来的:**`源码根=在 · 命中 0 次 · 落空 259 次`** ——
> > **「零命中」与「根不在」当时在代码里同形**(第 10 条坑同族)。
> >
> > **修法(V14b,`ea6dbd7` → `5ae762e`)**:源码根锚点从「有没有 `Selftest.cs`」
> > 改成「`Selftest.cs` **且** `localai-client.csproj`」—— csproj 只存在于真正的源码根,
> > **两个一起看才是在问「这儿是不是客户端源码根」,而不是「这儿有没有一个叫这个名字的文件」**
> > (第 9 条坑)。★★ 而且它**分两次才改对**:第一次漏了第三处锚点,于是缺陷完整地留在那一处 ——
> > 它自己的留痕值得抄在这里:「**一个修法漏改一处,缺陷就完整地留在那一处。
> > 改完要把同形状的地方都数一遍。**」
> > ★ **今天 `main` 里那三处锚点仍是三份各自为政的拷贝**(未抽成一个函数)——
> > 同一个漏改还会再发生一次,这笔账记在这里。
> >
> > ★ 上上版是 `PASS=4197`(2026-08-07,V7 语音 + V12 P4 收口并入之后),已被本行取代。
> > **不要拿 4197 去减 4431 之外的任何数**:各形态量程不同,相减得到的是没有任何东西量过的数字。
> ---
> **↓ 以下是【已被取代的 2026-08-07 那一行】的配套明细,留档不删** ——
> 它记的是 4095 → 4197 那次怎么对的账,方法仍然有效;**数字本身已不是当前基线**。
>
>   ★★ **4095 → 4197 的 +102 逐套件对上一分不差**:33 个套件里 6 个变了、**27 个 ±0** ——
>   `client --selftest` 2074→**2116**(+42)· `test_speech_contract` **28**(新套件)·
>   `check_contract_pairs` 227→**252**(+25)· `test_gpu_policy` 83→**87**(+4)·
>   `test_gpu_broker` 583→**585**(+2)· `test_imports` 13→**14**(+1)。Σ=102。
>   ★ **上一版那一行(4095)是 V7 并入【之前】的数,已经过期** —— 本行取代它。
>
> ────── 以下是**上一版**(2026-08-07 · V9 全部并入 · 4095)的构成,保留作沿革 ──────
>   ★★ **4085 → 4095 的 +10 逐套件对上一分不差**:32 个套件里**只有 1 个变了** ——
>   `test_gpu_broker` 573→**583**(+10),其余 **31 个逐个 ±0**。
>   (上一段 4062 → 4085 的 +23 同样逐套件对上:只有 `client --selftest` 2051→2074 变了。)
>   ★ V9 取号 **D101**(开机即判角色 → 主机自动起栈)· **D102**(关栈判据)。
>   ★★ **关栈的「自动」那一半被 V9 自己撤掉了** —— 中枢只回答「关了会不会切断别人」,
>   **关是人的动作**;跨机空闲阈值 / 副机在线名单 / 定时巡检**全部撤掉**,理由是同一句:
>   **它们是在替人做判断。**
>   ★★★ D102 立了一条通则:**凡「写好了的收尾/清理函数」,都必须有一条断言钉住它有调用点** ——
>   今天同一形状**第三次**(A5 库零调用点 · `doctor` ⑫ 环写好没挂进 LINKS ·
>   `ModelLoader.shutdown()` 零调用点 ⇒ 关网关时按需起的后端不被收,显存继续占着)。
>   ★ D101 顺带查出一件比它本身更要紧的事:**`dist/host/` 里根本没有网关** ——
>   「意图即起」在引导层断掉的根因**不是"没人写代码",是网关没有随包发布**。
>
> ────── 以下是**上一版**(2026-08-07 · P4 收官 · 4062)的构成,保留作沿革 ──────
>   ★★ **3994 → 4051 的 +57 逐套件对上一分不差**:32 个套件里**只有 2 个变了** ——
>   `test_gpu_broker` 535→**573**(+38)· `client --selftest` 2032→**2051**(+19);
>   其余 **30 个逐个 ±0**。Σ=57,与总数差完全一致(核法见 ASSERTION-PITFALLS 第 3 条)。
>   ★ V8 决议包 §8 写的是「`test_gpu_broker` 544→573 · 客户端 1989→2055」——
>   那是**它自己 worktree 里**量的,基线不同,**不要拿去对账**;以本行为准。
>
> ────── 以下是**上一版**(2026-08-06 收盘 · 3994)的构成,保留作沿革 ──────
>   构成 = **Python 1647**(gateway **1061** + gpu-broker 134 + 调试工具自检 66 +
>   契约配对元规则 **227** + 记忆套件 159)+ **dotnet 315**(identity **139** ·
>   lan-edge **93** = 69+9+15 · transport **83**)+ 客户端 **2032**。
>   ★★ **3696 → 3994 的 +298 逐套件对上一分不差**(32 个套件里 6 个变了,26 个 ±0):
>   `check_contract_pairs` 119→**227**(+108)· 客户端 1961→**2032**(+71)·
>   `lan-edge selftest` 33→**69**(+36)· `test_sync` 85→**120**(+35)·
>   `test_gpu_broker` 501→**535**(+34)· `transport selftest` 69→**83**(+14)。Σ=298。
>   ★★★ **但这 +298 分不到各条车道头上,如实说明**:V5 与 V6 并入 main 之后
>   **没有人重跑过 `-Full`**,而我在并 V4 之前也没有在 main 上跑过一次
>   ⇒ 3696 与 3994 之间隔着 **V5 + V6 + V4 + 收盘那条护栏**四批改动,**它们不可分离**。
>   唯一能单独归因的是收盘那条护栏:`check_contract_pairs` 189 → 227 = **+38**(两次实跑之差)。
>   **其余的不补一个看起来自洽的拆分** —— 同一条教训上面那段「撤回一次编造的归因」已经记过一次。
>   ★ 这本身是一条账:**基线在脚下移动**,而没有任何东西强制"并入即重跑"。
>   (08-04 首跑 2111;08-05 凌晨 2833;08-05 P4-S15 后 2913;08-06 并入 D89/D90 后 3201;
>   同日 A1/A2/D91 后 3320 → C3 复查 +1 = 3321;同日夜 V2+V3 并入后 3638;V1 并入后 3696;
>   **收盘 V4/V5/V6 + D98 护栏后 3994**。)
>
> ────── 以下是**上一版**那一行(V1+V2+V3 收盘)的原文,**保留作沿革,不再是当前基线** ──────
>   构成 = **Python 1470**(gateway **992** + gpu-broker 134 + 调试工具自检 **66** +
>   契约配对元规则 **119** + 记忆套件 **159** = tainted 75 · route 55 · repo 29「+1 SKIP」)
>   + **dotnet 265**(identity 11+15+42+14+57=**139** · lan-edge **57** = 33+9+15 三个入口 · transport **69**)+ 客户端 **1961**。
>   ★ **更正一处加错的和**:上一版这格写「Python **1332**」,而它自己列的分项
>   992+134+66+119+159 = **1470**。分项是对的、总数 3638 也是对的,**错的是那个小计**(差 138)。
>   ⇒ 已改成 1470。**不是覆盖变了,是一个数加错了** —— 两件事别混。
>   (08-04 首跑 2111;08-05 凌晨 2833;08-05 P4-S15 后 2913;08-06 并入 D89/D90 后 3201;
>   同日 A1/A2/D91 后 3320 → C3 复查 +1 = 3321;同日夜 V2+V3 并入后 3638;**收盘 V1 并入后 3696**。)
>   ★★ **3638 → 3696 的 +58 逐套件对上,一分不差**(核法见 ASSERTION-PITFALLS 第 3 条:
>   数变了而你没增删断言 ⇒ 先查是不是跑错了产物)。并前并后各跑一次 `-Full`,
>   **32 个套件里只有 3 个变了**:`lan-edge selftest` 20→**33**(+13)·
>   `transport selftest` 58→**69**(+11)· `client --selftest` 1927→**1961**(+34)。
>   其余 29 个**逐个 ±0**。Σ = 13+11+34 = **58**,与总数差完全一致。
>   ★ V1 决议包 §7.1 草稿里写的是 **3379 / client 1935** —— 那是 **rebase 之前**在它自己
>   worktree 里量的,**与本行不是同一个基线**,不要拿去对账。
>   ★ **3321 → 3638 的 +317,只写两次实跑之间能对上的那部分**:
>   V2 车道 **+150**(`test_gpu_broker` 385→**501** = +116 · `test_gpu_policy` 81→**83** = +2 ·
>   `test_sync` 79→**85** = +6 · 客户端 1901→**1927** = +26)·
>   V3 车道 **+169**(`selfcheck` 40→**66** = +26 · `check_contract_pairs` **119**(新套件)·
>   `lan-edge` 的 client-e2e **9** + admin-e2e **15** = +24「此前门禁从来不跑,不是新写的」)。
>   ★★ **两者相加 319,比实测的 +317 多 2 —— 这 2 条差在【旧口径那一侧】,没去数,如实空着。**
>   按当前门禁逐行反推,改动前 gateway 合计应是 **868**,而上一版这格写的是 **870**。
>   **不补一个看起来自洽的拆分**(教训见下方"撤回一次编造的归因"那一段)。
>   ★ V2 那 +116/+26 里,**「新增覆盖」与「原来就该有却没有」两类都有**:
>   B1–B6 六条各自的正反断言是新增覆盖;而 B4 那条是**把一条恒绿的假断言换成行为断言**——
>   它不增加数字上的净覆盖,却把一句**当时为假**的话变成了可以为假的判据。
>   ★★ **SKIP 从此看得见**(D91 裁定⑤):`test_repo` 自报 1 条没验。
>   **SKIP 不是 PASS** —— 一个套件把断言跳过、覆盖账照样全绿,正是本项目第一戒律要防的形状。
>   ★ 3201 → 3320 的 +119,**四项都是本轮两次实跑之间的差**(不是推算,下面这条的教训就在旁边):
>   route **55** + repo **29** = 84(这两行此前根本不出现在门禁输出里,现在出现了)·
>   `test_gpu_broker` **357 → 385** = +28 · 客户端哨兵 **1893 → 1900** = +7。84 + 35 = 119。
>   ★ 其中 repo 的 29 里有 **4 条是新写的护栏断言**(钉住它自己的只读 try/except),
>   其余 **80 条是"终于有人跑了",不是新增覆盖** —— 这两类不许混记。
>
>   ★★★ **这个数只对【开发树产物】成立,不能拿去和发布产物比**(2026-08-06 审计 C3 复查量出来的):
>   `Selftest` 里 **268 处** `TryReadSource` 在**发布产物旁边读不到源码**,而调用方一律是
>   `if (src is not null) { Assert… }` ⇒ **整段跳过**:不计 PASS、不计 FAIL、**也不计 SKIP**。
>   实测同一份源码、同一个全量 `--selftest`、同一个哨兵:
>   **开发树 1901 · 发布产物 852**,**1049 条断言静默消失**,
>   而 `VERSION.txt` 上那行读起来像是「这个产物通过了自检」。
>   ★ 这正是本项目第一戒律要防的形状 —— 而它长在**自检自己**身上,躺了不知道多久。
>   ⇒ 已修三处:① 自检**数出落空次数**并印进汇总行与哨兵(`SRCHIT`/`SRCMISS`);
>     ② `build-client.ps1` 把口径写进终端**与 `VERSION.txt`** —— 数字和"它在量什么"一起被读到;
>     ③ 加一条**反向钉**「源码读得到时一次都不许读不到」(实测 命中 268 · 落空 0)。
>     没有 ③,把一个源文件改名/挪走就会让一批断言**无声消失**,而 PASS 只是小了一点。
>   ★ **少的不是覆盖变差了**,是那批结构/接线断言在发布形态下**测不了**。两件事别混。
>   ★★ **2026-08-06 撤回一次编造的归因(留痕)。** 上一版这里写「**+288 的来源已逐项对上**:
>   dotnet +123 与客户端 +39 来自 D89……」—— **那个归因是编的,+288 从来没有被逐项对过**。
>   审计在基线 `481955e` 实测 **3126**,而 **D89 的四个 `.cs` 在那个基线上就已经存在**、早被数进 3126;
>   `Selftest.cs` 自该基线起一个字没改。**把已经计入的东西再算一遍,数就会凭空多出来。**
>   **真话只有一句可核对的**:`3126 + 75 = 3201`,**净贡献 +75**,全部来自 `test_tainted.py` 提级进门禁
>   —— 那 75 条此前**被 `10-core\memory` 目录规则整条连坐成 manual,从来没有人跑过**。
>   ★★ **不是新写了 75 条断言,是它们终于有人跑了 ⇒ 本轮「新增覆盖」= 0 条。**
>   `2913 → 3126` 那 **+213 的逐项归因没有做过**,**如实空着,不补一个看起来自洽的拆分**。
>   ★ 教训与本文件反复记的是同一条:**先去数,再下结论**。这次是"没数基线里已经有什么"。
>   ★ **`lan-edge` 那格与旧口径行对不上**(旧行写 8+6+15=29,门禁实跑一条 `lan-edge selftest` = 20)——
>   **如实记着,不改成看起来自洽的数**;哪个对要复核后才谈得上改(见下方回归口径行的同款告警)。
>   ★★ **S15 审计发现:此前门禁的客户端半边一直在跑【上一次出包的产物】** ——
>   改了 `Selftest.cs` 加 3 条断言,门禁照报 `PASS=1852` 纹丝不动。
>   ⇒ 现在跑之前**比时间戳**,源码比 exe 新就判「没跑起来」并拒绝执行(不是警告后照跑 ——
>   跑出来的那个绿数字比不跑更有害)。**这意味着 08-05 凌晨之前的客户端数字都只代表"上一次出的包"。**
>   ★★ **这一版是在【栈跑着】的情况下跑的** —— 见 [ASSERTION-PITFALLS.md](ASSERTION-PITFALLS.md) 第 5 条:
>   有 11 条断言此前**整天是绿的,恰恰因为产品还不能用**(它们把「后端没起」焊进了判据)。
>   ⇒ **自查办法:把栈起起来再跑一次全套**,这类断言那一刻会全部现形。
>   ★ 跑 `-Full` 需 `-ExecutionPolicy Bypass`(本机默认策略禁脚本;pre-commit 钩子内部已带)。
>   ★★ **客户端自检必须用 Release/win-x64 那个产物**:Debug(`net9.0-windows`)下只跑出 1471 条,
>   Release(`net9.0-windows10.0.19041.0`)下 1755 条 —— **差 284 条**,拿 Debug 对基线会得出错误结论。
>   判据:**断言总数变了而你没增删断言 ⇒ 先查是不是跑错了产物**(见 [ASSERTION-PITFALLS.md](ASSERTION-PITFALLS.md) 第 3 条)。
>   ★ **记忆库 14 个套件不在其中**:它们连真实库且 `pg_ident` 只映射 `ai-mem`,当前身份 SSPI 会被拒 ——
>   运行器会把这条**明写在覆盖账里**,不静默省略(D82 纪律:跑不了和跑过了必须长得不一样)。
>
> 回归口径(**2026-08-06 按门禁实际打印的分组更新**):identity 11+15+**42**+14(成员层)+**57**(证书生命周期,D89)
> · lan-edge **44**(= 20 + 9 + 15,**三个入口**)· transport **58** ·
> ★ **「旧行的来历待复核」已结清(2026-08-06):两组数【都对】,量的不是同一个东西。**
>   旧口径行(`identity …+28+13 · lan-edge 8+6+15 · transport 5`)按**测试入口**分段记;
>   新行此前记的是**门禁实际打印的那几段**,而门禁在 `lan-edge` 上**只跑了三个入口里的第一个**。
>   ⇒ 此前那句「`lan-edge` 三段相加 29,而门禁只打印一个 20,**两者不可能同时对**」
>   是**错判** —— 29 数的是三个入口,20 数的是其中一个,它们本来就不该相等。
>   **物证**(不是推断):写下旧行的那次提交(`80a7315`)原文是
>   `lan-edge 8+6+15(管理面 E2E)` —— 括号点名的正是第三个入口 `admin-e2e`,
>   而它今天实测**仍是 15**,一个数都没变。identity 与 transport 同理:
>   分段方式从来没变过(identity 一直是 5 段、transport 一直是 1 段),只是段内的数长了。
> ★★ **真正的病不在记账**:`lan-edge` 的 `client-e2e`(9)与 `admin-e2e`(15)——
>   **共 24 条断言从写下那天起没有任何东西跑过**,而且连"没跑"都不在覆盖账里,
>   覆盖账读起来像是 lan-edge 全跑了。根因是门禁的 dotnet 半边当时是一份**手写清单**、
>   背后没有反向全表(ASSERTION-PITFALLS 3b)。已修:入口清单改从各工程的 `args[0]`
>   分派表解析得出,三个入口现在都跑;新增入口不登记当场判红。
>   见 `decision-packets/gate-honesty-2026-08-06.md`。
> ★ 现在两组数**可以比了**(都按入口分段);但仍别拿 08-06 之前那个 `lan-edge 20`
>   去和 44 相减推"新增了多少条" —— 那 24 条不是新增的,是**一直在、一直没人跑**。
> **客户端 selftest 1901**(2026-08-06 C3 复查后实跑,FAIL=0;桌宠状态机 46 条已含在内)
> (外壳 30 → 主页 224 → 聊天/项目/存储/共享 562 → 翻译/焦点/皮肤词汇 853 → … → P3c 收尾 1566 → 验收签字 1728 → P4-S9 1755 → S11 接入模型 1790 → S13 内网同步 1844 → S15 审计 1854 → 08-06 三车道并入 1893 → A1 止血 1900 → **C3 复查 1901**)
> ★ **纪律:这个数只在这一行出现**,别处一律引用而不复写 —— 本文件曾同时挂着 853 / 1544 / 1566 三个数。
> ★★ **S15 起这个数才真正代表"当前源码"**:此前门禁跑的是上一次出包的 exe(见上方基线段)。
> ★ 1901 是 `main` 上的数;另有**封存**的 5 条(自配对 ㉕)在分支 **`parked/selfpair`** 上
>   (见 `decision-packets/selfpair-review-2026-08-04.md` 与 D80/D81;**该分支不得合入 main**),
>   解封时这一行要跟着改。identity 配对套件 22 → 28 是 2026-08-04 补的「半截记录会过期」那 6 条。
>
> ---
> ### 2026-08-03 补记:桌宠定形 + **P8 的运行时内核被提前做掉**(D76)
>
> **★ 归属没有变:桌宠一直在 P8**(§14 P8 = Vigil + 它的身体)。本轮只是把其中
> **不依赖 P6 权限区域、不依赖 Vigil 本体、不依赖任何一张帧**的那部分提前做了。
> 理由不是抢跑:**A7-pet 本身就是 P8 清单的第一个 checkbox,而它需要一个能跑的渲染实现才测得了。**
>
> **裁定四条(详见 D76)**
> · **渲染 = 纯像素逐帧 6 fps**,D40 补记的「默认 DragonBones」作废 ⇒ STATE 待决事项 2 关闭。
> · **v1 = 哑巴猫**(表演层 + 反应层,零 token / 零记忆 / 零开口),拆 **v1a 家 → v1b 桌面 → v1c 串门**,
>   风险单调递增,只有 v1b 被 A7-pet 挡着。
> · **家 = 主页问候语右侧那条横条**,空白背景不做场景,猫以下方板块上沿为地板;横轴一维。
>   两扇门一左一右:**一门(右)通本机桌面、可开关**;**二门(左)通其他 PC、常开永不关**。
>   走过二门进的是**对面的家**。所有已配对 PC 的横条连成一条走廊 = 室内;桌面 = 室外。
> · **唯一实体**:猫只在其中一个家里;「猫在哪」的唯一记录落在 hub(D48 设备间只能走 lan-edge);
>   **hub 离线 → 走廊断,猫只能待在当前房间和本机桌面**(正确降级,不靠两台 PC 自己协商)。
>
> **两条结构性纪律(已成断言,不只是文档)**
> · **行为层与助手只能提意图,永远不能点名 clip / 帧 / 时长。** 动画层是姿态唯一权威,
>   有权拒绝、推迟、或改写成一条合法过渡链。规范 §6 的四条铁律因此是**结构上做不到违反**,
>   而不是靠自觉 —— 只要谁能直接点 clip,那四条就全部可绕过。
> · **助手的把手 = 反应层同一个通道**,不开专用 API。于是没有"可信调用方"这个概念
>   (正对着 P3a 的 `unseal_for_client` 硬编码 `TRUSTED_LOCAL` 那类亏)。
>   **助手不能开门,只能让猫去挠门** —— 权限模型变成了角色行为。
>   返回值三态(accepted / deferred / rejected),**v1 结构上不存在 `Speak`**。
>
> **已实作**:`20-client-win/app/Services/Pet/` 四个文件(`PetClock` / `PetManifest` / `PetIntent` / `PetAnimator`)
> + 46 条断言。manifest 校验是 **fail-closed**:孤儿 clip、悬空引用、死支路一律判错。
>
> **契约修了三处**:补 `suspended → behind_door` 边(原先猫在门外时本机休眠恢复会凭空生出第二只猫,
> **破坏唯一实体**)· `mobility_cut` 补齐两条 stalk 过渡(26 → 34 帧,原版留 8 张没人播的孤儿帧)·
> budgets 随之更正(142 → 134,133 → 125)。
>
> **★ 帧资产是真瓶颈,不是代码**:A 批 13 张身份锁定帧**仍不存在**,却已先产出 D 批的 8 张猫身帧
> —— 花纹基准尚未锁定。规范 §10 的跨帧一致性验收排除了扩散模型逐帧生成这条路。
>
> **仍待裁**:两扇门被绑成单向互斥(`door_enter`→desktop / `door_exit`→corridor)·
> corridor 门画成软垫猫窝而契约自禁 `sleeping_bed_or_resting_prop` · 猫如何知道"哪台机器有人"
> (系统空闲秒数不含"你在做什么",但正是 D40 那道斜坡的第一级台阶,**默认落禁止**)· arousal 层要不要。
>
> ---
> ### 2026-07-31 补记:翻译工作空间成形 + 两条全局纪律立成护栏
>
> **翻译工作空间(主体完成,待 AI 接入)**
> · 会话面板【统一成一份】:`BuildConvPanel` + `ConvSpec`,聊天与翻译差异全部参数化,
>   `BuildTranslationLayout` 已删除。修掉分叉期间悄悄退化的四件事
>   (归档入口只长在聊天分支 / 只读态不滚到底 / 折叠键用下标 / 占位空间内边距跳变)。
> · 下半条:程度竖排(直译·词解·例句·语法,从下往上递进,可滚轮调档)|
>   目标池 1×3 坑 | 语言池 2×3 坑(宽度恰为两倍,空坑放「+」进设置,齿轮已撤)| 翻译历史。
> · 第二阶【随目标语言变】:中日韩给读音、英德给词根、混着给「词解」(档位名一律两字)。
> · 目标池算不出目标时的【兜底级联】:母语 → 英语 → 在对话里问(带按钮,答过置灰)。
>   母语暂由界面语言推出,设置里可显式改写。
> · 待翻译内容分【单词/短句/长文本/带格式长文本(附件)】:长文本禁用语法与例句、自动回退直译;
>   长文本+词解改为"末尾单独列重点词表";附件内容一律按带格式长文本(不看长度)。
> · 翻译历史取代学习笔记:**不另存原文**,是会话消息的视图,只存收藏;点一条跳回原位。
> · 拖放:拿起即离开原池、跟手、松手飞入目标坑(FLIP),其余卡片各自滑动补位;到位才响。
>
> **两条全局纪律(已立成断言,不只是文档)**
> · **微风(苹果风)= 标准默认皮肤**:所有主要设计对着它做,其它皮肤只换色 + 微调。
>   `Views/` 下按皮肤分【结构】的地方**一处都不许有**(暖萌堆叠卡片已按用户裁定砍掉)。
> · **皮肤八基色**:背景 / 着重 / 次着重 / 主题(身份)/ 文字 / 文字反 / 着重反 / 各自深浅。
>   分工写死:着重色=可点可选中,主题色=身份(品牌标记),着重反色=指出位置,次着重色=第二数据序列。
>   风险色与可见范围色**跨皮肤恒定禁改**,不属于皮肤自由发挥。
>
> **键盘焦点纪律**:Tab 是二态开关(AI 输入框 ⇄ 不聚焦),方向键不移动焦点(走导航层,
> 绝不在隧道事件里吞方向键,否则输入框报废)。"不聚焦"= 把焦点停到专门的零尺寸元素上 ——
> 只调 `Keyboard.ClearFocus()` 会被 WPF 还回输入框(表现为"看着没选中却能打字、Tab 也回不去")。
>
> **本轮修掉的用户实测 bug**(择要):整个界面点不动(菜单状态靠计数器攒,漏一次 Closed 就永久锁死)·
> 气泡灰底白字 · 截图预览空白(DIB 无真 alpha)· 已删除项目里仍可发消息 ·
> 日程起止可颠倒(污染 Apple 同步)· 跳转设置后板块永久变灰 + 只滚到顶部 · 收藏夹必炸(元素双父节点)。
>
> **同声传译:界面骨架已成形(2026-07-31),语音链路未接**
> 已确认的平台事实:Windows 上【没有】不写内核驱动就能被别的软件选中的麦克风(视频侧有用户态
> 虚拟摄像头,音频侧从来没有);变声软件无一例外都在交这笔驱动税。
> **★ 决策更正(2026-07-31):用户裁定改为接受第三方 VB-CABLE**(而非自研签名驱动)——
> 换来的是全程自动安装、用户几乎察觉不到,代价是引入一个第三方内核驱动,界面如实告知。
> 客户端现状:场景切换(文字翻译/同声传译/预留)· 语言方向双坑拖入 · 我方/对方竖直音量 ·
> 底部字幕横条 · 同传设置(设备选择 + 延迟读数 + 声卡状态)· VB-CABLE 检测/安装/更新/卸载(安装前 Authenticode 官方签名校验) ·
> 示例同传记录。**语音链路(采集 / ASR / 合成 / 虚拟麦注入)全部未接** —— 界面如实标注,不伪造字幕。
> 按进程回环采集(只录会议软件的输出)有官方 API,可做到不录系统其它声音。
> 架构底线:**同传可以失败,用户的麦克风不可以** —— 透传是独立常驻、不经过任何模型的一条线。
> ★ 尚无正式 D 编号(VB-CABLE 路线决策待补一条 D)。
> 待核实:驱动签名的真实代价、流式克隆 TTS 的首包延迟与显存账(工作流进行中)。
>
> ---
>
> ★ **前端进度实话**(2026-07-30 晚):**主页 + 聊天(含项目体系)已成形并反复打磨**;
>   客户端规模 ≈ **14,000 行**(Views 7231 / Services 2505 / Theme 833 / I18n 208 / 根 3298),今日 38 次提交。
>   **用户裁定本轮客户端只做「主页 / 聊天 / 翻译」三块**,其余工作空间等它们搭得差不多再回来。
>
> ★ **本轮(2026-07-30)完成的大块**:
>   · **本地落盘持久化**(D50):项目/会话/待办/日历/记忆 明文存 `{state}/client/*.json`,原子写 + 坏档留证;
>   · **D45 可见范围过滤实装**(fail-closed:所有者未知一律不可见;绝不读界面显示名缓存);
>   · **项目体系**:软删除 + 跨空间共享回收站 + 已完成按空间隔离 + 只读浏览 + 继续/开分支 +
>     文件夹唯一性(同路径自动合并、子路径不算)+ 文件夹所在机器;
>   · **会话分层存储**:热层 200 条 / 温层按会话分文件 / 「加载更早」——★ 归档不是删除;
>   · **记忆库 + 存储清理**:一键清爽(勾选执行内容,危险项默认不勾)+ 记忆预览/删减/置顶 + 真实占用统计;
>   · **共享模型**(D52):会话/项目默认只在本机,显式「提升为共享」且**单向不可收回**,任何机器都能删除共享会话;
>   · 全局异常兜底(crash.log)、菜单/浮层点外部只关闭、三皮肤状态配色令牌化、大量动画与交互打磨。
>
> ★ **仍是"外壳 + 诚实占位"**,所有未接数据源均如实标注、绝不伪造:
>   **AI 未接入(P4)** —— 发送只记录消息 + 系统说明,不伪造回复;摘要必须由 AI 生成,未接入前
>   「整理摘要」什么都不做、记忆库为空并直说原因。日历改为**本地优先**(可增删改并落盘),
>   接 Apple 后走**增量合并不覆盖**;待办/日历改为**各自连自己的 Apple 账号**(共享交给 Apple,我们不再自建)。
>   天气(已接 api.open-meteo.com,只发 0.1° 取整坐标,D78/D55;"当前地"是时区推断非定位)· token 用量(未接,显"待接入")·
>   问候副句(本地精选,非模型生成,界面不谎称 AI 生成)· 左下角使用者身份是**推测**(来源如实标注 Hub/缓存/本机账户)。

| 阶段 | 状态 |
|---|---|
| **P0 地基** | ✅ 验收通过(2026-07-26) |
| **P1 基准测试** | ✅ **完成 17/17 全部实测**(2026-07-27) |
| **P2 核心链路** | ✅ **基本完成** —— 权限档位已移至 P4(D37);WebAuthn 已作废(D34) |
| **P3a 记忆系统** | ✅ **已验收(2026-07-28)** —— S0–S9 全部完成,四门全绿(见下) |
| **P3c 统一客户端** | ✅ **验收通过(2026-08-04 · 签字 = **D80**,包见 `decision-packets/p3c-signoff-2026-08-04.md`)** —— 两台真 PC 实测:中枢上 `HONGKONGPINGPON`(主机)与 **`SENIORBIRDS`(副机)** 双双 active,副机装的就是 `client-pack` 出的包。★ **08-03 标的 S4「✅」当时并不完整**:图形界面那条配对路径**从来没走完过**(P3b 走的是控制台,绕开了 enroll 之后那段;`Trusted()` 只钉根 CA 没关主机名校验 ⇒ 继续用 `https://<ip>` 必在 `/pair/status` 握手失败,设备永远停在 provisioning),`b136f01` 修掉后**今天才第一次端到端跑通**。验收四句:①装得上 ✅实机 · ②未获批看得到进不去 ✅实机 · ③换网段改地址不失效 🟡**结构性成立(`SetDial` 只改 `Dial`,证书/私钥一字未动)+ 实机换网段由用户裁定免验** · ④三语 ➡️P7。 —— S0 选型 ✅(D47 WPF)· S1 成员层 ✅ · S2 管理面 ✅(仅回环 D48)· S3 WPF 外壳 ✅ · S1b 证书续签 ✅(D49)· **协议版本协商 ✅**(客户端半边;网关回头那半行归网关线)· **主页 ✅** · **聊天 + 项目体系 ✅** · **本地落盘 ✅(D50)** · **D45 过滤实装 ✅**(客户端侧 fail-closed;**仍用设备本地哨兵,区分不了同机两名成员 —— 端到端归 P4/主机侧**)· **分层存储 / 记忆库 / 存储清理 ✅** · **记忆面板 浏览/编辑/删除/溯源 ✅** · **共享模型 ✅(D52)** · **翻译空间四场景 ✅**(文字翻译 · 同传界面骨架 · 文件翻译含 PDF 预览 · 多语言表)· **回信 ✅**(D61–D63)· **全功能幽灵会话 ✅**(D62)· **Open WebUI 退役 ✅** · **可分发产物 ✅**(`build-client.ps1`)。 **S4 配对审批与设备管理 ✅**(2026-08-03:走主机本地回环管理面 `127.0.0.1:8442`,六词 + 倒计时 + 批准/拒绝 + 设备解除;**是不是主机靠 ping 管理面拿肯定证据**,不靠猜拨号地址)· **可分发产物已实出** `dist/client-pack/`(exe + SHA256 + 版本戳 + 说明,自检门禁认退出码)。 **剩的两项已于 2026-08-04 做掉**(① 主机上批一次真配对 ✅ · ② 出的包在副机上装一遍 ✅)。 **已裁定移走:** i18n 三语 → P7 · 记忆面板接 P3a S5 后端 → P4 · D45 成员轴端到端 → P4 · 主机自识别按 D36 → P7 · 安装器 → P7/P11 · 桌宠 → P8(D76)。 **其余**五个**工作空间(资产 / 课程 / 电脑控制 / **财务管理** / 投资研究 —— 用户裁定**等这三块搭好再回来**;清单的单一事实来源是 `Views/Workspaces.cs`,现为**七个**)按用户裁定等这三块搭好再回来 |
| **P4 GPU Broker + 仲裁** | 🔵 **进行中(S0–S16b 落地;★ 模型已接入、内网同步已通;★ 按需装载已接动作;★ D87③「压力即让」已裁并落地 = D99)** —— 形状裁定 = **D83**(包见 `decision-packets/p4-broker-shape-2026-08-04.md`,已并入):**租约不挺过中枢重启 ⇒ 进程内单写者**(租约的意义就是"持有者死了就过期",而重启意味着每个持有者都已经死了;收益是 gpu-broker 的断言继续留在 CI 快层)· **Broker 直接进 `gateway.py`**(复用已被断言守住的 `ROUTE_TIERS` 反向全表/档位判定/成员表主体解析,并结构性保住「预览与准入必须是同一段代码」)。 **已落地五片:** **S0 闸复活 ✅**(★ 唯一的生产集成自 2026-07-28 集成起就是坏的:cp936 下连**通过**路径也抛 `UnicodeEncodeError` 退出码 1,而 `start-stack` 只看退出码 ⇒ 一直"拒绝启动"或靠 `-Force`,**而 `-Force` 把三道闸整个跳过**;修法=编码双保险 + **退出码三态**(2=闸没跑起来,`-Force` 不能覆盖)+ 单次采样 + 未登记组件显式化;顺带更正 `8b@8k` peak 5.0→**5.31**,原值是 NUM-1 同行的**权重列**)· **S1 `gpu.*` 架构反向全表 ✅**(**赶在工具池存在之前**写 —— 否则规则只活在文档里,新增注册表那次提交不会被拦)· **S3 Broker 骨架 ✅**(单写者 · 世代号 · 只读快照 · 1 Hz 采样;`GET /v1/gpu/snapshot`)· **S4a 三集合动态闸 ✅**(`free − incoming − Σpeak(reserved \ loaded \ 请求集)`;★ 双重差集是承重的 —— 塌成一个集合会让**两个客户端双双获批**,即 D37 ④ 的竞态从关它的参数里重新打开)· **S4b 租约账本 ✅**(kind = **两根正交轴**、新 kind 默认**不可驱逐**、**惰性过期不设收割线程**、续租条件写且 `NOT_HOLDER`/`EXPIRED` **可区分**;**`/v1/session/end` 终于存在** —— 客户端此前每次退出都在调一条网关里不存在的路由且吞掉失败)。  **本晚续落五片:** **S2 词表桥 ✅**(桥放网关侧;每个别名**必填** `components_any_of`,零 GPU 别名**必填** `no_gpu_reason` —— 不给"留空 = 不需要显存"这条默认路;顺带暴露两个**从没被任何表登记过**的别名 `speech.lite`/`speech.full`)· **S5 推送非轮询 ✅**(`GET /v1/gpu/events` SSE + `POST /v1/gpu/lease` 带 `if_generation`,不匹配回 **409 + 全量快照**;★ 开工前先**实测**了 lan-edge 能不能流式直通 —— 量到 411ms,能直通,**不必退回轮询**)· **S6 硬件指纹 ✅(GPU 半边)**(不一致 → 拒绝并说清**要重测什么**;★★ 显示半边**明写未标定并拒绝回填** —— `gpu_uuid`/`vbios` 是当天**补录**的,只能检测 08-04 之后的换卡,07-27~08-04 之间若换过同型号卡我们不知道,**而且不假装知道**)· **S7 四集合不变式 ✅**(I2 保留**蕴含**形态 —— 写成双条件会造出「永久违反、告警无法消解」的状态,逼系统自动改写锚点,**恰好违反 D10 的「不做自动触发」**;I3 **无状态前件**,是准入白名单的**运行期**版本;I4 电源轴与意图轴分离,`set_power` 绝不碰 `intended`;`RECONCILE_WATCH` **只报告不修复**。★★★ `actual_resident` **今天不是独立观测** —— 没有装载器 + WDDM 不暴露逐进程显存 ⇒ 它就是 Broker 自己的账本,故 I2/I3 的 `confidence` 标 `self_reported`,**不标就是个假检测器**)· **S8 事务语义 ✅**(★★★ 拒绝写出假事务:装载器不存在 ⇒ `_loader is None` 时在**预检阶段**失败关闭落 `loader_absent`,**绝不到达 READY** —— 空实现会让每次事务都"成功"、四集合全等、I2 永远绿,**而显存里一个字节都没有**;状态机是**白名单**,未登记的转换与源状态一律拒绝且**抛异常**;`DEGRADED_SAFE` 终态只能靠人重开电源轴离开;`RECONCILING` **必须继续服务**;失败落点恒在 AI 侧但 **`guarantee` 恒为 False** —— WDDM 不按优先级驱逐,**这是策略不是保证**;`admission_guard` 全程**不看进程名**)。  **S9 客户端半边 ✅**(那两件「未做」当晚做掉了):**① 显存条改读中枢 ✅** —— ★ 此前它在**副机上显示的是另一台机器的显卡**,标签却只写「显存」,而两台机器的数字长得一模一样、**没有任何地方能看出来看错了**;现改为显式三态(`Hub`「中枢显存」/ `LocalFallback`「**本机显卡(不是中枢的)**」/ `None` 整条隐藏),★ **退回本机不是「精度差一点」,是【换了被测对象】,所以必须改名字**;★ 中枢连着但它自己没采到 ⇒ **不退回本机**(否则「中枢采样器坏了」会显示成「一切正常」)。走 SSE 订阅(D37 ② 推送非轮询),**心跳是判活判据**(没它,死掉的长连接与「一直没变化」长得一样)。· **② 组件挑选面板 ✅** —— S8 的事务语义有了界面落点;清单由中枢下发,★ **取不到清单时什么都不列**(列本地兜底 = 把自造清单当成中枢的真实清单);★ 两种撞墙**分开说**(撞预算可调预留 / 撞实时调了没用);★★ **今天点确定必然得到 `loader_absent`**(中枢还没有装载器),面板如实说「没有生效」,不假装模型装上了。· **③ 第三套词汇删干净 ✅** —— `ModelCatalog.All` 那六个 key(chat.8b/speech/image…)跟网关别名与显存组件 id **一个都对不上**,连同 `IsModelEnabled`(一个**没人读**的假开关)一并删,断言反过来钉住它们不在了(**D84**)。· **④ 服务端补三处 ✅**:`GET /v1/gpu/components`(目录 = 准入白名单**本身**,不过滤、不藏装不下的)· `POST /v1/gpu/intended`(失败绝不回 200)· `finish_startup()` —— ★ Broker **从来没离开过 STARTING**,事务路径此前一次都没走通过。 **★ 仍待裁(不挡后续):** `desktop_floor` 统计量(P99 vs max)· 驱动口径 · `unregistered-local` 算不算出境 sink(D81)。  **★★★ 2026-08-05 凌晨续落四片,产品第一次真的能用:** **S10 权限档位六元组 ✅** —— 清单上那条明确条目(从 P2 移来)。★ 开工前实测到一个洞:GPU 面**每个端点只有一行档位检查**,于是 §6.8 明文「绝不放行」的 `denied-account` **能读快照、能把驻留集合清空、能申请 10^9 秒的独占租约**。根因是**两条路径各写各的档位判断**(chat 那套只长在那一条路径上)。修法:能力做成**一张表**,并断言 GPU 面不得再有散落比较。★★ 补齐**参数维**(`components == []` 被映射成**另一个动作** `unload_all` —— 它与普通变更走同一端点、HTTP 上长得一模一样,但意思是「卸掉全部」;ttl 上限;独占租约不给副机)与**额度维**。 · **S11 接入模型 ✅** —— ★★★ **这个项目第一次能被说话**。开工前实测确认「断的只有客户端一环」。端到端:`text/event-stream` · 首包 **2912ms(冷)/ 约 1s(热)** · 276 帧。用户实机确认「字是逐字的」。token 预算截断**估高不估低**(估低撞窗口、用户只看到「它忘了前面」;估高只是少带两条 —— **两个方向代价不对称**),截断了**在会话里明说**。 · **S12 行为底线提示词 ✅** —— ★★★ 实测发现**模型第二轮就在编**:问「你记得我是谁吗」答「当然记得啦!我们之前聊过天气…」,而那段「之前」根本不存在。根因是全链路一句提示词都没有。⇒ 底线注入在**中枢**(客户端可以漏发,**一条靠每个调用方自觉带上的底线不是底线**);★ 只写行为不写人设 —— 混在一起的话调人设时会顺手把底线一起改掉。 · **S13 内网同步 ✅(D86)** —— D52 那句「真正的上传/同步要等中枢接入(P4+)」到期。★ 用户裁定三条逐条实测:**只同步家庭/共享的**(判据在**服务端** —— 「把私人东西推到另一台机器」**不可撤销**,不能只靠客户端把关)· **实时**(SSE,真 HTTP 实测 **24ms**)· **后到的赢 + 保留被覆盖的**(绝不静默丢)。★★ **未同步必须看得见**:主机不在线时本地照常改,但明说「别的设备现在看不到」;推不上去**不丢**,连上自动补推。 **★ 仍待裁 / 未做:** 家庭→个人**降级**时中枢那份不删(已记 `DowngradedWhileShared`,待裁)· 冲突提示只到「记下了」,**界面还没显示**· **两台机器的实机同步验证还没做**(本夜是单机模拟)· `desktop_floor` 统计量 · 驱动口径 · `unregistered-local` 算不算出境 sink(D81)。 **➡️ P4 的按需装载(S16-b)已于 2026-08-06 夜接了动作**(V2 车道 `dd06af3`,包 `gpu-identity-ondemand-2026-08-06.md`):
`POST /v1/gpu/intent`(意图即起)+ **transient 平面**(结构上够不着 `committed`)+ 空闲即卸三条硬条件 +
组件面板那一列「允许按需装载」(D90 裁定①要求的那次授权,只有主机能写)。
同批收口审计 B 级六条:**B1** 自报 holder(桶 key 可绕 + 名字会印进「正在跑」对话框)· **B2** 一台副机可点名释放另一台的全部租约 ·
**B3** 同一客户端两套身份(★ B1/B3 **同一提交**,因为 `/v1/session/end` 横跨两者)· **B4** 续租吃变更配额(钉它的断言是恒绿的假断言)·
**B5** `lease_count` 不过滤过期(fail-open,而它是「idle 可不可信」的开关)· **B6** 按需授权缺主机闸。
★ **D87③「显存压力即让」已于 2026-08-07 裁定并落地(D99,V8 车道 `2f139c4`)** —— 上一版这里写「仍未做且有意留空」,那是 D90 当时的状态。★ D99 **比 D87③ 多一条恢复路径**(暂停 → 在任务进度里再开 → 显存允许才可用),引用时别退回去引 D87③。
★ **旧说法作废**:下面这段写的「今天后端仍由 `start-stack.ps1` 静态启动,所有事务的终点是 `loader_absent`」
**在 S14(2026-08-05)那天就已经不成立**,留着是因为它记录了当时的判断。它一接上,S7/S9 里那两处**今天是恒真式**的判据(`actual_resident` 独立观测 · `finish_startup` 的放行条件)才从形状变成真检测器。 **建议移出 P4:** B10⑥ 重标定一键流程(它自己就是一站)· D42.6 自动跟随与冲突弹窗(依赖最深) |
| **P3b 身份与准入** | ✅ **实机上线完成(2026-07-29,两台真 PC)** —— S0–S5 全绿,**S6 部分**(★ 2026-08-06 更正:此处原写「S0–S6 全绿」,**过宽** —— S6 验收清单约 25 项、**打勾项 = 0**,实机背书的只有下列四项;拔 WAN 冷启动 · 换 DHCP 免重配 · **流式中**吊销 · 暴露面扫描 · 权限回归 · 拒绝**且告警** 全仓无记录。方案书那一格已同步改为 `[~]`,**两份文档此前的分歧不是靠改勾选消解的,是两边都过宽**)。S5:Edge LAN 绑定(`run-lan <ip>`)+ 交互式配对控制台(六词弹屏 + `approve/deny/list`,`Pairing` 线程安全)+ **提权护栏**(D46)+ 双击启动器。S6 实机验收:**配对**(mTLS+六词SAS,SENIORBIRDS→active/LAN_DEVICE)· **成员调用 200**+Edge 注入已验证指纹(客户端伪造的 X-LocalAI-* 被剥离)· **吊销后 401** · 重配对新证书 active。真 hub = `f6hsduipeesexb6f`(CA 在 TPM;★ **2026-08-06 更正:原写「普通用户等级铸」——不成立**。本机 `EnableLUA=0`(见下方环境事实表),`Policies\System` 键**自 2025-09-08 起未被写过**、早于铸身份约 **11 个月** ⇒ 铸的那一刻就**不存在**「普通用户」上下文,这把 CA 是 **High 下铸的**。D46 的根因叙述在本机不可能成立,见 D46 条目的留痕更正)。回归:identity 48 + lan-edge 8 + client-e2e 6 + client-transport 单文件 5。产物:`dist/2nd-pc/`(第二台 PC 自包含 exe + 配对说明)、`dist/host/`(identity+lan-edge exe + 双击启动器 `启动Edge.cmd`/`重置并铸身份.cmd`)。防火墙:`LocalAI-LAN-Edge` 仅 8443/LocalSubnet/以太网/该程序 |

### ★ P3a 验收签字(§14 四门 · 2026-07-28)

- [x] **eval 通过率达标**:S7 五闸全过 —— 召回 1.0 · 精确 1.0 · **误记 0(独立硬上限)** ·
  **溯源 1.0(硬线)** · **S2 泄漏 0(硬线)**。多闸 AND,实测证明高召回掩盖不了投毒。
- [x] **两条例句**:S1「我妹妹叫小雨」结构化轨答出溯源俱全;S2「上次聊的灯光问题」向量轨
  命中金标只给 passages。均在满载库上复核(S9 演练)。
- [x] **每条可溯源**:溯源完整率 = 100%;每条 trace 含来源·时间·记录设备·原文片段
  (凭证登记类按 §6.9.8 不吐原值)。
- [x] **恢复演练成功**:满载库(承载真实 S0–S4 数据)pg_dump→restore 精确 count 保真 +
  金丝雀回来 + **恢复库上安全性质仍成立**(远程角色读基表被拒、v_memory_nons2 仍不含 S2)。

**回归口径:记忆侧 459 断言全绿**(tainted 64 · route 55 · repo 31 · gate 46 ·
S1 22 · S2 43 · S3 冒烟 25 · S3 50 · S4 35 · S5 27 · S6 15 · S7 15 · S8 19 · S9 演练 12)
+ 网关侧 123 + verify.sql 17 + 账户隔离套件 16。

**六条验收例句(每条一句话说清):**
S1 我妹妹叫小雨 · S2 上次聊的灯光问题 · S3 网页读来的不能自己进库 ·
S4 我家住哪进得了库出不了门 · S5 局域网连过来 S2 读不到本机读得到 ·
S6 冷启动只跑一次录后能改 · S7 四个数实测填阈值误记0即FAIL · S8 改一字节即拒执行局域网碰不到L4。

---

## P2 清单(§14)

| 项 | 状态 | 证据 |
|---|---|---|
| 命名决定(RP ID 前置) | ⚠️ **产物待定** | 机器名 `HONGKONGPINGPON` 保留;tailnet 名 `tail71cfd7.ts.net` 的去留随 **D34 待决项 1** 一并裁定。★ D14a 自称"不可推翻",但其依据是"已注册 passkey 会作废"——而当前**设备数 =1、无 passkey**,故改动代价仍为零 |
| 统一入口网关 + 别名层 | ✅ | 自写非 LiteLLM(D29)。**验收①②实测通过**(见下) |
| 账户隔离 ai-mem/ai-asset/ai-exec | ✅ | 三账户 + memory 目录 Deny ACL,`Get-Acl` 确认真 Deny ACE |
| PostgreSQL + Qdrant | ✅ | PG18 SSPI · Qdrant×2(6333 mem_main / 6335 mem_s2)· 全 ai-mem · 全回环 |
| PG schema(含 sensitivity_domain/crypto_tier) | ✅ | 24 表 · 2 角色 · `v_memory_nons2` · 否定用例含行级 S2 实攻。**验收口径已于 P3a-S2 收紧**:除数 PASS 外,还核对「预期报错」的条数与顺序 —— 正是顺序对不上暴露了 §4.5 铁律失效(见下) |
| **E1 入口凭证检测器** | ✅ | 6 类 + 校验和 · 扫整个载荷 · 流式亦拦 · 带内解除 · 审计不泄露 |
| `secret_ref` 登记表 | ✅ | 随 schema 建,整表 S2,远程角色 SELECT 抛权限错 |
| 网关调用方身份校验(D30) | ✅ | **allowlist(D30 修正,2026-08-03 `f7cda8e`)**:`config/caller-accounts.toml` 是「谁配得上 trusted-local」的**唯一来源**,缺文件/缺字段/空表/通配 **拒绝启动**;表外账户降 `unregistered-local`(不断连,但失去 E1 解除权与 S2 正文权)。denylist 保留为纵深:ai-asset/ai-exec/ai-vigil/ai-ctl/ai-op。★ 改判据的原因是实测到两个未登记的外部 AI 沙箱账户按原判据能拿到 trusted-local |
| embedding / rerank(CPU :18084) | ✅ | 服务 Running · `.\ai-mem` · 自启 · 仅回环 · 1024 维 · rerank 语义正确 |
| 首版界面 Open WebUI | ✅ **已退役(2026-08-03,D77)** | 历史任务完成(把网关端到端验通);入口已改统一客户端。数据已归档进 `{state}\quarantine\20260803-openwebui\` |
| 剪贴板历史/跨设备同步检查 | ✅ | 2026-07-28 复验:两者均关闭。策略层未配置(加固项,非 §14 要求)—— 任何以你身份运行的程序仍可重新打开 |
| **无 Broker 期显存过渡措施** | ✅ **已做**(2026-07-28) | §8.1 三层检查落地:`config/vram-budget.toml`(唯一数据源,peak 全实测)+ `10-core/gpu-broker/vram_gate.py`(38 测试)。接进 start-stack。拒绝归因到具体闸并带数字;两种撞墙给不同建议。**同时是 P4 Broker 的判定内核** |
| ~~WebAuthn 设备身份~~ | ❌ **作废**(D34) | 非本机设备上不再有浏览器 ⇒ RP ID 不可变陷阱整类消失。局域网身份改走 **mTLS**(P3b) |
| **权限档位(六元组)** | ➡️ **移至 P4** | D37 让它有了第一个真实客户:**谁能改 GPU 状态**。不再是悬空欠账 |

### §14 P2 验收标准逐条

| 标准 | 结果 |
|---|---|
| 本机可用别名对话 | ✅ `assistant.fast` → 「德国的首都是柏林。」· 契约头 · 流式均通 |
| 换别名映射,客户端零改动 | ✅ 客户端代码逐字未动,契约 `8b`→`8b-remapped`,对话照常 |
| 非授权账户连接记忆库被拒**且告警** | 🟡 **部分**:被拒✅(三层实攻);**「告警」仅落审计文件** —— 告警通道在 **P7 客户端 v2** 闭合 |

---

## P3a 记忆系统(项目核心里程碑)

| 步 | 内容 | 状态 |
|---|---|---|
| **S0** | schema 加固:append-only · supersede 方向 · tombstone/隔离区 · 分级需票据 · 禁批量确认 | ✅ |
| **S1** | 最小可用:`tainted.py` 类型层 → `repo.py` 唯一写模块 → `gate.py` 唯一写入口 → `route.py` 纯函数选路 | ✅ 验收 22/22 |
| **S2** | 向量轨 + 双 Qdrant + **编码指纹** | ✅ 验收 43/43 |
| **S3** | 完整记忆闸(分级 · E3 · 队列 · 熔断 · 冲突) | ✅ 验收 50/50 |
| **S4** | 隐私三层 + 账户隔离 + 日志纪律 | ✅ 验收 35/35 |
| **S5** | 记忆面板**后端 API**(浏览/编辑/删除/溯源/标密)—— UI 移 P3c(D36) | ✅ 验收 27/27 |
| **S6** | 冷启动初始化会话(只跑一次)+ S2 自动分级 | ✅ 验收 15/15 |
| **S7** | 评测集 + 四指标 + 阈值标定(最小可交付达标集) | ✅ 验收 15/15 |
| **S8** | L4 程序记忆独立写路径 + 哈希绑定批准 + executor | ✅ 验收 19/19 |
| **S9** | 满载库恢复演练 + P3a 验收签字 | ✅ 演练 12/12 |

**★ P3a S0–S9 全部完成,验收签字见本文件顶部。**

### ★ P3a 全程揪出并修掉的「看着有防护、实际没有」(累计 ≥16 条,均实测可证)

| # | 缺陷 | 修在 |
|---|---|---|
| 1 | §4.5 铁律因 `is_user_fact` 要求 sc≥1.0 而保护范围为空 | S3 权威序 |
| 2 | 「强制 pending」无强制点 —— web_content 可静默成为活跃 L3 | S3 分流 |
| 3 | 两套状态列,CHECK 守着没人改的那个 | S3 统一 status |
| 4 | `pr_derived_forced_pending` 把派生候选**永久锁死**(死锁 = DoS) | S3 改 BEFORE INSERT |
| 5 | 拒绝审计与被拒的写同事务 → rollback 一起抹掉 | S3 独立连接 |
| 6 | **域S2 是空集** —— 凭证与机密被合并,S2 分支是死代码 | S4 概念分叉 |
| 7 | `TaintedText.__eq__` 是**不记账的猜测-确认预言机** | S4 改比句柄 |
| 8 | `unseal_for_prompt` 判 sensitivity 而非 egress → S0 记忆静默出境 | S4 改 Backend 契约 |
| 9 | **PG 服务器日志**写入正文片段(触发器带值 + 失败语句) | S4 去值 + 钉死 config |
| 10 | `caller`/`egress` 裸值 = denylist,新档位/新别名默认放行 | S4 枚举 + allowlist |
| 11 | `unseal_for_client` 唯一调用点硬编码 TRUSTED_LOCAL → S2 隔离退化为「信任调用方诚实」 | S5 档位由调用方传入 |
| 12 | 编辑复用 submit 会**并列存两条活跃矛盾行**(直写不退休旧行) | S5 显式 supersede |
| 13 | 溯源缺原文片段 + 凭证登记类会吐原值 | S5 补 snippet + kind 分支 |
| 14 | 删情节不删向量点 / 标 S2 不迁向量 → 悬空命中 / DB 声称 S2 正文留 mem_main | S5 连带删/迁 |
| 15 | 向量轨**无最低分闸** → 负例精确率结构性无下限 | S5 实测标定 0.20 |
| 16 | L4 在 DB 层零门槛 · 签名可空 · 无哈希复核基准 · 「默认排除」可翻转措辞 | S8 应用层门槛 + CHECK + 批准链 + 结构性排除 |
| 17 | quarantine 排除只做了文件系统一半,DB 表随整库进不加密备份 | S9 M26 排除表数据 |
| 18 | 备份指纹用 `n_live_tup` 估计值,空库守卫只挡表数=0 | S9 M27 精确 count |

> 这一族缺陷贯穿 P3a,形状都是**判据写成放行优先,新增的东西默认自由**
> (provenance denylist · E1 override · unseal caller · egress 缺字段 · append-only 新列 ·
> L4「默认排除」)。已成为本项目固定审查视角:**任何白/黑名单,先问「加一个新值,默认落哪边」。**
>
> ★ 另一条反复出现:**我自己修一个洞时引入下一个**(is_user_fact 修法引入低档覆盖高档;
> classify_sensitivity 改语义漏了 submit_fact 调用方)。教训:改判据必须重跑全部调用方,
> 改幂等脚本必须重验幂等。这也是每步都留架构断言 + 全量回归的理由。

### ★ S2 期间揪出的三条「看着有防护、实际没有」(均非新引入)

1. **§4.5 铁律的保护范围实际为空** —— `is_user_fact` 要求置信度 ≥1.0,而 §4.4.2 规定 1.0 必须有
   `panel_ticket` 背书 ⇒ 正常写入的用户话语一律 <1.0 ⇒ 都不算「用户事实」⇒ 任何自动来源都能
   覆盖用户亲口说的话且不报错。已改为**只看 provenance**,并把 `panel_ticket` 纳入人类权威
   (否则用户永远无法修改自己的记忆)。
2. **append-only 误绑 `l1_session_summary`,该表被变成不可用** —— L1 是滚动压缩摘要,按设计就要改写;
   且它没有 `superseded_by` 列,拒绝时给出的建议根本无法执行。已移出绑定清单。
3. **tombstone 可被一条 UPDATE 撤销** —— `redacted_at` 非NULL→NULL 畅通,与「悄悄复活旧值」同源。
   已加单向约束;恢复路径应为**从隔离区新增一行**。

> 这三条能藏住,是因为 `verify.sql` 的夹具在 S0 加固后插不进去,用例匹配 0 行 → 不报错 → 伪装成通过,
> 且金丝雀行消失后隔离断言报出的是「远程读不到」——**像安全告警,实为测试自身损坏**。
> 已加 **C0 夹具就位断言**(放在所有隔离断言之前),让两类失败长得不一样。

---

## ★ 2026-07-28 架构改向(D34–D41)

**一句话**:取消"全功能远程接入";能力按**是否装了客户端的内网 Windows PC** 两档划分;
人在外面走**外联文本通道(本期 Signal)**;桌宠是 Vigil 的形体。

| | 全功能端 | 聊天端 |
|---|---|---|
| 是谁 | 内网 + Windows + **装了客户端** | 手机及其他一切设备 |
| 能干什么 | 全部 | 只有外联通道的文本/语音消息 |
| **断网时** | **照常用** | 用不了 |

**能力边界与信任边界重合** —— 不必再为手机设计权限模型,它物理上只有一条文本通道。
路由器上**永不开任何入站端口**。

**新增/重定义的阶段**(详见 v3.0 §14):
P3b 局域网身份与准入 · P3c 统一客户端 v1(唯一入口)· P3d 外联通道 Signal · P11 产品化(条件触发)

**★ 三条必须先于对应阶段落地的硬前置:**
1. **网关绑 LAN 与 LAN 上有 TLS 是同一个不可分割的交付项**(P3b)——
   审查确认全仓库零 TLS 资产,而 WPA2-PSK 无前向保密。**做不出 TLS 之前继续只绑回环。**
2. **响应侧出境闸**(P3d)—— L5「记忆内容禁止出境」在代码里**从未实现**;
   E1 只扫入口,非流式与流式两条回程分支都无扫描点。
3. **A7-pet 实测**(P8)—— `desktop_floor` 的新值不得估算。

---

## 下一步

> ★ 2026-07-31 更正:原「下一步」写的是 **P3a S3**,而 P3a 早已整体验收通过(见上表)——
> 那是一段过期的指路牌。以下为当前为真的下一步。

**★ 2026-08-04 更正:P3c 已验收通过**(签字见 `decision-packets/p3c-signoff-2026-08-04.md`),
上一版这里写的「只剩两项要跑实机」已完成 —— 又一段过期的指路牌。按优先级:

1. **★ 两台机器的实机同步验证** —— 唯一需要人在两台电脑前做的事
   > ★ 2026-08-05 更正:上一版写「P4 清单剩下的两条」,其中**六元组已落地(S10)**。
   > 这个文件的「下一步」到今天为止已经过期/写错**五次**,所以这一版只写
   > **必须有人做**的和**已经确知的**,不写推测。

   本夜(08-05 凌晨)把内网同步做通了,但**验证是单机模拟副机推送**的:
   用 HTTP 直接扮演 PC-B 推数据、订阅端确认收到(实测 24ms)。
   ★ **没有人在两台真机上做过一遍**。要验的三句:
   · 副机加一条**家庭**待办 → 主机看得到;个人待办 → 主机**看不到**;
   · 副机把会话**提升为共享** → 主机看得到整段对话;
   · **主机关掉**时副机照常改 → 界面显示「★ 有 N 项还没同步 —— 别的设备现在看不到」→
     主机开回来后**自动补推**。
   > 这与 P3c 那次的教训同源:S4「✅」曾经是走**控制台**验的,
   > 图形界面那条路径**从来没走完过**,直到 08-04 才发现。

2. **按需装载(S16-b)—— P4 清单最后一条 · 🟢 已接动作(2026-08-06 夜,V2 车道 `dd06af3`)**

   > ★ 2026-08-06 更新:**D90 已裁**(见 `DECISIONS.md` D90 与 `decision-packets/D-ondemand-loading-2026-08-06.md`)。
   > 裁定的形状是**禁令的主语收窄**,不是禁令被推翻 —— §8.1.7 三格禁止列**原样有效**:
   > 自动装卸只许动 `permitted_on_demand`,`committed`(你勾选并点确定的那一份)**一个字节都不许自动改**。

   **基座(2026-08-06 白天)**:装载器本体接通(S14,`actual_resident` 终于是独立观测)·
   客户端真的持 `client_session` 租约 + 网关 `POST /v1/gpu/lease/renew`(`LeaseKeeper.cs`)。
   ★ 后者是承重的:没有它,「任何一台都没有活动」这个判据是**恒真式** ——
   10 分钟后会把**正在打字的人**卸掉,而那正是 D87 补裁⑧(计时器是主副机**共享的一个**)要防的事。

   **接动作(2026-08-06 夜)**:
   · **意图即起**(D87①)—— `POST /v1/gpu/intent {alias}`,一条端点同时是"起"的触发点、
     "我在用"的心跳锚点、和 `model_ref` 租约的发放口。客户端的触发点是**聊天输入框的第一次敲键**
     (`ChatView` 一行 `NoteIntent("assistant.fast")`,去抖 20s);★ 客户端**只点别名不点组件**。
   · **transient 平面** —— 自动路径 `request_on_demand` / `sweep_idle_transient` 的源码里
     **没有任何对 `_committed` 的写**,由反向断言逐字扫;★ 并用 `apply_intended` 验证过
     那条正则**真的会响**(否则它是个永远不响的探测器)。
   · **空闲即卸**(D87②)三条硬条件:组件级空闲 ≥ 阈值 · `blocking_leases()` 为空(D90 裁定②,
     **硬条件**不是"提醒后仍卸")· 目标在 transient 平面里。收割**搭在采样循环里**,不另起 task。
   · **那次必须存在的授权** —— 组件面板新增一列「允许按需装载」,只有主机档能写(审计 B6 补的闸)。

   **★★ 一处必须记住的更正:`idle_seconds` 不能当放行条件。**
   D90 包 §4.4 建议的条件① 是「`idle ≥ N`」,而它被客户端心跳(TTL/3 ≈ 30 秒)刷新 ——
   只要有任何一台客户端活着,它**永远到不了 10 分钟**。
   ⇒ 拿它当条件是把一个**恒真式**换成一个**恒假式**,两者都不是检测器。
   改看逐组件的 `transient_idle_s`(中枢上的一份,任何一台机器的意图都刷新它)。
   `idle_seconds` 保留为**纯观测量**,并有断言钉住没有任何动作读它。

   **★★ D87③「显存压力即让」** —— 上一版这里写的是「没做,而且是有意的」(D90 明写它仍需单独裁)。
   **2026-08-07 用户已裁并落地(D99,V8 车道 `2f139c4`)**,原文保留在这里是因为它记录了当时为什么不能做。
   现状:**判据 = NVML free 连续 3 次低于 1.5 GiB**(不猜别的进程要多少 —— WDDM 不暴露逐进程显存)。
   ★★ 它与 D90 空闲回收是**两条独立路径,不许合并**:
   D90 问「我们自己还要不要它」⇒ **正在跑的不动**;D99 问「别人要不要显存」⇒ **正在跑的也让,但必须弹提示**。
   每次自动卸载都带 `unload_reason`(`idle_reclaim` / `vram_pressure`)——
   合成一条之后**没人说得清某次卸载是哪条规则干的**,而那正是本项目最贵的那种状态。
   ★ 让谁:**按 peak 从大到小,与 kind 无关**(写成 kind 白名单的话,新增组件会默认落在「不让」那边,
   于是压力来了让不出东西而日志里什么都看不出来);有一条**正面**断言验 speech 组件确实会被选中。
   ★ 只动 transient 平面;**不撤销任何租约**(I1 只允许拒发新的)—— 让位动的是显存,不是别人手里的凭据。
   ★ `admission_guard()` **仍然只报告降幅、不触发卸载**:它是**降幅**规则(5 秒掉 1 GiB),
   与 D99 的**水位**判据不是一回事,两者并存。

   **两条 D90 未决项的处置见 [待决事项](#待决事项不得默认须显式裁定) 第 9/10 行 —— 落地了,但裁定权仍在用户。**

   ★ 落地时的**结构性约束**(D90 裁定③)已照做:自动路径**不复用 `apply_intended`**
   (它第一行就写 `self._committed = list(requested)`),是结构上**够不着** `committed` 的独立路径,
   并配反向断言。只写注释 = 又一个「看着有防护、实际没有」。

   ★★ **两处恒真式的现状(逐条,不含糊)**:
   · `finish_startup` 的放行条件 —— S14 接上装载器之后**已经不再恒真**(`actual_resident` 是独立观测);
     本次只把它的作用域与 I2 对齐(同样减掉 transient),**判据本身没动**;
   · 空闲判据 —— **本次换掉了**:从「全网 `idle_seconds`」(恒真 → 接上租约后变恒假)
     换成「逐组件 `transient_idle_s`」,并有正反两面的断言(没空够久 / 有租约挡着 / 常驻永不入选)。

3. **几条小的(都已记账,不会丢)**
   · 家庭→个人**降级**时中枢那份**不删**(删了会让另一台上的条目凭空消失,
     而那台的用户没做过任何事)—— 已记在 `TodoCenter.DowngradedWhileShared`,**待裁**;
   · 冲突提示只到「记下了」,**界面上还没显示**「这条被另一台改过」;
   · 助手的**人设**(名字、语气)—— S12 只写了**行为底线**,人设是用户的裁定,故意没碰。

   **仍待裁(不挡开工)**:`desktop_floor` 重测取哪个统计量
   (A7 协议写 P99,而定出 6.53 的那轮 n=64,**P99 必然退化为 max**)· 驱动口径 ·
   `unregistered-local` 算不算出境 sink(D81 的五条待裁之一)。
2. **方向 B(响应回程出境闸)** —— 方向 A 已于 2026-07-31 落地,方向 B 仍是空的。
   ★★★ **2026-08-10 更正**:此处原文写「它同时卡三条线…做一次解锁三处,按性价比可以排在 P4 之前」
   —— **那是一句记错的话,复核已做完**(`decision-packets/egress-b-gates-impact-2026-08-06.md`
   §2 两道闸签名互不相交 · §7 判据表):**实际只解开一件**(P3d)。
   · 「让 AI 自己上网干活」要防的是**读回来的网页里藏指令**,不是靠认凭据的扫描器能拦的;
   · 「桌宠说记忆」走的是**另一条门**(回客户端那条),而且 v1 的猫**结构上不会开口**
     (意图枚举里没有 `Speak`,方案书 :2644-2646)。
   ⇒ 它就是 P3d 自己的前置,**按 P3d 的优先级排即可**;而「排在 P4 之前」这半句
   今天更没有意义 —— **P4 已于 2026-08-09 实机复验完**。
   ★ 这条是 D81 五条待裁里的第五条,**它不需要裁,它需要有人回来划掉** —— 现已划掉。
3. **同声传译接语音链路**(采集 / ASR / 合成 / 虚拟麦注入,VB-CABLE 路线)—— 界面骨架已成形;
4. 其后:其余工作空间(用户裁定等主页/聊天/翻译三块搭好再回来)。

> ★ **P3c 关闭时仍挂在别处、不由 P3c 负责的**:主机自配对(**封存**,判 DO_NOT_COMMIT,
> 需 core 补回环 self-enroll)· 记忆面板接 P3a S5 后端(P4)· D45 成员轴端到端(P4)·
> 主机自识别按 D36 原文(P7)· i18n 铺满正文(P7)· 真正的安装器(P7/P11)· 桌宠(P8)。

> **2026-08-03 · P3c 收尾(D77,决议见 `decision-packets/p3c-closeout-2026-08-03.md`)**
> · **i18n 中日英整条移到 P7**(用户裁定):骨架留在 P3c,铺满 2275 条硬编码正文随 P7;验收第四句一并移走。
> · 补齐:记忆面板的**编辑**与**溯源展开** · **协议版本协商**(客户端半边)· **改连接地址** ·
>   **Open WebUI 退役** · **可分发产物** `90-ops/build-client.ps1`(自检不过不出包)。
> · 验收③「换路由器自动重新发现」与 D43(DNS-SD 推迟 P3b.2)打架,**修订为**「换网段能改地址且配对不失效」。
> · 仍欠、且**不由 P3c 关**:记忆面板接 P3a S5 后端(等记忆服务化)· D45 成员轴端到端(「仅本人」目前挡不住同机另一成员)·
>   主机自识别按 D36 原文重做 · 真正的安装器(P7/P11)。
> · 桌面宠物**归 P8**(PLAN 前置 = P6 权限区域 + A7-pet 实测);其逻辑层已在 P3c 期提前落地,不计入 P3c 完成度。
> · 客户端自检:**PASS=1566 FAIL=0**(P3c 开工时 30 → 收尾 1566)。

日常使用:数据库四件套是 Windows 服务(开机自启,不用管);**llama 后端与网关不是服务**
(按设计由 P4 的 GPU Broker 按需装载)。

**★ 2026-08-07(D101)起,「用之前先跑 start-stack」这句要分主/副机看:**

- **主机上、且客户端是 `36173ab` 之后出的包** ⇒ 客户端启动时按 `HostSetup.DecideBoot`
  **自动起 gateway(`127.0.0.1:8080`)+ lan-edge**,**不必再手跑**。
  ★★ 两个前提**缺一不可**:这台上有**仓库源码** + `<AI 根>\venvs\gateway`;
  且客户端 exe 旁边有 `..\host\localai-lan-edge.exe`(见下方「怎么装」那条约束)。
- **副机上** ⇒ **这条命令根本跑不了**(没有仓库、没有 venv)。副机只连主机,不起任何东西。
- **手跑的场合仍然存在**(没装新包 / 前提不满足 / 要指定 `-Ctx`):

```
powershell -ExecutionPolicy Bypass -File "E:\.meine\.Proj_Soft\.Proj\.localAI\90-ops\start-stack.ps1"
```

> ★★★ **为什么必须分开写**:在「主机上装了新包、开机点开客户端」这个场景下,
> 网关与 Edge 已被客户端起过 —— 人照旧文再跑一次,uvicorn 撞 8080 起不来,
> 脚本按自己的探活逻辑判「网关未就绪」→ `Stop-All` ⇒ **把已经在跑的那套一起收掉**。
> 旧文在**两个方向上都是假的**(主机上多此一举且有害,副机上根本跑不了),而它没有分主/副机。

日常入口是**统一客户端** `dist\client\localai-client.exe`。
> ★ **本机磁盘上现在有两份包,路径别指错**:
> `dist\client\`(主机自己那份,当前 **`20260807-1942+4e5c258`**)·
> `dist\client-pack\`(可分发那份,当前 **`20260807-2036+bfe52e7`**)。
> **两份都已含 V9 自动起栈**(它随 `36173ab` 并入);分发与副机实测请用 `client-pack`。
> ★★ 版本戳会随每次出包变 —— **以 `dist\<目录>\VERSION.txt` 第二行为准,别信这里抄的数**;
> `doctor.py` 第 ⑨ 环会比提交号,落后 HEAD 时当场报出来。
★ Open WebUI 已于 2026-08-03 退役(D77),数据归档进 `{state}\quarantine\20260803-openwebui\`;
`90-ops/install-openwebui.ps1` 仅作历史保留,**不要再跑**。

~~**P3a 之前必须先解决的**:活数据库的备份方式~~ → ✅ **2026-07-28 已完成**。（无 Broker 期显存闸也已做:vram_gate.py + config/vram-budget.toml,见上表。）

---

## 阻塞

| 事项 | 阻塞什么 | 需要什么才能解 |
|---|---|---|
| ~~活数据库的备份方式~~ | ~~P3a~~ | ✅ **2026-07-28 已解除**:逻辑转储落地 + 每次备份自证可恢复 + 金丝雀演练 PASS + 真机备份跑通(G:) |
| ~~上行带宽未测~~ | ~~P3b 手机端体验~~ | ✅ **2026-07-28 作废**:手机不再有全功能路径(D34),外联通道是文本,带宽无关 |
| ~~**★ LAN 命名 + 证书方案未定**~~ | ~~P3b 全部内容~~ | ✅ **2026-07-29 落定(D43 + S1/S2.1 实证)**:命名 `localai-<hub-id-short>.local`;应用自有 mTLS,CA 私钥落 TPM 不可导出;TLS 栈 = Kestrel + .NET/CNG。回环 spike + 签发核心已在真机验通 |
| **★ 出境闸:方向 A 已做,方向 B 待做** | **P3d 外联通道** | **★ 2026-07-31 更正**:07-30 这格原本写「已落地 … test 13 + gateway 套件 71 全绿」——**那是假的**。`e4_egress.py` 从未入库(git 全历史无此文件),而 `gateway.py:28` 的 `import e4_egress` 是真的,于是**整个网关一天都起不来**(`import gateway` → ModuleNotFoundError,6 个测试文件里 4 个连收集都过不去,`start-stack.ps1` 必失败)。根因:07-30 的 commit 4f49a63 把 gateway 挂点夹带进一个客户端提交,模块本体没 stage。<br>**07-31 已补齐并实测**:`e4_egress.py`(E1 检测器的薄封装,**签名里没有 override 参数** —— 出境的放行能力在结构上不存在)+ `test_e4_egress.py` 18 条。gateway 全套 **233 PASS / 0 FAIL**(2026-08-03 实跑 10 个文件:caller_identity 10 · caller_policy 20 · e1 50 · e4_egress 18 · egress_registry 12 · gateway_e1 36 · imports 6 · lan_edge_policy 10 · local_only_registry 69 · no_proxy 2 —— 增量来自调用方判据改 allowlist 与 local_only 两组新测试)。**仍缺方向 B**(响应回程闸:上游返回 / P3d Signal 出口的扫描点)。**接外联通道之前必须补方向 B** |

> P1 阶段的全部阻塞(语音栈选型 · C4 语料 · llama.cpp · 桌面基线)**均已解除**。

---

## 待决事项(不得默认,须显式裁定)

| # | 事项 | 判据 / 影响 |
|---|---|---|
| 1 | ~~**Tailscale 留不留**~~ | ✅ **2026-07-29 裁定=移除**(D43 S0.4)。用户答"不会带笔记本出门用全功能"⇒ LocalAI 命名/发现/CA/配对/撤销/可用性一律不依赖 Tailscale,决策包 §9 transport 作废。外联仅走 Signal |
| 2 | ~~宠物渲染选型~~ | ✅ **2026-08-03 裁定 = 纯像素逐帧 6 fps(D76)**,D40 补记的「默认 DragonBones」作废。连锁解掉:素材解析器攻击面归零 · Live2D/Expandable 商业化风险线消失 · AIRI 报告 §8 建议 2(自写 MFCC→嘴型槽)随之作废。A7-pet 的实测对象因此定为「逐帧位图 + WPF 分层透明窗口 + DWM 合成」—— ★ 注意那笔钱两种渲染方案都要付,真正的变量只是 atlas |
| 3 | 租约三参数 | TTL / 心跳间隔 / 空闲卸载(实测后定)。★ **压力那两个参数另立第 13 行** —— 它们不是租约参数 |
| 4 | 产品化是否真启动 | 决定 D41 各条何时从"记账"变"裁决" |
| 5 | **主页数据「来源 × AI 权限 × 回滚」矩阵**(2026-07-30 用户提出) | 需为每个主页板块显式定四件事:① 数据**从哪来**(网络拉取 / AI 生成 / 用户输入 / 本地推断)· ② 是否**可直接给 AI 读**· ③ AI **能否直接上手操作**· ④ AI 操作后**如何回滚**(不及预期时一键还原)。★ 这是能力边界 + 出境闸 + 记忆纪律的交汇点,**须先定稿再接数据源**——尤其"AI 可操作"的板块必须先有回滚与审计,否则等于给了无撤销的写权限。待与用户逐板块裁定。 |
| 6 | **★ Agent Worker 的隔离路线三选一**(D65,2026-08-03 · C 于 D65 补追加) | A **容器化**(WSL2/Hyper-V,独立网络栈,出站白名单才能真正生效)/ B **先给 `llama-server` 加鉴权** / ★ C **AppContainer**(**默认拒绝回环**,是 Windows 上不启用 WSL/Hyper-V 也能禁回环的唯一机制,**三条里唯一零提权**;可行性待核实)。★ 判据前提是一条 Windows 事实:**防火墙不过滤回环、账户 ACL 管不了 TCP** ⇒ 把 worker 跑成 `ai-exec` **挡不住**它直连 18081 绕过网关与 E1/审计。★ **DD-16 采纳自带分发后权重变了**:用户要消掉的"自己动手"一大半是**启用 WSL2 那一步**(要管理员、可能重启),不是装 Hermes 本身 ⇒ **若 C 成立,它同时满足"零提权"与 DD-7 隔离**,应优先核实。B 的收益(消技术债)独立成立,**做 A 也应顺手做 B**。见 [HERMES_INTEGRATION_DESIGN.md](HERMES_INTEGRATION_DESIGN.md) §6 |
| 7 | **★ DD-2 的 MCP 端点走 HTTP 还是命名管道**(D65 补,2026-08-03) | 设计图画的是 **MCP over HTTP**;决议包 **D73 裁定层一全链路无 HTTP、走命名管道 + 父子/SID 双校验**。走 HTTP 则引入一条 D73 明令不存在的网络面,须单独按三通道表新开一行补齐 fail-closed,**不得沿用"反正在回环"**。★ **与待决 6 耦合**:选容器则管道跨边界成问题,选 AppContainer/本机则管道自然 ⇒ **两件事一并裁定** |
| 8 | **决议包的处置**(2026-08-03 · **2026-08-04 更新**) | ① `two-layer-mcp-decisions`(**D66–D75**,两层 MCP)—— **封存等 P6**,不并入,到期按其 §4 采纳清单复核;② `negative-cases-N1-N14`(43 条否定用例)—— 同上,随 ① 一起取用;③ `p3c-closeout`(**D77**)—— ✅ **已于 2026-08-03 并入**;④ `worktree-split` —— ✅ **已于 2026-08-04 并入为 D82**(★ 其 §3 车道排期**未并入**:已过期,见 D82);⑤ `p3c-signoff` —— ✅ **并入为 D80**;⑥ `egress-direction-b` —— ✅ **并入为 D81**;⑦ `selfpair-review`(判 DO_NOT_COMMIT)—— **暂不取号**:它是一份"不要提交"的裁定,内容已被 D80/D81 引用,待自配对解封时随实现一并取号 |
| **9** | **★★ 按需装载走 `committed` 还是新开 transient 平面?**(D90 未决②,2026-08-06)<br>**⇒ 2026-08-06 夜:V2 车道已给出处置并落地,【待用户追认或推翻】** | **直接决定 I2/I3 怎么求值。** 走 `committed` ⇒ 自动改驻留集合,**撞 §8.1.7 禁止列第 1 格**;新开 transient ⇒ I2 的 `actual == committed` 会因为多出来的按需组件而**永久违反**,除非把 transient 从 I2 的作用域里排除 —— **那是对不变式本身的修改,要一起裁**。<br>**★ V2 的处置(`gpu-identity-ondemand-2026-08-06.md` §3):新开 transient 平面,并把 I2 的作用域显式收窄为 `actual − transient == committed`。** 理由三条:① 走 `committed` 被 D90 裁定①明文禁掉,且裁定③要求自动路径**结构上够不着**它;② 不收窄会造出一个**永久违反、消解不掉**的不变式,而那会反过来逼系统自动改写锚点(D10 禁的事);③ 收窄的是**作用域**不是把检测器关小 —— 减法**只减 transient 登记过的成员**,野组件减不掉、I2 照样红(正反两面都有断言)。<br>★ 字段名按 D90 裁定③一律 `transient_` 前缀,快照里是**新键** `sets.transient_resident`,既有键含义一字未改;界面上是**两列**(常驻 / 按需)。<br>★ **这是一次对不变式的修改,所以它必须停在这一行等你的字** —— 落地不等于裁定 |
| **10** | **★★ `AutoStartPreset` 与 D87 裁定①「不做开机预热」正面矛盾,留哪个?**(D90 未决④,2026-08-06)<br>**⇒ 2026-08-06 夜:V2 车道已撤掉 `AutoStartPreset`,【待用户追认或推翻】** | 「连上中枢就自动装预设」与「不做开机预热」在同一台机器上不可能同时为真。<br>**★ V2 的处置:撤掉 `AutoStartPreset`(不是继续置灰)。** 三条理由:① 它与 D87 裁定①正面矛盾,而 D90 放行按需装载的**全部依据就是 D87** —— 一边引用新裁定、一边留着与它相反的开关,是拿新裁定当挡箭牌;② 按 D90 裁定①,「连上就自动装」属于**自动改 `committed`** 那一类,**连一条合法车道都没有**;③ 置灰的含义是"这件事有人管、在等某一步",而**它等不到那一步了**。<br>★ 取代它的是组件面板新增的那一列**「允许按需装载」** —— 那正是 D90 裁定①的**代价段**要求的那次授权(「没有它,系统就是在你没同意的情况下自己动显存」)。<br>★ **`AutoUnloadIdle` 留着且仍置灰,理由不同**:「空闲即卸」已落地,但它是**中枢**的策略(计时器主副机共享,D87⑧),做成每台客户端各自的开关正是那条裁定要防的事 ⇒ 它要么变成主机上的中枢设置、要么撤掉,**那是第 11 行**。<br>★ 连带记账:`Views/ModelCatalog.cs` 的 `Presets` 表**从此无读取方**,已在该文件写明为什么不删(D87⑤ 的落点是「模型选择策略」那片占位框,那件事还没做) |
| **11** | **★ `AutoUnloadIdle` 该变成主机上的中枢设置,还是撤掉?**(2026-08-06 夜,由待决 10 分出来) | 「空闲 10 分钟自动卸」(D87②)**已经在中枢落地**,而客户端上那个复选框**仍然没有读取方**、仍然置灰。★ 它不能就这么点亮:计时器是主机与副机**共享的一个**(D87⑧),做成每台客户端各自的开关正是那条裁定点名要防的事。⇒ 两条路:**A** 它变成主机变更面上的一个中枢设置(和「按需授权」同一类,只有主机能写);**B** 撤掉,阈值就写死在中枢。★ **在裁定之前不许把开关点亮** |
| **12** | **★ 那两个 `Merge branch 'main' into <branch>` 提交留着,还是本仓从此接受非线性?**(2026-08-06 收盘) | 本仓历史**此前一直线性**,而本轮出现两个:`8d2d325`(V6)· `4cbfcff`(V3) —— 它们是「把 main 并进分支」而不是 rebase。★ **两条路各有代价,不是洁癖问题**:线性 ⇒ `git log` 与 `git bisect` 上每条提交都是一个可独立复现的状态,而本项目大量结论依赖「在某个提交上实跑出什么数」;非线性 ⇒ 保留了「那条车道当时是基于哪一版 main 干的」这个事实,rebase 会把它抹掉。★ **不建议现在回改历史** —— 那两个提交已经在 main 上,重写会让所有已发布的版本戳失去指向(版本戳里就是提交号,而 `dist/client/VERSION.txt` 已经指向其中一条的后代)。⇒ 要裁的是**从今往后**:是否把「只用 rebase + --ff-only」写成一条纪律。本轮 V4 已按此执行。 |
| **13** | **★ D99 的两个阈值【没有实测支撑】,要不要按 A7 那样量一次?**(2026-08-07 · V8 车道自报) | **`PRESSURE_FREE_FLOOR_GIB = 1.5`** 与 **`PRESSURE_NOTICE_TTL_S = 300`** 都是**推**出来的,不是**量**出来的。<br>★ 1.5 的三条依据是**约束**不是数据:高于 `safety_margin`(0.8,要在桌面撞墙之前让)· 低于最小组件 peak(2.07,否则一让就是全让、还来回抖)· 它是观测阈值不是预算。**而真机上桌面显存波动的分布从来没测过**(P1-A7 那轮测的是占用峰值,不是波动)。<br>★★ **两个方向的代价不对称**:定低了漏让(桌面可能被挤,而 D24 说失败不得落在桌面);定高了**误打断正在跑的任务** —— 后者更贵,因为它是用户看得见的伤害。<br>★ 300 秒决定「副机重连多久之内还能看到那条提醒」,而**副机重连时长也没测过**。<br>⇒ 要裁的是:**现在就按 A7 的形式量一次**(桌面重负载下 free 的分布 + 副机重连耗时),还是**先按推出来的值跑,等真的被误打断一次再回来量**。★ 本车道没有替它裁,已在决议包的「不声称」里写明这两个数是推出来的。 |

### ★ 许可核实(2026-07-28)带回的三条硬约束

1. **`signal-cli` 运行时必然携带 AGPLv3**(`turasa:signal-network` → `org.signal:libsignal-client`),
   而 GPLv3 §13 规定组合体整体承担 AGPL §13 的网络源码义务 ⇒ **产品不打包 signal-cli**,
   安装向导引导用户自取;产品侧只留自己原创的 JSON-RPC 客户端。**绝不 fork、绝不 patch。**
2. **FSF 的判准是双要件:通信机制 + 通信语义。** IPC 不是免死金牌 ——
   契约必须**语义贫瘠**(只传扁平 DTO,禁对象图/回调/共享内存)。
3. **Discord 开发者条款要求 encryption of data at rest** ⇒ 接 Discord 通道
   **必须重新裁定 D21/D22**(不加密)。这是合同义务,不是隐私偏好。

另:**faster-whisper 默认联网拉权重** —— 需构建期预置 + `local_files_only`;
**Piper 已改为 GPL-3.0** 且逐个语音的 MODEL_CARD 各不相同 ⇒ 需白名单 + CI 校验。
**代码许可通过 ≠ 权重许可通过**,两套台账分开记。

---

## 硬性前置(不满足则不得启动)

| 阶段 | 前置条件 | 当前 |
|---|---|---|
| **P3a** | 恢复演练 · 备份目标 · 活库备份方式 | ✅ 三项全部满足(2026-07-28 真机验证) |
| **P3b** | 待决事项 1 裁定 | ✅ 已裁定(D43 S0.4=移除 Tailscale);四问已答、审计通过、已开工 |
| **P3b** | **LAN TLS 与绑 LAN 捆绑交付** | ✅ **2026-07-29 已捆绑交付**:lan-edge 在 `<LAN IP>:8443` 跑 Kestrel + mTLS(UseHttps + 客户端证书校验),CA 私钥落 TPM;回环门禁全过。**本条前置解除** |
| **P3c** | P3b 完成(客户端要能配对)+ P3a S1 | 🔵 S1 已完成;P3b ✅ 已实机验收(2026-07-29,见上) |
| **P3c** | **★ 身份层「设备 × 成员」**(D45) | 🟡 **部分**:成员表已落(identity S1 selftest 14/14 · Store.cs 成员/角色/设备默认成员)。**端到端未通**:gateway/membership.py 只解析到设备、memory 无 `visibility_scope` 列、客户端拿不到成员 id(MemberContext 用设备本地哨兵)。⇒ D45 裁定 2/3 尚未生效,「仅本人」目前挡不住同机另一成员。已按用户裁定在此前提下开工,真身份接入排在 S4 之后 |
| **P3d** | **响应侧出境闸** | 🔵 方向 A(请求出境 E4)✅ **2026-07-31 真正落地**(07-30 标 ✅ 系误记 —— 模块当时并不存在,详见上方阻塞表);方向 B(响应回程)❌ 仍是硬前置 |
| **P6** | **★ Agent Worker 的三条前置**(D65) | ❌ ① 隔离路线裁定(待决 6)· ② **P4 Broker 租约**(一次 agent 任务 = N 次模型调用,与桌宠/同传抢同一块 8.52 ⇒ **没有 Broker 就没有 Agent**)· ③ **方向 B**(会浏览、会读文件的 loop 是迄今最大的不可信文本入口)。分期 H0–H4 见 [HERMES_INTEGRATION_DESIGN.md](HERMES_INTEGRATION_DESIGN.md) §4 |
| **P7** | **审批界面是 L4 技能候选的硬前置**(D65/DD-6) | ❌ 在 P7 之前技能候选只能积压在 `pending_review`,**不得开任何临时放行通道** |
| **P8** | P6 权限区域 + **A7-pet 实测** | ❌ |

---

## P1 实测结论(17/17 · 2026-07-27 收官)

**五个改变架构的发现:**

1. **q8_0 KV 转正**(A2+B1+B3b 三重证明,零质量损失)→ 省 1–2 GiB,**32K 上下文解锁**
2. **30B-A3B 深度模式活了且飞快**(11.88 GiB · 57.5 tok/s,MoE offload 几乎免费)→ 推翻「出局」预期,`assistant.deep` 已改绑
3. **Piper CPU TTS 83ms / 0 显存 / 多语言** → XTTS 推 P9(D27 修订);A5 顺带解决(TTS 在 CPU,不合并)
4. **`desktop_floor` = 6.6**(A7 实测 6.53)→ AI 侧上限 **8.52**;日常常驻 `8b@16k` + `speech.lite`
5. **vlm.small 比估算重 74%**(漏算 mmproj,实测 4.35)→ 视觉组合更紧

| 组 | 项 | 结果 |
|---|---|---|
| A | A7 | `desktop_floor` **6.6** · AI 上限 8.52 |
| A | A1 | CUDA ctx **227 MiB**/进程,线性可加 |
| A | A2 | 可加性差 **0 MiB** · 挖出 KV 2 倍浪费 · vlm.small 4.35 |
| A | A3 | 30B-A3B ngl30 → **11.88 GiB** / 38% offload |
| A | A5/A8 | ASR 实时率 **0.385** · TTS **83ms**(Piper CPU) |
| B | B1 | q8_0 KV 达标 / F16 超标 |
| B | B2 | 冷启动 prefill 超阈 → **必须开 prompt cache** |
| B | B3 | 长上下文衰减 52%(仍可用)· **B3b q8_0 零质量损失** |
| C | C1 | 150 tok/s(5×) |
| C | C3 | 30B-A3B **57.5 tok/s** |
| C | C4 | WER lite vs full **+0.4pp**(≪3pp)→ speech.lite 安全 |
| D | D1/D2 | SDXL 峰值 **8.14 GiB** · 单图 **5.6s** |
| E | E5 | Vigil **零显存** ✓ · 脉冲 2s |

**语音栈定稿(D27 修订)**:ASR faster-whisper large-v3(full)/ turbo(lite)· TTS **Piper 全档** · VLM Qwen2.5-VL-3B。
speech 三档:cpu 0 · lite 2.07 · full 4.05。

**C2b**(含网关+别名 TTFT)—— P2 交付物,C2a 裸 server 已测 33ms。
测量证据归档在 `00-docs/p1-data/`。

---

## 显存分配模型

| | |
|---|---|
| **D24** | 彻底放弃核显。显示拓扑永久接独显,桌面显存为**永久扣项** |
| **D25** | **取消高/中/低档位**,改为**主机面板勾选组件 + 实时预览 + 确定生效** |

```
desktop_floor   你设的「至少给桌面留多少」(跨会话持久)
vram_budget  =  15.92 − desktop_floor − 0.8      ← 导出值,不单独设
确定按钮启用 :  Σpeak(已选) ≤ min(vram_budget, NVML free − 0.8)
```

**★ `cap` 这个词已全文废止** —— D24 已把它与「降它能腾显存」的误解永久绑定。
§8.1.3 已随 P1-A2 实测定稿(可加性成立,差 0 MiB)。

---

## 环境事实(变更即更新)

| 项 | 值 | 核实日期 |
|---|---|---|
| **物理盘** | Disk 0 = 1TB → C: ／ **Disk 1 = 2TB → D: + E:(同一块盘)** ／ Disk 2 = USB SSD → G: | 2026-07-26 |
| **备份介质** | G: SanDisk Extreme · USB · 931 GB · exFAT · **不加密(D21,预期行为)** | 2026-07-26 |
| **网络** | 有线 **1 Gbps** 全双工 · 延迟 <1ms。HF 下载实测 **29 MiB/s ≈ 232 Mbps** | 2026-07-26 |
| GPU | RTX 5080 · 驱动 **610.62** · 16303 MiB · 84 SM · 桌面占用 691–1085 MiB | 2026-07-26 |
| **CUDA Toolkit** | **13.2**(nvcc V13.2.51)· sm_120 编译+运行通过 | 2026-07-26 |
| Python / git / node / ffmpeg | 3.12.10 ／ 2.54.0 ／ v24.16.0 ／ 8.1.1 | 2026-07-26 |
| **PostgreSQL** | **18.4-2** · 服务 `pg-mem` · `.\ai-mem` · 仅 127.0.0.1:5432 · SSPI(无 DB 口令) | 2026-07-27 |
| **Qdrant** | **v1.18.3**(pin)· `Qdrant` 6333/6334 + `Qdrant-s2` 6335/6336 · 均 `.\ai-mem` · 各自 api_key | 2026-07-27 |
| **embedding** | bge-m3(1024 维)+ bge-reranker-v2-m3 · venv `D:\AI\venvs\embedding` · **transformers 钉 <5** | 2026-07-28 |
| ~~**Open WebUI**~~ | **已退役并归档(2026-08-03,D77)**:DATA_DIR 移入 `{state}\quarantine\20260803-openwebui\`(1.25 MB,库内 0 条对话);venv `D:\AI\venvs\openwebui` 待清 | 2026-08-03 |
| **记忆库 venv** | `D:\AI\venvs\memory`(psycopg + httpx)· 记忆各模块与验收脚本在此运行 | 2026-07-28 |
| **向量空间** | `mem.vector_space` 登记编码指纹:bge-m3 · 1024 · Cosine · 无前缀 · 归一化。**指纹不一致则拒绝启动** | 2026-07-28 |
| **`{state}/secrets`** | `D:\AI\state\secrets` · 凭据与私钥的唯一落点 · 强 ACL(实测真 Deny ACE)· **且排除出备份**(实测:11 个金丝雀,`state` 计数纹丝不动;`robocopy /XD` 含嵌套一并挡住) | 2026-07-28 |
| **TLS 资产** | **无**。全仓库零证书/私钥;`install-qdrant.ps1` 明写 `enable_tls: false`(回环故不需要)。★ 这是 P3b 绑 LAN 的硬前置 | 2026-07-28 |
| speech venv | `D:\AI\venvs\speech`(faster-whisper + Piper + CUDA12 运行时) | 2026-07-27 |
| **记忆库** | `D:\AI\state\memory\` · **不加密(D22)** · ACL:ai-mem+SYSTEM+Admins FullControl / ai-asset+ai-exec **Deny** | 2026-07-27 |
| **★ UAC** | **`EnableLUA = 0` —— 彻底关闭**(`ConsentPromptBehaviorAdmin=0` · `PromptOnSecureDesktop=0`)。机主 `Zori Ma` 在 Administrators 组 ⇒ **该账户没有分裂 token,它启动的每一个进程都是 High + Administrators enabled**,双击 `.cmd`、Explorer 拉起、计划任务 `/rl LIMITED`、`runas /trustlevel` **全都一样是 High**(2026-08-06 逐条实测)。<br>**⇒ 这台机器上不存在「普通用户 / Medium」上下文。** 两处连带后果:① **D46 那条「双击 = 普通用户 = 与 init 同级」的前提不成立** —— 故 `lan-edge` 的提权护栏已改成「直接试着打开 CA 私钥」而不是判管理员身份(`Program.cs:205-213`,见 `decision-packets/integrity-guard-asks-wrong-question-2026-08-03.md`);② ★★★ **2026-08-10 更正 —— 此处原文是「任何『靠降权来保证安全』的设计在本机一律失效」,
那句话 literally 是假的**,2026-08-06 审计已单列为一条发现(`AUDIT-2026-08-06-full.md:351`,B19),
理由逐字是「**它会让下一个人直接否掉路线 C**」——**而它至今仍原样留着,并且真的差点做到了**。
实测的真相**窄得多**:关在 AppContainer 里的程序**自己拆不开**那道墙(实测 exit 5「拒绝访问」),
子孙进程也一起被关住;**能拆的只有机主本人**,而且是**零摩擦、零提示、无端口粒度**。
⇒ 正确的说法是:**降权在本机挡得住「被关进去的那个程序」,挡不住机主自己**。
两者差别决定了 C 可不可用 —— 前一句会否掉它,后一句只要求给它捆一条
「墙被撤 ⇒ 拒绝启动并如实说明」的纪律。<br>★ 本条此前只活在两份决议包里,已因此**咬过两次**(身份护栏那轮 · AppContainer 勘察那轮),故上表 | **2026-08-06** |
| **剪贴板** | 历史与跨设备同步均已关闭(P0 实测)。⚠ 策略层未配置,需复检 | 2026-07-26 |
| ComfyUI | `D:\ComfyUI` · **`extra_model_paths.yaml` 未配置** | 2026-07-26 |

---

## 待决事项

→ 已上移至本文件「待决事项(不得默认,须显式裁定)」一节。
D1–D33 全部已决;**D34–D41 于 2026-07-28 新增**(架构改向),其中 4 项显式待决。

---

## 已知技术债(2026-07-28 P2 收尾审查 · 57 条发现)

严重项已修完(见 worklog 与 `00-docs/p2-audit-backlog.md`)。**未修的记在 backlog 文件里**,
不在此重复。要点:

- `require_trusted_local`(fail-closed)**当前无人调用** —— 前瞻脚手架,不是已生效的防护
- 后端 llama-server **无鉴权** —— 网关不是唯一咽喉,同机进程可直连 18081 绕过 E1/审计。
  ★ **D65 把这条从"技术债"升级为"待决项 6 的一半"**:Windows 防火墙不过滤回环、账户 ACL 管不了 TCP,
  所以对一个跑在本机的 Agent Worker,这条**默认成立且无法用现有手段封堵**
- `/health` 不做身份校验 —— **S3 已收窄为只回 `{status:ok}`,设计如此,不是欠账**。~~`/v1/models`~~ **已于 P3b S3 纳入认证**(ROUTE_TIERS 标 authenticated + remote-unauthenticated 401),本条撤销
- 身份解析是**同步阻塞调用**跑在 async 端点里(WMI ~250ms 会卡事件循环)
- schema 中标 `[推断]` 的列是设计发明,待 P3a 最终化
- 审计日志 `session_id` 恒为 `unknown` —— Open WebUI 不发 `X-Session-Id`,凭证命中记录无法关联到具体对话
