# STATE — 本地 AI 中枢

> 更新时间: 2026-07-28 20:10
> 方案书版本: **v3.0**(v2.2 已归档 · 阶段重排见 D34-D41)
> 格式定义: v3.0 §12.1 记录协议
>
> ★ 2026-07-28 重写:此前本文件累积了三段互相矛盾的 P1 状态、已解决却仍列着的阻塞项、
>   以及两处断裂表格(P2 收尾审查发现)。本版只写**当前为真**的状态。

---

## 当前阶段:**P3a 记忆系统 · 🔵 S0–S2 / S9**(2026-07-28)

| 阶段 | 状态 |
|---|---|
| **P0 地基** | ✅ 验收通过(2026-07-26) |
| **P1 基准测试** | ✅ **完成 17/17 全部实测**(2026-07-27) |
| **P2 核心链路** | ✅ **基本完成** —— 权限档位已移至 P4(D37);WebAuthn 已作废(D34) |
| **P3a 记忆系统** | 🔵 **进行中:S0 · S1 · S2 已完成**(2026-07-28) |

---

## P2 清单(§14)

| 项 | 状态 | 证据 |
|---|---|---|
| 命名决定(RP ID 前置) | ⚠️ **产物待定** | 机器名 `HONGKONGPINGPON` 保留;tailnet 名 `tail71cfd7.ts.net` 的去留随 **D34 待决项 1** 一并裁定。★ D14a 自称"不可推翻",但其依据是"已注册 passkey 会作废"——而当前**设备数 =1、无 passkey**,故改动代价仍为零 |
| 统一入口网关 + 别名层 | ✅ | 自写非 LiteLLM(D29)。**验收①②实测通过**(见下) |
| 账户隔离 ai-mem/ai-asset/ai-exec | ✅ | 三账户 + memory 目录 Deny ACL,`Get-Acl` 确认真 Deny ACE |
| PostgreSQL + Qdrant | ✅ | PG18 SSPI · Qdrant×2(6333 mem_main / 6335 mem_s2)· 全 ai-mem · 全回环 |
| PG schema(含 sensitivity_domain/crypto_tier) | ✅ | 24 表 · 2 角色 · `v_memory_nons2` · 否定用例含行级 S2 实攻。**验收口径已于 P3a-S2 收紧**:除数 PASS 外,还核对「预期报错」的条数与顺序 —— 正是顺序对不上暴露了 §4.5 铁律失效(见下) |
| **E1 入口凭证检测器** | ✅ | 6 类 + 校验和 · 扫整个载荷 · 流式亦拦 · 带内解除 · 审计不泄露 |
| `secret_ref` 登记表 | ✅ | 随 schema 建,整表 S2,远程角色 SELECT 抛权限错 |
| 网关调用方身份校验(D30) | ✅ | 端口→PID→WMI GetOwner,拒 ai-asset/ai-exec |
| embedding / rerank(CPU :18084) | ✅ | 服务 Running · `.\ai-mem` · 自启 · 仅回环 · 1024 维 · rerank 语义正确 |
| 首版界面 Open WebUI | ✅ **过渡件** | 已完成历史任务(把网关端到端验通)。**P3c 客户端就绪后退役**(D36) |
| 剪贴板历史/跨设备同步检查 | ✅ | 2026-07-28 复验:两者均关闭。策略层未配置(加固项,非 §14 要求)—— 任何以你身份运行的程序仍可重新打开 |
| **无 Broker 期显存过渡措施** | ✅ **已做**(2026-07-28) | §8.1 三层检查落地:`config/vram-budget.toml`(唯一数据源,peak 全实测)+ `10-core/gpu-broker/vram_gate.py`(38 测试)。接进 start-stack。拒绝归因到具体闸并带数字;两种撞墙给不同建议。**同时是 P4 Broker 的判定内核** |
| ~~WebAuthn 设备身份~~ | ❌ **作废**(D34) | 非本机设备上不再有浏览器 ⇒ RP ID 不可变陷阱整类消失。局域网身份改走 **mTLS**(P3b) |
| **权限档位(六元组)** | ➡️ **移至 P4** | D37 让它有了第一个真实客户:**谁能改 GPU 状态**。不再是悬空欠账 |

### §14 P2 验收标准逐条

| 标准 | 结果 |
|---|---|
| 本机可用别名对话 | ✅ `assistant.fast` → 「德国的首都是柏林。」· 契约头 · 流式均通 |
| 换别名映射,客户端零改动 | ✅ 客户端代码逐字未动,契约 `8b`→`8b-remapped`,对话照常 |
| 非授权账户连接记忆库被拒**且告警** | 🟡 **部分**:被拒✅(三层实攻);**「告警」仅落审计文件** —— 告警通道在 **P7 客户端 v2** 闭合 |

---

## P3a 记忆系统(项目核心里程碑)

| 步 | 内容 | 状态 |
|---|---|---|
| **S0** | schema 加固:append-only · supersede 方向 · tombstone/隔离区 · 分级需票据 · 禁批量确认 | ✅ |
| **S1** | 最小可用:`tainted.py` 类型层 → `repo.py` 唯一写模块 → `gate.py` 唯一写入口 → `route.py` 纯函数选路 | ✅ 验收 22/22 |
| **S2** | 向量轨 + 双 Qdrant + **编码指纹** | ✅ 验收 43/43 |
| S3 | 完整记忆闸(分级 · E3 · 队列 · 熔断 · 冲突) | ⏭ 下一步 |
| S4 | 隐私三层 + 账户隔离 + 日志纪律 | |
| S5 | 记忆面板的**后端 API**(浏览/编辑/删除/溯源)—— ★ **UI 已移至 P3c 客户端**(D36) | |
| S6 | 冷启动初始化会话 + S2 分级(只跑一次) | |
| S7 | 评测集 + 四指标 + 阈值标定 | |
| S8 | L4 写路径 + 哈希绑定确认 | |
| S9 | 备份/恢复演练 + P3a 验收签字 | |

**两条验收例句均已端到端跑通:**
「我妹妹叫小雨」→ 问名字 → 走**结构化轨**答出,溯源俱全(S1);
「上次聊的那个灯光问题」→ 走**向量轨**,命中金标,**只给 passages 不给 answer**(S2)。

回归口径:239 断言(tainted 50 · route 55 · repo 29 · gate 40 · S1 22 · S2 43)+ `verify.sql`
16 PASS / 0 FAIL / 10 条预期报错逐条核对条数与顺序。

### ★ S2 期间揪出的三条「看着有防护、实际没有」(均非新引入)

1. **§4.5 铁律的保护范围实际为空** —— `is_user_fact` 要求置信度 ≥1.0,而 §4.4.2 规定 1.0 必须有
   `panel_ticket` 背书 ⇒ 正常写入的用户话语一律 <1.0 ⇒ 都不算「用户事实」⇒ 任何自动来源都能
   覆盖用户亲口说的话且不报错。已改为**只看 provenance**,并把 `panel_ticket` 纳入人类权威
   (否则用户永远无法修改自己的记忆)。
2. **append-only 误绑 `l1_session_summary`,该表被变成不可用** —— L1 是滚动压缩摘要,按设计就要改写;
   且它没有 `superseded_by` 列,拒绝时给出的建议根本无法执行。已移出绑定清单。
3. **tombstone 可被一条 UPDATE 撤销** —— `redacted_at` 非NULL→NULL 畅通,与「悄悄复活旧值」同源。
   已加单向约束;恢复路径应为**从隔离区新增一行**。

> 这三条能藏住,是因为 `verify.sql` 的夹具在 S0 加固后插不进去,用例匹配 0 行 → 不报错 → 伪装成通过,
> 且金丝雀行消失后隔离断言报出的是「远程读不到」——**像安全告警,实为测试自身损坏**。
> 已加 **C0 夹具就位断言**(放在所有隔离断言之前),让两类失败长得不一样。

---

## ★ 2026-07-28 架构改向(D34–D41)

**一句话**:取消"全功能远程接入";能力按**是否装了客户端的内网 Windows PC** 两档划分;
人在外面走**外联文本通道(本期 Signal)**;桌宠是 Vigil 的形体。

| | 全功能端 | 聊天端 |
|---|---|---|
| 是谁 | 内网 + Windows + **装了客户端** | 手机及其他一切设备 |
| 能干什么 | 全部 | 只有外联通道的文本/语音消息 |
| **断网时** | **照常用** | 用不了 |

**能力边界与信任边界重合** —— 不必再为手机设计权限模型,它物理上只有一条文本通道。
路由器上**永不开任何入站端口**。

**新增/重定义的阶段**(详见 v3.0 §14):
P3b 局域网身份与准入 · P3c 统一客户端 v1(唯一入口)· P3d 外联通道 Signal · P11 产品化(条件触发)

**★ 三条必须先于对应阶段落地的硬前置:**
1. **网关绑 LAN 与 LAN 上有 TLS 是同一个不可分割的交付项**(P3b)——
   审查确认全仓库零 TLS 资产,而 WPA2-PSK 无前向保密。**做不出 TLS 之前继续只绑回环。**
2. **响应侧出境闸**(P3d)—— L5「记忆内容禁止出境」在代码里**从未实现**;
   E1 只扫入口,非流式与流式两条回程分支都无扫描点。
3. **A7-pet 实测**(P8)—— `desktop_floor` 的新值不得估算。

---

## 下一步

**P3a S3:完整记忆闸(分级 · E3 · 队列 · 熔断 · 冲突)。** 它不依赖任何待决事项,可立即开始。

其后按依赖:P3b(身份与准入)→ P3c(客户端)→ P4(Broker + 仲裁)。
P3d(外联)独立,可与 P3b/P3c 并行,但其硬前置**响应侧出境闸**必须先做。

日常使用:数据库四件套是 Windows 服务(开机自启,不用管);**llama 后端与网关不是服务**
(按设计由 P4 的 GPU Broker 按需装载),用之前先跑:

```
powershell -ExecutionPolicy Bypass -File "E:\.meine\.Proj_Soft\.Proj\.localAI\90-ops\start-stack.ps1"
```
然后 Open WebUI 在 http://127.0.0.1:8081(另起:`install-openwebui.ps1`)。

~~**P3a 之前必须先解决的**:活数据库的备份方式~~ → ✅ **2026-07-28 已完成**。剩:无 Broker 期显存闸(建议先做,防 OOM)。

---

## 阻塞

| 事项 | 阻塞什么 | 需要什么才能解 |
|---|---|---|
| ~~活数据库的备份方式~~ | ~~P3a~~ | ✅ **2026-07-28 已解除**:逻辑转储落地 + 每次备份自证可恢复 + 金丝雀演练 PASS + 真机备份跑通(G:) |
| ~~上行带宽未测~~ | ~~P3b 手机端体验~~ | ✅ **2026-07-28 作废**:手机不再有全功能路径(D34),外联通道是文本,带宽无关 |
| **★ LAN 命名 + 证书方案未定** | **P3b 全部内容** | ★ **性质已变**:RP ID 陷阱随 WebAuthn 一起消失(D34 —— 非本机设备上不再有浏览器)。剩下的是**必须有 TLS**:全仓库零 TLS 资产,而 WPA2-PSK 无前向保密。改走 mTLS 后命名可返工,但**加密不可省** |
| **★ 响应侧出境闸不存在** | **P3d 外联通道** | 审查确认 L5 在代码里从未实现:E1 只扫入口;非流式与流式两条回程分支均无扫描点;四个解封点都不知道"回复要去哪儿"。**接任何外联通道之前必须先做** |

> P1 阶段的全部阻塞(语音栈选型 · C4 语料 · llama.cpp · 桌面基线)**均已解除**。

---

## 待决事项(不得默认,须显式裁定)

| # | 事项 | 判据 / 影响 |
|---|---|---|
| 1 | **Tailscale 留不留** | 选 Signal 后 Funnel 用途消失,它只剩"LAN 加密 + 准入 + 设备清单",而那三项可由 mTLS + 管理端替代且**断网时更硬**。**判据只有一个:会不会带笔记本出门用全功能?** |
| 2 | 宠物渲染选型 | 决定 A7-pet 实测对象与 `desktop_floor` 新值 |
| 3 | 租约三参数 | TTL / 心跳间隔 / 空闲卸载(实测后定) |
| 4 | 产品化是否真启动 | 决定 D41 各条何时从"记账"变"裁决" |

---

## 硬性前置(不满足则不得启动)

| 阶段 | 前置条件 | 当前 |
|---|---|---|
| **P3a** | 恢复演练 · 备份目标 · 活库备份方式 | ✅ 三项全部满足(2026-07-28 真机验证) |
| **P3b** | 待决事项 1 裁定 | ❌ |
| **P3b** | **LAN TLS 与绑 LAN 捆绑交付** | ❌ 未做。**做不出 TLS 之前继续只绑回环** |
| **P3c** | P3b 完成(客户端要能配对)+ P3a S1 | 🔵 S1 已完成 |
| **P3d** | **响应侧出境闸** | ❌ 未做,硬前置 |
| **P8** | P6 权限区域 + **A7-pet 实测** | ❌ |

---

## P1 实测结论(17/17 · 2026-07-27 收官)

**五个改变架构的发现:**

1. **q8_0 KV 转正**(A2+B1+B3b 三重证明,零质量损失)→ 省 1–2 GiB,**32K 上下文解锁**
2. **30B-A3B 深度模式活了且飞快**(11.88 GiB · 57.5 tok/s,MoE offload 几乎免费)→ 推翻「出局」预期,`assistant.deep` 已改绑
3. **Piper CPU TTS 83ms / 0 显存 / 多语言** → XTTS 推 P9(D27 修订);A5 顺带解决(TTS 在 CPU,不合并)
4. **`desktop_floor` = 6.6**(A7 实测 6.53)→ AI 侧上限 **8.52**;日常常驻 `8b@16k` + `speech.lite`
5. **vlm.small 比估算重 74%**(漏算 mmproj,实测 4.35)→ 视觉组合更紧

| 组 | 项 | 结果 |
|---|---|---|
| A | A7 | `desktop_floor` **6.6** · AI 上限 8.52 |
| A | A1 | CUDA ctx **227 MiB**/进程,线性可加 |
| A | A2 | 可加性差 **0 MiB** · 挖出 KV 2 倍浪费 · vlm.small 4.35 |
| A | A3 | 30B-A3B ngl30 → **11.88 GiB** / 38% offload |
| A | A5/A8 | ASR 实时率 **0.385** · TTS **83ms**(Piper CPU) |
| B | B1 | q8_0 KV 达标 / F16 超标 |
| B | B2 | 冷启动 prefill 超阈 → **必须开 prompt cache** |
| B | B3 | 长上下文衰减 52%(仍可用)· **B3b q8_0 零质量损失** |
| C | C1 | 150 tok/s(5×) |
| C | C3 | 30B-A3B **57.5 tok/s** |
| C | C4 | WER lite vs full **+0.4pp**(≪3pp)→ speech.lite 安全 |
| D | D1/D2 | SDXL 峰值 **8.14 GiB** · 单图 **5.6s** |
| E | E5 | Vigil **零显存** ✓ · 脉冲 2s |

**语音栈定稿(D27 修订)**:ASR faster-whisper large-v3(full)/ turbo(lite)· TTS **Piper 全档** · VLM Qwen2.5-VL-3B。
speech 三档:cpu 0 · lite 2.07 · full 4.05。

**C2b**(含网关+别名 TTFT)—— P2 交付物,C2a 裸 server 已测 33ms。
测量证据归档在 `00-docs/p1-data/`。

---

## 显存分配模型

| | |
|---|---|
| **D24** | 彻底放弃核显。显示拓扑永久接独显,桌面显存为**永久扣项** |
| **D25** | **取消高/中/低档位**,改为**主机面板勾选组件 + 实时预览 + 确定生效** |

```
desktop_floor   你设的「至少给桌面留多少」(跨会话持久)
vram_budget  =  15.92 − desktop_floor − 0.8      ← 导出值,不单独设
确定按钮启用 :  Σpeak(已选) ≤ min(vram_budget, NVML free − 0.8)
```

**★ `cap` 这个词已全文废止** —— D24 已把它与「降它能腾显存」的误解永久绑定。
§8.1.3 已随 P1-A2 实测定稿(可加性成立,差 0 MiB)。

---

## 环境事实(变更即更新)

| 项 | 值 | 核实日期 |
|---|---|---|
| **物理盘** | Disk 0 = 1TB → C: ／ **Disk 1 = 2TB → D: + E:(同一块盘)** ／ Disk 2 = USB SSD → G: | 2026-07-26 |
| **备份介质** | G: SanDisk Extreme · USB · 931 GB · exFAT · **不加密(D21,预期行为)** | 2026-07-26 |
| **网络** | 有线 **1 Gbps** 全双工 · 延迟 <1ms。HF 下载实测 **29 MiB/s ≈ 232 Mbps** | 2026-07-26 |
| GPU | RTX 5080 · 驱动 **610.62** · 16303 MiB · 84 SM · 桌面占用 691–1085 MiB | 2026-07-26 |
| **CUDA Toolkit** | **13.2**(nvcc V13.2.51)· sm_120 编译+运行通过 | 2026-07-26 |
| Python / git / node / ffmpeg | 3.12.10 ／ 2.54.0 ／ v24.16.0 ／ 8.1.1 | 2026-07-26 |
| **PostgreSQL** | **18.4-2** · 服务 `pg-mem` · `.\ai-mem` · 仅 127.0.0.1:5432 · SSPI(无 DB 口令) | 2026-07-27 |
| **Qdrant** | **v1.18.3**(pin)· `Qdrant` 6333/6334 + `Qdrant-s2` 6335/6336 · 均 `.\ai-mem` · 各自 api_key | 2026-07-27 |
| **embedding** | bge-m3(1024 维)+ bge-reranker-v2-m3 · venv `D:\AI\venvs\embedding` · **transformers 钉 <5** | 2026-07-28 |
| **Open WebUI** | 0.11.0 · venv `D:\AI\venvs\openwebui` · DATA_DIR `D:\AI\state\openwebui` | 2026-07-28 |
| **记忆库 venv** | `D:\AI\venvs\memory`(psycopg + httpx)· 记忆各模块与验收脚本在此运行 | 2026-07-28 |
| **向量空间** | `mem.vector_space` 登记编码指纹:bge-m3 · 1024 · Cosine · 无前缀 · 归一化。**指纹不一致则拒绝启动** | 2026-07-28 |
| **`{state}/secrets`** | `D:\AI\state\secrets` · 凭据与私钥的唯一落点 · 强 ACL(实测真 Deny ACE)· **且排除出备份**(实测:11 个金丝雀,`state` 计数纹丝不动;`robocopy /XD` 含嵌套一并挡住) | 2026-07-28 |
| **TLS 资产** | **无**。全仓库零证书/私钥;`install-qdrant.ps1` 明写 `enable_tls: false`(回环故不需要)。★ 这是 P3b 绑 LAN 的硬前置 | 2026-07-28 |
| speech venv | `D:\AI\venvs\speech`(faster-whisper + Piper + CUDA12 运行时) | 2026-07-27 |
| **记忆库** | `D:\AI\state\memory\` · **不加密(D22)** · ACL:ai-mem+SYSTEM+Admins FullControl / ai-asset+ai-exec **Deny** | 2026-07-27 |
| **剪贴板** | 历史与跨设备同步均已关闭(P0 实测)。⚠ 策略层未配置,需复检 | 2026-07-26 |
| ComfyUI | `D:\ComfyUI` · **`extra_model_paths.yaml` 未配置** | 2026-07-26 |

---

## 待决事项

→ 已上移至本文件「待决事项(不得默认,须显式裁定)」一节。
D1–D33 全部已决;**D34–D41 于 2026-07-28 新增**(架构改向),其中 4 项显式待决。

---

## 已知技术债(2026-07-28 P2 收尾审查 · 57 条发现)

严重项已修完(见 worklog 与 `00-docs/p2-audit-backlog.md`)。**未修的记在 backlog 文件里**,
不在此重复。要点:

- `require_trusted_local`(fail-closed)**当前无人调用** —— 前瞻脚手架,不是已生效的防护
- 后端 llama-server **无鉴权** —— 网关不是唯一咽喉,同机进程可直连 18081 绕过 E1/审计
- `/health` 与 `/v1/models` **不做身份校验**(信息量低,但与其他端点不一致)
- 身份解析是**同步阻塞调用**跑在 async 端点里(WMI ~250ms 会卡事件循环)
- schema 中标 `[推断]` 的列是设计发明,待 P3a 最终化
- 审计日志 `session_id` 恒为 `unknown` —— Open WebUI 不发 `X-Session-Id`,凭证命中记录无法关联到具体对话
