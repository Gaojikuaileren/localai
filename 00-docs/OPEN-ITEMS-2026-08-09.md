# 未了项总表 · 2026-08-09

> 第 0 条车道写于 2026-08-09 凌晨,基线 `main@c17f164`。
>
> **为什么有这份文件**:2026-08-07/08 两天十几条车道的交回里散着大量
> 「没做的 / 待裁的 / 交给别人的」,而**它们没有任何一处汇总** ——
> 每一条都写得很清楚,只是分别躺在十六份决议包和一份审计里,没人能一眼看完。
>
> **怎么做出来的**:扫 `00-docs/decision-packets/*2026-08-0[789]*.md`(16 份)
> + `00-docs/audit/AUDIT-2026-08-08-full.md`,抽出 **212** 条,
> 然后**逐条回到当前代码/文档里查实它是否已经被别的车道顺手解掉**。
>
> ★★★ **那一步查实不是形式**:212 条里 **41 条(19%)已经被解掉了**。
> 只抄不查的话,这张表**有五分之一是假的** ——
> **而一张假的欠债表比没有表更坏:人会照着它排优先级,而排错的那部分永远不会自己暴露。**
>
> ★ 本表**不是**任务清单,是**账**。它不替任何人排期,只保证「今天还欠着什么」这件事有一处可查。

---

## §0 这张表**不声称**什么

- **不声称**它覆盖了全部欠债 —— 它只扫了 08-07/08/09 那批包 + 那一份审计。
  ★ 已知**扫描范围之外**还有东西(见 §1 后半),那几条是按用户点名补进来的,不是扫出来的;
- **不声称**每条的「挡着什么」都准 —— 那一列来自包的自述,只有「查实」那一列是回查过的;
- **不声称** `已被解掉` 的 41 条从此不用管 —— 它们的证据都在 §4,**证据可以被推翻**;
- ★ **不声称**任何一条的优先级。**排期是用户的事**,本表只把牌摊开。

---

## §1 用户 2026-08-09 点名要的那几条(逐条对号)

★ 这一节是按用户「已知至少这些,别漏」逐条核的。**其中三条落在扫描范围之外**,单独补捞。

| # | 点名的 | 在不在扫描结果里 | 今天的状态 |
|---|---|---|---|
| ① | **V17 存储拓扑改向** | ✅ 在(`shared-data-topology-host-authoritative`,**13 条**) | **待裁**。整包是勘察+建议,用户裁定过「不取号,等裁」——今天仍未裁 |
| ② | **待决 6 / 待决 7(挡着 P6 整条)** | ❌ **不在** —— 它是 `appcontainer-isolation-mcp-transport-2026-08-05.md`,日期在扫描窗口之外 | **待裁**。包里已给出结论(MCP 只能走管道,走 HTTP 结构上自毁;命名管道跨 VM 边界**未测**,与 D73「全链路无 HTTP」冲突)——**结论有了,裁没裁** |
| ③ | **D81 待裁五条** | ❌ **不在** —— 它在 `DECISIONS.md` 里,不是决议包 | **待裁**。D81 本身明写「**本决议不开工**」;方向 B 是 P3d 的硬前置 |
| ④ | **记忆库 B 档 269 条** | ❌ **不在** —— `memory-suite-revival-2026-08-06.md`,窗口之外 | **待做**。包里逐字:「那 **269** 条在 B 档真跑之前,状态是**未知**,不是绿」(269/459 = 59%) |
| ⑤ | **`GET /health` 那条契约** | ✅ 在 | **待做**。DEBT 仍是 **1/30**;注记已于 08-08 按实测改对(D110),**修法未做**(要 `start-stack.ps1` 解析响应体) |
| ⑥ | **翻译空间目标池没喂给模型** | ✅ 在(`v20-chat-view-fixes` §) | **待裁**。用户实测 AI 答「我无法访问任何外部系统」。★ 包里明写**不是 bug,是"待 AI 接入"**,且要先裁「客户端状态怎么进提示词」——系统提示词的权威在中枢 |
| ⑦ | **S6 清单至今 0 项打勾** | ✅ 在(方案书 §14 P3b) | **待做**。★ 2026-08-06 撤回过一次过度声称;新版实机清单已写好:`90-ops/lan/P4-acceptance-checklist.md` §4 |
| ⑧ | **管理端断言不在门禁里** | ✅ 在 | ✅ **已解(2026-08-09,提交 `c17f164`)** —— 已接进 `run-tests.ps1`,单列不并总数,SKIP 透出,并红测过 |

★★ **②③④ 这三条落在扫描窗口之外这件事本身要记一笔**:
本表的扫描范围是 `*2026-08-0[789]*`,而**欠债不按日期排队**。
⇒ 下一次做这种表,范围要按**「所有还没被裁定关闭的包」**取,不是按日期取。
**用户点名补上的这三条,正是这个取法缺陷的证据。**

---

## §2 数量分布(先看形状,再看条目)

| | 条数 |
|---|---|
| 扫出来 | **212** |
| 其中**已被解掉**(查实推翻) | **41** |
| **仍未了** | **144** |
| **部分解掉**(有一半已被解) | **26** |
| 查不出 | **1** |
| ⇒ **本表登记的未了项** | **171** |

按类型:**待裁 36** · **待做 107** · **已交别人 28**。
按归属:**未指派 68**(最大的一坨)· 用户 13 · 并入时执行 11 · V10 车道 8 · 其余分散。

★★★ **「未指派 68」是这张表最该被看见的数字。**
它不是「还没排期」,是**没有任何一条车道认领** —— 而认领之前,它们不会自己往前走。

---

## §3 全部未了项 —— 按出处逐份列(共 171 条)

> 每份包一张表。**「查实」那一列是扫完之后逐条回查过的**,不是照抄包里的话。
> `⚠部分` = 有一半已被别的车道解掉,读的时候要连着看它的证据(见本文件末尾 §4 同名条)。

### `AUDIT-2026-08-08-full.md` —— 55 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待做 | 未指派(§7「收口前必须做」) | A-1:「关栈入口」到底关不关栈没查清,两份已并 main 决议包据此算出的拆分权衡净额要重算 | 实机场次 ⚠ 四条里「压力即让 / 任务暂停恢复 / 证书归因」三条被记成已解决;以及压在这笔错账上的拆分权衡净额 | 仍未了 |
| 已交别人 | 证书/配对切片 | 缝⑨:store.json 的 C#→Python 跨语言文件格式契约仍无成对断言(缝表里后果最重的一条) | C# 侧改 JSON 命名(如给 Devices 加 [JsonPropertyName("devices")])三个自检全绿而 Python 侧 fail-closed —— 配对身份链断掉没人拦 | 仍未了 |
| 已交别人 | 门禁车道 | 缝②:lan-edge MapFallback 吐的非路由型 HTTP 响应契约,D95 提取器 _MAP_RE 结构上抓不到 | 有真实消费者的响应契约不在那 30 条账里;§7 建议⑨「门禁车道一刀减 2」的第一刀 | 仍未了 |
| 已交别人 | 门禁车道 | 缝⑤:error.type 是值域契约而 D95 只钉顶层键集合(服务端 21 词 / 客户端读 6 / 15 个一字没读) | 改一个 error.type 词,顶层键集合一个字不变,门禁不红;§7 建议⑨「一刀减 2」的第二刀 | 仍未了 |
| 已交别人 | 门禁车道 | 缝③:HTTP 请求体契约无覆盖,D95 明写不在范围内 | 请求体字段跨进程漂移无人拦 | 仍未了 |
| 已交别人 | GPU/同步车道 | 缝④:SSE 每一帧的契约只半覆盖(路由枚举了,帧形状不定义) | 流式帧形状变了不会被判红 | 部分解掉 ⚠部分 |
| 已交别人 | V14 车道 | 缝⑥⑦⑧:命令行动词+参数 / 退出码 / stdout 文本三类跨边界契约全欠 | V14 正在新造这三类的实例;§1.1 明说「建表的最好时机是它落地那一刻」,错过就补不上 | 仍未了 |
| 已交别人 | 第 0 条车道 | 缝⑩:跨语言 TOML 格式契约无覆盖,四个各自手写的解析器(实测双引号/带空格节名两例 KeyNotFoundException) | 配置文件写法一变,不同解析器行为分家(已实测抛异常) | 仍未了 |
| 已交别人 | GPU 车道 | 缝⑫:跨语言档位/动作词表只半覆盖 —— V13 为这一个装了端到端,但没有元规则去数「还有几个」 | 其余词表实例数目未知,广度不收口 | 仍未了 |
| 已交别人 | V14 车道(⑮);⑬⑭ 未点名归属 | 缝⑬⑭⑮:启动时序 / 注册表 / ACL 账户三类缝无覆盖 | ⑮ 已有车道,⑬⑭ 的归属在表里没写清 | 仍未了 |
| 已交别人 | V14 车道 | 缝⑯:出厂产物目录布局契约无覆盖 | 出厂目录结构改了没人判红 | 仍未了 |
| 已交别人 | 第 0 条车道(缝表①「第 0 条车道裁定 GET /health」) | 缝①的 DEBT=1:GET /health 的处置(推荐让 start-stack.ps1:144 解析响应体,不走 state 豁免),且欠债表里那条注记今天与代码不符 | 契约成对元规则唯一的 DEBT=1 归不了零;D6-F1 已实测那条注记的 state 与代码不符 | 仍未了 |
| 待裁 | 用户 | 待裁:17 类缝那张表要不要升级成一条元规则 —— 今天它是人手维护、不会自动变大 | 整张欠债表的可证伪性:不能变大的欠债表是装饰 | 仍未了 |
| 待裁 | 用户 | 待裁:A-2 的处置 —— 栈整个审计窗口都在跑却没人用,要不要把「跑一次真请求」写进审计口径 | 下一轮审计仍会把「只能读码」当前提;所有 HTTP 行为结论仍是读码或 TestClient | 部分解掉 ⚠部分 |
| 待裁 | 用户 | 待裁(上几轮遗留至今无裁定):记忆系统要不要接后端 —— 生产路径上零接线,require_trusted_local 至今零引用 | 上一轮 A4 的另一半闭不了环 | 仍未了 |
| 待裁 | 用户 | 待裁(上几轮遗留至今无裁定):EnableLUA=0 与三个不存在的账户 · 三段预览 C5 | 这两件已经跨了几轮无人应答,继续挂着 | 仍未了 |
| 待做 | 未指派(§7「收口前必须做」) | 装一条「会话要结束」时刻的护栏(今天 14 条机械化护栏全在「已经决定提交」之后,而「根本没到提交那一刻」这一族数 = 0) | 「活干完没提交」两日内发生 4 次,今天一条护栏都拦不住 | 部分解掉 ⚠部分 |
| 待做 | 未指派(§7「当晚可做」第 1 条) | run-tests.ps1:845 那句硬编码「主树里是 1901 条」是今天为假的事实陈述,少报约 239 条(实测 2140) | 它落在 if ($inWorktree) 分支 ⇒ 每一条并行车道都会读到这句假话,并据此低估自己没跑的量 | 仍未了 |
| 待做 | 未指派(§7「当晚可做」第 2 条) | DECISIONS.md:4989「已钉两个方向」是已落地裁定里的假完成陈述,至今无留痕划掉 | 中央决议文档里一句与出处提交 b4fd524 交回直接打架的完成声明还在生效 | 仍未了 |
| 待做 | 未指派(§7「当晚可做」第 3 条) | N1:InstanceLock.cs:63 把一条不存在的护栏(D106 裁定②的 SequenceEqual)当既成事实引用并作设计依据 | 全仓无任何 SequenceEqual,只有一句 Contains —— 设计理由踩空 | 仍未了 |
| 待做 | 未指派(§7「当晚可做」第 4 条) | 6 条实测恒真断言 + 17 条「判词说行为、判据是某句注释在不在」逐条修,并把「注释把断言弄绿」这个方向补进 ASSERTION-PITFALLS 第 1 条 | 这 23 条今天注入它们自称禁掉的东西一条不红;陷阱库只钉了「注释把断言弄红」的反方向 | 仍未了 |
| 待做 | 未指派 | B18/B19:两条中央文档里过宽的断言句一字未改 | 不挡 | 仍未了 |
| 待做 | 未指派 | A6 的判据闭环了但「只报不判红」—— dist/client 版本戳落后 HEAD 9 个提交,装机客户端仍是 08-07 暂停签字那一版 | 修好 P4 三格 ❌ 的那些提交到不了真机(用户手上仍是旧客户端) | 仍未了 |
| 待做 | 未指派 | 方案书第 3 行至今仍把 P3b 列进「已验收」,而它自己 §14 里 P3b 有 3 格不是 [x]、DECISIONS 里根本没有 P3b 的验收签字 | 方案书顶部的验收总账与自身 §14 打架 | 仍未了 |
| 待做 | 未指派 | 收工判据第②条「已合进 main」在车道工作树里执行不了(必须写进另一个会话正占着的工作树,是全项目唯一没有锁的串行点) | 每条并行车道都无法真正执行自己的收工判据 ⇒ 「活干完没提交」根源之一 | 仍未了 |
| 待做 | 未指派 | worktree 目录被跨车道回收时,git checkout 会把未提交的活静默带到新车道分支上(实测 rc=0,已改文件与未跟踪文件全部跟过去) | 车道之间的活会串台且无声 | 仍未了 |
| 待做 | 未指派 | safe_to_stop_stack(写好了却零调用点的收尾函数)没有进任何「收尾/清理函数必须有断言钉住调用点」的表 —— 而 D102 裁定②亲手立了这条通则 | D102 裁定②立的通则今天只对状态机的边落地了,对收尾函数这一类没落地 | 仍未了 |
| 待做 | 未指派 | C1-F1:membership.active_device 是副机身份唯一来源,整条判定链零断言(三个方向变异全绿) | 恒放行/恒拒这类身份判定改法今天没有任何断言会红 | 仍未了 |
| 待做 | 未指派 | C1-F2:客户端「回环档位不够退回 Edge」降级路的喂食口在生产里由谁写入零断言(掐死喂食口自检 PASS=2140 逐位不变) | 围着降级路的 20 多条断言全部悬空 —— 输入没了它们照样绿 | 仍未了 |
| 待做 | 未指派 | D6-F2:非 paired 条目的 state 是无人校验的自由文本 ⇒ 任何以 state 值实现的豁免天生是后门 | 堵住这条之前,不能用 state 豁免来处理 GET /health 等 DEBT(§5⑥ 明确不推荐走豁免) | 仍未了 |
| 待做 | 未指派(与门禁车道相关) | D6-F3:契约表的遍历源写死三个服务,此刻在跑的 embedding_service(:18084)三条路由 + 另一进程的响应体解析器一条都不在那 30 条账里 | D95 那张表声称的覆盖面比实际小 —— 新服务加进来不会被登记 | 仍未了 |
| 待做 | 未指派 | I8-1:store.json 的原子替换在任何读者持有句柄时整条失败(饱和读者下 89.6% 的写被拒),而唯一的并发防护装在实测不出事的读者一侧 —— 风险方向与防护方向相反 | 最承重的身份文件在并发下写不进去 | 仍未了 |
| 待做 | 未指派 | I8-2:identity-locators.json 非原子整覆盖 —— 唯一一个丢了中枢就起不来、又被明令排除出备份的文件 | 写盘中途崩 ⇒ 中枢起不来且无备份可恢复 | 仍未了 |
| 待做 | 未指派 | C-2:项目自己的假防护扫描器 scan_fake.py 对「恒真断言」这一类今天命中 0,而 0 在它的输出里根本不出现;对本轮实测确认的 4 条恒真断言 0/4 命中 | 项目的自查仪表在这一类上是瞎的,而且瞎了看不出来(0 不打印) | 仍未了 |
| 待做 | 未指派(V15 车道查过并交接了一半) | D3-B1:开机那次连接探测只跑一次、不重试、无定时重探,唯二其它入口都在设置页按钮里 | 实机上副机必须手动进设置页才连得上 —— 机制已定位,修没做 | 仍未了 |
| 待做 | 未指派 | D4-4:V13 那句「不是 V13 引入、此前一次都不可能被触发」与本仓 08-07 自己的记录矛盾 | 一条已入库的归因结论与仓库自身记录打架,未更正 | 仍未了 |
| 待做 | 未指派(与门禁车道相关) | 契约表第⑤组「有没有消费者」按路径匹配不认服务,今天就已经误判(GET /health 被算成有消费者,唯一命中的是 speech 客户端) | 消费者判据出错 ⇒ 覆盖账虚高 | 仍未了 |
| 待做 | 未指派(与门禁车道相关) | 同一服务里把路由挪进第二个 .cs 文件就能从契约账上消失 | 一次无害重构就能悄悄削掉契约覆盖 | 仍未了 |
| 待做 | 未指派 | D99 那条唯一生产触发点 ChatView.cs:816 零断言,而守它的 4 条全是「源码里有没有那个词」 | D99 的行为今天没有行为判据在守 | 仍未了 |
| 待做 | 未指派 | i18n 只有正向护栏(缺键的后果是界面直接显示 ⟦key⟧) | 缺键会直接漏到界面上 | 仍未了 |
| 待做 | 未指派(与门禁车道相关) | 元规则自己点名的「后备控制」(那条真打端点的元断言)对 speech 恰好坐在一个门禁永不执行的文件里 | D95 声称的后备控制对 speech 实际不存在 | 仍未了 |
| 待做 | 未指派 | S6 验收清单 21 项今天打勾数仍是 0 | S6 这条线的验收整体未开始 | 仍未了 |
| 待裁 | 用户(要不要采纳这条跨轮建议) | 08-06 审计建议⑬要的 90-ops/baseline.json(把可计算字段挪出硬编码)今天不存在 —— 审计明说「没做不是缺陷」,但建议仍未采纳 | 不挡(审计明确写「baseline.json 没做不是缺陷」);但硬编码那句话的根治要靠它 | 仍未了 |
| 待做 | 未指派 | parked/selfpair 是有意封存不是残留,但今天没有任何机器可读的东西知道它存在 | 将来任何自动清理都可能把它当残留清掉 | 仍未了 |
| 待做 | 未指派 | 本轮全程零实机、零第二台机器 —— 所有「副机」都是构造出来的 | 一切副机身份/配对结论都未在真硬件上验过 | 仍未了 |
| 待做 | 未指派 | CQ3 的原语清单是手写的,轴 B 完整性只相对那张清单成立;WM_COPYDATA · 共享内存/MMF · COM · WMI · 剪贴板 · 计划任务 XML 六个传输面没查过 | 「17 类」这个数不能声称穷尽 | 仍未了 |
| 待做 | 未指派 | EnvVar 的 96 处命中没有逐条判断哪些是真跨进程的(审计自称「一个知道自己没做完的口子」) | 环境变量这一类缝的真实规模未知 | 仍未了 |
| 待做 | 未指派 | CQ3 的扫描器只吃 .py/.cs ⇒ 若还有第 18 类缝只出现在 .ps1 里,这张表看不见它 | .ps1 承载的跨边界契约整类不可见 | 仍未了 |
| 待做 | 未指派 | 半覆盖那两类(SSE 帧 / 档位词表)只读了 docstring 自述,没有逐条红测 | 这两类「半覆盖」的评级本身没有实测支撑 | 仍未了 |
| 待做 | 未指派 | V11 §3.3 的 C-B1(自配对进程内委托)没复核完,因此没进表 —— 关系没查清,不敢照 V11 原话复述 | 上一轮 V11 的一条发现今天既没被确认也没被撤销,悬着 | 部分解掉 ⚠部分 |
| 待裁 | 用户(缝表里所有车道归属都还没被裁定过) | 缝表「谁来让它变短」那一栏是建议不是裁定,没有验算过任何一条改法真的能落地 | 上面所有「已交别人」的归属都只是审计的建议,不是有效移交 | 仍未了 |
| 待做 | 未指派 | scan_fake.py 报的 87 条静默吞异常仍未逐条复核(上一轮 69 条,同样未做) | 连续两轮积压,规模还在涨(69→87) | 仍未了 |
| 待做 | 未指派 | 记忆库 14 个套件门禁只跑了 3 个,11 个未跑(含 test_gate.py 46 条) | 记忆库这一块的门禁结论只覆盖 3/14 | 仍未了 |
| 待做 | 未指派 | 不声称那 6 条恒真断言是全部 —— 它们只是实测确认的下界,真实条数未知 | 修完这 6 条也不等于这一类清了 | 部分解掉 ⚠部分 |
| 待做 | 未指派 | 不声称上一轮 24 条的闭环判定全部独立复验 —— 除被点名更正的两条外,其余未逐条重验 | §4 那张「真闭环 16 · 半闭环 5 · 一行没动 3」的总账不能当已复验的定论用 | 部分解掉 ⚠部分 |

### `v11-invariant-assertions-and-doc-drafts-2026-08-07.md` —— 30 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待做 | 未指派 | 配套的行为断言(谁在喂第④格:_stamp_activity 中间件 / SSE 只在建连盖一次章 / 续租心跳把计时器打回近乎 0 / health 在豁免表)复核认为站得住,建议一并写,但本包没写 | 不挡(但 B2 那条隐式依赖——今天在喂第④格的是客户端 30 秒续租心跳而不是判据自己——仍然只写在包里,没写进测试) | 部分解掉 ⚠部分 |
| 待做 | 未指派 | §1.4 表里第 4 格「真起进程验 SIGINT」被判『站得住』,却没被写进存活的三条,本包没给它判据与放置位置 | 不挡(但它是七条里最不可能恒真的一格,丢掉可惜) | 仍未了 |
| 待做 | 未指派 | §1.1 那条结构性断言在「发布产物原位跑」时走跳过分支 ⇒ 它在出厂产物上不提供任何保护 | 挡着出厂产物上的这条不变式 —— 用户手里那份包上没有守卫 | 部分解掉 ⚠部分 |
| 待做 | 未指派 | A3 那 15 分钟从来没被实跑过,是读码 + 逐条对齐推出来的 | 不挡(是一条未实测的结论,引用它的人别当已验) | 部分解掉 ⚠部分 |
| 待做 | 未指派(并入时执行) | 文档草稿 §2.1:STATE.md「日常使用」段(633-638 行)要改成分主/副机两种情况 —— 本包只给草稿,没改 | 照原文在主机上再跑一次 start-stack.ps1,会 Stop-All 把客户端已经起好的那套一起收掉 | 仍未了 |
| 待做 | 未指派(并入时执行) | 文档草稿 §2.2:STATE.md:639 要补一句「磁盘上有两份包,路径别指错」(dist\client 不含 V9 自动起栈,dist\client-pack 含) | 照 STATE 去实测 V9 自动起栈的人会用错 exe,得出「V9 没生效」并去找一个不存在的回归 | 仍未了 |
| 待做 | 未指派(并入时执行) | 文档草稿 §2.3:DECISIONS.md:2002 的 D48 标题要去掉「P3c」三个字并加措辞补注(方法论一字未改) | 管理面搬走后,照标题去 20-client-win 里找 /admin/* 的人会找不到,而「谁在结构上够得到」正是 D48 全部安全性质的落点 | 仍未了 |
| 待做 | 未指派(并入时执行) | 文档草稿 §2.4:DECISIONS.md:2016 的 D48 裁定 1 末尾括注要改成「今天:……」+ 一句「裁定本身只有一句话:/admin/* 只在 127.0.0.1 上存在」 | 照原括注读会推出「关客户端 ⇒ 管理面消失 ⇒ 副机也用不了」,而新架构恰恰要求相反的性质成立 | 仍未了 |
| 待做 | 未指派(并入时执行) | 文档草稿 §2.6:90-ops/build-client.ps1:305 那句「AI 模型尚未接入(P4)」仍在出厂,要改成「AI 已接入(P4 · S11,2026-08-05)」 | 这句印在标着「如实说」的段落里、同时印在两份包里 —— 读的人会以为聊天坏了而不去用 | 仍未了 |
| 待做 | 未指派(并入时执行) | 文档草稿 §2.7:build-client.ps1:284-287「怎么装」要补主机上的例外(client\ 与 host\ 必须并排,否则未配对首启时自动起栈整条不走) | 挡着「主机上把客户端目录单独拷走」这一路:主机被判成 fail-closed 的『不是主机』,自动起栈整条不走(经复核收窄:只在未配对首启这一窄场景成立) | 仍未了 |
| 待做 | 未指派(并入时执行) | 文档草稿 §2.9:PROJECT_PLAN_v3.0.md §11 目录结构两处今天就不实(:1879 写成 v2.2 现行 · :1890 的 30-client-web 一个提交都没有过且 D34 已作废) | 挡着拆分加目录那一步 —— 那张图是唯一会被照抄的 | 仍未了 |
| 待做 | 未指派(并入时执行) | 文档草稿 §2.10:DECISIONS.md:3950 的 D87「中枢服务化」要补一段「2026-08-07 部分变了(V9)」并写明本节缺口没有被 V9 填上 | 照原文排期的人会继续把中枢服务化往 P7/P11 放,而拆分已把「管理端后台运行 + 开机自启」当作前提 | 仍未了 |
| 待做 | 未指派(并入时执行) | 文档草稿 §2.11:DECISIONS.md:3960 的 D87「本决议不声称」第一条「只要主机开着」要改成「只要主机那边的中枢在跑」 | 这条被写进了对用户的诚实口径里 —— 照它解释「副机为什么连不上」会归错因:人去查网络与配对,而根因是主机上没人开界面 | 仍未了 |
| 待做 | 未指派(并入时执行) | 文档草稿 §2.12:PROJECT_PLAN_v3.0.md:2259 +同族四处(STATE.md:373/:621/:629 与 DECISIONS.md:3382)的「主机自识别未做 [~]」要改成 [x] 已做,并写明它现在做权限判定、不再是「只做状态显示」 | 危险的一半是「只做状态显示、不做权限判定」——有人据此认为角色判断随便搬,搬的时候就会拆掉那条唯一挡住副机起栈的 fail-closed 护栏 | 仍未了 |
| 待做 | 未指派 | 主机自识别的 GUI 冷启动整条路没跑过 | 不挡(HostSetup.DecideRole 的纯函数部分已有断言 Selftest.cs:7499-7601,缺的是整条冷启动路径) | 仍未了 |
| 待裁 | V10 车道 | 待裁:拆分后管理面挂在哪个进程、由谁绑回环 | 挡着 D48 标题补注最后一句的定稿,也挡着拆分时 /admin/* 的落点 | 仍未了 |
| 待裁 | V10 车道 | 待裁:主机把客户端关掉之后,谁还够得到管理面 | 挡着 D48 裁定 1 括注的定稿;这正是「关客户端后副机还能用」这条新架构性质的核心一格 | 仍未了 |
| 待裁 | V10 车道 | 待裁:副机上记忆能不能编辑/浏览 | 挡着 build-client.ps1「已知边界(如实说)」里记忆库那一条的定稿 | 部分解掉 ⚠部分 |
| 待裁 | V10 车道 | 待裁:「主机管理」在客户端里还剩什么、叫什么 —— 裁定之前不要照 CLIENT_UX_DESIGN §3 实现这一行 | 挡着 P3c 客户端左导航「系统」组那一行的实现 —— 包里明确要求裁定前不许照做 | 仍未了 |
| 待裁 | V10 车道 | 待裁:管理端落在哪个目录(不要提前占目录名) | 挡着 PROJECT_PLAN_v3.0.md §11 目录图的定稿,也挡着拆分开工时新建目录 | 仍未了 |
| 待裁 | V10 车道 | 待裁:主机自识别(HostSetup.DecideRole / MayStartStack)拆分后搬到哪个进程 | 挡着拆分动手 —— 搬错会拆掉唯一挡住副机起栈的 fail-closed 护栏 | 仍未了 |
| 待裁 | V10 车道 | 待裁:「统一客户端 = 唯一入口」这个定位的措辞整条留给 V10,本包一个字没碰 | 挡着 STATE.md:639 那句「日常入口是统一客户端」的定稿(§2.2 只更正了路径与版本戳) | 仍未了 |
| 待做 | 未指派 | 结构性缺口 C-A1:契约登记表那 27 条本身就是漏的 —— lan-edge 的 app.MapFallback 吐着一条有真实消费者的响应契约,提取器的正则结构上抓不到 | 挡着契约登记表的完备性 —— 表里说「全了」而事实上漏了一条有真实消费者的契约 | 仍未了 |
| 待做 | 未指派(拆分开工前必须先修) | 结构性缺口 C-A2:元规则的遍历源写死了两个服务,管理端成为第三个进程后它的路由结构上进不了反向全表 ⇒ 承重性质从「默认判红」退化成「默认沉默」 | 挡着拆分:管理端一落地,契约门禁对它整条路由静默,承重性质当场失效 | 仍未了 |
| 待做 | 未指派 | 结构性缺口 C-A3:「起栈 / 关栈」今天根本不是跨进程契约(起 = 客户端 Process.Start + 端口探测;关 = 网关给自己发 SIGINT 自杀) | 挡着 admin.stack.start/.stop/.status 那几条预期契约 —— 它们没有任何今天的形状可以搬 | 仍未了 |
| 待做 | 未指派 | 结构性缺口 C-C1:「发身份」今天不是 HTTP 契约,是 localai-identity.exe 的退出码 + stdout,这类契约在枚举源里结构上不存在 | 挡着 id.mint/.enroll/.approve/.revoke 四条 —— 换成 HTTP 就是一次全新的契约面,登记表今天看不见它 | 仍未了 |
| 待做 | 未指派 | SSE 三条(聊天 · 同步 · 显存)今天只登记欠着、不定义帧怎么钉,元规则第 ④ 条不覆盖 | 不挡(但这三条在拆分后仍留在客户端 ↔ 网关面上,帧格式没有任何守卫) | 仍未了 |
| 待做 | 未指派 | 契约登记表唯一欠债:GET /health 无消费者(TOTAL=27 PAIRED=26 DEBT=1) | 不挡(拆分后消费者从「无」变成「管理端」,那时这条债会自然还上) | 仍未了 |
| 待裁 | V10 车道 / 协调层 | 待裁:拆分前要不要把预期契约条目「预登记」进登记表 —— 本包只摆出两边代价,不给裁定 | 挡着 §3.2 那 15 条预期契约条目要不要落进登记表;实测证明今天那张表不允许预登记、不支持分表、没有分阶段的账 | 仍未了 |
| 已交别人 | V10 车道 | 契约清单不声称穷尽:它按已裁定的五件事推的,V10 若在拆分中发现第六件,这张表要跟着长 | 不挡(是一条交接约束,提醒别把 15 条当成全集) | 部分解掉 ⚠部分 |

### `admin-app-split-2026-08-07.md` —— 13 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待裁 | 用户 | V10 那份拆分提议整包仍是建议、用户还没逐条裁(所以故意不取 D 号) | 挡着拆分的一整批下游动作:逐文件搬迁边界 · DecideRole 处置 · 网关打包形态 · 出包/自检/门禁的加份 —— 管理端那六条裁定本身也不覆盖拆分全部 | 部分解掉 ⚠部分 |
| 待裁 | 用户 | DecideRole / MayStartStack 的处置(A 降级为管理端自检断言 / B 直接删除)两个方案都没动一个字节,由用户裁 | 挡着 V9 那批刚落地的断言的锚点归属(它们钉在一个将要消失的判据上),也挡着「谁能起 Edge」这个入口的收口 | 部分解掉 ⚠部分 |
| 已交别人 | core(10-core) | 回环 self-enroll(让 hub 在回环上受理主机自己的 enroll)归 core,可先于拆分单独做 —— 拆分本身不会凭空长出这个监听者 | 挡着自配对 blocker 的消失(今天主机自己 enroll 只能走局域网、逼出全局开窗),也挡着 parked/selfpair 分支的重写 | 仍未了 |
| 待做 | 未指派 | 重写 parked/selfpair 分支时,selfpair-review §5 记的那两条假断言(Contains("finally") 分开查那条)要一并修 —— 本包没动那条分支 | 挡着 parked/selfpair 分支的重写(带着两条假断言复活会把假绿一起带回来) | 仍未了 |
| 待裁 | 用户(措辞由第 0 条车道并入) | 「副机不能浏览记忆」说的是人不是 AI —— 这条措辞要逐字写进裁定,否则一定会被实现成「副机上的 AI 不带记忆」 | 挡着「副机不能编辑/浏览记忆」这条裁定的实现口径 —— 写不死就会把副机降级成失忆终端,与 D45/D52 相反 | 部分解掉 ⚠部分 |
| 待裁 | 用户 | 网关随管理端发布的三个打包形态(A 独立 venv 随包 / B python-embed / C 单文件 exe)只给了方案、不实作,建议 A 但要用户选 | 挡着网关随管理端出厂 —— 实测 dist/host 里一个网关产物都没有,网关今天只活在仓库 + D:\AI\venvs\gateway | 仍未了 |
| 待做 | 未指派 | 无论选哪个打包形态,都必须给网关也配一条「产物比源码旧」的守卫(客户端那条已有) | 挡着网关产物「跑的是不是刚改的那个」这条判据 —— 本仓已在这上面栽过 4 次 | 仍未了 |
| 待做 | 未指派 | 出包要再生一份 build-admin.ps1,并出两份版本戳、两份 SHA256、两份安装说明 | 挡着管理端出厂(今天只有 build-client.ps1 一份) | 部分解掉 ⚠部分 |
| 待做 | 未指派 | 客户端自检要拆成两套,而且两套都要有「产物比源码旧」的守卫 | 挡着第二套自检的可信度 —— 没有守卫就天生带着已被记录 4 次的假绿 | 部分解掉 ⚠部分 |
| 待做 | 未指派 | 管理端与客户端各自升级会带来「版本对不上」这个新故障模式,要把管理端纳入 D45 那套 X-LocalAI-Protocol 协商 | 挡着两个 exe 分别升级后的互操作(§11 推翻条件第 1 条就是「版本漂移成为常态故障 ⇒ 退回单应用」) | 仍未了 |
| 已交别人 | V3 车道 | 门禁:run-tests.ps1 的 dotnet 入口反向全表要为管理端加一条 —— 明确写着是 V3 地盘 | 挡着管理端进门禁(反向全表少一条 = 管理端那套自检可以不跑而没人发现) | 部分解掉 ⚠部分 |
| 已交别人 | 第 0 条车道(V0) | P3c「唯一入口」的口径澄清(不推翻,只说明运维面不在射程内)已写好措辞,待第 0 条车道并入中央文档 | 不挡(是澄清不是推翻);但不并入,拆分与 P3c「唯一入口」的关系就没有正式出处 | 仍未了 |
| 待做 | 未指派 | 「管理员和普通用户都可以进」要落成断言:管理端不得包含任何 IsInRole 一类的身份判断 | 不挡;但这条的前提已在两堵墙包里被用户重裁(「都可以进」→「只有机主能进」/「不用提权」),断言口径要按重裁后的读法重新定 | 部分解掉 ⚠部分 |

### `shared-data-topology-host-authoritative-2026-08-08.md` —— 13 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待裁 | 用户 | 共享数据存储拓扑改向(主机单权威 + 副机只读缓存)整包待用户裁 + 待排期,一行生产代码都没改 | 挡整个改向开工。包里建议排在 V14 管理端收工之后、不与之并行(两条都要动 App.xaml.cs 的开机路)。★ 复查:COORDINATOR-HANDOFF 仍把「V17 存储拓扑改向」列在「等用户裁的」里。 | 部分解掉 ⚠部分 |
| 已交别人 | 客户端车道 + 90-ops 车道 | 硬前置:V15 那两条移交(客户端半边断言 · check_contract_pairs 错 note)必须先做掉,才准改拓扑 | 挡拓扑改向开工。★ 实查:两处至今都是原样(Selftest.cs:8044 钉 FrameContinues;check_contract_pairs.py:187 仍是旧 note)⇒ 硬前置**未满足**。 | 仍未了 |
| 待裁 | 用户 | 待裁:要不要为「往共享会话追加一条消息」单开排队例外;不开就得改掉 D52 补充「先记在本机」那句 | 挡改向裁定②/③的收口。不裁就会出现「一边留着『人常常就是想先把事记下来』这句承诺、一边做不到」。 | 仍未了 |
| 待裁 | 用户 | 降级共享(家庭待办改成个人时中枢那份还在)—— TodoCenter 里明写待裁,建议随改向一并裁掉 | 不挡;它今天已经挂在代码里待裁,改向正好给它一个落点(新模型下等于「从主机删掉 + 本机新建一条个人的」,要按 D52 规则 C 同级确认)。 | 仍未了 |
| 待做 | 拓扑改向那条车道(未指派) | 改向前必须新增的判据①:「空」与「没拿到」要长得不一样 —— 没有新鲜快照时界面不许渲染空/短列表 | 挡改向落地。待办列表是混合的(个人在本机、家庭在主机),主机不可达时它会少掉一部分,而一张少掉一部分的列表和「本来就只有这些」长得一模一样 —— 这是改向新引进来的形状。 | 仍未了 |
| 待做 | 拓扑改向那条车道(未指派) | 改向前必须新增的判据②:counts 从「顺带」变「承重」—— 增量帧下 counts 与 data 对不上 | 不挡今天(客户端不看 counts);改向后如果有人拿它当「我该显示几条」,就会显示一个和列表对不上的数字。 | 仍未了 |
| 待做 | 未指派 | D86 裁定③ 的界面那半【从来没做过】:SyncClient.Conflicts / ClearConflicts 全仓零界面消费者 | 不挡今天;但退休留痕里绝不许把它写成「曾经需要、现在不需要」。包里记为「有机制、没调用点」这一族的第四次。 | 仍未了 |
| 待做 | 未指派 | 同步的归因判据今天就是错的(判词「被另一台改过」,判据却是「不是我写的」)—— 建议换成服务端 superseded | 不挡今天(Conflicts 没有界面消费者,所以永远发现不了);改向后副机看到的每一条都来自主机 ⇒ 那张表会包含全部记录,接上界面就满屏都是「这条被另一台改过」。 | 仍未了 |
| 待裁 | 用户 | 墓碑两个用途只退休一个 —— 裁掉哪个必须说清,否则「删除又变成删不掉」 | 挡改向落地。裁错(整条删掉)会让删除再也传不出去 —— 那正是 2026-08-05 用户实测报过的「删除时还是没法同步删除」,等于兜一圈回到原点。 | 仍未了 |
| 待裁 | 未指派(单独一条决议) | 可选的真简化「帧永远发当前可见全集」—— 明说不在本改向里顺手做,该单独一条决议 | 不挡;它能一次换掉一整族「丢一帧就永远补不回来」的缺陷,但动的是契约语义,所以只是先留了个位置。 | 仍未了 |
| 待做 | 未指派 | 聊天面档位表今天咬不咬人 —— ★ 未核,引的是 V13 的结论,本包没有独立复核 | 不挡;包里据此明写「不声称七条都答完了」——第 7 条有一格没有独立证据。 | 仍未了 |
| 已交别人 | 第 0 条车道 | V17 的中央文档改写草稿(D86 加 ⚠ 留痕段 · D52 补充第一节整节改写 · STATE 增量 · PITFALLS 补注)本车道不改,待第 0 条车道并入 | 依赖上面那条用户裁定。★ 与 D112/D113 不同(那两包的草稿已并入 DECISIONS.md),这一包至今没并入、也没取号。 | 仍未了 |
| 待做 | 第 0 条车道 | 既有文档漂移:D52 补充第一节「待办/日历(各自 Apple 账号,不经主机)完全可用」在 D57 与 D86 之后就过期了 | 不挡代码;但改正稿压在一份未并入的草稿里,而错的原文还在中央文档上挂着。 | 仍未了 |

### `sync-snapshot-pull-on-connect-2026-08-08.md` —— 12 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待做 | 用户(两台真机场次) | 「副机开机拉不到共享会话」从未在真机复现过 —— 三条路堵上了,但两台真机验收仍欠着 | 挡这条症状的结案。副机侧 mTLS 私钥在 CNG 里不可导出,Python 工具替不了;「冷启动」这个条件单机也造不出来。§7 已写好两台机器的验收步骤。 | 仍未了 |
| 待做 | 未指派 | 扣下 ≠ 撤回:已经同步出去、后来变得不合格的记录,副机上那一份追不回 —— 本车道不做 | 不挡;但要真追回得新立一条「撤回」语义,形状与墓碑同级、风险更高。DECISIONS D113 裁定④ 把这条原样收进去了。 | 仍未了 |
| 待做 | 未指派 | 读侧没有额度维 —— /v1/sync/snapshot 这条路上一个闸都没有,连不上的机器每 30 秒拉一次全量 | 不挡今天的功能;这是一个明写「没人守」的方向,sync_policy 只对 sync_write 计额度。 | 仍未了 |
| 待做 | 未指派 | Absorb 持续失败时的补拉重试没有节流/退避 —— 单飞闸只收并发,串行重试不受限 | 不挡;Absorb 持续失败且数据在变时,每一帧都会点一次拉全量。 | 仍未了 |
| 已交别人 | 90-ops 车道 | 移交 90-ops:check_contract_pairs.py 里 sync.snapshot 那条 note 写的用途现在是错的 | 挡下一个改这条路的人。★ 实查:90-ops/gate/check_contract_pairs.py:187-192 至今仍是旧文(「用途**不是**重连对齐……而是丢一帧之后的补全量」)⇒ 这条移交**没做**。 | 仍未了 |
| 已交别人 | 客户端车道 | 移交客户端车道:CONTRACT:sync.snapshot 的客户端半边钉错了位置(钉的是断层判据 FrameContinues) | 挡 V17 拓扑改向开工(V17 把它列为硬前置)。★ 实查:20-client-win/app/Selftest.cs:8044-8045 至今仍钉 FrameContinues(0,0) ⇒ 这条移交**没做**。 | 仍未了 |
| 待做 | 未指派 | 红测有一个方向没干净地红过:「data 里少发一个集合」今天的表现是崩溃,不是判红 | 不挡;但这条断言只有「多一个键」那个方向被干净地验过,少一个键会把整套打崩、看不出是哪条没守住。 | 仍未了 |
| 待裁 | 对应车道(HubClient 禁区)+ 用户 | 移交:开机路径上拨号打不通时要不要自动走一次 RediscoverAsync(今天只挂在设置页底部那张设备卡上) | 挡「副机开机自己连上」。这是实机那句「手动进设置滑到底才开始连」的代码形状:换网段/DHCP 变了之后,唯一的修法只有用户滚到设置页底部才点得到。 | 仍未了 |
| 待裁 | 用户 | 移交:裁「主机客户端的同步面要不要也走回环」,并连带裁归因迁移(库里旧记录的 device 要不要改名) | 挡主机客户端同步面的身份收口。一换名字,库里已经归在主机 LAN 名下的记录会突然看起来像「另一台写的」,界面提示会集体指错人。 | 仍未了 |
| 已交别人 | 客户端车道 | 「Sync.Start() 在 LoadStores 之后」「那句拉全量一定到达」今天没有任何断言钉住 —— 建议客户端车道补 | 不挡;但开机路上「同步一定起得来」这件事今天靠的是静态阅读,不是断言。 | 仍未了 |
| 待做 | 未指派 | App.xaml.cs 开机路上有几处不在 SafeImport 保护内 ⇒ 存在「进程活着、没窗口、同步流一次都没起过」的路 | 不挡(包里明说它不解释 08-07 那两格);但这是一条真实存在、且没有任何东西会自愈的路。 | 仍未了 |
| 待做 | 未指派 | 跨进程契约欠债仍是 1/30 —— 只剩 GET /health 没成对 | 不挡(收工要求是欠债不许涨,两条车道都没涨);是一笔一直挂着的存量债。 | 仍未了 |

### `v20-chat-view-fixes-2026-08-08.md` —— 10 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待做 | 未指派(并入 DECISIONS.md 时执行) | V20 五条尚未取 D 号:并入 DECISIONS.md 时要统一编号,并把九个源码文件里的 `V20-①..⑤(D?)` 注释同步换成真号 | 挡着这五条裁定进中央文档 —— 在取号之前它们只存在于包里和源码注释里 | 仍未了 |
| 待做 | 未指派(并入 DECISIONS.md 时执行) | ASSERTION-PITFALLS 第 16 条与 WPF-PITFALLS 第 14 条已就位,但还要在 DECISIONS 里引一句 | 不挡(两条坑本身已落地,缺的是中央文档里的引用) | 仍未了 |
| 待做 | 未指派(并入时执行) | STATE.md 里的客户端自检基线 2189 已过期,要更新成本轮实测的 2262(worktree 形态) | 不挡(但三种环境的数不可互比,写回时要标明是 worktree 形态) | 仍未了 |
| 待做 | 未指派(需要一台装着模型的主机) | ④ 用户报的「左下角不再说『暂无』」没有端到端验过 —— 自检断言的是算法,不是真机上那一行字 | 挡着「用户实测报的那个现象真的修好了」这句话 —— 今天只能说算法对了 | 仍未了 |
| 待做 | 未指派(出包动作,不在本轮范围) | GUI 没跑过:①②④⑤ 的可见效果全靠无头断言 + 真 WPF 排版,没有在真机上点开客户端看过 | 挡着四条修复的真机确认 —— worktree 里没有单文件产物,按 D46 护栏发新版要关进程 + 覆盖 dist\client | 仍未了 |
| 待做 | 未指派 | ⑤ markdown「画出来好不好看」没有断言,如实不测 | 不挡(这是有意的:少一条诚实的判据好过多一条假的;记号认得对不对那一半已由 MarkdownLite 纯解析断言覆盖) | 仍未了 |
| 待做 | 未指派 | ① 的自检里没有跑消息循环,排在 DispatcherPriority.Loaded 的东西不会执行 | 不挡(包里论证这使判据比真机更严:真机上归零→拉回底两帧都发生,自检只留下归零那一帧,而归零本身就是缺陷) | 部分解掉 ⚠部分 |
| 已交别人 | 翻译线 | 翻译工作空间的目标池/语言池状态没有喂给模型(ChatClient.StreamAsync 只发 messages,TheApp.Translation 一个字节都没进去)—— 归翻译线,本轮不做 | 挡着「在翻译空间问 AI 能不能看到目标池」这条用户实测路径(今天 AI 答『我无法访问任何外部系统』);而它自己被下面那条待裁挡着 | 仍未了 |
| 待裁 | 未指派(权威在中枢侧;翻译线动手前要先有这条裁定) | 待裁:客户端状态怎么进提示词 —— 系统提示词的权威在中枢(system_prompt.ensure),客户端擅自往 messages 里塞状态会造出第二套提示词来源 | 挡着翻译线把工作空间状态接进 AI 那一步 | 仍未了 |
| 待做 | 未指派 | 仓库门禁的契约欠债 1/30 本轮未变(仍欠一条) | 不挡(V20 没碰契约面;这条与 V11 包里记的 DEBT=1 是同一笔账,只是总数从 27 涨到 30) | 仍未了 |

### `broker-unload-reconcile-2026-08-08.md` —— 8 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待裁 | config 车道(本包边界外) | config/vram-budget.toml 里 speech.lite 的 peak=2.07 与 launch.toml 的 device="cpu" 互相矛盾,没改;要么重测,要么加一个与 peak 分开的 runtime_vram 字段;且「两份数据源说相反的话」今天没有任何断言在守 | 一个实测 0 显存的组件仍按 2.07 GiB 占准入闸的预算额度;两份数据源的矛盾无断言守,会再次静默漂移 | 仍未了 |
| 待做 | 客户端侧(20-client-win 当日由 V14 在动,避让合并冲突) | Selftest.cs 里 gpu.snapshot 的客户端 fixture 没补上新增的四个键(residency_truth / reconcile_log / reconcile_note / footprint) | 不挡门禁,也不挡契约配对(新增键对客户端可加);但成对断言的严格度在这一格上欠了一次 | 仍未了 |
| 待裁 | 未指派(要动的是「谁该查状态」这条设计,超出本包) | serves_requests() 仍然零调用点 ——「RECONCILING 仍然提供服务」是遗漏出来的正确,不是被守住的性质 | 哪天有人加一个查 Broker 状态的端点,RECONCILING 下仍能服务这条性质会静默消失,而没有断言会红 | 仍未了 |
| 待做 | 按需平面/过账口径那条车道 | request_on_demand 撞上「端口已经有人」时走认领,不起进程不占显存,但闸已按它的 peak 算过一遍并放行,账是错的 | 不挡(方向是多算、安全的),但按需平面的显存账面长期偏高 | 仍未了 |
| 待裁 | 用户(口径裁定,不该由本车道顺手做掉) | p4-broker-shape 决议里「采样器崩了该进 DEGRADED_SAFE」与实现不符(实现只记 sampler_error),查出来了没改 | 已生效决议与实现对不上;照决议读代码的人会以为采样器崩溃有降级保护 | 仍未了 |
| 待做 | 未指派 | vram_not_reclaimed 落 READY 那条路上 intended 与 committed 会不相等,而这一格没有任何不变式/断言在守 | 不挡(I2/I3/I4 都不问 intended,面板勾选来自客户端自身,界面看不出异常);但差异无人守 | 仍未了 |
| 待裁 | 未指派(删边是一次口径变更,不该由本车道做) | 白名单里 READY → RECONCILING 那条直达边今天没有驱动者,已登进 _EDGES_WITHOUT_DRIVER 欠债表,没顺手删 | 不挡(欠债表已登记、断言不红);但 §8.1 状态图与实际驱动者对不上,这张表只许变短 | 仍未了 |
| 待裁 | 未指派 | reconcile_to_actual 没做成 HTTP 端点(只挂在网关开机路上),因为新增契约要在 Selftest.cs 写客户端半边而那个文件当日由 V14 在动 | 运行中若账本与现实长期对不上,人没有入口手动「以现实为准对齐账本」,只能重启网关 | 仍未了 |

### `host-loopback-business-route-2026-08-08.md` —— 6 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待裁 | 用户 | ChatClient 仍走 lan-edge:改成回环会顺带给主机 E1 解除权,属安全扩权,须单独裁定 | 挡住「主机在 chat 面上也是 trusted-local」这件事收口;不挡今天的功能(包里说 chat 与 GPU 租约身份不交叉、不咬人)。DECISIONS D112「已知未收口」里原样保留了这条。 | 仍未了 |
| 已交别人 | V15 车道 | SyncClient 仍走 lan-edge —— 本包判为 V15 禁区没动,主机在 sync 面上仍是 device_id | 不挡今天的功能;挡住「主机在各面上是一个身份」的收口。与 V15 包 §11.4 那条「请裁同步面要不要也走回环」是同一件事的两头。 | 仍未了 |
| 已交别人 | 另一条车道(包里没点名) | 网关没有随包发布 ⇒ 纯发布安装的主机自动起栈起不了网关 —— 本包明说是另一条车道的债 | 挡纯发布安装的主机自动起栈:网关起不来时回环与 lan-edge 两条业务路一起没有;对着仓库跑的开发机不受影响。 | 仍未了 |
| 待做 | 未指派(实机场次) | 实机两格只差鼠标那一层没点 —— 要覆盖正在跑的 dist\client 才能验界面事件 | 挡 P4 那两格的界面事件层证据。路由、档位、端点三环都实测过了,只差真的用鼠标点一遍。 | 部分解掉 ⚠部分 |
| 待裁 | 用户(网关侧身份语义) | principal_device 对回环一律返回固定名 local ⇒ 主机客户端租约进了共享桶,本包有意不修、等裁定 | 不挡今天;挡住「主机客户端的租约有自己的名字」。同机任何 trusted-local 调用方(含服务账户 ai-mem)调一次 /v1/session/end 就能把它们一起放掉。 | 仍未了 |
| 待做 | 客户端车道 | V13 唯一生产触发点(App.xaml.cs 里 NoteRole 那一行)今天只有结构判据守着,行为判据没做 | 不挡(删行/挪位两个反证都实测能红);但那一行是否真的在开机路上被执行过,今天没有行为层断言。 | 仍未了 |

### `boot-role-autostack-2026-08-07.md` —— 5 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待做 | 未指派(出包/发布) | dist/host 里根本没有网关 —— 网关只存在于仓库里,没有随包发布 | 挡发布安装上的自动起栈与「意图即起」的引导层 —— 它决定了 EnsureStackAsync 在发布安装上能做到什么。与 V13 包 §1.4 记的是同一笔债的两个出处。 | 仍未了 |
| 待做 | 未指派 | 启动Edge.cmd 里写死了 192.168.178.61 —— 换路由器/网段就废,而 D36 明说地址要用主机名不用 IP | 挡换路由器/换网段之后的主机上线;包里标为「只报告」,没有人接。 | 仍未了 |
| 待做 | 未指派 | AI 根靠 Split-Path 猜(D91 说过那是猜测),LocateGateway() 照抄了 —— 正解是在 paths.toml 里给网关登记 venv 键 | 不挡今天(本次有意不引入第二套口径);但布局一变,start-stack.ps1 与 LocateGateway 会一起错。 | 仍未了 |
| 已交别人 | 出包流程 | GUI 冷启动整条路没有跑过 —— 要证「点开图标它自己起」得先出一次包再冷启动,属出包流程 | 挡「点开图标它自己起」这句话的证据。今天证到的是机制(按同一条命令行手工起、4 秒后探到 200),不是冷启动。 | 仍未了 |
| 待做 | 未指派 | 「起了一半」(HalfUp)的失败路径没有实机制造过 —— 只钉了判据,没真把 Edge 弄挂看界面 | 不挡;HalfUp 这一格只有判据被钉住,界面上它长什么样没人看过。 | 仍未了 |

### `p4-closeout-selfpair-and-resident-write-2026-08-07.md` —— 4 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待做 | 用户(须在非提权上下文跑;由我拉起 lan-edge 会撞 TPM 完整性等级的环境噪音) | 自配对没做「杀掉中枢 → 冷启动客户端 GUI → 肉眼看它自己跑完」那一遍(只对两道闸各量了一次) | 自配对整条链没有端到端跑通的实测;两道会挡住它的闸单独验过,合起来那一趟没验到 | 部分解掉 ⚠部分 |
| 待裁 | 未指派(包里判断「那不是自检该做的事」) | EnsureEdgeAsync 那三条新断言只是结构判据,没有升级成行为判据(要真起一个中枢:要身份、要证书、要端口) | 不挡;但起栈参数那一格只守得住源码形状,守不住真实起栈行为 | 仍未了 |
| 待做 | 未指派 | 起栈进程日志的 broken pipe 风险没根治(让中枢自己写文件,或套一层 cmd /s /c 重定向),留给专门一次改动 | 不挡(窗口很小,且与 DevicesView 今天的做法逐字一致);根治方案本身还会牵动「Edge 还活着吗」的判断对象 | 仍未了 |
| 待做 | 未指派(三条车道都只维持、未还这一笔) | 跨进程契约欠债仍是 1/30 —— 只剩 GET /health 没成对 | 不挡门禁(欠债表已登记);但契约成对表始终差最后一条 | 仍未了 |

### `pressure-yield-task-pause-2026-08-07.md` —— 4 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待裁 | 协调层 | V8 收到的派工指令被截断(任务 C 正文缺失、无【拥有】/【禁区】清单),自定的边界与依据等协调层复核或推翻(含两处越界报备) | 不挡(活已干完并留痕);但本车道的边界判断和两处越界(App.xaml.cs 一行、Selftest.cs)还没被确认 | 部分解掉 ⚠部分 |
| 待做 | 未指派 | 压力让位阈值 1.5 GiB 不是实测最优值 —— 真机上桌面显存波动的分布没有测过 | 不挡;但阈值定高定低会直接决定用户任务被打断的频率,今天没有实测依据 | 仍未了 |
| 已交别人 | 将来做「有真实断点的长任务」的那条车道 | 「再开」不保证从断点续跑 —— 有真实断点的长任务接进来时,续不续、从哪儿续归那条车道 | 不挡(今天的按需任务是不确定进度的,没有断点这个概念) | 仍未了 |
| 待做 | 未指派 | §8.1 三段预览之外的界面欠账没查过 | 不挡;但界面与方案书之间除三段预览外还有多少缺口,今天没人量过 | 查不出 ❓查不出 |

### `admin-app-phase2-migration-done-2026-08-08.md` —— 4 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待做 | 未指派 | 记忆溯源的跨进程深链没做:溯源条目不再是可点链接,要去客户端按 id 找 | 不挡 P3a 验收硬线(「每条可溯源」——溯源信息本身还在),挡的是「一键跳过去」这个体验;原因是 chat.json / projects.json 归客户端独占。 | 仍未了 |
| 待做 | 未指派 | admin/SelftestMoved.cs 里的 Body/CodeOnly/Slice 是客户端那三个的第二份,还没提成 Services/SourceProbe.cs 共编一份 | 挡着「同一段逻辑只存在一处」这条纪律:本轮唯一一处两份并存,而且两份漂了不会有任何东西红(DECISIONS.md D114 里逐字写明:一旦漂了,裁定的 DEBT 就变成缺陷,须立刻提成共编一份)。 | 仍未了 |
| 待做 | 未指派(「将来」) | 本轮踩的坑只记进了代码注释,「够格的」还没并进 ASSERTION-PITFALLS | 不挡。但本轮里第 1 条已是第 11 次复发、第 15 条那条推论又整份退回重做,不写进 PITFALLS 就只能靠人记住。 | 仍未了 |
| 已交别人 | 用户 / 实机场次(铸身份不可回退,且提权上下文下铸出的身份用户打不开) | 那两页的出包实测用的是桩中枢,真起中枢(要先铸身份)没做、也明说不该我做 | 挡着「真中枢 + 真身份」下的端到端证据:桩只打在「回环那个端口后面坐着谁」,HTTP→解析器→视图→可视树没被替;而中枢真的不在时,那一节整段 SKIP 并明说「这一节没跑,不要读成那两页没问题」。 | 仍未了 |

### `admin-app-lifecycle-2026-08-07.md` —— 3 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待做 | 未指派 | 管理端那六条裁定(生命周期状态机·角色判据改指向物·皮肤同步·UI link·托盘关栈入口)一条都没实现,一行生产代码没改 | 挡着整个主机管理端的落地 —— 而本包 §0 把管理端定成了「解开 P4 那三格 ❌ 的前提」 | 部分解掉 ⚠部分 |
| 待做 | 未指派 | 实机那条 ⚠(压力即让 / 任务暂停恢复 / 证书归因 无法测,缺停栈入口)退回未解;裁定⑤「托盘右键关闭 = 关栈入口,调 safe_to_stop_stack()」仍是要做的事 | 挡着实机三项(压力即让 / 任务暂停恢复 / 证书归因)——没有停栈入口就测不了;另有一份已并 main 的决议包在这条假解上算过拆分权衡的净额 | 部分解掉 ⚠部分 |
| 待裁 | 未指派 | 客户端与管理端两个托盘图标怎么区分 —— 今天没有方案,且不许默认「加个角标就行」(16×16 高 DPI 会糊) | 挡着管理端托盘常驻上线后的可用性(用户会看到两个分不清的图标) | 仍未了 |

### `admin-app-two-walls-measured-2026-08-08.md` —— 2 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待做 | V14 车道(本轮实现) | 管理端本轮实现的三条硬要求还没做:HKCU 自启 + --tray、清单里不得有 requireAdministrator、不靠右键「以管理员身份运行」 | 挡着把「不用提权」从一句愿望变成一条做得到、且验得了的性质 | 部分解掉 ⚠部分 |
| 已交别人 | P3b.2 阶段 | signer-service-account 的 ACL 隔离当时就知道、并被推迟到 P3b.2(Ca.cs 只用 CurrentUser 作用域是 D43 S0.10 的有意选择) | 不挡本轮(走【甲】,身份作用域一个字不动);挡的是将来【乙】那条跨账户/服务账户的路 | 仍未了 |

### `admin-app-packaging-2026-08-08.md` —— 1 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 待做 | 未指派(要驱动 shell,自检进程里做不到 ⇒ 实机/人工) | 「在通知区域上右键真的会弹出这个菜单」这个动作没有被自动化验证过 | 不挡出包。但托盘右键是「真正关闭」的唯一入口,今天只钉住了「菜单挂上了 + 那一项在 + 点它真的关得掉」,没钉住「右键弹得出这个菜单」。 | 仍未了 |

### `admin-app-phase2-migration-map-2026-08-08.md` —— 1 条

| 类型 | 归谁 | 未了项 | 挡着什么 | 查实 |
|---|---|---|---|---|
| 已交别人 | 第 0 条车道(按 D111 裁定③,中央三文档只在 main 上改) | V14b 这份迁移地图的 D 号至今未取,没并进 DECISIONS.md | 不挡代码。★ 本次实查 DECISIONS.md:D112=V13、D113=V15、D114=V21,V14b 这份地图今天在 DECISIONS.md 里查不到号,标题仍是 `D?`;包里写的「当前最大 D107」也 | 仍未了 |

---

## §4 已被别的车道顺手解掉的 41 条(留档 —— 它是这张表被查过的证据)

> ★★ 不查这一步的话,上面那张表会有 **41/212 ≈ 19%** 是假的。
> **一张假的欠债表比没有表更坏 —— 人会照着它排优先级,而排错的那部分永远不会自己暴露。**

| 出处 | 原以为未了的 | 实际(实证) |
|---|---|---|
| `broker-unload-reconcile-2026-08-08.md` | host-loopback-business-route 与 sync-snapshot-pull-on-connect 两份决议包本轮仍未取 D 号(第 0 条车道第二次报欠账) | 提交 daa9a0e「取号 D112(V13 回环业务路由)/ D113(V15 sync 全量拉取)」。DECISIONS.md:5715 = D112,其出处行 DECISIONS.md:5785 逐字写着 `decision-packets/host-loopback-business-route-2026-08 |
| `p4-closeout-selfpair-and-resident-write-2026-08-07.md` | V10 那份拆分包仍是建议、用户还没裁,本包一行都没照它改 | 实质已裁并已执行:D108 开篇 DECISIONS.md:5346「2026-08-07 实机场次之后用户裁定**直接上管理端**」;D112 里 DECISIONS.md:5764「③ 不动管理端(**已裁定拆分**,另一条车道)」;D114(DECISIONS.md:5869 起)裁定① 记 3100 行搬完的逐 |
| `p4-closeout-selfpair-and-resident-write-2026-08-07.md` | 交草稿:PROJECT_PLAN_v3.0.md:388 P4 清单 S10 那格要追加 D107 补正(本车道没动中央四文档) | PROJECT_PLAN_v3.0.md:2340-2353(行号已随文件生长从 388 挪到 2340):`- [x] **★ 权限档位六元组**` 那一格下面就挂着「> **★ 2026-08-07 补正(D107)**:这个 ✅ 此前**盖过了一处没有实现的规格**」,含实测 200 的复现、change_res |
| `p4-closeout-selfpair-and-resident-write-2026-08-07.md` | 交草稿:ASSERTION-PITFALLS.md 要新增一条「源码判据有两种运行环境(仓库/出厂产物)」(本次真踩了,挡过一次出包) | 00-docs/ASSERTION-PITFALLS.md:638「## 11. ★★ 源码判据有【两种运行环境】,写的时候要两种都想到 —— 已踩 1 次(2026-08-07)」,:642 写明两种环境(仓库原位 / `build-client.ps1` 第 [2] 步的出厂产物原位)。同一提交 51b0f30。★ |
| `p4-closeout-selfpair-and-resident-write-2026-08-07.md` | 交草稿:STATE.md 要追加 V12 那条收盘记录(自配对不可达 + 副机能写驻留集合) | 00-docs/STATE.md:218-237「> ### 2026-08-07 · V12 · P4 签字前两处收口(**D107**)」,两条都在::219-223「自配对在开机路上【结构上】不可达 …… 两道闸,只补管理面是不通的」、:224-227「副机今天真能写 `intended_resident_set |
| `admin-app-lifecycle-2026-08-07.md` | 单实例锁今天是会话级 Local\,拆分后升 Global\ 涉及 ACL 与提权上下文,改之前要先想清楚谁能打开它 —— 待裁(已从 B 级升为硬前置) | D108 裁定⑤(DECISIONS.md:5396-5400)明裁「升成 Global\ 是错的」,改按用户作用域。实现在 app/Services/InstanceLock.cs:1-31 文件头带实测证据(SeCreateGlobalPrivilege 只给 S-1-5-19/20/32-544/6,没有 BUI |
| `admin-app-lifecycle-2026-08-07.md` | 管理端 csproj 要 link 的 Theme 目录里,Icons.cs 与 ThemeManager.cs 是否一并 link —— 待定 | 两条都 link 了:20-client-win/admin/localai-admin.csproj:39(ThemeManager.cs)与 :40(Icons.cs)。而且被反向钉住 —— app/Selftest.cs:8794-8805 把裁定④点名的 12 份(五个原语 + 五个皮肤字典 + ThemeMa |
| `admin-app-lifecycle-2026-08-07.md` | V11 那两堵墙今天是否仍然成立,本包没有重新核对,原样移交给接手的人去读 AUDIT-2026-08-07-v11-toward-split.md §4 | V14 车道做了独立复核并出包:decision-packets/admin-app-two-walls-measured-2026-08-08.md(275 行,§1 墙①、§2 墙②,全部带实测 DACL / CngKey 容器路径 / EnableLUA=0)。结论已入库为 D108 裁定③④(DECISIONS |
| `admin-app-lifecycle-2026-08-07.md` | P4 那三格 ❌ 的修法本身尚未裁定:根因是「主机客户端掉到了 lan-device」,方向是让它别掉下去,具体怎么做没填 | PROJECT_PLAN_v3.0.md:2491 那句「~~正确方向是让主机客户端不要掉到 lan-device —— 具体怎么做属于裁定,本文不替它填~~」已被删除线划掉,:2492-2494 换成「★ 2026-08-08 已裁并落地(D112):已判定为主机的那台,业务调用直连回环网关 ⇒ trusted-lo |
| `admin-app-split-2026-08-07.md` | HubClient 里那组打 /admin/* 的方法(ListDevicesRawAsync / RevokeDeviceAsync / ParseDevices)建议整组删除,今天还在维护同步 | app/Services/HubClient.cs:754-755 逐字「★★★ V21:『主机管理 API』那一组(ListDevicesRawAsync / KnownDevices / CacheDevices / RevokeDeviceAsync / ParseDevices)**整组删除**」。D114 裁 |
| `admin-app-split-2026-08-07.md` | 要跟着搬的断言没有精确数:要靠「把断言逐条按锚点归类」才算得出,本包明确把它推给实作阶段 | 实作阶段真的算了:decision-packets/admin-app-phase2-migration-done-2026-08-08.md:103-125 §4「断言账」给出逐类去向 —— 整块搬进管理端 / 判词翻面(逐条写明为什么不是改宽)/ 合并(MayStartStack 四条 → 元断言①)/ 新增(元断 |
| `admin-app-two-walls-measured-2026-08-08.md` | 管理端要扛得住被提权启动:照客户端 D46 那道护栏(先试开密钥,打不开就自己降权重开,重开不成才如实拒绝) | admin/Program.cs:41-63:先 `Elevation.IsElevated() && !Elevation.DeviceKeyUsable(out var keyNote)`(问真问题:密钥打不打得开,不是问 IsInRole),打不开就 `Elevation.TryRelaunchAtMediumI |
| `admin-app-two-walls-measured-2026-08-08.md` | 第二位家庭成员是否存在没问过用户,【甲】(只有机主能进)的前提未验;将来真要用是一次新裁定,登记点是 caller-accounts.toml 的 trusted_local | 前提被换掉了,所以不再需要验:D108 裁定③(DECISIONS.md:5373-5387)记着用户当天晚些时候的澄清 ——「原意是『不需要以管理员身份运行』,不是『另一个账户也能打开』」⇒ 实现口径仍是【甲】,但**理由从『受限于 OS』换成『需求本来如此』**,并明写「这不是文字游戏:前者意味着将来有人会来推翻它 |
| `AUDIT-2026-08-08-full.md` | N2:AdminAppPresent 今天结构上恒为 false(全仓无脚本产 localai-admin.exe),DecideRole 第②条肯定证据恒不成立 | main 上跑的不是 V18 的实现(包 §0 自己写明代码没合 main),是 V14b 的。20-client-win/admin/Selftest.cs:177-254 `RunLive()` 真 `Process.Start` 客户端 exe(状态目录指到临时目录)→ 等它拿到单实例锁 → `ClientLin |
| `AUDIT-2026-08-08-full.md` | 缝⑪:端口/上游地址常量这一类缝完全无覆盖 | D114 已裁并落地(00-docs/DECISIONS.md:5869-5882,提交 045b5b8;实作提交 6af0bce/54630d6)。实测:管理端现有四个页签(20-client-win/admin/AdminWindow.cs:39-42「主机中枢 / 模型 / 记忆库 / 客户端与栈」),对应 ad |
| `AUDIT-2026-08-08-full.md` | 待裁:dist\host 此刻正在服务却没有任何脚本能重建它(无 VERSION.txt / SHA256.txt,两个产物身份判据都只看 dist\client) | 7 条各有落点,V21 逐条裁了并落地:① `HubGpu` → 拆 partial,解析那半提成 20-client-win/app/Services/GpuWire.cs(:1-20 文件头逐字写明「✗ 整个链进管理端会拖进半个客户端 / ✗ 另写一份是第二个解析器」),两个 csproj 编同一份;② `Memo |
| `AUDIT-2026-08-08-full.md` | 待裁:worktree 残留清理(451 MB 死壳 + 目录名已不标识车道),本轮只出建议未执行 | 整族净删,不是搬。20-client-win/app/Services/HubClient.cs:753-774 那段 V21 注释逐字写着 `ListDevicesRawAsync` / `KnownDevices` / `CacheDevices` / `RevokeDeviceAsync` / `ParseDev |
| `AUDIT-2026-08-08-full.md` | A3 残留:STATE 的「下一步」整段过期,与同一份 STATE 顶部的 08-07 实机场次直接打架(上一轮点名三处里唯一一字未动的那处) | 由 V16 收口并取号 **D109**(00-docs/DECISIONS.md:5440-5537)。逐条对上代码:① 期望值从 peak 换成实测足迹 —— 10-core/gateway/gpu_broker.py:643 `self._footprint: Dict[str,float] = {}`、:896 |
| `AUDIT-2026-08-08-full.md` | 不声称报告写完时仍然准确:审计跨度内 main 前进 8 个提交,结论未对齐审计之后的 main | 查已经有人替着做了,答案是**零生产调用点**。10-core/gateway/gateway.py:898 是定义;全仓仅 test_gpu_broker.py:2619 / :2626 / :2636 三处调用,gateway.py 内没有任何路由暴露它。V14 车道把这条实测写进了提交了的源码:20-client |
| `admin-app-packaging-2026-08-08.md` | C-3 没解:dist\host\ 今天无人产出——本车道只做到「不去覆盖它」 | 被测对象整组已删,该断言无对象。10-core/gateway/gateway.py:891 那段留痕自述:b4fd524 删掉 _stack_shutdown_sweeper · 它在 startup 里的接线 · _last_request_at 中间件 · stack_idle_seconds · peers_o |
| `admin-app-packaging-2026-08-08.md` | 管理端关栈时「请客户端优雅退出」那一支,自检里结构上做不到,没测 | 被测的那行手法已删:git log -S'SIGINT' -- 10-core/gateway/gateway.py 只有两条 —— a84cfec 加、b4fd524 删;今天 10-core 全树 grep SIGINT 只剩 test_gpu_broker.py:2638 那条「safe_to_stop_stac |
| `admin-app-packaging-2026-08-08.md` | 管理端自检在「无桌面」环境下整段 SKIP、写 LIFE=0、门禁判红这条分支,没红测过 | 结论作废,实跑无对象。00-docs/audit/AUDIT-2026-08-07-v11-toward-split.md:186-190 逐字:「自动关栈已删 ⇒『15 分钟后自己关掉』这件事不会再发生,本条的结论随之作废」,条目从 A 降为 B、标题改成「起栈是一次性动作,没有持有者」;删的那笔是 b4fd524( |
| `admin-app-packaging-2026-08-08.md` | DecideRole 判出 IsHost=true 那一格没有真机端到端跑过——要一台铸过身份的机器,交实机场次 | 提交 875606b(V11 §2 文档草稿并入七处)。00-docs/STATE.md:830-848:「★ 2026-08-07(D101)起,「用之前先跑 start-stack」这句要分主/副机看」,主机一条(含「两个前提缺一不可」)、副机一条(「这条命令根本跑不了」)、手跑的场合一条,并附「为什么必须分开写」 |
| `admin-app-packaging-2026-08-08.md` | 管理端还没有自己的图标,托盘上与客户端分不开 | 提交 875606b。00-docs/STATE.md:851-857:「★ 本机磁盘上现在有两份包,路径别指错」+ dist\client\ 与 dist\client-pack\ 各自的版本戳。★ 草稿那处被判**已过期而没照抄**:并入时两份包都已含 V9 自动起栈,于是按今天事实写,并加了一句更要紧的「以 di |
| `admin-app-packaging-2026-08-08.md` | 设备 / 组件 / 存储三页没有搬,管理端还是个壳 | 提交 875606b。00-docs/DECISIONS.md:2002 标题今天逐字是「D48 · 管理面**只绑回环**;配对窗口不再随启动自动打开」——「P3c」三个字已去;:2004-2012 是那段措辞补注(方法论一字未改 / 载体 vs 裁定 / 拆分后挂哪个进程 ⏳)。 |
| `admin-app-packaging-2026-08-08.md` | run-tests.ps1 没接「跑管理端自检」这一步——留给 V0 裁要不要接进 -Full 段 | 提交 875606b。00-docs/DECISIONS.md:2025 括注今天是「(**今天**:主机上的客户端走回环够得到 /admin/*,副机连不到那个端口)」;:2026-2033 补注逐字含「裁定本身只有一句话 —— /admin/* 只在 127.0.0.1 上存在,它对任何进程一视同仁」,并点名 D1 |
| `admin-app-packaging-2026-08-08.md` | %TEMP% 里那份别的会话留下的 Selftest.cs 污染源没清,还会骗到别的读源码的工具 | 磁盘上那份三处都改了(文件确实不在仓库,.gitignore:39)。dist/host/主机-开机上线.txt:5-11 =(a)主机定义换成「铸过中枢身份、持有 CA 私钥与成员表的那一台(D36)」,IP 降级成「今天的事实,不是身份的一部分」并写明「该换的是地址,不是去怀疑身份坏了」;:25-42 =(b)②  |
| `admin-app-phase2-migration-done-2026-08-08.md` | GUI 点击实测没做——屏幕控制被用户拒,只能换成自检里的真触发 | 提交 875606b。90-ops/build-client.ps1:355 今天逐字是「· AI 已接入(P4 · S11,2026-08-05):聊天能逐字回答。」,并按草稿补了「★ 前提是中枢那台在跑」与「记忆库是这台机器自己的本地库」两句。 |
| `admin-app-phase2-migration-done-2026-08-08.md` | 归档删除后的记忆对账没做:溯源链上会出现「点得到、但原文其实已经删了」的条目 | 提交 875606b。90-ops/build-client.ps1:328 是【怎么装】段,:337 逐字「⇒ 主机上请保持 client\ 与 host\ 并排。」;提交信息记明是按复核后收窄的版本写的(只在「未配对首启」这一窄场景成立),没照抄被推翻的原提法。 |
| `admin-app-phase2-prereqs-2026-08-08.md` | A3 那 7 份「链不动」的文件不是 csproj 缺条目,是迁移要做的切分决定,交给动手的人逐条裁 | 2026-08-07 实机场次跑到了:两台真 PC · 包 20260807-2340+6c3cbf0,00-docs/STATE.md:20 与 00-docs/PROJECT_PLAN_v3.0.md:2449 逐字「✅ 通过:自动起栈(**没跑 start-stack**)· 自配对走完」,00-docs/wor |
| `admin-app-phase2-prereqs-2026-08-08.md` | HubClient.ParseDevices 只剩自检在调,V19 明说「本轮不动」,留给 §2.4 那一族一起裁 | 绑回环的仍是 lan-edge(D48 一个字没动):20-client-win/admin/Services/HostProvision.cs:378-396 逐条写明用 run-lan 而不是 run 的理由(run 不传 AdminPort ⇒ 回环管理面根本没绑),并注明「run-lan 放到网卡上的是业务口  |
| `admin-app-phase2-prereqs-2026-08-08.md` | 契约门禁「第七条」之外有没有第八条,没人查过——V19 明确不声称 | 答案是管理端。DECISIONS.md:5350-5354(D108 裁定① 生命周期八条)逐字含「客户端关闭(主/副)⇒ 管理端**仍挂后台**」与「管理端点 × ⇒ 缩托盘,真正关闭只能托盘右键」;而 /admin/* 的消费者今天住在管理端进程里(admin/Services/HubAdmin.cs 整块)。不变 |
| `admin-app-phase2-prereqs-2026-08-08.md` | 寄生在死代码/名字上的断言到底有多少,没人系统查过——V19 明确不声称只有那 5 条 | 用户裁定:**不能**。20-client-win/admin/Services/MemoryCenter.cs:6 逐字「★★ 依据(V10 §0.4 用户裁定):**副机上不能编辑记忆,也不能浏览**」,:11-13 划清边界「『副机看不到记忆』≠『副机上的 AI 没有记忆』—— 模型在主机上跑、检索在主机上做,记 |
| `admin-app-phase2-prereqs-2026-08-08.md` | V19 这份前置包的 D 号至今未取,没并进 DECISIONS.md | 由代码里那条路径决定,不由「好看」决定:00-docs/decision-packets/admin-app-packaging-2026-08-08.md:68-92「判据是 `<客户端 exe 所在目录>\..\admin\localai-admin.exe` ⇒ 管理端必须出到 `<Out 的同级>\admin\ |
| `v11-invariant-assertions-and-doc-drafts-2026-08-07.md` | §1 的三条不变式断言只交回了「形状」,本包零生产代码改动 —— 断言本身还没写进 Selftest.cs / test_gpu_broker.py | 剩一颗按钮。20-client-win/app/Views/SettingsView.cs:115「★★ V14 裁定第 5 条:设置里那颗「打开管理端面板」按钮」· :126 Ui.Primary("打开管理端面板", …) · :133 分组小标题 Ui.Subtitle("主机管理端")。判据是**安装事实**不 |
| `v11-invariant-assertions-and-doc-drafts-2026-08-07.md` | §1.3 方向二那条断言【不要照抄】:被测对象已被 b4fd524 删除,必须按 safe_to_stop_stack 的新形状(三格:非恒假 / 四条理由不许合并 / 必须有调用点)重写 | 源码 20-client-win/admin/(localai-admin.csproj,含 Services/ 10 个文件 + Views/ 4 个文件 + Selftest.cs/SelftestMoved.cs);产物 dist\admin\,可分发 dist\admin-pack\(磁盘上已有 localai |
| `v11-invariant-assertions-and-doc-drafts-2026-08-07.md` | safe_to_stop_stack() 今天到底有没有调用点,本轮没查 —— 按 V9 自己立的 D102 通则这正是必须钉住的一件事,是留给下一个人的第一件事 | 答案是**拆开**:角色判据留客户端、起栈那半搬管理端。20-client-win/admin/Services/HostProvision.cs:7 逐字「留在客户端的是**角色判据**(DecideRole / DetectRoleAsync / ResolvesToThisMachine / …)」;:182 逐 |
| `v11-invariant-assertions-and-doc-drafts-2026-08-07.md` | 文档草稿 §2.5:dist/host/主机-开机上线.txt 三处要改(主机定义绑死 IP · ②每次上线整段没有网关 · 「只做配对演示不需要 gateway」),而该目录在 .gitignore 里、不在仓库中,并 | 拆分已经发生完了,而这张表**一条都没长**,所以这笔交接已经关掉 —— 且答案与 §3.2 的预测相反。契约总数 27→30 那 +3 来自语音(git log 显示 6a647cc「P5 语音 v1 骨干」动的 check_contract_pairs.py),不是拆分;D108 裁定⑥「本条**不产生**新契约条 |
| `v11-invariant-assertions-and-doc-drafts-2026-08-07.md` | 文档草稿 §2.8:CLIENT_UX_DESIGN_v1.md §3 要加一段事实陈述——「主机管理」这一行拆分后不再是客户端左导航的一行 | 提交 977b6f0「STATE 基线更新:三个数分开写并注明不可互比」。00-docs/STATE.md:242-270 现在是 2026-08-09 · main@045b5b8 的三个数:① -Full PASS=4404 FAIL=0 · ② `client --selftest` **2092**(Relea |
| `v11-invariant-assertions-and-doc-drafts-2026-08-07.md` | 待裁:目录约定拆分后怎么排(今天要求主机上 client\ 与 host\ 并排) | 90-ops/build-client.ps1:247-260(提交 45ce2ba)新增 [3] 段:dotnet publish 20-client-win/admin/localai-admin.csproj → $AdminOut,并 `if (-not (Test-Path $adminExe)) { …ex |
| `host-loopback-business-route-2026-08-08.md` | 打通回环后撞出的三条 Broker 缺陷(speech.lite 不吃显存 / RECONCILING 卡死只能重启 / 孤儿 llama-server 被重整采纳)不归本车道修 | 00-docs/PROJECT_PLAN_v3.0.md(提交 f8614b4)第 3 行现在是「**已验收**:P0 / P1 / P2 / P3a / **P3c**」,第 4 行新增「**★ P3b = 部分,不是"已验收"**(2026-08-08 更正,详见下条)」,并在 :6-14 附了「★★★ 2026- |

---

> ★ 本表由第 0 条车道生成于 2026-08-09 凌晨(基线 `main@c17f164`),
> **表格内容由脚本从查实结果直接生成,没有人工转抄** —— 转抄会失真,而本仓两天内已经因转述失真出过一次错
> (第 0 条车道自己给的 P4 根因错了一半,由 V13 复核推翻,见 D112 裁定①)。
> ★★ 下次更新:重跑同一套扫描 + 查实,**并把范围从"按日期"改成"按未关闭"**(见 §1 末尾那条)。
