# 协调层交接 · 2026-08-08

> 写给**下一任协调层**(上一任上下文用尽)。
> ★ 本文件**只写 git 里查不到的东西**。局面、数字、决议、审计报告一律**自己去查**,
>   下面第 1 节给了重建状态的那一条命令。**不要引用本文件里的任何数字** —— 它们写下的那一刻就开始过期。

---

## 1. 先重建状态(别看转述,自己量)

```bash
cd "E:\.meine\.Proj_Soft\.Proj\.localAI" && git log --oneline -10 && git status --short && git worktree list && grep -o '^## .* · D[0-9]*' 00-docs/DECISIONS.md | tail -1 && grep -o 'PASS=[0-9]* FAIL=[0-9]*' 00-docs/STATE.md | head -1 && python 90-ops/gate/check_contract_pairs.py 2>&1 | grep TOTAL= && head -2 dist/client/VERSION.txt | tail -1 && git ls-remote --heads origin
```

一条命令回答:main 在哪 · 谁还没并 · D 号到几 · 门禁基线 · 契约欠债 · 出厂包是哪版 · 远端同没同步。

**再读三份**(读结构与最近一节,不要读全文):
`00-docs/STATE.md` 的「当前阶段」与「下一步」· `00-docs/ASSERTION-PITFALLS.md` **全文** ·
`00-docs/audit/` 里最新那份审计。

---

## 2. 你是什么

**协调层不写生产代码。** 你做四件事:

1. **派工** —— 把用户的裁定拆成车道 prompt,划清拥有/禁区;
2. **核实** —— 车道交回的每一条重要结论,**自己量一遍**(见第 3 节,这是本文件最重要的一节);
3. **裁技术判断** —— 能从已有决议推出来的,你裁并留痕;推不出来的,交用户;
4. **保护串行点** —— 中央三文档、D 号、合并顺序。

★ **第 0 条车道**(main 上那条)才做合并与中央文档并入。你不要自己合分支。

---

## 3. ★★★ 上一任在这一天里错了十一次,全是同一族

**共同形状:从一个不完整的观测下结论。** 逐条列出来,因为它们是可复现的陷阱:

| # | 说错的 | 漏了量什么 |
|---|---|---|
| 1 | 「双击 .cmd = 普通用户,TPM 密钥会绑错完整性等级」 | 没量运行上下文(本机 `EnableLUA=0`,**根本没有 Medium**) |
| 2 | 「P3d 两道闸串联:闸一放行还会撞闸二」 | **没数调用点**(两个函数签名不相交,而且 `unseal_for_prompt` 生产调用点 = 0) |
| 3 | 「实测支持 STATE ⇒ 改方案书 S6 的勾选」 | **没数清单项**(拿一条证据支持了一张 25 项的清单)|
| 4 | 「dist/host 的双击启动器已不在」 | `ls \| head -10` **截断了**,没看完输出 |
| 5 | 关栈条件②「全网空闲 ≥ 阈值」 | **没读那个属性自己的注释** —— 它写着"被心跳刷新,永远到不了" |
| 6 | 「改成 run-lan 会把管理面推上局域网,违反 D48」 | 没读 `Program.cs:1287`(管理面**硬编码 Loopback**) |
| 7 | 「已踩 8 次」写成加粗 `**8**` | 判据**依赖了 markdown 排版** |
| 8 | 「"中央四文档"是我的加码」 | **认错本身也没查** —— D92 原文逐字写着四份 |
| 9 | 「照 V10 §2.1 逐文件搬」 | 那份表的**行号基于三天前的基线**,文件早已长了 |
| 10 | `grep -c "admin"` 判定契约门禁已修 | **判据太松**,命中的是契约名里的 "admin" |
| 11 | `grep -c "ClientSourceRoot"` 判定 main 已有 | 同上,量的是车道工作树不是 main |

### ⇒ 落成四条纪律,请照做

1. **凡在规格里引用一个具体的量(属性/字段/函数),下笔前先读它自己的定义与注释。**
2. **凡断言"某处没有 X",先确认自己的判据不会被截断、不会被排版骗、不会命中同名的别的东西。**
3. **认错也要先查。** 第 8 条是为了认错而说了一句不实的话。
4. **★ 车道有权推翻你。** 上一任写过"照它搬,不要重新判归属",而车道复核后拒绝执行 ——
   **它是对的**。每份 prompt 里都带上一句:「**先复核协调层的说法,我已经错过很多次**」。

---

## 4. 车道 prompt 的固定件(每份都要有)

```
【★ 收工判据】报告「跑完」之前必须:① `git status` 为空 ② 已合进 main ③ 交回带**提交号**。
  **只有活干完而没提交 = 没干完。**
```
★ 这条不是客套 —— 2026-08-07/08 两天内**六次**东西躺在工作树里差点丢
(doctor ⑫ 环 · V11 两份产物 · V15 四份 · V13 一批 · V19 · **V21**),
而 **V21 是连报两遍**的:它自己在决议包里写着「这一轮两次报告『跑完』,而 git 里一条提交都没有」。

### ★★★ 而且:**「跑完」必须用机器判据,不看车道的自述**

上面那条是给车道的**要求**;下面这条是给你的**验法** —— 两者不是一回事。
**六次里没有一次是车道主动说"我没提交"的**,全都是报告「跑完了」。
⇒ 交回的那一刻,协调层**第一件事**跑这两条,**在读它的交回正文之前**:

```bash
git -C <worktree> status --porcelain | wc -l     # 必须是 0
git rev-list --count main..<branch>              # 必须 > 0
```

> **两个数不是 `0` 和 `>0` ⇒ 当作没跑完。** 不管交回写得多详细、数字多好看。

★ 为什么要"在读正文之前":交回正文里那些实跑数字**是真的**——
它们就是在那个没提交的工作树里跑出来的。**先读正文会让你相信它跑完了,而那与"提交了没有"无关。**
★★ 这两条命令是**机器判据**,而交回正文是**自述**。
本项目对这一对的裁决一向是同一句:**判据看得见的,不问它自称是什么。**
★ 临时目录会被清理 —— 一份没提交的活,和没干过的区别只在你什么时候发现。

### ★★★ 边界怎么画 —— 上一任在同一天里画错了三次(2026-08-09)

| 车道 | 错法 | 后果 |
|---|---|---|
| V24 | **禁区太紧**(「`app/Selftest.cs` 是禁区」) | 它被逼在「印一个说谎的数字(`PASS=0` vs 真实 2100)」与「护栏不接门禁」之间二选一 —— 两条各违反一条成文判词。**它停下来问了,那是对的** |
| V24 | **拥有太窄**(只写 `app/Views/**`) | 同一族缺陷散在 `app/Services/` 五处,**既不在拥有清单、也不在禁区** ⇒ 它只能回来问 |
| V26 | **约束会产出死代码**(「只许在文件末尾追加」) | EOF 追加**没有调用点**,唯一分派在别人的 hunk 里 ⇒ 加了也不会跑,**正是本仓踩过四次的「零生产调用点」**。它拒绝执行并绕道,那也是对的 |

**共同根因:按文件与位置画边界,而缺陷不按文件与位置分布。**

⇒ 边界段要回答三个**问题**,不是列三个**目录**:

1. **这一族缺陷在哪儿?**(不是「这个目录归谁」)——
   派工前先扫一遍同族,把散落的一并点名;扫不全就明说「发现同族散在别处直接报,别自己憋着绕」。
2. **车道要加的东西,有没有地方挂调用点?** ——
   ★ 如果你的约束让它挂不上,**那条约束本身就在造死代码**。
3. **撞车是同一个 hunk 才算撞,不是同一个文件。** ——
   9000 行的文件,一个插在 :44、一个追加在 :9200,git 自动合。
   把整个文件设成禁区,换来的不是安全,是一个更坏的实现。

> ★ 这三次都是**车道停下来问或拒绝执行**才没铸成错。第 4 条纪律(**车道有权推翻你**)
> 在同一天被行使了三次 —— 它不是客套话,是这套流程唯一的兜底。
> **每份 prompt 都要带上那句「先复核协调层的说法,我已经错过很多次」。**

### ★★★ 第四次,而这一次的形状是新的:**我给的"要普查这几处"的清单,一处都不对**

2026-08-10,管理端滚轮那条。我在 prompt 里给了诊断 + 一张普查清单:

> 真凶是 `Wheel.cs` 不在 admin 的 csproj 里 ⇒ 一处都没挂。
> **普查管理端每一个内层可滚区(设备列表 · 模型列表 · 记忆库列表 · 组件面板)。**

车道核完之后**三处全推翻**:

| 我写的 | 实况 |
|---|---|
| 真凶是「没 Link」 | Link 只是**必要条件**。不写投放逻辑,Link 了也一处挂不上 |
| ★★ 普查这四处 | **这四处一个都不是 `ScrollViewer`**,全是裸 `StackPanel` ⇒ **照这张清单去修,会一处都修不到** |
| ★ 「管理端有 ScrollViewer(`Ui.Page` 返回的就是)」 | 那指的是**最外层**那一个,而外层恰恰是**唯一不该挂让路逻辑的** ⇒ 按我的话去挂,**比不挂更坏** |

真凶是**页中页**:窗口用 `Ui.Page` 当页容器,而某一页**又用 `Ui.Page` 当自己的根**;
内层那个 `ScrollableHeight == 0`,而 WPF 的 `ScrollViewer.OnMouseWheel` 是**无条件 `Handled = true`**。

⇒ **教训不是"我诊断错了"** —— 那是第 3 节那一族,已经记过。
**新的是:一张清单比一个错误诊断更危险。**
诊断错了,车道复核时会撞上;而**清单看起来像帮忙**,它把我的错误诊断**固化成了车道的工作范围**,
车道逐条做完还会得到「四处全修好了」的**假象** —— 而真正坏的那一处根本不在表上。

**⇒ 纪律:prompt 里给【怎么找】,不给【找哪几处】。**
· 可以给:症状 · 已量到的实据(带 file:line)· 判据形状 · 已知同族;
· ★ 不要给:一张"就这几处"的清单 —— 除非你**逐处验证过它们确实是那一类**,
  并明写「这张表是我量的,可能不全,**以你自己扫的为准**」。

其余固定件:
- **拥有 / 禁区 / 共享**三段,禁区要点名**正在跑的车道**碰的文件;
- **契约欠债**当前值,并写「收工时必须仍是这个数」;
- **D 号写 `D?`**,当前最大号由你给,并注明"取号在并入那一刻"(D75);
- **中央三文档只读,出草稿**(D111 已装 pre-commit 闸,车道分支碰它们会判红);
- 需提权的动作按 **D46** 交用户双击,车道不自跑。

---

## 5. 这个项目的判词(引它们比讲道理管用)

- **给错原因的提示比不给提示更坏。**
- **跑不了和跑过了必须长得不一样。**
- **没红过的护栏和没有护栏是一回事。**
- **只有活干完而没提交 = 没干完。**
- **一个不能变大的欠债表是装饰。**
- **零命中与全清白长得一模一样。**
- **一个修法漏改一处,缺陷就完整地留在那一处。**
- **临时测量工具本身要先被验**(上一任三次栽在自己的 grep 上)。

---

## 6. 用户的工作方式(这条影响你怎么问问题)

- 他会**逐条裁定**,而且裁得比你提的选项更完整
  (例:压力即让那条,他补出了 D87③ 原文没有的"任务暂停 + 恢复路径")；
- 他**不喜欢抽象** —— 问他架构问题时要给**具体影响描述**(「你开游戏时会发生什么」),
  不要给架构名词。他明说过一次:「没有具体影响描述的话我很难理解」;
- 他会做**实机测试并逐格报结果** —— 那是这个项目最贵的输入,
  2026-08-07 那次一口气验出三格 ❌,而当时门禁 4197 条全绿;
- **他的裁定要立刻落进 decision-packet**,别攒在对话里。上一任攒过一批,
  差点随上下文一起丢(后来靠 `admin-app-lifecycle-2026-08-07.md` 抢救回来)。

---

## 7. 交接那一刻还挂着的(★ 2026-08-11 重写 · 只列"人知道才知道"的,**数字自己查**)

> 上一版(08-09 早)整节已过期,**不要照它行事**:它写的三格 ❌、网关不可达、关栈没有执行体
> —— 全部已解并实机复验。**旧版留在 git 历史里,不在这里。**

### 今天(2026-08-10/11)一天并入十一条车道 —— 先跑第 1 节那条命令自己量

一句话局面:**挡在"用户能验"前面的东西第一次全清了。**
出厂三件同轮、零 `.dirty`;D 号已落到目前最大;门禁 `FAIL=0`。
★ 但**门禁退出码是 1** —— 判定逐字是「FAIL=0,**没跑起来 2 个**」(客户端与管理端两半
「产物比源码旧」被闸拒绝执行)。**「FAIL=0」与「门禁过了」不是一回事**,STATE 里分开写了。

### ★ 立刻能做的(都不需要用户)

1. **重跑一次 `-Full` 补齐仓库形态那两个数** —— 出包已完成,产物比源码新了,**现在跑就有**;
2. **V24–V28 五条 + 存量那批包取号** —— 它们已并入 main 而抬头仍挂草案标记;
   ★ 那道闸今天已经能抓住**新增的**(D 号闸补了「该取号而没取」这一半),
   存量按只许变短的表冻着 —— **表只许变短,别让它变长**;
3. **并发闸仍然不存在**(只登记了)。★ 上一次出包没撞上,
   **是因为只有一条车道在跑,不是因为它被挡住了**;
4. **出包哨兵还没带 `OWED`/`LIFE`** ⇒ 出包形态那两个数**分不开「测不了」与「本该跑而没跑」**。

### ★★ 等用户的

- **实机**:复验他 08-10 报的十条(滚轮 · 弹窗 · 顶栏 · 图标 · 就绪闸 · 假进度条 ·
  气泡按钮 · 默认按需 · 人设 · 法规话)+ 那张 27 格表的 §5 六格 + 副机三条 `Test-NetConnection`;
  ★ 全部实机反馈与结果在 `decision-packets/p4-realmachine-findings-2026-08-09.md`(**持续追加**);
- **记忆库造测试数据**:唯一来源是人直接改 `%LOCALAPPDATA%\LocalAI\admin\memory.json`,
  ★ **必须先退管理端**(它只在 OnStartup 读、改一次存一次会覆盖);
- **待裁**:隔离路线(用户已裁「先做 B、C 先测一格再裁」,★ 那一格改 spike 一处就能当场验)·
  D81 剩四条(★ 第五条是**记错的话,已划掉**)· 记忆库 B 档 · V17 存储拓扑。

### ★★★ 今天这一任错了什么(比局面更该读)

**边界画错四次**(禁区太紧 · 拥有太窄 · 约束会产出死代码 · **给的普查清单一处都不对**)——
四次都是车道拦下来的,已写进第 4 节。

**被两句不带时态的留痕骗过两次**:
`ChatView.cs` 那句「异常全吞」(那条早已修掉)· `HostProvision.cs` 那段探 8443 的陷阱(早已改成 8442)。
⇒ **判词:带时态的留痕不会骗人,不带时态的会。**

★ 而**第 0 条车道犯了同族的一次、并写进了中央文档**:它 grep 标签 `V33` 零命中,
就断言「那条工作没有对象」,而工作是以**别的名字**提交的。已原地划掉带时态更正。
⇒ **零命中不是不存在。** 判据问的是「这个字符串在不在」,而要回答的是「这条工作在不在」。

### 今天新立的判词(引它们比讲道理管用)

- **带时态的留痕不会骗人,不带时态的会。**
- **一个说不清自己是哪一版的产物,它验出来的东西也说不清是谁的。**
- **旧的观察也是没观察。**(快照比这次意图旧,就没资格说"它不在")
- **一个承诺了动作的确认框,比没有按钮更坏。**
- **一个不知道自己进度的进度条,是在假装它知道。**
- **一张清单比一个错误诊断更危险** —— 诊断错了车道会撞上,清单会被逐条做完并给出"全修好了"的假象。

### ★ 两条分支永远不许合

`parked/selfpair`(DO_NOT_COMMIT)· `worktree-agent-ad36411fae961778d`
(V18 存档 —— 合它会**删掉一万多行**今天的工作,它的价值早被逐项吸收)。

## 8. 最后一句

这个项目最值钱的性质是:**它的状态几乎全部外化在 git 与文档里**,所以协调层可以换人。
**代价是纪律**:裁定要落包、数字只写在唯一一处、车道收工必须提交。
你接手时如果发现某条纪律"被绕过了很多次",**先问它是不是写得太宽**——
D82 已经因此失效过两条(`main` 只接受合并 → D92 推翻;`00-docs` 其余只读 → D111 收窄)。
**一条太宽的规矩装不了闸,因为它会天天误报,而误报会训练人绕过它。**
