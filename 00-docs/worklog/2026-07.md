# 开发记录 — 2026-07

> 格式定义: 方案书 **v2.2** §12.1
> 类型: 决策 · 实作 · 实测 · 问题

---

## 2026-07-26

### [实测] P0 · 环境盘点 — 三处与文档不符

**做了什么**:对磁盘、缓存、工具链、GPU、ComfyUI 做只读盘点。

**观察到的数字**:

```
Disk 0  Samsung 9100 PRO 1TB  ->  C:  387.7 GB 空
Disk 1  Samsung 9100 PRO 2TB  ->  D:  578.7 GB 空
                              ->  E:  467.1 GB 空
F: 为虚拟光驱,非存储
```

**三处与文档不符**:

1. **D: 与 E: 是同一块物理盘。** 方案书从未提及 —— 这意味着代码根(E:)与数据根(D:)
   在同一个故障域内,且「备份到另一块盘」在当时无法实现。已记入 `paths.toml` 注释与 STATE.md。
2. **`cmake` 不在系统 PATH。** 文档说「VS2022 自带 CMake 已装」是对的,但编译时需用
   Developer Command Prompt 或手动加 PATH。属会踩的坑,已记入 STATE.md 阻塞表。
3. **桌面显存占用实测 ~1107 MiB**,低于文档估计的 1.7GB。不过取决于当时开了什么,
   正式数值待 P1。

**其余核实一致**:`nvcc` 未在 PATH(CUDA Toolkit 确未装)· Python 3.12.10 · git 2.54.0 ·
node v24.16.0 · ffmpeg 8.1.1 · `extra_model_paths.yaml` 不存在而 `.example` 存在。

---

### [问题] F1 结案 —— 审计的推测是错的

**背景**:`AUDIT_v2.0.md` F1 指出「15.7GB」在 v1 中同时指「模型资产」与「HF 缓存」,
并推测「那批模型早已在 D:\ComfyUI,v1 记录有误」。

**实测**:

```
C:\Users\<user>\.cache\huggingface  =  15.71 GB / 21 files
  models--RunDiffusion--Juggernaut-XL-v9          6.62 GB
  models--InstantX--InstantID                     3.91 GB
  models--xinsir--controlnet-union-sdxl-1.0       2.34 GB
  models--xinsir--controlnet-openpose-sdxl-1.0    2.33 GB
  models--ezioruan--inswapper_128.onnx            0.52 GB
```

**真相**:v1 的两处描述**指的是同一批数据**。那批模型就是通过 HF hub 下载的,
躺在缓存目录里,所以它既是「模型资产」也是「HF 缓存」—— **v1 没记错**,
只是没人意识到这两个名字指同一样东西。

而 `D:\ComfyUI\models` 里的是**另一份独立副本**,不是审计推测的「同一份」。

**结论**:审计 F1 的问题定性对(口径确实混淆),但对成因的推测错了。已记入 DECISIONS.md,
待下一版方案书与审计报告一并修订。

---

### [实测] HF 缓存与 ComfyUI 副本 SHA256 逐文件比对

**做了什么**:对两侧所有 >100MB 文件计算 SHA256 并比对。耗时 26 秒(12:42:38 → 12:43:04)。

**结果**:

| SHA256 (前 8 位) | 大小 | ComfyUI 侧对应 |
|---|---|---|
| `c9e3e68f` | 6.62 GB | `checkpoints\Juggernaut-XL-v9.safetensors` |
| `02b3618e` | 1.57 GB | `instantid\ip-adapter.bin` |
| `9fae2e50` | 2.34 GB | `controlnet\union-sdxl-promax.safetensors` |
| `c8127be9` | 2.33 GB | `controlnet\instantid-controlnet.safetensors` |
| `b8524e55` | 2.33 GB | `controlnet\openpose-sdxl.safetensors` |
| `e4a3f08c` | 0.52 GB | `insightface\inswapper_128.onnx` |

**重复 6 / 无对应 0。** 且 HF blob 的文件名(即预期 sha256)与实算值全部吻合,缓存未损坏。
无符号链接 —— 是真实的第二份副本。

---

### [实作] P0-2 冗余回收

先移入隔离区(跨盘移动 15.71GB 耗时 4 秒,约 3.9 GB/s),写 `MANIFEST.md`;
随后依你的明确要求提前清除,保留清单作为审计痕迹。

**净效果**:C: 387.7 → **403 GB**;D: 无净变化;`D:\ComfyUI\models` 61 files / 16.92 GB 完好未动。

设 `HF_HOME` = `D:\AI\cache\hf`、`TORCH_HOME` = `D:\AI\cache\torch`(用户级),今后下载不再落 C:。

---

### [实作] P0-5 目录结构 · paths.toml · pre-commit 钩子

按 v2.1 §11 建立 `AI_ROOT`(21 个目录)与 `D:\AI`(13 个目录)。

`config/paths.toml` 落地,含五个逻辑根、缓存收编、GC 水位线、备份配置,
并把「D: 与 E: 同盘」这条实测事实写进注释。

**钩子已实测**,不是「写完就算」:

```
✗ 绝对路径: 10-core/gateway/_hooktest.py
      2:MODEL_PATH = "D:\AI\models\foo.gguf"
      3:CACHE_DIR  = "E:/tmp/whatever"
>>> 退出码 = 1
```

豁免规则:`config/paths.toml`、`00-docs/**`、`.githooks/**`、`*.md`。

---

### [实作] P0-7 git 初始化 + 三份记录文件

`git init -b main`;`core.hooksPath=.githooks`;`core.autocrlf=false`。
按 v2.1 §12.1 建立 `STATE.md`、`DECISIONS.md`、`worklog/`。

同时按 v2.1 附录规则,把 `PROJECT_PLAN_v1.md`、`PROJECT_PLAN_v2.md` 移入 `00-docs/archive/`
—— 现行文档不引用已作废文档。

---

### [决策] 备份改为手动触发 + 脚本保证一致性

你告知有移动固态并会手动备份。据此把 `[backup].status` 从 `not_provisioned` 改为 `manual`,
并写了 `90-ops/backup/backup.ps1`:

- 从 `paths.toml` 读根路径(脚本内零绝对路径,能通过自己的 pre-commit 钩子)
- **拒绝备份到与源同一块物理盘**(靠 `Get-Partition` 比对 DiskNumber)
- 检测目标盘 BitLocker 状态,未加密则警告
- state 全量 · adopted 全量 · code 走 `git bundle --all` · models 只备清单+哈希
- 产出 `SHA256SUMS.txt` 与 `BACKUP-REPORT.md`(含恢复步骤)

**尚未实跑。** 下次插盘时先 `-DryRun`。

---

### [问题] 待你确认:`cache/hf` 是否排除出 GC 自动清理

v2.1 §8.4 定 CACHE 为「唯一可静默清理的根」,85% 水位自动清。
但 HF 缓存重获成本高(十几 GB,且模型可能下架)。
暂行设 `hf_cache_auto_purge = false`,**这是对方案书既定策略的偏离,待你确认后写回文档。**

---

### [实作] P0-3 接网线 —— 问题在端口不在线材

**过程**:接上网线后 Realtek 2.5GbE 只协商到 **100 Mbps**,比 WiFi 7 的 866Mbps 协商值还低。

先排除本机因素:「连接速度和双工模式」= **自动侦测**,未被锁死;
驱动 1125.27.50.919 (2025-09-19)。但发现一批省电项全开:
`节能乙太网路(EEE)` · `环保节能` · `Gigabit Lite` · `Power Saving Mode`。

**当时的判断是错的。** 我把线材列为第一嫌疑(理由:100 Mbps 只需 2 对线,
1G/2.5G 需 4 对全通,所以 100M 典型对应「只有 2 对可用」),端口问题排在第二。

实际原因是**插错了口** —— 插在蓝色 WAN 口上。开链路监控(0.4 秒采样)后换到 LAN1,
捕捉到完整协商过程:

```
13:29:02  Up             100 Mbps    ← 蓝色 WAN 口
13:30:00  Disconnected               ← 拔线
13:30:03  Up             1 Gbps      ← LAN1
```

**线材完全正常**(能跑 1 Gbps 说明 4 对线全通,至少 Cat5e)。省电项也没动过。

**最终实测**:

| 项 | 值 |
|---|---|
| 链路 | 1 Gbps 全双工 |
| 到网关延迟 | < 1 ms(30 次全部低于 PS 的 1ms 分辨率) |
| 抖动 | 0 |
| 丢包 | 0 / 30 |
| 路由优先级 | 以太网 IfMetric 25 < WLAN 35 ✓ |

方案书接 2.5GbE 的目的是「消除 WiFi 抖动风险」—— **该目的已达成,且与带宽无关**。
未达 2.5G(FRITZ!Box LAN 口速率待查),但不影响任何项目目标。

---

### [实测] 一个需要修正预期的观察:瓶颈不在网卡,在宽带

下载 CUDA installer(2.3GB,NVIDIA 官方 CDN)时,速度稳定在 **约 12 MB/s ≈ 96 Mbps**。

链路是 1 Gbps,所以瓶颈在别处 —— 要么是上游互联网接入带宽约 100 Mbps,
要么是 CDN 单连接限速。

**这个观察修正了一个隐含预期**:换到千兆口**不会加快模型下载**,
因为互联网侧本来就没有那么快。千兆的价值在**局域网内部**:
将来第二台 PC 接入(B6)、Parsec 串流、备份到网络位置。

**对 P2 的影响**:30B-A3B 约 17GB,按 12 MB/s 计需要约 24 分钟。下载要留出时间。
待确认:是宽带套餐上限还是 CDN 限速(可用多线程下载或换源验证,非当前必要)。

---

### [决策] CUDA Toolkit 选 13.2 —— 匹配未来而非存量

**关键前提**:实测发现 `D:\ComfyUI\venv` 已在跑 **torch 2.11.0+cu128**,
arch list 含 sm_120,`cuda.is_available()=True` —— **而当时系统根本没装 CUDA Toolkit**。
因为 PyTorch 的 wheel 自带完整 CUDA 运行时。

**这改变了问题**:系统 Toolkit 只在**自己编译** CUDA 代码时才需要,不是「让 GPU 能用」的前提。

**选 13.x 而非 12.8 的理由**:
1. PyTorch 已把 cu128 移出构建矩阵,**cu130 是当前 stable default**,cu126 只留给旧驱动。
   项目里新建的所有 venv(P2 embedding/rerank、P5 ASR/TTS)都会装 cu130 —— Toolkit 应匹配未来
2. ComfyUI 的 cu128 环境不受影响(wheel 自带运行时)
3. llama.cpp 不链接 PyTorch,无版本耦合

**选 13.2 而非 winget 的 13.3**:当时驱动 596.36 报告支持上限为 CUDA 13.2,
装更高版本的 toolkit 编译出的东西可能报 driver insufficient。在跑 P1 之前不想动驱动。

---

### [实作] P0-6 CUDA Toolkit 13.2 —— 一个判断失误与一个意外好结果

**判断失误**:我想用 `cuda_13.2.0_windows.exe -extract=<dir>` 只解包、读出真实组件名,
再静默安装排除 Driver 组件。**但 `-extract=` 并没有只解包,它启动了完整安装向导**
(解包目录为空)。你看到弹窗后自己点了默认安装。

**因此实际走的是默认全量安装**,我原本要规避的风险(Driver 组件覆盖驱动)直接发生了 ——

**但结果是升级而非降级**:

```
驱动  596.36  →  610.62
```

CUDA 13.2 installer 带的驱动比原来的新。这反而把 toolkit 放到了最安全的位置
(toolkit 13.2 ≤ driver 支持的版本),比我原本保守选 13.2 时设想的情形更宽松。

**附带安装的东西**(默认安装的代价):NVIDIA App · ShadowPlay · FrameView SDK ·
PhysX · Virtual Audio · Telemetry Client · Nsight Compute 2026.1.0 · Nsight Systems 2025.6.3 ·
Nsight VS Edition。常驻 3 个 `nvcontainer` 进程,合计约 188 MB 内存。
**对一个显存吃紧的项目,ShadowPlay 与 NVIDIA App 的常驻部分值得评估是否关闭。**

**安装结果**:

| 项 | 值 |
|---|---|
| nvcc | release 13.2, V13.2.51 |
| CUDA_PATH | 已注册(机器级),`bin` 与 `bin\x64` 已入系统 PATH |
| 占用 | 4.1 GB(C: 403.4 → 390.5 GB) |
| 下载校验 | MD5 `58b553e346d97155d5f7476be08bed41` 与官方 md5sum.txt 一致 |

**驱动升级后的回归验证(关键)**:

```
torch 2.11.0+cu128   available=True   capability=(12,0)   arch list 含 sm_120
实跑 50× 4096³ fp16 matmul —— 通过
```

ComfyUI 的 cu128 环境在新驱动 610.62 下**完全正常**。

> ⚠ 那次 matmul 测出 33.2 TFLOPS,**这个数字不能当基准** ——
> 没有 warmup、GPU 处于 P8 低功耗态、样本太短。正规测量是 P1 的事。

---

### [实作] 构建环境封装 `90-ops/devshell.ps1`

**动机**:把 cmake 加进 PATH 是不够的。编译 CUDA 还需要 `cl.exe`、`link.exe`、
Windows SDK 头文件与库路径,这些只有 vcvars64 初始化后才存在。

脚本做四件事:从注册表刷新 PATH(安装后已有会话不继承新值)→ 用 **vswhere** 定位 VS
(不硬编码路径,遵守 §11.1)→ 导入 vcvars64 环境 → 校验 `cl`/`link`/`nvcc`/`cmake`/`ninja` 齐全。

同时把 VS2022 自带的 **cmake 3.31.6** 与 **ninja 1.12.1** 加入用户级 PATH
(原 PATH 已备份),解除 STATE.md 里的 cmake 阻塞项。

**最终验证** —— 真编译一个 sm_120 kernel 并运行:

```
device      : NVIDIA GeForce RTX 5080  sm_120
runtime/drv : 13030 / 13030
VRAM        : 14983 MiB free / 16303 MiB total
SM count    : 84
kernel out  : 0 2 4 ... 62
```

---

### [问题] 四个 Windows PowerShell 5.1 的坑(会再遇到,记下来)

1. **UTF-8 无 BOM 的 `.ps1` 会乱码。** Write 工具产出无 BOM UTF-8,而 PS 5.1 按 ANSI(GBK)
   读脚本 → 中文全乱、语法解析崩溃。**含中文的 `.ps1` 必须存为 UTF-8 with BOM。**
   `backup.ps1` 也有同样问题(此前从未实跑所以没暴露),已一并修复。
   注意:**用 Edit 工具改过之后 BOM 会丢失,需要重新加。**

2. **`& exe 2>&1` 会炸。** PS 5.1 里对原生 exe 做 stderr 重定向会把每行包装成 ErrorRecord
   并令 `$?` 为 false,配合 `$ErrorActionPreference='Stop'` 直接中断脚本。
   `cl.exe` 不带参数正是把版权信息写到 stderr。**解法:把重定向交给 cmd —— `cmd /c "cl 2>&1"`。**

3. **`~` 在 PS 路径里是主目录简写。** 临时目录的 8.3 短名 `ZORIMA~1` 会被误解析,
   `Set-Location` / `Push-Location` 报「path does not exist」。
   **解法:用长路径(`$env:USERPROFILE`)或 `-LiteralPath`。**
   另注:`New-Item` 在 PS 5.1 里**没有** `-LiteralPath` 参数。

4. **`link`/`cl` 不带参数返回非零退出码**,会污染脚本的 `$LASTEXITCODE`。
   dot-source 场景不能用 `exit`,需显式 `$global:LASTEXITCODE = 0`。

---

### [问题] 编译 CUDA 时的 C4819 警告噪音 —— P2 会遇到

smoke test 编译时刷了几十行:

```
cuda_runtime_api.h(13772): warning C4819: 该文件包含不能在当前代码页(936)中表示的字符
```

MSVC 在 GBK 代码页下读取含 UTF-8 字符的 CUDA 头文件所致。**无害但极吵**,
编译 llama.cpp 时会淹没真正的警告。

**解法**:给 nvcc 加 `-Xcompiler /utf-8`;CMake 项目里设
`add_compile_options($<$<COMPILE_LANGUAGE:CUDA>:-Xcompiler=/utf-8>)`。
待 P2 编译 llama.cpp 时应用。

---

### [实作] P0-8 完整验收 —— 备份 + 恢复演练

移动固态接入:**SanDisk Extreme 55AE · G: · 931 GB · 461.8 GB 空闲 · Disk 2**。
独立物理盘,满足「不与源同盘」的硬要求。

`backup.ps1` 首次实跑(先 `-DryRun` 再正式),脚本逻辑全部正确:
从 `paths.toml` 读根路径 · 认出 Disk 2 ≠ Disk 1 通过跨盘检查 · BitLocker 警告正确触发。

**恢复演练结果**:

```
git clone code.bundle → 2 个提交 · 29 个文件 · 关键文件全在
HEAD tree 哈希  5c6dfd94...  ==  原仓库  5c6dfd94...     ✓ 完全一致
```

---

### [问题] 恢复演练暴露了 `backup.ps1` 的一个真缺陷:校验清单不可移植

第一次演练时用 `sha256sum -c` 校验,**3/3 全部 MISMATCH**。
用 PowerShell 重新比对则 3/3 通过 —— 说明**数据完好,是清单文件的格式有问题**:

| 问题 | 后果 |
|---|---|
| UTF-8 **BOM** | 第一行的哈希前面多了 BOM 字符 |
| **CRLF** 行尾 | bash 的 `while read` 会把 `\r` 读进路径变量 |
| **反斜杠**路径 | 非 Windows 环境无法解析 |

**为什么这不是小事**:灾难恢复的典型场景就是 Windows 起不来 —— 那时只有
Linux live USB 或 WSL 可用,`sha256sum -c` 是最自然的校验方式。
清单只能被 PowerShell 读,等于**把恢复路径限死在「Windows 还能启动」这个前提上**,
而这恰恰是备份要应对的场景。

**修复**:改为无 BOM · LF · 正斜杠 · 双空格分隔(GNU coreutils 格式)。
`BACKUP-REPORT.md` 同时改为无 BOM,并补齐两套校验命令与四步恢复流程。

**修复后复验**:

```
前3字节  36 62 66  (无 BOM)      行尾 LF      路径 正斜杠
sha256sum -c → 3 个全 OK,退出码 0
tree 哈希仍与原仓库一致
```

---

### [决策] 关闭 NVIDIA App / ShadowPlay —— 但结果修正了我的一个错误说法

**做了什么**:

| 对象 | 处置 |
|---|---|
| `NvContainerLocalSystem`(NVIDIA App / ShadowPlay 容器) | 停止 + 启动类型 Disabled |
| `NVIDIA App SelfUpdate` 计划任务 | 禁用(否则更新时会把服务重新拉起) |
| `NVDisplay.ContainerLocalSystem`(**驱动核心**) | **保留 Running/Automatic** |

**回归验证**:`nvidia-smi` 正常 · torch `available=True` · capability `(12,0)` ·
之前编译的 sm_120 kernel 重跑通过。驱动功能零影响。

**⚠ 但收益不是我此前说的那样**:

```
关闭前: nvcontainer 3 进程 · 188.8 MB 内存 · 显存 737 MiB
关闭后: nvcontainer 0 进程 ·   0.0 MB 内存 · 显存 737 MiB   ← 未变
```

我此前在本文件与 STATE.md 里写「对一个显存吃紧的项目,ShadowPlay 与 NVIDIA App
的常驻部分值得评估是否关闭」—— **这个暗示是错的。它们占系统内存,不占显存。**

实测占显存的进程全是 GUI 程序:

```
dwm.exe · explorer · SearchHost · StartMenuExperienceHost · TextInputHost
ShellHost · ApplicationFrameHost · LGHUB · EdgeWebView
RadeonSoftware.exe · AMDRSSrcExt.exe    ← 连 AMD 自己的软件也在独显上渲染
```

**因为显示器接在独显上,所有 GUI 的渲染都落在独显。**
这反而强化了 P0-4(改接核显)的理由 —— 它是唯一能真正腾出显存的手段。

**净收益**:188.8 MB 系统内存 + 少 3 个常驻进程。显存收益为零。

**恢复方法**(若日后要用 ShadowPlay 录游戏):

```powershell
Set-Service -Name NvContainerLocalSystem -StartupType Automatic
Start-Service -Name NvContainerLocalSystem
Enable-ScheduledTask -TaskName "NVIDIA App SelfUpdate_*"
```

---

### [实作] P1 阈值表落地为独立工作表

`00-docs/P1-thresholds.md`,共 **14 项**(A 显存 5 · B 上下文 3 · C 吞吐延迟 4 · D ComfyUI 2)。
每项含:怎么测 · 阈值 · **阈值的依据** · 不达标时的动作 · 实测值(空)· 结论(空)。

做成独立文件而非塞进方案书:P1 执行期要频繁编辑(逐项填结果),方案书应保持稳定。
v2.1 §14 P1 改为引用它,并保留 6 项关键阈值的摘要。

**表的第 1 节专门写测量方法**,因为今天有现成的反面例子:
那次 33.2 TFLOPS 的 matmul —— 没 warmup、GPU 在 P8 低功耗态、样本太短。
故规定:≥5 次 warmup · 测前确认已升频 · 取中位数 · 显存用 NVML 实测 ·
记录当时环境(接核显/独显,差 1GB 以上)· 每项 3 轮独立测量。

**已预判的结论**:A3(`deep` 峰值 ≤ 15.0 GiB)与 C3(`deep` tok/s ≥ 15)最可能触发换方案 ——
§8.1 自己估的 15.3 GiB 就已超线,而那还没算 offload 的速度代价。
若触发,`assistant.deep` 的定义与 §3.2 能力别名表都要改。

---

### [实作] 备份保留策略 + 清理两个过时备份集

**起因**:你问 `G:\localAI-backup` 里两个文件能不能删一个。核查发现:

```
1406 与 1408 的 code.bundle 哈希完全相同(6bf977880d8c6de5...,95174 bytes)
两者都只含到 91acdba,而当前 HEAD 已是 01bd153
```

两次备份只隔 2 分钟,仓库未变,所以数据相同;区别只在 1406 的清单有 BOM。

**但更要紧的是两个都已过时** —— 且有个讽刺之处:`01bd153` 正是修复 backup.ps1
清单格式的那次提交,所以**1408 的 bundle 里装的是未修复版本的 backup.ps1**。
真从它恢复,会拿回那个有缺陷的脚本。

**处置**:重跑备份得 `2026-07-26_1418`(含 01bd153 · `sha256sum -c` 全 OK ·
tree 哈希一致 · 恢复出的 backup.ps1 已确认是修复版),然后删掉两个旧集。

**顺带补上脚本缺的功能**:v2.1 §8.5.2 定了「30 日增量 + 12 周全量」的保留策略,
但脚本每跑一次就新建时间戳目录、从不清理。现在每集 105 KB 无所谓,
**但 P3 记忆库进来后(§8.3 估 2–10 GB)会堆积。**

实现遵循 §8.4 的原则 ——**只报告,不自动删**:

- `paths.toml` 新增 `[backup].keep_last = 12`
- 每次备份后报告超出保留数的集及总占用
- 确需清理时用 `-PruneOld`,删除前打印完整清单

理由:备份不是 CACHE 根。§8.4 明确「除 CACHE 外一律先报告后执行」,
而**自动删备份**恰恰是那条规则最该防的事。


---

### [实作] 关闭剪贴板历史与跨设备同步(§6.9.5 的 P2 验收项,提前完成)

**起因**:D23 的对抗性审查指出,剪贴板是凭证泄露的一条完整通路 ——
从密码管理器复制的每个 IBAN 都会留在 `Win+V` 历史里 25 条;
若跨设备同步开着,它还会上传到微软账户,按 §4.11.5 的 **D2 判据**(进入你无法单方面终止访问的系统)
**构成出境**。

**你的操作**:在设置 → 系统 → 剪贴板里关闭两项。

**实测核验**(不只看设置界面):

```
HKCU\Software\Microsoft\Clipboard  EnableClipboardHistory = 0
WinRT  Clipboard.IsHistoryEnabled()  = False
WinRT  Clipboard.IsRoamingEnabled()  = False
组策略 AllowClipboardHistory / AllowCrossDeviceClipboard = 未配置
```

**两个容易误判的点:**

1. **`cbdhsvc_1b05e5` 服务仍在 Running,这是正常的。** 它是剪贴板**基础**服务(管普通复制粘贴),
   不是历史功能的开关。判断依据是上面那两个 WinRT 返回值,**不是服务状态**。
2. **策略层「未配置」意味着这只是用户设置,不是强制。** 任何以你身份运行的程序、
   或你自己误触开关,都能把它重新打开。若要锁死需加组策略键
   (`AllowClipboardHistory=0` / `AllowCrossDeviceClipboard=0`)—— 属改系统策略,未执行。

**§6.9.5 的 P2 验收项因此可以标为已满足**,但**需在 P2 时复检一次**
(中间可能被任何软件改回来),并把复检写进 P2 的验收清单。


---

### [决策] D24 · 彻底放弃核显 —— 并追认本文件三处已被取代的结论

**你的决定**(2026-07-26):「我们放弃了核显的使用,不要省这么点显存搞这么麻烦。」
正式条目见 `DECISIONS.md` **D24**。本节只做一件事:**标注本文件里已被 D24 取代的记录。**

> **本文件是历史记录,原文一律不改。** 以下三处保持原样,读到时请一并读本节。

| 位置 | 原文结论 | 现在的状态 |
|---|---|---|
| 403–404 行 | 「这反而强化了 P0-4(改接核显)的理由 —— 它是唯一能真正腾出显存的手段」 | **手段被弃用,结论本身仍然成立** |
| 429 行 | 「记录当时环境(接核显/独显,差 1GB 以上)」 | 「接核显/独显」不再是变量,**永久接独显** |

**★ 必须说清的区别**:接核显**确实**能腾出那 0.69–1.06 GiB —— 它是被**主动放弃**的,
不是被证明无效。写成「其实也腾不出显存」是错的。正确表述是:
**手段弃用,桌面显存自此为永久扣项。**

**顺带纠正一处我当时没看出来的错误**:
本文件与 `P1-thresholds.md` 都曾写「桌面占用波动 → Broker 需要一个恒定值 → 所以要接核显」。
**Broker 从来不需要恒定值** —— §8.1.1 的动态闸按 NVML 实测 `free` 逐次判定,天然吸收波动。
这条推理链从第一步就错了,而它当时被当作接核显的主要技术理由之一。

**排查中发现的三件比核显本身重要的事**(详见 D24 与方案书修订):

1. **`deep` 与 `asset` 两个模式在 D8 落地那一刻就已不成立** —— v2.1 双列表里它们的
   「接独显」格子写的就是 ❌,而 v2.2 定 D8 时没回头核对。P1 的 A3/A4 一直拿
   「接核显」那一列的数字当阈值。
2. **降 cap 是伪动作** —— cap 按 §8.1.1 不含桌面,降它腾不出一个字节。
   「A7 不达标就降 cap」这条退路从设计上就是空的。
3. **失败落点未定义** —— A7 的 3.5 是 P99、真实缓冲只有 0.22 GiB、I1 不撤已授租约、
   KV 启动即预分配、sysmem fallback 已关闭。五者叠加,尖峰时先崩的很可能是
   Chrome/UE5 而不是 AI 侧,**与设计意图相反**;而落点由 WDDM 决定,本项目控制不了。


---

### [实测] P1 开工 · 一条被推翻的既有记录:带宽

**做了什么**:启动 P1 第 1 步 —— 后台下载模型 + 装 llama.cpp。

**★ 推翻了 worklog 里自己的一条结论。**

本文件早先记着:

> 「下载 CUDA installer 时速度稳定在约 12 MB/s ≈ 96 Mbps……
>   **瓶颈在别处** —— 要么是上游互联网接入带宽约 100 Mbps,要么是 CDN 单连接限速。」

今天实测(两次一致):

```
Qwen3-8B-Q4_K_M   4.68 GiB / 2.7 分钟 = 29.2 MiB/s
Qwen3-4B-Q4_K_M   2.33 GiB / 1.4 分钟 = 28.6 MiB/s
                                       ≈ 232 Mbps
```

**当时列的两个可能性里,是第二个** —— NVIDIA CDN 单连接限速。
**接入带宽至少 232 Mbps,不是 100。** 原记录里「瓶颈在互联网接入侧」这句是错的。

> **原记录不改**(worklog 只增不改),本条承接。
> STATE.md 环境事实与方案书 §2 已同步修正 —— 那两处是**现行事实**,必须改。

**连带修正的三处预期**:
1. runbook 的「27 GB / 42 分钟」→ 实际 25.48 GiB **约 15 分钟**
2. 「下载是关键路径」这条结论**不变**,只是它挡路的时间从 42 分钟变成 15 分钟
3. P3b 手机端的体验预期本来押在「接入侧很慢」上 —— **该假设已失效**,
   但上行带宽仍未测(下行快不代表上行快,德国 VDSL/Cable 常见非对称)

**顺带记两个工具层的坑:**

| 坑 | 实况 |
|---|---|
| `hf_transfer` | 在 huggingface_hub **1.24** 已废弃,换成 **Xet**。副作用:`.incomplete` 文件大小**不反映真实进度**,看着卡在 0 字节实际在下。**判断进度要看目录总大小** |
| `Invoke-WebRequest` | 下 137 MB 的 zip 两分钟没下完(它缓冲在内存 + 进度条开销)。**换 `curl.exe` 快一个量级** |

**模型来源的一处偏离(已记入 runbook)**:
官方 `Qwen/Qwen3-1.7B-GGUF` **没有 Q4_K_M**,只有 Q8_0(1.71 GiB)。
而 §17 明写 Vigil 用 Q4_K_M。改用 **`ggml-org/Qwen3-1.7B-GGUF`**(llama.cpp 项目自己的转换,1.19 GiB)。
没选更小的 unsloth 版(1.03 GiB)—— **基准测量要标准件,不要变体**。

**llama.cpp 的选择**:release b10107 有 cuda-12.4 与 cuda-13.3 两个 Windows 包。
**12.4 直接排除 —— CUDA 12.4 根本不支持 sm_120**(Blackwell 消费卡要 12.8+)。
选 13.3;驱动侧 `nvidia-smi` 报 `CUDA UMD Version: 13.3`,恰好够,不用升驱动。
**含不含 sm_120 仍待冒烟测试断言。**


---

### [实测] P1 第 1 步完成 —— sm_120 断言通过,不需自编译

**模型全部就位**(`D:\AI\models\`,合计 **25.48 GiB**):
`Qwen3-8B-Q4_K_M` 4.68 · `Qwen3-4B-Q4_K_M` 2.33 · `Qwen3-1.7B-Q4_K_M` 1.19(**ggml-org**,
官方 Qwen 仓库无 Q4_K_M)· `Qwen3-30B-A3B-Q4_K_M` 17.28 GiB。

**★ 那个分叉解决在好的方向。** runbook 说「预编译含不含 sm_120 是唯一可能让第 1 步
从 10 分钟变成 1 小时的分叉」。答案:**含**。

```
Device 0: NVIDIA GeForce RTX 5080, compute capability 12.0, VMM: yes
| qwen3 8B Q4_K - Medium | 4.68 GiB | 8.19 B | CUDA | 99 | tg32 | 119.50 |
```

`compute capability 12.0` + `backend CUDA` + `ngl 99` + 119 tok/s —— 是 GPU 无疑
(这颗 CPU 跑 8B 约 10–20 tok/s)。**不需要自编译。**

> ⚠ **119.5 tok/s 不是 C1 的实测值** —— 无 warmup、只 2 轮、GPU 未必升频。
> C1 要按 §1 纪律正式测;这里只用来断言「跑在 GPU 上」。

选包依据:b10107 有 cuda-12.4 与 cuda-13.3 两个包。**12.4 直接排除 ——
CUDA 12.4 不支持 sm_120**(Blackwell 消费卡要 12.8+)。驱动报 `CUDA UMD Version: 13.3`,
恰好够,不用升驱动。

**★ E5 的零显存断言提前通过,而且第一次测错了**

```
第一次(NVML 总量增量法)  : 增量 48 MiB   ✗ 假阳性
第二次(逐进程归因)        : 峰值 0.0 MiB  ✓
```

第一次错在**后台有游戏在波动** —— 「跑完后」没回到「跑之前」就是证据。
按 `P1-thresholds.md` §1 新写的那条纪律(「窗口前后各记一次基线,漂移超标该轮作废」),
**第一轮本就该作废**。纪律当场生效了一次。

正确结论:`llama-cli -ngl 0` 跑 1.7B,**自身显存峰值 0.0 MiB**;
即使 CUDA 构建(已加载 `ggml-cuda.dll`)也不分配。**D12 的「0 显存」这一半成立。**
单脉冲耗时与日累计核心秒仍未测。

**★ 一个提前到手的 A7 线索**:下载期间机器上跑着 Dead by Daylight,
逐进程归因合计 **3979 MiB = 3.89 GiB**,**已越过 3.72 的悬崖** ——
玩这个游戏时 `8b@16k + speech.full` 装不上,而 DbD 不算重型。
A7 的 P99 很可能落在 3.5 以上。(不是 A7 实测值,只作线索。)

**又踩了一次本文件记过的坑**:写 `.ps1` 探针用了无 BOM 的 UTF-8,
PowerShell 5.1 按 GBK 读,中文乱码 + 一串 ParserError。
**改纯 ASCII 后一次通过** —— 和当初 `backup.cmd` 的处置一样。


---

### [实测] A7 / A1 / A2 —— 三项结案,其中一项挖出可直接消除的 2 倍浪费

**A7 桌面重负载上界:6.53 GiB,不达标**

原设计只要**一个数**,而那个数会变成 `desktop_floor`(跨会话持久,改一次要走哈希绑定批准)。
用户当场指出「我的这套组合本身就很重,平常也不会用这么重」——**他是对的**,
于是改测**场景阶梯**:

| 场景 | P99 | AI 侧上限 |
|---|---|---|
| 日常基线(只留常驻小程序) | **1.22 GiB**(极差 0)| ~13.9 |
| **一般工作**(UE5 Epic 实时 + 网页 + HD 视频) | **6.53** | **8.52** ← 定 `desktop_floor` |
| 极端(再加游戏 + 4K 全屏) | 6.98 | 8.14 |

**★ 最重要的发现:游戏不是问题,UE5 才是。** 加上游戏只多吃 **0.45 GiB**,
而 `UnrealEditor` 单进程就占约 **4.0 GiB**、自身波动近 2 GiB(64 个样本里 64 次都是最大占用方)。
所以「只开游戏」那个场景不必测 —— 必然更轻。

**A1 每进程 CUDA context:227 MiB,完美线性可加**

n=1/2/4 三档、3 轮独立测量**完全一致**,零漂移;227×4=908 vs 实测 909。
✅ 接受(阈值 512)。

> ⚠ **已写进文档防误用**:探针只做 `cudaSetDevice`+`cudaFree(0)`,是**纯 context 下界**。
> 真实 worker 还要加载 cuBLAS/cuDNN。**拿 227 去改 §8.1.2 会把估算表改废。**

**A2 逐组件 peak:两个 FAIL 指向同一个可消除的根因**

```
4b@8k  3.78 (+5.0%)  PASS       8b@8k  5.83 (-6.0%)  PASS
4b@16k 4.92 (+17.1%) FAIL       8b@16k 6.97 (+2.5%)  PASS
8b@32k 9.23 (+16.8%) FAIL
```

上下文越长偏得越多。增量:`4b` 与 `8b` 的 8K→16K **都是 1160 MiB**
——**「KV 与参数量无关」被精确证实**;而 16K→32K 是 2320,恰好 2 倍,线性。

推算 KV(8K) = **1.13 GiB**,而 §8.1.2 的公式按 `× 1 B`(FP8)算出 0.56。
**F16 的理论值正是 1.12。** 根因确认:**公式假设 FP8,而 llama.cpp 默认 F16,每个上下文吃 2 倍。**

开 `--cache-type-k/v q8_0` 实测:

```
8b@16k  6.97 → 5.92   省 1.05 GiB
8b@32k  9.23 → 7.19   省 2.04 GiB
```

按 A7 定的上限 8.52:`8b@32k + speech.cpu` 从 **9.23(装不下)** 变成 **7.19(余 1.33)**。
**32K 上下文从「日常用不了」变成「日常可用」。**

> ⚠ 但**不是免费的** —— KV 量化损失精度,长上下文尤其,而**本项目没有任何一项在测这个**。
> 已给 **B3** 加测项(q8_0 vs F16 的中段信息检索准确率,判据 ≤5 个百分点)。
> **在 B3 出结果前不得写成默认配置。**

**✅ 可加性完美成立(差 0 MiB)** —— `8b@16k`(7135)+ `4b@8k`(3873)同时 = 11008。
**勾选界面「把 peak 加起来」的前提有效,D25 的推翻条件 ② 不触发。**
A1 已证明裸 context 可加,A2 证明**含权重与库的真实 worker 也可加**。

---

**★ 同一个教训今天生效了三次:样本不够就不要下结论**

| 时点 | 我说了什么 | 之后 |
|---|---|---|
| 36 样本 | 「极差只有 4 MiB,静态摆着不是真负载」 | 55 样本后极差 **1983 MiB** |
| 13 样本 | 「形状已经出来了」P99 **5.21** | 64 样本后 **6.53** |
| 首次 E5 | NVML 总量增量法测出 48 MiB | 逐进程归因是 **0.0 MiB**(游戏在波动) |

第三次是纪律当场拦住的 —— `P1-thresholds.md` §1 新写的「窗口前后各记一次基线,
漂移超标该轮作废」正是为此。**规则写下来了,执行时还是会犯;写下来的价值是能被抓住。**

**又踩了三个本文件记过的坑**:无 BOM 的 UTF-8 `.ps1` 被 PS 5.1 按 GBK 读 ·
`2>&1` 在原生 exe 上触发 NativeCommandError · nvcc 缺 `-Xcompiler /utf-8` 导致 C4819 刷屏。
**三个都在文档里写着,三个都又踩了一遍。**

**顺带**:`devshell.ps1` 第一次实用即一次通过(五个工具全验证)。
`llama.cpp` 选 b10107 **cuda-13.3**(12.4 不支持 sm_120);冒烟测试报
`compute capability 12.0` + `backend CUDA` + 119 tok/s,**sm_120 可用,不需自编译**。


---

### [问题] 过夜无人测量三次尝试:核心教训 —— 这几项必须交互式测

**结论先行**:B3b / A3+C3 / E5 / VLM 这几项**不能无人值守跑**。三次尝试(两次被会话拆掉、
一次用脱离会话的独立进程)都产出了**未察觉的坏数据**,全部是 harness bug,不是真实测量。
**我不在场就无法当场发现。已停止无人尝试,留待交互式重测。**

**为什么放到独立进程还是失败**:会话拆掉能靠 `Win32_Process.Create` 脱离进程规避,
但脱离进程解决的是「活下来」,解决不了「测得对」—— bug 依旧,而且更没人看着。

**三个 bug(明天重测直接照这个改)**:

| 项 | 崩因 | 修法 |
|---|---|---|
| **A3+C3** | `LoadDelta` 把 `-ngl` **写死 99**,sweep 的 48/36/26 被忽略 → 30B 全塞 GPU,`cudaMalloc 17524 MiB OOM`。**分层 offload 根本没测到** | LoadDelta 加 ngl 参数;30B-A3B **48 层**,q8_0 KV;从 ngl 24 起往上试 |
| **B3b** | **Qwen3-8B 是推理模型**,`-no-cnv -f` 下进 `[Start thinking]`,`-n 12` 只截到思考开头 → F16 也 **0/5**(证明是探针坏,不是模型) | 加 `/no_think` 或用 chat 模板;`-n` 给足(≥256);答案在 think 之后解析 |
| **E5** | `e5.out` 卡在 `Loading model...`,90s 大半在**加载**,计时含加载;323 MiB 是前面 OOM 的 30B server 没释放干净 | 分离「加载」与「生成脉冲」计时(用 llama.cpp 自报的 eval time,不用 wall clock);测前确认 GPU 干净 |

> **E5 的零显存断言仍然成立**(白天交互式实测 0.0 MiB,那次是干净的)。今晚的 323 MiB
> 是污染,不推翻白天的结论。

**A3 的 OOM 顺带确认了一件事**:30B-A3B Q4 权重 **17.5 GiB**,全塞 GPU(ngl 99)必然 OOM
(独显只有 ~14.9 可用)。这本就是预期 —— A3 的意义正是测「分层 offload 后 GPU 驻留多少、
速度代价多大」,而那个测量因 ngl 写死而没跑成。**不是「30B 不可行」的结论,是「没测到」。**

**已安全提交的仍是 7/17**(A7/A1/A2/B1/C1/B2/B3a),其中最高价值的 q8_0 KV 省显存发现稳固。
今晚 batch#3 的原始输出存 `00-docs/p1-data/night-batch3-INVALID.md`(明确标记无效,仅供诊断)。


---

## 2026-07-27

### [实测] 昨晚崩掉的 5 项当面重测,全部通过 —— 且有一个大翻转

**背景**:昨晚 3 次无人跑全产出坏数据(全是 harness bug)。今早当面测,先修 bug 再跑,逐项验证。
**同样的 5 项,无人跑全崩,交互跑全过 —— 这就是「测得对需要人在场」的实证。**

**三个 bug 都修好了:**

| 项 | 昨晚崩因 | 修法 | 今早结果 |
|---|---|---|---|
| B3b | Qwen3-8B 是推理模型,`-no-cnv -n 12` 只截到 `[Start thinking]`,F16 也 0/5 | `-cnv -st -f` 套 chat 模板 + `/no_think` + 给足 token | **F16 5/5 · q8_0 5/5** |
| A3/C3 | `LoadDelta` 把 `-ngl` 写死 99,30B 全塞 GPU OOM | 加 ngl 参数做真 sweep | 见下(大翻转) |
| E5 | 计时含加载(卡 90s)· 总量增量被桌面污染(323 MiB) | 逐进程归因测显存 + 模型自报计时 | **0.0 MiB · 脉冲 2s** |

**★ 最大翻转:30B-A3B 深度模式活了,而且飞快。**

文档一直让「做好 30B-A3B 出局的心理准备」。实测 ngl sweep:

```
ngl 36   14.14 GiB · 25% offload · 75.5 tok/s
ngl 30   11.88 GiB · 38% offload · 57.5 tok/s   ← 甜点
ngl 24    9.62 GiB · 50% offload · 45.8 tok/s
```

**关键洞察:它是 MoE,只 3B 活跃参数。** offload 的是不常激活的冷专家,几乎不拖速度 ——
哪怕 50% offload 还有 45.8 tok/s(阈值 15 的 3 倍)。**与 dense 30B 完全不同**(后者 offload 崩速度)。
`assistant.deep` 已改绑回 30B-A3B。**文档的悲观预期基于 v2.1 估算 + 从未实测 offload —— 一测就翻。**

**★ q8_0 KV 闭环关上:** B3b 证明 q8_0 needle 5/5 = F16 5/5,**零质量损失**。
配合 A2+B1(省 1–2 GiB),**q8_0 转正为默认,32K 上下文解锁**。

**其余:** C1 150 tok/s(5×)· ComfyUI SDXL 峰值 8.14 GiB(估 11)/ 单图 5.6s ·
VLM 4.35 GiB(**估 2.5,漏算 mmproj +74%**)· Vigil 零显存坐实。

**顺带三个 harness 坑(都记下了):**
- 无 BOM 的 `.ps1` 被 PS 5.1 按 GBK 读 → 崩解析。加 UTF-8 BOM 解决
- ComfyUI **seed 相同返回缓存**(0.5s/0 MiB 假象),必须换 seed 强制真生成
- `Start-Process -PassThru` 的 ExitCode 取不到值(昨晚误杀 C1/B2/B3a 汇总)

**P1 至此 13/17。** 用实测值重算了 §8.1.2 权威表与 §8.1.3 推荐组合 ——
**32K 与出图现在都进日常了**(原估过订,实测 q8_0 后 7.19/8.14)。
剩 4 项全阻塞:A5/A8/C4(等语音栈+语料)· C2(P2 网关)。

## 2026-07-27 · P2 账户隔离落地(§6.8)

用户在管理员终端跑 `90-ops/setup-accounts.ps1`,Claude 独立核验:

- 三个低权限服务账户 `ai-mem` / `ai-asset` / `ai-exec` 建成,均 Enabled、密码永不过期、
  **不在 Users/Administrators 组**(最小权限)。机器名 HONGKONGPINGPON,无域。
- `D:\AI\state\memory` NTFS ACL(`Get-Acl` 确认):
  - ai-mem / SYSTEM / Administrators → **Allow** FullControl
  - ai-asset / ai-exec → **Deny** FullControl(真 Deny ACE,非空允许)
  - 继承已关(AreAccessRulesProtected=True),ACE 均 OI+CI 向下传播
- **S3 威胁在 OS 层堵死**:资产侧账户即使被加进有权组,deny 先于 allow 生效。
  D22 不加密后,此隔离是主要保护层 —— 现成立。

一个踩到的坑:`New-LocalUser -Description` 上限 48 字符,原描述 63 字报错;缩短后通过。

下一步:PG + Qdrant 装到 ai-mem 下(装库时重置 ai-mem 密码并配进服务)。

## 2026-07-27 · PostgreSQL 18 落地(SSPI · ai-mem)

用户跑 `90-ops/install-postgres.ps1`(PG 18.4-2 binaries ZIP),Claude 只读核验:

- 服务 pg-mem 以 `.i-mem` 运行 · Auto · 仅监听 127.0.0.1:5432(非 0.0.0.0)
- 数据目录 ACL:ai-asset/ai-exec **Deny** · ai-mem Allow(继承强 ACL,安装未松动)
- 认证:pg_hba 全 sspi + pg_ident 映射 `ai-mem→postgres`/`ai-mem→mem_rw`;postgres 与 mem_rw
  **均无 DB 口令**(SSPI 绑 SID)· initdb UTF8/locale=C/data-checksums · 库 memory(OWNER mem_rw)
- ★ **隔离正向验证**:以当前登录用户(未映射)连 postgres → `FATAL: SSPI authentication failed`。
  非映射 Windows 身份即使在本机、能打到回环端口也连不上 —— 这正是 ai-asset 的处境。D30 安全模型实证成立。
- 编码 UTF8:initdb --encoding=UTF8 与 CREATE DATABASE ENCODING 'UTF8' 均成功(PG 校验编码/locale 兼容性,
  成功即证 UTF8)+ 脚本 STEP8 SHOW server_encoding 未触发红色告警。

途中修的坑:①%TEMP% 短名含 ~(用户名 'Zori Ma' 有空格)崩 Remove-Item → 全程改用 D: 目标目录;
②CREATE ROLE/DATABASE 挤一个 -c 被包进事务,CREATE DATABASE 不能在事务块 → 拆开 + 角色幂等。

下一步:Qdrant ×2(mem_main 6333 / mem_s2 6335)。

## 2026-07-27 · Qdrant ×2 落地(mem_main + mem_s2 · ai-mem)

用户跑 `90-ops/install-qdrant.ps1`(官方原生 v1.18.3 + NSSM),Claude 只读核验:

- 服务 Qdrant(6333/6334)+ Qdrant-s2(6335/6336)均以 `.i-mem` 运行;进程宿主 = ai-mem
- 四端口全 **127.0.0.1**(非 0.0.0.0);config.yaml 对 ai-asset/ai-exec **Deny**
  (Qdrant 无 SSPI 等价物,api_key 是唯一运行时屏障 → 靠 NTFS ACL 让 ai-asset 读不到 key)
- 鉴权:无 key → 401;两实例各自 api_key,**main 的 key 打 s2 端口 → 401**(密钥互不通用)
- collection:mem_main@6333 / mem_s2@6335,均 green · 1024 维(bge-m3)· Cosine;
  两端口 collection 命名空间互不可见 —— §4.11.4「独立 loopback 端口」结构级隔离落地
- ★ 密码耦合已处理:重置 ai-mem 密码时同步了 pg-mem 服务凭据,**pg-mem 仍 Running**(未被搞挂)

至此记忆库**基础设施全部就位**:PostgreSQL 18(SSPI)+ Qdrant×2(key)· 全在 ai-mem 强 ACL 内 · 全回环。
下一步:PG schema(角色二分 / v_memory_nons2 / 实体表 / secret_ref / 审计 / l4_procedure)+ E1 凭证检测器。

## 2026-07-27 · 记忆库 schema 落地 · 否定用例 20/20 全通过

用户跑 `90-ops/apply-schema.ps1`。schema(mem_rw)→ roles(postgres)→ verify(否定用例)三步全 OK。

**24 张表 · 2 角色 · 1 视图**建成。逐条实测结果:

| 用例 | 结果 |
|---|---|
| a3 视图 `security_barrier=true` | PASS(核验 FAIL 修正在库里坐实) |
| a4 视图不投影 write_seq | PASS(空洞侧信道已封) |
| b1/b2 sensitivity_domain 无 DEFAULT · crypto_tier 可空 | PASS |
| B3 不带定级 INSERT | **ERROR** not-null violation —— 忘定级=插不进 |
| B4 客户端谎报 write_seq=999999999 | 实得 write_seq=2 —— 服务端强制覆写(§4.11.3) |
| B5 自动事实 supersede 用户事实 | **ERROR**「自动事实(id=4)不得 supersede 用户事实(id=3) — §4.5 铁律」触发器生效 |
| C2/C3/C4/C4b/C5 远程读 l3_fact/secret_ref/cred_access_audit/gate_rejection/l4_procedure | 全 **permission denied**(§6.9.10 否定用例⑦落地) |
| C8 远程调 next_write_seq() | **permission denied**(PUBLIC 撤权修正坐实) |
| C9 远程写入 | **permission denied** |
| ★ C6 混表里埋一行 S2,远程经视图读 | **PASS —— 看不见** |
| ★ C7 对照:非 S2 行远程经视图读 | **PASS —— 看得见** |

★ C6+C7 成对才是命门:单有 C6 可能只是"视图坏了什么都不返回";C7 证明视图正常工作**且**恰好挡住 S2。
这正是对抗性核验指出原版缺失的那条测试。

**本轮核验修掉的三个真问题**(工作流 s2-isolation 判 FAIL):
1. 视图缺 `security_barrier` → 调用方谓词可下推到 S2 过滤之前求值,借错误反推机密行(项目自警的"沉默泄漏")
2. 视图投影 write_seq → S2 行也消耗全局序列,空洞泄露其存在性/条数/时序
3. `REVOKE EXECUTE ... FROM ai_mem_remote` 撤了个寂寞 —— 函数默认授 PUBLIC,那条还在;改 `FROM PUBLIC`

小瑕疵:PS5.1 按 GBK 读 UTF-8 日志导致中文显示乱码(纯显示,数据无损),已给 Get-Content 加 -Encoding UTF8。

★ 诚实标注:方案书多处只到概念未到字段级,DDL 中这些列标 [推断],待 P3a 接入真实记忆时最终化。

下一步 P2:E1 入口凭证检测器(§6.9.0/§14 必做且先行)· embedding/rerank(CPU,18084)· 网关补调用方 SID 校验。

## 2026-07-28 · embedding/rerank 服务代码就绪(prep · 未跑测)

`10-core/memory/embedding_service.py`:CPU 侧向量轨。FastAPI @ 127.0.0.1:18084,
OpenAI 兼容 /v1/embeddings(bge-m3 · 1024 维 dense,与 Qdrant collection 对齐)+ /rerank(bge-reranker-v2-m3)。
网关已把 embedding.default/rerank.default 路由到 18084。

★ 诚实标注:**代码就绪,尚未跑测**(不像 E1 实测过)。需 2.3GB 模型 + torch,由 install-embedding.ps1
下载安装 —— 该安装脚本留待与用户在场时跑(大下载 + torch 装 CPU wheel,预计首跑要联调,像 PG 那样)。

顺带定了 D31(ai-mem 服务读代码的部署边界):授 ai-mem 对 10-core 服务子目录 RX,不做 deploy-copy;
理由=要隔离的是数据不是代码,秘密都不在 10-core(api_key 在强 ACL 配置、DB 走 SSPI 无口令)。
gateway 上 ai-mem 服务时需补授(此前以 admin 测)。

P2 剩:embedding 安装(与用户在场)· 网关补调用方 SID 校验(D30)· Open WebUI。

## 2026-07-28 · 网关调用方 SID 校验(D30 混淆代理修正)· 纯代码 + 自测

用户「先补网关 SID 校验」。`10-core/gateway/caller_identity.py` + gateway 策略:

- 端口→PID(GetExtendedTcpTable,ctypes,低权限可用)→ owner(令牌快路径 → WMI GetOwner 跨用户)
- ★ 途中一个关键发现,并实测证实:**令牌读取跨用户会失败** —— 标准用户令牌 DACL 只授本人+SYSTEM,
  低权限网关(ai-mem)读不到 ai-asset 的令牌,连人类的也读不到 → 两者都 None 无法区分。
  实测(xuser_test):管理员上下文令牌法能读到 ai-asset(因 admin 可读任何令牌),但这不代表 ai-mem 能;
  **WMI GetOwner 对 ai-asset 进程正确返回 HONGKONGPINGPONi-asset**(SYSTEM 代理,低权限可用)→ 采用之。
- classify_caller:隔离账户(ai-asset/ai-exec)→ denied-account(403 + 审计 denied_access.jsonl);
  人类/ai-mem → trusted-local。fail 策略:chat fail-open,记忆代理路径用 require_trusted_local fail-closed。
- 测试:test_caller_identity 6/6(真实自连接解析出当前用户 Zori Ma)· test_caller_policy 11/11
  (拒 ai-asset/ai-exec、放行人类/ai-mem、fail-open-chat vs fail-closed-memory、大小写、远程走 WebAuthn)。
  E1 集成 12/12 无回归。

★ 诚实记 D30 补记:为何弃令牌法用 WMI;fail 策略;WMI 子进程 ~100ms 已缓存;人类账户当前只区分
「非隔离」不区分具体是谁(单用户机可接受)。

P2 剩:embedding 安装(与用户在场)· Open WebUI。

## 2026-07-28 02:30 · embedding/rerank 服务实测通过(不再是 prep)

用户睡前跑 `-DownloadOnly` 下完 2.3GB 模型 + open-webui。两个坑当场修掉:

1. **PS5.1 stderr 陷阱**:HF「未设 HF_TOKEN」警告走 stderr,配 `2>&1` + `ErrorActionPreference='Stop'`
   被包成 NativeCommandError 中断脚本(python exit code=0 也照断)。改 'Continue',真失败靠既有
   显式 $LASTEXITCODE/Test-Path 检查;补上管理员阶段原依赖 Stop 的两处。已用探针脚本复现+验证。
2. **transformers 5 不兼容 FlagEmbedding reranker**:transformers 5.14.1 移除 `prepare_for_model`,
   FlagEmbedding 1.4.0 的 reranker 仍调用 → `XLMRobertaTokenizer has no attribute prepare_for_model`。
   embedding 路径不受影响,故症状是「1024 维对、rerank 崩」。钉 `transformers<5`(装 4.57.6)后两条都通。

**HTTP 端到端实测**(服务前台起在 127.0.0.1:18084):
- `/health` → ok · dim 1024 · bge-m3 / bge-reranker-v2-m3
- `/v1/embeddings` → 2 条 · **1024 维**(与 Qdrant collection 对齐)
- `/rerank` 「我妹妹的名字」→ 我妹妹叫小雨 **0.9912** · 今天天气不错 0.0000 · 晚饭吃了面 0.0000
  —— 正是 P3a 验收例句要用的检索能力,向量轨可用。

★ 状态从 prep 升为**已实测**。剩:注册成 ai-mem 服务(管理员,明早)· Open WebUI 配置首启。

注:curl 在 Git Bash 里打这两个端点会报 "error parsing the body"(引号/编码),用 Python
urllib 直打正常 —— 是 harness 问题,非服务问题。

## 2026-07-28 03:00 · P2 收尾:验收实测 + Open WebUI + 流式 E1 修复

用户睡前授权「把 P2 全部做完、验证、审查、修复、再检验、记录、更新 artifact、关机」。

### §14 P2 验收标准逐条对照(实测)

| 标准 | 结果 |
|---|---|
| 本机可用别名对话 | ✅ `assistant.fast` → 「德国的首都是柏林。」· 契约头 `8b` · model 回写 · 流式亦通 |
| 换别名映射,客户端零改动 | ✅ 客户端代码逐字未动,改 registry.toml 契约 `8b`→`8b-remapped`,重启网关后对话照常、契约头随之变化 |
| 非授权账户连接记忆库被拒**且告警** | ⚠ **部分**:被拒已达成(三层实攻);**「告警」仅落审计文件**,未接托盘/通知 —— §9.3 告警通道要到 P5 桌面壳才有落点。**不把日志说成告警。** |

### 自查读出的真 bug(并修复)

**E1 拦截在 `stream:true` 下回了非流式 JSON**。Open WebUI 等主力客户端默认流式,
收到普通 JSON 会解析失败 → 用户看到报错而非「这一轮没有发送」的说明。
即 E1 在**最常用的路径上「拦住了但说不清楚」**。改为按 `body.stream` 分支回 SSE
(chunk + content_filter + [DONE])。新增 8 条流式用例,E1 集成 12→20。

### Open WebUI(实测)

- 入口是 `Scripts\open-webui.exe`;`python -m open_webui` 不可用(包内无 `__main__`)
- 首启跑几十条 alembic 迁移需 1–3 分钟(不是卡死)
- `webui.db` 落 `state\openwebui`(DATA_DIR 生效)· HTTP 200 · 网关 `/v1/models` 返回 4 个 chat 别名
- `.webui_secret_key` 默认写进 venv `Scripts\` —— **秘密散在代码目录**,改用 `WEBUI_SECRET_KEY_FILE`
  指向 DATA_DIR,已把生成的那份挪过去
- RAG 指向本地 18084,避免它另下一套 embedding 模型
- ★ **首个账户未注册** —— 创建账户是用户的事,Claude 不代建

### 未做且明确标注

- **Embedding 未注册成服务**:代码+模型已实测可用,但注册要重置 ai-mem 密码 + 改 ACL + 注册服务
  = 改系统安全设置,属 Claude 红线;且会重新同步三个**正在运行**的服务凭据,凌晨无人值守风险高。
  留给用户一条命令。
- `require_trusted_local`(fail-closed)**当前无人调用** —— 尚无记忆代理端点,是前瞻脚手架,
  代码与 README 均标注,不当作已生效的防护。

测试总计 **81**:schema 否定用例 20 · E1 61(41 单元 + 20 集成)· 调用方身份 17(6+11)。
全部通过,无回归。

## 2026-07-28 04:30 · P2 收尾对抗性审查:57 条发现,11 条严重全修

用户授权「做完 → 验证 → 审查 → 修复 → 再检验 → 记录 → 更新 artifact → 关机」后去睡。
四维并行审查(网关代码 / 安装脚本 / 安全姿态 / 文档一致性)+ 逐条对抗性核验。

### ★ 最重要的一条:我自己代码里的 use-after-free

`caller_identity._tcp_rows()` 返回**指向已释放内存**的 ctypes 数组 —— D30 的调用方身份判定
建立在读野指针上。**我独立复现验证了四条断言**(没有盲信 subagent,其中一个还被标了安全警告):

| 断言 | 实测 |
|---|---|
| `cast(整数地址)` 不建 keepalive | `cast(int)._objects = None`(对象版是 dict) |
| 真实返回值没引用住 buf | `rows._objects = None` |
| 同尺寸缓冲释放后落回同址 | **50/50** |
| 精确复刻后立刻读 | state 应为 0xAAAAAAAA,实得 **0x1fa** —— 已被写坏 |

**后果是安全性的**:PID 读错 → `classify_caller` 只在**肯定**解析到隔离账户时才拒 →
`ai-asset` 可能被**静默判成可信账户放行**,不报错、不落审计。今天靠堆运气在正常工作,
而原测试(调用后立即迭代、分配量小)**结构性地测不到**。
修:`from_buffer` + 值拷贝。回归:2 万轮堆 churn 后结果不变。

### 其余已修的严重项

- **TIME_WAIT 残留行**:只按 ip:port 匹配会命中 pid=0 的行(本机 **102/151**)→ fail-open。改按 state==ESTABLISHED 过滤。
- **`::1` 旁路**:被当可信回环,但身份解析只查 IPv4 表 → 对 IPv6 恒 fail-open **且无日志**。改为只认 127.0.0.1。
- **流式静默降级**:`return StreamingResponse(gen())` 立即返回,异常在 return 之后 → 后端挂了给 **200 + 空 body**(§8.1.4 明令禁止)。改为先 `send(stream=True)` 拿状态码再返回流;异常放宽到 `RequestError`(ConnectTimeout/ReadTimeout 都不是 ConnectError 子类,原会裸奔 500)。**实测:流式与非流式现在都返回 503 带缺口。**
- **E1 扫描范围**:只扫最后一条 user 消息 —— system 消息 / 历史 user / 无 type 的 content part **三种绕过均已确认**。改为扫整个将发出的载荷。
- **E1 误报 = 硬墙**:high_entropy 对 git SHA / URL / SHA256 误报(开发者文本 **3/8**),而 Open WebUI 发不了自定义 header → **无法解除**。改:URL 段与带标签十六进制不计入;并加**带内暗号 `#E1放行`**,拦截文案直接告诉用户怎么解除。(一个天天误报的检测器要么把人逼疯要么被关掉,两种结局都等于没有 E1。)
- **install-embedding 只 /grant RX 收紧不了任何东西**(grant 只增不减):代码/venv/模型对 ai-asset/ai-exec **可写** = 一跳打穿隔离。改:显式 `/deny` 写删。
- **重跑 install-postgres 会打死 Qdrant**:重置 ai-mem 密码却只同步 pg-mem。改:按 **SID** 同步所有 ai-mem 服务。
- **install-postgres 无条件覆写 pg_hba/pg_ident** → 抹掉 schema 追加的角色映射。改:加首次写入守卫。
- **计划任务抢跑**:Start 后状态仍是 Ready 就退出循环 → 拿旧 LastTaskResult 并拆掉在跑的任务。改:先等进入 Running。
- **审计日志 fallback 指向 10-core**(git 树内且按 D31 对 ai-asset 可读)。改:退到系统临时目录。
- **Open WebUI 未传 `--host`** → 绑 **0.0.0.0 暴露局域网**,且注册开放。改:`--host 127.0.0.1` + `ENABLE_SIGNUP=false`。

### 复验

**100 项测试全过**(E1 单元 50 · E1 集成 25 · 身份解析 10 · 策略 15),含针对上述每条的回归。
真实后端复验:非流式/流式对话、契约头、model 回写、后端挂掉时 503 带缺口 —— **8/8**。
所有 `.ps1` 语法自检通过。

### 未修的 34 条

写入 `00-docs/p2-audit-backlog.md`,每条标了为什么当时没修。要点(也进了 STATE 技术债):
`require_trusted_local` 无人调用(前瞻脚手架,非已生效防护)· llama-server 无鉴权(同机可直连绕过网关)·
`/health`+`/v1/models` 不做身份校验 · 身份解析同步阻塞跑在 async 端点里。

### 顺带重写 STATE.md

审查指出它累积了**三段互相矛盾的 P1 状态**、已解决却仍列着的阻塞、两处断裂表格、一处 venv 路径笔误。
已按「只写当前为真」重写。
