# 开发记录 — 2026-08

> 格式定义: 方案书 **v3.0** §12.1（v2.2 已归档；DOC-1：现行文档不引用已作废文档）
> 类型: 决策 · 实作 · 实测 · 问题

---

## 2026-08-01 · 天气接入 + 第二轮审计收口(8 次提交)

> ★ **2026-08-03 回填**,依据提交记录与 D51/D52(后按 D75 改号为 **D78/D79**)。

**[决策]**
- **D78**(原误编 D51)天气数据来源署名:**自家自用不显示,对外分发时必须加回**;字串保留在词表里。
- **D79**(原误编 D52)**工作空间对项目是【标签】不是【归属】**:同一个文件夹可同时挂在多个空间下。

**[实作]**
- **天气接入**:Open-Meteo · **固定白名单端点** · 只发 0.1° 取整坐标 · 缓存 + stale,**无假实时**;
  缓存接自动落盘 —— 不接这一行,「显示上次那份」整条纪律就是空的。
- 夜间图标(晴→月亮 / 局部多云→月亮+云);当前无雨时说"几天后有雨";逐小时那排不贴卡底。
- 时间轴默认视野改 08:00–22:00。

**[实测]**
- 清完**第二轮审计**剩余 12 条(此前已修高危 11 条;47 个 agent / 41 条发现 / 8 条被驳倒)。

---

## 2026-08-02 · 翻译空间第二、第三场景 + 第三轮审计(11 次提交)

> ★ **2026-08-03 回填**,依据提交记录与 D53–D59。

**[决策]**
- **D53** 会话跟随自己的工作空间(跨空间会话点开就转过去)· **D54** 目的地菜单只列可见空间。
- **D55** 天气:逐小时按**那座城的当地时间**;气温曲线与刻度**同轴**并按温度分段上色。
- **D56** 移除 Apple 提醒事项入口 —— 这条路对 iCloud 账号是死的(2019 年起 Reminders 走 CloudKit)。
- **D57** 待办改**纯本机**数据,清掉全部同步残留。
- **D58** 同传加显式【开始/结束】:**进页面只是看看,开会才是开会**;设置随时可点;窄卡完整自适应。
- **D59** 翻译第三场景**文件翻译**:左右预览 + 标注框 + 工具栏;导入即缓存副本(源删了照样干活);
  右键平移 / 左键标注 / 拖角标移动 / 拉边调大小 / Ctrl+Z / 页码角标。

**[实测]**
- **全项目审计第三轮**:42 条发现 **39 条成立,当晚全部修复**。

---

## 2026-08-03

### [决策] 桌宠定形 —— 逐帧像素猫 / 家在主页 / 两层状态机 / 助手只有把手(D76)

**起因**:准备开搭宠物功能,先做现状勘察。读 `D40` · `§14 P8` · `AIRI_INTEGRATION_FEASIBILITY.md` ·
`Assets/pet/loading-cow-cat/` 后发现**文档里挂着两份互相排斥的方案**:

| | 决议层(D40 · §14 P8 · AIRI 报告) | 美术层(6 fps 逐帧规范) |
|---|---|---|
| 渲染 | DragonBones 骨骼/网格 + 可替换贴图槽 | 纯逐帧位图,**明文禁**骨骼/蒙皮/网格变形/补间 |
| 换装 | 核心需求 | 不存在(色板只有黑/白/透明) |
| 嘴 | AIRI 建议自写 MFCC→音素→嘴型槽 | 「**不画**……人类 AEIOU 口型」 |

美术那边其实已经单方面裁过一次(把骨骼版 rig spec 标为废止),决议层没跟上。

**裁定 = 逐帧像素猫。** 理由不是美术偏好,是它连锁解掉的东西:

- **A7-pet 从"未知倍数"变可预估** —— 真正的成本是 WPF 分层透明窗口 + DWM 合成,
  而**那笔钱两种方案都要付**,真正的变量只剩 atlas;
- **素材解析器攻击面归零**(`.model3.json` / `skeleton` → PNG + 自家 JSON);
- **Live2D/Expandable 的贩卖阻断线消失**,D41 少一个待裁项;
- **D40 反对预渲染的唯一理由(换装组合爆炸)前提不成立** —— 这只黑白猫没有换装维度。

代价说清楚了:100–140 张跨帧花纹一致、零灰边的像素帧是**纯手工美术活**,
规范 §10 的验收排除了扩散模型逐帧生成。**这是整个功能的真瓶颈,不是代码。**

**其余三条裁定**(详见 D76):v1 = 哑巴猫,拆 v1a 家 / v1b 桌面 / v1c 串门 ·
家 = 主页横条空白背景、两扇门(右通桌面可开关、左通其他 PC 常开)·
行为层与助手只能提意图,永远不能点名 clip。

---

### [实作] P8 运行时内核提前落地(P3c 期间)

**为什么提前**:**A7-pet 是 P8 清单的第一个 checkbox,而它需要一个能跑的渲染实现才测得了。**
先做内核不是抢跑,是在解 P8 的硬前置。而且两层状态机在 v1a(家)就能完整验证 ——
桌面模式只是给同一套状态机换个地板和一套窗口壳。**阶段归属不变,桌宠仍在 P8。**

`20-client-win/app/Services/Pet/`:

| 文件 | 内容 |
|---|---|
| `PetClock.cs` | 6 fps 定步长。单调 + accumulator;最多追赶 2 tick,超了丢积压重同步;时钟回拨不产生负 tick |
| `PetManifest.cs` | manifest 唯一事实来源 + 转移图。BFS 寻路 · 通配边 · **fail-closed 校验** |
| `PetIntent.cs` | 意图通道。枚举白名单 · 三态返回 · 助手速率限制 · 审计 |
| `PetAnimator.cs` | 动画状态机 = 姿态唯一权威。must_finish · can_exit · 朝向只经 turn_180 · latest-wins |

`Selftest.cs` +46 条断言。**客户端 selftest 1544 全绿。**

**三条从规范里推出来、但规范自己没写的**(已写进 D76):

1. **眨眼/耳动进不了状态图。** 规范 §0「角色身体仍是一张完整 sprite」⇒ 身体只有一条动画轨,
   `blink` 只能是 idle clip 的插入变体。不写死这条,实现时几乎必然做成"身体层 + 表情层"两轨,
   然后发现要为每个姿态 × 每个表情重画组合帧 —— **正是当初否掉预渲染的那个组合爆炸,
   从换装换到了表情上**。
2. **动画层必须内建寻路。** `Sleep → Walk` 图上没有直达边,实际要走四段。不做 BFS,
   路径就散落硬编码在行为层各处。
3. **意图不排队,latest-wins。** 规范 §5 为卡顿禁止了"快速补播一串动作",同一条理由适用于意图。

---

### [问题] 一个会让猫永远走不完第一步的 bug

`Pump` 会在过渡的**最后一 tick** 用**旧状态**重新寻路 —— 因为状态要等 clip 播完才改
(`CompleteClip` 里),而 `CanExitNow` 在最后一 tick 就已为真。结果是同一段过渡被无限重启。

**15 条断言当时全挂在这一个根因上。** 修法是在 `Pump` 开头加一道闸:
过渡在飞行中(`_activeEdge is not null`)就不再解算。

> 形状是「两个判据用了不同的时间点」:一个看"clip 播完没",一个看"能不能离开"。
> 单独看都对,合起来差一个 tick,而那一 tick 恰好是重入窗口。

---

### [问题] 契约里三处硬伤 + 一处会破坏唯一实体的边

由另一路会话(GPT)产出的 manifest / 门资源契约,复核后发现:

1. **`suspended` 唯一出边是 `→ stand`。** 而 `*any → suspended` 允许从 `behind_door` 进挂起。
   于是:猫走出门去了桌面或另一台 PC → 本机休眠 → 恢复 → **猫在这个家里重新出现**,
   而另一台 PC 可能仍持有它。**两只猫,直接破掉唯一实体(D40)。**
   → 已补 `suspended → behind_door` 边(`clip: null`,它本来就不在这儿,没有演出)。
2. **`mobility_cut` 删掉 stalk 却留下 `stand_to_stalk` / `stalk_to_stand`** —— 8 张帧成为
   指向已删状态的孤儿,画了没人播。`pounce` 的唯一入边也来自 stalk。
   → `omit_clips` 补齐(26 → **34** 帧),budgets 更正(142 → **134**,133 → **125**)。
3. **校验脚本只验全集**,从不套用 `optimizations.*.omit_clips` 再跑一遍 ⇒ 两个变体的图完整性
   从未被检查过 —— 而 145 张那个方案正是其中之一。
   → `PetManifest.Validate()` 补了**孤儿 clip 检查**,并立成断言。
4. **门资源的像素级断言独立复核成立**(alpha ∈ {0,255} · 不透明像素纯白 · `ground_y=112` 有接触 ·
   112 以下全透明 · leaf 覆盖单调递减 · portal_mask 与关闭态 leaf 完全重合)。
   但**契约语义有三处硬伤**:`door_enter` 绑死 desktop / `door_exit` 绑死 corridor
   (两扇门变成单向互斥)· corridor 门画成软垫猫窝而同文件 `excluded` 自禁
   `sleeping_bed_or_resting_prop` · `door_desktop_leaf` 第 4 帧 opaque=0(上翻门板全开应是压扁的一条)。
   **未改,待裁。**

**流程问题**:A 批(13 张身份锁定帧)**不存在**,却先产出了 D 批的 8 张猫身帧 ——
在没锁花纹的前提下画的。已废止的 `loading-cow-cat-pixel-rig-spec-v1.md` 也回到了盘上,
正文里躺着"推荐用 DragonBones 准备素材"。

---

### [问题] 跑自检时拿错了 exe

`bin\Debug\net9.0-windows\` 里躺着一个旧 TFM 的陈年 exe(几小时前的快照),
真正的输出在 **`net9.0-windows10.0.19041.0\`**。拿错的那次看到 3 条"失败",
实际当前构建是 0 失败。**排查时先核对 exe 时间戳。**

另一条:`--selftest` 走 `AttachConsole(-1)`,PowerShell 管道捕获不到输出;
要用 `Start-Process -RedirectStandardOutput`。

---

> 以下由另一路会话(MCP 线)记录 —— 与上方桌宠线无交集,未改 `20-client-win/`。

### [决策] 两层 MCP 设计封存等 P6;只提前执行三条「回头补」

本轮起于「探讨项目内的 MCP 功能情况」。产出的两层 MCP 设计(层一「内部的手」/ 层二外部控制面)
**按用户裁定封存** —— 项目当前在 **P3c**,层一工具池属 `P6`,而**层二在方案书里没有阶段归属**
(`grep laictl|ctld|外部控制面` 在 `PROJECT_PLAN_v3.0.md` 与 `STATE.md` 里 **0 次命中**;
包内的 M0–M3 是撰写时自造的标签,不是项目阶段)。

封存位置 `00-docs/decision-packets/`:`two-layer-mcp-decisions-2026-08-03.md`(D66–D75 草案)·
`negative-cases-N1-N14-2026-08-03.md`(43 条否定用例)。
`90-ops/ai-op-account/`(形态 A 受限账户脚本)**已标封存,不要跑** ——
层二不存在时建那个账户没有东西会用它,却真实增加一个要维护 ACL 的攻击面。

**唯一提前执行的例外是下面三条** —— 它们不是 P6 的事,是 **P2/P3a 已交付并标着 ✅ 的代码上的
现存缺陷**,而且当时正在成立。

★ 封存期间仍然成立的一条接口约束:Hermes(D65)将来加 `agent.default` 别名时必须
①补 `local_only` + `agent_allow` ②`"agent-worker"` 先进 `KNOWN_AGENTS`(顺序不能反)
③`agent_allow` 不得写 `vigil`/`pet`(会命中反向全表断言)。**否则网关起不来。**

---

### [实测] 回头补一 · `classify_caller` 是 denylist + fail-open,本机上**已经**漏了

`Get-LocalUser` 实测到两个**未登记**的外部 AI 沙箱账户 ——
`CodexSandboxOffline` / `CodexSandboxOnline`,**Enabled、在 `Users` 组、不在 `LOCAL_DENY_ACCOUNTS` 里**。

按当时判据(不在拒绝表 → `trusted-local`,解析不到也 → `trusted-local`),这两个账户连上
`127.0.0.1:8080` 就是 `trusted-local`,而它是 `E1_OVERRIDE_ALLOWED_TIERS` 的**唯一**成员、
`tainted._ALLOWED_CALLERS["S2"]` 的**唯一**成员。
⇒ **本机上任何没被点名拒绝的账户,连上来就是全系统最高档。**
与 provenance denylist / E1 override / unseal caller 同族:**判据写成放行优先,新增的默认自由,且不报错。**
`gateway.py` 自己的注释预言过这个失效模式,但只写给了将来的 `ai-op`。

**修法**:新建 `config/caller-accounts.toml`(allowlist),`load_caller_accounts()` 对
缺文件/缺字段/空表/通配 `*` 一律**拒绝启动**(照 `load_registry` 的形状);
`classify_caller` 与 `require_trusted_local` 双双改 allowlist,表外账户落新档 `unregistered-local`。
★ **降档不断连**:它仍能用 chat(等价于直连 `llama-server`,不构成回归),失去的只是
E1 解除权与 S2 正文权 —— 这是为了不把正在跑的客户端打死。
★ `require_trusted_local` 今天**还没有调用点**,正因如此现在改代价最小。

`test_caller_policy.py` 15 → **20 PASS · 0 FAIL**。原有那条 `classify fail-open → trusted-local`
**改写而非删除**,注释写明它记录的判据被什么实测推翻。
红测:改回 denylist → 5 条红;去掉 `require_trusted_local` 那道闸 → 1 条红。

★ **一条待裁**:本机账户 `Alle`(Enabled、在 `Users` 组)—— 若它是第二位家庭成员的登录账户,
需进 `trusted_local`,否则该成员客户端拿不到 E1 解除权。**裁定前保持不登记(fail-closed)。**

---

### [实测] 回头补二 · `pg_ident.conf` 零看守,而它是记忆库唯一那道不依赖 NTFS 的强闸

先证明这道闸是真的 —— 以**提权的机主账户**连 PG:

```
psql -h 127.0.0.1 -U mem_rw   -d memory  →  FATAL: SSPI authentication failed
psql -h 127.0.0.1 -U postgres -d memory  →  FATAL: SSPI authentication failed
```

`pg_hba` 四行全 `sspi map=mem`;`pg_ident` 四行 SYSTEM-USERNAME **全部 `ai-mem`**。
SSPI 认连接进程的 Windows SID,**没有可窃取的秘密** —— 比 Qdrant 的 bearer key 强
(后者 `api_key` 明文在 `config.yaml`,靠 NTFS ACL 护着,**是 NTFS 防线的派生物**)。
⇒ **两者不等强,文档不得并列成一条。**

**缺陷**:`verify-isolation.ps1` 一个字不读 `pg_ident`(唯一提及是 ④ 段注释);
`install-postgres.ps1` 的重跑守卫只查 `pg_hba`、else 分支却**同时覆写两个文件**。
⇒ 往 `pg_ident.conf` 追加一行 `mem <任意账户> mem_rw`,记忆库对它全开,
**全套断言没有一条会红**,还会被一个从不读它的守卫永久保住。

**修法**:`verify-isolation.ps1` 新增 ⑤ 段 —— **反向全表断言**:SYSTEM-USERNAME 列有且只有
`ai-mem`(不写「检查 ai-mem 在不在」:那条在有人**追加**一行时不会响,而追加正是事故的形状);
另加 `pg_hba` 无 `trust/md5/password` 兜底行、每条 `sspi` 都带 `map=`。
`install-postgres.ps1` 守卫改为同时看两个文件,只缺 `pg_ident` 时**抛错而非静默跳过**。

**顺带修掉一个既存死代码**:④ 段那句 `Skip-It '连不上 —— 需以 ai-mem 身份运行'` **一次都没跑过**。
第 25 行 `$ErrorActionPreference='Stop'` 把 psql 的 stderr 每一行包成 `NativeCommandError` ⇒
终止错误 ⇒ 脚本在那一行中止,**永远走不到下面的 `if`**,⑤⑥ 两段也不执行 —— 而退出码还是 0。
「写了个优雅降级分支,它一次都不会跑」正是 `DECISIONS.md`「假断言」整节的形状。
修完后脚本**第一次跑完**:**17 PASS · 0 FAIL · 3 SKIP**。

**红测**:对假 `pg_ident`/`pg_hba` 跑同源判据(**不碰真实配置**),四条全部命中 ——
追加 `ai-op` 映射 → 反向断言红;加 `trust` 行 → 弱认证红;去掉 `map=` → 缺 map 红。

**一条要降级的表述**:`pg_ident` 第一行把 `ai-mem` 映到 PG **超级用户 `postgres`**,
超级用户绕过一切权限检查 ⇒ `roles.sql` 的 `REVOKE UPDATE, DELETE` 只对 `ai_mem_local/remote` 成立。
**不得声称 `mem.audit_log` 的 append-only 是绝对的。** 该提示已写进 ⑤ 段输出。

---

### [实测] 回头补三 · `{state}` 六个子目录对所有已认证账户可写(含审计与成员表)

`Get-Acl D:\AI\state` 逐目录:

```
memory · secrets                                        Protected=True   ✓ 已断继承
db · identity · logs · openwebui · quarantine · tickets Protected=False  ✗ 继承 Authenticated Users : Modify
```

后果具体到两个文件:**`identity\store.json`**(成员表,D45 可见范围的唯一来源)与
**`logs\gate_rejection.jsonl`**(E1 凭证拦截审计,实测 35 KB 真实数据)——
两者对**每一个已认证账户**可写,**包括网关在 API 层明令拒绝的 `ai-exec` / `ai-asset`**。
网关在门口拒了它,**它转身直接改文件就行**。
`PLAN §6.7.5` 自己写过「P6 建立专用账户之前 OS ACL 层是零防护」。

**产出** `90-ops/state-acl/`:`harden-state-acl.ps1`(`-WhatIf` / 逐步确认 / `-Revert`)·
`verify-state-acl.ps1`(纯只读)· README。
★ **改 NTFS ACL 属系统安全设置变更,AI 不代跑,由机主执行。**

**✅ 2026-08-03 18:44 机主已执行,五个目录逐个确认施加。复核结果 27 PASS · 4 FAIL:**

```
★ ai-asset 写不了 logs        PASS   ← 加固前 FAIL
★ ai-asset 写不了 identity    PASS   ← 加固前 FAIL
★ ai-exec  写不了 logs        PASS   ← 加固前 FAIL
★ ai-exec  写不了 identity    PASS   ← 加固前 FAIL
★ ai-mem 仍能写 logs          PASS   ← 服务没被打死
```

4 条 FAIL **全部是 `openwebui`** —— 脚本故意没动它(见下)。
施加后实测 Embedding / pg-mem / Qdrant / Qdrant-s2 四个 `ai-mem` 服务仍全部 Running。

---

### [问题] `{state}\openwebui` —— 一个在路径契约之外、也在所有排除清单之外的数据仓

加固脚本的**反向全表**(穷举盘上子目录 vs `paths.toml` 登记)当场抓到它:
`{state}` 下真实存在 `openwebui`,但 `[state]` 段里**没有这个键**。

实测内容:

```
.webui_secret_key            32 B     ← 密钥
webui.db                    659 KB    ← Open WebUI 的 SQLite:用户 / 会话 / 设置
vector_db/chroma.sqlite3    188 KB    ← 上传文档的向量
uploads/ · cache/
最后修改 2026-07-28(一周未动)· 进程未运行 · 端口未监听
```

**三条同时成立:**

1. **不在路径契约里** ⇒ 任何按 `paths.toml` 遍历的运维脚本都碰不到它
   (本次加固脚本正是因此**拒绝动它** —— 契约里没有的东西,不该由一个运维脚本擅自定权限);
2. **不在备份排除清单里** —— `Copy-Root -Name 'state' -ExcludeDir @('quarantine') -ExcludeAbs $excludeAbs`,
   而 `$excludeAbs` 只有 memory 的几个子路径 + `secrets` + `identity`
   ⇒ 这把密钥与那批会话记录**会被逐字复制到 G:(USB · exFAT · 不加密)**;
3. **ACL 未加固** ⇒ 每个已认证账户可写,包括网关明令拒绝的 `ai-exec` / `ai-asset`。

`secrets` 与 `identity` 被专门排除出备份是有明确理由的(D43 S0.8)。
`.webui_secret_key` 大致属于同一类,但它**从来没被算进去** —— 因为没人知道这个目录存在。

**✅ 已裁定并执行(2026-08-03,机主选「归档删除」)**

移动前先查了那个 SQLite:**基本是空的** —— `chat` / `chat_message` / `memory` / `file` /
`document` / `knowledge` **全是 0 行**;只有 `auth` 1 行(一个注册账户,含口令哈希)、
`user` 1 行、`config` 341 行(设置)。装好、注册、配置过,**一次对话都没存过**。
⇒ 敏感度低于预估:剩下的是一份账户凭据 + 一把会话密钥 + 一堆设置,没有聊天内容。

按纪律**永不 delete**,移进 `{state}\quarantine\20260803-openwebui\`(1.25 MB)——
隔离区已加固,且本来就在备份排除清单里(`ExcludeDir @('quarantine')`)。

---

### [问题] ★★ 同卷 Move 保留 DACL —— 「移进隔离区」不等于「被隔离」

**归档动作本身当场暴露了一条对 §6.5 有直接影响的事实。**

移完复核那个归档目录,发现它 `Protected=True`、DACL 里赫然还有
`Authenticated Users : Modify` —— **宽泛权限跟着数据一起搬进了加固过的隔离区**。

原因:**同卷 Move 是重命名**。NTFS 为了让对象在新位置保持相同的有效访问,
会把它原先【继承来的】ACE **转成显式**带过去,并置上 Protected 位。

> **「移进一个加固过的目录」不等于「变成加固过的」。**

⇒ 对 §6.5「隔离区 = delete 的替代品,永不 delete」的直接影响:
**P6 的执行器把一个人人可写的文件移进隔离区之后,它还是人人可写** ——
隔离区看着把东西关起来了,实际只是换了个位置。这正是本项目固定审查视角
说的「看着有防护、实际没有」,而且这一次是**机制本身**的性质,不是谁写漏了。

**修法**:入区时 `icacls <目标> /reset /T`(丢掉显式 DACL、改回从隔离区继承)。
本次已对 `20260803-openwebui` 执行,复核确认现在只剩
`Zori Ma(M)` · `Administrators(F)` · `SYSTEM(F)`,且全部 `IsInherited=True`。

**已立成断言(不只是文档)**:`verify-state-acl.ps1` 新增 ⑤ 段,逐个检查隔离区条目
`AreAccessRulesProtected -eq $false` 且三个宽泛主体无写权限。
现在 **35 PASS · 0 FAIL**(顺带也验过了 7 月那条既有条目 `2026-07-26_hf-cache-from-C`)。

★ **这条要带进 P6**:隔离区的入区流程必须包含 ACL 重置,否则「隔离」只是改了个路径。

**ACL 表是取证出来的,不是猜的**:逐文件读 Owner —— `logs` 37 个文件里 **34 × `ai-mem`**,
`identity` 4 个全是 Administrators,`db`/`tickets` 空。⇒ 只有 `logs` 需额外授 `ai-mem`。
**授漏了服务日志会静默消失,授多了等于没加固。**

**我自己写的复核脚本第一版有假 PASS,跑出来才发现**:`Get-EffectiveWrite` 只比对
**账户自己的 SID**,于是「`ai-asset` 写不了 `logs`」报 PASS —— 但它没有具名 ACE 不代表写不了,
它在 `Authenticated Users` 里,那条组 ACE 有 Modify。**有效权限算的是令牌里所有 SID 的并集。**
现由 `Get-TokenSids` 穷举本地组成员关系 + 补 well-known 组(`S-1-5-11`/`S-1-1-0`)。
修完后脚本说真话:加固**前**基线 **3 PASS · 28 FAIL**。

**README 里写明本脚本没解决的**:审计文件与服务 stdout 日志仍在同一目录,
断继承之后能写服务日志的账户仍能改审计 —— 真正的修法是独立的 append-only 审计目录。

---

### [实作] M0a · `registry.toml` 的 `local_only` / `agent_allow`(D69 草案)

「Vigil / 宠物始终本地」是说出口的承诺,但此前**没有任何强制机制** —— `registry.toml` 只有
`egress`,今天把某别名 `backend` 改成云端地址、或给 Vigil 配一条指向 `escalate.cloud` 的别名,
**不会有任何东西报错**。

两个字段必填 + `KNOWN_AGENTS` 封闭表 + 六条 fail-closed,第六条是**反向全表断言** ——
允许 `vigil`/`pet` 的别名**有且只有** `assistant.resident`。正向断言只管一条,
而事故的形状永远是**新增的那条忘了管**。

新增 `test_local_only_registry.py`,**69 PASS · 0 FAIL**;逐条改坏代码红测,8 个变异全部命中
正确的那条断言。最强一例:偷偷给 Vigil 加一条云端别名 ⇒ `REGISTRY = load_registry()` 在模块
加载期抛 `RegistryError`,**网关根本起不来**。

**抓到我自己写的一条假断言**:「通配 `*` 必须拒绝」原本删掉检查仍全绿 —— `*` 不在 `KNOWN_AGENTS` 里,
「未登记 Agent」那条**抢先命中**,消息里照样有别名名字。修法:每条 `expect_refuse` 加 `reason=`,
要求消息里出现**它自己那条检查**的理由词。

★ **必须如实写下的强度限制**:`agent_allow` **只在加载期被校验**,
**运行期没有任何调用点按 agent 过滤别名**(网关请求上下文里没有 agent 维,唯一会话标识是
客户端自报的 `x-session-id`,读它违反 §6.6 F1)。真正兜底的是端点隔离与 `LOCAL_DENY_ACCOUNTS`。
**不得声称它是运行期防线。**

---

### [问题] 多会话并发写中央文档

本轮实测到**四路会话**同时写这个仓库,`DECISIONS.md` / `PROJECT_PLAN_v3.0.md` / `STATE.md`
长时间未提交。D64/D65 已经撞过一次号(靠"已进 git 历史的那条留住编号"化解);
本条记录时,连本文件(`worklog/2026-08.md`)都是在我准备新建的**同一分钟内**被另一路会话建出来的
—— 差一点就整份覆盖掉桌宠那几条。

据此立的规矩(写进 D75 草案,**随包封存**):D 编号取号以**已提交**为准 ·
动中央文档前先 `git status` · **绝不用 Write 整体覆盖既有文件** ·
决议先落 `decision-packets/` 由人串行并入。

**本轮全程未改** `DECISIONS.md` / `PROJECT_PLAN_v3.0.md` / `STATE.md` —— 记录本条时它们仍是脏的。

---

## 2026-08-03 · 客户端线(本节 2026-08-03 晚补记)

> 当天 `worklog` 原本只记了桌宠线与两层 MCP 线两路;客户端这一路(约 12 次提交)缺记,此处补上。

**[决策]**
- **D60** 多语言表(翻译第四场景):一个 JSON = 一个会话;导出**一个完整 JSON 文件**;
  「复制 Prompt」是给对方项目的 **AI 建表指令**,不是翻译指令。
- **D61** 聊天空间第二功能**回信**:三种载体 × 三种语气 × 随语言变的问候祝福;
  **按下生成才落记录**,有记录才进会话列表;编辑旧记录再生成 = 追加不覆盖。
- **D62** **全功能幽灵会话**:幽灵跟当前场景同型;回信/译表/文件翻译三张文档表**都不许落盘**;
  幽灵文件翻译**不抄副本**;换场景 = 换了一次,切场景就抹掉。
- **D63** 署名日期改**日期选择浮窗**(复用日程/待办那套滚轮);**文件翻译的 PDF 预览真的接上**
  —— 用 Windows 自带的 `Windows.Data.Pdf`,不引第三方二进制、不联网。
- **D64** 对抗式复核收口:六视角 42 条发现,每条被三票独立反驳,**23 条站住并逐条修**。
- **D77** P3c 收尾:i18n 移 P7 · 四条判据补齐 · 两条判据修订 · 四条明确归属他人。

**[实测 · 两起事故,同一根因]**
- 切载体到「信件」必报错:动作排每次刷新都把字段级 `_signDate` 塞进**新建的**容器,旧父子关系没解开;
- **闪退**:同一段代码跑在那个输入框自己的 `TextChanged` 里 —— **带焦点的 TextBox 在 TSF 持锁期被重挂,
  WPF 直接 `Environment.FailFast` 杀进程**,catch 不住、`crash.log` 里一个字都没有(只在 Windows 事件日志)。
- 改法:动作排**只建一次、刷新只改属性**。「重挂前先摘」治不了 FailFast —— 摘+挂本身就是移树。

**[问题 · 并发]**
- 与另外三路会话并行,**撞了三次**:两次是 `git add -A` 把别人的活卷进自己的提交(双向各一次),
  一次是 D 号撞车(D76 → 改 D77)。教训已写进 `decision-packets/worktree-split-2026-08-03.md`。

**[口径]**
- 客户端 selftest:**PASS=1566 FAIL=0**(P3c 开工时 30)。客户端代码 **34.5k 行**(四天前 14k)。

---

## 2026-08-04 · 接手方复核:证书名修复留住,自配对封存

> 新会话接手,对上一手**未提交**的 5 个客户端改动跑对抗式复核(14 agent / 4 维度 / 每条发现独立反驳)。

**[实作]**
- **`b136f01`** 证书名修复单独提交:enroll 之后必须把 `edgeUrl` 换成证书名 `localai-<hubShort>.local`
  —— `Trusted()` 只钉根 CA、**没关主机名校验**,继续用 `https://<ip>` 会在 `/pair/status`
  第一次握手就失败,设备永远停在 provisioning。**图形界面这条配对路径此前从没走完过**
  (P3b 那次两台实机走的是控制台,绕开了这段)。复核对这条**零发现**。

**[决策]**
- **`0732690`** 决议包 `decision-packets/selfpair-review-2026-08-04.md`:主机自配对整块
  **判 DO_NOT_COMMIT 并封存**(留在工作区未提交)。

**[问题] 自配对每次未配对启动都在局域网上敞开匿名准入窗口**
- `SelfPair.cs:67` 的 `WindowAsync(true,1)` 由 `App.xaml.cs:145` 每次启动无条件触发;
  `OpenWindow` 置的是**全局** `_windowOpen`(无来源/无 CSR 限定),`/pair/enroll` 匿名,
  而 `run-lan` **只把 8443 绑网卡 IP、回环没有监听者** ⇒ 主机自己也只能走局域网,逼出全局开窗。
  灌满 `MaxPending=8` 即可让主机**永远停在 provisioning**,每次开机被远程拒一次。
- 同时违反三处成文契约:规格 §6「不该开局域网窗口」· `WindowAsync` 自身文档 ·
  **审查发现[3] 专门拿掉过的暴露**(`OpenPairingWindowOnStart:false`)。
- ★ **自检 0 FAIL 洗不掉它** —— 测试把违规钉成了「正确行为」。合规出路(§5.1 回环 self-enroll)
  需 core 支持,**客户端单方面改不成**。
- 另记:两条自配对路径**两把独立的锁**(并发出重复设备记录 + 悬空签名密钥,中危)·
  三条假断言/重言式(`Selftest.cs:5179` finally 与关窗分开查 · `:5183` `Log(` 命中方法定义 ·
  `:5157` ㉔ 只钉声明漏了承重的 `edgeUrl` 行)。

**[报告 · Artifact]**
- 阶段进度总览(P0→P11 · 逐阶段百分比 · 阻塞依赖 · 下一步顺序):
  https://claude.ai/code/artifact/773858f7-59a2-418a-abaf-8ab7e4f626b6

---

## 2026-08-04 深夜 · 用户实测两条 bug 修掉(`d2c941c`)

> 用户实测报:「关闭的时候会卡一段时间」+「匹配的列表里面会将同样的机器出现多次」。
> ★ 后者**正是当日复核判 MEDIUM 的那条**(两条自配对路径各用各的锁),
> 但取证后发现**真正的根因更靠下、也更普遍** —— 见下。

**[实测] 重复设备:不是 UI bug,是中枢上的真实记录**

先排除渲染侧:`DevicesView.cs:126/905` 两处 `Children.Clear()` 都在,且列表已按
`Status != "revoked"` 过滤 ⇒ **不是「刷新只追加不清空」**。

直接问正在跑的中枢(`127.0.0.1:8442` 管理面)取实物:

```
/admin/pairing/pending → 空          ← 不是待批列表
/admin/devices         → 9 条,其中 7 条 HONGKONGPINGPON
                          6 条 approvedAt=null(半截),1 条 active
```

**根因**:设备记录在 **Approve 那一刻**就建好(`Pairing.cs:175` → `AddProvisioning`),
要等客户端 claim/complete 才转 `active`。中间任何一步失败,它就**永远**停在 `provisioning` ——
**全仓原先没有任何代码让它过期**。而证书名 bug(`b136f01`)让 `/pair/status` 第一次握手就失败,
于是每重试一次就多留一条。

> ★ 握手 bug 已修,但**积累机制是独立缺陷**:今后任何一次没走完的配对都会再留一条。
> 所以修的是机制,不是那一次的症状。

**修法** `Store.SweepStaleProvisioning(ttl)`,挂在 `Pairing.Approve` 建新记录**之前**(那就是增长点):
- 落 `revoked` 而**不新造状态词** —— 客户端按 `Status != "revoked"` 过滤,新词会让死记录
  继续显示在「已配对电脑」里;也**不 delete**,`RevokedAt` 就是痕;
- 连带吊销它那张 candidate 证书(留着等于给一条死记录留一张能领的证);
- **判据只用记录自己的时间,不用自报的设备名** —— `UntrustedDisplayName` 是对方随便写的,
  拿它当去重键等于让局域网上任何人决定哪条记录该被清掉;
- TTL **15 分钟**,远大于客户端约 5 分钟的领证等待 ⇒ 不会掐掉正在进行中的配对(已立断言)。

**[实测] 关闭卡顿:总预算够,但没有【单步】上限**

`App.xaml.cs:536` 退出时阻塞等善后,善后有 5 秒**总**预算(`ShutdownCoordinator.cs`)。
步骤是**顺序**跑的,而第 ② 步 `end-session+release-vram` 要发一次真实网络请求
(`Transport.Send` 每次现建 HttpClient 走完整 mTLS 握手,**用的是默认 100 秒超时**)。
中枢或网关没起时它就干等,把总预算吃光 ⇒ ③ 保存设置 · ④ 停显存监视 · ⑤ 收托盘图标
**一个都轮不到**。

> ⇒ 症状不只是「卡」:**设置没保存上、托盘图标还赖在那儿**,而这两条从来没人往这儿想。

**修法** 加 `PerStepBudget`(1.5 秒),与总预算取先到者(linked CTS);
**单步超时只掐掉它自己,后面的步骤照跑**。新断言实测用时 **202ms** ——
没有这道闸时会是 5000ms,这个时间差本身就是判别证据。

**[问题] ★ 出包门禁自己漏了一次(记账,**已于次日修掉,见 `8bed788`**)**

`build-client.ps1` 第二形状自检**没跑起来**却判了通过:

```
Failed to map file. CreateFileW(...\localai-gate-...\client\localai-client.exe) failed with error 32
Failure processing application bundle. Couldn't memory map the bundle file for reading.
    自检通过（两种安装位置,退出码均为 0）      ← 门禁照样打印通过
```

`error 32` = 文件被占用(刚 `Copy-Item` 完就跑,多半是杀软扫描持锁)。
门禁看的是 `$LASTEXITCODE`,而**「exe 根本没起来」与「自检跑完且全绿」在这一位上不可分辨**。
⇒ 这正是本项目在猎的**假门禁**形状。本次靠人工另跑一遍才拿到真结果,**没有据此放行**。
**修法待定**(建议:自检成功时打印一行哨兵到文件,门禁要求哨兵在场,而不是只看退出码)。

**[口径]**
- identity:11 + 15 + **28**(配对套件 22→28)+ 14 + 13,FAIL=0;
- 客户端 selftest **PASS=1724 FAIL=0**(仓库内布局跑;打包布局下是 769 ——
  差额是**读源码那类断言在源文件不在旁边时静默跳过**,不是回归)。
- 部署 `dist\client\localai-client.exe`,版本戳 `20260804-0335+c37a890.dirty`。
  ★ **本次部署物【不含】自配对整块** —— 构建前把封存的三个文件 stash 出去了,
  所以它不再携带决议包 §2 那个「每次未配对启动敞开局域网准入窗口」的 blocker
  (决议包 §17 原本就建议这么做)。封存代码已原样恢复回工作区(逐字比对一致)。

---

## 2026-08-04 白天 · 出包门禁修真(`8bed788`)

> 接着昨晚记的那条「第二形状 error 32 没跑起来却判通过」往下挖,
> **发现记错了根因** —— 真正的病比「退出码分不出没启动和全绿」还靠下一层。

**[实测] 门禁从来没有在等自检跑完**

客户端是 **WinExe(GUI 子系统)**,而 PowerShell 的调用运算符 `& $exe` 对这类进程
**不等待**,当场返回。于是:

```powershell
& $exe --selftest
$code = $LASTEXITCODE     # ← 拿到的是【上一条命令】的残留值,恰好是 0
```

⇒ **门禁读的根本不是自检结果**,两种安装位置的检查双双形同虚设。
昨晚看到的 `error 32` 只是让这件事露了个头:那次是子进程真的没起来,
但即使它起来了、即使它红了,门禁**同样不会知道**。

> ★ 这条比昨晚记的那版严重:昨晚以为是「判据不够细」,实际是**判据读错了变量**。
> 记账要改口径:**不是门禁太宽,是门禁没接线。**

**[实作] 三处修法**

1. **真等它结束**:改 `Start-Process -Wait -PassThru`,读 `$proc.ExitCode`;
   stdout 重定向到文件 —— 新进程没有可 `AttachConsole` 的父控制台,不重定向就什么都留不下。
2. **判据从「没有失败的迹象」改成「跑过的证据」**:自检跑到最后一行才写哨兵
   (`Selftest.WriteSentinel`,受 `LOCALAI_SELFTEST_SENTINEL` 控制,不设就不写 ——
   人手跑 `--selftest` 不该在磁盘留东西)。**四条同时成立才算过**:
   哨兵在 + `FAIL=0` + `PASS>0` + 退出码 0。哨兵缺失重试 3 次(`error 32` 是暂态),
   仍无则判红;**哨兵与退出码打架也判红**——不挑一个信。
3. **失败要能行动**:把自检输出摆出来,有 FAIL 就直接列出红的那几条。
   否则门禁只会说「红了」而不说红在哪。

**[实测] 红测(判据取自真脚本抠出来的门禁函数,不是另写一份副本)**

| 坏输入 | 结果 |
|---|---|
| 退出码 0 但自检**根本没跑**(事故原形) | 红 ✅ |
| `FAIL>0` | 红 ✅ |
| 哨兵说全绿、退出码非 0(两份账打架) | 红 ✅ |
| `PASS=0`(跑了但一条都没断言到) | 红 ✅ |
| 正常全绿 | 绿 ✅(门禁把好包也拦掉同样是坏门禁) |

**[实作] 顺带修版本戳:脏树构建原来说不清自己是什么**

原先脏树只加 `.dirty`,而戳把人指向 HEAD —— exe 里装的却不是 HEAD 的代码。
实测吃过:03:40 那份戳着 `c37a890`,可它携带的两条修复在 `d2c941c`,
按戳去翻 `c37a890` 一个字都找不到。(更荒唐的是 `.dirty` 后面还跟着**同一个提交的全哈希**,
等于零新信息。)
⇒ 改成 `.dirty-<工作树指纹>`:指纹 = `porcelain` 清单 + 每个脏文件
(**含未跟踪文件** —— 它们照样会被编进去)的内容哈希,再取一次哈希。
`VERSION.txt` 另把脏文件清单原样列出,让拿到包的人(和三个月后的自己)说得清手上是什么。
本次验证产物戳:`20260804-1346+72da99b.dirty-948693f0`。

**[口径]**
- 客户端 selftest **PASS=1729 FAIL=0**(仓库内;含封存的 ㉕ 五条)。
- 出包两种形状各 **PASS=765 FAIL=0**,**均有哨兵佐证**。
  (打包布局比仓库内少,是读源码那类断言在源文件不在旁边时跳过 —— 已知差异,不是回归。)
- ★ 本轮**没有重新出包到 `dist\`**:验证用的产物写到临时目录。
  原因是工作树带着封存的自配对,出到正式包里会把那个 blocker 带进分发物。
  `dist\client\localai-client.exe` 仍是昨晚 03:40 那份(不含自配对)。

---

## 2026-08-04 下午 · 核实昨晚那两条修复:量到 1602ms,并补上退出留痕(`1426a68`)

**[实测] 关闭耗时 = 1602ms(外部计时:WM_CLOSE → 进程退出)**

对**修复后的产物**实测。修复前的形状是 5 秒总预算被网络步骤吃光,
且 ③ 保存设置 · ④ 停显存监视 · ⑤ 收托盘图标**根本轮不到**。
现在 1.6 秒,构成基本是第 ② 步 `end-session` 吃满它自己的 1.5 秒单步上限 + 本地步骤(毫秒级)。

★ **重复设备那条也站住了**:关了几次之后 `/admin/devices` 仍是 9 条,**无新增**。

**[问题] 排查时撞上:退出善后【一个字都不写】**

想核实"到底好了没",却发现**没有任何可测的东西**:

- `ShutdownCoordinator` 支持 `log` 回调,而唯一调用点 `App.RunCleanup` 调
  `RunOnceAsync(reason)` 时**没传** ⇒ 整条退出路径静默至今;
- 落盘文件的 mtime **不能当判据** —— `SaveStores` 同时挂在会话中的防抖定时器上
  (`_saveDebounce`),分不出「退出时存的」和「用着用着自动存的」。
  ★ 我一度据 mtime 推出「间隔 1.5 秒 = 单步上限」,**那个推断是错的**,
  真实时序是 `settings.json` 早于其余 stores;判据本身不成立。**已作废该推断。**

⇒ 与项目自己的纪律是同一句话:**静默的自动流程必须留痕**。
那句话已经为自配对立成断言,而退出路径这条更早存在、也更常跑,却一直漏着。

**[实作]** 留痕由 `ShutdownCoordinator` **自己兜底**,不依赖调用方记得传 `log`(当初就是忘了传)。
默认落 `%TEMP%\localai-shutdown.log`:`reason` / 两档预算 / **每步结果与耗时** / 总耗时;
被单步上限掐断的步骤**指名道姓**记下来 —— 否则只知道慢,不知道慢在哪一步。
一次性追加而非逐行写(退出路径上每行开一次句柄本身就会让关闭更慢,
**不能让测量手段污染被测对象**)。四条断言钉住。

**[实测 · 排查中发现的两条]**

1. **用户跑的是 `dist\client-pack\` 那份,不是 `dist\client\`。** 这也是构建一度失败的原因
   (exe 被运行中的客户端锁着)。两处现已同步为同一份。
2. **中枢在网关没起时抛未处理异常**:`/v1/models` → `lan-edge` 代理到 `127.0.0.1:8080`
   被拒 → `Kestrel` 未处理异常进 Windows 事件日志(今日 13:55:55 / 13:56:16 各一条)。
   应答成干净的 502/503 而不是 500 + 事件日志噪音。**记账,未修**(属 core 车道)。

**[口径]** 出包两种形状 **PASS=773 / 769,FAIL=0**,均有哨兵佐证。
产物戳 `20260804-1415+e2b73a0.dirty-27f714ef`(脏树指纹已生效)。
★ 两种形状 PASS 数**不相等**是已知的:4 条断言依赖 `dist\host` 真实存在;它们是**跳过**不是失败。

---

## 2026-08-04 · ★ P3c 验收签字(两台真 PC)

**[决策] P3c 统一客户端 v1 验收通过,阶段关闭。下一站 P4。**
决议草案 `decision-packets/p3c-signoff-2026-08-04.md`,**建议取号 D80**(已提交最大 D79),
按 D75 由机主串行并入 `DECISIONS.md`。

**[实测] 中枢上两台 active 设备**

| 设备 | 状态 | 批准于(本地) | 指纹 |
|---|---|---|---|
| `HONGKONGPINGPON`(主机) | active | 02:19:26 | `A7C61F9D` |
| **`SENIORBIRDS`(副机)** | **active** | **13:59:28** | `CE3D22A7` |

`generation = 12`;副机装的就是 `dist/client-pack/` 出的包。
⇒ STATE 上挂着的两项(主机批一次真配对 · 出的包在副机装一遍)**同时满足**。

**[问题] ★★ 08-03 标的 S4「✅」当时并不完整 —— 这一笔必须留在签字里**

图形界面那条配对路径**从来没有走完过**:P3b 那次两台实机走的是**控制台**,
绕开了 `ClientTransport` 里 enroll 之后那段,而那段有个必然失败的分支
(`Trusted()` 只钉根 CA、没关主机名校验 ⇒ 继续用 `https://<ip>` 必在 `/pair/status`
第一次握手就失败,设备永远停在 provisioning)。`b136f01` 修掉后,**今天才第一次端到端跑通**。

> 形状与本项目固定审查视角同族:**验收路径与用户实际走的路径不是同一条,勾就是假的。**

**[决策] 验收第三句的落点 —— 不写成假绿**

用户裁定「我没办法换地址没办法测试,直接算过了」。**按裁定放行,但拆成两半记**:
- **已证明**:`HubClient.SetDial()` **只重写 `Profile.Dial` 一个字段**,
  `KeyName`/`CaCertB64`/`DeviceCertB64`/`HubId`/`EdgeUrl` **一字未动**
  ⇒「配对不失效」**结构性成立**,换地址这条路径根本碰不到证书材料;
  入口有断言(`Selftest.cs:5607-5612` / `:5315-5317`);
  `RediscoverAsync()` 只接受 hub_id 完全一致的那台,不放松信任。
- **未证明**:真实换一次网段后的端到端重连。
  ⇒ 表述定为「**机制结构性成立 + 实机换网段由用户裁定免验**」,
  **不得写成「已实测通过」**。★ 这是 P3c 验收里唯一一条没有实机背书的。

**[口径]** identity 11+15+**28**+14+13 · lan-edge 8+6+15 · transport 5 ·
**客户端 selftest PASS=1728 FAIL=0**(已提交态;工作区另有封存的 5 条)。
出包两种形状 773 / 769,FAIL=0,均有哨兵佐证。

**[记账] 随签字一并交代、不计入 P3c 完成度的**
主机自配对**封存**(DO_NOT_COMMIT,需 core 补回环 self-enroll;出包时已排除,分发物不含该 blocker)·
`[~]` 主机自识别仍是启发式(D36 原文移 P7)· 待决 8 ④ `worktree-split` 决议包仍待取号并入 ·
副机的**业务调用未验**(实测时网关 8080 未起;顺带记 core 侧缺陷:该情形 lan-edge 抛未处理异常,应答 502/503)。

---

## 2026-08-04 · 方向 B 勘察:三条更正 + 两条实测约束(`87ac3b9` · 决议包待并入)

> 起于「下一步做方向 B」。**没有开工** —— 勘察结论是:开工前有三条记载要先更正,
> 否则会照着一份错的地图施工。决议草案 `decision-packets/egress-direction-b-2026-08-04.md`(建议 **D81**)。

**[问题] ★★ 三条更正**

1. **`sink` 是真缺的,不是已有物改名**(★ 更正我自己当天早些时候说的「D39 有一半已做掉」)。
   `Backend.egress` 管**请求发给哪个模型**,D39 的 `sink` 管**答案发到哪儿** —— 两根轴。
   失效路径:桥调 `assistant.fast`(`egress=false`)→ `unseal_for_prompt` **干净放行** →
   记忆进 prompt → 模型复述 → 顺通道出去。`tainted.py:331` 那道闸**全程没被触发**。
2. **`channel-relay` 造不出来**。`classify_caller` 只能返回五个值,没有它;
   真接一个桥今天会落 `unregistered-local` —— 按 D30「降档不断连」**照样走 chat、照样拿流式**,
   且与"没人登记的普通账户"不可区分。
3. **响应扫描器永远守不住记忆**。复述没有正则特征;真正的强制在 §4.6.3
   ——**出境 sink 的会话不该挂载 `memory.search`**。⇒ 方向 B 只能是凭证线 + 审计线,
   **不得**写成「L5 的代码对应物」。

**[实测] 两条会毁掉实现的约束(自己复测,非转述)**

- **扫整个响应体 = 100% 全拦**:OpenAI 响应的 `id`(32 位随机串)单独就触发 `high_entropy`。
  只扫 `content` 才干净。⇒ 只能扫"生成内容";而那是 **denylist 形状**,必须配反向断言。
  ★ naive 实现会让每条回答都被拦,而当值的人多半会去"修" —— **修法几乎必然是调松闸门**。
- **扫原始 SSE 字节 = 对转义内容瞎**:全角卡号解码后命中 `card_pan`,
  `\uXXXX` 转义形态命中 `[]`。`normalize()` 折全角折中文数字,**不折 `\uXXXX`**。
  ⇒ 必须先解码再扫。★ **澄清**:这条**不是**请求侧的活洞(body 先被 `request.json()` 解码),
  只在方向 B 新写的"扫原始帧"代码里才成立 —— 一度误判为现存漏洞,已更正。

**[决策] 用户裁定**:出境 sink **不走流式、发一整段**;**PC 端照旧流式**(它在受控设备集内);
向手机发送**按通道契约限长**(上限长在 `ChannelCapabilities` 上,缺字段 fail-closed;
两层=请求侧 `max_tokens` 省事 + 闸门侧强制保证;顺序写死「全文过闸 → 放行了才裁剪」;截断不静默)。
★ 澄清:「不给流式」≠「一条巨长消息」—— 扫完之后按段落分条发是自由的。

**[实作] `87ac3b9` 网关错误响应不再披露内部信息**(现存问题,与方向 B 无关)
404 曾枚举别名全表 · 502 曾回 `r.text[:500]`(上游原始字节)· 上游 4xx/5xx 同样回 `detail`+`backend` ·
503 曾回后端 URL 与 fallback。威胁模型具体:D30 有意保留连接的 `unregistered-local`
(08-03 实测到两个外部 AI 沙箱账户)拿这些当侦察材料。**定性:加固,非高危**。
诊断改落 `{state}/logs/upstream_problem.jsonl`;新增 `_short_echo()` 回显截断 64 + 剔控制字符。
★ 承重的是**反向全表 AST 断言**:走遍 `chat_completions` 每个 `return`,带披露字段就红。
红测:偷偷把 `backend` 加回 503 → 精确报 `(243,'backend')` 退 1;还原复绿。
**网关套件 233 → 251 PASS / 0 FAIL**(11 个文件,全部退出码 0)。

**[问题] ★ 没有 CI —— 断言只有人记得跑才会红**
`.githooks/pre-commit` 只 grep 绝对路径;`90-ops/` 下没有任何东西跑网关/记忆测试。
按本项目自己的标准,**没人跑的断言就是假断言**。主张把「一键跑全部套件」作为方向 B 的**前置交付**。
(今天顺手实证成本很低:一个循环就跑完 11 个文件并汇总。)

**[待裁]** `unregistered-local` 算出境还是本地 sink(fail-closed 答案会改变本机每个未登记账户的行为)·
`unseal_for_prompt` 要不要真加 `sink` 参数(迁移成本有界,约 10 个调用点全在测试里)·
拦下时调用方收到什么 · 「答案」的字段边界 · **STATE 说的「解锁三条线」需复核**
(Hermes H2 要防的是不可信抓取文本回流,凭证扫描器解决不了,可能只解锁一条半)。

---

## 2026-08-04 · 三个决议包并入 `DECISIONS.md`(`855534f` + 后续交叉引用修正)

**[决策]** 取号以已提交为准(此前最大 **D79**),顺次:

| 号 | 内容 | 出处包 |
|---|---|---|
| **D80** | P3c 验收签字:两台真 PC 实测通过,阶段关闭 | `p3c-signoff-2026-08-04.md` |
| **D81** | 出境闸方向 B:开工前的三条更正与两条实测约束(**暂不开工**) | `egress-direction-b-2026-08-04.md` |
| **D82** | 多进程并行:worktree 分工与并发纪律 | `worktree-split-2026-08-03.md` |

按 **D82 自己刚写死的协议**执行:取号后**立刻提交**,不写完全文再提交 —— 号靠"已进 git 历史"确定。

**[问题] D82 并入时做了一处口径更正 —— 原包 §3 已过期**

`worktree-split` 写于 08-03,其 §3「现在这四条车道各干什么」并入时已有两条不成立:
- 车道 4「P3c 最后两项实机验收」**已于 08-04 完成**(D80),任务作废;
- 车道 1 说方向 B「一件事同时解锁三处」**已被 D81 列为待复核**
  (Hermes H2 要防的是不可信抓取文本回流,凭证扫描器解决不了,可能只解锁一条半)。

⇒ **只并入 §1/§2/§4/§5 的纪律部分,§3 车道排期不并入**,留在包内作历史记录。
**不把过期的优先级写进中央文档** —— 中央文档的价值在于「当前为真」。

**[实作] 交叉引用收口**
三个包头部标注为已并入 · `STATE.md` 待决事项 8 改写(新增 ⑤⑥⑦ 三项,其中
`selfpair-review` 明确**暂不取号**:它是一份"不要提交"的裁定,内容已被 D80/D81 引用,
待自配对解封时随实现一并取号)· `STATE.md` / `PROJECT_PLAN` 里的「建议 D80」改成正式号。
★ **worklog 与决议包不回头改写** —— 它们是时点记录;活文档(STATE/PLAN/DECISIONS)才要求当前为真。

**[实测] 并入后自检**:活文档里已无「建议 D8x」残留;D 号无撞号
(自检一度报出 D30/D40/D50/D52/D59/D65 重复,复查确认全是既有的 `补/补记/补充/再补` 续条体例,非撞号)。

---

## 2026-08-04 · 收尾两件去风险动作:封存代码上分支 + 全仓基线首次实跑

**[实作] 封存的自配对从「工作区未提交」改为分支 `parked/selfpair`(`2f33314`)**

原形式有两个真实代价,今天都吃到了:
- **被一次 `git reset --hard` 连带抹掉**(回滚一条红测提交时;已从当天早些的备份完整恢复并逐字比对);
- **每次出包都要做一遍"摘出去再放回来"的手术**(㉕ 摘出 → 构建 → 放回),做了三次。

放分支上两个问题一起消掉。★ **该分支不得合入 `main`** —— 它不是待合并的功能分支,是存放点。
解封前必须先做的三件事写在该分支的提交信息里:裁定 D81 的出路 · 修三条假断言 · 统一两个入口的锁。
取回:`git checkout parked/selfpair -- <文件>`。
指针已在 `DECISIONS.md` D80、`STATE.md` 两处同步(活文档必须当前为真)。

**[实测] ★ 全仓基线首次实跑:`run-tests.ps1 -Full` → PASS=2111 FAIL=0,35 秒**

| 组 | PASS |
|---|---|
| Python(gateway 251 + gpu-broker 38) | 289 |
| dotnet(identity 81 · lan-edge 8 · transport 5) | 94 |
| 客户端 `--selftest` | 1728 |
| **合计** | **2111** |

★ 这是这个仓库**第一个完整的跨模块基线数字**。此前各套件的数只分散在各自的记录里,
从没有人把它们一次跑齐过 —— 而"没人跑的断言就是假断言"。

★ **`-Full` 这条路径此前从没跑过**(写完就提交了)。**在门禁里放没跑过的代码,正是本项目在猎的形状**,
所以本轮特意先跑一遍再收工:一次通过,无需修补。

★ 记忆库 14 个套件**不在这 2111 里**,运行器把理由明写在覆盖账中,不静默省略。

---

## 2026-08-04 · P4 开工:五片落地(S0 · S1 · S3 · S4a · S4b)

> P3c 签字后转入 P4。**第一发现不在 P4 清单上。**
> 形状建议来自四路并行勘察 + 切法评审;两条形状裁定见
> `decision-packets/p4-broker-shape-2026-08-04.md`(建议 **D83**)。

### [问题] ★★ S0 —— 显存闸唯一的生产集成,自集成起就是坏的

复现(清空 `PYTHONIOENCODING` 的干净 cp936 PowerShell,跑本该**通过**的 daily 预设 7.99 ≤ 8.52):

```
stdout: (空)
stderr: UnicodeEncodeError: 'gbk' codec can't encode character '\u2713'   ← 判定符 ✓
EXIT  = 1
```

而 `start-stack.ps1:76` 只看退出码 ⇒ 打印「拒绝启动」。
⇒ 自 2026-07-28 集成以来,这台机器上要么起不了栈、要么靠 `-Force`,
而 **`-Force` 把三道闸整个跳过** —— 那道「无 Broker 期显存过渡措施」**实际上一天都没生效过**。

★ **已有的 38 条断言全绿却抓不到它**:它们是**进程内**跑的,而坏掉的恰好是唯一那条**跨进程**的生产路径。
形状与当天早些时候修掉的出包门禁同源:**失败与成功长得一模一样**。

**修法四处**:编码双保险(`stdout.reconfigure` + 判定符换 ASCII `[OK]`/`[XX]`——
安全闸的判定不该依赖控制台编码)· **退出码三态**(0 通过 / 1 判定为拒 / **2 闸自己没跑起来**,
且 `-Force` **不能覆盖 2**)· 只采样一次 NVML(原先预览采一次、退出码判定又采一次,
桌面一变两者可矛盾,§8.1 规则 18)· 预览不再静默吞掉未登记组件。

★ 红测时验到一个额外的好性质:**把编码 bug 还原后,退出码是 2 而不是 1** ——
即使它再回来,也不会被误读成"拒绝",且 `-Force` 盖不住。

**顺带修一个数据转抄错误**:`llm.assistant.8b@8k` 的 peak 是 `5.0`,而那是 NUM-1 表**同行的权重列**;
peak 列写的是 **5.31 ★实测**。证据:其余 8 个组件与 NUM-1 **逐字相等**,唯独这行不等、
且不等的值恰等于同行权重列 ⇒ **转抄错误,不是判据分歧**。方向也是 fail-safe(闸更保守)。
该组件在 `start-stack -Ctx≤8192` 的生产路径上。

### [实作] S1 —— `gpu.*` 架构反向全表,**赶在工具池存在之前**写

规则在三处一字不改地重复过,但今天是**真空成立**的(`config/tools.toml` 不存在、
`10-core/mcp-tools/{http,stdio}` 是空目录)—— 也**没有任何断言会在工具池被建出来的那一刻变红**。
⇒ 规则活在文档里,而新增注册表的那次提交不会被拦。**晚一天写,它就永远是纸面条款。**

★ 与项目吃过的亏同形但**不同类**:`require_trusted_local`/`backend_of()` 是「写了没人用」,
这一条是「**不写就没人拦**」—— 代价发生在未来某次提交里。

承重的是第二层:**扫描根清单本身**被钉住(全仓扫"看起来像工具声明"的文件,不在 `SCAN_ROOTS` 即红)。
红测:把工具池放到 `10-core/orchestrator/` → 精确点名判红。

### [实作] S3 —— Broker 骨架(按裁定:进程内 · 进 gateway)

单写者 + 世代号 + 只读快照 + 1 Hz 采样。**本片只读**,变更端点属 S4。
四条结构性质逐条红测:世代号与状态**同一把锁下**一起改 · 采样器死掉**必须看得见** ·
**锁内不得跨 await 网络 I/O**(本进程同时持 300s 流式聊天连接)·
**反向全表:GPU 面此刻只能有 `GET`**(S4 加变更端点时要连这条断言一起改 —— 那是一次有意的语义变更)。

★ 非 AI 显存占用标注为**推算**且**不可点名**:本机实测 `nvidia-smi --query-compute-apps`
对全部进程的 `used_memory` **都是 `[N/A]`**(WDDM 不暴露逐进程显存)。**这是结构性限制,不是没做完。**

### [决策 · 实作] S4a —— 三集合动态闸,堵死双批竞态

判据关键是**哪些显存已经被 NVML `free` 反映了**:`loaded` 已扣过(再减 = 重复计)·
`reserved` 还没占(不减 = 别人会把这块"空闲"再批一次)· `incoming` 必须减。
⇒ `free − incoming − Σpeak(reserved \ loaded \ 本次请求集) ≥ safety_margin`

★★ 承重的是那个**双重差集**。红测把**危害本身**演示了出来:塌成一个集合后,
「free=9.0 · 别人预留 speech.full 4.05 · 我要 8b@16k 5.92」这一局闸判**通过** ——
**两个客户端双双获批**,正是 D37 ④ 要关的竞态,**从关它的那个参数里重新打开**。

拒绝文案分处境:**被别人的预留挡住 ≠ 被桌面占用挡住**(前者要等/协商,后者要关程序)。

### [决策 · 实作] S4b —— 租约账本

★ **kind 是两根正交的轴**,不是一套枚举:§8.1.7 问「能不能被驱逐」(生命周期)、
§8.1.6 问「有没有人在等」(阻塞性)—— 本来就是不同的问题,合成一套必然丢信息。
断言里留了一条:**若将来两轴变成一一对应,它会变红**,提示那时重新裁定是否合并。

★★ 新增 kind 默认**不可驱逐**(今天只有 `model_ref` 可驱逐,集合相等钉住)·
**惰性过期,不设收割线程**(收割线程 = 第二个写者 = 双持有从侧门回来;断言直接数 `create_task` 次数)·
**时间纪律**(任何时间值不得在拿到锁之前捕获、不得跨两条租约复用 —— `clock_timestamp` 的进程内等价物)·
**续租失败原因可区分**(`NOT_HOLDER` 立刻自隐别重试 / `EXPIRED` 可重申;混成一个 `False`
调用方只能猜,而**猜错的方向是重试 = 双持有**)。

★ **`/v1/session/end` 终于存在**:客户端 `HubClient.cs:230` 每次退出都调它,
而网关里此前**没有这条路由**、失败还被吞掉 —— 一次**伪装成成功的静默失败**,
也是当天「关闭卡一段时间」的一部分成因。释放走**条件写**(带 `fence_token`),
回话说清释放了几条,不是一律 200 空体让调用方无从分辨。

### [口径]

**全仓基线 2111 → 2283 PASS / 0 FAIL**(`run-tests.ps1 -Full`)。
fast 层 **251 → 461**;gpu-broker 38 → 96;gateway 新增 `test_gpu_broker.py` 105 条 +
`test_gpu_tool_isolation.py` 8 条。

### [待裁](S2 / S5 / S6 各卡一条,不适合在长会话末尾开头)

词表桥归属方向 · A7 显示配置(**不可回填**)· `desktop_floor` 统计量 · 驱动口径 ·
`unregistered-local` 算不算出境 sink(D81)。

---

## 2026-08-04 晚 · P4 GPU 侧做完:S2 · S5 · S6 · S7 · S8(`9011b01`…`d4ce28f`)

> 承接上一节「P4 开工五片」。本节把 GPU 侧那一半做完。**客户端半边未动**(见文末)。

### [实作] S2 —— 词表桥:别名 ↔ 显存组件

网关的别名(`chat.default` 之类)与显存组件 id(`llm.assistant.8b@16k`)**此前零交集** ——
网关里连 `component` 这个概念都没有。桥放网关侧,`registry.toml` 每个别名**必填**
`components_any_of`;零 GPU 的别名**必填** `no_gpu_reason`(不给"留空 = 不需要显存"这条默认路)。
五条 fail-closed + 反向全表:新增别名不填字段 ⇒ 网关**起不来**,不是静默按零显存处理。

★ 顺带暴露两个从没被任何表登记过的别名(`speech.lite` / `speech.full`),
现进 `[unaliased]` 段 —— **它们本来就在系统里,只是没人登记过**。

### [实作] S5 —— 推送非轮询 + 世代号 409

D37 ② 明写「推送非轮询」。`GET /v1/gpu/events`(SSE)+ `POST /v1/gpu/lease`(带
`if_generation`,不匹配回 **409 + 全量快照**,不是空 body 让调用方猜)。

★ **开工前先实测了一件本来要靠猜的事**(`cbdc124`):lan-edge 代理**到底能不能流式直通**。
给上游加了个 `/__sse_probe` 桩,量到分片间隔 **411ms** —— 能直通,**不必退回轮询**。
若不测就得按最坏情况设计,那会白白多一层轮询兜底。

### [实作] S6 —— 硬件指纹 fail-closed(GPU 半边)+ B10④ 预算诊断

启动期比对 GPU 指纹,不一致 → **拒绝**,并说清**要重测什么**(GPU 指纹不符 ⇒ 重测组件 peak 里
与硬件相关的部分;显示指纹不符 ⇒ 重测 `desktop_floor` —— 两者可移植性不同,驱动不同的重测项)。

★★ **显示半边明写未标定,并拒绝回填。** `gpu_uuid` / `vbios` 是**当天补录**的,
只能检测「08-04 之后」的换卡;07-27~08-04 之间若换过同型号卡,我们不知道,**而且不假装知道**。
`display_calibrated = false` 写死并附理由:把今天的配置追认为当时的测量条件,
是一次**伪装成"补录"的捏造** —— 而 `display_note` 的字面意思恰恰会把人引向它。

### [实作] S7 —— 四个集合 + I2/I3/I4 + RECONCILE_WATCH

`RECONCILE_WATCH` / `intended_resident` / `committed_resident` / `actual_resident` /
`permitted_on_demand` 六个词此前在**全仓各 0 命中** —— 而方案书自己写着
「没有检测器的不变式等于没有」「没有任何机制能发现 3 个装了 2 个却仍报 READY」。

★★ **I2 必须保留【蕴含】形态**(`state == READY ⟹ actual == committed`)。
`DEGRADED_SAFE` 不是 `READY` ⇒ 前件为假 ⇒ I2 自动成立。
写成双条件会造出「**永久违反、告警无法消解**」的状态,进而逼系统去自动改写锚点 ——
而那恰好违反 D10 存活下来的「不做自动触发」。
★★ **I3 无状态前件**,任何状态下恒成立 —— 它是准入白名单的**运行期**版本(白名单只在申请那一刻把关)。
★★ **I4 电源轴与意图轴分离**:`set_power` 绝不触碰 `intended`,否则一次关电就吞掉用户的勾选。
★★ `RECONCILE_WATCH` **只报告不修复** —— 修复即"自动触发"。

★★★ **最要紧的不是代码,是一条诚实标注**:`actual_resident` 今天**不是独立观测**。
两条结构性原因 —— ① 装载器还不存在(没有"谁真的把模型装进去了"这个事实源);
② WDDM 不暴露逐进程显存。⇒ `actual` 只能是 Broker 自己的账本,
**用自己的账本跟自己的账本比,永远相等**。故 I2/I3 的 `confidence` 标 `self_reported`。
**不标就是个假检测器。**

### [实作] S8 —— 「确定 = 一次事务」+ 状态机白名单 + 失败落点

★★★ 本片最要紧的是**拒绝写出一个假事务**:装载器不存在(那是 P5)。最容易掉的坑是给
loader 一个空实现 —— 于是每次事务都"成功",状态机一路走到 READY,四个集合全部相等,
I2 永远绿,**而显存里一个字节都没有**。那是「3 个装了 2 个却仍报 READY」的更坏版本。
⇒ `_loader is None` 时事务在**预检阶段**失败关闭,落 `loader_absent`,**绝不到达 READY**;
断言还钉住这个检查在**进 APPLYING 之前** —— 否则就成了先把旧的卸了、再发现装不上。

事务四条各有断言:① 点确定**重新求值**(测试直接构造「预览过 → 确定不过」这一幕)·
② 预检不过**一个组件都没卸**(源码级钉住 `_back_to_staging` 路径上没有一行写 `_committed`)·
③ 装载失败回滚,回滚失败 → `DEGRADED_SAFE` · ④ 有任务在跑先给 5 秒排空窗口再交还用户裁定
(主语已按 D25 从「降档」改为「变更驻留集合」)。

**状态机是白名单**:未登记的转换拒绝,未登记的**源状态**也拒绝;非法转换**抛异常**而非静默忽略。
`DEGRADED_SAFE` 是终态,唯一出口是显式电源循环(人的动作)。
`RECONCILING` **必须继续服务** —— 否则一个语音 worker 掉线会连带把仍可用的 LLM 也判死。

**失败落点**(D24 排查带出,原文完全没定义过):失败恒落 AI 侧,`never == "desktop"`,
**但 `guarantee` 恒为 `False`** —— WDDM 不按优先级驱逐,已发生的驱逐落在谁头上本项目控制不了。
**这是策略,不是保证**,UI 文案不得据此声称"保证桌面不被挤"。

`admission_guard` 全程**不看进程名**(原文「检测独占全屏游戏」特例已删 ——
这台机器上比游戏更常见的显存大户是 UE5 与多标签 Chrome),断言直接扫源码不得出现
`process`/`exe`/`fullscreen`/`game`。

### [★ 发现] 出包门禁拦下一条与当次改动无关的红 —— 它本身就是会漂的

提交 S8 时门禁判红,红的却是 `test_vram_gate` 第 13 节:那条"通过路径"**直接把 CLI 打到实机 NVML**。
当晚 20:1x 桌面吃到 7.5 GiB,闸如实拒绝 → 退出码 1 → 断言无辜变红。
**这条从写下那天起就是会漂的**,只是此前显存一直够。

更刺眼的是:该文件**开头第 4 行**自己就写着「动态闸统一注入固定的 free 值,不依赖真实 GPU 状态
(否则测试结果随桌面占用漂移)」—— **第 13 节违反的是它自己声明的纪律**。

★ 真实代价不是"偶尔红一下",是**训练人去用 `--no-verify`** —— 而本项目已被同款模式咬过一次
(D24:`-Force` 一路跳过三道闸)。修法:通过路径改走跨进程引导,在子进程里注入 free 再调 `main()`。
**不给 CLI 加 `--free` 之类旁路开关** —— 那种开关迟早被用来绕闸。
★ 中途走过一条弯路已写进注释:PATH 里放桩 `nvidia-smi.cmd` **不成立**,
Windows 的 `CreateProcess` **不执行 `.cmd`**,`nvidia-smi` 仍解析到 System32 的真 exe。
★ 代价说清:这条不再经过 `if __name__ == "__main__"` 那一行 —— 编码修复本身在 `main()` 里
仍被完整执行,而真正的 `__main__` 入口由【拒绝路径】与【无参数列表模式】两条覆盖,
那两条的判定与 free 无关,本来就是确定性的。并补**元断言**:输出里的 free 必须正好是注入值。

### [★ 修好一条已在假绿边缘的老断言]

`test_gpu_broker` 第 8 节「锁内不得跨 await I/O」的正则
`async with self._lock:(.*?)(?=\n    def )` 一路捕到下一个方法定义。
S8 之前每个锁块**恰好是方法的最后一段**所以看着对;`apply_intended` 里锁块后面还有
【缩进已退回去】的代码,被误算进锁内 → 三条全红。修法是**收紧**判据(只取缩进更深的行)。
并补**元断言**:提取到的块数必须等于源码里 `async with` 的出现次数 ——
提取器一旦匹配不上,那个 `for` 一次都不跑,三条检查会**静默消失**。

### [过程事故 · 记下防复发]

红测 S8 时在复合命令里用了 `git checkout -- test_gpu_broker.py` 撤销临时改动,
把**未提交的 250 行 S8 测试**一并冲掉,已按上下文重建并复绿。
**今天第二次**被破坏性 git 命令咬(前一次是 `git reset --hard`,冲掉封存的自配对文件)。
⇒ 规矩:临时改动一律用**文件备份**还原,**绝不在有未提交内容的文件上用 `git checkout` / `reset`**。

### [报告 · Artifact]

进度总览已按用户要求标注:**Hermes Agent 与 Gemma 4 各自落在哪一站**,
并给每个部分加了**大白话收益条**(「这部分完成了可以获得什么」)。同一 URL 原地更新:
https://claude.ai/code/artifact/773858f7-59a2-418a-abaf-8ab7e4f626b6

### [口径]

**全仓基线 2283 → 2531 PASS / 0 FAIL**(`run-tests.ps1 -Full`)。
fast 层 **461 → 709**;`test_gpu_broker.py` 105 → **260**;`test_vram_gate.py` 96 → **133**;
新增 `test_component_bridge.py` 56 条。
★ 跑 `-Full` 需 `-ExecutionPolicy Bypass`(本机默认策略禁脚本;pre-commit 钩子内部已带)。

### [未做 · P4 的另一半]

**客户端侧一片没动:**
1. **组件挑选面板不存在** —— S8 的 `STAGING` 编辑态在 UI 上没有落点;
2. **显存条读的是本机显卡**(`nvmlDeviceGetHandleByIndex(0)`)——
   副机上显示的是**副机自己的卡**,标签却只写「显存」,会让人以为看的是中枢的。

### [仍待裁]

`desktop_floor` 统计量(P99 vs max)· 驱动口径 · `unregistered-local` 算不算出境 sink(D81)。

---

## 2026-08-04 深夜 · P4 客户端半边:显存条改读中枢 · 组件挑选面板(`a777693` · `0abd94d`)

> P4 的 GPU 侧十片已于当晚早些时候做完。本节把**客户端那一半**补上,P4 两半齐了。

### [★ 发现] 显存条在副机上显示的是【另一台机器的显卡】

`VramMonitor` 无条件直调本机 nvml 的 `index 0`。主机上碰巧没错(本机就是中枢);
**副机上它显示副机自己那张卡**,而标签只写「显存」—— 用户会以为看的是中枢那 8.52 GiB 预算。

★★ 最要命的不是读错,是**看不出来**:两台机器的 "8.3 GiB" 长得一模一样,
界面上没有任何地方能让人发现自己看错了。

修法(D84 裁定一):数据源做成**显式三态** ——
`Hub` 标题「中枢显存」/ `LocalFallback` 标题「**本机显卡(不是中枢的)**」/ `None` 整条隐藏。
★ **退回本机不是"降级到差一点的同类数据",是【换了被测对象】** ——
所以它不能只是精度差一点,必须在界面上改名字。
★ 中枢连着但它自己那一轮没采到 ⇒ **不退回本机**:
那会把"中枢采样器坏了"显示成"一切正常",两种情况必须长得不一样。

### [实作] 订阅而不是轮询(D37 ②)

新增 `Services/HubGpu.cs`,走 `GET /v1/gpu/events`(SSE)。
★ **心跳是判据的一部分**:没有它,一条【死掉】的长连接与"一直没变化"在客户端看来一模一样。
故记 `LastFrameAt`,超过 40 秒没有任何帧(服务端心跳 15 秒)即判 `Live=false`。
★ `Transport.OpenStream` 必须用 `HttpCompletionOption.ResponseHeadersRead` ——
默认那个会等【无限长】的响应体读完 = 永远挂住,**症状很像"服务端没发",会把人引去查服务端**。

### [★ 发现] 客户端里躺着【第三套词汇】

`Views/ModelCatalog.cs` 的 `chat.8b` / `chat.8b.long` / `chat.30b` / `speech` / `vlm` / `image`
跟网关别名(`chat.default`…)对不上,跟显存组件 id(`llm.assistant.8b@16k`…)也对不上,
**谁也映射不到谁**。勾了不会发生任何事,而界面看着像配置好了。
它自己的注释写着「接入 GPU Broker(P4)后以中枢下发的清单为准替换这份占位」—— 现在就是那个"接入后"。

⇒ 服务端补 `GET /v1/gpu/components`:**目录 = 准入白名单本身**,不是它的摘抄。
★ 反向全表逐条列出,**不做任何过滤** —— 不藏"装不下的":
藏一个的后果不是"界面少一项",是【用户看不见但闸仍然会算它】,
于是"我明明没勾它"和"闸说装不下"同时成立而无从对账。
★ 连同 `AppSettings.IsModelEnabled/SetModelEnabled` 一并删 —— 那是个**没人读**的偏好,
用户拨了以为生效、实际什么都没发生,标准的假开关。**不是注释掉留着**,留着它就是它回来的方式。

### [实作] 组件挑选面板 —— S8 的事务语义终于有了界面落点

`Views/ComponentPicker.cs`。★ 取不到清单时**什么都不列**(列本地兜底 = 把自造清单当成中枢的真实清单)。
★ 两种撞墙**分开说**(§8.1):撞预算 ⇒ 改桌面预留**有用**;撞此刻可用 ⇒ 改预留**没用**,得关程序。
★ 提交用推送流里【当前】那个世代号,不是面板加载时那个旧号(挑选要几十秒)。
★★ 七种失败七种说法。**今天点确定必然得到 `loader_absent`**(中枢还没有装载器),
面板如实说「**没有生效**」,不假装模型装上了。

### [★ 发现] Broker 从来没离开过 STARTING

服务端补 `finish_startup()`。此前**没有任何代码**做这个转换,而 `apply_intended` 只接受 READY
⇒ 整条事务路径**从来走不到**,实测恒返回 `busy`。
放行条件**就是 I2 的后件**(`actual == committed`)—— 不给"启动阶段先放过"开例外。

★★★ 但必须写明:**该判据今天是恒真式**。`actual_resident` 就是 `_committed` 本身
(S7 已如实标注),于是它等价于 `committed == committed`。
⇒ 它**今天不是检测器,是为 P5 预留的形状**。断言直接钉住这条恒真性 ——
P5 让 `actual` 变成独立观测的那天它会红,提醒回来把它当真检测器复核。

### [★ 发现] S8 的重试路径整条是断的

预检不过的落点是 `STAGING`,而 `apply_intended` 原来只收 `READY`
⇒ 用户改完选择再点一次确定,拿到的是 `busy`。
**S8 的测试每个 broker 只跑一次事务,恰好把它盖住了** ——
不是断言写错,是【根本没构造第二次】,典型的**测试形状盲区**。
修法:`ACCEPTS_TRANSACTION = {READY, STAGING}`,并补连续三次事务的用例。

### [★★★ 用户裁定] 同一个陷阱踩满三次 ⇒ 必须落文档 + 护栏(D85)

用户当晚指出:**同一个陷阱踩了三次以上就要记下,防止以后再踩。**
触发它的是实况 ——「断言撞在**解释它已经被删了**的那句注释上」当天踩了 **6 次**,
而我每次都只是就地收紧那一条判据继续走,**没有留下任何痕迹**。

新增 [ASSERTION-PITFALLS.md](../ASSERTION-PITFALLS.md)(`WPF-PITFALLS.md` 的姊妹篇,同样格式)。
七条,每条标**已踩次数** —— 次数是判断要不要装护栏的依据。

护栏三层,均已红测:
· Python 侧 `10-core/gateway/assert_helpers.py`(`code_only` / `lock_bodies` / `assignments_to`);
· C# 侧 `Selftest.CodeOnly()`(同时剥注释**与字符串字面量**);
· ★ **反向全表断言**「没有测试文件自己再写一份去注释器」——
  **它当场抓到一份漏网的**:`test_error_disclosure.py` 自带一份,
  而那份实现的 docstring 里就写着「★ 必须这么做」。**它自己就是第 6 次。**

★ 顺手堵掉一个 fail-open:裸 `re.sub(r"#.*")` 会把 `detail = "a #b backend"` 砍成 `detail = "a `,
让「不得出现 backend」**因为砍掉了真违规而变绿**。
改成逐字符判断,`#` 在字符串里不算注释 —— **去注释的方向天然 fail-open,宁可少去一点,不能多去。**

★ 根因值得单记:**一段负责任的删除必然会在注释里提到被删的东西** ——
不提,后人就不知道这里为什么空着、以及不许再加回来。**越写得清楚越容易弄红反向断言**,
所以这个坑不会自己消失,只能靠工具。

### [口径]

**全仓基线 2531 → 2601 PASS / 0 FAIL**。客户端自检 **1728 → 1755**;
`test_gpu_broker.py` 260 → **302**;fast 层 709 → 752。

★ 记一条当天踩了两次的量法坑(已进 ASSERTION-PITFALLS 第 3 条):
客户端自检在 **Debug**(`net9.0-windows`)下跑出 1471 条、在 CI 用的
**Release/win-x64**(`net9.0-windows10.0.19041.0`)下跑出 1755 条 —— **差 284 条**。
拿 Debug 的结果去对 CI 的基线会得出完全错误的结论。
**判据:断言总数变了而你没增删断言 ⇒ 一定是跑错产物或漏跑段落,先查这个,别去查结果。**

### [P4 收口]

GPU 侧十片(S0–S8)+ 客户端两件(S9)落地。
仍待裁(不挡后续):`desktop_floor` 统计量(P99 vs max)· 驱动口径 ·
`unregistered-local` 算不算出境 sink(D81)。

> ### ★★ 更正(2026-08-05 凌晨,同一晚写下、当晚查出)
>
> 上面这段原本写的是「**P4 主体完成**」+「真正的下一站是 **P5 装载器**」。**两处都错**:
>
> **① P4 没有主体完成。** 清单(方案书 §14 行 2287)上还有一条明确条目:
> **「★ 权限档位六元组(从 P2 移来)」—— 而它一维都没做。**
> §6.2 原话:**「六元组里如果只实现了前四维,等于没有实现」**——
> 缺的恰好是它点名最关键的**参数维**:
> 同一个工具,**参数决定它是安全还是灾难** ——
> `POST /v1/gpu/intended` 的 `components` 传空数组就是**卸掉全部**;
> `ttl_s` 传个极大值就是**永不过期的租约**。另外**额度维**也没有:
> 今天任何档位都能无限次发起变更与租约。
>
> **② P5 是「语音 v1(半双工,常驻)」**(方案书行 2306),不是装载器。
> **「装载器」这个词在方案书里一次都没出现** —— 是我当晚临时造的说法。
> 按需装载实际隐含在 D37 四件套的「**预留先于装载**」里,**属于 P4**;
> 后端今天由 `start-stack.ps1` 静态启动。
>
> ★ 教训与 ASSERTION-PITFALLS 第 3 条同源:**给一件事起了个方案书里没有的名字,
> 就等于把它从任何清单里摘了出来** —— 之后没有任何反向全表能发现它没被登记。
> 下次给阶段/条目命名时,先 `grep` 一次方案书。

---

## 2026-08-05 凌晨 · 模型接入 + 内网同步(`d5fe246` … `36bbea4`)

> 承上一节。这一夜做完两件大的:**模型第一次真的能对话**,**共享数据第一次真的会同步**。

### [★★★ 里程碑] 模型接入(S11)—— 这个项目第一次能被说话

开工前把整条链实测了一遍,结论是**断的只有客户端一环**:
模型文件 ✅ · llama-server.exe ✅ · start-stack 会起 18081 ✅ ·
网关 `/v1/chat/completions` ✅(流式路径**已经**堵掉「200 + 空 body」静默降级)·
lan-edge 8443 ✅ · mTLS 两台真机测过 ✅ —— 而 `ChatCenter.Send` 根本不调那条路由。

**端到端实测(经网关)**:`text/event-stream` · `X-LocalAI-Contract: 8b` ·
**首包 2912ms(冷)/ 约 1s(热)· 276 帧 · 总 3.3s**。
用户实机确认:「字是逐字的,首包很快,1秒左右」。

三条硬规矩:**任何失败路径都不写 Assistant 消息**(一个字都没收到时删掉那条占位空回复)·
**半截保留 + 说明没说完**(那是模型真说过的)· **七种失败七种下一步**
(「后端没起」直接告诉你跑哪个脚本)。

**token 预算截断**:客户端没有分词器 ⇒ 全是估算,所以**估高不估低**
(估低撞窗口、用户只看到"它忘了前面";估高只是少带两条 —— **两个方向代价不对称**)。
窗口推不出来取**最小**那一档并标 isGuess。截断了**在会话里明说**「本轮带了 N / 共 M 条」。

### [★★★ 实测发现] 模型第二轮就在编

用户实机对话第二轮:
> 用户:你记得我是谁吗
> 助手:**当然记得啦!我们之前聊过天气、日常趣事,还有你对未来的期待呢~**

**那段"之前"根本不存在。** 会话就是从「你好」开始的,发上去的历史是 0 条、2 条、4 条。
用户打了一句「不允许撒谎」它才改口。

**根因:全链路一句提示词都没有** —— 模型落回自带聊天人设,那种人设的默认行为就是营造熟稔感。
⇒ 加**行为底线提示词**(S12),注入在**中枢**而不是客户端:
客户端可以漏发、可以被改,将来还有桌宠/Agent/同传好几个调用方 ——
**一条"靠每个调用方自觉带上"的底线不是底线**。与「权限按档位挂载而非运行时判断」同一条纪律。

修后同一段对话:「这次对话里没有这些信息。」「不能。我无法访问互联网或外部信息。」
★ 底线**只写行为,不写人设** —— 混在一句里的后果是:调人设时会顺手把底线一起改掉。

### [★★ 实测反馈] 两条"不是 bug,是界面在说会误导人的话"

「副机提升到共享,主机看不见」「共享家庭待办对方看不到」——
核实:中枢**根本没有存放共享会话的地方**(逐条列全部路由确认);待办是**纯本机**(D57 用户自裁)。

★★ 共同根因:**系统是一件一件接入的,而那些说法都用了笼统主语**。
模型接进来那一刻,「AI 未接入」「中枢尚未接入」**同时失信** ——
用户没法判断哪句还算数,于是推断"中枢好了,共享应该也好了"。

⇒ 修法:**指名道姓 + 把说明摆在期望形成的地方**
(待办那句从归档页挪到**可见范围下拉旁边**)。

### [实作] S13 内网同步(D86)—— 两半都通了

**D52 写着「★ 真正的上传/同步要等中枢接入(P4+)」**,中枢接入了,这半边到期。
★ 不是推翻 D57:那四条路全是**外部服务**,而**中枢不是外部源**。

用户裁定三条,逐条实测:
① **只同步家庭/共享的** —— 判据在**服务端**(客户端可能有 bug,而"把私人东西推到另一台机器"
   **不可撤销**)。实测一批 4 条:家庭待办/共享会话/消息全收,**个人待办被拒**并说得出理由。
② **实时** —— SSE,真 HTTP 实测 push → 收到 update **24 ms**。
③ **后到的赢 + 保留被覆盖的** —— 绝不静默丢;界面提示「这条被另一台改过」。

★ **未同步必须看得见**:主机不在线时本地照常改,但状态行明说
「★ 有 N 项还没同步到中枢 —— 别的设备现在看不到」。一切正常时**整行不出现**。
★ **推不上去不丢**:网络失败不出队,连上后自动补推。

### [★★★ 这一夜最值得记的一条:断言也会说假话]

同一类问题当晚出现**五次**:
① 输入框「AI 模型尚未接入」—— 模型接进来后**直接变假**;
② 共享框「中枢尚未接入」—— 字面还真,但用户刚跟中枢聊完,**读起来是假的**;
③ STATE 的「下一步」—— 错了四次;
④ 同步做出来后,「会话同步还没有做」「待办只存在这台电脑上」当场变假(**两处**,
   我第一次只改了一处,底部还留着一句);
⑤ ★★★ **过期的是断言本身** —— 我几小时前写的四条断言在守「会话同步还没有做」这句话在场,
   而同一晚把同步做出来之后,**守住那句话就等于守住一句谎言**。

> **⇒ 每做成一件事,要回头查的不只是文案,还有【断言】。**
> **笼统的说法失信最快,但指名道姓的也会过期 —— 区别只是晚一点。**

### [★ 判据自己的坑,四条]

· `Slice` 终点用 `}` —— 撞在 `t with { … }` 自带的 `}` 上,切片太短 ⇒ 代码明明推了却判红;
· 另一个终点标记写的是 **doc 注释里**的字样,而切的是 `CodeOnly` **之后**的源码(注释已剥掉)⇒ 标记不存在;
· 「未同步要说出来」第一版写成 `Contains("看不到") || Contains("同步")` ——
  红测把关键分支整个去掉,回退文案「正在同步 N 项…」含「同步」照样过。**一条放宽到抓不住东西的断言**;
· ★★ 「推不上去不丢」这条**当时根本没被测到**:注入「catch 里清空队列」**一条断言都没红**
  (没有 hub 时 FlushAsync 在进 try 之前就 return 了)。已补源码级断言。

### [★★ ASSERTION-PITFALLS 新增两条]

**第 5 条第 2 次(尤其刺眼)**:`test_gateway_e1` 里 11 条断言写成 `status_code == 503`,
文件 docstring 明写「**无后端时** chat 转发会 ConnectError → 503,正好证明 E1 放行了」。
**模型第一次真的接进来之后,这些 503 全变成 200,两个套件当场红/崩。**
> 第 1 次(显存闸)是环境有噪声;**这一次是"功能终于能用了"弄红的**。
> **它整天是绿的,恰恰因为产品还不能用。**
> ⇒ 判据:**一条断言若会因为「功能终于能用了」而变红,它测的就不是它自称在测的东西。**
自查办法:**把栈起起来再跑一次全套** —— 这类断言那一刻会全部现形。

**第 6 条(新)**:用 TestClient 测"跨连接的实时推送" —— 每个 TestClient 各起一个事件循环,
`asyncio.Event` **跨循环叫不醒**,SSE 收不到 update。我差点据此认为**推送设计有问题**。
> **测法制造的假象比测不出问题更危险:它会诱你去改一段本来正确的代码。**
⇒ 凡判据涉及**两条连接之间的实时交互**,必须走**真 HTTP**。

### [口径]

**全仓基线 2601 → 2833 PASS / 0 FAIL**。客户端自检 1755 → **1844**;
新增 `test_sync.py` 61 · `test_gpu_policy.py` 77。
出包版本戳 `20260805-0120+36bbea4`(**不带 .dirty**,工作区干净),已部署到 `dist/client`。

### [仍待裁 / 未做]

· **家庭→个人降级**时中枢那份**不删**(删了会让另一台上的条目凭空消失),已记在
  `TodoCenter.DowngradedWhileShared` 里 —— **待裁**,不假装处理好了;
· 冲突提示只到"记下了"这一步,**界面上还没显示**「这条被另一台改过」;
· 两台机器的**实机**同步验证还没做(本夜是单机模拟副机推送);
· `desktop_floor` 统计量 · 驱动口径 · `unregistered-local` 算不算出境 sink(D81)。

---

## 2026-08-05 · 调试工具箱 + 全项目审计(P4-S15)

> 用户要求:「做好 debug 测试组件或环境或工具(特别是模型切换和切换条件检验,主副机同步检验,
> 还有什么 Debug 重要的你自己开发开发,后期产品化的时候可以一键移除)。做好之后跑一遍全项目审计,
> 能顺手修的修了,修完核验,更新文档和 artifact。」
> 追加:「debug 工具一定要稳定简单方便,不然到时候 debug 工具出问题了排查会很麻烦。」

### [实作] 调试工具箱 `90-ops/debug/`(5 个工具,可整目录删除)

| 工具 | 做什么 |
|---|---|
| `doctor.py` | 11 环链路体检,只读;逐环报 up/down **和为什么** |
| `probe_switch.py` | 模型切换实测:逐组件装/卸、量耗时与显存、验三道闸与不变式。**默认只读**,`--write` 才动进程 |
| `probe_sync.py` | 主副机同步实测:范围/冲突/实时性。记录一律带 `__probe_` 前缀,**绝不碰用户真数据** |
| `scan_fake.py` | 假防护扫描器:6 类扫描,专治「看着有防护、实际没有」 |
| `selfcheck.py` | **工具自己的自检**,不需要系统在跑 —— 直接对着用户那句担心写的 |

**三条稳定性纪律**(用户那句话的落法):
① **零项目依赖** —— 工具不 import 项目代码,项目坏了的时候工具还得能跑,那正是最需要它的时刻;
② **逐项隔离** —— 一个扫描挂了其余照跑,否则一处笔误就让工具哑掉,而"哑掉"和"没发现问题"在终端上长得一样;
③ **退出码 1 = 系统坏了 / 2 = 工具坏了** —— 混成一个,你就分不清该修哪个。

★ `selfcheck.py` 的 40 条断言里有 13 条是**反过来钉识别器**的:
喂 `os.replace(a,b)` 必须报、喂 `str(p).replace(a,b)` 必须不报;
散文里的匹配必须排除、真代码里的必须仍然抓到。
**只钉一边等于没钉** —— 只验"能报"挡不住误报泛滥,只验"不报"挡不住漏报。

### [实测] 全项目审计 —— 8 处真问题,全部当场修掉并钉了断言

**① ★★★ 门禁的客户端半边一直在跑旧产物**(P4 全程存在的假绿)
`run-tests.ps1 -Full` 直接执行已有的 exe,从不重编。改了 `Selftest.cs`、净增 3 条断言,
门禁照报 `PASS=1852 FAIL=0` —— 与改动前**一模一样**。
⇒ 改坏客户端源码但不出包,门禁会一直是绿的。
**护栏**:跑之前比时间戳,源码比产物新就判「没跑起来」并**拒绝执行**。
★ 关键是**不跑**而不是"跑了再警告" —— 跑出来的那个绿数字比不跑更有害。
两个方向都实测:产物新 ⇒ 照常跑;把 exe 时间戳回拨 3 天 ⇒ 门禁拒绝并判未过。

**② ★★★ `loader_absent` 把一个生产故障说成了预期行为**
装载器接不上时,网关的 `except` 是个光秃秃的 `pass`,**原因一个字都没留下**;
而失败消息写着「装载器尚未实现(P5)」—— 两处都是假话(S14 就实现了;P5 是语音 v1)。
运维照这条消息去查,得出的结论是"意料之中,等下个阶段",而真相是**接线断了**。
⇒ 快照加 `loader_present` / `loader_error`(与 `sampler_error` 同款处理),
消息分成"接线失败:{原因}"和"这台实例有意没接"两种,并**断言两者不许长成同一句话**。

**③ ★★★ 守卫只盖住了它自称范围的一部分(一次抓出 4 处)**
「模型已接入,那句『AI 模型尚未接入』必须删掉」——判据只读 `Views/ChatView.cs`,
而那句话正躺在 `Services/ChatCenter.cs`;
「不得再用笼统的『中枢尚未接入』」——同样只读 ChatView,而它躺在 `Views/ProjectUi.cs`。
判词说"全仓/每一个",判据是手边那个文件。**它在你搬动代码的那天才开始说谎,而那天没有任何东西会红。**
⇒ 改成 `TryReadAllSources()` 全仓扫描 + 元断言"确实读到 ≥20 个文件";
面板操作名单改成 AST 导出(手写 7 个,实际 11 个);`_gpu_fns` 与 `app.routes` 对拍。

**④ ★★★ 一条为了修谎言而写的断言,自己是空的**
`Assert(!CodeOnly(cv3).Contains("AI 模型尚未接入"), …)` —— **恒真**。
`CodeOnly` 把每个字符串字面量整个换成 `""`,而界面文案只可能出现在字符串里。
★ 它的来历最该记住:动机对、措辞诚恳、上面还留着一整段"⇒ 反过来钉"的说明,**而修法是空的**。
⇒ 新的全仓守卫用 `NoComments`,**第一次跑就把这行的针抓了出来**。

**⑤ 界面上 8 处「未接入」在说假话** —— 不是"这件事没做",而是**把原因说错了**:
`ChatCenter` 说「AI 模型尚未接入(P4 GPU Broker)」,真相是**这台还没配对中枢**(去配对,三步的事);
`ProjectUi` 说「中枢尚未接入」,真相是中枢已在同步会话与家庭待办,只是项目共享还没进同步范围;
`StorageView` 说「模型还没接入」,真相是**摘要这条链路**还没接上模型。
★ 给错原因的提示比不给提示更坏 —— 它把人引向错的方向。

**⑥ 恒真断言 2 条(记忆库套件)**:`check("测试数据已清", True)` / `check("满载夹具已种", True)`。
后者尤其坏:夹具一条都没种进去它也绿,而后面整套恢复演练全建立在"种进去了"之上 ——
一次什么都没验证过的恢复演练,比不做演练更坏。⇒ 改成真的去数。

**⑦ 「一键移除」这条承诺从来没被检查过**
README 写着由 `10-core/gateway/test_debug_removable.py` 检查 —— **那个文件从来没存在过**;
真正在检查的 `selfcheck.py` 因为不叫 `test_*.py`,门禁扫描器根本不收它。
一份自称"专治看着有防护、实际没有"的工具箱,自己就栽在同一个形状上。
⇒ `run-tests.ps1` 条件执行 `selfcheck.py`(存在才跑,删了自动跳过),承诺进了门禁。
★ 为什么不在 `10-core` 下建那个测试:那条判据要扫"生产代码里不得出现 `90-ops/debug`",
而测试文件本身必须写出这个路径 ⇒ **它会绊倒自己**。守卫必须待在它所检查的范围之外。

**⑧ 扫描器自己的 4 处误报**(全部当场收紧,并**两边都钉**):
注释里引用的旧断言、docstring 里的说明、`str.replace` 当成 `os.replace`、
把"整集合逐字相等"这种**更强**的元断言当成缺失。
★ 判据:**噪声太大的扫描器没人看,等于没有**(与 flaky 测试训练人用 `--no-verify` 同一个失败模式)。
嫌疑数 229 → 149 → **113**,且每一次收紧都配了反向断言,防止它往"越来越干净"的方向瞎掉。

**⑨ ★★ 扫描器自己有一条静默的零断言 —— 由 pre-commit 钩子在提交那一刻照出来**
`scan_fake.py` 里那条「硬编码绝对路径」判据写死的盘符是 `D`,
而**这个项目在 E 盘** ⇒ 它**在本仓永远匹配不到东西**。
起因是钩子拒绝了提交:它扫「代码里不得出现绝对路径」,而它当然分不清
"一条硬编码路径"和"一条用来抓硬编码路径的正则"——
**同款形状今天已经踩了好几次**(守卫撞在描述它所守之物的那段文本上)。
⇒ 盘符改成拼出来的 `[A-Za-z]` + `:` + 两种斜杠,**任意盘符都算**;
并两边都验过:`E:\` / `C:/` / `d:/` 必须抓到,`https://` / `a/b` / `12:30` 必须不报。
★ 这条是今天第 4 个"守卫自己坏了"的实例,而且是**唯一一个不是我发现的** ——
是钩子发现的。**护栏确实会在你自己看不见的地方替你挡一下。**

### [口径]

调试工具箱 `selfcheck.py` **40 PASS · 0 FAIL**;假防护扫描 **113 条嫌疑**(全部人工过了一遍)。
全仓基线见 STATE(纪律:那个数只在 STATE 那一行出现)。

### [仍待裁 / 未做]

· `mark_confidential_episode` **零调用点、零测试** —— 它自己的注释点名说漏了向量迁移就是 §4.11.4 破防。
  写它的测试要连真实记忆库(psycopg 未安装 + `pg_ident` 只映射 `ai-mem`),**本轮结构上跑不了**,
  已改的两条恒真断言也只过了 `py_compile`,**没有实跑过** —— 如实记着,不当作验过;
· `sensitivity.is_confidential()` 无调用点(`classify()` 取代了它)—— 留着是个假可用入口,未删;
· 静默吞异常 70 处 / 过期文案嫌疑若干:逐条看过,余下的都能说出为什么正当,未改;
· 上一轮的未竟项仍然挂着:两台机器实机同步验证、家庭→个人降级中枢那份不删(待裁)、
  冲突提示界面未显示、D87 按需装载(S16)。

---

## 2026-08-06 · 四条执行线并行一天 + 收束(`ac886fe` … `6319b14`)· D89–D91

> 用户裁定开四条并行车道(按 D82 的 worktree 分工),协调层负责派工、串行点与并入。
> 车道:**主线**(P4 / S16-b)· **identity 证书生命周期** · **待决 6/7 + 方向 B** · **记忆套件复活**。
> 当日晚收束回一条,四棵 worktree 全部移除。

### [★★★ 本轮最该记的结构性发现] 缝隙成了主要缺陷来源,而缺陷是并行造出来的

第二轮全项目审计的 **A 级 6 条里有 4 条是同一个形状:接线断了,而缝的两侧各自都绿。**
客户端与服务端 JSON 形状对不上 · 门禁不编译客户端 · identity 库写好了客户端不调 · 产物没出。

上一轮的信号是「记账带宽跟不上推进速度」;**这一轮不同 —— 代码量上去之后,断言全长在缝的两侧,
而没有任何断言跨在缝上。** 并行让缝变多,所以这一条不是巧合。

⇒ 落成纪律:**跨语言/跨进程的响应形状必须有一条成对断言** ——
服务端钉顶层键集合,客户端钉「拿这个形状能解析出目标字段」。已随 A1 一起落地。

### [★★★ 止血] A1:LeaseKeeper 从未持住过任何租约,而副作用全部交付了(`98f4184`)

提交标题曾写「客户端真的持租约了 —— 『有没有人在用』终于是个可以为假的判据」。**实测:一次都没持住过。**

```
gateway.py:977      {"status","lease","fence_token","generation"}   lease_id 在 lease 子对象里
LeaseKeeper.cs:155  在顶层找 lease_id → null → AcquireAsync 恒为 false
```

★ **它藏这么久的原因**:`fence_token` 恰好在顶层拿得到,只有 `LeaseId` 落空 ⇒
"解析成功了一半"看起来不像坏了。

**目标一条没达成,副作用一条不少**:每次点确定先卡 5 秒,再弹一句没有任何任务在跑的
「有任务正在跑」;对话框两个选项**一个是假的、一个是错的**(「等它跑完」永远不会跑完;
「优雅中断」让人以为打断了一个正在跑的推理)。
★ 上一轮刚做好的「拒绝信息含占用者」**第一次上场就在说假话**。

修法三处:解析取 `lease` 子对象 · **解析失败当场把租约还回去**(拿到了却记不住 ⇒
中枢那边 grant 是真成功的,稳态并存约 3 份没人认领的幽灵租约)· `blocking_leases()` 加
「租约必须点名了组件」——`components` 为空的 `client_session` 声明的是**「有人在」不是「有活在跑」**。

★ 连带修掉一条**把错误行为钉成正确**的用例:门禁里 `needs_user_choice` 那条把
`DRAIN_WINDOW_S` 临时改成 0.01,**把 5 秒代价整个盖住**,还把「`client_session` 挡住用户自己的变更」当成正确行为钉死了。

### [实作] A2:门禁从不编译客户端工程 —— 38 分钟编译断裂,而它连报两次绿(`e8795a7`)

`app` 是**唯一装到用户机器上**的工程,却是**唯一不被编译验证**的工程
(identity / lan-edge / transport 三个都是真编的)。事故已真实发生:`aa2b310` 调了两个类而
csproj 显式 Compile 名单里都没有 ⇒ 结构上编不过;窗口内 `20a06b0` 的提交信息写着「PASS=3126 FAIL=0」。

**时间戳是「产物旧」的代理,它代理不了「编不过」。** ⇒ `-Full` 加 `dotnet build` 客户端工程,
扫描根改由**解析 csproj 反向核对**,不再写死目录。

### [★★ 裁定 D91] 自动门禁只接纳【结构上不可能连库】的套件;「今天连不上」不算护栏

记忆库 14 个套件里 **4 个不需要活库**,而它们被 `10-core\memory` 的**目录级**规则整条连坐成
`manual` —— **201 条断言因此 8 天没人跑**。规则粒度是目录,事实粒度是文件。

裁定过程本身值得记,**因为发现被复核后收紧了而不是打折**:

1. 协调层初裁:「`test_gate` 的提级只在**只读护栏成立且有断言钉着护栏本身**时才允许」;
2. 记忆车道回查:**仓库里根本不存在只读跑测试的机制** —— 无 psycopg 桩、无 socket monkeypatch、
   无 `sys.modules` no-connect 断言。审计里那句「只读护栏切断…后跑」描述的是**审计当场搭的临时夹具**;
3. ⇒ 裁定理由**升级**:不是"护栏还没写好",是**没有护栏机制**;而 `test_gate` 每跑一次
   `autocommit` INSERT 进 `mem.gate_rejection`,`roles.sql:39` 对 `ai_mem_local`
   **REVOKE 了 DELETE** ⇒ **写进去清不掉**。今天安全的唯一理由是**身份被拒**。
   **不能为了验那堵墙而拆掉那堵墙** —— 那 459 条里有一部分测的就是这堵墙。

**落点:tainted 75 + route 55 + repo 25 = 155 进门禁;gate 46 挂起。**
分档形态取「**`Files` 白名单键**」而非「`Dir` 塞文件名」——
`-like '*<Dir>*'` 子串 + 首个匹配胜出是**静默失效的温床**(改了、看起来改了、其实没生效),
`Get-Rule` 改成 `Files` 优先 ⇒ **顺序在结构上不再承重**。
★ 顺序护栏**没删,改判据**:测 `Get-Rule` 的行为(造一个同时匹配两侧的路径,断言返回 `Files` 那条),
**测效果不测配置** —— 不能因为"现在不会发生了"就删护栏。

两条实测约束(不是偏好):`test_repo` 必须用 memory venv 的 python(系统 python 在 `import psycopg` 处即失败),
而它的真前置是**先在 `config/paths.toml` 登记 venv**(§11.1 路径契约,pre-commit 会当场抓);
`test_repo` 报 `1 SKIP` 而运行器**看不见 SKIP** ⇒ 那 1 条会静默消失,已扩汇总行解析并计进覆盖账。

★ 顺带记一个漂亮的误报:另一条车道的静态扫描把 `test_route.py` 判成"需后端",
**只因为该文件的【禁止字符串清单】里字面写着 `"requests."`** ——
扫描器撞在**描述它所守之物**的那段文本上,ASSERTION-PITFALLS 第 1 条的形状,这次撞的是扫描器。

### [★★★ 协调层自己的账] 同一形状连错三次:从一个数据点推出结构性结论

三次全部由协调层下达、由执行层或审计查出:

| # | 我说的 | 实际 | 我漏了数什么 |
|---|---|---|---|
| 1 | 「双击 `.cmd` = 普通用户,TPM 密钥会绑错完整性等级」 | `EnableLUA=0`,本机**不存在** Medium 上下文 | 没数运行上下文 |
| 2 | 「P3d 两道闸串联:闸一放行还会撞闸二」 | 两个函数**签名不相交**,且 `unseal_for_prompt` **生产调用点 = 0**,两道都是休眠脚手架 | **没数调用点** |
| 3 | 「实测支持 STATE ⇒ 改方案书 S6 的勾选」 | 那条"实测"只是 P3c 签字里的**副机 active 一条**,被用来支持一张约 25 项的清单 | **没数清单项** |

第 3 条的后果最重:执行层照做并**加写了一句「已按清单走完,全过」**,
而 `S6-acceptance-checklist.md` **打勾项 = 0**。那句话在此之前的任何文档里都不存在 ——
**不是对齐,是写出了比两份文档都强的新话**,正是 P1-A7「拒绝把今天的配置追认为当时的测量条件」要防的形状。

⇒ 三处已于 `0d511a5` 全部撤回并留痕(见下方口径)。**指令侧的根因写进了方案书那一格里,没有藏。**

### [★★ 一次被执行层挡回来的] 前提不过就停手,而且没去 `stash`

合并作业的前提检查第 3 条是「2 号分支已静止」。主线执行层查出该分支有
**已 `git add`、未 commit** 的 236 行(正是协调层让它写的收工声明),**判前提不过,一个字没动**。

★ 值得记的不是"它停了",是**它识别出下一步会想去 `stash` 并且没去** ——
ASSERTION-PITFALLS 第 2 条(破坏性 git 命令冲掉未提交的活,已踩 2 次)**这次真的挡了一次**。
若 stash 掉再被清理步骤的 `worktree remove` 一抹,那 236 行就没了。

### [★ 派工的账] 两次派重,都是协调层没划边界

· 待决 6/7 车道做完自己的活后**绕进了记忆套件**,与记忆车道**各写了一份可跑性包**(合计约 750 行);
· 更早一次,它还先撞过 `test_tainted` 提级那一格。

两边发现**互补而非重复**(一边量代价:那道墙给的不是读库权限而是 **PG 超级用户**;
一边量范围:4 个不连库、201 条被连坐),所以没浪费,但**这是运气不是设计**。
⇒ 纪律:派工时**必须写清这条车道在哪儿停**,不能只写它做什么。

### [★ D82 车道纪律的实际遵守情况] 五条里只有一条被完整遵守

`D 号草案写 D?、并入那刻才取号` ✔ 唯一完整遵守的 ·
`main 只接受合并不直接干活` ✘(直接提交 12 次)· `90-ops/** 归 ops 车道` ✘(ops 车道根本不存在,三条车道同时写它)·
`00-docs 除自己决议包外只读` △(破过两次,其中一次是协调层点名 —— **但那条例外没写进 D82**)·
`收工即合不攒` △。

★ 最该记的一处:`baf9c5c` 的提交信息里**专门演示了正确姿势**(「那是 core/identity 的地盘」),
**同一次提交却直接改了 `20-client-win/app/**`** —— **纪律被引用、被讲解、然后被绕过**。

### [口径]

全仓基线见 STATE(纪律:那个数只在 STATE 那一行出现)。本轮 **3201 → 3320**,
增量 = 记忆套件提级 80(route 55 + repo 25)+ A1 的跨语言成对断言 + A2 的元断言。
★ 协调层预算时只算了套件提级(算出 3281)、**漏了 A1/A2 新增的断言** —— 差的 39 是预算漏项,不是数字有问题。

**取号 D89 / D90 / D91。** 另有五份包**明确不取号**:AppContainer 可行性 · 方向 B 判据 ·
sink 轴清单 · 两份记忆套件可跑性 —— 它们是**建议、勘察与交接,不是裁定**;
`test_tainted` 提级那份原拟取 D91,**取消** —— 包自己第 3 行写着「性质:执行清单,不是决议」,
**给一份清单追认 D 号就是追认**,与 S6 硬件指纹「拒绝回填」同一条纪律。

**文档三处撤回(`0d511a5`)**:P3b S6 的 `[x]` 改回 `[~]` 并逐项写开背书了什么 ·
STATE「+288 已逐项对上」是编的(真话只有 `3126 + 75 = 3201`,**新增覆盖 0 条**;
`2913→3126` 那 +213 的归因**没做过,如实空着**)· D46 根因留痕更正
(`Policies\System` 键最后写入 **2025-09-08**,而 hub 是 **2026-07-29** 铸的 ——
UAC 早关了约 11 个月,那把 CA 是 **High 下铸的**;**D46 正文一字未改,历史不重写**)。

### [仍待裁 / 未做]

· ★ **`.dirty` 复发**:`dist/client/VERSION.txt` 版本戳 `20260806-1558+d7c04c9.dirty-…`。
  上一轮审计 C3 判「`.dirty` 消了但产物落后 31 个提交」,本轮落后修好了(基于 `d7c04c9`),
  **而 `.dirty` 回来了**。来源是工作树上两份未提交的**审计文档** —— 与产物无关的东西污染了产物身份;
· **`VERSION.txt` 的自检数 `PASS=852 / 848` 与 STATE 记的客户端 1893 对不上** ——
  可能量的不是同一个东西(看着像"两种安装位置各自的哨兵佐证"),**没去数,所以不下判断**;
· **A5 潜伏**:`TlsFailure.cs` / `CertLifecycle.cs` / `RenewDeviceCertIfDue` 在 WPF 客户端里**零调用点**,
  且 `TlsFailure.cs` 已被链进构建 ⇒ **随包发布的死代码**。**客户端一接上,A5 立刻从潜伏变 A 级**;
· **`test_gate` 46 条挂起**,解锁条件见 D91 §3.3(其中一条可能要动 P3d 车道);
  **B 档 269 条**仍无法跑 —— `ai-mem` 密码**无人持有**(`setup-accounts.ps1:12` 自己写的),
  三条可跑路径**每一条都要动生产配置**,而其中一部分断言**正是在验那个隔离本身**;
· **P3d 两道闸都是休眠脚手架** —— 别再按「两道闸拦着」写,真相是**它们守的那条路今天没有任何人走**,
  而 PLAN §4.6.3 那条上游强制(「出境 sink 的会话不挂载 `memory.search`」)**代码里完全不存在**;
· 审计 **B 级 16 条**大部分未动(GPU 面 holder 自报 · `/v1/session/end` 可点名释放别人的租约 ·
  同一客户端两套身份 · `permitted_on_demand` 缺主机闸 · 假防护扫描器仍看不见
  `strings.json`/`.ps1`/`.toml` · 24 条 lan-edge 断言门禁从来不跑且不在覆盖账里 · …);
· 待决 6/7 **仍待用户裁**;D81 待裁五条**仍待用户裁**。

---

## 2026-08-06 夜 · 第一批【垂直切片】车道(D92 之后)—— V2 GPU 面身份与租约收口 + S16-b 接动作(`dd06af3`)

> 本条只记 **V2 车道**;同夜并入的 V3(门禁与产物的诚实性)由它自己那条记。
> 车道形态是 D92 之后的**垂直切片**:一条功能从服务端到客户端到门禁的整条路径归同一条车道。

### [★★★ 为什么 B1 与 B3 必须在同一提交] 两边各自自洽,所以谁也不炸

开工前的实况:GPU 面 `holder` 来自 `body["holder"]`(客户端填 `Environment.MachineName`),
同步面 `device` 来自证书指纹经成员表反查(2026-08-05 审计 C1 已修)。
**两套身份各自自洽 —— 所以它们各自都不炸。**

而 `/v1/session/end` **恰好横跨两者**:它拿一个自报的 `device` 去逐条比 GPU 面的 `holder`。
⇒ 只改一边,这条路径**当场失配**:退出不再释放任何租约,而那条路径吞异常、外面看不出来。

★ 这就是 D92 说的「缝落在车道边界本身」的正面形态:**把缝收进一条车道里,由同一个人对两侧负责。**
按目录切**做不到**"同一提交"(服务端在 `10-core/gateway`,客户端在 `20-client-win/app`)。

### [实测] B1–B6 六条,逐条前后对照

复现脚本跑在同一份代码的改前 / 改后两次(脚本不进仓库,结论以断言形式落进 `test_gpu_*.py`)。

| # | 原来怎么复现 | 现在怎么不复现 |
|---|---|---|
| **B1** 限流桶 key 用自报 holder | 同一 holder 打 22 次 → 第 21 次 429;换成 `PC-A-0…24` 打 25 次 → **429 共 0 次(25/25 全过)**;租约 holder 回显 `'我是主机(其实不是)'` | 换名打 25 次 → **429 共 5 次**(桶 key 是服务端解析出来的);holder 回显服务端认定的那个 |
| **B2** 点名释放别人的租约 | PC-B 提交 `{"device":"PC-A"}` → `released_leases=2`,租约数 3→1,**受害者两份当场消失** | 受害者两份**原封不动**;响应带 `ignored_device` 说清自报值被忽略;解析不出身份 ⇒ 403,一条都不放 |
| **B3** 两套身份 | `_sync_device` 走成员表,GPU 面走 `body["holder"]` | `principal_device()` 成为唯一解析口,`_sync_device` 只剩一行转发,**同步面行为一字未变** |
| **B4** 续租吃变更配额 | 20 次续租后桶 `20/20`,点确定 → `denied_quota`(第 21 次判红) | 20 次续租后变更桶 `1/20`,点确定 → 过 |
| **B5** `lease_count` 不过滤过期 | ttl=0.05s 租约等 0.2 秒 → `leases=[] lease_count=1 idle_is_meaningful=True` | → `leases=[] lease_count=0 idle_is_meaningful=False` |
| **B6** 按需授权缺主机闸 | 副机 POST 一次 → 200,Broker 收到 `['llm.assistant.8b@16k']` | → **403 `denied_action` / dim=tool**,授权抵达不了 Broker |

★ **B1 的第二个后果比额度维更该记**:那个自报的名字会被印进客户端「正在跑:xxx」对话框
⇒ **占用者的名字由被中断方自己填**,而看对话框的人正要据此决定要不要打断。

### [★★★ ASSERTION-PITFALLS 第 9 条,又一次] B4 的断言当时是绿的,而它守的那句话是假的

原断言问的是「源码里有没有 `change_resident` 这个词」,而它想守的是
「续租**做不做得到**不吃变更配额」。归档确实归对了 —— 但 `lease` 与 `change_resident`
**共用同一个令牌桶、同一个上限**,所以它**恒绿而事实为假**。

⇒ 新判据是**行为**:把续租桶打满,再点一次确定,它必须还能过。
★ 归档那条**留着**但降级为辅助 —— 它仍拦得住"手滑改了档位",只是不再单独承担这句话的真伪。
★ 这条不是"又发现一个 bug",是**同一种提问方式**第 4 次咬人;文档里那句
「它每次都伪装成一个具体的小 bug」这次的伪装是"归档标签写对了"。

### [实作] S16-b 接动作 —— 结构上够不着 `committed`

· **意图即起**(D87①):`POST /v1/gpu/intent {alias}` —— 一条端点同时是"起"的触发点、
  "我在用"的心跳锚点、和 `model_ref` 租约的发放口。客户端触发点是聊天输入框的敲键
  (`ChatView` 一行,去抖 20s),★ **只点别名不点组件**。
· **transient 平面**:`request_on_demand` / `sweep_idle_transient` 的源码里**没有任何对 `_committed` 的写**
  (D90 裁定③的反向断言,逐字扫)。★ 并且**用 `apply_intended` 验证过那条正则真的会响** ——
  否则它就是个"永远不响的探测器",而那种探测器与没有探测器等价。
· **空闲即卸**三条硬条件:组件级空闲 ≥ 阈值 · `blocking_leases()` 为空 · 目标在 transient 平面。
  收割**搭在采样循环里**,不另起 task(模块头:不设收割线程 = 第二个写者)。
· **那次必须存在的授权**:组件面板新增一列「允许按需装载」,只有主机能写。
  ★ 界面上**不预先灰掉副机的那一列** —— 「我是不是主机」是个**代理指标**,
  而真正要问的是"这个操作做不做得成"(第 9 条)。让人点,失败时由中枢给出它**真的验过**的理由。

### [★★ 一处必须写下来的更正] `idle_seconds` 不能当放行条件 —— 恒真式换成了恒假式

D90 包 §4.4 建议的条件① 是「`idle ≥ N`」。而 `idle_seconds` 被客户端心跳
(`LeaseKeeper` 每 TTL/3 ≈ 30 秒续一次租)刷新 ⇒ **只要有任何一台客户端活着,它永远到不了 10 分钟。**

⇒ 拿它当条件,是把一个**恒真式**换成一个**恒假式**。两者都不是检测器,
而第一戒律禁的正是这个 —— 只是这一次它换了个方向,**看起来更安全**(永不卸载),
所以更不容易被发现。改看逐组件的 `transient_idle_s`。

★ D87⑧「计时器是主副机共享的一个」在这里的落点是**共享在中枢上**,
而不是"一个全局标量必须同时管所有组件"。

### [仍待裁 / 有意留空]

· **D87③「显存压力即让」没做** —— D90 明写它「若指让给别的程序,仍需单独裁」,
  而 D90 的未决项由用户保留。`admission_guard()` 照旧只报告降幅、不触发任何卸载,**并有断言钉住这一点**;
· **D90 未决 ②④ 已给出处置并落地,但裁定权仍在用户**(STATE 待决 9/10 两行):
  ② 新开 transient 平面 + I2 作用域收窄为 `actual − transient == committed`(**这是对不变式的修改**);
  ④ `AutoStartPreset` 撤掉(与 D87 裁定①正面矛盾);
· **新分出一条待决 11**:`AutoUnloadIdle` 该变成主机上的中枢设置,还是撤掉?
  它今天仍无读取方、仍置灰 —— 不能就这么点亮,因为计时器是主副机共享的(D87⑧);
· 连带记账:`ModelCatalog.Presets` **从此无读取方**,已在该文件写明为什么不删
  (D87⑤ 的落点是「模型选择策略」那片占位框,那件事还没做)。★ 写出来,免得它变成又一份随包发布的死代码(D92 点名的 A5 形状)。

### [★ D92 硬前置照做了,而且当天就被另一条车道咬住]

五条跨进程契约进 `CROSS_PROCESS_CONTRACTS`,客户端 `Selftest.cs` 以同名 `CONTRACT:` 标记配对,
元断言枚举全表、**缺配对即判红**;★ 读不到客户端源码**也判红** ——「查不了」不等于「没问题」。
写它时那五条先是红的,写完客户端那半边才绿。

★★ 同夜 V3 车道把这条元规则做成了全仓门禁(`90-ops/gate/check_contract_pairs.py`),
**合并当天就抓到第一条:本车道新加的 `POST /v1/gpu/intent` 没登记进他们那张表**,两张表双向咬住。
⇒ 现状 **27 条契约,已成对 4,欠 23** —— 一张**只许变短**的欠债表。

### [口径]

全仓基线见 STATE(纪律:那个数只在 STATE 那一行出现)。本车道两次实跑能对上的增量 **+150**:
`test_gpu_broker` 385→501 · `test_gpu_policy` 81→83 · `test_sync` 79→85 · 客户端 1901→1927。
★ 其中 B4 那条**不增加净覆盖**,它是把一条恒绿的假断言换成行为断言 —— 两类不许混记。

★ **客户端自检在 worktree 里跑不了**(`bin/` 不进 git,门禁如实报「没跑起来」)。
**手工跑过**本次源码:`dotnet build -c Release` 之后 `--selftest` → **PASS=1931 FAIL=0**;
合回 main 出包后门禁那条是 **1927**(两个数量的是同一份源码的两种产物形态,见 STATE 关于发布产物的那段)。

### [★ 越界报备] 四处,已在决议包 §7 逐条点名

`test_sync.py`(B3 必然让两条源码级断言变红,顺手把判据从"长什么样"改成"做不做得到")·
`ModelsView`/`AppSettings`/`ModelCatalog`(D90 未决④)· `ChatView.cs` **一行** `NoteIntent` ·
`Selftest.cs`(D92 要求的客户端那半边)。★ **`localai-client.csproj` 一个字没动**(没有新增源文件)。
