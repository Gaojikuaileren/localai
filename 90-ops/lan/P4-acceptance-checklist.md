# P4 实机验收清单 · 2026-08-09 版

> 写给**明天早上那趟**的人。★ 上一版清单写于**管理端还是个壳**的时候,**已经过期**;
> 本版按 2026-08-09 凌晨的实际状态重写(D114 之后:3100 行**真的搬进了管理端**)。
>
> ★★ P4 在方案书里**没有验收句** —— 这张表就是补那一句。它与 `S6-acceptance-checklist.md` 是同门:
> **每格写清「怎么做 / 应该看到什么 / 看到别的说明什么」,而"看到别的"那一列才是它值钱的地方。**
>
> ★★★ 一条贯穿全表的纪律:**看不到比看得到更该验。**
> 搬走了却还留着,和没搬一样 —— 而"还留着"在界面上长得和"搬对了"**一模一样**,
> 除非你专门去找它不在。

---

## 0. 开工前 —— 拷哪个目录、两边应该是什么版本

**★ 别早上现找。** 两台机器上应该出现的版本戳**是同一个**:

```
20260809-0113+045b5b8
```

| 机器 | 拷什么 | 拷到哪 | 备注 |
|---|---|---|---|
| **主机** | `dist\client-pack\` 全部 5 个文件 | 覆盖 `dist\client\` | ★ 本机已覆盖(2026-08-09 01:36,SHA 与 pack 逐字节一致) |
| **主机** | `dist\admin-pack\localai-admin.exe` | `dist\admin\`(**新建**) | ★ 见下方 §0.1 —— 这一步**今天还没人做过** |
| **副机** | `dist\client-pack\` **整个目录** | 副机上随便一个目录 | ★ 副机**不要**拷 `host\` 与 `admin\` |

**验版本戳**:两边都打开 `VERSION.txt`,第二行必须是上面那个戳。
> **看到别的说明什么**:副机戳比主机旧 ⇒ 你拷的是上一次的包,**这一趟测出来的东西不算数**。

### 0.1 ★★ 管理端的位置由代码决定,不由审美决定 —— 而这一格今天是**空的**

客户端判「这台是不是主机」的指向物,V14 裁定②起是**管理端程序装没装**。
`dist\admin-pack\` 是出包产物,**不是**客户端会去找的那个位置。

⇒ **主机上要把 `localai-admin.exe` 放到客户端 exe 旁边的 `..\admin\` 里**,即:

```
<某个目录>\client\localai-client.exe
<某个目录>\admin\localai-admin.exe      ← 要新建
<某个目录>\host\localai-lan-edge.exe    ← 已有
```

> **★ 这一步没做的话,后面 §2 全部会"看起来没搬成功"** ——
> 而真相是客户端根本没找到管理端。**先做这一步,再往下走。**
>
> ★★ 另有一处**已知过期文案**(不影响功能,但会误导人):
> `安装说明.txt` 里仍写着主机判据是 `..\host\localai-lan-edge.exe` ——
> 那是 V14 裁定②**之前**的写法。看到它别照做,以本节为准。

---

## 1. 管理端:三页**真的点开有东西**

> ★ V21 出包自检验过它们"接上了",但那是**自检**。
> **人亲手点一遍才算** —— 自检验的是"这一页能构造出来",不是"这一页对人有用"。

### 1.1 双击 `admin\localai-admin.exe`

- **怎么做**:双击。**不要**右键「以管理员身份运行」。
- **应该看到**:窗口打开,左边四个页签:**主机中枢 · 模型 · 记忆库 · 客户端与栈**。
- **看到别的说明什么**:
  - 提示要管理员权限 / 打不开设备密钥 ⇒ 你用提权方式启动了。**TPM/CNG 用户密钥绑定铸造时的完整性等级**,提权是另一个身份(D108 裁定④);
  - 窗口一闪就没 ⇒ 看 `%LOCALAPPDATA%\LocalAI\admin\`,并把现象记下来 —— **不要反复双击**。

### 1.2 「模型」页

- **怎么做**:点「模型」。
- **应该看到**:**真的列出模型**(不是「这一页没有接上」,也不是空白)。
- **看到别的说明什么**:
  - 显示「这一页没有接上」⇒ 那是管理端自己的占位文案,说明这一页的接线没接;
  - 空列表但没有报错 ⇒ 可能中枢没起。**先去「客户端与栈」看栈在不在跑**,别在这一页上猜。

### 1.3 「已配对设备」(在「主机中枢」页里)

- **怎么做**:点「主机中枢」。
- **应该看到**:**列出已配对的设备**(至少有副机那一台,状态 active)。
- **看到别的说明什么**:一台都没有 ⇒ 要么中枢没起,要么这一页读的是空库 ——
  **看副机能不能正常收发消息**就能分开这两种:能收发 ⇒ 是这一页的问题,不是配对的问题。

### 1.4 「记忆库」页

- **怎么做**:点「记忆库」。
- **应该看到**:记忆条目列得出来;**溯源信息还在**(来自哪个项目、覆盖几条会话、原文删没删)。
- **看到别的说明什么**:
  - ★ **溯源那几条不再是可点的链接** —— **这是有意的**(D114 裁定④):它读的是客户端独占的
    `chat.json` / `projects.json`,跨进程读会踩「每个 json 只有一个写者」那条纪律。
    **P3a 的硬线是「每条可溯源」,溯源仍然在;少掉的只是"一键跳过去"。别把这个记成回归。**
  - 条目一条都没有 ⇒ 记忆库连不上,那是另一条线(记忆套件 B 档),记下来别在这儿修。

---

## 2. ★★ 客户端里**应该已经没有**的东西(这一节比第 1 节更该验)

> **搬走了却还留着,和没搬一样。** 而"还留着"要专门去找才发现。

打开 `client\localai-client.exe`,逐项确认**看不到**:

| # | 应该**看不到**什么 | 在哪找 | 看到了说明什么 |
|---|---|---|---|
| ① | 左栏没有「**模型**」这一项 | 左侧导航下半区(系统项) | 还在 ⇒ 你跑的是**旧包**。先核 `VERSION.txt` |
| ② | 没有**组件勾选面板**(勾组件 / 三段预览 / 「确定」) | 原来在「模型」页里 | 还在 ⇒ 同上 |
| ③ | 「设备」里没有**主机侧管理**(批准/拒绝/吊销别的设备) | 设置 → 设备 | 还在 ⇒ 同上。★ **副机上也不该有** |
| ④ | 「存储」里没有**记忆库管理**那一半 | 设置 → 存储 | 还在 ⇒ 同上 |
| ⑤ | ★ **显存条应该还在**(这一条是"该看得到") | 主界面 | **不见了 ⇒ 搬多了** —— 显存是「现在什么在跑」的观察,两边都该看得见 |

★ 主机客户端设置里**应该有**一颗「**打开管理端面板**」按钮;**副机上不该有**。
> **看到别的说明什么**:主机上没有这颗按钮 ⇒ **§0.1 那一步没做**(客户端没找到管理端),回去做。
> 副机上有这颗按钮 ⇒ 副机上被拷进了 `admin\`,**把它删掉** —— 副机不该有那个程序。

★★ **主机上的客户端现在与副机上的长得完全一样**:也敲门、也显示六个词、也等对面批准 ——
只不过对面就在同一张桌子上(管理端)。**这是 D114 裁定③,不是缺陷。**
> ★ 一键自配对**没了**是**有意的**:旧的一键路径**明文跳过六词比对**。现在主机也逐字比对了。

---

## 3. 副机:**拉得到主机已有的共享会话**

> ★ 这是 D113(V15)那条修复的**唯一验收口径**。
> ★★ 而 V15 自己写着:**它没有在真机上复现过那句症状** —— 所以这一格是**第一次真验**。

- **怎么做**:
  1. 主机上开一个会话,把它**提升为共享**,说几句;
  2. 副机上**关掉客户端**,再打开(冷启动);
  3. 副机上看那个共享会话。
- **应该看到**:副机上**能看到整段对话**,不用等、不用手动刷新。
- **看到别的说明什么**(★ 这一列是本格的价值):
  - 副机说「已同步」而**一条都没有** ⇒ 那是 D113 §0 那三条路里的**第四格**。
    **现在它会自己说话**:界面上应有一行「**★ 还没跟中枢对齐(拉全量没成)——
    这台机器上现在可能少了对方的改动**」。**把那行原文抄下来**,并去主机
    `{state}\shared\withheld\` 看有没有被扣下的东西;
  - 副机要**手动进设置滑到底**才开始连 ⇒ 那是 08-07 实机记下的 ⚠,**这一趟要确认它还在不在**。

---

## 4. 五格 S6 —— 至今**打勾项 = 0**

> ★ 出处 `90-ops/lan/S6-acceptance-checklist.md`。**2026-08-06 撤回过一次过度声称**
> (有人把它从 `[ ]` 改成 `[x]` 并写「已按清单走完,全过」,而那句话在此之前任何文档里都不存在)。
> ⇒ **这五格今天仍然一格都没走过。** 能走几格走几格,**走不动的如实留空,不要顺手打勾。**

| # | 怎么做 | 应该看到什么 | 看到别的说明什么 |
|---|---|---|---|
| ① **拔 WAN 冷启动** | 拔掉光猫/路由的外网线,两台都重启 | 配对不失效、能正常收发 | 要重新配对 ⇒ 有东西依赖外网(**不该有**),记下来 |
| ② **换 DHCP 免重配** | 路由上给主机换一个 IP(或重启路由让它重发) | 副机能自己找回主机,**不必重新配对** | 要重配 ⇒ 地址被写死在某处;★ 记下**副机报的原话** |
| ③ **流式中吊销** | 副机正在接收流式回复时,在管理端**吊销**副机证书 | 流**当场断**,副机提示证书已吊销 | 流照常跑完 ⇒ 吊销只在**建连时**生效,那是一条真缺陷 |
| ④ **暴露面扫描** | 从副机 `Test-NetConnection <主机IP>` 扫 8080 / 8442 / 8443 | **只有 8443 通**;8080 与 8442 应当**只在回环上** | 8080 或 8442 从副机通得了 ⇒ **管理面/网关暴露到了网段上**,立即记下并停手 |
| ⑤ **两边重启** | 两台机器都完整重启,不做任何手工步骤 | 主机自动起栈、副机自动连上、共享数据还在 | 要手跑 `start-stack.ps1` ⇒ 自动起栈没生效;★ 主机上**不要**手跑它(会撞 8080,反而把已跑的收掉) |

---

## 5. 走完之后

- **每格如实标 ✅ / ❌ / ⚠(测不了)**,`⚠` 要写**为什么测不了**;
- ★ **不要为了让表好看而打勾** —— 2026-08-06 那次撤回就是这么来的;
- 有 ❌ 的格子,把**看到的原话**抄下来(不是"报错了",是那一行字);
- 回来后这张表连同结果一起进 `00-docs/decision-packets/`,由第 0 条车道并入中央三文档。

> ★ 本表由第 0 条车道写于 2026-08-09 凌晨,基线 `main@045b5b8` · 包 `20260809-0113+045b5b8`。
> ★★ 它**不声称**自己覆盖了 P4 的全部 —— 它覆盖的是「今天变了的那些」加「S6 那五格」。
> 方案书 §14 P4 的 13 格清单仍是权威。
